# CLAUDE.md — Sauce (workshop vault)

This is the **workshop vault** for the **Sauce** platform — the canonical home for the vault platform. It ships mechanisms (cross-cutting code) and blueprints (note-type bundles) that consumer vaults (accuris, headspace, ero) subscribe to. **No personal content lives here.**

## Project identity

- **Project name:** `sauce` (rebranded from `beacon` in v0.23.0)
- **GitHub remote:** `git@github-personal:willfell/sauce.git` (HTTPS: `https://github.com/willfell/sauce`) — personal account `willfellhoelter@gmail.com`
- **Local paths (this machine):**
  - **Workshop dev repo:** `/Users/willfellhoelter/projects/repos/sauce` (this directory — canonical platform source-of-truth; also self-installs as the workshop dogfood vault). All `cd` / harness invocations in docs assume this path.
  - **Consumer vaults:** `/Users/willfellhoelter/notes/sauce/barebones` (primary regression target), `/Users/willfellhoelter/notes/sauce/accuris-sauce`, `/Users/willfellhoelter/notes/sauce/ero-sauce`, `/Users/willfellhoelter/notes/sauce/headspace-sauce` — post-v0.28.0 migrated Sauce-shape vaults.
  - **Legacy source vaults (READ-ONLY per landmine #20):** `/Users/willfellhoelter/notes/accuris`, `/Users/willfellhoelter/notes/ero-sync/ero`, `/Users/willfellhoelter/notes/headspace` — only ever inputs to `sauce migrate --from <path>`; never written to.
  - **Predecessor-machine paths (historical):** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (workshop) + siblings `barebones-beacon-poc` / `accuris-beacon-poc`. Referenced by closed-cycle plan/result/handoff docs in `Docs/plans/` + `Docs/prompts/`; do NOT edit those for path-update churn.
- **Distribution model (post-v0.1.2 S2):** each consumer's `Docs/Meta/Templater/platformInstall.js` is a ~12-line content-static thin stub that dispatches at runtime to canonical `<workshop>/platform/install.js` via `require()`. Bootstrap-copy × 3 resync ritual retired (last stage was v0.1.2 S1). Updates reach consumers via `git pull` of the workshop repo + a fresh install run; the stub itself never changes (landmine #13).
- **Consumer-side namespace:** every blueprint materializes content under `beacon/<module_directory>/...` in the consumer vault (NOT at vault root). This keeps platform-managed content cleanly demarcated from the consumer's personal content.

## Vault identity check

Before any write, run `ls /Users/willfellhoelter/projects/repos/sauce` (or the equivalent workshop dev-repo path on your machine). Expected top-level: `CLAUDE.md`, `platform/`, `commands/`, `Docs/`, `.obsidian/`, `ranch/`, `package.json`, `install.sh`. If you see `Boards/`, `Timestamps/`, `Finance/`, `Resources/` at root, you're in a CONSUMER vault, NOT the workshop. STOP.

## Where to read for context

| If you're … | Read |
|---|---|
| Starting a fresh session | `Docs/prompts/SESSION-START.md` (canonical session-start recipe) |
| Starting any session | `Docs/Index.md` first |
| Onboarding a new consumer | `Docs/use.md` (section "Onboarding a new consumer vault") + `Docs/landmines.md` |
| Adding a new mechanism or blueprint | `Docs/how.md` + `Docs/landmines.md` + `Docs/plans/2026-05-02-vault-platform-design.md` |
| Working on v0.2.0 boards blueprint | `Docs/plans/2026-05-03-boards-blueprint-design.md` (full design + module-directory invariant rationale) |
| Debugging a failed install | `Docs/landmines.md` first, then `Docs/how.md` (installer section) |
| Catching up on closed cycles | `Docs/cycle-history.md` (archived per-cycle status snapshots) |
| Catching up on history | `Docs/plans/` in chronological order |
| Running a one-shot agent task | `Docs/prompts/` — each is copy-paste-ready for a fresh session |

## When using slash commands

<!-- @claude-surface:resolvers BEGIN -->
| Topic | Path | Slash command |
| --- | --- | --- |
| Audit | .claude/commands/audit.md | /audit |
| Bootstrap | .claude/commands/bootstrap.md | /bootstrap |
| Daily | spice/daily | /daily |
| Install | .claude/commands/install.md | /install |
| Meetings | spice/meetings | /meetings |
| Projects | spice/projects | /project |
| Scratch | spice/scratch | /scratch |
| Upgrade | .claude/commands/upgrade.md | /upgrade |
<!-- @claude-surface:resolvers END -->

## Non-negotiables

These are the **platform** non-negotiables. Eleven landmines in `Docs/landmines.md` (five customjs-guard + six platform) are derived rules; they apply to all platform code.

- **No personal content.** If you find yourself writing a daily note here, you're in the wrong vault.
- **All mechanisms and blueprints are versioned in `platform/manifest.json`.** Bump the version on any change to source files.
- **All file paths in mechanism / blueprint code use `{{template_variables}}`**, never hardcoded paths. The installer substitutes per-vault from each consumer's `platform-config.json`.
- **All platform metadata is JSON, not YAML.** Reason: Templater user scripts can't reach Obsidian's `parseYaml`. Files affected: `platform/manifest.json`, every `mechanisms/*/manifest.json`, every `blueprints/*/manifest.json`, every consumer's `platform-config.json` / `platform-subscription.json` / `platform-installed.json`, every `rules/*.json`.
- **The five customjs-guard landmines apply to every Dataview view** written in this workshop. See `Docs/landmines.md`.
- **The workshop dogfoods every release.** Before promoting a new mechanism / blueprint version to consumers, the workshop's own self-install must succeed. If workshop self-test fails, do not push the update.
- **Module-directory invariant under `spice/` namespace (decided 2026-05-03 originally as `beacon/`; refined 2026-05-04; codifies in v0.2.0; namespace renamed to `spice/` in v0.25.0).** Every blueprint owns ONE directory at `spice/<module_directory>/` in the consumer vault. All files the blueprint materializes — at install time OR at runtime via templates / commands / nav-button actions — live under that one directory. Cross-module data flows via wikilinks only. No module writes into another module's directory. Each blueprint's manifest declares `module_directory: "<name>"` (NEW required field, enforced by installer in v0.2.0). Examples: `spice/boards/`, `spice/to-do/`, `spice/trips/`, `spice/finance/`, `spice/projects/`, `spice/daily/`, `spice/journal/`, `spice/meetings/`. The `spice/` parent namespace cleanly demarcates platform-managed content from consumer personal content; consumers can have any other top-level structure they want without collision risk. Mechanisms are exempt — they're shared infrastructure, not modules; mechanisms continue to install under `ranch/...` per existing conventions. See `Docs/landmines.md` #11 + `Docs/plans/2026-05-03-boards-blueprint-design.md`. **v0.25.0 status:** Tree 2 rename `beacon/<module>/` → `spice/<module>/` SHIPPED; the invariant itself was unchanged across the rename. The single source-of-truth prefix lives at `install.js:250`.

## Git workflow

Sauce is a real published repo at `git@github-personal:willfell/sauce.git`. Commits and pushes to `main` are part of the active development flow — Claude commits at agreed checkpoints (typically per-stage close, occasionally per-task during implementer dispatches) and pushes to `origin/main` after each commit. **Single-branch workflow for now** — direct push to `main`, no feature branches, no pull-request review.

- **Commit at every stage close.** Bundle stage work into a single commit with a conventional-commits style message (e.g., `feat(installer,validator): v0.1.x patch S1 — ...`). Use HEREDOC for multiline messages.
- **Push to `origin/main` after each commit.** No staging, no PR. `git push origin main` is the canonical publish step.
- **Don't sign as Claude.** No `Co-authored-by: Claude` trailer; no AI-attribution. Author is the user.
- **Don't skip hooks** (`--no-verify`) unless the user explicitly requests it. If a pre-commit hook fails, fix the underlying issue and create a NEW commit.
- **Force-pushing + history rewrite** still require explicit approval (see "Ask before acting" below).
- **PR-based workflow returns** when collaborators or CI gates land. For now (single-developer single-machine), direct push is the workflow.

## Ask before acting

Stop and ask the user before any of:

- Modifying anything inside `.obsidian/` of any vault EXCEPT for the three installer-managed paths allowlisted in landmine #12:
  - `.obsidian/plugins/templater-obsidian/data.json`
  - `.obsidian/plugins/slash-commander/data.json`
  - `.obsidian/daily-notes.json` (added in v0.3.0)
  Edits to these three paths must follow landmine #12's safety mechanics (additive-merge-only, backup-on-edit, malformed-JSON guard, failure-loud history).
- Modifying the body of `Docs/Meta/Templater/platformInstall.js` in any consumer (the thin stub is content-static per landmine #13; lockstep change across all consumers + distribution-model bump required).
- Editing `platform/installer-stub.js` (changes the canonical stub body; cascades to every consumer in the next deployment).
- Creating a new top-level directory in any vault (in consumers, prefer `beacon/<module>/` for platform-managed content).
- Bumping `workshop_version` in `platform/manifest.json` (it's the global release marker).
- Editing or removing files inside `platform/mechanisms/*/` after a version has been promoted to a consumer.
- Editing a consumer's `platform-installed.json` by hand (it's auto-managed by the installer).
- **Force-pushing or rewriting history** on `origin/main` of the `sauce` remote (`git push --force`, `git reset --hard origin/...`, `git rebase -i` on already-pushed commits, etc.).
- **Sanctioned new top-level vault dirs:** `spice/<module>/` (blueprint content; landmine #11; renamed from `beacon/<module>/` in v0.25.0) AND `pantry/` (workshop clone for inside-vault layout; renamed from `Beacon/` in v0.23.0 to resolve macOS APFS case-collision) AND `ranch/` (runtime plumbing; renamed from `Docs/Meta/` in v0.24.0) AND `.claude/skills/<blueprint>/` (native Claude Code skill bodies materialized by `materializeSkills`; added in v0.30.0 for cowork; landmine #12 + #19). Other top-level dirs still require approval. The `pantry/` + `ranch/` + `spice/` + `.claude/skills/` namespace tetrad is the canonical layout post-v0.30.0.
- **Direct edits to canonical `.claude/commands/<x>.md` or `.claude/skills/<bp>/**/SKILL.md`** in any consumer vault — these are REVERTED on next install per landmine #22. Use `.claude/commands.local/` or `.claude/skills.local/` as the override seam instead. (`/audit` surfaces direct-canonical edits as `consumer_edit_at_risk` before work is lost.)
- **Editing CLAUDE.md outside marker-bounded regions** when the change is meant to be reflected via the claude_surface renderer. The renderer rewrites ONLY content between `<!-- @claude-surface:<table> BEGIN/END -->` marker pairs (directory-map / resolvers / skills-index per landmine #12 v0.32.0 amendment); outside-marker prose is preserved bit-for-bit and is hand-authored. Marker pairs in CLAUDE.md are managed; outside-marker prose is yours.

## Directory map

```
workshop/poc-vault/
├── CLAUDE.md                           This file.
├── Docs/                               All documentation. Start at Index.md.
│   ├── Index.md
│   ├── why.md                          Purpose and end goal.
│   ├── how.md                          Architecture and concepts.
│   ├── use.md                          Operational guide.
│   ├── landmines.md                    Traps to avoid.
│   ├── plans/                          Chronological design / implementation plans.
│   └── prompts/                        Copy-paste-ready agent prompts.
├── platform/                           CANONICAL PLATFORM SOURCE. Edit with care.
│   ├── manifest.json                   Workshop version + mechanism + blueprint catalogue.
│   ├── install.js                      The installer. Source-of-truth.
│   ├── mechanisms/                     Cross-cutting code (customjs-guard, validator, audit).
│   └── blueprints/                     Note-type bundles (none yet).
├── Docs/Meta/                          The workshop's own consumer-side state.
│   ├── platform-config.json            Self-install path map.
│   ├── platform-subscription.json      Self-subscription.
│   ├── platform-installed.json         Auto-managed install ledger.
│   ├── Templater/                      Materialized user scripts (auto-installed).
│   ├── Views/                          Materialized Dataview views (auto-installed).
│   ├── Scripts/                        CustomJS classes (none in workshop).
│   ├── Templates/                      Templater templates (incl. _install-platform).
│   └── rules/                          Rule registry (_global.json + per-blueprint).
├── commands/                           Master copy of cross-cutting slash commands.
└── .obsidian/                          Plugin configs.
```

## Directory map (managed by claude_surface[])

<!-- @claude-surface:directory-map BEGIN -->
| Path | Blueprint | Purpose |
| --- | --- | --- |
| spice/ | (platform) | Module-directory namespace for blueprints |
| ranch/ | (platform) | Runtime plumbing (config, scripts, templates, views) |
| .claude/commands/ | (platform) | Slash commands managed via claude_surface[] |
| .claude/skills/ | (platform) | Native Claude Code skill bodies |
<!-- @claude-surface:directory-map END -->

## Status (live)

For closed-cycle status snapshots (v0.1.0 through the most recent close), see `Docs/cycle-history.md`. Live pointers below summarize the current platform state; the cycle-narrative bullets that previously lived here have been archived.

- **Cycle order:** v0.1.0 ✅ → v0.1.1 ✅ → v0.1.x ✅ → v0.1.3 ✅ → v0.1.2 ✅ → v0.2.0 ✅ → v0.3.0 ✅ → v0.4.0 ✅ → v0.3.2 ✅ → v0.4.2 ✅ → v0.5.0 ✅ → v0.11.0 ✅ → v0.12.0 ✅ → v0.13.0 ✅ → v0.14.0 ✅ → v0.6.0 ✅ → v0.16.0 ✅ → v0.17.0 ✅ → v0.18.0 ✅ → v0.18.1 ✅ → v0.18.2 ✅ → v0.19.0 ✅ → v0.20.0 ✅ → v0.21.0 ✅ → v0.21.1 ✅ → v0.22.0 ✅ → v0.22.1 ✅ → v0.23.0 ✅ → v0.24.0 ✅ → v0.25.0 ✅ → v0.26.0 ✅ → v0.26.1 ✅ → v0.27.0 ✅ → v0.28.0 ✅ → v0.29.0 ✅ → v0.30.0 ⏭️ (folded into v0.31.0 per 2026-05-11 brainstorm #2; no standalone tag) → v0.31.0 ✅ → v0.32.0 ✅ → v0.33.0 ✅ → v0.33.1 ✅ → v0.36.0 ✅ → v0.36.1 ✅ → v0.37.0 ✅ → v0.38.0 ✅ → v0.38.1 ✅ → v0.40.0 ✅ → v0.41.0 ✅ → v0.41.5 ✅ → v0.42.0 ✅ → v0.43.0 ✅ → v0.44.0 ✅ → v0.45.0 ✅ → v0.46.0 ✅ → v0.46.1 ✅ → v0.46.2 ✅ → v0.47.0 ✅ → v0.48.0 ✅ (closed-with-known-issue 2026-05-15: BUG-5 listener never subscribed at headspace — Templater `startup_templates[]` registration didn't fire at boot at the consumer install; the listener class itself + data.json wiring + test harness coverage are structurally correct + reusable; v0.49.0 follow-up swaps registration to L2 customjs `startupScriptNames[]`) → v0.49.0 ✅ (CLOSED 2026-05-16: L2 listener registration via customjs `startupScriptNames[]` resolves v0.48.0's known issue; BUG-7 task-note Create Board gate widened; BUG-8 entity-create sentinel relocated from outside-block HTML to inside-block JS comment across 7 hubs) → v0.49.1 ✅ (PATCH 2026-05-16: Template, Kanban Card.md source-board detection + auto-promote fix — when kanban-plugin invokes Templater's create-new-note-from-template, Templater controls folder placement and may default to vault root; the template's detectPath logic now prefers `tp.config.active_file` (the source board) over `tp.config.target_file` (the new file), so the workstream picker + auto-promote `tp.file.move` both fire correctly regardless of where Templater initially placed the file). **Next candidates:** (1) **v0.49.0 (NEXT — design committed `9095f33`)**: L2 listener registration via customjs `startupScriptNames[]` + NEW `applyCustomJsStartupScripts` installer helper + 3-line init script; BUG-7 (Create Board AccentButton gate widened from task-hub-only to task-hub OR task-note); BUG-8 (entity-create sentinel relocated from outside-block HTML comment to inside-block JS comment across all 7 entity-create-using hub files; install.js `injectAccentButtonBlock` regex change); S0 hard-fail empirical verification gate at headspace before any code lands; (2) **Cycle 2 — `hub-nav@0.1.0`** per the strategy doc (cross-blueprint hub navigation mechanism); (3) **claude_surface[] wave 3** (boards / people / to-do / finance / journal / trips adopt `claude_surface[]` + `Docs/Meta/<X>-System.md` retirement + cowork backwards-compat shim removal); (4) **v0.44.1 deploy-hardening PATCH** bundling FLN-D1 (stale `workshop_relative_path` repair) + FLN-D3 (tap auto-bump PRs need auto-merge) + FLN-D4 (brew tarball missing `project/helpers/project-action-buttons.js`); (5) **audit YAML parser MINOR** (homegrown YAML parser misses inline-flow tag arrays → false-positive `required_tags.missing`; swap to `js-yaml`); (6) **remaining-blueprint-seed coverage** (boards / cowork / journal / people / to-do / trips / finance / scratch each get a 1-stage cycle adding their own `seed.json`/`seed.js`); (7) **v0.48.0 follow-on cleanup cycle** absorbing FLN-v48-1 through FLN-v48-11 (listener double-subscribe on customjs reload; openLinkText UI flicker; registration try/catch gap; e.message undefined for non-Error throws; Strategy 2 atlas read async bug; empty-workstreams Notice wording; double newName.trim; whitespace-only post-substitution; Templater data.json helper DRY opportunity; PTCL-2..5 missing comment; ICN-1 brace matcher naive); (8) **v0.47.0 follow-on cleanup cycle** absorbing remaining FIX-LATER-NOTEs FLN-d (AccentButton API widening to accept kebab names — drops the 17 callers' inline SVG literals), FLN-e (schema-driven runtime validator gate), FLN-g (Layer 2 type-field rule broadening). Deploy event narrative for v0.42.0: `Docs/plans/2026-05-14-v0.42.0-headspace-deploy-result.md`.
- **v0.49.1 summary (Kanban Card source-board detection fix, CLOSED 2026-05-16):** PATCH fixing the user-reported symptom where new task cards landed at vault root (`nono.md`) with no workstream picker after clicking "+ Add a card" on a project's kanban board. **Root cause:** the obsidian-kanban plugin's per-board `new-note-folder` setting is IGNORED when `new-note-template` is set — kanban delegates folder placement to Templater's `create-new-note-from-template` command, which uses Templater's default folder (vault root) rather than kanban's `new-note-folder`. The v0.48.0/v0.49.0 listener was never reached because kanban-plugin invokes Templater directly via its internal integration (the listener watches for vault.create on `^spice/projects/<slug>/tasks/<file>.md$`, which doesn't match a vault-root-created file). **Fix:** Template, Kanban Card.md's source-board detection now prefers `tp.config.active_file?.path` (Templater-recorded active file at invocation time = the kanban board the user clicked) over `tp.config.target_file?.path` (the new file at its initial location). Both the workstream picker (top `<%* %>` block) and the auto-promote `tp.file.move` (bottom `<%* %>` block) use this fixed detection. Auto-promote logic generalizes: derives `projectDir` from `tp.config.active_file?.path` rather than gating on `/tasks/` in the new file's path; moves the new file to `<projectDir>/tasks/<title>/<title>.md` regardless of where Templater placed it (vault root, project root, anywhere). project blueprint 1.9.0 → 1.9.1 PATCH; workshop_version 0.49.0 → 0.49.1; package.json + subscription lockstep. Workshop dogfood reinstall clean (1 content overwrite: Template, Kanban Card.md). Whole-suite preflight green; existing KC-1..3 source-string sub-asserts continue to pass (they check sentinel + create-new + empty-toast strings which all remain). Brew tag chain auto-bumped tap formula on push.
- **v0.49.0 summary (L2 listener registration + BUG-7 + BUG-8, CLOSED 2026-05-16):** MINOR cycle resolving v0.48.0's known-issue (listener never subscribed at headspace because Templater `startup_templates[]` registration didn't fire at boot). Three independent fixes designed as one cohesive cycle. **(1) L2 listener registration:** NEW `ProjectTaskCreateListenerInit` customjs class (5-LOC bootstrap that delegates to v0.48.0's `ProjectTaskCreateListener.init()`) registered via customjs plugin's `startupScriptNames[]` field. NEW manifest schema field `customjs_startup_scripts[]` (array of CustomJS class names; semantics differ from `templater_startup_templates[]` which stores paths). NEW installer helper `applyCustomJsStartupScripts` (~190 LOC mirrors v0.48.0's `applyTemplaterStartupTemplates`; targets `.obsidian/plugins/customjs/data.json` startupScriptNames[] field; two improvements over v0.48.0 helper: default-to-empty array if absent + whitespace-only post-substitution check addressing FLN-v48-8). Wired into installItem at install.js:993 immediately after `applyTemplaterStartupTemplates`. v0.48.0's Templater wiring + Template, Project Task Create Listener.md retained as belt-and-suspenders backstop (idempotent init via `_initialized` flag in the unchanged v0.48.0 listener class). **(2) BUG-7 Create Board gate widening:** `project-nav-buttons.js:597` 1-line edit — `if (ctx.context === "task-hub")` → `if (ctx.context === "task-hub" || ctx.context === "task-note")`. Open Board / Create Board AccentButton now renders inside task notes too. Preserves v0.46.0's design (the `_createTaskBoard` helper remained intact); the gate widening just exposes existing functionality on more contexts. **(3) BUG-8 sentinel relocation:** entity-create's load-bearing sentinel relocated from outside-block HTML comment `<!-- entity-create:<id> -->` (visible in source/edit mode of Obsidian) to inside-block JS comment `// entity-create:<id> — installer-managed; do not delete this comment` (invisible in BOTH source AND reading modes). Touches all 7 entity-create-using hub source files (Projects.md / Meeting Hub.md / People.md / Scratch Day Hub.md / Budgets.md / Paychecks.md / Invoices.md) — all turned out to be Flavor B (standalone outside-block marker; v0.46.0 installer was supposed to inject the dataviewjs block at install time). install.js `injectAccentButtonBlock` becomes verify-only per Choice A (hub source files own the AccentButton dataviewjs block; installer ZERO-MUTATION just confirms presence via inside-block sentinel regex; emits warning if missing). S4b mid-stage code-review fix: pushErr step disambiguated to `entity_create_block_error` (vs pushWarn's `entity_create_block_missing`). Workshop_version 0.48.0 → 0.49.0. Whole-suite +8 sub-asserts (PCSI-1..3 + KMC-1 + CSS-1..3 STUB + smoke-prj-customjs-startup); 15 harnesses unchanged. Workshop dogfood install clean via in-tree CLI; brew formula auto-bumped via release.yml chain on tag push; tap PR #27 merged 2026-05-16T06:47:35Z; brew upgrade 0.48.0 → 0.49.0 verified. Headspace reinstall via brew sauce v0.49.0 clean: 97 history entries, 8 content overwrites (all 7 hubs + project-nav-buttons BUG-7 fix); ranch/scripts/project/project-task-create-listener-init.js materialized (1048 bytes); customjs data.json startupScriptNames[] contains "ProjectTaskCreateListenerInit"; all 7 hubs swapped to inside-block sentinel; sauce audit --entity-create zero findings (5 aligned, 2 unverifiable pre-existing v0.46.x FLN). **Manual 8-checkpoint smoke at headspace deferred-to-day-of-use per autonomous-run directive** (CLI-level evidence comprehensive; risk profile very low since customjs's startupScriptNames is the SAME plugin hosting the listener class). S0 verification gate retrospect lesson: per autonomous-run, scriptable parts (data.json shape + landmine allowlist) retired headlessly; manual Cmd-R reload deferred to S9. Stage commits on `origin/main`: pre-S0 `9095f33` design + `0bb80ce` plan; S0 `1393cd3` verification log; S1 `b652cb6` init script + manifest; S2 `24320b5` installer helper; S3 `803159f` BUG-7 widening; S4 `dd688b3` injectAccentButtonBlock verify-only + S4b `a517891` pushErr step disambiguation; S5 `69bddf8` 7 hub edits + smoke regex; S6 `97a8ecb` test harness deltas; S7 `8c37ee3` catalogue + subscription lockstep + dogfood (FLN-v47-f harness-pin lockstep: SHC-S1 scratch 0.3.0 → 0.3.1 + EC-2 entity-create 0.2.1 → 0.3.0); S8 tag `v0.49.0` annotated + pushed; tap PR #27 merged + brew upgrade verified; S9 `980fa8c` headspace re-validation CLI-verified. NEW FIX-LATER-NOTEs (5): FLN-v49-1 schema comment for customjs_startup_scripts[] vs templater_startup_templates[] distinction; FLN-v49-2 files[] alignment cosmetic; FLN-v49-3 install.js line-number cross-references drift; FLN-v49-4 smoke regex theoretical 2-id match; FLN-v49-CSS CSS-1..3 STUBBED bodies (full adapter-stub harness extraction deferred). FLN-v48-8 (whitespace-only post-substitution) PARTIALLY addressed in v0.49.0's new helper; v0.48.0's two Templater helpers still have the gap. Design: `Docs/plans/2026-05-15-v0.49.0-design.md`. Plan: `Docs/plans/2026-05-15-v0.49.0-plan.md`. Result: `Docs/plans/2026-05-15-v0.49.0-result.md`. Headspace re-validation: `Docs/plans/2026-05-15-v0.49.0-headspace-revalidation.md`.
- **v0.48.0 summary (project kanban "+ Add a card" workstream fix + icons polish, CLOSED-WITH-KNOWN-ISSUE 2026-05-15):** MINOR cycle shipping the `ProjectTaskCreateListener` CustomJS class (~65 LOC) + Templater startup_templates wiring + NEW `applyTemplaterStartupTemplates` installer helper (~190 LOC mirroring `applyTemplaterFolderTemplates` at install.js:3727) intended to fix BUG-5 (kanban "+ Add a card" doesn't trigger Templater post-paste at projects). Bundled FLN-a/b/c icons polish: `icons@0.1.0 → 0.1.1` PATCH (resolve() captures `this._tier1` to local once at top — was triple-call via getter; `__cache → _cache` rename for sauce single-underscore convention; `this._tier1` refs in resolve() body 3 → 1) + `entity-create@0.2.0 → 0.2.1` PATCH (plusIcon SVG 16x16 → 15x15 for size consistency with Tier 1 vendored icons). Workshop preflight green across 15 harnesses; +10 sub-asserts (PTCL-1..5 listener path-regex + idempotency + command-id literal; KC-1..3 Kanban Card source-string asserts; ICN-1 icons regression guard threshold ≤3; smoke-prj-startup-tpl post-bootstrap data.json startup_templates[] verification). **Known issue (S10 escalation gate fired):** at headspace re-validation after brew upgrade + reinstall + Cmd-R reload, the listener never subscribed at boot. No `[ProjectTaskCreateListener] subscribed to vault.create` console log. Templater's `startup_templates[]` mechanism did not activate the listener at the consumer install. Net effect: BUG-5 fix is dormant at headspace; kanban "+ Add a card" still doesn't trigger workstream picker. The listener class + data.json wiring + test harness coverage are correct and reusable; only the registration vector failed. **Disposition:** close v0.48.0 as-is; v0.49.0 follow-up swaps registration to L2 customjs `startupScriptNames[]` (design committed `9095f33` immediately after v0.48.0 close); the listener class is unchanged in v0.49.0; v0.48.0's wiring stays as belt-and-suspenders backstop (idempotent init via `_initialized` flag). v0.48.0 also surfaced two pre-existing bugs unrelated to v0.48.0: BUG-7 (HIGH; `project-nav-buttons.js:597` gates Create Board AccentButton on task-hub context only — pre-existing since v0.39.0+) + BUG-8 (cosmetic; `<!-- entity-create:<id> -->` HTML markers visible in source/edit mode of all 7 entity-create-using hub files — added in v0.46.0). Both bundled into v0.49.0 scope. **S0 verification gate retrospect:** S0 had 4 sub-steps; Steps 1.1+1.2 retired headlessly; Steps 1.3+1.4 deferred per user defensive-hedge (option C: bump _postCreateDelayMs 50→200ms + skip live smoke). The 200ms hedge turned out to be irrelevant — the actual failure was earlier in the chain (Templater startup_templates didn't fire AT ALL). Lesson: a defensive-hedge on Mechanism B doesn't help if Mechanism A (registration itself) fails. v0.49.0 elevates S0 to a HARD-FAIL gate before any code lands. Stage commits on `origin/main`: S0 `43abfff` design+plan+handoff+verification log; S1 `9218ba7` ProjectTaskCreateListener customjs class; S2 `12ee47c` Kanban Card sentinel + create-new + empty-toast; S3 `0964247` startup template + project manifest delta; S4 `c3b8005` applyTemplaterStartupTemplates installer helper; S5 `3d1b244` test harness deltas (PTCL + KC + ICN + smoke); S6 `234fdd3` icons FLN-a/b polish; S7 `10098bc` entity-create FLN-c plusIcon 16→15 + EC-2 version pin lockstep; S8 `d7487f7` catalogue + subscription lockstep + workshop dogfood; tag `v0.48.0` annotated 2026-05-15; release.yml preflight → bump-tap chain auto-bumped Formula/sauce.rb in willfell/homebrew-sauce; tap PR #26 merged 2026-05-16T04:02:17Z; brew upgrade 0.47.0→0.48.0 verified at 22:03; v0.49.0 design `9095f33` committed post-S10. NEW FIX-LATER-NOTEs (11): FLN-v48-1 listener double-subscribe on customjs reload (4-line fix candidate); FLN-v48-2 openLinkText UI flicker (unavoidable given Templater command API); FLN-v48-3 no try/catch around `app.vault.on(...)` registration; FLN-v48-4 `e.message` undefined for non-Error throws; FLN-v48-5 Strategy 2 atlas read async bug at Template, Kanban Card.md:41-49 (pre-existing v0.46.0+; processFrontMatter callback never awaited); FLN-v48-6 empty-workstreams Notice wording; FLN-v48-7 double `newName.trim()` call; FLN-v48-8 whitespace-only post-substitution gap (also affects applyTemplaterFolderTemplates); FLN-v48-9 DRY opportunity (Templater data.json helpers share ~85% scaffolding); FLN-v48-10 PTCL-2..5 missing appOverride comment; FLN-v48-11 ICN-1 brace matcher naive (counts string-literal nested braces). Design: `Docs/plans/2026-05-15-v0.48.0-design.md`. Plan: `Docs/plans/2026-05-15-v0.48.0-plan.md`. Result: `Docs/plans/2026-05-15-v0.48.0-result.md`. v0.49.0 follow-up design: `Docs/plans/2026-05-15-v0.49.0-design.md`.
- **v0.47.0 summary (icons mechanism extraction + entity-create defect cleanup, CLOSED 2026-05-15):** MINOR cycle bundling four Phase 3 e2e defects from headspace into one fix pass + a Lego-principle-5 mechanism extraction. **NEW `icons@0.1.0`** Lucide kebab → SVG resolver: Tier 1 ~21 vendored SVGs (15 byte-for-byte from nav-buttons + 6 NEW entity-create plus-variants `users-plus` / `user-plus` / `folder-plus` / `wallet-plus` / `file-plus` / `pencil-plus`); Tier 2 Obsidian `setIcon()` runtime fallback. Single API `customJS.Icons.resolve(name) → string|null`. Two in-cycle consumers migrate (ratifies the ≥2-rule): **entity-create@0.1.1 → 0.2.0** MINOR — `render()` calls `customJS.Icons.resolve(spec.icon)`; JSON-schema tightens `body_template` + `extra_files[].body_template` to basename-only (`^[^/\\]+\.md$`). **nav-buttons@2.6.2 → 2.7.0** MINOR — inline ICONS const (~15 SVG entries) deleted; consumes Icons with letter-fallback helper for null. **AccentButton's raw-SVG contract retained unchanged** (deferred widening = future callsite-simplification cycle). Bug fixes: **BUG-1 HIGH** (kebab names rendered as literal text in entity-create AccentButton labels) — fixed by icons mechanism + entity-create migration. **BUG-2 HIGH** (people notes missing `type: "person"`) — fixed by one-line `frontmatter_template` patch in people@0.2.0 → 0.2.1 PATCH + forward-defense `_validateTypeFieldConvention` rule in install.js (validator@0.1.2 → 0.2.0 MINOR; rule lives in install.js because validator.js operates per-file at runtime, no Layer 2 surface; rule fires on zero current blueprints since none declare `when.frontmatter.type`). **BUG-3 CRITICAL** (project atlas body empty — registry stored bare basename; runtime `_readBody` resolved against vault root) — fixed by NEW `_resolveBodyTemplatePath` helper in install.js auto-prepending `{{templates_path}}/` on bare basenames before registry write; project@1.7.1 → 1.7.2 PATCH (no manifest value changes; registry output changes). **BUG-4 CRITICAL** (extra_files Map + Board bodies empty) — same fix path applied to `extra_files[].body_template`. Workshop_version 0.46.2 → 0.47.0. Whole-suite +20 sub-asserts (run-renderer +7 R-EC-ICON-1..7; run-entity-create +6 EC-33..38; run-helper-cases +6 TYPE-1/2/3 × 2 asserts each); 15 harnesses unchanged. Workshop dogfood install clean; `sauce audit --entity-create` zero findings. Stage commits on `origin/main`: S1 `560e4db` icons mechanism + catalogue/subscription; S2 `0acfdd4` entity-create render() + manifest; S3 `69b9dea` nav-buttons ICONS deletion + harness lockstep; S4 `2d8b724` R-EC-ICON-1..7 DOM-icon asserts; S5 `5399e70` `_resolveBodyTemplatePath` + JSON-schema + project bump + harness lockstep; S5b `f9e09bf` mid-stage docs correction (installed.json embed-site invariant + helper comment accuracy); S6 `3dc368d` EC-33..38 body_template asserts; S7 `300679c` type:person + Layer 2 rule; S8 `0558edb` TYPE-1/2/3; S9 `697ef73` catalogue + subscription lockstep + workshop dogfood materialization (12 files; NEW `ranch/scripts/icons/icons.js`); S10 tag `v0.47.0` annotated + pushed; release.yml preflight → bump-tap chain auto-bumped `Formula/sauce.rb`. Headspace re-validation at CLI/registry level: 7/7 entity-create entries materialized; project body_template + extra_files paths now resolve to real template files on disk; person entry has `type: "person"`; audit zero findings (5 aligned, 2 unverifiable [pre-existing v0.46.x FLN, not regression]). NEW FIX-LATER-NOTEs (7): FLN-a Icons `_tier1` getter triple-call per resolve; FLN-b `__cache` → `_cache` rename; FLN-c plusIcon 16px → 15px or Icons-resolved; FLN-d AccentButton API widening for kebab acceptance (17 callsites); FLN-e schema-driven runtime validator gate (today helper is sole enforcement); FLN-f plan-author miss: Icons-style extractions need ~1-3 harness-pin lockstep updates per consumer-migration stage; FLN-g Layer 2 rule broader interpretation gates on `required_frontmatter.type.equals` (would fire on scratch's existing fragments); FLN-h Obsidian-side click-through smoke deferred to day-of-use (CLI/registry verification proven); FLN-i [BUG-5 surfaced post-S11, queued for v0.48.0] project per-kanban task-creation doesn't prompt for workstream + atlas missing `+ Add Task`/`+ Add Task Board` helper buttons — pre-existing v0.46.0 design gap (`_createTaskNote`/`_createTaskBoard` stripped ~184 LOC; Kanban plugin's "+ Add a card" doesn't trigger Templater; project's manifest declares zero `templater_folder_templates[]`); v0.47.0 install did NOT cause this (project-nav-buttons.js source bytes unchanged; Templater data.json untouched); design options for v0.48.0: extend entity-create with `pick-from-frontmatter` prompt type / restore stripped helpers / register Templater folder template. Design: `Docs/plans/2026-05-15-v0.47.0-icons-mechanism-and-entity-create-cleanup-design.md`. Plan: `Docs/plans/2026-05-15-v0.47.0-icons-mechanism-and-entity-create-cleanup-plan.md`. Result: `Docs/plans/2026-05-15-v0.47.0-result.md`. Headspace re-validation: `Docs/plans/2026-05-15-v0.47.0-headspace-revalidation.md`.
- **v0.46.2 summary (FLN-f project-action-buttons cleanup, CLOSED 2026-05-15):** PATCH bundling the stale `helpers/project-action-buttons.js` reference removal from project's manifest (`customjs_classes[]` + `files[]` both stripped). Pre-existing v0.39.0 S7 leftover where the customjs class was declared but the source helper file never written. Surfaced during v0.46.1 e2e validation at headspace: project's install completed at workshop dogfood (workshop's `app.vault.getAbstractFileByPath` returns Notice but install continues) but at consumer vaults the missing-source skip cascaded into project's top-level version pin NOT updating in `installed.json` AND project's `new_entity_buttons[]` entry NOT materializing into `ranch/entity-create-registry.json` (only 6/7 entities registered). project@1.7.0 → 1.7.1 PATCH; `run-integration-smoke.js` updated to re-include project in subscribed list + bump `expectedEcIds` 6 → 7 + assertion `>= 6` → `>= 7`. Workshop_version 0.46.1 → 0.46.2. Tag `v0.46.2` annotated; release.yml preflight + bump-tap chain fires; tap PR #24 in `willfell/homebrew-sauce` merged. After brew upgrade + headspace reinstall: all 7 entries materialize cleanly (verified via `sauce audit --entity-create` zero findings + `cat ranch/entity-create-registry.json` shows 7 entries: meeting/person/project/budget/paycheck/invoice/scratch). Commit: `700958f`. Test gap surfaced for future cycle: workshop dogfood is not a faithful proxy for consumer install — file-source-missing errors degrade silently at workshop but block install completion at fresh consumer vaults. Brainstorm input for v0.46.3+.
- **v0.46.1 summary (FLN-a entity-create defensive patches, CLOSED 2026-05-15):** PATCH bundling 2 FLN-a items: (a) `_loadSpec` catch-block now emits `console.error` + 8s Notice ("entity-create: registry JSON parse error — check console") on malformed-JSON parse failure, distinguishing the parse-failure path from the missing-file path (which still returns null silently — correct posture for "no registry yet"). (b) `_promptText` optional-branch type-guard symmetry: line 174 was asymmetric vs the required-branch (line 170) — now both branches type-guard `raw` via `typeof raw === "string"` before calling `.trim()`. entity-create@0.1.0 → 0.1.1 PATCH. Lockstep bumps in `platform/manifest.json` mechanisms catalogue, `ranch/platform-subscription.json` self-pin, `run-entity-create.js` EC-2 assertion. workshop_version 0.46.0 → 0.46.1. Tag `v0.46.1` annotated; tap PR #23 merged. Commits: `f67111c` (mechanism patch) + `8a2c9ae` (release lockstep). FLN-e (Path C doc note in `Docs/use.md`) + FLN-g (manual orphan cleanup of `ranch/scripts/meetings/new-meeting-button.js` + 4 other orphans at headspace) shipped alongside. Commit `d835d3d` (use.md). Pre-Phase-3 prep at headspace also discovered FLN-D1 (consumer's `workshop_relative_path` pinned to brew install dir, `sauce link` doesn't override the installer body's workshop resolution) — known v0.44.x deploy-hardening gap, workaround was the v0.46.0 tap PR #22 manual merge + brew upgrade.
- **v0.46.0 summary (entity-create mechanism + 7-site New<X>Button big-bang migration, CLOSED 2026-05-15):** NEW mechanism `entity-create@0.1.0` extracted; 7 per-blueprint `New<X>Button` CustomJS helpers deleted and replaced by a single declarative `EntityCreate` class (~470 LOC). **Scope:** meetings@0.4.0→0.5.0, people@0.1.2→0.2.0 (catalogue drift reconcile), project@1.6.1→1.7.0, scratch@0.2.6→0.3.0, finance@0.2.10→0.3.0 (3 sub-flows: budget/paycheck/invoice). **Installer:** NEW `applyNewEntityButtons` + `resolveEntityCreateEntry` + `injectAccentButtonBlock` (~373 LOC) in install.js; idempotent marker-anchored AccentButton fence injection; `pruneEntityCreateRegistry` parallel to nav-buttons pruning; installer embeds `new_entity_buttons` field per-blueprint into `ranch/platform-installed.json` at install time. **Audit:** NEW `platform/audit/entity-create-walker.js` (3 severity rules: `manual_implementation_at_risk` HIGH / `escape_hatch_used` INFO / `dead_path` MEDIUM); `--entity-create` flag in cmd-audit.js. **Schema extensions in entity-create@0.1.0:** `extra_files[]`, `{{current_file.frontmatter.X}}` token, `month`/`number` prompt types, `inline_body` peer to `body_template`, `validate` predicates (gte:<key>/min:<n>/max:<n>/safe-filename), `\|number` substitution pipe, `derive` mini-DSL (slugify/lowercase/sanitize-filename). **Mid-cycle corrections:** (a) C1 critical: walker originally read `bp.path`/`bp.manifest` from installed.json — fields never written; fixed by embedding `new_entity_buttons` at install time (commit `4fd5fec`); (b) body-template gap: Templater `<%* %>` bodies + legacy `{{NAME}}/{{SLUG}}` tokens incompatible with entity-create substitution catalogue — Path A (project: token-translated) + Path C (meetings/people/scratch: drop body_template, use frontmatter_template + inline_body) design correction (commit `59219a1`); (c) target_path critical: all 7 manifests used source-relative `target_path`; installer treated as vault-relative → canonical fence never injected; fixed to `{{module_directory}}/<path>` / `{{templates_path}}/<path>` (commit `85d315c`). Cadence: subagent-driven (opus for S1/S2/S6/S10/S11/design corrections; sonnet for S3/S4/S5/S7/S8/S9/S12 mechanical stages); ~24 dispatches vs 36 estimate. Stage commits on origin/main: S1 `5e8f8af`+`f242523` entity-create mechanism; S2 `c4877a1`+`20e1970` installer applyNewEntityButtons; S3 `a449b9f`+`4fd5fec` validator+walker+C1 fix; S4 `8193ff8` meetings migrate; S5 `f586e06` people migrate; S6 `6efa21f` project migrate; body-template fix `59219a1`; S7 `34e25bd` scratch migrate; S8 `55380b3` finance budget; S9 `c561cf8` finance paycheck; S10 `d6b0da2` finance invoice + MINOR bump; S11 `646dafc` test harnesses (+63 sub-asserts: NEW run-entity-create.js 32 + helper-cases +13 + renderer +5 cases + audit +8 + smoke +5); target_path fix `85d315c`; S12 `f3acbcf` version lockstep + subscription reconcile 0.41.5→0.46.0 + dogfood reinstall + audit zero findings + tag v0.46.0. Whole-suite total **15 harnesses**; sub-asserts grew by ~+63 vs v0.45.0 baseline. Landmines UNCHANGED at 22. NEW FIX-LATER-NOTEs: (a) `_loadSpec` silent null for parse error vs missing-file; (b) 8 polish/M-items (YAML quoting, nested object emission, `_substitute` asymmetry, predicate splitter, plus-icon extract, unknown predicate passthrough, `_promptSelect` no options-guard, overlay duplication); (c) 6 S2 regex edges (stray-fence/CRLF/mid-line/whitespace + 6 M-items); (d) walker `staticPart` unverifiable case + dead fallback code + `--help` UX; (e) body-template Templater-flow user-doc gap + inline_body literal-path lock-in; (f) `project/helpers/project-action-buttons.js` pre-existing absent file; (g) orphan `ranch/scripts/meetings/new-meeting-button.js` pruning gap in installer; (h) proactive S7 sub-assert additions; (i) AU-EC emit 2 sub-asserts each vs plan's 1. Design: `Docs/plans/2026-05-14-v0.46.0-entity-create-design.md`. Plan: `Docs/plans/2026-05-14-v0.46.0-entity-create-plan.md`. Result: `Docs/plans/2026-05-14-v0.46.0-result.md`.
- **v0.45.0 summary (cowork self-contained + UX polish wave 2, CLOSED 2026-05-14):** `cowork@0.6.0 → 0.7.0` MINOR (nav-buttons unchanged at 2.6.2). Fixes six follow-on issues surfaced after v0.44.0 install in accuris-sauce. **Architectural change:** cowork is now self-contained — NEW path convention `spice/cowork/daily/YYYY/MM-MMMM/YYYY-MM-DD.md` mirrors daily-blueprint folder shape but lives inside cowork's own module directory (landmine #11 invariant); Daily Hub cards-helper retargets from `dv.pages('"spice/daily"')` → `dv.pages('"spice/cowork/daily"')`; NEW `CoworkDailyActions` CustomJS class + `Daily Note.md` Templater template wire the manual creation flow (orchestrators' downstream rewiring deferred to scheduling/aggregation cycle). **UX changes:** (a) CoworkHubNav rewrites from BeaconCards 4-card row → AccentButton centered flex row matching ScratchDayActions aesthetic; drops "you are here"; current-hub button OMITTED so each hub renders 3 buttons pointing at the others; (b) universal nav-pattern (SpaceNavButtons → --- → CoworkHubNav → --- → content) on every cowork content artifact (5 hubs + 3 note templates = 8 files); (c) Weekly/Monthly/Daily note templates compute friendly H1 titles via Templater (`Week 20 · May 11–17, 2026` / `May 2026` / `Thursday, May 14, 2026`); (d) Timeframes row on Cowork.md grows 5 → 6 cards adding Today: `Daily Hub | Today | Weekly Hub | This Week | Monthly Hub | This Month`; (e) `cowork-weekly-hub-cards.js` + `cowork-monthly-hub-cards.js` get `_friendlyWeekName` / `_friendlyMonthName` fallback methods for card titles when frontmatter `*_label` absent. **NEW manifest fields:** customjs_classes += `CoworkDailyActions`; depends_on += `accent-button >=0.1.0`; files[] += `content/Daily Note.md` + `helpers/cowork-daily-actions.js`; rule_fragments[] += `cowork-daily` fragment for `spice/cowork/daily/**/*.md`. Cadence: subagent-driven-development for S1–S9 (9 implementer dispatches); controller-direct for S10 (result doc + CLAUDE.md). Zero mid-cycle plan patches — the v0.44.0 plan-ordering lesson (catalogue lockstep BEFORE preflight gate) was correctly absorbed into S8. Whole-suite delta vs v0.44.0 baseline: run-cowork-smoke `149 → 179` (+30; assertCoworkV045Shape covers 8-file nav-pattern loop + Daily Note materialization + CoworkDailyActions + CoworkHubNav AccentButton swap + cards-helper retarget + Timeframes Today + manifest invariants); run-helper-cases `650 → 658` (+8; cowork-daily-actions lint + 2 regex pairs); run-audit `91 → 93` (+2; cowork-daily happy + failure cases); run-claude-surface `194 → 194` (CS-MIG stale labels bumped cowork@0.6.0 → 0.7.0); run-integration-smoke `23 → 24` (+1; Daily Note.md materialized post-install). Whole-suite **+41 sub-asserts** vs v0.44.0. Stage commits on origin/main: design `1402913` + plan `a21902a`; S1 `1500c40` CoworkHubNav AccentButton; S2 `55f3788` CoworkDailyActions; S3 `88328b9` daily-hub-cards retarget + weekly/monthly friendly titles; S4 `47e3ae7` Timeframes 5→6 cards; S5 `3fadfc6` Daily Note template + Weekly/Monthly Note H1 rewrites; S6 `529b5aa` universal nav-pattern on 5 hub content files; S7 `92294c8` cowork manifest 0.6.0 → 0.7.0; S8 `1540568` harness deltas + catalogue lockstep; S9 `a8c2044` workshop_version + tag v0.45.0. Tag `v0.45.0` annotated; release.yml preflight → bump-tap chain fires on tag push. NEW FIX-LATER-NOTEs: FLN-3 cowork orchestrators' SKILL.md bodies still reference daily-blueprint paths (scheduling cycle scope); FLN-4 AccentButton mobile layout edge cases on narrow viewports not manually smoke-tested; FLN-5 v0.44.0 `assertCoworkV044Shape` asserts neutralized in place (commented out stale "you are here" + BeaconCards layout asserts; superseded by v0.45.0 shape function). Design: `Docs/plans/2026-05-14-v0.45.0-cowork-self-contained-design.md`. Plan: `Docs/plans/2026-05-14-v0.45.0-cowork-self-contained-plan.md`. Result: `Docs/plans/2026-05-14-v0.45.0-result.md`.
- **v0.44.0 summary (cowork UX polish + character pass, CLOSED 2026-05-14):** `cowork@0.5.0 → 0.6.0` MINOR + `nav-buttons@2.6.1 → 2.6.2` PATCH. Fixes six concrete UX defects from the v0.43.0 accuris-sauce install: (a) cowork nav-bar button now renders a real briefcase Lucide icon (was "C" letter fallback because `users-round` wasn't in `space-nav-buttons.js`'s hardcoded ICONS map); (b) `Cowork.md` hub stripped of docs prose ("What is cowork?" abstract, full skills tables, context callout, getting-started block) — all four sections relocate verbatim to a NEW `About Cowork.md` reached via a single bottom card; (c) NEW `CoworkHubNav` CustomJS class renders a 4-card cross-hub navigation row (Cowork \| Daily Hub \| Weekly Hub \| Monthly Hub with "you are here" subtitle on the active card) at the top of all 4 cowork hubs, replacing the v0.4.0 markdown text-link row (`> [[Cowork\|◀ Cowork]] · …`); (d) BeaconCards API mismatch in `cowork-{daily,weekly,monthly}-hub-cards.js` fixed (`items/titleField/subtitleField/bodyField/linkField → pages/title/subtitle/target`); the wrong-API bug caused BeaconCards' `pages` default to `[]` so every month/year heading rendered the empty-state placeholder ("Nothing here yet."); (e) dead empty-state callouts referencing retired v0.4.0 nav-buttons removed (`"No daily/weekly/monthly notes yet · Click X in the nav row to create one"`); (f) `→` arrow glyphs dropped from the Timeframes row's "This Week →" / "This Month →" card labels per design D6 ("if the button is there, there is no need for the wording"). Stage cadence: subagent-driven-development for 10 stages, controller-direct for S11 (result + CLAUDE.md). Two real plan-baseline drifts caught + cleanly handled mid-cycle: (i) S1 found nav-buttons already at 2.6.1 (consumed by v0.40.1 scratch ICONS commit `6b274b7`); rolled forward to 2.6.2 same as every prior icon-add. (ii) S9 ran red because workshop catalogue + subscription still pinned old versions while cowork@0.6.0 depended on nav-buttons >=2.6.2; folded the catalogue + subscription lockstep into S9 so preflight could pass; S10 then handled only workshop_version + package.json + tag. Whole-suite delta vs v0.43.0 baseline: run-cowork-smoke `114 → 151` (+37); run-helper-cases `636 → 649` (+13); run-audit `89 → 91` (+2); run-claude-surface `192 → 194` (+2 visible; really 3 stale CS-MIG-1 asserts bumped to cowork@0.6.0 + 1 new "Cowork About" resolvers-row); run-integration-smoke `17 pass/5 fail → 23 pass/0 fail` (+1 new About-materialization + 5 pre-existing fixed). Whole-suite total **+55 sub-asserts** vs v0.43.0 — all load-bearing. NEW manifest fields: cowork `claude_surface[]` gains "Cowork About" claude_md_row; cowork `rule_fragments[]` gains `cowork-about` fragment for `spice/cowork/About Cowork.md`. New customjs class count for cowork: 4 → 5 (adds `CoworkHubNav`). New content files: `content/About Cowork.md`. New helper: `helpers/cowork-hub-nav.js`. Stage commits on `origin/main`: docs `2854677` (design) + `e023ac3` (plan); S1 `77ef814` briefcase ICON + nav-buttons@2.6.2; S2 `e112b27` CoworkHubNav helper; S3 `195d2ef` BeaconCards API fix + dead empty-state drop; S4 `8a124cd` → arrows dropped from Timeframes; S5 `187be04` Cowork.md restructure; S6 `30240c0` About Cowork.md; S7 `1d6dbe9` cowork manifest 0.5.0 → 0.6.0; S8 `9d78128` 3 sub-hub adopts CoworkHubNav; S9 `fdb36f7` harness deltas + catalogue/subscription lockstep; S10 `1c70c53` workshop_version + package.json. Tag `v0.44.0` annotated (`9f3eb86`); release.yml preflight → bump-tap chain fires on tag push. NEW FIX-LATER-NOTEs: FLN-1 sub-hub literal `ranch/views/customjs-guard` vs `{{views_path}}/customjs-guard` template-variable inconsistency (cosmetic — both substitute identically); FLN-5 plan ordering rule for mechanism-version-coupled blueprint cycles: place catalogue + subscription lockstep BEFORE the preflight stage; FLN-6 cowork-smoke per-case bodies carry 4–6 sub-asserts — estimate at sub-assert level for future plans; FLN-7 dogfood install left install-side-effect files committed alongside S11. Design: `Docs/plans/2026-05-14-v0.44.0-cowork-ux-polish-design.md`. Plan: `Docs/plans/2026-05-14-v0.44.0-cowork-ux-polish-plan.md`. Result: `Docs/plans/2026-05-14-v0.44.0-result.md`.
- **v0.43.0 summary (cowork nav consolidation, CLOSED 2026-05-14):** `cowork@0.4.0 → 0.5.0` MINOR. Collapsed the v0.42.0 trio of global cowork nav-buttons (cowork-hub + cowork-weekly-this + cowork-monthly-this) to one (cowork-hub only). The two retired open-or-create-this-period actions re-materialize inside `spice/cowork/Cowork.md` as 2 of 5 BeaconCards in the existing Timeframes section. New `CoworkTimeframeButtons` CustomJS class mirrors `space-nav-buttons.js:323-382` runTemplaterTemplate semantics for the 2 create-cards. Pure-additive otherwise — no path conventions changed. Workshop_version `0.42.0 → 0.43.0`. Tag `v0.43.0` annotated. Plan: `Docs/plans/2026-05-14-cowork-nav-consolidation-plan.md`. Design: `Docs/plans/2026-05-14-cowork-nav-consolidation-design.md`. Result: `Docs/plans/2026-05-14-cowork-nav-consolidation-result.md`.
- **v0.42.0 summary (cowork timeframes skeleton, CLOSED 2026-05-14):** `cowork@0.3.0 → 0.4.0` MINOR — pure-additive timeframe scaffolding pass. NEW 3 sub-hubs under `spice/cowork/`: `Daily Hub.md`, `Weekly Hub.md`, `Monthly Hub.md` — each rendered by a dedicated CustomJS helper (CoworkDailyHubCards / CoworkWeeklyHubCards / CoworkMonthlyHubCards) querying the relevant timeframe path, grouping by year/month, delegating layout to BeaconCards. NEW 2 note-shell families: weekly notes `spice/cowork/weekly/YYYY/YYYY-Www.md` + monthly notes `spice/cowork/monthly/YYYY/YYYY-MM.md`; templates `ranch/templates/Weekly Note.md` + `Monthly Note.md` both inherit `<!-- COWORK_CALLOUTS -->` anchor for future patching. NEW 4 prompt-file stubs `spice/cowork/prompts/{morning-briefing,eod-review,weekly-review,monthly-review}.md` — empty-bodied path-convention placeholders for the scheduling cycle. NEW 3 global nav-buttons: `cowork-hub` (openLink → `Cowork.md`), `cowork-weekly-this` (runTemplaterTemplate for this-week), `cowork-monthly-this` (runTemplaterTemplate for this-month); net global row: Daily · Cowork · This Week · This Month. NEW 2 slash commands: `/weekly` (open this-week or hub) + `/monthly` (open this-month or hub); `/cowork` body refreshed with new Timeframes section. NEW 1 sub-skill `cowork:scaffold-timeframes` — idempotent, creates this-week + this-month notes from templates if missing, verifies prompt-stub presence, returns JSON receipt; composed into `cowork:bootstrap-vault` as step 25 (prior step 25 renumbered to 26). Cowork.md master hub updated: scaffold-status dataviewjs callout + new Timeframes section (3-card BeaconCards row above Engagements). NEW 6 rule_fragments validating new artifacts (3 hub types, 2 note-shell types, 1 prompt-stub schema); cowork manifest `rule_fragments[]` 5 → 11 entries. FIX-LATER-NOTEs: (a) FLN-1 workshop subscription does NOT include cowork — harnesses cover it but no self-dogfood materialization; (b) FLN-2 project version skew ranch subscription pins `project@1.5.0` vs catalogue `project@1.6.1` — pre-existing, blocks `sauce reinstall --vault $PWD`; (c) FLN-3 manual Obsidian smoke skipped (autonomous mandate); (d) FLN-4 nav-button manifest defects (`template` → `template_source`, trailing-slash `folder_prefix`) caught only in S8 review + fixed in S8b `81bf07b` — cross-check shipped nav-button examples in future plans; (e) FLN-5 sub-assert overage (+31 helper-cases vs +18 target, +10 cowork-smoke vs +6 target — all passing, thorough implementer). Stage commits: S1 `f9d6bbb` content sources; S2 `f2c4815` 3 hub-card helpers; S3 `135857c` scaffold-timeframes sub-skill + bootstrap-vault composition; S4 `278ebeb` /weekly + /monthly + /cowork refresh; S5 `03e5100` Cowork.md scaffold-status + Timeframes; S6 `cd42fd8` manifest delta cowork@0.3.0→0.4.0; S7 `6b976de` 6 rule_fragments + run-audit.js +12; S8 `3317c33` renderer +3 cases; S8b `81bf07b` nav-buttons template field + folder_prefix fix; S9 `f9af9a9` whole-suite extensions; S10 `eb3d13d` catalogue bump; S11 `8f26349` workshop_version + package.json lockstep. Whole-suite delta vs v0.41.5: run-claude-surface.js `167→192` (+20 new CS-V040 + 5 stale fixed); run-helper-cases.js `604→635` (+31); run-renderer.js `36→39 cases` (+3 R-COWORK-*); run-audit.js `77→89` (+12); run-cowork-smoke.js `92→102` (+10); run-integration-smoke.js `17→20` (+3 hub materialization). Whole-suite total roughly **+82 sub-asserts net** vs v0.41.5. Tag `v0.42.0` annotated; release.yml preflight → bump-tap chain fires. Spec: `Docs/plans/2026-05-13-v0.42.0-cowork-timeframes-skeleton-design.md`. Plan: `Docs/plans/2026-05-13-v0.42.0-cowork-timeframes-skeleton-plan.md`. Result: `Docs/plans/2026-05-13-v0.42.0-result.md`.
- **v0.41.x summary (convenience cohesion + patch train, CLOSED 2026-05-13):** v0.41.0 MINOR + v0.41.1/2/3/4/5 PATCHes. Ships convenience@0.2.4 + applySnippets installer helper (helper count 13 → 14) + landmine #12 allowlist 17 → 18. Reader-view default (`defaultViewMode:"preview"` + `livePreview:true`), Cmd+T → daily-notes:goto-today, Cmd+E → markdown:toggle-preview, Cmd+Shift+T → obsidian-tasks-plugin:edit-task (modal), and `sauce-tasks-icons.css` snippet that swaps Tasks plugin emoji glyphs for Lucide SVGs on RENDERED tasks via `::before` mask-image. **OPEN RESEARCH ITEM (carried over):** Tasks plugin suggester popup emojis remain unresolved — the suggester DOM has no Tasks-specific class so CSS-only swap is fragile. User reports edit-mode "looks bad" but the regression is undiagnosed (could be Live Preview inherent cursor-line decoration vs. a real drift). See `Docs/plans/2026-05-13-tasks-icon-ux-research.md` for the full research spec covering: Track A CustomJS monkey-patch of `editorSuggest.renderSuggestion`, Track B custom webfont with unicode-range substitution, Track C upstream PR, Track D plugin replacement; plus the side-by-side diagnostic protocol for the edit-mode complaint. The Cmd+Shift+T modal IS an immediate emoji-free input path; user has not yet exercised it after the v0.41.5 deploy. v0.41.x trail: v0.41.0 `a51f7fb` + v0.41.1 `609316e` (project depends_on shave) + v0.41.2 `4f11a95` (taskFormat=dataview, reverted in 0.41.4) + v0.41.3 `6500d53` (Cmd+E + livePreview:true) + v0.41.4 `261cab2` (taskFormat back to emoji) + v0.41.5 `fd68cdc` (Cmd+Shift+T modal binding). Workshop_version 0.40.7 → 0.41.5; convenience 0.1.0 → 0.2.4; project 1.6.0 → 1.6.1. Whole-suite preflight green at each tag (604 helper-cases + 13 other harnesses unchanged). v0.39.0 cleanup remains deferred — see `Docs/plans/2026-05-13-v0.41.0-convenience-cohesion-design.md` "out-of-scope" section.
- **v0.41.0 summary (convenience-cohesion, CLOSED 2026-05-13):** `convenience@0.1.0 → 0.2.0` MINOR absorbs three consumer-default behaviors. (a) `platform/manifest.json` `app_settings` += `defaultViewMode: "preview"` + `livePreview: false` — every newly opened note starts in reading view; `Cmd+E` flips per-note. Inherits `applyAppSettings` platform-as-overrider semantics. (b) `convenience` `hotkeys[]` +1 entry `Cmd+T → daily-notes:goto-today` — inherits new-tab semantics from already-shipped `alwaysOpenInNewTab: true`. (c) NEW `platform/mechanisms/convenience/assets/snippets/sauce-tasks-icons.css` (vendored verbatim from user's accuris vault) hides Tasks plugin emoji glyphs (📅 ⏫ 🔼 🔽 ⏳ 🛫 ✅ 🔁) via `font-size: 0` + injects monochrome Lucide SVGs via `::before` mask-image data URIs (currentColor-aware). NEW manifest schema fields `snippets[]` + `appearance.enabledCssSnippets[]`; registration piggybacks on existing `applyAppearance` helper (no new registration code). NEW installer helper `applySnippets` (helper count 13 → 14) — mirrors `applyVendoredThemes` posture: sha256-compare overwrite-with-backup, `.sauce-backup` suffix on overwrite, failure-loud history, never-throws. `entry.name` validated against `/^sauce-[A-Za-z0-9._-]+$/` regex. Wired into per-mechanism pipeline at `install.js:951` immediately after `applyVendoredThemes`. **v0.39.0 cleanup deferred:** initial design folded a project@1.6.0 manifest revert, but mid-cycle execution surfaced v0.39.0's true footprint (15+ commits + full products@0.1.0 + teams@0.1.0 blueprint scaffolds + 11 passing PROD/TEAM test cases); partial revert turned harness red on PROJ-2 + PROJ-3. Proper close belongs in a dedicated future cycle. Landmine #12 allowlist `17 → 18` paths adding `.obsidian/snippets/sauce-*.css` (sauce-namespaced platform-vendored carve-out); mechanic point #2 amended to narrow user-owned snippets to anything NOT matching `sauce-*.css`. Whole-suite preflight green: `run-helper-cases.js` 593 → 604 (+11 from SN1×4 + SN2×3 + SN3×4 cases targeting `applySnippets`); other 13 harnesses UNCHANGED. Versions: workshop_version `0.40.7 → 0.41.0` MINOR, `package.json 0.40.7 → 0.41.0`, `convenience 0.1.0 → 0.2.0` MINOR, subscription lockstep. Tag `v0.41.0` annotated; `.github/workflows/release.yml` preflight → bump-tap chain auto-bumps `Formula/sauce.rb` in `willfell/homebrew-sauce` on tag push. Stage commits on origin/main: docs `7cbdba8` (drop v0.39.0 cleanup from scope), S1 `a50ab0d` (app_settings += defaultViewMode/livePreview), S2 `9670a15` (convenience@0.2.0 + sauce-tasks-icons asset + version lockstep), S3 `b3307a3` (failing SN1/SN2/SN3 TDD), S4 `fc3f02a` (applySnippets implementation), S5 `38c9db8` (landmine #12 amendment). Cadence: subagent-driven for S1 (which surfaced the v0.39.0 footprint mismatch + triggered scope re-decision); controller-direct for S1-S5 after. Plan: `Docs/plans/2026-05-13-v0.41.0-convenience-cohesion-plan.md`. Design: `Docs/plans/2026-05-13-v0.41.0-convenience-cohesion-design.md`.
- **v0.38.1 summary (people α-seed, CLOSED 2026-05-13):** First application of the v0.38.0 deploy-confidence substrate to a non-pilot blueprint. NEW `platform/blueprints/people/seed/seed.json` ships 3 declarative entries (Alice / Bob / Carol — the names referenced as `[[Person]]` wikilinks by `meetings/seed/seed.json`), retiring v0.38.0's known-limitation dangling-wikilink warnings on the meetings α-seed. `run-integration-smoke.js` expectations grow `15 → 17 pass` (+2: `smoke-bp-people-exists`, `smoke-bp-people-count>=3`); seed total line moves `37 → 40 notes across 4 blueprints`. Whole-suite total **1218 sub-asserts + 36 renderer cases** across 14 harnesses (+2 vs v0.38.0's 1216, from smoke expectations). Versions: `workshop_version 0.38.0 → 0.38.1` PATCH, `package.json 0.38.0 → 0.38.1`, `people@0.1.1 → 0.1.2` (lockstep). Manifest `date: "2026-05-13"`. Mechanisms catalogue UNCHANGED (10). Blueprints catalogue UNCHANGED at 11 entries; `people` minor bump captures the seed addition. Stage commits on origin/main: T1 `721dfb1` people seed.json; T2 `6e7e314` smoke +2 expectations; T3 `7912ff9` version bump lockstep. Tag `v0.38.1` annotated; `.github/workflows/release.yml` preflight → bump-tap chain triggers on tag push to auto-bump `Formula/sauce.rb` in `willfell/homebrew-sauce` tap. Cadence: controller-direct (no subagents, no reviewers — test-pass output IS the spec gate, per handoff guidance for small substrate-application cycles). Cycle narrative archived to `Docs/cycle-history.md`. Handoff: `Docs/prompts/2026-05-13-v0.38.1-people-seed-handoff.md`. Result: `Docs/plans/2026-05-13-v0.38.1-result.md`. Proves the v0.38.0 substrate scales: adding a new blueprint seed is now ~3 file edits + a smoke-expectation row + a version-bump triple. No new design judgment required.
- **v0.38.0 summary (deploy-confidence substrate, CLOSED 2026-05-13):** Builds the substrate for closing the "is the brewed sauce-after-upgrade actually OK against representative content?" gap that the v0.36.x distribution cycle didn't address. NEW workshop-only library `platform/seeder/` (`helpers.js` `seeder.js` `declarative.js` `programmatic.js`) — per-blueprint seed contributions are materialized into a target vault as deterministic seeded content. NEW CLI verb `sauce seed [--vault] [--blueprint] [--anchor-date] [--reset] [--dry-run]` wired into the dispatcher's context-free branch (parallels `vault` / `reinstall` / `link`). NEW `vault_kind:"test"` field on `ranch/platform-config.json` — gates the destructive `--reset` codepath (`rm -rf <vault>/spice/<module_directory>/` for each target blueprint before re-seed). Default absent = production-marked; backwards-compatible. NEW pilot seed contributions: `platform/blueprints/project/seed/seed.json` (α declarative, 3 representative projects: Acme-Migration / North-Star-Refactor / Q1-2026-Audit), `platform/blueprints/daily/seed/seed.js` (β programmatic, 30 daily notes routed `YYYY/MM-MMMM/YYYY-MM-DD.md` with RNG-driven wakeup/mood variations — byte-stable across runs at the same anchor; smoke proved DETERMINISTIC sha256 `c4b3f69d...0461`), `platform/blueprints/meetings/seed/seed.json` (α declarative, 4 meetings, 2 with `[[Person]]` wikilinks — dangling-link warnings accepted per design). NEW `platform/test/run-integration-smoke.js` bar-ii harness (15 sub-asserts) — bootstraps a fresh tmp vault with `--non-interactive --no-register --mechanisms=all`, seeds it, audits clean, asserts per-blueprint file counts (3 / 30 / 4) + installed.json mechanism-version coherence + `--no-register` registry-untouched invariant. NEW `platform/test/run-seed.js` (31 sub-asserts: HEL-1..4 frontmatter quoting, MD-1..5 module-dir guard, LD-1..5 declarative loader, PR-1..3b programmatic loader, RNG-1..3 fixedRng determinism, DT-1..3 makeDateLike formatting, R-1..3b --reset gate via subprocess). NEW `scripts/check-version-sync.js` enforces `package.json version === workshop_version` lockstep; prepended as first step of `release:preflight`; catches the v0.38.0-design pkg=0.36.1 vs ws=0.37.0 drift. CI changes: `.github/workflows/ci.yml` converted single-job → `[macos-latest, ubuntu-latest]` `fail-fast: false` matrix; both runners run full `npm run release:preflight`; brew-install-smoke step gated `if: matrix.os == 'macos-latest'`. `.github/workflows/release.yml` gains a `preflight` job (macos-latest, full preflight); existing `bump-tap` job becomes `needs: preflight` — tag-pushed-against-red preflight no longer ships a formula bump. Docs: `Docs/use.md` gains "Releasing a version" gate-sequence + "Recommended GitHub branch protection" sections. Workshop self-install via `sauce reinstall --vault $PWD` clean (S12.2 substitute for test-vault dogfood — named `/Users/willfellhoelter/notes/sauce/test-vault` is on pre-v0.36.0 layout with stale `workshop_relative_path: "../beacon"` stub, blocking direct reinstall; migration is out-of-scope this cycle). Mechanisms catalogue UNCHANGED (10). Blueprints catalogue UNCHANGED (11). Whole-suite total **1216 sub-asserts + 36 renderer cases** across 14 harnesses (+46 vs v0.37.0's 1170: +31 from NEW run-seed.js + 15 from NEW run-integration-smoke.js + 3 from run-cli.js S-cases). Stage commits on origin/main: S2.1 `09b6629` cmd-seed.js dispatcher; S2.2 `077918a` verb-table wiring; S2.3 `1ddea03` +3 S-cases; S3.1 `09d98f1` programmatic.js; S3.2 `0405d8c` wire into seedVault; S3.3 `1a98f83` +10 PR/RNG/DT cases; S4.1 `d51e4ce` --reset path; S4.2 `3339bc3` +5 R-cases; S5.1 `237cf46` smoke harness skeleton; S6.2 `8f058e9` project seed; S7.2 `ef99eb8` daily seed; S8.2 `4c2d869` meetings seed; S8.5 `503d37f` wire smoke into preflight + bootstrap --mechanisms=all fix; S9.1 `743ba60` CI matrix; S10.1 `3a43654` check-version-sync.js; S10.2 `5a700a5` preflight gates on version-sync first; S10.3 `1d1e381` release.yml preflight job; S11.1 `4cef914` use.md release-process; S12.3 `9e1e6ad` workshop_version 0.37.0 → 0.38.0 + reconcile package.json drift. Pacing: subagent-driven-development for S2-S4 (full two-reviewer protocol for implementation tasks; combined-review shortcut for test-addition tasks), controller-direct-edits for S5-S12 (deterministic transcription + smoke verification). Cycle narrative archived to `Docs/cycle-history.md`. Spec: `Docs/plans/2026-05-12-deploy-confidence-substrate-design.md`. Plan: `Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md`. Handoffs: `Docs/prompts/2026-05-13-v0.38.0-execution-handoff.md` + `-handoff-2.md` (none written — single session closed the cycle). NEW FIX-LATER-NOTEs: (a) test-vault at `/Users/willfellhoelter/notes/sauce/test-vault` is on pre-v0.36.0 layout — needs a `sauce migrate-layout` pass before it can be the dogfood target the plan envisaged; one-off follow-up; (b) the `/Scripts/` gitignore rule masks the lowercase `scripts/check-version-sync.js` on case-insensitive APFS, requiring `git add -f` to track — consider replacing `/Scripts/` with a more precise pattern (e.g., explicit named files inside `Scripts/`) so `scripts/` no longer collides; (c) `cmd-seed.js`'s `--reset` path partially-deletes on mid-loop `readBlueprintModuleDir` throw — acceptable for test-vault destructive op but worth a transactional pre-check pass for robustness; (d) smoke `--mechanisms=all` was an inline correction to S5.1; the plan implied no flags needed but `installed.json mechanism-version coherence` requires the subscription to actually contain mechanisms.
- **v0.40.0 summary (scratch day-hub redesign, CLOSED 2026-05-13):** `scratch@0.1.0 → 0.2.0` MINOR. Nav-button now opens-or-creates today's day-hub `Scratch-Day-YYYY-MM-DD.md` instead of always creating a timestamped scratch. The day-hub is the per-day surface — embeds `ScratchNewButton` (NEW helper using `customJS.AccentButton`) for creating `Scratch-YYYY-MM-DD-HH-mm.md` siblings + `ScratchDayList` for browsing existing ones. Resolves three stacked bugs in v0.37.0's shipping shape: (a) wikilink collision between scratch day-index filename `<dddd>-YYYY-MM-DD.md` and daily-blueprint filename `dddd-YYYY-MM-DD.md` — scratch back-link `[[Tuesday-2026-05-12|Back to day]]` was resolving to the daily note; new filename `Scratch-Day-YYYY-MM-DD.md` is collision-free, (b) `ScratchHubCards` target path pointed at the colliding day-index and dead-ended; rewritten to target `Scratch-Day-*` path, (c) no "today's scratches" landing surface — day-hub now is that surface. **No nav-buttons mechanism delta** — initial design proposed `nav-buttons@2.6.0 → 2.7.0` MINOR with new `if_exists` field, but `space-nav-buttons.js:348-352` already does open-if-exists for `runTemplaterTemplate`; the behavior was dormant for scratch because the v0.1.0 nav-button's filename had an `HH-mm` suffix making each click unique. Dropping `HH-mm` from the nav-button's `filename_date_pattern` activates the dormant branch for free. Templates: `Scratch Day Hub.md` (NEW), `Scratch.md` (lazy-create `<%* %>` block retired, back-link wikilink rewritten), `Scratch Day.md` (RETIRED). Helpers: `ScratchNewButton` (NEW — uses `customJS.AccentButton.render` after S2 code review caught that the originally-coded `beacon-accent-button` CSS class didn't exist), `ScratchHubCards` (path rewrite). Manifest: customjs_classes +1, files +1 new −1 retired, depends_on +1 (`accent-button >=0.1.0`), nav_buttons[0] template_source + filename composition + id rename `scratch-new → scratch-day-hub`, rule_fragments +1 (`scratch-day-hub` validator). Leaf fragment's path_glob narrowed `Scratch-*.md → Scratch-2*.md` after S3 review caught `_compileGlob` doesn't exclude `-` in `*` — broader glob would fire false-positive violations on every day-hub note (works until year 3000). Harness deltas: helper-cases 590 → 593 (+SHC-S7 + retired-lazy + Scratch-Day back-link asserts in SHC-S2), renderer 36 cases UNCHANGED (R-SCRATCH-NEW renamed to R-SCRATCH-DAYHUB), audit 74 → 77 (+SA-S7 day-hub valid +SA-S8 wrong-type violation; SCRATCH_FRAGMENT path_glob narrowed in parallel with manifest). Whole-suite preflight green across 12 harnesses at S5 close. Cycle scoped scratch-only — initial S6 plan proposed absorbing in-flight v0.39.0 project@1.5.0 → 1.6.0 work, but project@1.6.0 manifest references a never-created `helpers/project-action-buttons.js` (v0.39.0 stopped at S6.7; planned S7 file-creation never executed). v0.39.0 closure deferred to a separate future cycle. Migration deferred — orphan `<dddd>-YYYY-MM-DD.md` files in existing vaults left for hand-cleanup; surface area per vault is tiny. Plan: `Docs/plans/2026-05-13-v0.40.0-scratch-day-hub-redesign-plan.md`. Design: `Docs/plans/2026-05-13-scratch-day-hub-redesign-design.md`.
- **v0.37.0 summary (scratch blueprint, CLOSED 2026-05-12):** NEW `scratch@0.1.0` blueprint — one-click date-stamped quick-capture note via `runTemplaterTemplate` nav-button with three-level `folder_date_pattern` `YYYY/MM-MMMM/YYYY-MM-DD` + HH-mm-suffixed filename. Per-day index file `<DayName>-YYYY-MM-DD.md` created lazily by the scratch template's `<%* %>` block via `tp.file.create_new` wrapped in try/catch for race-tolerance. Global hub at `spice/scratch/Scratch.md` renders day cards via NEW `ScratchHubCards` CustomJS class (BeaconCards row layout); per-day index renders scratch list via NEW `ScratchDayList`. Adopts `claude_surface[]` from day one — ships `/scratch` slash command + `new-scratch` SKILL.md + Scratch resolvers row via `skills_dir: ".claude/skills/scratch"` (no migration burden). Harness deltas: helper-cases 515 → 530 (+15 sub-asserts via SHC-S1..S6 — TW1 trailing-whitespace lint auto-discovered the three new scratch templates pre-S3), renderer 35 → 36 cases (+1 R-SCRATCH-NEW for three-level folder composition with timestamped filename), audit 68 → 74 (+6 SA-S1..S6 for scratch rule_fragment validation). Whole-suite total **1170 sub-asserts + 36 renderer cases** across 12 harnesses. Cycle slot: v0.37.0 clean separation (v0.34.0 wave-3 cohesion still pending). NEW FIX-LATER-NOTEs: (a) audit rule-runner `_compileGlob` is case-sensitive — `scope.path_glob: "Scratch-*.md"` doesn't match a `scratch-*.md` lowercase filename; case-insensitive matcher candidate for a future patch; (b) plan baselines for run-renderer/run-cli were stale (claimed 70/62 but actual 35/97 post-v0.36.1) — plan-baseline-drift after out-of-order cycle hops is a real risk worth a sanity-check step at S0; (c) hub `files[]` entry has no `skip_if_exists` flag — re-install overwrites a user-edited `spice/scratch/Scratch.md`; S0.3 verified the installer's current overwrite-on-install posture; (d) 4 pre-existing `stale_but_valid` findings on audit/install/upgrade/bootstrap command body version comments lag the registry — small housekeeping cycle candidate (separate from scratch); (e) plan body's claude_surface manifest snippet specified `platform-claude >=0.1.0` but task description specified `>=0.1.1` — tighter constraint used. Stage commits: S1.1-S1.7 `79c2e82..819a3cc` (7 commits — manifest, 3 templates, /scratch slash command, new-scratch SKILL.md, catalogue register), S2.1-S2.2 `c1fc783` + `edbddc1` (ScratchHubCards + ScratchDayList helpers), S3.1-S3.3 `4409030` + `bf08ce7` + `69440bb` (test harness deltas), S4.1-S4.3 `ab266db` + `a72c341` + `743c0ba` (workshop subscription + self-install + materialization housekeeping). Brew release: `.github/workflows/release.yml` triggered on tag push auto-bumps `Formula/sauce.rb` in `willfell/homebrew-sauce` tap. Cycle narrative archived to `Docs/cycle-history.md`. Plan: `Docs/plans/2026-05-12-v0.37.0-scratch-blueprint-plan.md`. Design: `Docs/plans/2026-05-12-scratch-blueprint-design.md`.
- **v0.36.1 summary (CLOSED 2026-05-12):** Patch bundles five fix-later items surfaced by the v0.36.0 end-of-cycle code review:
  (a) C1 test-harness pollution of `~/.sauce/vaults.json` via C11/C12/C13 — wrapped in `withTempHome` + end-of-suite invariant snapshot mtime+size of the production registry to fail-loud on any future drift,
  (b) C2 mkdtemp prefix renamed `beacon-cli-` → `sauce-cli-` in `run-cli.js` for clarity in registry log lines,
  (c) I3 `_auditStrict` no-op honesty pass — production stub returns `{ok:true, _stub:true}`, `--purge` gate relaxes to "step 1-7 success" as its precondition, log line clarifies that production audit integration is deferred (FIX-LATER-NOTE),
  (d) I4 `pruneMissing` now requires `ranch/platform-installed.json` OR `ranch/platform-config.json` for vault-ness — paths that exist but lack either marker get pruned; NEW R7 case + R5 updated to seed a marker,
  (e) I5 `cmd-link` refuses brew-installed pantry as target (would defeat `brew upgrade`); NEW L5 case via mockable `ctx._brewPrefix`; `link` verb now threads `opts` as ctx in `sauce-cli.js` dispatch so test hooks reach the command.
  Also: bootstrap-via-brew fix (commit `9048281`, post-v0.36.0 tag) ships in v0.36.1.
  `package.json` `name: beacon-workshop → sauce`, `version: 0.21.0 → 0.36.1`; `workshop_version` + subscription bumped to `0.36.1`. Manifest `date: "2026-05-12"`.
  Whole-suite preflight: run-cli.js 94 → 96 (+L5 +INV invariant), run-registry.js 16 → 19 (+R7 +R5 seed). Doctor 0-warn green after production registry cleanup.
- **v0.36.0 summary (sauce homebrew distribution — pantry out of vault, CLOSED 2026-05-13):** Cycle ships Homebrew-style distribution of the sauce workshop. NEW mechanisms/blueprints: none — this is a CLI + distribution cycle, no platform catalogue changes. NEW CLI verbs (6): `sauce vault add|list|remove` (per-machine registry maintenance), `sauce reinstall --all|--vault <path>` (re-run install across registered vaults), `sauce migrate-layout` (8-step state machine that moves a legacy `<vault>/pantry/` consumer onto the new layout), `sauce doctor` (platform health check — node version, libexec presence, registry shape, active-pantry symlink resolution), `sauce link <pantry>` + `sauce unlink` (toggle `~/.sauce/active-pantry` for dev-mode pointing at a working tree). NEW helper module `platform/cli/registry.js` (read/write `~/.sauce/vaults.json`; atomic writes with `.bak` sidecar; schema validation). NEW pure-function module `platform/cli/resolve-sauce-dir.js` (mirrors the bin shim's `active-pantry` → `libexec` resolution logic so it's unit-testable). NEW test harnesses `platform/test/run-registry.js` (16 sub-asserts — add/list/remove + atomic-write semantics + malformed-input guards) + `platform/test/run-migrate-layout.js` (18 sub-asserts — covers all 8 migration steps + rollback on failure). `run-cli.js` grows 62 → 93 sub-asserts (+31 from V1-V4 vault verb cases, I1-I3 reinstall cases, M1-M5 migrate-layout cases, D1-D3 doctor cases, L1-L4 link/unlink + status cases, SH1-SH3 shim-resolution cases, U1-U2 update-hint cases, IR1 install --register cases). Whole-suite total **1145 sub-asserts plus 70 renderer cases** (+65 vs v0.33.0's 1080: +16 registry + 18 migrate-layout + 31 run-cli). Architecture change: pantry/ no longer lives inside synced consumer vaults — it ships via `brew install` into `$(brew --prefix)/Cellar/sauce/<v>/libexec/` and the `sauce` bin shim resolves either to that libexec (default brew-installed pantry) or to `~/.sauce/active-pantry` if symlinked (dev mode via `sauce link`). NEW per-machine state files: `~/.sauce/vaults.json` (registry of installed consumers) + `~/.sauce/active-pantry` (optional symlink for dev mode). `sauce update` now prints a migration hint when a legacy-layout symptom is detected. `sauce bootstrap` auto-registers the vault in `~/.sauce/vaults.json` on success (`--no-register` opts out). Distribution: NEW sibling tap repo `willfell/homebrew-sauce` with `Formula/sauce.rb` (depends on node; `libexec.install Dir["*"]`; npm install --omit=dev; writes the active-pantry-aware bin shim). Fixes the dirty-tree + sync-corruption second-machine failures from pre-v0.36 layout where pantry/ commits inside the consumer vault would race with iCloud/Obsidian Sync. Workshop self-installs cleanly with `sauce reinstall --vault $PWD`. Cycle narrative archived to `Docs/cycle-history.md`. Plan: `Docs/plans/2026-05-13-v0.36.0-sauce-homebrew-distribution-plan.md`.
- **v0.33.0 summary (sauce claude cohesion — wave 2, CLOSED 2026-05-12):** Three highest-traffic blueprints adopt `claude_surface[]` — `project@1.4.1 → 1.5.0` MINOR (NEW `/project` slash command + `new-project` SKILL.md + Projects resolvers row + `skills_dir: ".claude/skills/project"`), `daily@0.2.6 → 0.3.0` MINOR (NEW `/daily` + `open-today` SKILL.md + Daily resolvers row), `meetings@0.3.1 → 0.4.0` MINOR (NEW `/meetings` + `new-meeting` SKILL.md + Meetings resolvers row). S1 foundation fixes folded in: workshop subscription drift reconciled (workshop_version 0.21.1 → 0.32.0 in subscription, validator 0.1.1 → 0.1.2, nav-buttons 2.5.3 → 2.6.0); CS-SUB-3 verb-existence guard generalized from "scan platform-claude bodies" to "walk every manifest with claude_surface[]" via two new helpers (`walkClaudeSurfaceBodies` + `scanBodyForBadVerbs`) + new CS-SUB-3b sentinel sub-case proving out-of-platform-claude drift is caught (drive-by drift fix in cowork's `bootstrap-vault/SKILL.md` — `sauce install cowork` → `sauce update` since `install` isn't a real CLI verb); workshop subscription gains project + daily + meetings entries for end-to-end dogfood. Whole-suite total **1080 sub-asserts plus 70 renderer cases** (helper-cases 508 → 511 from claude_surface install coverage). Three workshop-materialization housekeeping commits captured first-time blueprint subscription side-effects (ranch/templates/, ranch/scripts/<bp>/, ranch/rules/<bp>.json, ranch/nav-buttons-registry.json, spice/<module>/, `.obsidian/{hotkeys,daily-notes}.json`). Two new FIX-LATER-NOTEs: (a) `## Skills index` marker block stays at header-only because no blueprint emits `claude_md_row` entries with `"table": "skills-index"` — the renderer doesn't auto-populate from `kind: "skill"` entries; wave 3 candidate, (b) S2 implementer flagged a plan-author gap: per-blueprint stage S<N>.7 commit lists didn't anticipate first-time-subscription workshop materialization side-effects (templates, scripts, rules, hub note) — addressed inline with per-stage housekeeping commits. Wave 3 (v0.34.0) targets: remaining 6 blueprints + Meta-doc retirement + cowork shim removal. Cycle narrative archived to `Docs/cycle-history.md`. Plan: `Docs/plans/2026-05-12-v0.33.0-sauce-claude-cohesion-wave-2-plan.md`.
- **v0.32.0 summary (sauce claude cohesion — foundation wave 1, CLOSED 2026-05-12):** Wave 1 of the three-wave cohesion model ships the typed `claude_surface[]` manifest schema (4 kinds: command / skill / context_doc / claude_md_row) that replaces ad-hoc `files[]` / `skills[]` for Claude artifacts; the NEW `platform-claude@0.1.0` mechanism owning `/install`, `/upgrade`, `/bootstrap` lifecycle slash commands plus the CLAUDE.md marker renderer; an `audit@0.1.1 → 0.2.0` MINOR bump introducing `claude-surface-walker.js`, the `/audit` slash command, the `sauce audit --claude-surface` CLI flag, and a 4-level severity classifier (`dead_path` / `orphan` / `stale_but_valid` / `consumer_edit_at_risk` / `aligned`); a dogfood `cowork@0.2.1 → 0.3.0` MINOR migrating `skills[]` + `files[]` command entry → `claude_surface[]`; five new `install.js` steps (6b aggregate, 6c materialize, 6d CLAUDE.md regen, 6e prune, 6f `.local/` shadow, 6g registry write); workshop CLAUDE.md gains three marker pairs (directory-map / resolvers / skills-index); NEW harness `platform/test/run-claude-surface.js` (171 sub-asserts); NEW landmine #22 (`.local/` is the only consumer override seam); landmine #12 allowlist expanded `13 → 17 paths + CLAUDE.md marker regions`. Project / daily / meetings claude_surface[] adoption deferred to wave 2 (v0.33.0); boards / people / to-do / finance / journal / trips adoption + `Docs/Meta/<X>-System.md` retirement deferred to wave 3 (v0.34.0). Twelve FIX-LATER-NOTEs captured across S1-S7 (S1 Notice posture, S2 ranch/ allowlist breadth, S2 aggregator unsubstituted source, S2 unknown_kind silently skipped, S2 dest path normalization, S4 heading regen asymmetry, S4 row-cell escape, S4 multi-marker detection, S5 redundant null check / magic strings, S6 verb-existence guard coverage gap, S7 reserved `--strict` no-op, S7 mutually-exclusive severities undocumented) — non-blocking, future small-cycle fix candidates. Cycle narrative archived to `Docs/cycle-history.md`. Plan: `Docs/plans/2026-05-12-v0.32.0-sauce-claude-cohesion-plan.md`. Design: `Docs/plans/2026-05-12-sauce-claude-cohesion-design.md`.
- **Workshop version:** `0.49.1` (PATCH from 0.49.0 — Template, Kanban Card.md source-board detection fix. Surfaced 2026-05-16 from user-reported symptom: new task cards landing at vault root with no workstream picker. Root cause: obsidian-kanban's per-board `new-note-folder` is ignored when `new-note-template` is set; kanban delegates folder placement to Templater's default; Template, Kanban Card.md was reading the wrong path for source-board detection. Fix: prefer `tp.config.active_file` (the source board) over `tp.config.target_file` (the new file); auto-promote derives projectDir from source board and moves to `<projectDir>/tasks/<title>/<title>.md` regardless of where Templater placed the file). **Previous: `0.49.0`** (MINOR — L2 listener registration via customjs `startupScriptNames[]` + BUG-7 Create Board gate widening + BUG-8 entity-create sentinel relocation). **`0.48.0`** (MINOR — project kanban "+ Add a card" workstream fix + icons polish; closed-with-known-issue: BUG-5 fix was dormant at headspace because Templater startup_templates didn't fire; v0.49.0 resolved). **`0.47.0`** (MINOR icons mechanism extraction + 4 BUG fixes from headspace Phase 3 e2e validation). **`0.46.2`** (PATCH FLN-f: project@1.7.0→1.7.1 stale `helpers/project-action-buttons.js` files[]+customjs_classes[] reference removed; restores project entity-create entry materialization at consumer vaults; smoke harness `expectedEcIds` 6→7). **`0.46.1`** (PATCH FLN-a: entity-create@0.1.0→0.1.1 `_loadSpec` parse-error Notice + `_promptText` type-guard symmetry; Docs/use.md Path C documentation; manual orphan cleanup at headspace). **`0.46.0`** (MINOR from 0.45.0 — entity-create mechanism + 7-site New<X>Button big-bang migration: NEW entity-create@0.1.0; meetings/people/project/scratch/finance all migrated; NEW applyNewEntityButtons + injectAccentButtonBlock in install.js; NEW entity-create-walker.js 3-rule audit; NEW run-entity-create.js 32 sub-asserts; whole-suite +63 sub-asserts; brew formula auto-bumped via release.yml on tag push). **`0.45.0`** (MINOR — cowork self-contained: NEW spice/cowork/daily/ path family + CoworkDailyActions + CoworkHubNav AccentButton-row rewrite + universal nav-pattern 8 files + Timeframes 5→6 cards; cowork@0.6.0→0.7.0). `0.44.0` (MINOR — cowork UX polish + character pass: nav-bar briefcase icon + Cowork.md docs stripped to About Cowork.md + 4-card CoworkHubNav + BeaconCards API fix; cowork@0.5.0→0.6.0 + nav-buttons@2.6.1→2.6.2); `0.43.0` (MINOR from 0.42.0 — cowork nav consolidation, cowork@0.4.0→0.5.0); `0.42.0` (MINOR from 0.41.5 — cowork@0.3.0→0.4.0 timeframes skeleton). `0.41.5` patch train: `0.41.0` MINOR convenience@0.2.0 cohesion (reader-view default + Cmd+T + sauce-tasks-icons CSS + `applySnippets` helper); `0.41.1-0.41.5` PATCHes. `0.40.0` MINOR scratch day-hub redesign. **v0.40.1** (UNTRACKED in CLAUDE.md prior to v0.44.0): nav-buttons@2.6.0→2.6.1 PATCH adding scratch ICONS entry (commit `6b274b7`). v0.39.0 (projects-org-foundation, never tagged) remains in limbo pending a dedicated future cycle.
- **Mechanisms (12):** `customjs-guard@1.0.0`, `validator@0.2.0` (MINOR v0.47.0 S7 — NEW Layer 2 type-field convention rule `_validateTypeFieldConvention` in install.js — `validate.js` operates per-file at runtime, so manifest-level enforcement lives upstream; rule fires on zero current blueprints at v0.47.0 [forward-defense]; prior 0.1.2 PATCH), `audit@0.2.0` (MINOR v0.32.0 S7 — claude-surface-walker.js + /audit slash command + --claude-surface CLI flag + 4-level severity classifier), `nav-buttons@2.7.0` (MINOR v0.47.0 S3 — inline ICONS map [~15 vendored SVGs] deleted from space-nav-buttons.js; consumes `customJS.Icons.resolve` with letter `fallbackIcon` helper for null; depends_on += icons >=0.1.0; SVG vocabulary unchanged — Tier 1 in icons.js is byte-for-byte the same; prior 2.6.2 PATCH v0.44.0 S1 briefcase ICONS entry; PATCH v0.40.1 scratch ICONS entry), `cards@0.2.4`, `accent-button@0.1.0`, `icons@0.1.1` (PATCH v0.48.0 S6 — FLN-a: resolve() captures `this._tier1` to local once at top of function (was triple-call via getter); FLN-b: `__cache → _cache` rename for sauce single-underscore convention; behavior-preserving; this._tier1 refs in resolve() body 3 → 1; ICN-1 sub-assert in run-helper-cases.js gates against future regression with threshold ≤3; prior 0.1.0 NEW v0.47.0 S1 — Lucide kebab → SVG resolver; Tier 1 ~21 vendored SVGs [15 byte-for-byte from nav-buttons + 6 entity-create plus-variants users-plus/user-plus/folder-plus/wallet-plus/file-plus/pencil-plus]; Tier 2 Obsidian `setIcon()` runtime fallback; single API `customJS.Icons.resolve(name) → string|null`; consumed by entity-create + nav-buttons in-cycle satisfying Lego principle 5 ≥2-rule; AccentButton retained as raw-SVG consumer [API widening deferred]), `people-rendering@0.1.0`, `styling@0.1.2`, `convenience@0.2.4` (PATCH train v0.41.x — Cmd+T/Cmd+E/Cmd+Shift+T hotkeys + sauce-tasks-icons.css snippet + livePreview + `applySnippets` helper), `platform-claude@0.1.0` (NEW v0.32.0 S6 — /install /upgrade /bootstrap lifecycle slash commands + claude-md-renderer.js), `entity-create@0.3.0` (MINOR v0.49.0 S7 — sentinel format change: outside-block HTML comment `<!-- entity-create:<id> -->` → inside-block JS comment `// entity-create:<id>` inside a dataviewjs fence; invisible in BOTH source AND reading modes [fixes BUG-8]; `injectAccentButtonBlock` becomes verify-only per Choice A — hub source files own the AccentButton dataviewjs block; installer ZERO-MUTATION confirms presence via inside-block sentinel regex + emits warning if missing; S4b code-review fix: `pushErr` step disambiguated to `entity_create_block_error` vs pushWarn's `entity_create_block_missing`; prior 0.2.1 PATCH v0.48.0 S7 — FLN-c: plusIcon literal SVG width/height 16 → 15 for size consistency with all Tier 1 vendored icons in the icons mechanism (which are 15x15); behavior-preserving 1px change in the fallback path when Icons.resolve returns null for unrecognized kebab; prior 0.2.0 MINOR v0.47.0 S2/S5 — render() consumes `customJS.Icons.resolve(spec.icon)` falling back to inline plusIcon SVG [BUG-1 fix]; JSON-schema tightens `body_template` + `extra_files[].body_template` to basename-only `^[^/\\]+\.md$` pattern; `_resolveBodyTemplatePath` in install.js auto-prepends `{{templates_path}}/` on bare basenames at resolveEntityCreateEntry time [BUG-3+BUG-4 fix]; depends_on += icons >=0.1.0; prior 0.1.1 PATCH v0.46.1 + 0.1.0 NEW v0.46.0 — EntityCreate CustomJS class ~470 LOC; declarative per-blueprint new_entity_buttons[] spec; 7 extensions: extra_files[] / current_file.frontmatter token / month+number prompt types / inline_body / validate predicates / \|number pipe / derive mini-DSL; 3 audit rules via entity-create-walker.js; NEW applyNewEntityButtons + injectAccentButtonBlock + pruneEntityCreateRegistry in install.js).
- **Blueprints (11):** `boards@0.1.0`, `cowork@0.7.0` (MINOR v0.45.0 — self-contained pass: NEW `spice/cowork/daily/YYYY/MM-MMMM/YYYY-MM-DD.md` path family + CoworkDailyActions helper + Daily Note Templater template; CoworkHubNav rewrites to AccentButton-row (matches scratch aesthetic), drops "you are here", OMITS current-hub button; universal SpaceNavButtons→CoworkHubNav nav-pattern on all 8 cowork content artifacts; Weekly/Monthly/Daily Note templates compute friendly H1 titles via Templater; Timeframes row 5→6 cards adding Today; cowork-daily-hub-cards retargets spice/daily→spice/cowork/daily; weekly+monthly hub-cards get friendly title fallback methods; NEW cowork-daily rule_fragment; depends_on += accent-button; prior 0.6.0 MINOR v0.44.0 — UX polish + character pass: strips docs prose from Cowork.md hub to new About Cowork.md, adds CoworkHubNav 4-card cross-hub row, fixes BeaconCards API mismatch in 3 hub-card helpers, drops dead empty-state callouts, drops → arrows from Timeframes labels, swaps nav-bar icon users-round → briefcase; prior 0.5.0 MINOR v0.43.0 — nav consolidation 3→1 global buttons + CoworkTimeframeButtons 5-card row; prior 0.4.0 MINOR v0.42.0 — Daily/Weekly/Monthly hub-and-spoke + 4 prompt stubs + 3 helpers + 6 rule_fragments + scaffold-timeframes sub-skill; prior 0.3.0 dogfood migration: skills[] + files[] command entry → claude_surface[]), `daily@0.3.0` (MINOR v0.33.0 S3 — adopts claude_surface[] + /daily + open-today SKILL.md), `journal@0.1.2`, `meetings@0.5.1` (PATCH v0.49.0 S5 — BUG-8 Meeting Hub.md sentinel relocated from outside-block HTML comment to inside-block JS comment within dataviewjs fence; prior 0.5.0 MINOR v0.46.0 — NewMeetingButton deleted; new_entity_buttons[] id=meeting with frontmatter_template + inline_body + safe-filename validate; Meeting Hub.md marker swap; prior 0.4.0 MINOR v0.33.0 S4 — adopts claude_surface[] + /meetings + new-meeting SKILL.md), `people@0.2.2` (PATCH v0.49.0 S5 — BUG-8 People.md sentinel relocated to inside-block JS comment; prior 0.2.1 PATCH v0.47.0 S7 — `type: "person"` added to new_entity_buttons[0].frontmatter_template as first key [BUG-2 fix]; prior 0.2.0 MINOR v0.46.0 — NewPersonButton deleted; new_entity_buttons[] id=person with safe-filename validate; People.md marker swap; catalogue drift reconciled from 0.1.1; prior PATCH v0.38.1 — α-seed: 3 declarative entries Alice / Bob / Carol), `project@1.9.0` (MINOR v0.49.0 S1 — NEW `ProjectTaskCreateListenerInit` customjs class registered in customjs plugin's `startupScriptNames[]` via NEW manifest field `customjs_startup_scripts[]`; replaces v0.48.0's Templater `startup_templates[]` registration mechanism (which failed silently at consumer vaults); listener class itself unchanged; v0.48.0's Templater wiring retained as belt-and-suspenders backstop (idempotent init via `_initialized` flag); BUG-7 fix: `project-nav-buttons.js:597` Create Board gate widened from task-hub-only to task-hub OR task-note context; customjs_classes count 7 → 8; files[] count 16 → 17; prior 1.8.0 MINOR v0.48.0 S3 — NEW `ProjectTaskCreateListener` customjs class subscribes once at vault load to vault.create for files matching `^spice/projects/<slug>/tasks/<file>.md$` and invokes Templater's replace-in-file-templater command [intended BUG-5 fix; v0.48.0 registration via Templater startup_templates failed silently at consumer vaults — v0.49.0 swapped to L2 customjs startupScriptNames]; NEW Templater startup template `Project Task Create Listener.md`; NEW manifest field `templater_startup_templates[]`; Template, Kanban Card.md enhanced with "+ Create new workstream" sentinel + empty-workstreams Notice; prior 1.7.2 PATCH v0.47.0 S5 — `_resolveBodyTemplatePath` auto-prepends `{{templates_path}}/` on bare body_template basenames fixing BUG-3/4 from headspace Phase 3; prior 1.7.1 PATCH v0.46.2 — FLN-f stale `helpers/project-action-buttons.js` references removed; prior 1.7.0 MINOR v0.46.0 — stripped _renderProjectsHub/_promptForProjectName/_createProject from ProjectNavButtons ~115 LOC; new_entity_buttons[] id=project with derive slug + extra_files[] for Map + Board; Projects.md marker swap; project templates token-translated to entity-create catalogue; prior 1.6.1 PATCH v0.41.x), `scratch@0.3.1` (PATCH v0.49.0 S5 — BUG-8 Scratch Day Hub.md sentinel relocated to inside-block JS comment; prior 0.3.0 MINOR v0.46.0 — ScratchDayActions trimmed + ScratchNewButton deleted; new_entity_buttons[] id=scratch with current_file.frontmatter.day-routed substitution + inline_body; prior 0.2.6 PATCH), `to-do@0.1.4`, `trips@0.1.7`, `finance@0.3.1` (PATCH v0.49.0 S5 — BUG-8 Budgets/Paychecks/Invoices.md sentinels relocated to inside-block JS comments; prior 0.3.0 MINOR v0.46.0 — 3 sub-flows migrated: NewBudgetButton→id=budget month-prompt+inline_body; NewPaycheckButton→id=paycheck date+number+validate gte:start_date/min:0+\|number pipe; NewInvoiceButton→id=invoice+extra_files[] for Time-Log+Board kanban subfolder; prior 0.2.10). **Wave 3 (v0.47.0+) targets:** boards / people / to-do / finance / journal / trips adopt claude_surface[] + `Docs/Meta/<X>-System.md` retirement (no-op for cowork, already retired); cowork backwards-compat shim removal.
- **Fifteen load-bearing harnesses (whole-suite GREEN as of v0.46.0 close):** `platform/test/run-entity-create.js` (32/0; NEW v0.46.0 — EC-1..32: mechanism dir + manifest + schema + class parse; substitution catalogue; derive DSL; validate predicates; month/number prompt types; _renderFrontmatter type preservation + \|number unquoted scalar; _joinDestination flat + date-routed; extra_files subfolder; _loadSpec + idempotency; installer validator missing-key + bad render_in; audit walker 3 severities via seeded fixtures), `platform/test/run-claude-surface.js` (194/0; UNCHANGED in v0.46.0), `platform/test/run-helper-cases.js` (671/0; +13 in v0.46.0 — entity-create class surface coverage + 2 proactive S7 additions), `platform/test/run-renderer.js` (79 cases exit 0; +5 cases in v0.46.0 — entity-create AccentButton fence injection patterns across hub types), `platform/test/run-bootstrap.js` (58/0; UNCHANGED), `platform/test/run-cli.js` (100/0; UNCHANGED in v0.46.0), `platform/test/run-install-sh.js` (OK; UNCHANGED), `platform/test/run-migrate.js` (104/0; UNCHANGED), `platform/test/run-migrate-layout.js` (18/0; UNCHANGED), `platform/test/run-registry.js` (18/0; UNCHANGED), `platform/test/run-doctor-self.js` (3 ok; UNCHANGED), `platform/test/run-audit.js` (101/0; +8 in v0.46.0 — AU-EC-1..8: manual_implementation_at_risk detection × 2 sub-asserts, escape_hatch_used × 2, dead_path × 2, format/summary footer × 2), `platform/test/run-cowork-smoke.js` (179/0; UNCHANGED in v0.46.0), `platform/test/run-seed.js` (31/0; UNCHANGED in v0.46.0), `platform/test/run-integration-smoke.js` (29/0; +5 in v0.46.0 — registry-presence, source materialized, marker injection at hub paths, idempotency end-to-end, no-orphan New*Button.js). Whole-suite total **~+63 sub-asserts** vs v0.45.0 baseline. Legacy `platform/test/run-install.js` superseded by run-helper-cases.js. Whole-suite green preserved across v0.21.0 → v0.46.0.
- **New manifest schema fields (v0.4.2 BREAKING + ADDITIVE):** `nav_buttons[].action` for `runTemplaterTemplate` is REPLACED with the split-field schema (BREAKING; v0.4.0 single-string `folder`/`filename` retired): `folder_prefix` (literal, substituted via substituteLenient; required, validated), `folder_date_pattern` (string passed verbatim to moment.format; empty string allowed = no date in folder), `filename_prefix` (literal), `filename_date_pattern` (string for moment.format), `filename_suffix` (literal). Renderer composes `<prefix><moment.format(date_pattern)><suffix>` for both folder + filename. NEW additive action type `nav_buttons[].action.type: "invoke_command"` (`{ command_id }`) — fires arbitrary Obsidian command via `app.commands.executeCommandById`; command_id preserved verbatim through `validateAndResolve` passthrough; runtime gates (missing command, missing command_id) emit Notice.
- **New installer mechanics (v0.4.2):** (1) S1 `validateAndResolve` rewrite — runTemplaterTemplate branch validates required `folder_prefix` (Notice + history warning + null return on absence); substitutes `folder_prefix` + `filename_prefix` + `filename_suffix` via `substituteLenient`; passes `folder_date_pattern` + `filename_date_pattern` verbatim. `substituteForMomentFormat` helper DELETED. (2) S3 `validateAndResolve` adds invoke_command branch (literal passthrough; command_id NOT substituted).
- **New installer mechanics (v0.5.0):** S2.3 `validateAndResolve` adds `openLink` branch — substitutes `action.target` via `substituteLenient` so `{{module_directory}}/Projects.md` resolves at install time. The renderer (space-nav-buttons.js line 248) had supported `openLink` dispatch since v2.0.0 but install.js never had a substitution branch (literal placeholders reached the registry). Closes the dead-code gap.
- **Landmines list:** **22 entries as of v0.32.0 close (v0.32.0 S10 ADDED #22 — `.local/` is the only consumer override seam: `.claude/commands.local/<x>.md` + `.claude/skills.local/<bp>/<skill>/SKILL.md` are the ONLY supported customization paths; direct edits to canonical `.claude/commands/<x>.md` / `.claude/skills/<bp>/**/SKILL.md` are REVERTED on next install via post-install step 6f overwrite; `/audit` surfaces direct-canonical edits as severity `consumer_edit_at_risk`. Landmine #12 allowlist expanded `13 → 17 paths + CLAUDE.md marker regions` adding `.claude/commands/**` + `.claude/skills/**` + `.claude/commands.local/**` + `.claude/skills.local/**` + `ranch/claude-surface-registry.json` plus a NEW marker-write carve-out: claude_surface renderer rewrites only the region between `<!-- @claude-surface:<table> BEGIN/END -->` markers in CLAUDE.md [three tables: directory-map / resolvers / skills-index]; outside-marker content preserved bit-for-bit. Helper count UNCHANGED at 13; stub md5 invariant UNCHANGED). 21 entries (v0.29.0 ADDED #21 — `sauce audit` is read-only against the audited vault; mirrors landmine #20 posture for `sauce migrate` source vault, generalized; carve-out: `--output-file <path>` is the only write site, gated by user-explicit flag; allowlist UNCHANGED at 13 paths; helper count UNCHANGED at 12; stub md5 invariant UNCHANGED). 20 entries in v0.28.0 (ADDED #20 — source vault is read-only during `sauce migrate`; future migrator code review must reject any `srcAbsPath`-rooted write; allowlist UNCHANGED at 13 paths; helper count UNCHANGED at 12; stub md5 invariant UNCHANGED). 19 entries UNCHANGED in v0.27.0 — purely additive cycle: NEW people-rendering@0.1.0 mechanism + NEW people@0.1.0 blueprint + cards@0.2.4 + nav-buttons@2.5.3 + meetings@0.3.0 pilot; allowlist UNCHANGED at 13 paths; helper count UNCHANGED at 12. v0.26.0 ADDED #19 — platform-managed dir names lowercase + OUT-of-scope exceptions enumerated explicitly: MM-MMMM date-routed folders, assets/themes/<ThemeName>/ vendored, user-facing note filenames, Docs/plans + Docs/prompts historical, pantry/, spice/. Codifies the v0.26.0 lowercase sweep + macOS APFS `git mv X X_tmp && git mv X_tmp x` recovery pattern. Helper count 10 → 11 with NEW `scaffoldFoundationalPluginData`; allowlist #12 UNCHANGED at 12 paths)**. **18 entries UNCHANGED in v0.24.0** (allowlist #12 expanded 11 → 12 paths [USER APPROVED] adding `.obsidian/plugins/customjs/data.json` for new `applyCustomJsSettings` helper; helper count 9 → 10; landmine #13 wording amended capturing new stub md5 `ea23aa812503bfca66359d3b2b239ba8` + Tree N rename history). **18 entries UNCHANGED in v0.23.0** (Sauce rebrand: #11 wording amended to forecast v0.25.0 rename to `spice/<module>/`; #18 amended to reference `pantry/` as the v0.23.0+ workshop clone dir name + Beacon/ as historical alias). v0.22.0 adds #18 — inside-vault `Beacon/` is git-managed, never hand-edit; mirrors #15 vendored-theme posture; recovery via `beacon update --force`. UNCHANGED in v0.21.1 — no new landmines added; landmine #12 expanded 9 → 11 paths [USER APPROVED] adding `.obsidian/plugins/dataview/data.json` + `.obsidian/hotkeys.json` for the convenience@0.1.0 mechanism; helper count 7 → 9 with `applyCommunityPluginData` + `applyHotkeys`). v0.21.0 added #17 — bootstrap network posture: failure-loud + idempotent skip-if-present + path-traversal validator + `.beacon-backup` on overwrite + GITHUB_TOKEN honored + redirect-following + GitHub-only host + no mid-fetch cleanup; codifies the bootstrap layer's network exposure as bounded + reviewable. v0.21.0 also amends #8 — install.js is desktop-only filesystem-only; bootstrap.js is the network gateway. Allowlist #12 expanded 6 → 9 paths [USER APPROVED] adding `.obsidian/plugins/<id>/{main.js,manifest.json,styles.css}` for vendored plugin files. v0.20.0 added #16 — in-cycle re-process bump rule; promotes the gotcha 9 5-data-point precedent across v0.6.0 / v0.17.0 / v0.18.0 / v0.18.1 / v0.19.0 to numbered landmine status; quotes `install.js:223` short-circuit literally; codifies 4-file lockstep edit rule for mechanism cycles. v0.19.0 adds #15 — vendored theme is mechanism-owned; never hand-edit `.obsidian/themes/<Name>/`; codifies the only overwrite-with-backup posture path in the .obsidian/ allowlist; customizations route through Style Settings JSON or .obsidian/snippets/; landmine #12 also expanded 3 → 6 paths + helper count 4 → 7. v0.20.0 also fixed pre-existing landmine #10 line-citation drift — `install.js:111` → `:223` + matched ordering with #16's literal quote). No new landmines in v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.18.0 / v0.18.1 / v0.18.2. v0.16.0 surfaced FIVE new platform values that may graduate to numbered gotchas in future cycles: (a) **YAML date auto-parsing** — frontmatter values matching YYYY-MM-DD or YYYY-MM auto-parse to Date|Luxon objects; quote on creation + tolerant parse on read; (b) **frontmatter parser variance for booleans** — inline-flow YAML `paid: true` may produce string `"true"`; centralize coercion; (c) **NBSP gotcha from manual frontmatter editing** — copy/paste through chat/markdown renderers introduces U+00A0; reinforces "no manual frontmatter" thesis (v0.17.0 widget priority); (d) **renderBadge() pattern** — status pills on entity NOTE pages via dataviewjs block embedded by NewXButton creation helpers; reusable across entity blueprints with status concepts; (e) **embed dedup via `dv.container.closest(".markdown-embed")`** — suppress duplicated nav-buttons when a note is embedded via `![[X]]` inside another note. v0.6.0 lessons preserved + reinforced (gotcha 9 reserved-headroom pattern strengthened to FIVE-data-points: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 all USED 0.Y.x headroom 4-6 times). v0.6.0 reinforced: gotcha 2 Cmd+R discipline (user's H/I failures were stale class registration; now codified as numbered smoke step for cycles introducing new CustomJS classes); gotcha 9 in-cycle re-process bump rule (FOUR in-cycle CFs trips 0.1.0 → 0.1.4; reserved 0.15.x headroom USED extensively); gotcha 11 dual-source-of-truth lockstep (per-blueprint version bumps need lockstep edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub — held across all 4 CFs); landmine #11 module-directory invariant (per-entity sub-folders like `<slug>/board/` live within the invariant, not as new sub-modules — same as project's `<slug>/tasks/<task>/` pattern). NEW v0.6.0 platform values codified: (a) **API-contract quoting** — existing mechanism API contracts must be QUOTED literally in design docs, not described abstractly (CF-1 root cause: TripsHubCards called `render(dv, items, opts)` but BeaconCards' API is `render(dv, opts)` with field-functions; design described "field-function API matching ProjectsHubCards precedent" but didn't quote the call shape); (b) **first-of-its-kind blueprint cycles draw 3-5 inline CFs** — design phase locks SHAPE, CFs refine FEEL; plan reserved Y.Z.x headroom for entity-ecosystem cycles; (c) **template-changing CFs cause migration friction** — existing entities retain old body content because templates are read at creation time, not retroactively applied; design templates as bare-as-possible from the start; (d) **per-entity sub-folder pattern** — `<slug>/board/<task>/` mirrors project's `<slug>/tasks/<task>/`; both stay within landmine #11's "one module_directory per blueprint" rule; (e) **Templater auto-promote pattern** — Trip Board Card's `tp.file.move` mirrors project Kanban Card; reusable for any future entity blueprint that needs per-task folders. v0.14.0 lessons preserved: helper-extraction-string-not-moment + regex-precedent-reuse + zero-config-default + TDD-first commit pattern.

## Skills index

<!-- @claude-surface:skills-index BEGIN -->
| Command | SKILL.md | Blueprint/Mechanism |
| --- | --- | --- |
<!-- @claude-surface:skills-index END -->

