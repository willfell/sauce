/**
 * PersonNavButtons — per-person context nav with embed-dedup.
 *
 * S1 SCAFFOLD ONLY — full implementation in v0.27.0 plan T2.4.
 */
class PersonNavButtons {
  async render(dv, opts) {
    throw new Error("PersonNavButtons.render: not yet implemented (v0.27.0 T2.4)");
  }
}
