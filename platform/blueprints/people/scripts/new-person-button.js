/**
 * NewPersonButton — AccentButton + overlay-dialog for creating a new person note.
 *
 * S1 SCAFFOLD ONLY — full implementation in v0.27.0 plan T2.3.
 */
class NewPersonButton {
  async render(dv, opts) {
    throw new Error("NewPersonButton.render: not yet implemented (v0.27.0 T2.3)");
  }
}
