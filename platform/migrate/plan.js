// platform/migrate/plan.js — v0.28.0 S1 SKELETON.
//
// emitPlan(planEntries, opts) — pretty-print stdout table of all entries
// + write migration-plan.json to opts.outDir (typically process.cwd()).
//
// Implementation lands in S2 T2.6.

exports.emitPlan = function(_planEntries, _opts) {
    throw new Error("NotImplemented: emitPlan (S1 skeleton)");
};
