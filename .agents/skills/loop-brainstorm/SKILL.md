---
name: loop-brainstorm
description: Turn an idea into a fully-formed epic proposal for the bound board through collaborative dialogue. Use when starting any creative loop work — a new feature, capability, or refactor theme headed for the board — or when the user says "let's design", "think through", "brainstorm", or brings a half-formed idea that should become an epic. Produces a written proposal; /loop:plan turns it into board schema.
---

# loop-brainstorm (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/brainstorm/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
