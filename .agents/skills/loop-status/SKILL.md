---
name: loop-status
description: Read-only glance at the bound delivery board. Use when asking "how's the board", "what's the status", "how many things need me", "is the loop working", "what happened while I was away", or for a phone-sized digest of exceptions/progress. Shows the exception count, no-action summary, active claim, recent releases, and the retroactive "since you last looked" section. Never writes anything except the digest's own last-seen marker.
---

# loop-status (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/status/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
