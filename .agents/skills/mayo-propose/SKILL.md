---
name: mayo-propose
description: Turn an idea into a board-bound epic proposal for the bound delivery board. Use when asked to propose, shape, or scope an epic for the board, when a half-formed idea should become sequenced slices, or before /mayo:plan needs something concrete to mint from. Produces a written epic proposal against a specific board — not a general design conversation. Output is a proposal document; /mayo:plan turns it into board schema.
---

# mayo-propose (generated router)

GENERATED by the mayo plugin — do not edit; regenerate via /mayo:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/propose/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
