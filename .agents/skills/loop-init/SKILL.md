---
name: loop-init
description: Bind the current repo to a vault/board for the meta-loop. Use when onboarding a repo onto the loop system, when another loop skill refuses with config_missing, or when asked to "bind this repo", "set up the loop here", "point this repo at a board", or to regenerate the Codex skill surface after a binding change.
---

# loop-init (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/init/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
