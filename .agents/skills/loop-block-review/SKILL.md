---
name: loop-block-review
description: Heal and report dangling depends_on rot on the bound board. Use when asking "why is this epic blocked", "what's silently blocking the board", "unblock the board", "clean up dead dependencies", or after a supersession leaves dead pointers. Auto-fixes provable danglers through the coordinator and escalates never-minted foundations.
---

# loop-block-review (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/block-review/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
