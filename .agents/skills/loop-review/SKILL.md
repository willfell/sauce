---
name: loop-review
description: The Director's retroactive-review surface for the bound delivery loop. Use when asking "what did the loop decide while I was away", "walk me through the digest", "review the self-ratified amendments", "why is something parked", or to work the few genuine escalations. Walks the retroactive digest, surfaces perimeter items and escalations for decisions, and can reject a self-ratified amendment into a corrective intake — never writes cards.
---

# loop-review (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/review/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
