---
name: loop-intake
description: Turn raw requirements, bugs, themes, or roadmap batches into evidence-backed board-ready epics and execution slices on the bound board. Use when asked to intake, plan, decompose, prioritize, roadmap, scout, or prepare work for the loop, including supersessions that carry findings forward. Never implements or claims work.
---

# loop-intake (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/intake/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
