---
name: loop-execute
description: Drive a freshly minted epic to done in THIS session — sub-agent per slice with the full quorum — instead of waiting for the loop to pick it up. Use when the Director says "execute it now", "drive this epic here", "don't wait for the loop", typically right after /loop:plan mints. Every gate, lens, and receipt of the unattended loop applies; nothing is weakened by being interactive.
---

# loop-execute (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/execute/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
