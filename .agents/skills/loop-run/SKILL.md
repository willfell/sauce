---
name: loop-run
description: One bounded autonomous turn of the delivery loop against the bound board — claim or resume through CI, release, and deployment, driven entirely by the coordinator. Use when asked to run, resume, inspect, dry-run, pause, or recover the loop for this repo, or to run the loop engine unattended. Deterministic scripts own operational state; model context is spent only on implementation, repair, and review.
---

# loop-run (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/run/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
