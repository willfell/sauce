---
name: card-intake
description: Deprecated alias for loop-intake. Turn raw requirements, bugs, themes, or roadmap batches into evidence-backed board-ready work. Use when asked to intake, plan, decompose, prioritize, roadmap, scout, or prepare board work — then follow .agents/skills/loop-intake/SKILL.md.
---

# card-intake (deprecated alias)

This skill's canonical body moved into the sauce loop plugin as `intake` — one source of truth shared by Claude (`/loop:intake`) and Codex. Follow `.agents/skills/loop-intake/SKILL.md` (a generated router to the canonical body).

The deterministic intake script is unchanged and still lives at `.agents/skills/card-intake/scripts/card-intake.js` — the canonical body invokes it by that path.
