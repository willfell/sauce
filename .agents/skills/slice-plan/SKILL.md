---
name: slice-plan
description: Deprecated alias for loop-plan. Write an implementation plan as an ordered chain of board card slices minted through the intake rail. Use when asked to plan a feature as cards or turn a spec into sequenced testable slices — then follow .agents/skills/loop-plan/SKILL.md.
---

# slice-plan (deprecated alias)

This skill's canonical body moved into the sauce loop plugin as `plan` — one source of truth shared by Claude (`/loop:plan`) and Codex. Follow `.agents/skills/loop-plan/SKILL.md` (a generated router to the canonical body). In-session execution of a minted epic is `.agents/skills/loop-execute/SKILL.md`.
