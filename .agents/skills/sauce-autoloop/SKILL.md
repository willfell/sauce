---
name: sauce-autoloop
description: Deprecated alias for loop-run. Run or resume one bounded board execution slice from claim through CI, release, and deployment. Use when asked to run, resume, inspect, dry-run, pause, or recover the loop — then follow .agents/skills/loop-run/SKILL.md.
---

# sauce-autoloop (deprecated alias)

This skill's canonical body moved into the sauce loop plugin as `run` — one source of truth shared by Claude (`/loop:run`) and Codex, bound to this repo's board via `.loop/config.json`. Follow `.agents/skills/loop-run/SKILL.md` (a generated router to the canonical body).

The operations appendix moved with it (`<codex.plugin_root>/skills/run/references/operations.md`).
