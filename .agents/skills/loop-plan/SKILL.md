---
name: loop-plan
description: Write an implementation plan AS board schema — an epic plus ordered, contracted slices minted through the sanctioned intake rail on the bound board. Use when turning a spec, proposal, or requirement into sequenced testable slices, when asked to "plan this as cards", or after /loop:brainstorm produces a proposal. Prompts for the work-item id prefix and the board priority position before minting.
---

# loop-plan (generated router)

GENERATED by the sauce loop plugin — do not edit; regenerate via /loop:init (gen-codex-routers.js).

1. Read `.loop/config.json` at the repo root. `codex.plugin_root` is the directory holding the canonical loop skill bodies (single source of truth shared with the Claude plugin surface).
2. Read `<codex.plugin_root>/skills/plan/SKILL.md` and follow it exactly. Where that body references `${CLAUDE_PLUGIN_ROOT}`, substitute `<codex.plugin_root>`.
3. The repo binding (vault/board/policy) is `.loop/config.json`; the body's resolver commands read it.

If `codex.plugin_root` is unset or the body is missing, stop and report the exact path — never improvise the procedure.
