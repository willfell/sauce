---
date: 2026-05-07
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.26.1 cycle. Triggered by v0.26.1 close + tag v0.26.1 on origin/main (P1 carries — community-plugin baseline + applyAppSettings + sauce help + status warn + wizard auto-add convenience + 6 DV blueprint deps).
predecessors:
  - Docs/plans/2026-05-07-v0.26.1-p1-carries-result.md (most recent close — read first)
  - Docs/plans/2026-05-07-v0.26.1-p1-carries-plan.md
  - Docs/plans/2026-05-07-v0.26.1-p1-carries-design.md
  - Docs/prompts/2026-05-07-post-v0.26.0-next-cycle-handoff.md (predecessor handoff)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol)
gates:
  - v0.26.1 closed; tag v0.26.1 pushed; workshop manifest at 0.26.1
  - All 3 P1 items shipped (P1-1 plugin baseline, P1-2 applyAppSettings + allowlist 13, P1-3 sauce help + .gitignore + status warn + wizard auto-add)
  - 1 review-fix commit (S2 quality reviewer's I-1 + I-2 applied; I-3 + I-4 deferred to v0.26.x)
  - Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED
  - Allowlist #12: 12 → 13 paths (added .obsidian/app.json)
  - Helper count 11 → 12 (`applyAppSettings` NEW)
  - Landmines list UNCHANGED at 19 (#12 amended; no new landmine)
  - Acceptance gate PASSED on fresh `<workshop>/scratch/12/` (deterministic install + sauce status + sauce help + adversarial drop-convenience all GREEN)
  - Reserved-headroom precedent: 14 data points; 4th MIXED case (after v0.22.1, v0.23.0, v0.26.0)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.26.2 (or other)
---

# Onboarding — post-v0.26.1 next-cycle selection

> [!info] Project identity (post-v0.26.1)
> - **Project name:** `sauce` (Sauce rebrand sequence COMPLETE since v0.25.0)
> - **GitHub remote:** `git@github-personal:willfell/sauce.git`
> - **Local workshop dev path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (host filesystem path; not yet renamed; standalone v0.26.x housekeeping carry)
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target
>   - `../accuris-beacon-poc/` — secondary consumer (legacy layout; pre-existing version-pin drift)
>   - `../scratch/2/` … `../scratch/12/` — historical smoke targets
>   - `../scratch/12/` — v0.26.1 smoke target (numbers-only basename, vault-12 derivation regression-guard for v0.26.0 CF-2 lesson b)
>   - `../scratch/12.pre-v0.26.1/` — preserved v0.26.0 install for forensics

---

## Workshop state at session start

- `workshop_version: 0.26.1`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `accent-button@0.1.0`, `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.3` (PATCH v0.26.1), `journal@0.1.2` (PATCH v0.26.1), `meetings@0.2.2` (PATCH v0.26.1), `project@1.3.6` (PATCH v0.26.1), `to-do@0.1.4`, `trips@0.1.5` (PATCH v0.26.1), `finance@0.2.10` (PATCH v0.26.1)
- Tags on origin: ... `v0.25.0` `v0.26.0` `v0.26.1` (NEW)
- Harness counts: bootstrap 58/0 (was 45/0; +13 sub-asserts via BS14-BS17), cli 42/0 (was 27/0; +15 sub-asserts via C16-C22), install-sh 14/0, helper-cases 422/0 (was 407/0; +15 sub-asserts via AS1-AS5), renderer exit 0
- Allowlist (landmine #12): **13 paths** (UPDATED in v0.26.1; added `.obsidian/app.json`)
- Landmines: **19 entries** (UNCHANGED in v0.26.1; only #12 amended)
- Helper count: **12** (v0.26.1 added `applyAppSettings`)
- **Stub md5 invariant: `ea23aa812503bfca66359d3b2b239ba8`** (UNCHANGED in v0.26.1)
- **Backup-suffix convention: `.sauce-backup`** (UNCHANGED since v0.25.0)
- **Platform-managed directory casing:** lowercase under `pantry/`, `ranch/`, `spice/` (codified in landmine #19; v0.26.0 lowercase sweep COMPLETE)

**v0.26.1 commits in order (top-down):**
- `<HANDOFF>` docs(v0.26.1): post-v0.26.1 next-cycle handoff (THIS commit)
- `2bf668c` docs(v0.26.1): cycle close — workshop_version 0.26.0 → 0.26.1 PATCH + result writeup + CLAUDE.md status snapshot
- `10e34ab` docs(v0.26.1): S3 — install.md upgrade + landmine #12 amendment + workshop .gitignore /Scripts/
- `94217db` fix(v0.26.1): S2 review fixes — wire auto-add convenience into re-run wizard + suppress BS15 stdout leak
- `68ff61d` feat(v0.26.1): S2 — applyAppSettings + sauce help + status warn + wizard auto-add convenience + foundational plugins + 6 DV blueprint deps
- `e7e20e9` (intermixed) S1 TDD-RED harness work bundled with parallel-session docs(queue) commit — see v0.26.1 lesson d
- `6312821` docs(v0.26.1): P1 carries implementation plan
- `c9ee017` docs(v0.26.1): P1 carries design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git remote -v` → `origin git@github-personal:willfell/sauce.git`
> 3. `git log --oneline -10` — expect ~10 v0.26.1 cycle commits + handoff at top
> 4. `git status` → only pre-existing drift
> 5. `git ls-remote --tags origin | grep v0.26.1` → 2 lines (annotated tag + dereference)
> 6. **Stub md5 invariant** (4 paths must match `ea23aa812503bfca66359d3b2b239ba8`): `md5 platform/installer-stub.js ranch/templater/platformInstall.js ../barebones-beacon-poc/ranch/templater/platformInstall.js ../accuris-beacon-poc/ranch/templater/platformInstall.js`
> 7. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.26.1"`
> 8. `find pantry ranch spice -type d 2>/dev/null | grep -E '/[A-Z]'` → empty (lowercase invariant per landmine #19)
> 9. **All 5 harnesses GREEN:**
>    - `node platform/test/run-bootstrap.js` → `Result: 58 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 42 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 422 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol
> 2. **`Docs/plans/2026-05-07-v0.26.1-p1-carries-result.md`** — most recent cycle close. NEW lessons (a–h); reserved-headroom precedent extends to 14 data points (4th MIXED case)
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.26.1 close
> 4. **`Docs/install.md`** — read the v0.26.0 → v0.26.1 upgrade section
> 5. **`Docs/landmines.md`** — 19 entries; #12 amended in v0.26.1 (allowlist 12 → 13)
> 6. **`blueprints i want.md`** — pending feature carries
> 7. **`features-im-brainstorming/sticky-notes.md`** — queued blueprint candidate

---

## 🟡 PRIMARY candidates

### A. v0.26.2 — Sticky-notes blueprint (queued PRIMARY since post-v0.25.0 handoff)

**What:** New blueprint at `spice/sticky-notes/<YYYY-MM-DD>/` per `features-im-brainstorming/sticky-notes.md` user spec. Cmd+0 hotkey opens day's hub; "New Note" button creates ad-hoc sticky notes. Reuses `runTemplaterTemplate` action type + nav-button pattern from v0.4.0 to-do blueprint.

**Cycle shape estimate:** New blueprint — 3 stages. Possibly clean (zero CFs) or 1-2 CFs.

### B. v0.26.x — Atomic-write tmp orphan cleanup (I-3 from v0.26.1 quality review)

**What:** When `applyAppSettings`'s `writeFileSync` succeeds but `renameSync` fails, `target+".tmp"` lingers indefinitely. Add best-effort `try { fs.unlinkSync(tmp); } catch (_) {}` to the catch block before returning. Plus consider extracting a shared `atomicWriteJson(target, data)` utility for future helpers.

**Cycle shape estimate:** PATCH cycle. 1-2 stages. Trivial.

### C. v0.26.x — AS6 backup-fails-aborts coverage (I-4 from v0.26.1 quality review)

**What:** AS5 covers happy-path backup; AS6 should stub `fs.copyFileSync` to throw and assert no new content written. Locks the contract claimed in `applyAppSettings` function header. Pure harness extension.

**Cycle shape estimate:** PATCH cycle. 1 stage. Trivial.

### D. v0.26.x — Pantry-clone source mutation investigation (carry from v0.26.0 lesson e)

**What:** v0.26.0 surfaced that install.js writes back to template source files inside `<vault>/pantry/` (workshop clone) during install. Workshop source remains intact. Mutation self-heals on next `sauce update --force`. May be related to Templater's `overwrite_file_commands` API call sequence.

**Cycle shape estimate:** Investigation cycle. 2-3 stages.

### E. v0.26.x — Multi-session git-lock contention recovery (carry from v0.26.1 lesson d)

**What:** When user uses `/remote-control` orchestration with multiple sessions, git lock contention is real (v0.26.1 S1 commit got intermixed with a parallel session's commit). Document the pattern in `Docs/how.md`: serialize commits via worktree isolation OR add session-pause checkpoints.

**Cycle shape estimate:** PATCH cycle. 1 stage. Pure docs.

### F. v0.26.x — sauce-cli.js dead `printUsage` cleanup

**What:** Post-help-verb in v0.26.1, `printUsage` (kept for legacy "unknown verb" branch) is dead code. Delete in a future PATCH.

**Cycle shape estimate:** PATCH cycle. 1 stage. Trivial.

### G. v0.27.0+ — install.js `NO_OP_DUE_TO_MISSING_PREREQ` architectural refactor

**What:** From v0.21.0/v0.22.0/v0.24.0/v0.25.0/v0.26.0/v0.26.1 deferrals. Helpers explicitly signal back to orchestrator when prereq missing; orchestrator skips writing `installed_at` when nothing materialized.

**Cycle shape estimate:** Substantial. 4-5 stages.

### H. v0.27.0+ — Multi-theme presets — DEFERRED 7th time

**What:** Schema upgrade `style_settings_defaults_src` (single string) → `style_presets[]` keyed by name. DEFERRED 7 times now. If 8th DEFERRED, de-queue or commit.

**Cycle shape estimate:** First-of-its-kind schema-extension cycle. MINOR bump. 4 stages with 2-4 CFs.

### I. v0.26.x — Local workshop dev path rename + dogfood drift housekeeping

**What:** Rename `/Users/willfell/Documents/obsidian/sync/workshop/beacon` → `/workshop/sauce`. Pure host-filesystem rename. Could bundle dogfood drift cleanup.

**Cycle shape estimate:** PATCH cycle. 1-2 stages. Trivial — pure mv + sed.

### J. v0.26.x — Project Sub-tasks panel — UI cycle (carry since post-v0.18.2)

**What:** Render a "Sub-tasks" panel on each Task hub page showing the task's sub-task cards.

**Cycle shape estimate:** UI cycle — 3-4 stages with 2-4 inline-CFs typical.

---

## Recommendation

> [!info] Recommended order
> v0.26.2 = (A) sticky-notes blueprint per long-standing queue.
> v0.26.x = bundle (B) + (C) + (E) + (F) as low-risk PATCH-bundled housekeeping if appetite for batched cleanup.
> v0.26.x = (D) pantry-clone investigation OR (J) project Sub-tasks for higher-value work.
> v0.27.0+ = (G) install.js refactor; (H) multi-theme — commit or de-queue if 8th DEFERRED happens.

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock cycle scope.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on mechanical edits + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any literal call-site that changes, QUOTE it in the design.
> - **Two-stage subagent review** (v0.20.0; reaffirmed v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0 + v0.26.0 + **v0.26.1 — 7 data points**). Never skip either.
> - **Manual smoke is the close gate** for any user-facing surface.
> - Push to origin/main after each stage commit; annotated tag at cycle close (REQUIRES user approval).
> - **Acceptance smoke target naming:** rotate adversarial inputs (numbers-only basenames, special chars, kebab-case, etc.) per v0.26.0 lesson c.
> - **Plugin id pre-flight probe** (v0.26.1 lesson a) for any future `foundational_plugins[]` expansion.
> - **Multi-session git-lock awareness** (v0.26.1 lesson d) — when running under `/remote-control`, serialize commits or use worktree isolation.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; 7 data points reaffirmation)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **Module-directory invariant (#11)** — `spice/<module>/` namespace; one-module-per-blueprint rule unchanged.
> - **Inside-vault clone is git-managed (#18)** — `pantry/` is the canonical name; preserve.
> - **Allowlist 13 paths (#12)** — adding 14th+ requires explicit user approval. Backup-suffix `.sauce-backup`.
> - **Stub content-static (#13)** — never re-edit any `ranch/templater/platformInstall.js` body. Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED.
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **Platform-managed dirs lowercase (#19)** — `pantry/`, `ranch/`, `spice/` subdirs MUST be lowercase. EXCEPTIONS enumerated in #19 (MM-MMMM, assets/themes/<ThemeName>/, user-facing note filenames, historical Docs/plans + Docs/prompts).
> - **applyAppSettings posture (v0.26.1)** — workshop-level helper; NOT per-item; declared keys are platform-as-overrider, non-declared keys preserved verbatim. Mirrors v0.24.0 applyCustomJsSettings structural template.
> - **Plugin id pre-flight probe (v0.26.1 lesson a)** — for any future `foundational_plugins[]` expansion, run the probe BEFORE manifest commit.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, allowlist expansion (14th path), creating top-level vault directory, any `.obsidian/` edit outside #12 allowlist.

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. stub md5 + harness counts + lowercase audit).
> 2. Read v0.26.1 result writeup (NEW lessons a-h).
> 3. Surface candidates A-J via `AskUserQuestion`. Recommended: (A) v0.26.2 sticky-notes blueprint as PRIMARY.
> 4. Once scope is locked, brainstorm.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.26.1 next cycle. Read Docs/prompts/2026-05-07-post-v0.26.1-next-cycle-handoff.md FIRST — full onboarding doc with project identity (Sauce rebrand COMPLETE; pantry/ + ranch/ + spice/ namespace tripod canonical; lowercase platform-managed dirs codified in landmine #19; CLI binary `sauce`; env vars SAUCE_*; allowlist #12 expanded 12 → 13 in v0.26.1 with `.obsidian/app.json`), pre-flight checks (workshop_version 0.26.1; stub md5 ea23aa812503bfca66359d3b2b239ba8 unchanged; allowlist 13 paths; helper count 12; bootstrap 58/0, cli 42/0, install-sh 14/0, helper-cases 422/0, renderer green; landmines 19 entries), candidate cycles A-J (v0.26.2 sticky-notes recommended; v0.26.x carry items including atomic-write tmp orphan cleanup, AS6 coverage, pantry-clone source mutation investigation, multi-session git-lock recovery), constraints carried forward, and the v0.26.1 NEW lessons (a-h).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (host filesystem path).
2. Run pre-flight checks. Workshop_version 0.26.1; remote git@github-personal:willfell/sauce.git; stub md5 ea23aa812503bfca66359d3b2b239ba8 (unchanged from v0.24.0).
3. Read v0.26.1 result writeup + CLAUDE.md status snapshot + landmines (19 entries; #12 amended in v0.26.1).
4. Surface candidates via AskUserQuestion. Recommended ranking: (A) v0.26.2 sticky-notes > (B) atomic-write tmp orphan cleanup > (C) AS6 backup-fails-aborts coverage > (D) pantry-clone investigation > (E) multi-session git-lock recovery > (F) sauce-cli printUsage cleanup > (G) install.js NO_OP refactor > (H) multi-theme presets DEFERRED 7th time > (I) workshop dev path rename > (J) project Sub-tasks.
5. Once locked, brainstorm.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on mechanical edits + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Apply v0.26.1 lessons at design-time:
- Plugin id pre-flight probe (lesson a; reusable for any foundational_plugins[] expansion).
- applyAppSettings is the canonical workshop-level helper template for any future cycle that needs a declared knob in .obsidian/<file>.json (lesson b).
- Two-stage subagent review reaffirmed at 7 data points; never skip (lesson c).
- Parallel-session git race awareness (lesson d; serialize commits or use worktree isolation when running under /remote-control).
- Plan baseline-count drift continues to be harmless when actual reality dominates (lesson e).
- Defer-to-carry trade-off on backup-fails-aborts coverage is a real fragility surface (lesson f).
- Wizard auto-add scope question resolved by user during quality review surface (lesson g; ambiguous design wording → user-resolution question, not implementer judgment).
- Bundled multi-feature PATCH cycles where each feature has clear precedent reliably MIXED, not first-of-kind (lesson h).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13; md5 ea23aa812503bfca66359d3b2b239ba8 holds); landmine #12 allowlist expansion (14th+ path) requires approval; landmine #11 module-directory invariant: `spice/<module>/` is canonical; landmine #19 platform-managed dirs lowercase; two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0 + v0.26.0 + v0.26.1 reaffirmation).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
