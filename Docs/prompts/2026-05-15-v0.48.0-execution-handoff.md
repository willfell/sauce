# v0.48.0 execution handoff — fresh session

> **For the picking-up agent:** brainstorm + plan-writing for v0.48.0 closed in the prior session (2026-05-15). Design + plan are written but NOT yet committed (sit untracked). Your job: execute the plan task-by-task via `superpowers:subagent-driven-development`, starting at Task 1 (S0 verification gate). The S0 gate MUST pass before any implementation begins.

## Working directory + baseline

```bash
cd /Users/willfellhoelter/projects/repos/sauce
# Branch: main (sauce uses single-branch direct push — see CLAUDE.md)

git log --oneline | head -3
# Expected top: 123927f (v0.48.0 brainstorm handoff), 1c509c0 (v0.47.0 S12), 697ef73 (v0.47.0 S9)

git rev-parse HEAD                       # should be 123927f or descendant
git status --short                       # expect 2 untracked files (the design + plan you'll commit at S0)
ls -1 Docs/plans/2026-05-15-v0.48.0-*.md # expect: design.md, plan.md
npm run release:preflight                # exit 0 across 15 harnesses
brew info willfell/sauce/sauce | head -3 # stable 0.47.0
```

**Brew:** `0.47.0` (will bump to `0.48.0` after this cycle's S9).
**Workshop dogfood:** `/Users/willfellhoelter/projects/repos/sauce` (subscription pinned to `0.47.0`).
**Headspace consumer (re-validation target):** `/Users/willfellhoelter/notes/sauce/headspace-sauce` (subscription pinned to `0.47.0`).

## Read first (REQUIRED — in this order)

1. **`Docs/plans/2026-05-15-v0.48.0-design.md`** — the v0.48.0 design. Walks through BUG-5 problem statement (with the factual correction to FLN-i: only project-level `_promptForProjectName`/`_createProject`/`_renderProjectsHub` were stripped in v0.46.0, NOT `_createTaskNote`/`_createTaskBoard`), the Q1-Q8 decisions, the three-piece architecture (listener + startup template + installer helper + Kanban Card enhancement), risks + escalation gates.
2. **`Docs/plans/2026-05-15-v0.48.0-plan.md`** — the 12-task implementation plan. Task 1 = S0 verification (controller-direct, gates everything). Tasks 2-12 = implementation + lockstep + release + revalidation + close.
3. **`Docs/plans/2026-05-15-v0.47.0-result.md`** — v0.47.0 cycle result for context (especially FLN-i, the BUG-5 problem statement that v0.48.0 fixes).
4. **`CLAUDE.md`** — sauce conventions, 22 landmines, Status (live).

## Pacing recap (per design Q8)

| Stage | Mode |
|---|---|
| S0 | controller-direct (verification gate) |
| S1 | **subagent-driven** (NEW ProjectTaskCreateListener class) |
| S2 | **subagent-driven** (Kanban Card sentinel + create-new + empty-toast) |
| S3 | controller-direct (NEW startup template + project manifest delta) |
| S4 | **subagent-driven** (NEW applyTemplaterStartupTemplates installer helper) |
| S5 | **subagent-driven** (test harness deltas) |
| S6 | controller-direct (FLN-a + FLN-b icons polish) |
| S7 | controller-direct (FLN-c plusIcon size — includes grep to locate owner) |
| S8 | controller-direct (catalogue + subscription lockstep + dogfood reinstall) |
| S9 | controller-direct (tag v0.48.0 + tap auto-bump) |
| S10 | controller-direct (headspace re-validation manual smoke) |
| S11 | controller-direct (result doc + CLAUDE.md cycle close) |

**Subagent dispatches:** ~5 (S1, S2, S4, S5) + reviewers. Plus controller-direct in/out.

## Critical S0 gate (DO NOT SKIP)

Task 1 in the plan has 6 sub-steps that empirically verify three design assumptions at headspace BEFORE any code lands:

1. **Templater command id literal** — confirm `templater-obsidian:replace-in-file-templater` exists in headspace's Templater plugin. If renamed, update Task 2 + Task 6 literals before S1.
2. **Templater startup_templates fires at vault boot** — smoke a throwaway startup template at headspace. If it doesn't fire, escalate per design (re-scope to L3 or hand-rolled `workspace.onLayoutReady`).
3. **Kanban-plugin vault.create race timing** — confirm the 50ms defer in the listener is sufficient. If not, update the listener's `_postCreateDelayMs` before S1.

After S0 completes, **commit the design + plan + S0 verification log together as the first commit of the cycle** (the plan's Task 1 includes a step for this).

## Sauce conventions reminder (NON-NEGOTIABLE)

From `CLAUDE.md` "Git workflow" + "Ask before acting":

- **Single-branch direct push to `main`.** No feature branches, no PRs.
- **HEREDOC for multi-line commit messages.** Conventional commits prefix.
- **NO `Co-Authored-By: Claude` trailer.** Author is the user.
- **NO `--no-verify`** (pre-commit hooks must pass; fix the underlying issue).
- **NO force-push, NO history rewrite.**
- **NO `.obsidian/` writes outside the 17-path allowlist** in landmine #12. The new `applyTemplaterStartupTemplates` writes to `.obsidian/plugins/templater-obsidian/data.json` — already in the allowlist (same file as `applyTemplaterFolderTemplates`); the new field `startup_templates[]` is within the same file. No allowlist amendment needed.
- **Direct edits to `.claude/commands/<x>.md` or `.claude/skills/<bp>/**/SKILL.md` are REVERTED on next install** (landmine #22). Use `.local/` override seam.

## Escalation gates (STOP and report, don't auto-iterate)

- **S0 escalation: Templater startup_templates doesn't fire OR kanban race exceeds defer:** STOP. Revisit design Q3 listener registration. Options: L3 (convenience-mechanism CustomJS auto-load via hub) OR hand-rolled `workspace.onLayoutReady` registration. Update design doc, restart plan from S1 with revised mechanism.
- **S0 escalation: Templater command id renamed:** STOP after S1.2. Update Task 2's `_templaterCommandId` literal + Task 6's PTCL-5 assertion. Resume S1.
- **Any stage's subagent flags HIGH-severity concern in review:** STOP. Read the reviewer's note; decide whether to revise the stage spec, accept-with-FLN, or escalate to the user.
- **S10 escalation: headspace re-validation surfaces a NEW bug (BUG-6, BUG-7):** STOP. Do NOT proceed to S11. Capture the new bug in `Docs/prompts/2026-05-15-v0.49.0-<brief>-handoff.md`. Decide with the user: close v0.48.0 as-is (the 3 v0.48.0 scenarios pass; new bug is separate) OR roll back the v0.48.0 release if the new bug is a regression caused by v0.48.0.

## What success looks like

- v0.48.0 tagged + pushed; brew formula auto-bumped via release.yml.
- BUG-5 fixed at headspace: clicking "+ Add a card" on a project kanban board fires the workstream picker (with "+ Create new workstream" sentinel); new task lands at `tasks/<X>/<X>.md` (folder-promoted) with `workstream:<id>` frontmatter.
- Empty-workstreams Notice fires on a fresh project's first kanban card.
- FLN-a/b/c icons polish absorbed.
- Workshop preflight green; +~12 sub-asserts vs v0.47.0 baseline.
- v0.47.0 phase result fully wrapped.

## Decisions captured during v0.48.0 brainstorm (for context)

| # | Decision | Choice |
|---|---|---|
| Q1 | Cycle scope | BUG-5 + FLN-a/b/c icons polish |
| Q2 | Task creation entry point | (X) Kanban only — fix in place |
| Q2-mechanism | BUG-5 fix mechanism | A1 — Templater post-create command trigger |
| Q3 | Listener registration | L1 — Templater startup_templates |
| Q3-empty | Empty-workstream behavior | Toast notice + silent skip |
| Q3-create-new | "+ Create new workstream" affordance | Yes — picker sentinel + persist to atlas |
| Q4 | Atlas helper buttons | N/A (moot per Q2=X) |
| Q5 | Kanban "+ Add a card" coexistence | Unchanged — augmented by post-create listener |
| Q6 | Templater folder_templates hedge | N/A (L1 chosen) |
| Q7 | Acceptance gate | Workshop preflight + headspace re-validation |
| Q8 | Pacing | Subagent-driven for risky stages (S1/S2/S4/S5); controller-direct otherwise |

## Factual correction to FLN-i (kept here for reference)

FLN-i claimed v0.46.0 stripped `_createTaskNote` + `_createTaskBoard` (~184 LOC) "plus" the 3 project-level methods. Verified against `git show 6efa21f` (the v0.46.0 S6 strip commit):

- **What v0.46.0 S6 actually stripped (~184 LOC):** ONLY `_renderProjectsHub`, `_promptForProjectName`, `_createProject`. All PROJECT-level.
- **What was NOT stripped:** `_createTaskNote` (still at `project-nav-buttons.js:162`), `_createTaskBoard` (still at `:192`), `_promptForTitle` (still at `:88`). Render-side wiring for "New Note" / "Open Board" / "Create Board" buttons on task-hub + task-note contexts still intact at lines 567-625.
- **The "atlas missing helper buttons" framing is also misleading:** "+ Add Task" / "+ Add Task Board" buttons on the project atlas page were NEVER present pre-v0.46.0.

So BUG-5 is **one observable defect** (kanban "+ Add a card" doesn't run Templater post-paste). v0.48.0 fixes that single defect by triggering Templater post-creation via a startup-registered listener.

## Links

- [v0.48.0 design](../plans/2026-05-15-v0.48.0-design.md)
- [v0.48.0 plan](../plans/2026-05-15-v0.48.0-plan.md)
- [v0.48.0 brainstorm handoff (predecessor)](2026-05-15-v0.48.0-brainstorm-handoff.md)
- [v0.47.0 result (FLN-i = BUG-5 problem statement)](../plans/2026-05-15-v0.47.0-result.md)
- [v0.47.0 execution handoff (precedent format)](2026-05-15-v0.47.0-execution-handoff.md)
- Tag (anticipated): v0.48.0
