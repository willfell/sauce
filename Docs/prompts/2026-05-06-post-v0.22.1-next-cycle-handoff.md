---
date: 2026-05-06
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.22.1 cycle. Triggered by v0.22.1 close + tag v0.22.1 on origin/main + post-smoke critical finding (macOS APFS case-collision actively breaks Obsidian runtime, not just git status).
predecessors:
  - Docs/plans/2026-05-06-v0.22.1-non-interactive-and-dirty-tree-result.md (v0.22.1 close — read first; includes the post-smoke critical finding)
  - Docs/plans/2026-05-06-v0.22.1-non-interactive-and-dirty-tree-plan.md
  - Docs/plans/2026-05-06-v0.22.1-non-interactive-and-dirty-tree-design.md
  - Docs/plans/2026-05-06-v0.22.0-inside-vault-bootstrap-result.md
  - Docs/prompts/2026-05-06-post-v0.22.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (multi-theme entry now DEFERRED behind the rename)
gates:
  - v0.22.1 closed; tag v0.22.1 pushed; workshop manifest at 0.22.1
  - --non-interactive propagation working end-to-end (bash → cmd-bootstrap → wizard)
  - --mechanisms / --blueprints CLI override flags landed (CSV + "all" + "none")
  - install.sh --vault=PATH equals form (IMP-5 closed)
  - Workshop-side .gitignore root-anchored module-dir patterns shipping
  - Docs/install.md case-collision warning callout (strong wording, names rename as v0.23.0 fix)
  - 2 harness extensions (run-install-sh.js 10 → 14, run-cli.js 13 → 20)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.23.0
---

# Onboarding — post-v0.22.1 next-cycle selection

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target (NOT git-tracked; sibling-of-workshop layout)
>   - `../accuris-beacon-poc/` — secondary consumer (sibling-of-workshop layout)
>   - `../scratch/1/` — fresh-vault Phase C smoke target (created v0.21.0; sibling-of-workshop layout)
>   - `../scratch/2/` — v0.22.0 inside-vault layout smoke target (preserved as v0.22.0 baseline)
>   - **NEW:** `../scratch/3/` — v0.22.1 NI-default smoke target (5 default mechs subscribed)
>   - **NEW:** `../scratch/4/` — v0.22.1 NI-all smoke target (8 mechs + 8 blueprints; **demonstrates the case-collision RUNTIME breakage**)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults

---

## 🔴 Mission — TWO PRIMARY candidates compete for v0.23.0

### CRITICAL POST-SMOKE FINDING (the load-bearing context)

**v0.22.1 surfaced a real macOS user-facing bug.** Opening daily note in scratch/4 produced *"Failed to create daily note. Folder 'beacon/daily' not found."* All 8 blueprints are functionally broken on macOS APFS — the `.gitignore` fix in v0.22.1 masks `git status` but does NOT fix the runtime breakage.

**Why:** macOS APFS case-insensitivity aliases `Beacon/` (workshop clone, capital B per landmine #18) with `beacon/<module>/` (blueprint module-directory namespace, lowercase per landmine #11). Blueprint mkdirs land **inside** `Beacon/`. Obsidian's vault index is case-sensitive at the app layer — indexed as `Beacon/daily`, lookup for configured `beacon/daily` fails.

**Affected:** macOS users (the vast majority). Linux / case-sensitive macOS volumes work correctly.

---

**Workshop state at session start:**
- `workshop_version: 0.22.1`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.22.0` `v0.22.1` (NEW)
- Harness counts: bootstrap 45 / cli **20** (+7) / install-sh **14** (+4) / helper-cases 387 / renderer 23 — all green.
- Allowlist (landmine #12): **11 paths** UNCHANGED in v0.22.1.
- Landmines: **18 entries** UNCHANGED (no new landmine in v0.22.1).
- Helper count: 9 UNCHANGED.

**v0.22.1 commits in order (top-down):**
- `<HANDOFF>` docs(v0.22.1): post-v0.22.1 next-cycle handoff (THIS commit upcoming)
- `72aae62` docs(v0.22.1): cycle close — workshop_version 0.22.0 -> 0.22.1 PATCH
- `8711e91` fix(gitignore): v0.22.1 S2 CF-1 — root-anchor module-dir patterns
- `f88eab8` feat(cli): v0.22.1 S2 — --non-interactive flag propagation + dirty-tree fix
- `ed1a656` test(v0.22.1): S1 TDD-first — failing harness baseline
- `dbcf259` docs(v0.22.1): non-interactive + dirty-tree fix implementation plan
- `5ba69a6` docs(v0.22.1): non-interactive + dirty-tree fix design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git log --oneline -10` — expect 7 v0.22.1 cycle commits + handoff at top.
> 3. `git status` → clean (or carry-forward `.bak` artifacts + `.obsidian/` install-drift markers).
> 4. `git ls-remote --tags origin | grep v0.22.1` → 2 lines (annotated tag + dereference).
> 5. **Stub md5 invariant** (4 paths must match): `md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js` → all `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.22.1"`.
> 7. install.sh executable + CLI dispatcher + npm deps still present.
> 8. Workshop self-install green + idempotent: `node platform/test/run-install.js .` → exit 0.
> 9. **All 5 harnesses:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 20 passed, 0 failed.` (+7 from v0.22.1)
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.` (+4 from v0.22.1)
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0.
> 10. **Repro the macOS case-collision** (optional but recommended for ground-truthing): open `/Users/willfell/scratch/4` in Obsidian, click the Daily Notes plugin's "Open today's daily note" → expect *"Folder 'beacon/daily' not found."*

If any pre-flight fails (apart from #10's expected failure), STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-06-v0.22.1-non-interactive-and-dirty-tree-result.md`** — most recent cycle close. Read the "Post-smoke critical finding" block FIRST; it's the load-bearing context for v0.23.0.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.22.1 close.
> 4. **`Docs/install.md`** — read the `[!warning]` callout (lines ~99-110) for current user-facing wording on the case-collision.
> 5. **`Docs/landmines.md`** — 18 entries; #11 module-directory invariant + #18 inside-vault `Beacon/` is git-managed are the load-bearing landmines for the rename design.
> 6. **`blueprints i want.md`** — multi-theme entry still open but DEFERRED behind the rename.

---

## 🔴 v0.23.0 PRIMARY: Workshop clone dir rename — case-collision RUNTIME BREAKAGE FIX

> [!warning] This MUST ship before any new blueprint or mechanism cycle. macOS users currently can't use any blueprint runtime entry point.

**What:** Rename the workshop clone directory from `Beacon/` to a non-colliding name. The lowercase `beacon/<module>/` namespace stays per landmine #11 (it's the canonical content namespace for blueprints; consumers rely on it; CLI verbs reference it).

**Why:** Resolves the runtime breakage at the root. macOS APFS will see `<NewName>/` and `beacon/` as distinct directories; Obsidian's vault index will see them as distinct; Daily Notes plugin will find `beacon/daily/` correctly.

### Rename candidates — open brainstorm

User has indicated preference for **creative beacon-themed names** that relate to the platform identity, not generic placeholders or hidden dirs. Goals: short, memorable, doesn't collide with `beacon/`, plays with the lighthouse / signal / beam metaphor, comfortable in UI navigation.

**Strong candidates to surface in brainstorm:**

| Candidate | Pro | Con | Notes |
|---|---|---|---|
| `beak/` | Short (4 chars); beacon-themed (bird-of-prey lighthouse imagery? OR portmanteau of "beacon"); doesn't collide. | Slightly cute; ambiguous semantics ("the beak" vs "beacon"). | User-suggested. |
| `lighthouse/` | Strong metaphor ("beacon" → lighthouse); evocative; clearly platform-themed. | Long (10 chars); somewhat generic. | Classic beacon imagery. |
| `beam/` | Short (4 chars); beacon-themed (light beam = signal); doesn't collide. | Could be confused with build-system "beam" (Erlang BEAM VM). | |
| `signal/` | Strong metaphor; doesn't collide. | Generic; conflicts mentally with Signal messenger. | |
| `flare/` | Short (5 chars); beacon-themed (flare = signal); doesn't collide. | "Flare" suggests temporary/emergency; less platform-permanent feeling. | |
| `helix/` | Distinctive; doesn't collide; spirals + beacons share rotational metaphor. | Off-theme; user might not connect. | Wildcard. |
| `tower/` | Strong beacon imagery (lighthouse tower); doesn't collide. | Slightly enterprise-flavored. | |
| `crest/` | Short; doesn't collide; family-crest / wave-crest metaphor. | Off-theme for beacon. | |
| `BeaconCore/` | Explicit; ties back to platform name. | Long; CamelCase; may still feel like "Beacon" alias visually. | |
| `BeaconWorkshop/` | Explicit; describes the dir purpose. | Long (15 chars); CamelCase. | Conservative fallback. |
| `_beacon/` | Underscore-prefix indicates "infrastructure"; doesn't visually collide. | Underscore convention isn't standard in Obsidian; user said this could "get messy in UI navigation." | User RULED OUT. |
| `.beacon/` | Hidden-dir convention (no UI clutter). | Obsidian default-skips dot-dirs from vault scan — would HIDE the workshop from Obsidian entirely; CLI would still work; activate.sh + git tracking work. | User RULED OUT. |

**Brainstorm-mode recommendation:** open with `beak/` (user's seed) + `beam/` + `lighthouse/` as the top-3; rank by: doesn't-collide check + 4-7 char length + beacon-thematic + APFS-distinct test (mkdir + ls case-permutations).

### v0.23.0 cycle scope

**Files touched:**
- `install.sh` — `BEACON_DIR="$VAULT/<NewName>"` (line 67); `mv` paths in overwrite branches; final exec path.
- `platform/cli/beacon-cli.js` — dispatcher's `bootstrapCtxFromArgs` synthesizes `workshopPath = path.join(vaultPath, "<NewName>")` (line 63 currently); `config.workshop_relative_path` synthesis.
- `platform/bootstrap.js` — `phaseWriteActivation` writes `<vault>/<NewName>/Scripts/{activate.sh, beacon}` (currently `Beacon/Scripts/...`); `activate.sh` template's `BEACON_VAULT` + PATH lines.
- `platform/bootstrap-lib/wizard.js` — defaults: `workshopRelativePath || "<NewName>"` (currently `"Beacon"` lines 100, 171).
- `Docs/install.md` — every reference to `Beacon/` becomes `<NewName>/`; the `[!warning]` callout flips to "RESOLVED in v0.23.0" with migration instructions.
- `Docs/landmines.md` #18 — wording bumped to reference `<NewName>` (or kept generic — "the workshop clone dir is git-managed").
- `Docs/use.md` — curl-onboarding subsection.
- `Docs/how.md` — CLI surface section.
- `CLAUDE.md` — status snapshot + ask-before-acting + every reference to `Beacon/` becomes `<NewName>/`.
- `.gitignore` — root-anchored module-dir patterns can be REMOVED (no longer needed once dir doesn't collide). OR kept as defensive belt-and-suspenders.
- All 3 harnesses that reference `Beacon/` (`run-install-sh.js` I3/I4/I5/I6 hardcode `vaultPath/Beacon/...`; `run-bootstrap.js` BS10-BS13; `run-cli.js` C5/C6/C7/C8/C9/C10/C11-C14 stub manifests use `path.join(vaultPath, "Beacon")`).

**Migration story for existing v0.22.0 + v0.22.1 curl-bootstraps** (currently scratch/2 + scratch/3 + scratch/4):
- `beacon update` could detect old `Beacon/` and `mv Beacon <NewName>` after pulling new platform code. Tag `migration_v0_23_0` in installed.history.
- OR: ship a `beacon migrate` one-shot verb. Less magic; more explicit user consent.
- Document the migration in `Docs/install.md` "upgrading from v0.22.x" section.

**Cycle shape estimate:** 2-3 stages. MINOR bump (BREAKING for v0.22.x curl-bootstraps; one-time migration required). Reserved-headroom: expect 1-3 CFs (any rename touches many call sites; some path string is going to be missed).

---

## 🟢 NEXT-AFTER-v0.23.0 PRIMARY: v0.24.0 cowork blueprint + `/beacon-cowork-setup` skill (DESIGN + PLAN COMMITTED 2026-05-06)

> [!success] Ready to execute the moment v0.23.0 closes
> - **Onboarding doc:** `Docs/prompts/2026-05-07-onboard-v0.24.0-cowork-blueprint.md` — full handoff-protocol-compliant entry prompt with sequencing gate, slash command, all required reads.
> - **Design:** `Docs/plans/2026-05-06-v0.24.0-cowork-blueprint-design.md` (commit `af91bda`) — 12 sections; user-approved at brainstorm close.
> - **Plan:** `Docs/plans/2026-05-06-v0.24.0-cowork-blueprint-plan.md` (commit `513f78b`) — 882 lines; 4 stages; bite-sized task breakdown.
> - **Sequencing gate:** v0.24.0's pre-flight #4 verifies `git ls-remote --tags origin | grep -E 'v0\.23\.[0-9]+'` returns ≥ 1 entry. STOP if v0.23.0 unshipped (`beacon/cowork/` would case-collide on macOS APFS the same way v0.22.1 surfaced for daily).
> - **Approved platform deltas at brainstorm close 2026-05-06:** `.claude/commands/` allowlist (11 → 12 paths); `files[].target_root` discriminator; `cowork_mcp_id` canonical variable; nav-buttons 2.5.2 → 2.6.0 MINOR. Helper count stays at 9; workshop_version 0.23.x → 0.24.0 MINOR at cycle close.
> - **Skill bundling:** ships `<vault>/.claude/commands/beacon-cowork-setup.md` — interview-driven scaffold of tailored prompt notes with canonical 6-section shape + Self-Heal Protocol (3-retry cap; user-intent escape hatch; git-reviewable Iteration Log).
>
> When v0.23.0 closes, the closing session writes the post-v0.23.0 handoff. That handoff names v0.24.0 cowork as PRIMARY by reading the onboarding doc above. The slash command for the next session is at the bottom of `Docs/prompts/2026-05-07-onboard-v0.24.0-cowork-blueprint.md`.

---

## Other candidate cycles (queued AFTER v0.24.0 cowork)

> [!example]- v0.25.0 multi-theme presets
> Carried from v0.21.1 + v0.22.0 + v0.22.1 + (now) v0.24.0. Schema upgrade `style_settings_defaults_src` → `style_presets[]`; consumer subscription declares `style_preset: "baseline"` (or "headspace" / "ero" / "<fourth>"). Wizard adds preset prompt. **DEFERRED behind v0.23.0 + v0.24.0** because (a) all blueprint runtimes need to work first (v0.23.0 fix), and (b) cowork blueprint adoption surfaces real consumer feedback ahead of the multi-preset rollout.

> [!example]- v0.23.x architectural install.js refactor
> Helpers signal NO_OP_DUE_TO_MISSING_PREREQ to orchestrator. Eliminates landmine #16 unstick PATCH bumps. Substantial install.js change. Could bundle alongside v0.23.0 rename if the rename touches install.js significantly anyway.

> [!example]- v0.23.x project Sub-tasks panel on task hub
> Carry from post-v0.18.2. UI cycle.

> [!example]- v0.23.x finance polish carries
> Migration helper for pre-v0.17.0 entities; budget categories aggregate row; PaycheckExpensesEditor "All Paid" toggle.

> [!example]- v0.23.x cross-doc line-citation drift lint
> v0.20.0 NEW lesson c carry. Small additive harness extension.

> [!example]- v0.22.x install.sh --reset-config flag (NEW v0.22.1 carry)
> Force phaseFirstRunWizard re-run by deleting consumer-side platform-{config,subscription}. Useful for testing `--mechanisms` / `--blueprints` overrides on existing vaults. Out of scope until requested.

---

## Recommendation

> [!success] v0.23.0 — workshop clone dir rename — PRIMARY (load-bearing; macOS users currently broken)
>
> Brainstorm should:
> 1. Lock the dir-name choice via `AskUserQuestion` with the 12-candidate matrix above (rank top 3-5; user picks).
> 2. Verify the candidate doesn't collide on macOS APFS via `mkdir <name> && ls <NAME>` test.
> 3. Decide on migration path (beacon update auto-rename vs explicit `beacon migrate` verb vs documentation-only).
> 4. Decide on `.gitignore` retention (remove now-unneeded patterns vs keep as belt-and-suspenders).
> 5. Lock cycle shape (2-3 stages; MINOR bump).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock dir-name choice + migration shape.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on the rename mechanics (find-and-replace across many files) + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any path string that changes, QUOTE the literal in the design.
> - **Two-stage subagent review (v0.20.0 lesson d; reaffirmed in v0.22.1)** — spec compliance first, code quality second. v0.22.1 CF-1 was caught by quality reviewer; same posture for v0.23.0.
> - **Manual smoke is the close gate.** A fresh `<vault>/<NewName>/` curl-bootstrap on macOS APFS, opening Daily Notes successfully, is the v0.23.0 success criterion. Without that, the cycle is incomplete.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Reserved-headroom precedent now 9 data points (post-v0.22.1).** Decision rule: pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix / pure-additive helper-pair → reliably UNUSED EXCEPT when the two-stage subagent review surfaces a latent issue (v0.22.1 first MIXED case). New mechanism / new UI surface / first-of-its-kind orchestration → expect 1-6 CFs. **v0.23.0 rename is wide-touching but mechanical — expect 1-3 CFs from missed call-sites.**

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **Verify command ids against runtime** (gotcha 10).
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; reaffirmed v0.22.1)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **First-wins protects consumer trust** (v0.21.1 lesson c).
> - **Module-directory invariant under `beacon/` (#11)** — DO NOT change `beacon/<module>/` namespace as part of v0.23.0 rename. Only the workshop clone dir changes.
> - **Inside-vault clone is git-managed (#18)** — wording amended in v0.23.0 to reference `<NewName>/` instead of `Beacon/`.
> - **Allowlist 11 paths (#12)** — adding 12th+ requires explicit user approval.
> - **Stub content-static (#13)** — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **Per-shell source-activation (v0.22.0 lesson a)** — preserved in v0.23.0; just the path-baked-in changes.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory (v0.23.0 codifies a NEW sanctioned top-level: `<NewName>/` replacing `Beacon/`).

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. optional macOS Daily Notes repro in scratch/4).
> 2. Read v0.22.1 result writeup (start with the "Post-smoke critical finding" block).
> 3. Surface the rename as PRIMARY recommendation via `AskUserQuestion`. Include the 12-candidate matrix.
> 4. Once dir-name is locked, brainstorm: scope, locked decisions, migration path, `.gitignore` retention, harness updates needed, cycle shape.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.22.1 next cycle. Read Docs/prompts/2026-05-06-post-v0.22.1-next-cycle-handoff.md FIRST — full onboarding doc with project identity, pre-flight checks, the 🔴 PRIMARY recommendation (v0.23.0 workshop clone dir rename to fix macOS APFS case-collision RUNTIME breakage that v0.22.1 surfaced post-smoke), 12-candidate dir-name matrix (beak / beam / lighthouse / signal / flare / helix / tower / crest / BeaconCore / BeaconWorkshop with _beacon and .beacon ruled out), constraints carried forward, and the v0.22.1 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run pre-flight checks. Bootstrap 45/0, cli 20/0 (was 13 pre-v0.22.1; +C11-C14), install-sh 14/0 (was 10 pre-v0.22.1; +I7+I8 + I2 fix), helper-cases 387/0, renderer green. install.sh forwards --non-interactive + --mechanisms + --blueprints. Workshop_version 0.22.1.
3. Optionally repro the macOS case-collision: open /Users/willfell/scratch/4 in Obsidian, click Daily Notes "Open today's daily note" → expect "Folder beacon/daily not found."
4. Read v0.22.1 result writeup (Post-smoke critical finding block FIRST) + CLAUDE.md status snapshot + Docs/install.md case-collision callout + landmines (18 entries; #11 + #18 are load-bearing for the rename) + blueprints i want.md.
5. Surface candidates via AskUserQuestion. v0.23.0 dir rename = PRIMARY (load-bearing — macOS users currently broken). Multi-theme presets DEFERRED again. Architectural install.js refactor could bundle. Project Sub-tasks panel + finance polish + cross-doc lint queued.
6. Once locked, brainstorm dir-name choice (verify APFS-distinct via mkdir+ls test) + migration path (beacon update auto-rename vs explicit beacon migrate verb vs docs-only) + .gitignore retention.
7. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on the rename mechanics + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate — fresh-vault curl-bootstrap on macOS APFS + Daily Notes successful open is the v0.23.0 success criterion. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13); landmine #12 allowlist expansion (12th+ path) requires approval; landmine #11 module-directory invariant UNCHANGED in v0.23.0 (only the workshop clone dir name changes; beacon/<module>/ namespace stays); two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 reaffirmation); v0.22.0 lessons (per-shell source-activation; bootstrapCtxFromArgs pattern; bash-driver harness shim posture; --non-interactive bash-side-only [CLOSED v0.22.1]; fresh-clone dirty-tree [WORKED-AROUND v0.22.1; ROOT FIX in v0.23.0]); v0.22.1 NEW lessons (.gitignore root-anchoring; phaseFirstRunWizard short-circuits on existing config; BEACON_REPO_URL=file:// smoke transport; reserved-headroom MIXED case introduced; two-stage review reaffirmed; plan baseline-count drift harmless).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
