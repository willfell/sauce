---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.18.0 cycle. Triggered by v0.18.0 BeaconButton mechanism cycle close + tag v0.18.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.18.0-beacon-button-result.md (v0.18.0 close — read first)
  - Docs/plans/2026-05-05-v0.18.0-beacon-button-design.md
  - Docs/plans/2026-05-05-v0.18.0-beacon-button-plan.md
  - Docs/plans/2026-05-05-v0.17.0-finance-widgets-result.md
  - Docs/prompts/2026-05-05-post-v0.17.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; new-meeting-button parity still pending)
gates:
  - v0.18.0 closed; tag v0.18.0 pushed; workshop manifest at 0.18.0
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - NEW mechanism beacon-button@0.1.0 shipped (single-method renderer; opts {label, icon, onClick, flex?, disabled?, tone?})
  - project bumped 1.3.2 → 1.3.3 (4 _renderActionButton sites migrated; private method DELETED)
  - finance bumped 0.2.6 → 0.2.9 (8 sites migrated + CF-1 InvoiceControls amount recompute + drift detection)
  - All other blueprints + mechanisms UNCHANGED
  - Helper-cases harness 255 sub-asserts (UNCHANGED in v0.18.0)
  - Renderer harness 23 cases (UP from 17 in v0.17.0; +6 BB1-BB6 under new `beacon-button` selector)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.18.x or v0.19.0 (cycle-dependent)
---

# Onboarding — post-v0.18.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `../accuris/` / `../headspace/` / `../ero/` — production reference vaults (sources for pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.18.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, **`beacon-button@0.1.0`** NEW
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.0`, `meetings@0.2.0`, **`project@1.3.3`** (BUMPED), `to-do@0.1.2`, `trips@0.1.4`, **`finance@0.2.9`** (BUMPED 3 in-cycle bumps)
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` `v0.11.0` `v0.12.0` `v0.13.0` `v0.14.0` `v0.15.0` `v0.16.0` `v0.17.0` **`v0.18.0`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED in v0.18.0); `run-renderer.js` 23 cases (+6 BB1-BB6 NEW under `beacon-button` selector); pre-existing T4.0 fatal still carried (use selectors)
- `makeFinanceCustomJsStub` extended with BeaconButton stub (unblocks FF cases after finance migration)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 3 paths; helper count: 4

**v0.18.0 commits in order (top-down):**
- `<TBD>` docs(v0.18.0): cycle close — beacon-button result + status snapshot + post-v0.18.0 handoff (THIS commit upcoming)
- `dc56814` feat(finance): v0.18.0 S2 + CF-1 — finance migration to BeaconButton + InvoiceControls amount recompute; finance 0.2.6 -> 0.2.9
- `b30b4e0` feat(beacon-button): v0.18.0 S1 — mechanism + project migration; workshop 0.17.0 -> 0.18.0; project 1.3.2 -> 1.3.3
- `98abe04` docs(v0.18.0): BeaconButton mechanism implementation plan
- `cca3c2a` docs(v0.18.0): BeaconButton mechanism design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.18.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -8` — expect 5 v0.18.0 cycle commits + 3 prior (v0.17.0 close + v0.16.0 close + earlier).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `blueprints i want.md` + the `Docs/Meta/Scripts/cards/beacon-cards.js` install-drift M).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.18` → `v0.18.0` present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.18.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.18.0"`. Confirm `beacon-button` line shows `"version": "0.1.0"`, project `"1.3.3"`, finance `"0.2.9"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`, `beacon-button`) for regression sweeps. **T4.0 carried-forward 9 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors.
>    - All 7 BC + 2 DA + 8 FF + 6 BB cases green.
> 9. **Barebones state:** subscription pinned to `beacon-button@0.1.0`, `project@1.3.3`, `finance@0.2.9`. Install green + idempotent.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.18.0-beacon-button-result.md`** — most recent cycle close. Captures: 1 in-cycle CF (CF-1 InvoiceControls Save amount recompute + drift detection); 12 sites migrated (4 project + 8 finance); 6 NEW v0.18.0 lessons (in-cycle re-process bump rule at scale, mechanism-promotion-as-clean-cycle-shape, harness stub maintenance, reserved-headroom-UNUSED 3-data-point precedent, tone palette slot reservation, cross-file UX coupling discovered late).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.18.0 close.
> 4. **`blueprints i want.md`** — user's wishlist (post v0.18.0 close):
>    - REMOVE: BeaconButton mechanism — DONE in v0.18.0
>    - KEEP: styling (line 18-67; explicitly DEFERRED)
>    - KEEP: meeting cards on daily (verify whether v0.12.0 already satisfied)
>    - KEEP: new-meeting-button parity (line 64; reinforced by Smoke A user note in v0.18.0)
> 5. **`Docs/landmines.md`** — 14 entries unchanged in v0.18.0; **v0.19.0 landmine candidate:** in-cycle re-process bump rule (gotcha 9) is now strong enough to graduate to numbered landmine #15 (load-bearing for any cycle with CF iterations).
> 6. **`Docs/how.md`** — eight cycles of lessons NOT YET codified (v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.17.0 / v0.18.0); candidate for a docs polish cycle.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.18.x.y new-meeting-button parity **(PRIMARY recommendation — single-stage clean candidate)**
> **What:** Harmonize `new-meeting-button.js` with the now-fully-mature outline-accent button shape. Adopt `customJS.BeaconButton.render(...)` for the New Meeting trigger on `beacon/meetings/hubs/<YYYY>/<MM-MMMM>/Meetings-<YYYY-MM-DD>.md`. Mirror NewBudget/Paycheck/Invoice/Project pattern.
>
> **Why:** Carry-forward from v0.17.0 + v0.18.0 handoffs. User explicitly noted in v0.18.0 Smoke A that this surface still uses the old inline-cssText pattern. Single-stage clean candidate (no novel mechanism; pure migration). With v0.18.0's BeaconButton mechanism shipped, this is just a one-file adoption + lockstep manifest bump.
>
> **Estimated scope:** 0.5 stage. Workshop bump 0.18.0 → 0.18.1 PATCH. meetings@0.2.0 → 0.2.1 PATCH. Per `blueprints i want.md` line 64.
>
> **Open questions for brainstorming:** none significant; this is a near-clone migration. Should the existing icon SVG inside new-meeting-button.js stay verbatim (probably yes — preserves visual continuity with prior version)?

> [!example]- v0.19.0 workstream add/remove buttons migration **(RUNNER-UP — surfaced in v0.18.0 Smoke A)**
> **What:** Migrate workstream Add/Remove buttons in `ProjectWorkstreams` / `ProjectWorkstreamManager` to `customJS.BeaconButton.render(...)`. NEW v0.19.0 candidate from v0.18.0 Smoke A user note.
>
> **Why:** Visual cohesion across project blueprint. Currently these buttons use bespoke inline styling that drifted from the canonical pattern. With BeaconButton shipped, easy migration.
>
> **Estimated scope:** 0.5–1 stage. Workshop bump 0.18.0 → 0.18.1 PATCH (or 0.19.0 MINOR if combined with new-meeting-button parity). project@1.3.3 → 1.3.4 PATCH.

> [!example]- v0.18.x docs polish + T4.0 cleanup
> **What:** Fix the 9-cycles-old run-renderer.js no-selector fatal + lift v0.11.0–v0.18.0 lessons (especially in-cycle re-process bump rule and dv-shim composition discipline) into `Docs/how.md` + `Docs/landmines.md` (promote to landmine #15+).
>
> **Why:** Restores `run-renderer.js` to clean exit; codifies eight cycles of institutional memory. The in-cycle re-process bump rule is load-bearing now (3-data-point precedent: v0.6.0 + v0.17.0 + v0.18.0).
>
> **Estimated scope:** 1 stage. Workshop bump 0.18.0 → 0.18.1 PATCH (or no bump — harness + docs only).

> [!example]- v0.19.0 FrontmatterMutator mechanism (alternative pattern extraction)
> **What:** Promote `FinanceFrontmatter.update(file, mutator)` to a shared `frontmatter-mutator` mechanism. Generic API (no finance-specific semantics).
>
> **Why:** Mirrors BeaconButton thinking. But finance is currently the ONLY blueprint using widget-driven mutation — promotion may be premature (wait for second blueprint to surface the need). Defer until trip status mutation, project board card state, or similar surfaces.

> [!example]- v0.19.0 styling blueprint (deferred per user)
> **What:** Ship a `styling` blueprint bundling theme + CSS-snippet + Style Settings + Baseline community theme. USER EXPLICITLY DEFERRED in v0.6.0 cycle; not yet reversed.

---

## Recommendation

> [!success] **v0.18.x.y new-meeting-button parity** — PRIMARY (clean-cycle / single-stage)
>
> **Why:** Carry-forward from BOTH v0.17.0 and v0.18.0 handoffs (now triple-reinforced by Smoke A user note). Zero novel mechanism — just a one-file adoption of the v0.18.0 BeaconButton pattern. Single-stage clean cycle precedent (matches v0.13.0/v0.14.0/v0.18.0 reserved-headroom-UNUSED). Could even bundle with workstream add/remove migration as a "v0.18.1 BeaconButton adoption sweep" (one PATCH cycle, two surfaces).
>
> **Combined option:** v0.18.1 BeaconButton adoption sweep — migrate new-meeting-button.js + workstream Add/Remove buttons in one cycle. Both are pure adoption of the v0.18.0 mechanism; no novel surface. Single-stage achievable. workshop 0.18.0 → 0.18.1 PATCH; meetings 0.2.0 → 0.2.1 PATCH; project 1.3.3 → 1.3.4 PATCH.
>
> **Skip for now:** docs polish + T4.0 (defer until docs-cleanup priority signaled — but in-cycle re-process bump rule promotion to landmine #15 is worth doing soon); FrontmatterMutator (premature; wait for second blueprint); styling (deferred by user).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper authoring tasks; master-driven on manifest mechanics + version bumps.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or mechanism API, QUOTE the literal call shape in the design doc.
> - **Apply v0.18.0 lessons proactively** — (a) in-cycle re-process bump rule means CF iterations MUST bump version even at the same `0.X.Y`; (b) when migrating user-visible widgets, plan for one UX-CF (cross-file UX coupling discovered late); (c) harness stub maintenance — when migrating helpers that depend on a new mechanism, add the stub to `makeFinanceCustomJsStub` (or analogous) at the same time.
> - **TDD discipline on harness extensions** — BB1-BB6 precedent (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **First-of-its-kind UI cycle pulls 2-6 CFs (6-data-point precedent: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 / v0.17.0).**
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0 + v0.18.0):** for cycles with strong precedent + zero novel mechanism surface, single-stage is the right shape. **3-data-point precedent now.**
> - **Decision rule:** new mechanism / new blueprint with substantial CustomJS surface → expect CFs; pure-mechanism patch OR near-clone-of-existing → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9, v0.19.0 landmine #15 candidate) — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1. v0.18.0 was THREE in-cycle bumps (finance 0.2.6 → 0.2.9: S2 atomic → CF-1 first → CF-1 refinement) — pattern strongly established. The installer's `subscribed.version === installed.version` short-circuit (install.js:111) skips re-process when version unchanged even if content changed.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **dv-shim composition discipline (v0.17.0 lesson)** — when composing a hub renderer that hands a sub-container to another customJS class, the shim MUST proxy every dv method that sub-class might call (`pages` + `current` + `el` minimum).
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs.
> - **BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers** (v0.11.0 CF-1 lesson). BeaconButton callers are inside CustomJS classes, not dataviewjs blocks — no wrapper needed.
> - **Per-blueprint version bumps require LOCKSTEP edits** to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11).
> - **BeaconCards = hubs only / bullet lists for tasks** (v0.12.0 bounded-cohesion thesis).
> - **Outline-accent action buttons go through `customJS.BeaconButton.render(...)`** (v0.18.0 lesson) — no more inline cssText + hover handlers for new buttons. Tone palette slot reserved for future additive tones; only "accent" valid at v0.18.0.
> - **InvoiceControls Save UX pattern (v0.18.0 CF-1)** — when widget Save can be the cross-file truth source, also recompute derived fields (e.g., amount = rate × hours) on save AND extend `disabled` state to consider derived-field drift so users can re-sync stale entities.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first.
> 3. Read v0.18.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (new-meeting-button parity primary; possibly bundled with workstream migration as v0.18.1 BeaconButton adoption sweep).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.18.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.18.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with primary recommendation (new-meeting-button parity OR combined v0.18.1 BeaconButton adoption sweep with workstream Add/Remove migration), constraints carried forward, and v0.18.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Note T4.0 carried 9 cycles; use selectors (empty, malformed, unknown-action, beacon-cards, date-aware, finance, beacon-button) for renderer harness sweeps. Renderer harness has 23 cases (6 BB NEW in v0.18.0).
3. Read v0.18.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with primary recommendation. v0.18.x.y new-meeting-button parity = single-stage clean candidate; v0.18.1 BeaconButton adoption sweep = bundled new-meeting-button + workstream Add/Remove migration in one PATCH cycle.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.18.0 lessons proactively: (a) in-cycle re-process bump rule means CF iterations MUST bump version; (b) outline-accent action buttons go through BeaconButton.render; (c) when migrating user-visible widgets, plan for one UX-CF; (d) harness stub maintenance for new mechanism deps.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget classes; master-driven on manifest + version-bump mechanics; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (6-data-point UI precedent for 2-6 inline CFs); 3-data-point reserved-headroom-UNUSED precedent (v0.13.0 + v0.14.0 + v0.18.0) for non-UI dedupe cycles.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 3-5 inline CFs + template-changing migration friction + per-entity sub-folder pattern + Templater auto-promote reusable pattern); v0.16.0 lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry); v0.17.0 lessons (dv-shim composition discipline; badges[].style outline; outline-accent action button pattern as BeaconButton candidate — DONE in v0.18.0; FrontmatterMutator candidate; first-of-its-kind UI cycle pulls 5 CFs; visual cohesion as CF magnet); v0.18.0 NEW lessons (in-cycle re-process bump rule at scale; mechanism-promotion-as-clean-cycle-shape; harness stub maintenance for new mechanism deps; reserved-headroom-UNUSED 3-data-point; tone palette slot reservation; cross-file UX coupling discovered late).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
