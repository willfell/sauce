---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.18.1 cycle. Triggered by v0.18.1 BeaconButton adoption sweep + nav-button label cleanup close + tag v0.18.1 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.18.1-beacon-button-adoption-sweep-result.md (v0.18.1 close — read first)
  - Docs/plans/2026-05-05-v0.18.1-beacon-button-adoption-sweep-design.md
  - Docs/plans/2026-05-05-v0.18.1-beacon-button-adoption-sweep-plan.md
  - Docs/plans/2026-05-05-v0.18.0-beacon-button-result.md
  - Docs/prompts/2026-05-05-post-v0.18.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; project Map sub-tasks bug at line 68-69 is now the surfaced top candidate)
gates:
  - v0.18.1 closed; tag v0.18.1 pushed; workshop manifest at 0.18.1
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - meetings bumped 0.2.0 → 0.2.1 (NewMeetingButton migrated; +beacon-button dep)
  - project bumped 1.3.3 → 1.3.4 (workstream Add/Remove migrated)
  - to-do bumped 0.1.2 → 0.1.4 (S1 label edit + CF-1 trailing-space trim)
  - journal bumped 0.1.0 → 0.1.1 (label edit only)
  - All mechanisms UNCHANGED
  - Helper-cases harness 255 sub-asserts (UNCHANGED)
  - Renderer harness 23 cases (UNCHANGED)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.18.x or v0.19.0 (cycle-dependent)
---

# Onboarding — post-v0.18.1 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `../accuris/` / `../headspace/` / `../ero/` — production reference vaults (sources for pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.18.1`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0` (all UNCHANGED in v0.18.1)
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, **`journal@0.1.1`** (BUMPED), **`meetings@0.2.1`** (BUMPED), **`project@1.3.4`** (BUMPED), **`to-do@0.1.4`** (BUMPED 2 in-cycle bumps), `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.18.0` **`v0.18.1`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED); `run-renderer.js` 23 cases (UNCHANGED); pre-existing T4.0 fatal still carried (use selectors)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 3 paths; helper count: 4

**v0.18.1 commits in order (top-down):**
- `<TBD>` docs(v0.18.1): cycle close — adoption sweep result + status snapshot + post-v0.18.1 handoff (THIS commit upcoming)
- `5daba3b` fix(to-do): v0.18.1 CF-1 — drop trailing space after [ ] in Today To-Do template
- `f09311b` feat(v0.18.1): BeaconButton adoption sweep + nav-button label cleanup
- `aa8e76d` docs(v0.18.1): BeaconButton adoption sweep implementation plan
- `b946b84` docs(v0.18.1): BeaconButton adoption sweep + nav-button label cleanup design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.18.1 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -8` — expect 5 v0.18.1 cycle commits (close + CF-1 + S1 + plan + design) + 3 prior (v0.18.0 close + ...).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `blueprints i want.md` + the `.obsidian/community-plugins.json` + `Docs/Meta/Scripts/cards/beacon-cards.js` install-drift M).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.18` → `v0.18.0` + `v0.18.1` both present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.18.1:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.18.1"`. Confirm meetings `"0.2.1"`, project `"1.3.4"`, to-do `"0.1.4"`, journal `"0.1.1"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`, `beacon-button`) for regression sweeps. **T4.0 carried-forward 10 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors.
>    - All 7 BC + 2 DA + 8 FF + 6 BB cases green.
> 9. **Barebones state:** subscription pinned to meetings `0.2.1`, project `1.3.4`, to-do `0.1.4`, journal `0.1.1`. Install green + idempotent.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.18.1-beacon-button-adoption-sweep-result.md`** — most recent cycle close. Captures: 1 in-cycle CF (CF-1 to-do template trailing-space trim); 3 BeaconButton migration sites + 2 label edits; 4 NEW v0.18.1 lessons (pure adoption clean first-pass, trailing-whitespace defect class, 4-data-point reserved-headroom-UNUSED precedent, combined cycle bundle pattern).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.18.1 close.
> 4. **`blueprints i want.md`** — user's wishlist (post v0.18.1 close):
>    - REMOVE: new-meeting-button parity — DONE in v0.18.1
>    - PROMOTE: line 68-69 Project Map sub-tasks-as-unassigned bug — TOP candidate for next cycle
>    - KEEP: styling (line 18-67; explicitly DEFERRED)
>    - KEEP: meeting cards on daily (verify whether v0.12.0 already satisfied)
> 5. **`Docs/landmines.md`** — 14 entries unchanged in v0.18.1; **v0.19.0 landmine candidate:** in-cycle re-process bump rule (gotcha 9) is now load-bearing across v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1. Strong enough to graduate to numbered landmine #15.
> 6. **`Docs/how.md`** — nine cycles of lessons NOT YET codified (v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.17.0 / v0.18.0 / v0.18.1); candidate for a docs polish cycle.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.19.0 Project Map sub-tasks bug fix **(PRIMARY recommendation — surfaces user's wishlist)**
> **What:** Fix `blueprints i want.md` line 68-69 — sub-tasks (notes promoted to folder-style under a project's Kanban board card) appear in Project Map's task list as 'unassigned'. Filter the Map task listing to exclude sub-tasks (likely path-prefix or etag-shape check in `ProjectWorkstreams` and/or `Project Map` template).
>
> **Why:** Concrete user-facing bug carried in `blueprints i want.md` since at least the v0.5.0 project port. Now top-of-the-list with new-meeting-button + workstream Add/Remove polish closed in v0.18.1.
>
> **Estimated scope:** 1-2 stages. Workshop bump 0.18.1 → 0.18.2 PATCH (or 0.19.0 MINOR depending on filter mechanism shape). project@1.3.4 → 1.3.5 PATCH likely (filter-only edit).
>
> **Open questions for brainstorming:** Where is the filter (atlas template's dataviewjs query? ProjectWorkstreams class?)? Is the existing `etags.includes("#project")` filter (used by ProjectsHubCards per v0.11.0 status snapshot) reusable here, or do sub-tasks need a different identifier? Does a sub-task's frontmatter carry a parent-task wikilink we can detect?

> [!example]- v0.19.0 FrontmatterMutator mechanism (alternative pattern extraction)
> **What:** Promote `FinanceFrontmatter.update(file, mutator)` to a shared `frontmatter-mutator` mechanism. Generic API (no finance-specific semantics). Mirror v0.18.0's BeaconButton-promotion playbook.
>
> **Why:** Mirrors BeaconButton thinking. But finance is currently the ONLY blueprint using widget-driven mutation — promotion may be premature (wait for second blueprint to surface the need). The Project Map fix may itself surface a frontmatter-mutator use case (sub-task parent-link write?).

> [!example]- v0.19.0 docs polish + T4.0 cleanup + landmine #15 promotion
> **What:** Fix the 10-cycles-old run-renderer.js no-selector fatal + lift v0.11.0–v0.18.1 lessons (especially in-cycle re-process bump rule) into `Docs/how.md` + `Docs/landmines.md` (promote in-cycle re-process bump rule to landmine #15).
>
> **Why:** Restores `run-renderer.js` to clean exit; codifies nine cycles of institutional memory. The in-cycle re-process bump rule is load-bearing now (4-data-point precedent: v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1).
>
> **Estimated scope:** 1 stage. Workshop bump 0.18.1 → 0.18.2 PATCH (or no bump — harness + docs only).

> [!example]- v0.19.0 template trailing-whitespace lint
> **What:** Extend `run-helper-cases.js` to grep all blueprint template bodies for trailing-whitespace defects (the v0.18.1 CF-1 defect class).
>
> **Why:** Prevents the trailing-space-causes-blank-task class of bugs from recurring. Small additive harness extension; +1 sub-assert (or +N where N = template count). Could be bundled with docs polish.

> [!example]- v0.19.0 styling blueprint (deferred per user)
> **What:** Ship a `styling` blueprint bundling theme + CSS-snippet + Style Settings + Baseline community theme. USER EXPLICITLY DEFERRED in v0.6.0 cycle; not yet reversed.

> [!example]- v0.19.0 misc finance polish (carry from v0.17.0+v0.18.0)
> **What:** (a) Migration helper for pre-v0.17.0 finance entities (one-shot Templater command to inject editor blocks); (b) Budget categories aggregate row (Total Planned / Total Actual at bottom of editor); (c) PaycheckExpensesEditor "All Paid" toggle.
>
> **Why:** Carry from v0.17.0/v0.18.0 handoffs. Each is small.

---

## Recommendation

> [!success] **v0.19.0 Project Map sub-tasks bug fix** — PRIMARY (concrete user-facing bug)
>
> **Why:** Carry-forward from `blueprints i want.md` since at least v0.5.0; user-flagged. With the v0.18.1 polish complete (new-meeting-button + workstream + label cleanup), the wishlist's most concrete remaining item is this Map filter bug. Investigation-then-fix shape (need to read `Project Map.md` template + `ProjectWorkstreams` to locate the task-listing query first). Likely PATCH cycle (0.18.1 → 0.18.2) if filter is a clean predicate addition; could pull a new helper if the parent-link detection is non-trivial.
>
> **Skip for now:** docs polish + T4.0 (defer until docs-cleanup priority signaled — but in-cycle re-process bump rule promotion to landmine #15 is worth doing soon and could be bundled); FrontmatterMutator (premature; wait for second blueprint); styling (deferred by user); misc finance polish (smaller carry; bundle when there's a 0.19.x window).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper authoring tasks; master-driven on manifest mechanics + version bumps.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or mechanism API, QUOTE the literal call shape in the design doc.
> - **Apply v0.18.1 lessons proactively** — (a) pure adoption / pure dedupe / pure label edit cycles reliably land single-stage clean (4-data-point precedent); (b) trailing-whitespace defects in templates can survive cycles undetected — grep before merge; (c) combined cycles (two unrelated micro-tasks in one PATCH) are bundle-friendly when smoke matrix doesn't grow significantly; (d) the v0.18.0 in-cycle re-process bump rule (gotcha 9) reinforces every cycle with CF iterations — bump version even at the same `0.X.Y` to force re-process.
> - **TDD discipline on harness extensions** — BB1-BB6 precedent (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **First-of-its-kind UI cycle pulls 2-6 CFs (6-data-point precedent: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 / v0.17.0).**
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1):** for cycles with strong precedent + zero novel mechanism surface, single-stage is the right shape. **4-data-point precedent now.**
> - **Decision rule:** new mechanism / new blueprint with substantial CustomJS surface → expect CFs; pure-mechanism patch OR near-clone-of-existing OR pure adoption sweep → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9, v0.19.0 landmine #15 candidate) — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1. v0.18.1 was a 2-bump stack to-do 0.1.2 → 0.1.3 → 0.1.4 (S1 label + CF-1 trim). v0.18.0 was 3-bump stack finance 0.2.6 → 0.2.7 → 0.2.8 → 0.2.9. The installer's `subscribed.version === installed.version` short-circuit (install.js:111) skips re-process when version unchanged even if content changed.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **dv-shim composition discipline (v0.17.0 lesson)** — when composing a hub renderer that hands a sub-container to another customJS class, the shim MUST proxy every dv method that sub-class might call (`pages` + `current` + `el` minimum).
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs. v0.18.1's clean first-pass on T1.1 + T1.2 reinforces this.
> - **BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers** (v0.11.0 CF-1 lesson). BeaconButton callers are inside CustomJS classes, not dataviewjs blocks — no wrapper needed.
> - **Per-blueprint version bumps require LOCKSTEP edits** to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11).
> - **BeaconCards = hubs only / bullet lists for tasks** (v0.12.0 bounded-cohesion thesis).
> - **Outline-accent action buttons go through `customJS.BeaconButton.render(...)`** (v0.18.0 lesson) — no more inline cssText + hover handlers for new buttons. Tone palette slot reserved for future additive tones; only "accent" valid at v0.18.1.
> - **InvoiceControls Save UX pattern (v0.18.0 CF-1)** — when widget Save can be the cross-file truth source, also recompute derived fields (e.g., amount = rate × hours) on save AND extend `disabled` state to consider derived-field drift so users can re-sync stale entities.
> - **Trailing-whitespace template defect class (v0.18.1 CF-1)** — grep blueprint template bodies for trailing whitespace before merge (`grep -n " $" templates/*.md`); v0.19.0 candidate to extend helper-cases harness for this lint.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first.
> 3. Read v0.18.1 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (Project Map sub-tasks bug fix as primary).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.18.1 next cycle. Read Docs/prompts/2026-05-05-post-v0.18.1-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with primary recommendation (Project Map sub-tasks bug fix from blueprints i want.md line 68-69), constraints carried forward, and v0.18.1 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Note T4.0 carried 10 cycles; use selectors (empty, malformed, unknown-action, beacon-cards, date-aware, finance, beacon-button) for renderer harness sweeps. Renderer harness has 23 cases.
3. Read v0.18.1 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with primary recommendation. v0.19.0 Project Map sub-tasks bug fix = primary; v0.19.0 docs polish + T4.0 + landmine #15 promotion = secondary; FrontmatterMutator promotion = tertiary (premature); styling deferred.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.18.1 lessons proactively: (a) pure adoption / pure dedupe land single-stage clean; (b) template trailing-whitespace lint is a v0.19.0 carry; (c) combined cycles bundle-friendly; (d) in-cycle re-process bump rule still load-bearing.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget classes; master-driven on manifest + version-bump mechanics; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (6-data-point UI precedent for 2-6 inline CFs); 4-data-point reserved-headroom-UNUSED precedent (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1) for non-UI dedupe / pure adoption / pure label cycles.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 3-5 inline CFs + template-changing migration friction + per-entity sub-folder pattern + Templater auto-promote reusable pattern); v0.16.0 lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry); v0.17.0 lessons (dv-shim composition discipline; badges[].style outline; outline-accent action button pattern as BeaconButton candidate — DONE in v0.18.0); v0.18.0 lessons (in-cycle re-process bump rule at scale; mechanism-promotion-as-clean-cycle-shape; harness stub maintenance; reserved-headroom-UNUSED 3-data-point; tone palette slot reservation; cross-file UX coupling discovered late); v0.18.1 NEW lessons (pure adoption clean first-pass via API-contract quoting; trailing-whitespace template defect class; reserved-headroom-UNUSED 4-data-point precedent; combined cycles are bundle-friendly).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
