# Next-session handoff — post v0.112.0

**Paste this into the next session as the opening prompt.**

You're picking up after **v0.112.0** closed (workshop tooling MINOR — 4 additive artifacts cutting onboarding tax). Workshop at **0.112.0**, finance at **0.7.2**, base commit (TBD — set after S7 commit).

## Try the tooling first

The whole point of v0.112.0 was to replace the 5-10 tool-call onboarding scan with one command:

```bash
npm run status
```

If that works as expected, you have the full state picture and can skip the rest of this handoff for context. Read on for the decisions outstanding.

## Outstanding decisions for the user

1. **Push + tag v0.112.0?** Local main is ahead of origin/main. After preflight + dogfood green, push is the natural next step. Tag `v0.112.0` is user-gated per ask-before-acting.
2. **Pick the next lane.** Options from the still-open v0.111.x brainstorm at `Docs/plans/2026-06-15-v0.111.x-enhancements-brainstorm.md`:
   - **Bundle 2 — finance cohesion polish** (#1 debt-payment exclusion + #3 click-through + #5 FinanceYearReview annual band). Closes MonthlyOverview math correctness + adds annual perspective.
   - **Brew tap formula bump** (#17). Stale at v0.84.0 since ~28 cycles ago.
   - **`sauce doctor --finance`** (#14). Per-blueprint health check.
   - **cowork:doctor** (#18). Displaced for the 10th cycle.
3. **v0.112.x PATCH candidates** (from v0.112.0 result doc):
   - Full-history backfill in regen-cycle-status.
   - `.gitignore` cleanup (replace `/Scripts/` with specific paths to stop macOS case-insensitive collision).
   - `regen-cycle-status --check` wired into release:preflight as a CI gate.

## Read first, in order

1. `Docs/plans/2026-06-15-v0.112.0-workshop-tooling-result.md` — what just shipped + 5 lessons.
2. `Docs/agent-guides/dev-workflow.md` — NEW; the operational layer between build-test-verify.md and vault-paths.md.
3. `Docs/plans/2026-06-15-v0.111.x-enhancements-brainstorm.md` — open lane picker.
4. `Docs/agent-guides/cycle-status.md` — auto-regenerated at S7; should now reflect v0.112.0 as current.
5. `Docs/agent-guides/build-test-verify.md` — preflight + release workflow (unchanged).
6. `Docs/landmines.md` — non-negotiable traps (unchanged).

## What you should NOT do unprompted

- **Do not push to origin/main without user confirmation.** Workshop just bumped to v0.112.0 locally; the user gates the push.
- **Do not tag v0.112.0 without explicit approval.** Tags are ship-points; user-gated per ask-before-acting.
- **Do not start a new cycle without picking a lane.** The brainstorm is open — picking turns it into a brief.
- **Do not modify `Docs/agent-guides/cycle-status.md` directly** — that's what `npm run regen-cycle-status` is for. Edit the source result doc + re-run.

## Quick state-check commands

```bash
# All-in-one workshop state
npm run status

# Is cycle-status fresh?
npm run regen-cycle-status -- --check

# Did anything regress?
npm run release:preflight 2>&1 | grep -E "^Result:|FAIL "

# Behavioral harnesses
node platform/test/run-v01103-monthly-overview.js 2>&1 | tail -3
```

Expected: `npm run status` succeeds; `--check` exits 0; preflight all green; behavioral 91/0.

## Decision trees if user is vague

- **"Just continue with whatever you think makes sense."** → Try the new tooling end-to-end one more time (`npm run status` + `npm run regen-cycle-status -- --check`), confirm it feels useful, then open Bundle 2 (finance cohesion polish) with `superpowers:brainstorming` — that's the highest-user-value lane left.
- **"Pick the lowest risk thing."** → v0.112.1 PATCH: `.gitignore` cleanup (replace `/Scripts/` with specific paths). ~30min. Clears the recurring case-insensitive footgun.
- **"Push and tag what we have first."** → Confirm git state, ask which tag(s) to create (v0.110.3 / v0.111.3 / v0.112.0 are all candidates), then `git push` + `git tag -a` per build-test-verify.md.
- **"What's the highest user-impact lane?"** → Bundle 2 cycle #1 (debt-payment category exclusion semantics). MonthlyOverview's math is currently correct ONLY if the user disciplines budget categories — if they double-count debt payments in both `categories[*].actual` and `expenses[*].debt+paid`, the headline lies. Closing this is the difference between "useful most of the time" and "trustworthy."

## Reference: the four new tooling artifacts

| Script | Use it for |
|---|---|
| `npm run status` | First command of every session. Workshop state in <250ms. |
| `npm run regen-cycle-status` | At cycle close, after writing the result doc. Idempotent. `--check` for CI gate. |
| `npm run scaffold-harness -- <cycle-id> <topic>` | When starting a per-cycle behavioral harness. Canonical template. |
| `scripts/dev-sync.sh` | End-of-cycle multi-consumer sync (existing; unchanged). |

| Guide | Reach for it when |
|---|---|
| `Docs/agent-guides/dev-workflow.md` | NEW; first session, picking up a cycle, or any `scripts/*` confusion. |
| `Docs/agent-guides/build-test-verify.md` | Cycle close protocol; preflight; release workflow. |
| `Docs/agent-guides/cycle-status.md` | Live state pointer (auto-regenerated). |

End of handoff prompt.
