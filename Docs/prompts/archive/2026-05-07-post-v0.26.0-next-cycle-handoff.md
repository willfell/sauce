---
date: 2026-05-07
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.26.0 cycle. Triggered by v0.26.0 close + tag v0.26.0 on origin/main (first-run robustness — wizard double-wrap fix + scaffoldFoundationalPluginData + convenience default + ranch/ lowercase sweep + new landmine #19).
predecessors:
  - Docs/plans/2026-05-07-v0.26.0-first-run-robustness-result.md (most recent close — read first)
  - Docs/plans/2026-05-07-v0.26.0-first-run-robustness-plan.md
  - Docs/plans/2026-05-07-v0.26.0-first-run-robustness-design.md
  - Docs/prompts/2026-05-07-post-v0.25.0-next-cycle-handoff.md (predecessor handoff)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol)
gates:
  - v0.26.0 closed; tag v0.26.0 pushed; workshop manifest at 0.26.0
  - All 4 P0 items shipped (wizard fix, scaffoldFoundationalPluginData, convenience default, ranch/ lowercase sweep)
  - 2 in-cycle CFs surfaced + fixed pre-close (CF-1 scaffold-shape; CF-2 vault_identity_tag numbers-only)
  - Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED
  - Allowlist #12 UNCHANGED at 12 paths
  - Helper count 10 → 11 (`scaffoldFoundationalPluginData` NEW)
  - Landmines list 18 → 19 (#19 NEW — platform-managed dirs lowercase)
  - macOS APFS acceptance gate PASSED on fresh `<workshop>/scratch/11/` (manual Obsidian smoke user-confirmed all 8 blueprints' nav-buttons fire, daily Templater placeholders resolve, hotkeys bound, DataviewJS works, tags YAML valid)
  - Reserved-headroom precedent: 13 data points; 3rd MIXED case (after v0.22.1, v0.23.0)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.26.1 (or other)
---

# Onboarding — post-v0.26.0 next-cycle selection

> [!info] Project identity (post-v0.26.0)
> - **Project name:** `sauce` (Sauce rebrand sequence COMPLETE since v0.25.0)
> - **GitHub remote:** `git@github-personal:willfell/sauce.git`
> - **Local workshop dev path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (host filesystem path; not yet renamed; standalone v0.26.x housekeeping carry)
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target (legacy sibling-of-workshop layout; `../beacon` workshop_relative_path)
>   - `../accuris-beacon-poc/` — secondary consumer (legacy layout; pre-existing version-pin drift)
>   - `../scratch/2/` … `../scratch/10/` — historical smoke targets
>   - `../scratch/11/` — v0.26.0 smoke target (post-CF-2: vault_identity_tag = "vault-11"; ALL 8 blueprints + 8 mechanisms installed; ranch/ all lowercase; daily Templater + hotkeys + DataviewJS user-confirmed PASS)

---

## Workshop state at session start

- `workshop_version: 0.26.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `accent-button@0.1.0`, `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.22.1` `v0.23.0` `v0.24.0` `v0.25.0` `v0.26.0` (NEW)
- Harness counts: bootstrap 45/0, cli 27/0 (was 20/0; +1 case C15 in v0.26.0), install-sh 14/0, helper-cases 407/0 (was 387/0; +5 cases SF1-SF5 in v0.26.0), renderer exit 0
- Allowlist (landmine #12): **12 paths** (UNCHANGED in v0.26.0)
- Landmines: **19 entries** (UPDATED in v0.26.0; #19 NEW codifying lowercase platform-managed dirs)
- Helper count: **11** (v0.26.0 added `scaffoldFoundationalPluginData`)
- **Stub md5 invariant: `ea23aa812503bfca66359d3b2b239ba8`** (UNCHANGED in v0.26.0)
- **Backup-suffix convention: `.sauce-backup`** (UNCHANGED since v0.25.0)
- **Platform-managed directory casing:** lowercase under `pantry/`, `ranch/`, `spice/` (codified in landmine #19; v0.26.0 lowercase sweep COMPLETE)

**v0.26.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.26.0): post-v0.26.0 next-cycle handoff (THIS commit)
- `0d7cec8` docs(v0.26.0): cycle close + workshop_version → 0.26.0 + result writeup
- `c0a622c` fix(v0.26.0): CF-2 — daily note tags Type Mismatch + numbers-only vault_identity_tag
- `9065f5c` fix(v0.26.0): CF-1 — scaffold templater data.json with all array fields downstream helpers expect
- `dea7c41` fix(v0.26.0): S3 follow-up — ungitignore + untrack .bak / dogfood drift
- `fb092a0` feat(v0.26.0): S3 — lowercase sweep ranch/{Templater,Scripts,Templates,Views,Content} → lowercase
- `5a4d630` feat(v0.26.0): S2 — wizard double-wrap fix + scaffoldFoundationalPluginData + convenience default
- `4bbfda6` test(v0.26.0): S1 TDD-RED baseline — wizard double-wrap, scaffoldFoundationalPluginData, convenience-default, lowercase
- `a0ea00a` docs(v0.26.0): first-run robustness implementation plan
- `02389bd` docs(v0.26.0): first-run robustness design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git remote -v` → `origin git@github-personal:willfell/sauce.git`
> 3. `git log --oneline -10` — expect ~10 v0.26.0 cycle commits + handoff at top
> 4. `git status` → only pre-existing drift (`.obsidian/*`, `blueprints i want.md`, `features-im-brainstorming/`, `install-test-output.md`, `Docs/Meta/Scripts/cards/beacon-cards.js.bak`-equivalent if any)
> 5. `git ls-remote --tags origin | grep v0.26.0` → 2 lines (annotated tag + dereference)
> 6. **Stub md5 invariant** (4 paths must match `ea23aa812503bfca66359d3b2b239ba8`): `md5 platform/installer-stub.js ranch/templater/platformInstall.js ../barebones-beacon-poc/ranch/templater/platformInstall.js ../accuris-beacon-poc/ranch/templater/platformInstall.js`
> 7. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.26.0"`
> 8. `find pantry ranch spice -type d 2>/dev/null | grep -E '/[A-Z]'` → empty (lowercase invariant per landmine #19)
> 9. **All 5 harnesses GREEN:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 27 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 407 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol
> 2. **`Docs/plans/2026-05-07-v0.26.0-first-run-robustness-result.md`** — most recent cycle close. NEW lessons (a–h); reserved-headroom precedent extends to 13 data points (3rd MIXED case)
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.26.0 close
> 4. **`Docs/install.md`** — read the v0.25.x → v0.26.0 upgrade section
> 5. **`Docs/landmines.md`** — 19 entries; #19 NEW (platform-managed dirs lowercase + OUT-of-scope exceptions)
> 6. **`blueprints i want.md`** — pending feature carries
> 7. **`features-im-brainstorming/sticky-notes.md`** — queued blueprint candidate

---

## 🟡 PRIMARY candidates

### A. v0.26.1 — P1 carries (recommended next per v0.26.0 cycle scope decision)

**What:** Per the v0.26.0 brainstorm scope decision, P1 items deferred to v0.26.1:
- **P1-1** community-plugin baseline expansion (vendor + auto-enable: admonition, calendar, tasks, paste-url-into-selection)
- **P1-2** Daily-Notes "open today's daily note in new tab" wiring (decide Homepage community plugin OR core newFileLocation setting)
- **P1-3** `sauce help` verb + root-anchored `/Scripts/` line in workshop `.gitignore` + `sauce status` warns when blueprints depend on convenience but absent

**Cycle shape estimate:** 3-4 stages. PATCH or MINOR. 2-4 CFs likely (per first-of-its-kind precedent for new external_plugin vendoring).

### B. v0.26.2 — Sticky-notes blueprint (queued per post-v0.25.0 handoff E)

**What:** New blueprint at `spice/sticky-notes/<YYYY-MM-DD>/` per `features-im-brainstorming/sticky-notes.md` user spec. Cmd+0 hotkey opens day's hub; "New Note" button creates ad-hoc sticky notes. Reuses `runTemplaterTemplate` action type + nav-button pattern from v0.4.0 to-do blueprint.

**Cycle shape estimate:** New blueprint — 3 stages. Possibly clean (zero CFs) or 1-2 CFs.

### C. Pantry-clone source mutation investigation (v0.26.0 carry)

**What:** v0.26.0 surfaced that install.js writes back to template source files inside `<vault>/pantry/` (workshop clone) during install. Workshop source remains intact. Mutation self-heals on next `sauce update --force`. May be related to Templater's `overwrite_file_commands` API call sequence or a stale write-side-effect.

**Cycle shape estimate:** Investigation cycle. 2-3 stages. Could surface unexpected scope or could be quick.

### D. Architectural `install.js` refactor — `NO_OP_DUE_TO_MISSING_PREREQ` signaling

**What:** When a helper short-circuits because a prereq is missing, the helper currently runs without writing files but the orchestrator still records `installed_at` for the item. Refactor: helpers explicitly signal `NO_OP_DUE_TO_MISSING_PREREQ` back to the orchestrator; orchestrator skips writing `installed_at` when nothing was materialized.

**Carry from:** v0.21.0 + v0.22.0 + v0.24.0 result writeups; deferred again in v0.25.0 + v0.26.0.

**Cycle shape estimate:** Substantial. 4-5 stages.

### E. Project Sub-tasks panel — UI cycle

**What:** Render a "Sub-tasks" panel on each Task hub page showing the task's sub-task cards.

**Carry from:** post-v0.18.2 result writeup (carried since).

**Cycle shape estimate:** UI cycle — 3-4 stages with 2-4 inline-CFs typical.

### F. Multi-theme presets — DEFERRED 7th time

**What:** Schema upgrade `style_settings_defaults_src` (single string) → `style_presets[]` keyed by name. DEFERRED 7 times now.

**Cycle shape estimate:** First-of-its-kind schema-extension cycle. MINOR bump. 4 stages with 2-4 CFs.

### G. Local workshop dev path rename + dogfood drift housekeeping

**What:** Rename `/Users/willfell/Documents/obsidian/sync/workshop/beacon` → `/workshop/sauce`. Pure host-filesystem rename. Could bundle dogfood drift cleanup.

**Cycle shape estimate:** PATCH cycle. 1-2 stages. Trivial — pure mv + sed.

---

## Recommendation

> [!info] Recommended order
> v0.26.1 = (A) P1 carries. The v0.26.0 brainstorm explicitly scoped P0 only with P1 deferred to v0.26.1; following that sequence is the cleanest forward.
> v0.26.2 = (B) sticky-notes per post-v0.25.0 handoff.
> v0.26.x candidates = (C) pantry-clone investigation, (D) install.js refactor, (E) project Sub-tasks, (G) housekeeping.
> v0.27.0+ = (F) multi-theme — commit or de-queue if 8th DEFERRED happens.

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock cycle scope.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on mechanical edits + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any literal call-site that changes, QUOTE it in the design.
> - **Two-stage subagent review** (v0.20.0; reaffirmed v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0 + **v0.26.0 — 6 data points**). Never skip either.
> - **Manual smoke is the close gate** for any user-facing surface.
> - Push to origin/main after each stage commit; annotated tag at cycle close (REQUIRES user approval).
> - **Acceptance smoke target naming:** rotate adversarial inputs (numbers-only basenames, special chars, etc.) per v0.26.0 lesson c.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; 6 data points reaffirmation)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **Module-directory invariant (#11)** — `spice/<module>/` namespace; one-module-per-blueprint rule unchanged.
> - **Inside-vault clone is git-managed (#18)** — `pantry/` is the canonical name; preserve.
> - **Allowlist 12 paths (#12)** — adding 13th+ requires explicit user approval. Backup-suffix `.sauce-backup`.
> - **Stub content-static (#13)** — never re-edit any `ranch/templater/platformInstall.js` body. Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED.
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **Platform-managed dirs lowercase (#19 NEW v0.26.0)** — `pantry/`, `ranch/`, `spice/` subdirs MUST be lowercase. EXCEPTIONS enumerated in #19 (MM-MMMM, assets/themes/<ThemeName>/, user-facing note filenames, historical Docs/plans + Docs/prompts).
> - **Scaffold helpers must include EVERY field downstream helpers read (v0.26.0 lesson a)** — when authoring new scaffold registries, grep downstream helper bodies for every field they consume.
> - **Numbers-only inputs need explicit handling at every layer (v0.26.0 lesson b)** — `_deriveVaultIdentityTag` is the canonical pattern for tag-from-display-name derivation.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, allowlist expansion (13th path), creating top-level vault directory, any `.obsidian/` edit outside #12 allowlist.

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. stub md5 + harness counts + lowercase audit).
> 2. Read v0.26.0 result writeup (NEW lessons a-h).
> 3. Surface candidates A-G via `AskUserQuestion`. Recommended next: (A) P1 carries → v0.26.1, (B) sticky-notes → v0.26.2.
> 4. Once scope is locked, brainstorm.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.26.0 next cycle. Read Docs/prompts/2026-05-07-post-v0.26.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity (Sauce rebrand COMPLETE; pantry/ + ranch/ + spice/ namespace tripod canonical; lowercase platform-managed dirs codified in landmine #19; CLI binary `sauce`; env vars SAUCE_*), pre-flight checks (workshop_version 0.26.0; stub md5 ea23aa812503bfca66359d3b2b239ba8 unchanged; allowlist 12 paths; helper count 11; bootstrap 45/0, cli 27/0, install-sh 14/0, helper-cases 407/0, renderer green; landmines 19 entries), candidate cycles A-G (v0.26.1 P1 carries recommended; v0.26.2 sticky-notes; v0.26.x carry items including pantry-clone source mutation investigation), constraints carried forward, and the v0.26.0 NEW lessons (a-h).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (host filesystem path).
2. Run pre-flight checks. Workshop_version 0.26.0; remote git@github-personal:willfell/sauce.git; stub md5 ea23aa812503bfca66359d3b2b239ba8 (unchanged from v0.24.0).
3. Read v0.26.0 result writeup + CLAUDE.md status snapshot + landmines (19 entries; #19 NEW in v0.26.0).
4. Surface candidates via AskUserQuestion. Recommended ranking: (A) v0.26.1 P1 carries > (B) v0.26.2 sticky-notes > (C) pantry-clone investigation > (D) install.js refactor > (E) project Sub-tasks > (G) housekeeping > (F) multi-theme presets DEFERRED 7th time.
5. Once locked, brainstorm.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on mechanical edits + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Apply v0.26.0 lessons at design-time:
- Scaffold helpers must include EVERY field downstream helpers read (lesson a; relevant for any future scaffolding-style additions).
- Numbers-only inputs need explicit handling at every layer (lesson b; _deriveVaultIdentityTag pattern reusable).
- Acceptance smoke against literally-named adversarial vault dirs surfaces edge cases (lesson c; rotate smoke-target naming).
- Two-stage subagent review reaffirmed at 6 data points; never skip (lesson d).
- Pantry-clone source mutation is a separate latent bug (lesson e; carry to v0.26.x).
- In-cycle re-process bump rule (#16) applies when version bumps mid-stream (lesson f).
- Reserved-headroom precedent extends to 13 data points; 3rd MIXED case (lesson g).
- Explicit `git add <files>` preferred over `git add -A` for atomic commits (lesson h).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13; md5 ea23aa812503bfca66359d3b2b239ba8 holds across v0.27.0+); landmine #12 allowlist expansion (13th+ path) requires approval; landmine #11 module-directory invariant: `spice/<module>/` is canonical; landmine #19 platform-managed dirs lowercase; two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0 + v0.26.0 reaffirmation).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
