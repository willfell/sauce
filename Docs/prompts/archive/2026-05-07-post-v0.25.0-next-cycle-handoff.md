---
date: 2026-05-07
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.25.0 cycle. Triggered by v0.25.0 close + tag v0.25.0 on origin/main (Tree 2 module-namespace rename `beacon/<module>/` → `spice/<module>/` + `.beacon-backup` → `.sauce-backup` suffix rename). Sauce rebrand sequence COMPLETE.
predecessors:
  - Docs/plans/2026-05-07-v0.25.0-tree2-namespace-rename-result.md (most recent close — read first)
  - Docs/plans/2026-05-07-v0.25.0-tree2-namespace-rename-plan.md
  - Docs/plans/2026-05-07-v0.25.0-tree2-namespace-rename-design.md
  - Docs/prompts/2026-05-07-post-v0.24.0-next-cycle-handoff.md (predecessor handoff)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol)
gates:
  - v0.25.0 closed; tag v0.25.0 pushed; workshop manifest at 0.25.0
  - Tree 2 module-namespace rename `beacon/<module>/` → `spice/<module>/` shipped (workshop self-state — no module dirs; consumer scratch/7 fresh smoke; barebones beacon/ wiped pending Obsidian-runtime reinstall)
  - `.beacon-backup` → `.sauce-backup` suffix rename shipped (17 install.js sites + bootstrap layer + Docs)
  - Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED (Tree 2 doesn't touch stub body)
  - Allowlist #12 UNCHANGED at 12 paths
  - Helper count UNCHANGED at 10
  - Landmines list UNCHANGED at 18 entries (#11 + #12 wording amended)
  - macOS APFS Daily Notes acceptance gate PASSED on fresh `<workshop>/scratch/7/`
  - Sauce rebrand sequence COMPLETE: `pantry/` + `ranch/` + `spice/` namespace tripod is the canonical layout
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.26.0 (or other)
---

# Onboarding — post-v0.25.0 next-cycle selection

> [!info] Project identity (post-v0.25.0)
> - **Project name:** `sauce` (rebranded in v0.23.0; rebrand sequence COMPLETE in v0.25.0)
> - **GitHub remote:** `git@github-personal:willfell/sauce.git`
> - **Local workshop dev path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (host filesystem path; not yet renamed; standalone v0.25.x housekeeping carry)
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target (legacy sibling-of-workshop layout; `../beacon` workshop_relative_path; `beacon/` tree wiped post-v0.25.0; Obsidian-runtime install will materialize `spice/` on next platform-install)
>   - `../accuris-beacon-poc/` — secondary consumer (legacy layout; pre-existing version-pin drift on nav-buttons + project; no `beacon/` tree)
>   - `../scratch/2/` … `../scratch/6/` — historical smoke targets
>   - `../scratch/7/` — v0.25.0 smoke target (post-rename: `pantry/` + `ranch/` + `spice/` namespace tripod; ALL 8 blueprints + 8 mechanisms installed; Daily Notes acceptance gate PASSED)

---

## Workshop state at session start

- `workshop_version: 0.25.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `accent-button@0.1.0` (renamed from `beacon-button` in v0.24.0), `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.22.1` `v0.23.0` `v0.24.0` `v0.25.0` (NEW)
- Harness counts: bootstrap 45 / cli 20 / install-sh 14 / helper-cases 387 / renderer 23 — all green
- Allowlist (landmine #12): **12 paths** (UNCHANGED in v0.25.0; backup-suffix renamed `.beacon-backup` → `.sauce-backup`)
- Landmines: **18 entries** (UNCHANGED count; #11 + #12 wording amended in v0.25.0)
- Helper count: **10** (UNCHANGED in v0.25.0)
- **Stub md5 invariant: `ea23aa812503bfca66359d3b2b239ba8`** (UNCHANGED in v0.25.0; Tree 2 doesn't touch stub body; stub reads `ranch/platform-config.json` regardless of module-dir name)
- **Backup-suffix convention: `.sauce-backup`** (renamed from `.beacon-backup` in v0.25.0; v0.23.0 + v0.24.0 "preserve" lessons retired under fresh-start posture)

**v0.25.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.25.0): post-v0.25.0 next-cycle handoff (THIS commit)
- `<TBD>` docs(v0.25.0): cycle close + workshop_version → 0.25.0 + result writeup
- `88fc46c` feat(v0.25.0): S2 — beacon/<module>/ → spice/<module>/ + .beacon-backup → .sauce-backup
- `e379436` test(v0.25.0): S1 TDD-RED — spice/ module namespace + .sauce-backup expectations
- `69f1812` docs(v0.25.0): tree 2 module-namespace rename implementation plan
- `2d0da2b` docs(v0.25.0): tree 2 module-namespace rename design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git remote -v` → `origin git@github-personal:willfell/sauce.git`
> 3. `git log --oneline -10` — expect ~6 v0.25.0 cycle commits + handoff at top
> 4. `git status` → only pre-existing drift (`.obsidian/*`, `blueprints i want.md`, `features-im-brainstorming/`, `install-test-output.md`, `ranch/Scripts/accent-button/` untracked, `ranch/Scripts/*.bak` untracked)
> 5. `git ls-remote --tags origin | grep v0.25.0` → 2 lines (annotated tag + dereference)
> 6. **Stub md5 invariant** (4 paths must match `ea23aa812503bfca66359d3b2b239ba8`): `md5 platform/installer-stub.js ranch/Templater/platformInstall.js ../barebones-beacon-poc/ranch/Templater/platformInstall.js ../accuris-beacon-poc/ranch/Templater/platformInstall.js`
> 7. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.25.0"`
> 8. `grep -nE 'spice/\${' platform/install.js` → finds the install.js:250 prefix line
> 9. `grep -rE 'beacon/[a-z]|\.beacon-backup' platform/install.js platform/bootstrap.js platform/bootstrap-lib/ platform/blueprints/ platform/mechanisms/ platform/test/` → empty (Tree 2 cleanup verified)
> 10. **All 5 harnesses GREEN:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 20 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol
> 2. **`Docs/plans/2026-05-07-v0.25.0-tree2-namespace-rename-result.md`** — most recent cycle close. NEW lessons (a–h); reserved-headroom precedent extends to 12 data points (fourth MIXED case)
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.25.0 close
> 4. **`Docs/install.md`** — read the v0.24.x → v0.25.0 upgrade section + Sauce rebrand COMPLETE callout
> 5. **`Docs/landmines.md`** — 18 entries; #11 (final wording — namespace `spice/`; legacy violations RESOLVED + historical) + #12 (allowlist 12 paths; backup-suffix `.sauce-backup`) amended in v0.25.0
> 6. **`blueprints i want.md`** — pending feature carries (multi-theme deferred 6th time)
> 7. **`features-im-brainstorming/sticky-notes.md`** — new candidate blueprint

---

## 🟡 PRIMARY candidates (no single locked recommendation; user's call)

The Sauce rebrand sequence is COMPLETE. v0.26.0+ returns to feature/quality work. Several strong candidates queued:

### A. Architectural `install.js` refactor — `NO_OP_DUE_TO_MISSING_PREREQ` signaling

**What:** When a helper short-circuits because a prereq is missing (e.g., styling helpers when Style Settings plugin is absent), the helper currently runs without writing files but the orchestrator still records `installed_at` for the item. This forces a landmine #16 unstick PATCH bump on the next cycle to force re-process. Refactor: helpers explicitly signal `NO_OP_DUE_TO_MISSING_PREREQ` back to the orchestrator; orchestrator skips writing `installed_at` when nothing was materialized; next cycle's install picks up the work without a version bump.

**Carry from:** v0.21.0 + v0.22.0 + v0.24.0 result writeups.

**Cycle shape estimate:** Substantial — install.js changes + helper-API changes + harness extensions. 4-5 stages. Medium-cost.

### B. Project Sub-tasks panel — UI cycle

**What:** Render a "Sub-tasks" panel on each Task hub page showing the task's sub-task cards (already organized in `<task>/board/<card>/<card>.md` folders post-v0.5.0) with status badges + click-to-open. Mirrors the project-blueprint hub-cards pattern.

**Carry from:** post-v0.18.2 result writeup ("Project Sub-tasks panel on task hub").

**Cycle shape estimate:** UI cycle — 3-4 stages with 2-4 inline-CFs typical for first-of-its-kind UI surfaces (per v0.5.0/v0.11.0/v0.12.0/v0.6.0/v0.16.0/v0.17.0 precedent).

### C. Finance polish carries

**What:** Migration helper for pre-v0.17.0 entities; budget categories aggregate row; PaycheckExpensesEditor "All Paid" toggle.

**Carry from:** post-v0.17.0 result writeup.

**Cycle shape estimate:** PATCH cycle. 2-3 stages. Lower-cost.

### D. Multi-theme presets — DEFERRED 6th time

**What:** Schema upgrade `style_settings_defaults_src` (single string) → `style_presets[]` keyed by name (Baseline / Headspace / ERO / fourth). Vendor + canonicalize Style Settings JSON from each source vault. Wizard adds preset-select prompt during first-run.

**Carry from:** v0.21.1 → v0.22.0 → v0.22.1 → v0.23.0 → v0.24.0 → v0.25.0 (DEFERRED 6 times). Vendoring guides at `blueprints i want.md`.

**Cycle shape estimate:** First-of-its-kind schema-extension cycle. MINOR bump. 4 stages with 2-4 CFs (per first-of-its-kind precedent).

### E. Sticky-notes blueprint — new feature

**What:** New blueprint at `spice/sticky-notes/<YYYY-MM-DD>/`. Cmd+0 hotkey opens today's sticky-notes hub at `spice/sticky-notes/<YYYY-MM-DD>/Sticky-Notes-<YYYY-MM-DD>.md`; "New Note" button creates a new sticky note (optionally named, otherwise auto-named with date suffix) under the day's folder.

**Carry from:** `features-im-brainstorming/sticky-notes.md` (USER-AUTHORED feature spec).

**Cycle shape estimate:** New blueprint — 3 stages. Reuses `runTemplaterTemplate` action type + nav-button pattern from v0.4.0 to-do blueprint. Possibly clean (zero CFs) or 1-2 CFs.

### F. Local workshop dev path rename + dogfood drift housekeeping (standalone)

**What:** Rename `/Users/willfell/Documents/obsidian/sync/workshop/beacon` → `/workshop/sauce`. Pure host-filesystem rename. Could bundle dogfood drift cleanup (`.obsidian/*` + `Docs/Meta/Scripts/cards/beacon-cards.js` pre-Tree-3 stale path).

**Carry from:** v0.23.0 + v0.24.0 + v0.25.0 result writeups.

**Cycle shape estimate:** PATCH cycle. 1-2 stages. Trivial — pure mv + sed.

---

## Recommendation

> [!info] No single locked recommendation
> Sauce rebrand is COMPLETE; the next direction is the user's call. **My subjective ranking** (informed by carry-forward weight + user-impact + cycle cleanliness):
> 1. **(E) Sticky-notes blueprint** — clean new-feature cycle exercising the post-v0.25.0 `spice/<module>/` namespace; smallest new-blueprint scope on the candidate list; high user-utility.
> 2. **(D) Multi-theme presets** — has been deferred 6 times; if it's important, consider whether to commit or de-queue permanently.
> 3. **(B) Project Sub-tasks panel** — long-tail UI carry; medium-cost.
> 4. **(A) Architectural install.js refactor** — substantial; defer until a smaller cycle wedge.
> 5. **(C) Finance polish + (F) housekeeping** — opportunistic; bundle into other cycles.

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock cycle scope.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on mechanical edits + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any literal call-site that changes, QUOTE it in the design.
> - **Two-stage subagent review** (v0.20.0 + reaffirmed v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0) — spec compliance first, code quality second. Never skip either.
> - **Manual smoke is the close gate** for any user-facing surface.
> - Push to origin/main after each stage commit; annotated tag at cycle close (REQUIRES user approval).

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; reaffirmed v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **Module-directory invariant (#11)** — `spice/<module>/` namespace; one-module-per-blueprint rule unchanged. Sauce rebrand sequence COMPLETE.
> - **Inside-vault clone is git-managed (#18)** — `pantry/` is the canonical name post-v0.23.0; preserve.
> - **Allowlist 12 paths (#12)** — adding 13th+ requires explicit user approval. Backup-suffix is now `.sauce-backup` post-v0.25.0.
> - **Stub content-static (#13)** — never re-edit any `ranch/Templater/platformInstall.js` body. Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` post-v0.24.0 (UNCHANGED across v0.25.0).
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **`.sauce-backup` suffix** (post-v0.25.0; renamed from `.beacon-backup`) — backup-suffix conventions are now `.sauce-backup`-canonical going forward.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, allowlist expansion (13th path), creating top-level vault directory.

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. stub md5 + harness counts).
> 2. Read v0.25.0 result writeup (NEW lessons a-h; reserved-headroom 12 data points; Sauce rebrand sequence COMPLETE).
> 3. Surface candidates A-F via `AskUserQuestion`. Sticky-notes (E) is the cleanest new-feature cycle if user wants forward-momentum; multi-theme (D) if user wants to commit or de-queue the deferred carry.
> 4. Once scope is locked, brainstorm.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.25.0 next cycle. Read Docs/prompts/2026-05-07-post-v0.25.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity (Sauce rebrand COMPLETE; pantry/ + ranch/ + spice/ namespace tripod canonical; CLI binary `sauce`; env vars SAUCE_*), pre-flight checks (workshop_version 0.25.0; stub md5 ea23aa812503bfca66359d3b2b239ba8 unchanged; allowlist 12 paths; helper count 10; bootstrap 45/0, cli 20/0, install-sh 14/0, helper-cases 387/0, renderer green; backup-suffix now .sauce-backup), candidate cycles A-F (architectural install.js refactor; project Sub-tasks panel; finance polish; multi-theme presets DEFERRED 6th time; sticky-notes blueprint; local workshop dev path rename + dogfood drift), constraints carried forward, and the v0.25.0 NEW lessons (a-h).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (host filesystem path; standalone rename carry from v0.24.0/v0.25.0).
2. Run pre-flight checks. Workshop_version 0.25.0; remote git@github-personal:willfell/sauce.git; stub md5 ea23aa812503bfca66359d3b2b239ba8 (unchanged from v0.24.0).
3. Read v0.25.0 result writeup + CLAUDE.md status snapshot + landmines (18 entries; #11 final wording + #12 backup-suffix amended in v0.25.0).
4. Surface candidates via AskUserQuestion. No single locked PRIMARY (Sauce rebrand done). Subjective ranking: (E) sticky-notes blueprint > (D) multi-theme presets > (B) project Sub-tasks panel > (A) install.js refactor > (C) finance polish + (F) housekeeping.
5. Once locked, brainstorm.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on mechanical edits + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Apply v0.25.0 lessons at design-time:
- Single source-of-truth design pays off at rename-time (lesson a).
- Fresh-start posture eliminates migration helpers; ASK explicitly about consumer-state-preservation at brainstorm (lesson b).
- Backup-suffix convention is now `.sauce-backup` (lesson c).
- Two-stage subagent review reaffirmed at 5 data points; never skip (lesson d).
- "What NOT to rename" list is plan-level discipline (lesson e).
- sed scope MUST include source dirs comprehensively — including JSON manifest description/message strings (lesson f, reaffirms v0.24.0 lesson b).
- install.md upgrade section pattern is canonical (lesson g).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13; md5 ea23aa812503bfca66359d3b2b239ba8 holds across v0.26.0+); landmine #12 allowlist expansion (13th+ path) requires approval; landmine #11 module-directory invariant: `spice/<module>/` is canonical; two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 + v0.23.0 + v0.24.0 + v0.25.0 reaffirmation).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
