---
date: 2026-05-04
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.5.0 cycle. Triggered by v0.5.0 cycle close + tag v0.10.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-04-v0.5.0-project-blueprint-result.md (v0.5.0 close — read first)
  - Docs/plans/2026-05-04-v0.5.0-project-blueprint-design.md (design + locked decisions)
  - Docs/plans/2026-05-04-v0.5.0-project-blueprint-plan.md (plan v2)
  - Docs/plans/2026-05-04-v0.4.2-runTemplaterTemplate-schema-result.md (prior cycle close — context)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
gates:
  - v0.5.0 closed; tag v0.10.0 pushed; workshop manifest at 0.10.0
  - project blueprint at 1.2.2 (BREAKING from 0.3.0; module_directory `projects-legacy` → `projects`; full accuris pattern)
  - Helper-cases harness 255 sub-asserts (+13 from R6 openLink target substitution)
  - Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4` across all 4 paths
  - Mobile-aware visual design carried forward as renderer-level platform value
  - install.js validateAndResolve openLink branch substitutes `target` via substituteLenient (CF-1 v0.5.0)
  - ProjectsHubCards CustomJS class shipped; ProjectNavButtons visual divider/section-label posture
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval — user picks)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.6.x or v0.10.x or v0.11.0 (cycle-dependent)
---

# Onboarding — post-v0.5.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (load-bearing)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git` — personal account `willfellhoelter@gmail.com`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/poc-vault`
> - **Sibling vaults:**
>   - `../tmp-test-barebones-vault/` — primary regression target + manual-smoke target
>   - `../tmp-acc-vault/` — secondary consumer (deliberate-lag at subscription level; mechanisms-only posture preserved)
>   - `../accuris/` — production reference vault (NOT a v0.x consumer; source of pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.10.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.3.0`
- Blueprints: `boards@0.1.0`, `daily@0.1.4`, `meetings@0.1.2`, `project@1.2.2` (BREAKING from 0.3.0; full accuris pattern), `to-do@0.1.2`
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts; all 3 harnesses green
- Module-directory invariant codified at the installer level (T-FIX1)
- Landmine #12 allowlist: 3 paths; helper count: 4
- Filename pattern `<Name>-YYYY-MM-DD` invariant (gotcha 8) — applies to date-routed daily artifacts only; project artifacts are slug-routed
- Mobile-aware visual design as renderer-level posture (gotcha 7)
- In-cycle re-process bump rule (gotcha 9)
- NEW from v0.5.0: install.js `validateAndResolve` openLink branch (substitutes `target` via substituteLenient); `ProjectsHubCards` CustomJS class; visual divider/section-label posture in `ProjectNavButtons`

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.5.0 baseline
> 1. **Git head + recent commits:**
>    ```bash
>    git -C /Users/willfell/Documents/obsidian/sync/workshop/poc-vault log --oneline -10
>    ```
>    Expected (top-down): `82b8734` v0.5.0 cycle-close commit; `fc22617` CF-5; `9b555b9` CF-1; `8d2594a` CF-2; `4f00ca7` S2; `01c33b8` S1; `0d561b9` plan; `d2aeb94` design; `6037740` post-v0.4.2 handoff.
> 2. **Working tree:** `git status` → clean (or just `.bak` artifacts).
> 3. **Tags on origin:** `git ls-remote --tags origin | grep v0.10` → `v0.10.0` present + annotated.
> 4. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/platform/installer-stub.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-acc-vault/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 5. **Workshop manifest at 0.10.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.10.0"`.
> 6. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 7. **Helper-cases harness 255 green:** `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
> 8. **Barebones state:**
>    ```bash
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/beacon/projects 2>&1
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/beacon/projects/test-alpha 2>&1
>    cat /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/nav-buttons-registry.json | grep -A3 projects-hub
>    ```
>    Expected: `beacon/projects/` populated with `Projects.md` + `test-alpha/` (containing atlas + structure + board + `tasks/Setup/{Setup.md, notes/, board/Setup-board.md, board/Sub A/Sub A.md}`); `workstreams[]` frontmatter on `Test Alpha.md`; nav-buttons-registry.json contains `projects-hub` entry with `target=beacon/projects/Projects.md`.

If any pre-flight fails, STOP and surface to user via `AskUserQuestion`.

---

## Required reads (~15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol. END-OF-SESSION CHECKLIST IS LOAD-BEARING.
> 2. **`Docs/plans/2026-05-04-v0.5.0-project-blueprint-result.md`** — most recent cycle close. Captures the SIX-bump in-cycle iteration (project 0.3.0 → 1.2.2 across S1/S2/S2.2/S2.3/S2.4); openLink installer-side dead-code (CF-1); ProjectsHubCards mobile-aware design; folder-template path-token lesson. Read first to inherit cadence.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.5.0 close. Cycle order shows v0.5.0 ✅; next TBD.
> 4. **`Docs/landmines.md`** — 14 entries unchanged.
> 5. **`Docs/how.md`** — read the optional manifest fields callouts; openLink action_type now substitutes `target` at install time (v0.5.0 fix).
> 6. **`Docs/plans/2026-05-04-v0.5.0-project-blueprint-design.md`** + plan — most recent design + plan structure (use as template if you reach those phases).
> 7. **(If you pick v0.5.x project polish)** Reference reads:
>     - `platform/blueprints/project/manifest.json` — ships `module_directory: "projects"`; `pre_install[move]` retired in 1.2.x.
>     - `platform/blueprints/project/content/scripts/ProjectsHubCards.js` — mobile-aware card grid, the new pattern reference.
>     - `platform/install.js` `validateAndResolve` — openLink target substitution branch (CF-1 lesson).
> 8. **(If you pick v0.6.0 next-blueprint)** Reference reads:
>     - `platform/blueprints/to-do/manifest.json` + `platform/blueprints/meetings/manifest.json` — canonical examples of split-field nav_buttons + module_directory invariant.
>     - `platform/mechanisms/nav-buttons/space-nav-buttons.js` — top arrow row inheritance is automatic for any date-named filename (`\d{4}-\d{2}-\d{2}` regex).
> 9. **(If you pick v0.4.3 mobile install path investigation)** Reference reads:
>     - `Docs/landmines.md` #8 — desktop-only installer rationale.
>     - `platform/install.js` — `require("fs")` usage points (the desktop-only ties).
>     - Obsidian Sync docs for vault-adapter-only delivery patterns.

---

## Candidate cycles (pick ONE)

> [!example]- v0.5.x project polish iterations **(Strong candidate; default recommendation)**
> **What:** small polish pass on the project blueprint based on user feedback collected during v0.5.0 smokes. Likely scope:
> - Tighten `ProjectsHubCards` card styling (description column? compact mode?)
> - Investigate folder-template auto-creation for new projects (templates dynamically registered when a new project hub is created)
> - UX polish on the workstream selector modal
> - Better handling of project-board → `tasks/` folder pre-creation (currently only `_createProject` mkdirs)
>
> **Why:** maintains the "iron everything out before moving on" momentum; small surface; pure additive on v0.5.0 foundations. Direct user-feedback cycle — no design exploration needed beyond brainstorming the polish-item priority order.
>
> **Estimated scope:** 1 stage; project bump 1.2.2 → 1.3.0 MINOR. Workshop bump 0.10.0 → 0.10.1 PATCH (or 0.11.0 if scope grows).
>
> **Open questions for brainstorming:**
> - Which polish items rank highest from the user's daily workflow?
> - Anything currently blocking on project usage that v0.5.0 didn't address?
> - Does folder-template auto-registration touch installer (new helper) or stay renderer-side (Templater hook)?

> [!example]- v0.6.0 next-blueprint (NEW: journal / retros / weekly-hubs)
> **What:** ship a NEW blueprint (NOT a refactor like v0.5.0). Candidates:
> - `journal` (free-form daily journaling; lighter than daily; `beacon/journal/YYYY/MM-MMMM/Journal-YYYY-MM-DD.md`)
> - `retros` (per-cycle or per-week retrospective notes; `beacon/retros/YYYY/MM-MMMM/Retro-YYYY-MM-DD.md`)
> - `weekly-hubs` (week-level aggregation; cross-day rollup — Monday hub aggregates the whole week's daily/meetings/to-do)
>
> **Why:** continues the platform's blueprint catalog growth on the most user-visible axis. v0.5.0 foundations (project blueprint, openLink target substitution, ProjectsHubCards as a mobile-aware reference) are now stable. Filename pattern + nav-button arrow row + invoke_command surface all inherited for free.
>
> **Estimated scope:** 1-2 stage cycle. New blueprint manifest + ~1 template body + maybe 1 CustomJS class for hub-style aggregation. nav-buttons mechanism unchanged. Workshop bump 0.10.0 → 0.11.0 MINOR.
>
> **Open questions for brainstorming:**
> - Which blueprint? (journal vs retros vs weekly-hubs vs something else)
> - Does it need a Templater template? Hub-style or leaf-style?
> - What does the SpaceDailyDashboard panel show for it (if anything)?
> - Mobile-aware visual design carry-forward from ProjectsHubCards as the new pattern reference.

> [!example]- v0.4.3 mobile install path investigation
> **What:** feasibility study for retiring landmine #8 (desktop-only installer). Currently `platform/install.js` uses `require("fs")` directly; mobile Obsidian doesn't expose Node's `fs`. Would require Obsidian Sync delivery + vault-adapter-only writes + a pure-Obsidian execution path.
> **Why:** opens beacon to Obsidian mobile users. Not a blueprint cycle; design + spike work. Renderer + class layer is already mobile-aware (gotcha 7); installer is the last desktop-only frontier.
> **Estimated scope:** 1 stage spike (no version bump if pure investigation; design output only). Could split into v0.4.3 design + v0.4.4 implementation cycles.
> **Open questions for brainstorming:**
> - Path 1 — fully replace `fs` with vault adapter calls (every read + write through `app.vault.adapter`). Big lift; touches every helper.
> - Path 2 — keep `fs` for the workshop side; build a SEPARATE Obsidian-side install path that consumers use. Two installers; risks divergence.
> - Path 3 — consumers receive prebuilt pack via Obsidian Sync; install becomes a one-shot apply. Simpler runtime; more complex build pipeline.

> [!example]- v0.4.4 cross-day weekly aggregation
> **What:** extend MeetingsHubCards / SpaceDailyDashboard with week-level views — aggregate the daily-nav arrow row's filename-pattern stability into "this week" / "last week" panels.
> **Why:** useful with the daily-nav arrow row + filename pattern stable. Could co-design with `weekly-hubs` blueprint (v0.6.0 candidate).
> **Estimated scope:** 1 stage. CustomJS class extension; no installer changes. nav-buttons may add a week-nav row variant.
> **Open questions for brainstorming:**
> - Render in daily note, in a dedicated weekly-hub note, or both?
> - Mobile-aware: how do week panels collapse on small screens?
> - Date math for week boundaries (Monday-start vs Sunday-start vs configurable).

> [!example]- v0.4.5 documentation cycle (in-cycle redesign workflow)
> **What:** codify the in-cycle-redesign methodology (gotcha-6) and the inline-CF-extension model (used heavily in v0.5.0) in `Docs/use.md`. Pattern is now reproducible across THREE cycles (v0.3.2 v1→v2; v0.4.2 v1→v2; v0.5.0 S2.x sub-stages).
> **Why:** institutional memory. Prevents the "we should brainstorm this redesign? OK" friction by making the workflow first-class.
> **Estimated scope:** 0.5 stage. Pure docs. Workshop bump 0.10.0 → 0.10.1 PATCH (or no bump — docs-only cycle).
> **Open questions for brainstorming:**
> - Where in `Docs/use.md` does this live? Standalone section? Sub-section under Pacing?
> - Capture the "smoke surfaces misalignment → pause → brainstorm → write plan v2 → resume execution" flow as a checklist.
> - Capture the inline-CF-extension pattern: when user surfaces polish at S2 close, address as S2.x sub-stages BEFORE S3 close.
> - Reference the three case studies (v0.3.2; v0.4.2; v0.5.0).

---

## Recommendation

> [!success] **v0.5.x project polish iterations** — RECOMMENDED PRIMARY
> Momentum from user-flagged carry-forwards collected during v0.5.0 smokes. Small surface; ironing out the new project blueprint's rough edges directly serves the user's stated workflow ("iron it all out before moving on"). Pure additive on v0.5.0 foundations.
>
> **Runner-up: v0.6.0 next-blueprint** — appropriate if v0.5.x is deemed solidly closed by the user. The platform is ready to grow the blueprint catalog; ProjectsHubCards is the new mobile-aware reference pattern.
>
> **Skip for now (defer to dedicated cycles):** v0.4.3 mobile installer (large investigation; better as standalone spike), v0.4.4 weekly aggregation (likely co-designs with v0.6.0 weekly-hubs), v0.4.5 docs cycle (low urgency; bundle with another cycle).

---

## Cadence guidance (mirror v0.4.2 / v0.5.0)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval — user picks.
> - Per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - TDD discipline on harness extensions.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian after every Node-harness install when new CustomJS classes are introduced** OR when class bodies changed (v0.5.0 added ProjectsHubCards; v0.4.2 surfaced this with SpaceNavButtons rewrites).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **Inline-CF-extension pattern (v0.5.0 precedent):** when user surfaces polish items at S2 close, address as S2.x sub-stages with their own commits BEFORE S3 cycle close, rather than deferring to a follow-up cycle. Use this pattern when user explicitly says "iron it all out before moving on."

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints. Helpers + template content can use `{{xxx}}` tokens (`substituteLenient` resolves at install time).
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts only. Project artifacts are slug-routed; gotcha 8 does NOT apply to project files. Date-extraction in CustomJS classes uses regex `\d{4}-\d{2}-\d{2}`.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile. Adaptive layouts, touch-friendly targets, no hover-only affordances. ProjectsHubCards is the new pattern reference. Installer remains desktop-only (landmine #8 unchanged).
> - **In-cycle re-process bump rule** (gotcha 9) — when reusing in-cycle staged work at version X, in-cycle revisions to that work must bump X+1 to force consumer re-process. Pre-flight diff installed.json vs subscription before assuming a no-op.
> - **Verify command ids against runtime** (gotcha 10) — when an `invoke_command` action ships, verify the actual command id via `Object.keys(app.commands.commands).filter(...)` in Obsidian DevTools. Don't trust naming conventions.
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes.
> - Module-directory invariant under `beacon/` (#11) — codified at installer level via T-FIX1's mkdir.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.
> - **v0.5.0 lesson:** when porting CustomJS classes that hardcode template paths, USE `{{templates_path}}` token (installer substitutes at install time) NOT hardcoded `Docs/Meta/Templates`.

---

## Cross-cutting gotchas codified during recent cycles (READ BEFORE STARTING)

> [!warning] Gotchas surfaced 2026-05-03 / 2026-05-04 — load-bearing for any cycle touching runTemplaterTemplate, CustomJS classes, invoke_command, or new renderer action_types
>
> **Gotcha 1: Bracket-escape gap on runTemplaterTemplate folder/filename.** RESOLVED at v0.4.2 S1 — split-field schema makes literal text never reach `moment.format()`. Manifests still using legacy `folder` / `filename` fields are silently ignored (BREAKING). Future blueprints inherit the safety for free.
>
> **Gotcha 2: Node-harness install + Obsidian's plugin auto-rescan.** `platform/test/run-install.js` writes via `fs` (Node), bypassing Obsidian's vault adapter. Obsidian's CustomJS plugin watches the vault adapter for change events to auto-rescan its jsFolder. After a Node-harness install adds new files OR rewrites class bodies, the user MUST Cmd+R Obsidian to force plugin re-init. Smoke checklists for any cycle adding OR rewriting CustomJS classes should explicitly call out the Cmd+R step.
>
> **Gotcha 3: T-FIX1 mkdir module_directory at install time** (v0.3.0 codified). Every blueprint with `module_directory` set gets that directory mkdir'd between `applyPreInstall` and `files[]` loop. Mechanisms exempt.
>
> **Gotcha 4: Force re-process via installed.json edit (CLAUDE.md ask-before-acting).** `install.js:111` short-circuits per-item install when `subscribed.version === installed.version`. Tests/cycles that need to exercise behavior INSIDE `installItem()` must triple-bump versions OR drop the entry from `<consumer>/Docs/Meta/platform-installed.json:installed[]` (CLAUDE.md ask-before-acting; user-approved).
>
> **Gotcha 5: User-override preserved on folder_templates + nav-button registry overwrites.** `applyTemplaterFolderTemplates` preserves user-customized template paths. `applyNavButtons` REPLACES the contributions[<source>] array on each install. Existing `folder_templates` entries from prior installs may persist as user-overrides — cosmetic clutter but functionally inert.
>
> **Gotcha 6: Mid-cycle redesign methodology.** When smokes surface a misalignment with the user's mental model, ACCEPT the redesign rather than ship the wrong primitive. Workflow: pause SDD → invoke `de:brainstorming` → present revised design → get approval → invoke `de:writing-plans` → resume SDD. Used three times now (v0.3.2 v1→v2; v0.4.2 v1→v2; v0.5.0 inline S2.x sub-stages). Pattern is reproducible.
>
> **Gotcha 7: Mobile-aware visual design as platform value (v0.4.2; reinforced v0.5.0).** Every renderer (CustomJS class) MUST be designed for both desktop AND mobile. Adaptive layouts, touch-friendly targets, no hover-only affordances. ProjectsHubCards is the new pattern reference (v0.5.0). Installer remains desktop-only (landmine #8 unchanged); the visual contract is what changes.
>
> **Gotcha 8: Filename pattern `<Name>-YYYY-MM-DD` invariant (v0.4.2).** All date-routed daily artifacts use date-suffix; hyphen-only separator; no spaces. Date-extraction in CustomJS classes uses regex `\d{4}-\d{2}-\d{2}`. **NOTE:** project artifacts are slug-routed (NOT date-routed) — gotcha 8 does NOT apply to project files.
>
> **Gotcha 9: In-cycle re-process bump rule (v0.4.2; reinforced v0.5.0).** When reusing in-cycle work staged at version X, in-cycle revisions to that work must bump to version X+1 to force consumer re-process. v0.5.0 was a SIX-bump cycle (project 0.3.0 → 1.2.2 across S1/S2/S2.2/S2.3/S2.4) — pattern is now strong evidence: any cycle with surprises/feedback should reserve "Y.Z.x" headroom for in-cycle iteration. Pre-flight diff installed.json vs subscription before assuming a no-op.
>
> **Gotcha 10: Verify invoke_command command ids against runtime (v0.4.2).** When designing an invoke_command action, verify the actual command id via Obsidian DevTools (`Object.keys(app.commands.commands).filter(k => k.includes('<keyword>'))`). N/A for project blueprint (no invoke_command actions). Don't trust naming conventions or Obsidian docs; use the live runtime as truth.
>
> **NEW pattern (v0.5.0, not yet a numbered gotcha): renderer dispatcher action_types must have matching install.js validateAndResolve branches.** v0.5.0 surfaced openLink as dead-code installer-side — `target` literal `{{module_directory}}` was reaching the registry unresolved because the openLink branch didn't substitute. Landed CF-1. **Lesson:** for any new renderer action_type, ALSO add a `validateAndResolve` branch in install.js OR placeholders won't substitute at install time.

---

## Suggested first move

> [!todo] Recommended path
> 1. Run all 8 pre-flight checks. Surface discrepancies first.
> 2. Read the v0.5.0 result writeup + CLAUDE.md status snapshot + landmines + how.md (~15 min).
> 3. **Surface the cycle pick to user via `AskUserQuestion`** — present the 5 candidates above + recommendation; lock the choice before opening brainstorming.
> 4. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions listed for that candidate.
> 5. After brainstorming approval, invoke `de:writing-plans` to author the implementation plan.
> 6. After plan approval, invoke `de:subagent-driven-development` (same session) OR ask user to start a parallel session with `de:executing-plans`.
> 7. Manual Obsidian smokes at S2 close (with Cmd+R prerequisite if new CustomJS classes were added or class bodies changed).
> 8. Cycle close: result writeup + CLAUDE.md status snapshot + commit + tag + push branch + tag + post-<cycle> next-cycle handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.5.0 next cycle. Read Docs/prompts/2026-05-04-post-v0.5.0-next-cycle-handoff.md FIRST — the full onboarding doc with project identity, 8 pre-flight checks, 5 candidate cycles with recommendation (v0.5.x project polish iterations primary; v0.6.0 next-blueprint runner-up), constraints carried forward, and cross-cutting gotchas.

After reading the handoff doc:
1. Run all 8 pre-flight checks.
2. Read v0.5.0 result writeup + CLAUDE.md status snapshot + landmines + how.md.
3. Surface the 5 cycle candidates via AskUserQuestion with the recommendation. Get pick before brainstorming.
4. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown.
5. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian after Node-harness install when CustomJS classes appear OR class bodies change. Pre-flight diff barebones installed.json vs subscription. Inline-CF-extension pattern (v0.5.0 precedent): when user surfaces polish at S2 close, address as S2.x sub-stages before S3 close.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; filename pattern <Name>-YYYY-MM-DD invariant (gotcha 8) for date-routed daily artifacts (NOT for project artifacts); verify invoke_command command ids against live runtime (gotcha 10); for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
