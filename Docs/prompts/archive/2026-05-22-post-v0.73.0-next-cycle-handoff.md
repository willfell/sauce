# Post-v0.73.0 — next cycle handoff

**You are starting a fresh cycle.** This doc captures state inherited from v0.73.0's close (2026-05-22) and the candidate work for the next cycle.

---

## State inherited

**Workshop:** v0.73.0 at HEAD. Branch `main`. Tag `v0.73.0` pushed. homebrew-sauce tap PR #92 merged. Brew Cellar at `/opt/homebrew/Cellar/sauce/0.73.0/libexec`. All 4 consumer vaults at v0.73.0 with `KanbanStatusSyncInit` materialized + registered in `customjs.startupScriptNames[]`.

**Mechanism versions shipped:**
- `kanban-status-sync@0.2.0` — NEW startup-script class; `syncAllBoards` no longer in render hot path.
- `activity-feed@0.7.0` — persisted `<details>` state across re-renders; precedence comments; AF-V073 runtime asserts.
- `daily@0.13.0` — Part A removal + Part B `_renderSection` extension.

**Preflight + self-install:** GREEN as of cycle close (1160+ asserts).

**Recent commits (newest first):**
- `<close-commit>` — chore(workshop): v0.73.0 cycle-close artifacts.
- `6e433ef` — feat(workshop): v0.73.0 — kanban-progress architectural cleanup + activity-feed efficiency.
- `8334505` — fix(kanban-status-sync): v0.1.1 — once-per-day sync cache (workshop v0.72.1).
- `0665964` — docs(kanban-status-sync): correct manifest description re: computeDiff composition.
- `6b619d5` — chore(release): v0.72.0 — workshop_version bump + self-install.

---

## Open user-side validations from v0.73.0

1. **Part B `<details>` persistence smoke** — user-confirm: toggle the Activity section closed in any consumer's daily note → leaf-switch + return → section should still be closed. Same for inner activity-feed groups (cowork / project / kanban / scratch).
2. **Part C command** — Cmd+P in any consumer → start typing "Sauce: Re-sync" → command should appear → invoke → Notice should report `synced/archived/boards` counts.
3. **Smart-connections diagnostic still open.** User has NOT yet temporarily disabled smart-connections to confirm/refute it as the re-render trigger. If they report back:
   - **OFF stops scroll-resets** → landmine #24 candidate: dataviewjs blocks re-execute on smart-connections embedding-queue activity. Document in `Docs/landmines.md`.
   - **OFF doesn't stop them** → root cause elsewhere; investigate in a follow-up cycle.
4. **Mobile smoke** — landmine #23 territory. The new `KanbanStatusSyncInit.invoke()` runs `app.commands.addCommand` + reads `app.plugins.plugins.dataview.api`. Both are mobile-available per project-mechanism precedent, but smoke against the headspace mobile vault before declaring final close.

---

## Candidate next cycles

(Ordered by likelihood, not commitment.)

### A. Activity-feed audit — remaining items (small)

Items 7 (DOM allocations in `_renderFramedGroup`) and 8 (`_resolveTimeWindow` dual-path) were deferred from v0.73.0 D-audit. Revisit if a perf budget surfaces. Likely PATCH on activity-feed.

### B. Daily-dashboard mobile pass (small)

The `<details>` state persistence is desktop-validated only. Mobile may have toggle event timing quirks. If user reports mobile regression, single PATCH on `daily` + `activity-feed` to handle.

### C. `sauce update --bump-pins` flag (medium)

FLN-v67-7 still unresolved. The deploy chain on every cycle requires a manual 4-vault `node -e "..." && cd && sauce update --force` loop. A `--bump-pins` flag would read the workshop manifest, derive target versions, write the consumer subscription, and run install in one pass. Likely PATCH on CLI.

### D. Drift detection for consumer `ranch/cache/` (small)

The new `dashboard-section-state.json` lives outside the installer's purview. If a consumer manually corrupts the file (JSON malformed), the helpers swallow the read error and fall back to default-open. Optional: `/audit` could surface malformed state files as a new severity. Not blocking.

### E. Landmine #24 — if smart-connections confirmed (small)

Per the v0.73.0 result-doc's open question. Add a new landmine entry documenting that dataviewjs blocks re-execute on smart-connections embedding-queue activity. Same shape as landmine #23 (mobile-mtime). No code change; doc-only.

---

## Workshop conventions reminder

- **JSON not YAML** for all platform metadata.
- **Template variables for paths** — never hardcoded.
- **No `file.mtime` reads** in any new code (landmine #23).
- **`customJS.X` is a singleton instance**, never a class constructor.
- **Conventional-commits.** No Claude sign-off. No `--no-verify`. No `git add -A`.
- **`cd` into consumer vaults** before `sauce update` — `SAUCE_VAULT` env var is silently ignored when cwd is inside a sauce-managed vault.
- **Workshop self-install gates the release.** If self-install fails, do NOT push to consumers.

---

## Suggested session start

```bash
# 1. Confirm workshop state
cd /Users/willfellhoelter/projects/repos/sauce
git log --oneline -5
git status   # should be clean

# 2. Confirm consumer subscriptions
for V in barebones accuris-sauce ero-sauce headspace-sauce; do
  echo "=== $V ==="
  jq -r '"workshop_version=\(.workshop_version)"' /Users/willfellhoelter/notes/sauce/$V/ranch/platform-subscription.json
done
# All should report workshop_version=0.73.0

# 3. Read this handoff + the SESSION-START canonical recipe
cat Docs/prompts/SESSION-START.md
```

Cycle ordering convention is at `Docs/agent-guides/cycle-status.md` § Cycle order. Bumping `workshop_version` is a cycle-close action, not a session-start action.
