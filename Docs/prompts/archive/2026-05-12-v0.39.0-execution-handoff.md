# v0.39.0 — Projects Org-Chart Foundation: Autonomous Execution Handoff

> **For the picking-up agent:** This prompt is self-contained. Paste it as your first message in a fresh Claude Code session inside the `sauce` repo. It will carry the v0.39.0 cycle forward autonomously, escalating to the user only at well-defined gates.

## Working directory + git posture

`cd /Users/willfellhoelter/projects/repos/sauce` (workshop dev repo). Branch `main`.

Sauce convention (codified in `CLAUDE.md`): single-branch direct push to `main` after every commit. No feature branches, no PR review. No `Co-authored-by: Claude` trailer. Author = the user. HEREDOC for multi-line commit messages.

Run `git log --oneline | head -5` to confirm you're current. The last design-doc / plan commits should be visible: `ca759ea` (plan), `1232a29` (reslot), `b90a74f` (design).

## Read first (REQUIRED — in this order)

1. **`CLAUDE.md`** — sauce workshop conventions, 22 landmines, "Ask before acting" list. Especially:
   - Module-directory invariant (`spice/<module>/` per blueprint; `ranch/...` for mechanisms)
   - `.obsidian/` allowlist (only 4 paths editable; the rest are user-owned)
   - Cycle-close requires `workshop_version` bump in `platform/manifest.json` + `package.json` sync (gated by `scripts/check-version-sync.js`)
   - Workshop dogfoods before tag — if self-install fails, do NOT tag
2. **`Docs/plans/2026-05-12-projects-org-and-lifecycle-design.md`** — design (commit `1232a29`). 13 sections; the 12-row decisions table in §1 is load-bearing.
3. **`Docs/plans/2026-05-12-v0.39.0-projects-org-foundation-plan.md`** — implementation plan (commit `ca759ea`). 12 stages (S1–S12), ~60 sub-tasks, full code inline per task, pre-drafted commit messages.

## Current cycle state — how to determine

```bash
# What's the workshop currently at?
jq .workshop_version platform/manifest.json
# 0.38.1 means execution hasn't started or S11.2 hasn't shipped yet
# 0.39.0 means S11.2 has shipped — cycle is in S12 (close + tag)

# Which v0.39.0 task commits exist?
git log --oneline | grep "v0.39.0" | head -30

# Is the baseline green right now?
npm run release:preflight
# Must exit 0 before resuming any stage. If non-zero, STOP and report — the
# previous session left something unstable.
```

Match the latest commit's `SN.M` tag against the plan's task list to know which stage to pick up next.

## Skill to invoke

`superpowers:subagent-driven-development` — dispatch implementer subagent → spec compliance reviewer subagent → code quality reviewer subagent.

## Execution batching: per-stage, not per-task

The plan has ~60 tasks but they cluster into 12 logical stages. **Batch by stage, not by task.** One implementer dispatch per stage (the implementer follows all task steps in order, committing per-task per the plan's pre-drafted messages). One spec-reviewer dispatch reviewing the whole stage. One code-quality-reviewer dispatch reviewing the whole stage. This keeps dispatch count to ~36 (12 stages × 3 roles) instead of ~180, fits sauce's commit cadence (per-task commits with stage-close preflight), and matches recent sauce cycles' execution pattern (v0.36.0, v0.38.0 each shipped 12 stages this way).

If a stage's tasks have visibly different risk levels (e.g., S6 mixes a read-only S6.1 with code-modifying S6.2–S6.7), the implementer is free to chunk internally, but you (controller) still dispatch once per stage.

## Per-stage gates (in order)

After each implementer reports `DONE`:
1. Verify `npm run release:preflight` exits 0 in the post-stage state
2. Dispatch spec-compliance reviewer (use `spec-reviewer-prompt.md` template)
3. If spec reviewer ❌: implementer fixes → re-spec-review → repeat until ✅
4. Dispatch code-quality reviewer (use `code-quality-reviewer-prompt.md` template, dispatched via `superpowers:code-reviewer` Task subagent)
5. If code-quality reviewer ❌: implementer fixes → re-quality-review → repeat until approved
6. Mark stage complete in your TodoWrite

Move to the next stage only after both reviewers are green.

## STOP and ask the user when:

| Trigger | Why |
|---|---|
| Implementer reports `BLOCKED` with a blocker the plan didn't anticipate | Plan-vs-reality drift requiring user input |
| Reviewer issue can't be resolved in 2 round-trips with implementer | Likely a deeper design question |
| A design decision change (not just a name fix) becomes needed | User owns design decisions |
| About to ship the **S12.2 annotated tag `v0.39.0`** | This is a cycle-close gate per CLAUDE.md "Ask before acting" |
| CLAUDE.md `## Status (live)` backfill scope (S12.1) balloons beyond a single commit | User may want to review backfill before it lands |
| Any force-push, history rewrite, or destructive git op becomes tempting | Never without explicit user request |
| Workshop self-install (S11.3) fails | Diagnostic + user decision needed |
| `release:preflight` fails after a stage and the implementer can't make it green within one round | Likely a sauce-internals subtlety |

## Known plan-vs-reality drifts (caught before execution started)

The plan was written with illustrative names. The implementer must adapt:

### Test harness conventions (impacts S1.6, S2.4, S3.6, S4.4, S5.4, S9.3, S10.5)

`platform/test/run-helper-cases.js` does NOT have helpers named `freshFixtureVault` / `installPlatform` / `runValidator` (those are illustrative). Actual conventions:

- `withTempVault(fn)` — async helper; passes a tmp dir to `fn`; auto-cleans
- `makeTpStub(dir)` — constructs the Templater stub the installer entrypoint expects
- Installer runs as a subprocess: `execFileSync("node", [path.join(WORKSHOP, "platform/test/run-install.js"), tmpDir, ...])`
- Two assertion families (BOTH present, used per-case-era):
  - `assertEq(label, actual, expected)` — older convention (most pre-v0.29.0 cases)
  - `assertEqual(actual, expected, label)` — newer convention (HC-RF and v0.29.0+ cases); arg order is `actual, expected, label`
- For validator-coverage cases, the existing `applyRuleFragment` test pattern (HC-RF1, HC-RF2, HC-RF3 — grep them) shows how to exercise rule_fragment aggregation by writing fixture notes and re-running install.

**Implementer should:** `cat platform/test/run-helper-cases.js` first, identify the most recent test-case pattern, mirror that exact shape for new cases. Use `assertEqual` for new code (modern convention).

### CustomJS class names (impacts S2.2, S4.2, S6.x, S7.x, S8.x)

- `customJS.BeaconCards` — confirmed; used in existing `PeopleHubCards`. Mixed naming convention persists from the v0.23.0 beacon→sauce rebrand (cards mechanism kept `Beacon` prefix for backwards compat).
- `customJS.AccentButton` — confirmed by `grep "^class " platform/mechanisms/accent-button/accent-button.js`. NO `Beacon` prefix. The plan's `BeaconAccentButton` references must become `AccentButton`.
- New classes in this cycle (`ProductsHubCards`, `ProductPageCards`, `ProductActionButtons`, `TeamsHubCards`, `TeamPageCards`, `TeamActionButtons`, `ProjectActionButtons`) — match exactly. NO `Beacon` prefix on new classes.

### v0.38.0 infrastructure to leverage (impacts S10)

The plan's S10 references `sauce seed` + per-blueprint `seed/` dirs + `run-seed.js` + `run-integration-smoke.js`. All exist and were built in v0.38.0:
- `platform/cli/cmd-seed.js` — the verb handler
- `platform/seeder/` — the seeder engine module
- `platform/blueprints/project/seed/` (and people/, daily/, meetings/) — existing α-seed JSON files. **Read one before writing new ones to match the actual schema.**
- `platform/test/run-seed.js`, `platform/test/run-integration-smoke.js` — both wired into `release:preflight`

### Workshop subscription path

Plan S11.1 says "find with `find . -name platform-subscription.json`." Confirmed location: `/Users/willfellhoelter/projects/repos/sauce/ranch/platform-subscription.json`.

## Constraints reminders (from CLAUDE.md, abbreviated)

- All paths in mechanism / blueprint code use `{{template_variables}}` — installer substitutes per-vault.
- All metadata is JSON, not YAML.
- Module-directory invariant: each blueprint owns exactly one `spice/<module>/`. Mechanisms live under `ranch/...`.
- Workshop dogfoods every release; if self-install fails at S11.3, do NOT proceed to tag.
- Don't skip hooks (`--no-verify`).
- Never edit `.obsidian/` outside the four installer-managed paths (landmine #12 allowlist).
- Commit per task. Push to `origin/main` after each commit.

## Reporting cadence

After each stage closes (preflight green, both reviewers green, commits pushed), report 3-4 sentences to the user:
- Stage number + theme
- Key commits (SHAs)
- Any concerns surfaced for future cycles (FIX-LATER-NOTE candidates)
- Next stage

If the session approaches its context limit before S12 (cycle close), **update this file** with a "currently-at" footer line below, commit + push it, then report to the user with the recovery prompt:
```
Session bandwidth exhausted at SN.M. v0.39.0 execution paused.
Resume in a fresh session by pasting `Docs/prompts/2026-05-12-v0.39.0-execution-handoff.md`.
```

## Progress log (auto-updated by the executing agent — keep this section as the very last thing in the file)

<!-- The agent appends a single line each time a stage closes, in chronological order. Read the LAST line to know the next stage to dispatch. -->

- [INIT 2026-05-12] Handoff prompt written; v0.39.0 execution about to dispatch S1.
- [S1 CLOSED 2026-05-12] products@0.1.0 scaffold green. 6 commits 8e38432..58de975. run-helper-cases 532→553 (+21 PROD-N sub-asserts). 5 FIX-LATER-NOTE candidates captured in Docs/plans/2026-05-12-v0.39.0-execution-log.md.
- [S2 CLOSED 2026-05-12] products finish green. 4 commits 07f8b2f..eddb303. run-helper-cases 554→556 (+2: PROD-4 green / PROD-5 red). 5 more FIX-LATER candidates logged (theme: coordinated placeholder-migration for runtime customjs paths).
- [S3 CLOSED 2026-05-12 + S3.7 fast-follow] teams@0.1.0 scaffold green. 7 commits 313fd66..c938c39. run-helper-cases 556→580 (+24: TEAM-1/2/3 = 8+7+7). S3.7 patched 2 Important reviewer findings (H2 wikilink literal-text rendering bug + defensive product.path guard). 6 more FIX-LATER candidates logged including coordinated nav-buttons icon-additions PATCH (package + users + edit-3).
- [S4 CLOSED 2026-05-12] teams finish green. 4 commits bd770ae..d5e4e12. run-helper-cases 581→584 (+3 TEAM-4/5/6). Templater product-picker enforces org-chart hierarchy at note-creation; validator catches anyone bypassing template. Quality review APPROVED clean — zero Critical/Important, 4 Minor polish FIX-LATERs.
- [S5 CLOSED 2026-05-12] project@1.5.0 → 1.6.0 MINOR shipped. 4 commits 78682be..be1d775. run-helper-cases 584→587 (+3 PROJ-1 green/PROJ-2 missing/PROJ-3 invalid-enum). frontmatter_branch rule-handling worked transparently. Quality APPROVED clean. **Cycle-hygiene gate: workshop self-install WILL FAIL between now and S7 close** (manifest declares project-action-buttons.js but file ships in S7). Do NOT run `sauce install` against workshop until S7 closes. **Next: dispatch S6 — ProjectsHubCards upgrade (7 sub-tasks: status pill + team/product chips + default-scope filter + status filter chips + team+product filter chips + group-by toggle + within-group sort).**
