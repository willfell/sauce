# Post-v0.71.0 next-cycle handoff

**Closed:** 2026-05-21
**Workshop baseline at handoff:** v0.71.0 on `origin/main` (cowork-presentation-polish shipped).

## What shipped in v0.71.0

Six presentation-layer changes for cowork plus two v0.70.0 carry-forward closures:
1. `display_names` map in `_canonical-vocab.json` for the 6 cowork-* atomic-note types
2. `summary:{required}` + `warnings:{optional}` on the 6 atomic-note rule_fragments
3. Nav-button block + Obsidian-native body shape (admonitions + markdown tables + summary) in 6 write-run-note sub-skills
4. Runtime MCP-availability detection in `gather-{calendar,gmail,imessage}` — emits `gather-skipped:` cleanly when no MCP wired (closes accuris "failed to pull google calendar")
5. 15 per-engagement default prompts drop hardcoded MCP names + add gather-skipped handling
6. Cowork Daily/Weekly/Monthly Hub ActivityFeed panels: framed renderer + scoped + cadence-ordered + display-name labeled
- Bonus: `groupPreviewBuilder` opt on activity-feed + 6 cowork-* sub-type pill colors in daily dashboard (closes v0.70.0 carry-forwards)

Versions: workshop 0.71.0 · activity-feed 0.5.0 · cowork 0.12.0 · daily 0.11.0.

Full details: `Docs/plans/2026-05-21-v0.71.0-cowork-presentation-polish-result.md`.

## Open follow-ups

### Carry-forwards from prior cycles (still open)
- **FLN-v68-3**: Edit-tool revert pattern observed mid-v0.68.0. Watched during v0.71.0; did not recur. Continue to track.
- **v0.70.0 lesson #5**: FA4 floor lex-compare bug — string lex compare breaks for two-digit minor versions (`"0.10.0" >= "0.5.0"` is `false`). Workaround at S11 by bumping the floor to 0.11.0; underlying fix deferred. Small, isolated, high value — worth a focused cycle.
- **v0.70.0 lesson #6**: cycle-status.md catalogue housekeeping. After v0.71.0, the Mechanisms table still shows several stale rows (`cards: 0.2.4` actual 0.2.6, `validator: 0.2.0` actual 0.3.0, `audit: 0.2.0` actual 0.3.0, `convenience: 0.2.4` matches but should re-verify, `entity-create: 0.3.0` actual 0.4.0). Blueprints table similarly drifted on rows v0.71.0 didn't touch. Targeted backfill pass.

### Carry-forwards new in v0.71.0
- **FLN-v71-1 — `build-test-verify.md` pre-stage-commit checklist**: pin-cascade across `run-*.js` recurred this cycle (S3 missed `FA6-MANIFEST-activity-feed` in `run-helper-cases.js`; required follow-up commit). Worth elevating into the build-test-verify guide as a hard "before every manifest bump, grep `<name>` + `<old-version>` across all `platform/test/run-*.js`" rule.
- **FLN-v71-2 — Test-label prefix scheme migration**: `HC-V07X` is ambiguous (v0.70.X patch vs v0.71.0 minor). v0.71.0 introduced `HC-V0710-*` (4-digit padded) for its new asserts but did NOT migrate existing v0.70.x labels. A follow-up cycle could renumber the v0.70.x labels and document a future-cycle convention (e.g., `HC-V<WWXY>-*` where WW is workshop-minor and XY is workshop-patch).
- **FLN-v71-3 — Plan-vs-reality drift detector**: Three plan-vs-reality mismatches surfaced during v0.71.0 implementation (test-label collision; fictional `types` array; Dataview frontmatter access pattern). All caught by implementer subagents reading actual code before editing. A future cycle could add a plan-time linter that compares plan-cited paths/keys against the actual repo state at the moment the plan commits.
- **FLN-v71-4 — CoworkLatestRuns / per-helper audit**: missed `cowork-latest-runs.js` from the v0.71.0 file list (caught in S10 follow-up). General lesson: when surfacing a new frontmatter field, audit ALL helpers reading the affected pages, not just the obvious renderer.

### Cycle-time v0.70.0 carry-forwards still open
- **Generalize framed renderer to outer Tasks + Meetings sections** on the daily dashboard. Visual continuation of v0.70.0/v0.71.0 polish.
- **Vault-config overrides for `defaultClosed`** per blueprint.
- **Cowork display-name editing UI** for end-users (platform-shipped this cycle; could grow a user-overridable layer).

### v0.71.0-shipped, awaiting user-side smoke
- **Cowork-* sub-type pill colors** — shipped in v0.71.0 S10; visual smoke pending.
- **One-line preview for collapsed groups** — shipped in v0.71.0 S2 + S10; visual smoke pending on the scratch group.
- **FLN-v68-1: accuris-sauce weekly-snapshot.md cleanup** — user-side re-bootstrap pending (workshop dogfood doesn't have an `accuris` engagement).

## Likely next-cycle candidates (rank-ordered by ROI estimate)

1. **FA4 lex-compare fix** (FLN-v71-1 prerequisite) — small, isolated, unblocks proper semver-floor checks across the whole harness suite. ~1-2 stage cycle.
2. **Build-test-verify.md pre-stage-commit checklist** (FLN-v71-1) — pure docs, but elevates a recurring lesson into pre-flight discipline. ~1 stage.
3. **Cycle-status.md catalogue backfill** (FLN-v71-1 prerequisite + v0.70.0 lesson #6) — pure docs but high cumulative drift. ~1 stage to backfill all stale rows + cross-check against `platform/manifest.json`.
4. **Generalize framed renderer to outer sections** — medium scope; visual continuation. The substrate (activity-feed v0.5.0 + Quiet Frames CSS) is in place; daily dashboard outer Tasks + Meetings sections still use the legacy `<details>` shape. ~3-4 stages.
5. **User-side smoke findings** from v0.71.0 — likely a v0.71.x patch series rather than a separate cycle.

## Starting points for the next session

Read in this order:
1. `Docs/plans/2026-05-21-v0.71.0-cowork-presentation-polish-result.md` — what just shipped + the lesson chain
2. `Docs/agent-guides/cycle-status.md` — live workshop state (note: stale rows flagged in this handoff's open-followups)
3. Any user-side smoke findings — open as a punch list at the top of the next session
4. The `## Likely next-cycle candidates` ordering above — pick one or pause for user direction
