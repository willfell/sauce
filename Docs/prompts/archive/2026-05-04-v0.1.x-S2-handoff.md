---
date: 2026-05-04
purpose: Onboard a fresh agent session to drive Stage 2 (T2.1 → T2.8) of the v0.1.x validator-subsystem patch cycle. S1 closed + pushed; S2 is the cycle close.
predecessors:
  - Docs/plans/2026-05-04-v0.1.x-validator-subsystem-design.md (cycle design, approved)
  - Docs/plans/2026-05-04-v0.1.x-validator-subsystem-plan.md (task-by-task plan)
  - Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T1.1-T1.3-installer-validator-bootstrap.md (S1 implementer log)
gates:
  - S1 CLOSED 2026-05-04 — commit f1916d6 pushed to origin/main
  - S2 acceptance gate per design §6 + plan S2 acceptance section
target_consumer: tmp-test-barebones-vault
cycle_close_artifact: Docs/plans/<close-date>-v0.1.x-validator-subsystem-result.md
---

# Onboarding — v0.1.x validator-subsystem patch, Stage 2 + cycle close

> [!info] Project identity (load-bearing)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git` (HTTPS: `https://github.com/willfell/beacon`) — personal account `willfellhoelter@gmail.com`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/poc-vault`
> - **Sibling vaults you'll touch:**
>   - `../tmp-test-barebones-vault/` — primary regression target consumer
>   - `../tmp-acc-vault/` — secondary consumer
> - **Distribution model (current):** workshop pushes via bootstrap-copy resync of `platformInstall.js` (3 copies) + canonical `platform/install.js`. v0.1.2 (next cycle) retires this with a thin git-pull stub.

> [!warning] NEW git workflow (decided 2026-05-04, codified in CLAUDE.md "Git workflow" section)
> Beacon is now a real published repo and **commits + pushes to `main` are part of the active development flow**. Single-branch workflow; no feature branches; no PR review yet.
> - **Commit at every stage close** with conventional-commits style messages (HEREDOC for multiline).
> - **Push to `origin/main` after each commit** (`git push origin main`).
> - **DO NOT sign as Claude.** No `Co-authored-by: Claude` trailer; author is the user.
> - **DO NOT use `--no-verify`** unless the user explicitly approves.
> - **Force-push + history rewrite still require explicit approval.**

---

## Mission

Drive Stage 2 of the v0.1.x validator-subsystem patch to cycle close: ship the runner templates (`Validate.md` + `Audit.md`), declare `external_plugins[]` on the three subscribing manifests, run the four manual smokes, file the result writeup, commit + push S2 + result.

**S1 closed at `f1916d6` 2026-05-04.** All Stage 1 work is on `origin/main`. Validator hook is no longer crashing on the documented usage; semantic `checkRequiredBlocks` is in place; bootstrap copies × 3 byte-identical (md5 `de21110fb7b778d23727a9d510881457`).

---

## Required reads (don't skip; ~15 minutes)

> [!todo] In this order
> 1. **`Docs/plans/2026-05-04-v0.1.x-validator-subsystem-design.md`** — full cycle design. §1 (manifest schema delta `external_plugins[]`), §2 (runner templates), §6 (manual smoke acceptance), §7 (open trail — Slash Commander id verification + audit-walker output path).
> 2. **`Docs/plans/2026-05-04-v0.1.x-validator-subsystem-plan.md`** — Stage 2 tasks T2.1 → T2.8. Read fully; each task has step-by-step commands.
> 3. **`Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T1.1-T1.3-installer-validator-bootstrap.md`** — S1 implementer log. Confirms what code paths are now in place; you build on top.
> 4. **`CLAUDE.md`** (workshop root) — non-negotiables, "Git workflow" section (NEW 2026-05-04), "Ask before acting" gates.
> 5. **`Docs/landmines.md`** — all 11 entries non-negotiable. #6 JSON-only metadata, #9 substitution scope, #10 triple-bump protocol most relevant this stage.
> 6. **`platform/install.js`** — `applyExternalPlugins` at lines 656-746; wired into `installItem` at line 269. This is the helper your S2 manifest declarations exercise.
> 7. **`platform/mechanisms/validator/validate.js`** — current shape post-S1. The runner template you're writing in T2.2 calls into this.
> 8. **`platform/mechanisms/audit/audit-walker.js`** — read to verify what function name is exposed (e.g., `tp.user.auditWalker`?) + what output path it writes to. T2.1 step 2.1.3 explicitly requires this verification before you write `Audit.md`.
> 9. **`platform/mechanisms/validator/manifest.json` + `platform/mechanisms/audit/manifest.json` + `platform/blueprints/project/manifest.json`** — these are the three manifests you'll modify in T2.2 + T2.3 (add `files[]` entries + `external_plugins[]` arrays).
> 10. **`platform/test/run-install.js` + `platform/test/run-renderer.js`** — load-bearing harnesses. Don't bypass.

> [!info] Reference reads (skim if needed)
> - `Docs/why.md` / `Docs/how.md` / `Docs/use.md` — concept docs.
> - `Docs/plans/2026-05-03-registry-driven-nav-buttons-result.md` — v0.1.1 result writeup; §"T4.7 deferred — four real pre-existing bugs surfaced" + Postscript document the four findings this cycle closes.
> - `Docs/plans/execution-logs/2026-05-03-registry-driven-nav-buttons/T4.6-T4.7-manual-smokes.md` — T4.7 detailed diagnostic; helpful context for what's being fixed.
> - `Docs/plans/execution-logs/2026-05-03-registry-driven-nav-buttons/T4.0-T4.9-S4-harness-and-barebones-regression.md` — example bundled-implementer log shape for reference when you write your own log.

---

## What's already done (don't re-do)

> [!success] S1 deliverables — all CLOSED
> - **`platform/install.js`** — `applyExternalPlugins(tp, manifest, history)` helper added (read-only check of `.obsidian/community-plugins.json`; failure-loud Notice + history; C4 malformed-JSON guard parity). Wired into `installItem` after `applyNavButtons`.
> - **`platform/mechanisms/validator/validate.js`** — full rewrite: `resolveTFile` defensive input resolution, `whenMatches` predicate evaluator (extracted from inline), USAGE comment refresh, semantic `checkRequiredBlocks` against `kind`/`must_call`/`via` schema using customjs-guard wrapper regex. **`spec.content` substring path is gone.**
> - **Bootstrap copies × 3** — `platformInstall.js` in workshop + tmp-test-barebones-vault + tmp-acc-vault all byte-identical to canonical `platform/install.js`. md5: `de21110fb7b778d23727a9d510881457`.
> - **Harness sweep green** — workshop install + barebones install + renderer harness against barebones all exit 0.
> - **Reviews green** — spec-compliance ✅; code-quality APPROVED-WITH-FOLLOWUPS, M1 applied inline (renamed local `path` → `pluginsPath` in `applyExternalPlugins` to avoid Node `path` module shadowing).
> - **Commit `f1916d6` pushed to `origin/main`.**
>
> **Closed:** T4.7 Findings #3 (validate.js input resolution) + #4 (checkRequiredBlocks schema mismatch) — code paths only. #1 (slash-cmd UX) + #2 (runner templates) close in S2.

---

## What S2 needs to ship

> [!info] Source of truth
> The plan at `Docs/plans/2026-05-04-v0.1.x-validator-subsystem-plan.md` is the canonical spec. T2.1 → T2.8 each have step-by-step commands + acceptance criteria. Don't paraphrase; execute.

### Implementer-coverable (dispatch one or more bundled implementer subagents)

| Task | Surface |
|---|---|
| **T2.2** | Create `platform/mechanisms/validator/templates/Validate.md` + `platform/mechanisms/audit/templates/Audit.md`; add `files[]` entries to validator + audit manifests pointing to `{{templates_path}}/Validate.md` + `{{templates_path}}/Audit.md`. |
| **T2.3** | Add `external_plugins[]` arrays to validator + audit + project manifests using the **locked Slash Commander plugin id from T2.1**. |
| **T2.5** | Update `Docs/use.md` with Slash Commander setup section (install, map keywords, expected output). Update `CLAUDE.md` status snapshot section to log v0.1.x close. |
| **T2.7** | Draft result writeup at `Docs/plans/<cycle-close-date>-v0.1.x-validator-subsystem-result.md` — frontmatter + sections per the plan T2.7. Mirror the v0.1.1 result writeup shape (`Docs/plans/2026-05-03-registry-driven-nav-buttons-result.md`). |

### Master-agent / user-driven (cannot be implementer-dispatched)

| Task | Why manual |
|---|---|
| **T2.1 (discovery)** | Requires user to install Slash Commander in barebones Obsidian first (one-time prereq), then read `community-plugins.json` to lock the plugin id. Also: read `audit-walker.js` to verify the actual exported function name + output path before T2.2 writes `Audit.md`. |
| **T2.4 (harness re-run)** | Verification step — master agent runs install harness against barebones with Slash Commander enabled; expects no `external_plugins` warnings. Also verifies workshop self-install behavior (Slash Commander likely NOT installed in workshop → expected warnings; document as acceptable per plan §T2.4.4). |
| **T2.6 (manual smokes ×4)** | Obsidian-in-the-loop. Smoke A `/new-project`, B `/validate`, C `/audit`, D `external_plugins[]` warning path. User does the keystrokes; agent captures evidence in T2.6 log. |
| **T2.8 (cycle close commit + push)** | Master agent stages all S2 changes, commits with conventional-commits message (HEREDOC), pushes to `origin/main`. |

### S2 acceptance gate (must pass before cycle close)

- [ ] Slash Commander plugin id locked from disk + verified
- [ ] `Validate.md` + `Audit.md` ship under each mechanism's `templates/`
- [ ] `external_plugins[]` declared on validator + audit + project manifests
- [ ] Re-run install harness on barebones with Slash Commander ENABLED → exit 0, NO `warning, step: external_plugins` entries
- [ ] All 4 manual smokes PASS (per design §6)
- [ ] Result writeup committed + pushed
- [ ] Workshop self-install still green (warnings-on-validator/audit acceptable if Slash Commander not in workshop)

---

## Open trail items (resolve early in S2)

> [!warning] Verify before locking schema
> | # | Question | Disposition |
> |---|---|---|
> | 1 | Slash Commander's actual plugin id (`obsidian-slash-commander`? `slash-commander`? something else?) | T2.1 step 2.1.2 — install in barebones first; read `.obsidian/community-plugins.json`; lock the exact string. **Do NOT guess.** All three manifest declarations + the use.md doc reference + commit messages cite this id. |
> | 2 | Audit-walker's exported function name (e.g., `tp.user.auditWalker`?) + output path | T2.1 step 2.1.3 — read `platform/mechanisms/audit/audit-walker.js`; verify the function name in the body of `Audit.md` matches; verify the Notice's "see <output-path>" matches the actual write path. |
> | 3 | Validate.md output posture | **Locked: notice-only + `console.log`** (per design §7 disposition). |

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - **No personal content** in workshop.
> - **JSON only** for platform metadata (landmine #6).
> - **Failure-loud posture** — every error path = Notice + history entry. C4 malformed-JSON guard for any new file read.
> - **Substitution variables only from `platform-config.json:variables`** (landmine #9).
> - **Triple-bump protocol** for tests that need to re-run apply* fns (landmine #10).
> - **Module-directory invariant under `beacon/`** (landmine #11) — N/A this stage; no blueprint content materializing.
> - **Bootstrap copies × 3 STILL IN FORCE** this cycle (retired in v0.1.2). Any change to `platform/install.js` requires resyncing all 3 vault-local copies + md5 verification.
> - **Workshop dogfods every release.** Self-install must be green at S2 close.
> - **Workshop_version held at 0.4.0** — DO NOT bump this cycle.
> - **Mechanism versions held** — validator + audit stay at 0.1.0 (bug-fix-grade, not minor-grade); the triple-bump protocol forces re-processing under test.
> - **Desktop-only.** Mobile unsupported.
> - **Ask before** — `.obsidian/` edits, hand-editing platform-installed.json, removing platform/blueprints/* after promotion, force-push, history rewrite. Bumping workshop_version. **Pushes to `origin/main` are NO LONGER ask-before** — they're part of normal flow per the Git workflow section in CLAUDE.md.

---

## Pacing preference

Stage-level checkpoints with bundled implementer dispatches. Surface to user only at:
- T2.1 boundary (need Slash Commander install confirmation + plugin id from disk)
- T2.6 boundary (manual smokes — user keystrokes required)
- T2.8 boundary (cycle close — push to main; ask if user wants to defer push)

Don't fall back to manual Obsidian-in-the-loop for anything the harnesses cover. Manual is reserved for T2.6 smokes.

---

## Required sub-skill

Use **`de:executing-plans`** to drive this stage in the new session. (S1 used `de:subagent-driven-development` in the prior session; the user explicitly chose to switch to a separate-session execution for S2.)

The plan at `Docs/plans/2026-05-04-v0.1.x-validator-subsystem-plan.md` is the executing-plans input.

---

## Suggested first moves

1. **Read everything in the required-reads list above.** Confirm absorption by referencing specifics from the design + plan + S1 implementer log.
2. **Confirm git state:** run `git status` (should be clean; on `main`, up-to-date with `origin/main` at `f1916d6`). Run `git log --oneline -5` to verify.
3. **Surface T2.1 prereq to the user via AskUserQuestion:** "Is Slash Commander already installed + enabled in barebones, or do you need to do it now?" Don't proceed with T2.2 / T2.3 until the plugin id is locked from disk.
4. **Once plugin id is locked:**
   - T2.2 (runner templates + manifest `files[]` entries) — implementer-dispatchable, ~5 minutes.
   - T2.3 (`external_plugins[]` declarations) — implementer-dispatchable, ~3 minutes.
   - T2.4 (harness re-run + verify no warnings on barebones with Slash Commander enabled) — master agent.
5. **T2.5 doc updates** — implementer-dispatchable; mirror prior cycle's CLAUDE.md status snapshot edits.
6. **T2.6 manual smokes** — surface walkthrough to user; capture evidence in `T2.6-manual-smokes.md` execution log.
7. **T2.7 result writeup** — implementer-dispatchable once all smokes pass.
8. **T2.8 close** — master agent: stage all S2 files, commit with HEREDOC, push to `origin/main`.

---

## Where to log

- Per-task / bundled implementer dispatches → `Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T<task>-<slug>.md`
- T2.1 discovery → `Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T2.1-discovery.md`
- T2.6 manual smokes → `Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T2.6-manual-smokes.md`
- Cycle close result → `Docs/plans/<close-date>-v0.1.x-validator-subsystem-result.md`

Existing log directory `Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/` already created (T1.1-T1.3 log lives there).

---

## Reference summary

| Path | Purpose |
|---|---|
| `CLAUDE.md` | Vault identity + non-negotiables + NEW Git workflow section + ask-before gates |
| `Docs/Index.md` | Documentation entry point |
| `Docs/why.md` / `Docs/how.md` / `Docs/use.md` | Concept docs |
| `Docs/landmines.md` | 11 entries; #6 JSON-only, #9 substitution scope, #10 triple-bump most relevant |
| `Docs/plans/2026-05-04-v0.1.x-validator-subsystem-design.md` | Cycle design (approved) |
| `Docs/plans/2026-05-04-v0.1.x-validator-subsystem-plan.md` | THIS STAGE'S PLAN |
| `Docs/plans/execution-logs/2026-05-04-v0.1.x-validator-subsystem/T1.1-T1.3-installer-validator-bootstrap.md` | S1 implementer log |
| `Docs/plans/2026-05-03-registry-driven-nav-buttons-result.md` | Prior cycle result (the four findings origin) |
| `platform/install.js` | Installer post-S1 (incl. `applyExternalPlugins`) |
| `platform/mechanisms/validator/validate.js` | Validator post-S1 (incl. resolveTFile + semantic checkRequiredBlocks) |
| `platform/mechanisms/audit/audit-walker.js` | Verify function name + output path before writing Audit.md |
| `platform/mechanisms/validator/manifest.json` | Add `templates/Validate.md` to `files[]` + `external_plugins[]` |
| `platform/mechanisms/audit/manifest.json` | Add `templates/Audit.md` to `files[]` + `external_plugins[]` |
| `platform/blueprints/project/manifest.json` | Add `external_plugins[]` |
| `platform/test/run-install.js` | Install harness — load-bearing |
| `platform/test/run-renderer.js` | Renderer harness — load-bearing |
| `Docs/plans/2026-05-03-multi-vault-automation-design.md` | NEXT CYCLE (v0.1.2) — git-pull + thin stub |
| `Docs/plans/2026-05-03-boards-blueprint-design.md` | CYCLE AFTER NEXT (v0.2.0) — boards blueprint |

---

## After cycle close

Hand off to **v0.1.2** per `Docs/plans/2026-05-03-multi-vault-automation-design.md`. v0.1.2's S1 captures the post-v0.1.x state as the initial named git tag (per the design's S1 acceptance step). Workshop_version stays at 0.4.0 through v0.1.x; v0.1.2's S2 bumps to 0.4.1.

End of onboarding. Next session should be able to drive S2 → cycle close → result writeup → commit + push without further context from the prior session.
