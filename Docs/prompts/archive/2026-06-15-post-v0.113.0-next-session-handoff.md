# Next-session handoff — post v0.113.0

**Paste this into the next session as the opening prompt.**

You're picking up after **v0.113.0** closed (schema registry Stage A — workshop-side index + linter). Workshop at **0.113.0**, finance at **0.7.2**, base commit (TBD — set after S6 commit).

## Try the tooling first

```bash
npm run status              # workshop state in 250ms (v0.112.0 surface)
npm run lint-schemas        # validate platform/schemas-index.json (v0.113.0 surface)
npm run lint-schemas -- --list   # one-line per indexed schema
```

If both succeed and the registry list looks right, you have the full context. Read on for the open decisions.

## Outstanding decisions for the user

1. **Push + tag v0.113.0?** Local main is ahead of origin/main; preflight green (except parallel-cycle V01120 failure not my scope). Tag `v0.113.0` is user-gated per ask-before-acting.
2. **Pick the next lane.** Options:
   - **Stage B — consumer-side runtime schema validation mechanism.** Only if Stage A doesn't catch enough; needs explicit signal. v0.114.0 MINOR.
   - **v0.113.x PATCH candidates** (from v0.113.0 result § Carries forward): workshop-status schema row; orphan detector; version-pin drift hard-fail; regen-cycle-status `--check` as CI gate; `.gitignore` cleanup.
   - **Bundle 2 — finance cohesion polish** (from v0.111.x brainstorm): #1 debt-payment exclusion + #3 click-through + #5 FinanceYearReview.
   - **Brew tap formula bump** (still stale at v0.84.0).
   - **cowork:doctor** (10 cycles displaced).
3. **Parallel `v0.112.0-finance-cohesion` cycle is in flight.** Uncommitted `platform/install.js` modifications + V01120-FHR-FHS-2 test failure. That cycle will resolve when it closes — separate workstream from v0.113.0.

## Read first, in order

1. `Docs/plans/2026-06-15-v0.113.0-schema-registry-result.md` — what just shipped + 5 lessons.
2. `Docs/agent-guides/schemas.md` — NEW; the answer to "what schemas should I read before designing X?"
3. `platform/schemas-index.json` — the registry itself (20 entries).
4. `Docs/agent-guides/cycle-status.md` — auto-regenerated at S6; reflects v0.113.0.
5. `Docs/plans/2026-06-15-v0.112.0-workshop-tooling-result.md` — prior cycle (workshop tooling sprint that built workshop-status / regen-cycle-status / scaffold-harness / dev-workflow.md).
6. `Docs/plans/2026-06-15-v0.111.x-enhancements-brainstorm.md` — still-open lane picker.

## What you should NOT do unprompted

- **Do not push to origin/main without user confirmation.** Workshop just bumped to v0.113.0 locally.
- **Do not tag v0.113.0 without explicit approval.**
- **Do not start a new cycle without picking a lane.**
- **Do not auto-edit the schema index.** Use the registry's `kind` enum + `owner` catalog + manual seeding. Auto-discovery is a v0.113.x PATCH candidate, NOT a present capability.
- **Do not touch the parallel `v0.112.0-finance-cohesion` work** unless the user explicitly asks. That cycle's uncommitted `platform/install.js` mods are someone else's surface.

## Quick state-check commands

```bash
# Sauce workshop survey
npm run status

# Schema registry summary
npm run lint-schemas
npm run lint-schemas -- --list
npm run lint-schemas -- --json | python3 -c "import json,sys;d=json.load(sys.stdin);print(d['summary'])"

# Cycle-status freshness
npm run regen-cycle-status -- --check

# Full preflight
npm run release:preflight 2>&1 | grep -E "^Result:|FAIL "

# Behavioral harnesses
node platform/test/run-v01103-monthly-overview.js 2>&1 | tail -3
```

Expected: workshop 0.113.0 / finance 0.7.2; HEAD `<S6 commit>`; lint-schemas exit 0 with 20 schemas indexed; preflight may have 1 parallel-cycle failure (V01120-FHR-FHS-2 — NOT v0.113.0 scope); behavioral 91 / 0.

## Decision trees if user is vague

- **"Continue with whatever makes sense."** → Try Stage A end-to-end (`npm run lint-schemas -- --list`, browse `schemas.md`, simulate "I'm designing a new feature that touches the eod-review sidecar — which schemas are relevant?"). If it feels useful, return to Bundle 2 (finance cohesion polish). Defer Stage B.
- **"Lowest-risk thing."** → v0.113.x PATCH: orphan detector. ~2h. Walks known schema dirs, warns about files not in the index. Pure additive workshop-side.
- **"Push and tag what we have first."** → Confirm git state; ask which tags to create (v0.112.0 + v0.113.0 are the obvious anchors); then `git push` + `git tag -a` per build-test-verify.md.
- **"Build the consumer-side validator now."** → Stage B. v0.114.0 MINOR. Use Stage A's index as the source of truth. Estimated ~12-16h.

## Reference: the v0.112.0 + v0.113.0 workshop tooling surface

| Command | Use it for |
|---|---|
| `npm run status` | First command every session. Workshop survey in 250ms. |
| `npm run regen-cycle-status` | At cycle close, after writing the result doc. |
| `npm run scaffold-harness -- <cycle-id> <topic>` | Starting a per-cycle behavioral harness. |
| `npm run lint-schemas` | Validate the schema registry. Runs in preflight. |
| `npm run lint-schemas -- --list` | Browse what's indexed before designing a new feature. |
| `scripts/dev-sync.sh` | End-of-cycle multi-consumer sync. |

| Guide | Reach for it when |
|---|---|
| `Docs/agent-guides/dev-workflow.md` | First session, picking up a cycle, `scripts/*` confusion. |
| `Docs/agent-guides/schemas.md` | About to design a new feature; need to know what schemas it touches. |
| `Docs/agent-guides/build-test-verify.md` | Cycle close protocol; preflight; release workflow. |
| `Docs/agent-guides/cycle-status.md` | Live state pointer (auto-regenerated). |

End of handoff prompt.
