---
date: 2026-05-13
purpose: Next-cycle entry handoff after v0.38.1 people-α-seed close. v0.38.1 applied the v0.38.0 substrate to a non-pilot blueprint (people: Alice / Bob / Carol), retiring meetings-seed dangling-wikilinks; whole-suite preflight green at 1218 sub-asserts across 14 harnesses; tag `v0.38.1` annotated and pushed.
predecessors:
  - Docs/plans/2026-05-13-v0.38.1-result.md (v0.38.1 cycle close writeup)
  - Docs/prompts/2026-05-13-v0.38.1-people-seed-handoff.md (the v0.38.1 entry handoff — handoff-as-plan precedent)
  - Docs/plans/2026-05-13-v0.38.0-result.md (v0.38.0 cycle close writeup)
  - Docs/plans/2026-05-12-deploy-confidence-substrate-design.md (substrate spec)
  - Docs/prompts/2026-05-13-post-v0.38.0-next-cycle-handoff.md (the v0.38.0 next-cycle menu — this is its successor)
gates:
  - HEAD at v0.38.1 close commit on origin/main
  - tag v0.38.1 annotated + pushed
  - npm run release:preflight exits 0 (1218 sub-asserts + 36 renderer cases across 14 harnesses)
  - workshop_version: "0.38.1" + package.json version: "0.38.1" (synced; verified by scripts/check-version-sync.js)
target_artifact: brainstorm the next cycle from the curated menu below, then write a per-cycle plan (or — for substrate-application cycles in Batch B — write a handoff-as-plan, per v0.38.1 precedent)
---

# Post-v0.38.1 next-cycle entry handoff

v0.38.1 closed cleanly. The v0.38.0 substrate has now been applied to its first non-pilot blueprint (people), proving the scaling story works as designed: a substrate-application cycle costs ≈3 file edits + a smoke expectation row + a version triple, and can be executed autonomously from a tightly-specified handoff doc without a separate plan or design cycle.

Next cycle TBD. This handoff is the curated menu — pick one and brainstorm before writing a plan (or a handoff-as-plan for Batch B candidates).

## Required pre-cycle reads (~10 minutes)

1. `Docs/plans/2026-05-13-v0.38.1-result.md` — v0.38.1 cycle close writeup with verification artifacts, pacing rhythm, lessons.
2. `Docs/prompts/2026-05-13-v0.38.1-people-seed-handoff.md` — the v0.38.1 entry handoff. **This is the canonical template for any Batch B substrate-application cycle.**
3. `Docs/plans/2026-05-12-deploy-confidence-substrate-design.md` — substrate spec, especially § "How this composes with future cycles".
4. `CLAUDE.md` § Status (live) — current workshop_version (0.38.1), mechanisms (10), blueprints (11 — `people@0.1.2` newest), harness counts (14 harnesses / 1218 sub-asserts).
5. `Docs/cycle-history.md` § v0.38.1 (and § v0.38.0 below it) — archived narratives for the two cycles just closed.

## Candidate cycles (no priority order — brainstorm to pick)

### A. v0.34.0 — sauce claude cohesion wave 3 (still pending from v0.33.0 close)

The original wave-3 cycle slot, reserved since v0.32.0. Scope:
- 6 remaining blueprints adopt `claude_surface[]`: boards, people, to-do, finance, journal, trips.
- `Docs/Meta/<X>-System.md` retirement (these were the pre-v0.32.0 doc-as-code source; superseded by claude_surface).
- `cowork@0.x.x` backwards-compat `manifest.skills[]` shim removal (the shim was a v0.32.0 stop-gap; safe to remove now that all blueprints emit claude_surface[]).

Effort: 6 blueprint manifest edits + 6 new SKILL.md files + 6 new slash commands + retirement sweep + cowork shim removal. ~80-100 lines per blueprint × 6 + cleanup. Similar pacing to v0.33.0 wave-2 (3 blueprints).

### B. Remaining blueprint seed coverage (7 small cycles remaining after v0.38.1)

Each blueprint that didn't get a seed in v0.38.0 or v0.38.1 needs its own ~50-line cycle. v0.38.0 shipped pilots for `project` (α), `daily` (β), `meetings` (α). v0.38.1 shipped `people` (α). Remaining: `boards`, `cowork`, `journal`, `to-do`, `trips`, `finance`, `scratch`.

Each follows the v0.38.1 template:
- `platform/blueprints/<bp>/seed/seed.json` (α) or `seed.js` (β) per the substrate.
- A few representative entries; if cross-blueprint references exist, accept dangling-link warnings (and resolve them in a follow-up cycle by seeding the referenced blueprint, as v0.38.1 did for meetings → people).
- `run-integration-smoke.js` gains one `expectations` array row.
- Three-version lockstep bump: workshop_version PATCH, package.json PATCH, blueprint manifest PATCH.
- Single bundled cycle-close commit (CLAUDE.md status + cycle-history archive + result writeup + next-cycle handoff successor).
- Cadence: controller-direct, autonomous execution from a tightly-specified handoff-as-plan. v0.38.1's handoff is the canonical template.

Effort per blueprint: 30-45 minutes (proven by v0.38.1). Suggested batching:
- One cycle per blueprint (v0.38.2 = boards, v0.38.3 = to-do, etc.) — keeps each cycle small + green.
- OR bundle 2-3 blueprints into one cycle if they're independent (e.g., to-do + boards together — boards depends on no other blueprint's notes).

Picking-order considerations:
- `boards` and `to-do` are independent — no cross-blueprint references.
- `journal` and `trips` are independent.
- `finance` might benefit from a programmatic (β) seed (transactions across a date range).
- `cowork` and `scratch` are smaller; could bundle.

### C. v0.39.x — bar iii (renderer-stub upgrade)

Extends `run-renderer.js` to cover Dataview view compilation + Templater template rendering against seeded notes. Today's `run-renderer.js` covers nav-button rendering only (36 cases). Bar iii would:
- Stand up a Node-side Obsidian/Templater stub.
- Render each seeded blueprint's templates against the seeded vault.
- Catch breakage in templates, custom JS classes, Dataview view code.

Reuses the v0.38.0 seed substrate (seeded vault → renderer stub input). Significant work — own design cycle first.

### D. v0.40.x — bar iv (upgrade-survival)

Smoke runs twice: at `git checkout v0.38.0` pantry, then at HEAD. Asserts state survives the version delta. Adds checkout/install overhead — significant CI runtime cost. Catches drift across versions. Reuses everything below it.

### E. Small housekeeping cycle — v0.38.0 FIX-LATERs (still pending after v0.38.1)

Bundle the v0.38.0 FIX-LATER-NOTEs into a single small cycle:
- (a) `sauce migrate-layout` pass against `/Users/willfellhoelter/notes/sauce/test-vault` so it becomes the proper dogfood target.
- (b) Replace `.gitignore` `/Scripts/` rule with explicit named-file rules to avoid the APFS lowercase collision with `scripts/`.
- (c) Add transactional pre-check to `cmd-seed.js --reset` path so a mid-loop throw can't partially-delete.
- (d) Document `--mechanisms=all` requirement for smoke harnesses in plan-template guidance.
- (e) Consider per-blueprint `seed-template.md` (separate from user-facing `templates/*.md`) so seeded notes don't contain Templater `<% %>` literals. v0.38.1 reaffirms this is harmless-but-visually-confusing — still a fair FIX-LATER candidate.

Effort: ~half-day. Bundled commit; tag v0.38.x PATCH.

## Brainstorm prompts (use the `superpowers:brainstorming` skill)

Pick one of the candidate cycles above and brainstorm:
- What is the smallest scope that ships value?
- What are the gotchas / landmines this cycle will hit?
- What's the test strategy?
- Who is the consumer of this cycle's output?
- What's the rollback / recovery story?
- (For Batch B) Does this blueprint's seed retire a known-limitation from a prior seed? (e.g., v0.38.1 retired meetings dangling-wikilinks)

## Recommended next pick (rationale, not mandate)

**Batch B, item 2 = `boards` seed (or `to-do` seed)** is the natural continuation of v0.38.1's momentum:

- Same shape, same pacing, same cadence. Handoff-as-plan template already proven.
- No cross-blueprint reference complications (boards is self-contained).
- Continues the demonstration that the substrate scales — three blueprints in three cycles (project / daily / meetings in v0.38.0; people in v0.38.1; boards in v0.38.2).
- ~30-45 minutes per cycle = high cadence, high signal.
- Each cycle is a fully-closed unit of value (one more seed in the integration smoke; one more brewable release).

Alternatively, if cycle fatigue is setting in or larger work is desired, **Cycle A (claude cohesion wave 3)** is the right pivot — it's a multi-stage cycle with subagent-driven cadence and ships a meaningful architectural retirement (`Docs/Meta/<X>-System.md`).

## State at handoff time

```
HEAD: <T4 commit SHA on origin/main>
tag: v0.38.1 (annotated, pushed)
workshop_version: 0.38.1
package.json version: 0.38.1
people manifest version: 0.1.2

preflight: green
- run-claude-surface.js: 171/0
- run-helper-cases.js: 530/0
- run-renderer.js: 36 cases
- run-bootstrap.js: 58/0
- run-cli.js: 100/0
- run-install-sh.js: 5/0
- run-migrate.js: 104/0
- run-migrate-layout.js: 18/0
- run-registry.js: 18/0
- run-doctor-self.js: 3/0
- run-audit.js: 74/0
- run-cowork-smoke.js: 92/0
- run-seed.js: 31/0
- run-integration-smoke.js: 17/0
Total: 1218 sub-asserts + 36 renderer cases across 14 harnesses

Mechanisms (10): customjs-guard@1.0.0, validator@0.1.2, audit@0.2.0, nav-buttons@2.6.0, cards@0.2.4, accent-button@0.1.0, people-rendering@0.1.0, styling@0.1.2, convenience@0.1.0, platform-claude@0.1.0
Blueprints (11): boards@0.1.0, cowork@0.3.0, daily@0.3.0, journal@0.1.2, meetings@0.4.0, people@0.1.2, project@1.5.0, scratch@0.1.0, to-do@0.1.4, trips@0.1.7, finance@0.2.10
```

## Slash command for next session

For a Batch B substrate-application cycle (boards / to-do / journal / etc.), paste this into a fresh chat AFTER writing the per-cycle handoff doc at `Docs/prompts/2026-05-13-v0.38.x-<blueprint>-seed-handoff.md`:

```
/superpowers:subagent-driven-development

AUTONOMOUS RUN: Execute v0.38.x PATCH cycle — <blueprint> α-seed (or β-seed). Four tasks (T1 seed, T2 smoke expectations, T3 version bump + tag, T4 cycle close docs). No user check-ins; run to v0.38.x tag push + cycle close.

Handoff (READ FIRST): /Users/willfellhoelter/projects/repos/sauce/Docs/prompts/2026-05-13-v0.38.x-<blueprint>-seed-handoff.md

State: HEAD <SHA> on main (v0.38.1 closed); preflight green at 1218 sub-asserts across 14 harnesses; workshop_version 0.38.1 + package.json 0.38.1 synced.

Mission: complete T1-T4, tag v0.38.x, perform cycle-close protocol. Total scope ~30-45 minutes.

Working directory: /Users/willfellhoelter/projects/repos/sauce. Direct push to main. No Co-authored-by trailers.
```

For Cycle A (claude cohesion wave 3) or Cycle C/D (bar iii / iv), brainstorm + write a full plan first (multi-stage cycles benefit from per-stage commits + subagent-driven cadence with reviewer dispatch).

## A note to the next agent

v0.38.1 proved the substrate scales. Batch B is now a high-cadence rhythm: pick a blueprint, write a 30-line handoff doc that quotes the seed.json content + the smoke expectation row + the version-bump triple, dispatch the slash command, walk away. Each cycle is a fully-closed unit.

Don't over-think Batch B picks — they're independent of each other. Just pick the next blueprint that has no cross-blueprint reference complications, write the handoff, run the cycle.

The momentum is the value. v0.38.0 → v0.38.1 → v0.38.2 → ... = each cycle one more seed in the integration smoke; each cycle one more brewable release; each cycle visible proof that the substrate works.
