---
date: 2026-05-13
purpose: Next-cycle entry handoff after v0.38.0 deploy-confidence-substrate close. v0.38.0 shipped seeders + `sauce seed` verb + integration smoke + CI matrix + release-gate; whole-suite preflight green at 1216 sub-asserts across 14 harnesses; tag `v0.38.0` annotated and pushed.
predecessors:
  - Docs/plans/2026-05-13-v0.38.0-result.md (cycle close writeup)
  - Docs/plans/2026-05-12-deploy-confidence-substrate-design.md (spec)
  - Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md (plan)
gates:
  - HEAD at v0.38.0 close commit on origin/main
  - tag v0.38.0 annotated + pushed
  - npm run release:preflight exits 0 (1216 sub-asserts + 36 renderer cases across 14 harnesses)
  - workshop_version: "0.38.0" + package.json version: "0.38.0" reconciled
target_artifact: brainstorm the next cycle from the curated menu below, then write a per-cycle plan
---

# Post-v0.38.0 next-cycle entry handoff

v0.38.0 closed cleanly. The deploy-confidence substrate is in place — per-blueprint seed contributions, `sauce seed` CLI verb, `vault_kind:"test"` + `--reset` safety gate, bar-ii integration smoke, macOS+Linux CI matrix, `release.yml` preflight gate, `scripts/check-version-sync.js` lockstep enforcement, pilot blueprints proving the α/β shape. Workshop self-install verified clean. CI matrix runs on every PR/push; tag push triggers preflight-gated tap PR.

Next cycle TBD. This handoff is the curated menu — pick one and brainstorm before writing a plan.

## Required pre-cycle reads (~10 minutes)

1. `Docs/plans/2026-05-13-v0.38.0-result.md` — v0.38.0 cycle close writeup with surprises, pacing rhythm, FIX-LATER-NOTEs, next-cycle pointers.
2. `Docs/plans/2026-05-12-deploy-confidence-substrate-design.md` — spec, especially § "How this composes with future cycles" (the menu below maps to that section).
3. `CLAUDE.md` § Status (live) — current workshop_version, mechanisms, blueprints, harness counts.
4. `Docs/cycle-history.md` § v0.38.0 — archived narrative for the cycle just closed.

## Candidate cycles (no priority order — brainstorm to pick)

### A. v0.34.0 — sauce claude cohesion wave 3 (still pending from v0.33.0 close)

The original wave-3 cycle slot, reserved since v0.32.0. Scope:
- 6 remaining blueprints adopt `claude_surface[]`: boards, people, to-do, finance, journal, trips.
- `Docs/Meta/<X>-System.md` retirement (these were the pre-v0.32.0 doc-as-code source; superseded by claude_surface).
- `cowork@0.x.x` backwards-compat `manifest.skills[]` shim removal (the shim was a v0.32.0 stop-gap; safe to remove now that all blueprints emit claude_surface[]).

Effort: 6 blueprint manifest edits + 6 new SKILL.md files + 6 new slash commands + retirement sweep + cowork shim removal. ~80-100 lines per blueprint × 6 + cleanup. Similar pacing to v0.33.0 wave-2 (3 blueprints).

### B. Remaining blueprint seed coverage (8 small cycles)

Each blueprint that didn't get a seed in v0.38.0 needs its own ~50-line cycle. v0.38.0 shipped pilots for `project` (α), `daily` (β), `meetings` (α). Remaining: `boards`, `cowork`, `journal`, `people`, `to-do`, `trips`, `finance`, `scratch`. Each gets:
- `platform/blueprints/<bp>/seed/seed.json` (α) or `seed.js` (β) per the substrate.
- A few representative entries; if cross-blueprint references exist, accept dangling-link warnings.
- Smoke verification against a tmp vault.

These can be batched (one cycle per blueprint, or all 8 in one bundled cycle). The `people` seed unblocks the meetings α-seed's `[[Person]]` wikilinks. Effort per blueprint: 30-60 minutes.

### C. v0.39.x — bar iii (renderer-stub upgrade)

Extends `run-renderer.js` to cover Dataview view compilation + Templater template rendering against seeded notes. Today's `run-renderer.js` covers nav-button rendering only (36 cases). Bar iii would:
- Stand up a Node-side Obsidian/Templater stub.
- Render each seeded blueprint's templates against the seeded vault.
- Catch breakage in templates, custom JS classes, Dataview view code.

Reuses the v0.38.0 seed substrate (seeded vault → renderer stub input). Significant work — own design cycle first.

### D. v0.40.x — bar iv (upgrade-survival)

Smoke runs twice: at `git checkout v0.38.0` pantry, then at HEAD. Asserts state survives the version delta. Adds checkout/install overhead — significant CI runtime cost. Catches drift across versions. Reuses everything below it.

### E. Small housekeeping cycle — v0.38.0 FIX-LATERs

Bundle the v0.38.0 FIX-LATER-NOTEs into a single small cycle:
- (a) `sauce migrate-layout` pass against `/Users/willfellhoelter/notes/sauce/test-vault` so it becomes the proper dogfood target.
- (b) Replace `.gitignore` `/Scripts/` rule with explicit named-file rules to avoid the APFS lowercase collision with `scripts/`.
- (c) Add transactional pre-check to `cmd-seed.js --reset` path so a mid-loop throw can't partially-delete.
- (d) Document `--mechanisms=all` requirement for smoke harnesses in plan-template guidance.
- (e) Consider per-blueprint `seed-template.md` (separate from user-facing `templates/*.md`) so seeded notes don't contain Templater `<% %>` literals.

Effort: ~half-day. Bundled commit; tag v0.38.1 PATCH.

## Brainstorm prompts (use the `superpowers:brainstorming` skill)

Pick one of the candidate cycles above and brainstorm:
- What is the smallest scope that ships value?
- What are the gotchas / landmines this cycle will hit?
- What's the test strategy?
- Who is the consumer of this cycle's output?
- What's the rollback / recovery story?
- Is there design work to lock down before plan-writing?

Then either:
- (a) Write a design doc at `Docs/plans/YYYY-MM-DD-<cycle-name>-design.md` (lock scope before plan).
- (b) Go directly to plan-writing if scope is small/obvious. Use `superpowers:writing-plans`.

## Project state at handoff

- **Workshop version:** `0.38.0`
- **Tag:** `v0.38.0` annotated, pushed to `origin/main`
- **Whole-suite preflight:** GREEN (1216 sub-asserts + 36 renderer cases across 14 harnesses)
- **Mechanisms (10):** customjs-guard, validator, audit, nav-buttons, cards, accent-button, people-rendering, styling, convenience, platform-claude
- **Blueprints (11):** boards, cowork, daily, journal, meetings, people, project, scratch, to-do, trips, finance
- **CI matrix:** macOS + Linux on every PR/push
- **Release gate:** `release.yml` `preflight` job blocks `bump-tap` on red
- **Test-vault status:** on pre-v0.36.0 layout (FIX-LATER for cycle E)
- **`~/.sauce/vaults.json`:** untouched by tests (invariant enforced)

## Cycle-close protocol pointer

When this next cycle closes, follow `Docs/prompts/_handoff-protocol.md` § "End-of-session checklist (DEFINITIVE)":
1. Result writeup at `Docs/plans/YYYY-MM-DD-<cycle>-result.md`.
2. CLAUDE.md status snapshot updated (cycle order, workshop version, mechanisms/blueprints if changed, harness count if changed, new summary block).
3. Annotated tag pushed.
4. `Docs/cycle-history.md` entry appended.
5. Next-cycle handoff written.

## Slash command for next session

```
[brainstorming or planning slash command of choice, e.g.]
/superpowers:brainstorming

Brainstorm the next cycle from the menu in Docs/prompts/2026-05-13-post-v0.38.0-next-cycle-handoff.md. Read the handoff doc first; then pick one (or propose a different candidate).
```
