---
for_session_after: v0.115.0
workshop_version: "0.115.0"
finance_version: "0.9.0"
status: CODE COMPLETE — push/tag/brew/dev-sync pending
---

# Next-session handoff — post v0.115.0

**Paste this into the next session as the opening prompt.**

You're picking up after **v0.115.0** closed (monthly cohesion — Months sub-area + FinanceMath foundation + FinanceHubSummary landing + Paycheck Template lockstep fix). Workshop at **0.115.0**, finance at **0.9.0**. HEAD `b9bd56a` (Stage D — dogfood + behavioral harness wired into release:preflight).

## Live state

| Component | Version |
|---|---|
| Workshop | **0.115.0** |
| Finance blueprint | **0.9.0** |
| entity-create mechanism | 0.7.2 |
| cowork blueprint | 0.40.2 |
| Mechanism count | 17 |

- Design: `Docs/plans/2026-06-15-v0.112.0-monthly-cohesion-design.md`
- Plan: `Docs/plans/2026-06-15-v0.112.0-monthly-cohesion-plan.md`
- Result: `Docs/plans/2026-06-15-v0.115.0-monthly-cohesion-result.md`
- Cycle-status (live): `Docs/agent-guides/cycle-status.md`

## What just shipped (v0.115.0)

**Nine deliverables in one MINOR:**

1. NEW `spice/finance/months/` sub-area — `Month-<YYYY-MM>.md` entities + `Months.md` hub via `MonthsCards`.
2. NEW `FinanceMath` static-only helper class — canonical aggregation math; methods: `readDebts`, `readPaychecksForMonth`, `readBudgetForMonth`, `monthBounds`, `debtTotals`, `monthIncome`, `monthSpending`, `monthExpensesTotal`, `monthDebtPaid`, `debtPaidByDebt`, `measuredMovement`, `reconcile`, `fmtMoney`.
3. NEW `MonthDashboard` widget — three-section dashboard on every `Month-<YYYY-MM>.md`; Debt Changes uses reconciliation model (paydown applied + measured drop + derived interest/charges).
4. NEW `MonthsCards` widget — hub grid DESC by month, 24-card window, +N overflow.
5. NEW `FinanceHubSummary` widget — debt-forward landing on `Finance.md`; hero = TOTAL DEBT + ZERO-DEBT DATE; three secondary tiles (budget progress / latest paycheck / open invoices).
6. FinanceNav extension — `hub-months` + `entity-month` modes; Months cross-hub entry.
7. 2 new installer migrations: `applyFinanceMonthsScaffolding` + `applyFinancePaycheckDebtBandInjection` (heals pre-v0.114.0 paychecks missing the PaycheckDebtBand block); `applyFinanceHubsRepair` extended.
8. Manifest additions: 4 new customjs_classes, 5 new files, 1 new new_entity_button (month), 1 new rule_fragment (months).
9. Lockstep fix: `Paycheck Template.md` body now matches manifest inline_body block-for-block (was missing PaycheckDebtBand; was still referencing legacy FinanceNavRow).

Test result: 3538 / 0 helper-cases (+149); behavioral `run-v01120-monthly-cohesion.js` 52 / 0 (new, wired into release:preflight); `run-v01103-monthly-overview` 91 / 0 (unchanged). Preflight exit 0.

## Outstanding actions (user-gated)

1. **Push + tag v0.115.0?** Local main is ahead of origin/main. Tag is user-gated per ask-before-acting.
2. **Brew tap PR** — bump formula 0.114.0 → 0.115.0 → merge.
3. **Per consumer vault** — `scripts/dev-sync.sh` (or `sauce update --bump-pins && sauce install`) → Cmd+R Obsidian → smoke (Finance.md hero, + New Month, pre-v0.114.0 paycheck).

## Carry-forwards

- **MonthlyOverview + DebtsHubSummary → FinanceMath convergence.** Refactor both onto canonical methods (`measuredMovement` replaces MonthlyOverview's intra-month `_mom`; `debtTotals` replaces DebtsHubSummary L35-52). Low risk now that FinanceMath is proven by three widgets. Target: v0.115.1 or v0.116.x.
- **`FinanceHubActions` + `FinanceNavRow` deletion.** Still in `customjs_classes[]`/`files[]` for backcompat. After soak, delete in v0.116.0+.
- **`gather-cc-debt-snapshot` cowork skill** — already-reserved `balance_history[].source: "skill"` slot. Will densify the snapshot timeline that powers `measuredMovement`. Future cowork-side cycle.
- **`sauce update --bump-pins` subprocess race** — cross-machine deploy hazard. If new migrations don't fire via brew CLI after upgrade, heal with `node platform/install.js --vault <path> --auto-approve` one-liner.
- **Stale `cycle-status.md` Current pointer** — was at 0.108.0 for several cycles; v0.115.0 E cycle-close restores it to 0.115.0.

## Next-cycle candidates (priority order)

### 1. v0.115.1 PATCH: MonthlyOverview + DebtsHubSummary FinanceMath convergence

Low-risk refactor. Replace MonthlyOverview's intra-month `_mom` with `FinanceMath.measuredMovement`. Replace DebtsHubSummary L35-52 duplicate math with `FinanceMath.debtTotals`. Net deletion ~50 lines of duplicate arithmetic. FinanceMath is proven by three production widgets in v0.115.0 — the convergence is a mechanical substitution, not a redesign. Closes the "5 copies of the same math" risk that the brainstorm flagged as the primary motivation for the foundation-first approach.

### 2. v0.116.0 MINOR: `FinanceHubActions` + `FinanceNavRow` deletion

After soak period on headspace + accuris confirms no regressions. Pure cleanup: remove both classes from `customjs_classes[]` and `files[]` in the finance manifest; delete the two helper `.js` files. Reduces the finance blueprint's shipped surface. No consumer migration required if the installer already migrated all invocations to `FinanceNav` during v0.108.0's `applyFinanceNavRowMigration`.

### 3. v0.115.x: subprocess-race investigation

`sauce update --bump-pins` inconsistency on cross-machine deploy — new migrations may not fire if the brew-upgraded tarball hasn't propagated before the subprocess resolves the workshop path. Investigate whether adding a `require.cache` invalidation step or a post-upgrade re-resolve in the CLI dispatcher eliminates the race. See `platform/install.js` workshop-path resolution + v0.75.1 `_resolveWorkshopPath` bug note in MEMORY.md.

### 4. cowork-side: `gather-cc-debt-snapshot` skill MVP

Copilot Money MCP (`mcp__copilot-money__get_accounts`) → read current balances → append `balance_history[]` rows with `source: "skill"` to each matching `Debt-<Name>.md` entity. Primes `FinanceMath.measuredMovement` on headspace so the MonthDashboard Debt Changes section sharpens from "snapshot pending" to actual per-debt measured drops. The `source` enum slot was reserved in v0.108.0; this is the first consumer.

## Open landmines + cross-cycle hygiene notes

- **Landmine #16 (version-pin sweep)** — every cycle close requires sweeping hardcoded version strings in `run-helper-cases.js`. v0.115.0 C swept 13 occurrences. The behavioral harness `run-v01120-monthly-cohesion.js` will also need its own sweep when the cycle reaches v0.116.0+.
- **`Paycheck Template.md` lockstep discipline** — v0.115.0 B fixed a gap introduced by v0.114.0 (manifest inline_body updated; template not). Each future cycle that modifies a manifest `inline_body` must update the corresponding template in the same commit.
- **`cycle-status.md` § Current pointer freshness** — was stale at 0.108.0 for several cycles; now at 0.115.0. Update this pointer at EVERY cycle close (build-test-verify.md requirement).
- **ERO consumer is brew-only** — never flip `ero-sauce`'s `workshop_relative_path` to `../sauce-workshop`. Repair on sight.
- **Verify helpers before design asserts them** — per MEMORY.md and `Docs/agent-guides/asking-before-acting.md`. Always grep + read a helper's NAMED behavior and LOADING context before committing a design that asserts them.

## Reference files

| Guide | Reach for it when |
|---|---|
| `Docs/agent-guides/architecture.md` | Understanding mechanisms vs blueprints, installer, distribution. |
| `Docs/agent-guides/code-conventions.md` | Five non-negotiables, customjs gotchas, marker regions. |
| `Docs/agent-guides/build-test-verify.md` | Cycle close protocol; preflight; release workflow. |
| `Docs/agent-guides/cycle-status.md` | Live state pointer (updated this cycle). |
| `Docs/agent-guides/schemas.md` | Before designing a feature that touches frontmatter or sidecars. |
| `Docs/plans/2026-06-15-v0.115.0-monthly-cohesion-result.md` | Full v0.115.0 narrative — what shipped, test deltas, dogfood, lessons. |

## Quick state-check commands

```bash
# Workshop survey
npm run status

# Full preflight (includes run-v01120-monthly-cohesion.js now)
npm run release:preflight 2>&1 | grep -E "^Result:|FAIL "

# Behavioral harnesses individually
node platform/test/run-v01120-monthly-cohesion.js 2>&1 | tail -3
node platform/test/run-v01103-monthly-overview.js 2>&1 | tail -3
```

Expected: workshop 0.115.0 / finance 0.9.0; HEAD `b9bd56a`; preflight exit 0; helper-cases 3538 / 0; monthly-cohesion behavioral 52 / 0; monthly-overview behavioral 91 / 0.

## What you should NOT do unprompted

- Do not push to origin/main or tag without user confirmation.
- Do not start the FinanceMath convergence refactor (v0.115.1) until the user has confirmed v0.115.0 is deploy-verified on at least headspace.
- Do not delete `FinanceHubActions` or `FinanceNavRow` before the v0.116.0 soak decision.
- Do not auto-append `balance_history[]` entries to Debt entities without confirming with the user (destructive write to consumer vault content).

End of handoff prompt.
