---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.19.0 cycle. Triggered by v0.19.0 styling mechanism close + tag v0.19.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.19.0-styling-mechanism-result.md (v0.19.0 close — read first)
  - Docs/plans/2026-05-05-v0.19.0-styling-mechanism-plan.md
  - Docs/plans/2026-05-05-styling-mechanism-design.md
  - Docs/plans/2026-05-05-v0.18.2-project-map-subtasks-fix-result.md
  - Docs/prompts/2026-05-05-post-v0.18.2-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; styling line 11-67 NOW CLOSED)
gates:
  - v0.19.0 closed; tag v0.19.0 pushed; workshop manifest at 0.19.0
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - styling@0.1.1 NEW mechanism (after CF-1 prereq gate); mechanism count 5 → 6
  - Landmine #12 path-count 3 → 6; helper count 4 → 7
  - NEW landmine #15 — vendored theme is mechanism-owned
  - Helper-cases harness 255 → 312 sub-asserts (+57 from VT/AP/SS)
  - Renderer harness UNCHANGED at 23 cases
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.19.x or v0.20.0 (cycle-dependent)
---

# Onboarding — post-v0.19.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults (sources for pattern ports). NOTE: from beacon's pwd these are `../../{name}/`, NOT `../{name}/`.
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.19.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, **`styling@0.1.1`** (NEW)
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9` (all UNCHANGED in v0.19.0)
- Tags on origin: ... `v0.18.0` `v0.18.1` `v0.18.2` **`v0.19.0`** (NEW)
- Harness: `run-helper-cases.js` 312 sub-asserts (+57 from v0.19.0); `run-renderer.js` 23 cases (UNCHANGED); pre-existing T4.0 fatal still carried (use selectors)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 6 paths; helper count: 7
- New landmine #15 codified

**v0.19.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.19.0): post-v0.19.0 next-cycle handoff (THIS commit upcoming)
- `<RESULT>` docs(v0.19.0): cycle close — result writeup (THIS commit upcoming)
- `2f2c66c` docs(v0.19.0): cycle close — styling mechanism + landmine #12 expand 3 -> 6 paths + new landmine #15 + workshop_version 0.18.2 -> 0.19.0 MINOR
- `901bf4e` fix(styling): v0.19.0 S2 CF-1 — gate styling helpers on external_plugins prereq; styling 0.1.0 -> 0.1.1 PATCH
- `e8b74e7` feat(styling): v0.19.0 S2 — three installer helpers (applyVendoredThemes / applyAppearance / applyStyleSettings) + installItem wiring
- `8f393aa` test(styling): v0.19.0 S2 TDD-first — 15 failing harness cases for applyVendoredThemes / applyAppearance / applyStyleSettings
- `3822c06` feat(styling): v0.19.0 S1 — vendor Baseline theme + canonical Style Settings JSON + mechanism scaffold
- `49a13d4` docs(v0.19.0): styling mechanism implementation plan + frozen design doc

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.19.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -8` — expect 7 v0.19.0 cycle commits (handoff + result + close + CF-1 + S2 + S2-TDD + S1 + plan/design).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `blueprints i want.md` + `.obsidian/community-plugins.json` + `.obsidian/themes/Baseline/` + `.obsidian/appearance.json` + `.obsidian/plugins/obsidian-style-settings/data.json` + `Docs/Meta/Scripts/cards/beacon-cards.js` install-drift M).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.19` → `v0.19.0` present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.19.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.19.0"`. Confirm styling at `"0.1.1"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 312 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`, `beacon-button`) for regression sweeps. **T4.0 carried-forward 12 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors.
>    - All 7 BC + 2 DA + 8 FF + 6 BB cases green; 23 cases total.
> 9. **Barebones state:** subscription pinned to `workshop_version: 0.19.0` + `styling@0.1.1` + project `1.3.5`. Install green + idempotent (note: barebones may need Style Settings plugin installed via Obsidian UI for the styling helpers to write — if absent, expect 3 `skipped_missing_prereq` events and zero theme/appearance/data.json writes).
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.19.0-styling-mechanism-result.md`** — most recent cycle close. Captures: 1 in-cycle CF (CF-1 prereq gate), 8 NEW v0.19.0 lessons (helpers-that-materialize-new-state need stronger prereq contracts; shared `_externalPluginsSatisfied` gate; first-of-its-kind UI mechanism cycle pulled exactly 1 CF; per-helper local require("fs"); two backup-suffix conventions; parallel subagent dispatch for independent helpers; in-cycle re-process bump rule at scale extended to 6 data points; reserved-headroom-UNUSED precedent broken at v0.19.0).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.19.0 close.
> 4. **`blueprints i want.md`** — user's wishlist (post v0.19.0 close):
>    - **REMOVE:** styling lines 11-67 — DONE in v0.19.0.
>    - KEEP: meeting cards on daily (verify whether v0.12.0 already satisfied).
>    - KEEP: any newly-surfaced asks during this session.
> 5. **`Docs/landmines.md`** — **15 entries** (v0.19.0 added #15 + expanded #12 allowlist 3 → 6 paths + helper count 4 → 7). **v0.19.0 / v0.20.0 landmine candidate (carried)**: in-cycle re-process bump rule (gotcha 9) is now load-bearing across v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1 + v0.19.0 — 5 data points; strong enough to graduate to numbered landmine **#16**.
> 6. **`Docs/how.md`** — ELEVEN cycles of lessons NOT YET codified (v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.17.0 / v0.18.0 / v0.18.1 / v0.18.2 / v0.19.0); strong candidate for a docs polish cycle.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.19.x or v0.20.0 docs polish + T4.0 cleanup + landmine #16 promotion + trailing-whitespace lint **(PRIMARY recommendation — backlog has compounded further)**
> **What:** Fix the 12-cycles-old `run-renderer.js` no-selector fatal (hardcoded `kanban-board.md` path deleted in v0.2.0) + lift v0.11.0–v0.19.0 lessons (especially in-cycle re-process bump rule + new v0.19.0 lessons a-h) into `Docs/how.md` + `Docs/landmines.md` (promote in-cycle re-process bump rule to landmine **#16**) + add a trailing-whitespace harness lint for blueprint template bodies (carry from v0.18.1 lesson 2).
>
> **Why:** Restores `run-renderer.js` to clean exit; codifies 11 cycles of institutional memory; the in-cycle re-process bump rule is load-bearing now (5-data-point precedent: v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1 + v0.19.0); the trailing-whitespace lint extends helper-cases harness with a small additive case — bundle-friendly per v0.18.1 lesson 4.
>
> **Estimated scope:** 1-2 stages. Workshop bump 0.19.0 → 0.19.1 PATCH (or 0.20.0 MINOR if including new landmine + harness behavior change). Helper-cases harness +N where N = blueprint count (lint pass per template).
>
> **Open questions for brainstorming:** Exact T4.0 hardcoded path (grep `kanban-board.md` in `platform/test/run-renderer.js`); is the trailing-whitespace lint per-template or per-file regex? Should landmine #16 quote install.js:111 short-circuit literally? Workshop bump as PATCH or MINOR?

> [!example]- v0.19.x or v0.20.0 styling polish carries (potential CFs from real-world use)
> **What:** Now that styling has shipped, real-world use over a day/week of working in styled vaults may surface visual cohesion CFs (palette feels off in some surface, contrast issues with embedded note backgrounds, etc.). Bundle-able as a multi-CF styling@0.1.x patch sweep.
>
> **Why:** First-of-its-kind UI cycles historically pull 2-6 CFs; v0.19.0 only pulled 1 (the integration contract). Visual feedback usually surfaces at smoke-time but ALSO over real use. Carry until user reports drift.

> [!example]- v0.20.0 styling presets (multi-style support)
> **What:** Schema upgrade — `style_settings_defaults_src` becomes `style_presets[]` keyed by name; consumer subscription declares `style_preset: "rose-pine"` (default) or `style_preset: "catppuccin"` etc. Workshop ships 2-3 presets (rose-pine + catppuccin + nord candidates).
>
> **Why:** Carry from v0.19.0 design Section 4 out-of-scope. Premature unless user wants a second style soon.

> [!example]- v0.20.0 FrontmatterMutator mechanism (alternative pattern extraction)
> **What:** Promote `FinanceFrontmatter.update(file, mutator)` to a shared `frontmatter-mutator` mechanism. Generic API. Mirror v0.18.0's BeaconButton-promotion playbook + v0.19.0's parallel-subagent-helper precedent.
>
> **Why:** Mirrors BeaconButton thinking. Still premature — finance is the ONLY current consumer. Wait for second blueprint to surface the need.

> [!example]- v0.19.x finance polish carries
> **What:** (a) Migration helper for pre-v0.17.0 finance entities (one-shot Templater command to inject editor blocks); (b) Budget categories aggregate row (Total Planned / Total Actual at bottom of editor); (c) PaycheckExpensesEditor "All Paid" toggle.
>
> **Why:** Carry from v0.17.0/v0.18.0/v0.18.1/v0.18.2 handoffs. Each is small. Bundle-friendly with #1 IF the smoke matrix doesn't grow significantly.

> [!example]- v0.20.0 project: surface a "Sub-tasks" panel on a top-level task hub note
> **What:** Now that sub-tasks are correctly NOT on the Project Map (post-v0.18.2), surface them on the task page itself. New CustomJS class (or extend existing project helper) — query `<Task>/board/` for cards, render as a list/grid on the task hub note.
>
> **Why:** Closes the loop. Sub-tasks belong to the task; the task hub note is where they should surface. Adjacent to v0.18.2 closure.
>
> **Estimated scope:** 1-2 stages. Workshop bump 0.19.0 → 0.20.0 MINOR (new helper class) OR 0.19.x PATCH (extend existing).

---

## Recommendation

> [!success] **v0.19.x or v0.20.0 docs polish + T4.0 + landmine #16 + trailing-whitespace lint** — PRIMARY (backlog has compounded; bundle-friendly)
>
> **Why:** 11 cycles of uncodified lessons + 12-cycle-old harness fatal + 5-data-point-strong landmine candidate + small additive harness extension all share a single PATCH/MINOR window with low smoke risk. Per v0.18.1 lesson 4, combined cycles are bundle-friendly when the smoke matrix doesn't grow significantly. Pure docs + harness work; minimal manual smoke surface.
>
> **Skip for now:** styling presets (premature without second style); FrontmatterMutator (still premature); finance polish (smaller carry; bundle later); project Sub-tasks panel (adjacent but new helper class — better as a focused MINOR cycle); styling polish carries (wait until user reports real-world drift).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper authoring tasks; master-driven on manifest mechanics + version bumps + harness extensions + landmine doc edits.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or mechanism API, QUOTE the literal call shape in the design doc.
> - **Apply v0.19.0 lessons proactively** — (a) helpers that materialize NEW state need explicit prereq gates; (b) shared `_externalPluginsSatisfied` gate is reusable; (c) first-of-its-kind UI mechanism cycles can land near-clean when design quotes posture + harness covers spec literally + plan flags potential CF surfaces upfront; (d) per-helper local `require("fs")`; (e) two backup-suffix conventions (`.bak` for overwrite, `.beacon-backup` for additive); (f) parallel subagents for independent helpers; (g) reserved-headroom-UNUSED precedent broken at v0.19.0 (NEW mechanism cycles still pull at least 1 CF — usually integration contract).
> - **TDD discipline on harness extensions** — VT/AP/SS (v0.19.0) + BB1-BB6 (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **First-of-its-kind UI cycle pulls 1-6 CFs** (7-data-point precedent now: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 / v0.17.0 / v0.19.0).
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2)** — broke at v0.19.0; precedent NOW reads: pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix cycles → reserved-headroom UNUSED reliable; new mechanism / new UI surface / first-of-its-kind → expect at least 1 CF (integration contract is the most common CF surface).
> - **Decision rule:** new mechanism / new blueprint with substantial CustomJS surface → expect CFs; pure-mechanism patch OR near-clone-of-existing OR pure adoption sweep OR bug fix scoped to existing helper → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9, v0.20.0 landmine **#16** candidate) — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1. The installer's `subscribed.version === installed.version` short-circuit (install.js:111) skips re-process when version unchanged even if content changed. **Five-data-point precedent now**: v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1 + v0.19.0.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **dv-shim composition discipline (v0.17.0 lesson)** — when composing a hub renderer that hands a sub-container to another customJS class, the shim MUST proxy every dv method that sub-class might call (`pages` + `current` + `el` minimum).
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs.
> - **BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers** (v0.11.0 CF-1 lesson). BeaconButton callers are inside CustomJS classes, not dataviewjs blocks — no wrapper needed.
> - **Per-blueprint version bumps require LOCKSTEP edits** to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11). For mechanism bumps, lockstep extends to 4 files: workshop manifest + mechanism manifest + workshop subscription + barebones subscription (v0.19.0 CF-1 reinforcement).
> - **BeaconCards = hubs only / bullet lists for tasks** (v0.12.0 bounded-cohesion thesis).
> - **Outline-accent action buttons go through `customJS.BeaconButton.render(...)`** (v0.18.0 lesson) — no more inline cssText + hover handlers for new buttons. Tone palette slot reserved for future additive tones; only "accent" valid at v0.19.0.
> - **InvoiceControls Save UX pattern (v0.18.0 CF-1)** — when widget Save can be the cross-file truth source, also recompute derived fields (e.g., amount = rate × hours) on save AND extend `disabled` state to consider derived-field drift.
> - **Trailing-whitespace template defect class (v0.18.1 CF-1)** — grep blueprint template bodies for trailing whitespace before merge (`grep -n " $" templates/*.md`); v0.19.x or v0.20.0 candidate to extend helper-cases harness for this lint.
> - **Belt-and-suspenders predicate stacking (v0.18.2 lesson)** — for filters with multiple independent signals, prefer 2 cheap predicates over 1 cleaner one when surfaces are independent.
> - **NEW Helpers-that-materialize-new-state need explicit prereq gates (v0.19.0 CF-1)** — `applyExternalPlugins` only emits warnings; helpers that mkdir + fresh-write must short-circuit at the top via `_externalPluginsSatisfied(tp, manifest)` (or equivalent prereq check). Pattern reusable for any future helper whose missing-prereq state isn't recoverable post-hoc.
> - **NEW Two backup-suffix conventions (v0.19.0 lesson e)** — `.bak` for file-content overwrite (vendored themes; v0.2.0 boards Option B precedent), `.beacon-backup` for plugin-data additive merge. Mixing them is confusing — codified in landmine #12 mechanic #2.
> - **NEW Vendored theme is mechanism-owned (landmine #15)** — never hand-edit `.obsidian/themes/<Name>/`. Customizations route through Style Settings JSON OR `.obsidian/snippets/`.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at **6 paths** (#12 v0.19.0 expansion) — adding a 7th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 7th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first.
> 3. Read v0.19.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (docs polish + T4.0 + landmine #16 + trailing-whitespace lint as primary).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step IF CustomJS classes change).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.19.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.19.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with primary recommendation (docs polish + T4.0 cleanup + landmine #16 promotion + trailing-whitespace harness lint), constraints carried forward, and v0.19.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Note T4.0 carried 12 cycles; use selectors (empty, malformed, unknown-action, beacon-cards, date-aware, finance, beacon-button) for renderer harness sweeps. Renderer harness has 23 cases. Helper-cases harness has 312 sub-asserts (v0.19.0 added 57 via VT/AP/SS).
3. Read v0.19.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with primary recommendation. v0.19.x or v0.20.0 docs polish + T4.0 + landmine #16 + trailing-whitespace lint = primary; project Sub-tasks panel adjacent to v0.18.2 = secondary; styling presets multi-style = tertiary (premature); FrontmatterMutator promotion = quaternary (still premature); finance polish carries / styling polish from real-world use = quinary.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.19.0 lessons proactively: (a) helpers that materialize NEW state need explicit prereq gates; (b) shared _externalPluginsSatisfied gate posture; (c) reserved-headroom-UNUSED precedent broken at v0.19.0 — new mechanism cycles still pull at least 1 CF, usually integration contract; (d) parallel subagents for independent helpers when posture is shared; (e) per-helper local require("fs"); (f) two backup-suffix conventions (.bak overwrite, .beacon-backup additive); (g) in-cycle re-process bump rule at scale (5-data-point precedent — landmine #16 candidate).
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget classes; master-driven on manifest + version-bump mechanics + docs edits + harness extensions; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (7-data-point UI precedent for 1-6 inline CFs); 5-data-point reserved-headroom-UNUSED precedent (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2) for non-UI dedupe / pure adoption / pure label / pure docs / scoped-bug-fix cycles still holds.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 7th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11; for mechanisms extend to 4 files per v0.19.0 CF-1); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 1-6 inline CFs + template-changing migration friction + per-entity sub-folder pattern + Templater auto-promote reusable pattern); v0.16.0 lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry); v0.17.0 lessons (dv-shim composition discipline; badges[].style outline; outline-accent action button pattern as BeaconButton candidate — DONE in v0.18.0); v0.18.0 lessons (in-cycle re-process bump rule at scale; mechanism-promotion-as-clean-cycle-shape; harness stub maintenance; reserved-headroom-UNUSED 3-data-point precedent; tone palette slot reservation; cross-file UX coupling discovered late); v0.18.1 lessons (pure adoption clean first-pass via API-contract quoting; trailing-whitespace template defect class; reserved-headroom-UNUSED 4-data-point precedent; combined cycles are bundle-friendly); v0.18.2 lessons (belt-and-suspenders predicate stacking; reserved-headroom-UNUSED 5-data-point precedent — NOTE BROKEN at v0.19.0; master-driven cycles can be very fast; carry-forward bugs queue with line numbers); v0.19.0 NEW lessons (helpers that materialize new state need stronger prereq contracts; shared _externalPluginsSatisfied gate; first-of-its-kind UI mechanism cycle pulled exactly 1 CF; per-helper local require("fs"); two backup-suffix conventions; parallel subagents for independent helpers when posture is shared; in-cycle re-process bump rule at scale extended to 6 data points — landmine #16 candidate; vendored theme is mechanism-owned — landmine #15 codified).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
