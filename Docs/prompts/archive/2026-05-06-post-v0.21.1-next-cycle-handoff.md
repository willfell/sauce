---
date: 2026-05-06
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.21.1 cycle. Triggered by v0.21.1 consumer-convenience close + tag v0.21.1 on origin/main.
predecessors:
  - Docs/plans/2026-05-06-v0.21.1-consumer-convenience-result.md (v0.21.1 close — read first)
  - Docs/plans/2026-05-06-v0.21.1-consumer-convenience-plan.md
  - Docs/plans/2026-05-06-v0.21.1-consumer-convenience-design.md
  - Docs/plans/2026-05-06-v0.21.0-consumer-bootstrap-result.md
  - Docs/prompts/2026-05-06-post-v0.21.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; multi-theme entry from 2026-05-06)
gates:
  - v0.21.1 closed; tag v0.21.1 pushed; workshop manifest at 0.21.1
  - convenience@0.1.0 mechanism + 2 new helpers + daily 0.2.2 enrichment shipped
  - hotkeys[] + community_plugin_settings[] schema fields added (mechanism + blueprint compatible)
  - Allowlist (landmine #12) expanded 9 → 11
  - Helper-cases harness 387 sub-asserts (was 346)
  - Renderer harness 23 cases UNCHANGED (whole-suite green preserved)
  - Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`
  - All 4 manual smokes A-D PASS first-try; zero in-cycle CFs
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.21.x or v0.22.0 (cycle-dependent)
---

# Onboarding — post-v0.21.1 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `../scratch/1/` — fresh-vault Phase C smoke target (created 2026-05-06; not git-tracked)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults (sources for pattern ports). NOTE: from beacon's pwd these are `../../{name}/`, NOT `../{name}/`.
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.21.1`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, `styling@0.1.2`, **`convenience@0.1.0`** NEW
- Blueprints: `boards@0.1.0`, `daily@0.2.2` (PATCH in v0.21.1), `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.18.0` `v0.18.1` `v0.18.2` `v0.19.0` `v0.20.0` `v0.21.0` **`v0.21.1`** (NEW)
- Harness: `run-bootstrap.js` 36 sub-asserts (9 cases); `run-helper-cases.js` 387 sub-asserts (was 346); `run-renderer.js` 23 cases (whole-suite green).
- Allowlist (landmine #12): **11 paths** (was 9; v0.21.1 added 2).
- Landmines list: 17 entries (UNCHANGED in v0.21.1).
- Helper count: 9 allowlist-touching (was 7).

**v0.21.1 commits in order (top-down):**
- `<HANDOFF>` docs(v0.21.1): post-v0.21.1 next-cycle handoff (THIS commit upcoming)
- `7c5546a` docs(v0.21.1): cycle close — workshop_version 0.21.0 → 0.21.1 PATCH
- `f197a6a` feat(convenience): v0.21.1 S2 — applyHotkeys + applyCommunityPluginData + convenience@0.1.0 + daily 0.2.2 enrichment
- `1820044` test(convenience): v0.21.1 S1 TDD-first — 10 failing harness cases
- `b33698c` docs(v0.21.1): consumer convenience config implementation plan
- `d6f59fa` docs(v0.21.1): consumer convenience config design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.21.1 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -10` — expect 6 v0.21.1 cycle commits.
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `.obsidian/` install-drift `M` markers).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep -E 'v0\.21'` → `v0.21.0` + `v0.21.1` both present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.21.1:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.21.1"`. Confirm `convenience@0.1.0` in mechanisms[] + `daily@0.2.2` in blueprints[].
> 7. **npm deps installed:** `test -d node_modules/@inquirer/prompts && echo OK` → OK.
> 8. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 9. **Harnesses:**
>    - `node platform/test/run-bootstrap.js` → `Result: 36 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0; T4.0 + 7 selectors all PASS.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-06-v0.21.1-consumer-convenience-result.md`** — most recent cycle close. 4 NEW v0.21.1 lessons + reserved-headroom-UNUSED 7th data point + carry-forwards.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.21.1 close.
> 4. **`blueprints i want.md`** — user's wishlist. Multi-theme entry still open.
> 5. **`Docs/landmines.md`** — 17 entries; #12 expanded 9 → 11 in v0.21.1.
> 6. **`Docs/how.md`** — now includes "Convenience mechanism (v0.21.1+)" subsection.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.22.0 multi-theme presets (PRIMARY recommendation)
> **What:** Schema upgrade from `style_settings_defaults_src` (single string) to `style_presets[]` keyed by name. Each preset entry: `{ name, theme_src, style_settings_src, appearance_overrides }`. Consumer subscription declares `style_preset: "baseline"` (default) or `"headspace"` / `"ero"` / `"<fourth>"`. Workshop ships 4 presets. Wizard adds preset-select prompt during first-run.
>
> **Why:** User-requested 2026-05-06 in `blueprints i want.md`. Foundational; substantial schema upgrade. Primary recommendation since v0.21.1 closed the consumer-convenience surface and the user's wishlist now leads with multi-theme.
>
> **Source vaults to vendor + canonicalize Style Settings JSON from:**
> - Current Baseline (already shipped in v0.19.0)
> - Headspace vault: `/Users/willfell/Documents/obsidian/sync/headspace/CLAUDE.md` — read CLAUDE.md to identify which theme + its Style Settings config
> - ERO vault: `/Users/willfell/Documents/obsidian/sync/ero/Boards` — inspect `.obsidian/themes/` + `.obsidian/plugins/obsidian-style-settings/data.json`
> - One more for variety (TBD — picked from accuris or a fresh community theme)
>
> **Estimated scope:** 3-4 stages. Workshop bump 0.21.1 → 0.22.0 MINOR. New schema field at workshop manifest level; styling helpers extended to dispatch on preset; wizard adds preset prompt. **First-of-its-kind UI surface; expect 2-4 in-cycle CFs** at preset-select integration boundaries.

> [!example]- v0.22.0 architectural fix — install.js doesn't write installed_at when helpers no-op
> **What:** Refactor install.js so helpers can signal NO_OP_DUE_TO_MISSING_PREREQ back to the orchestrator + the orchestrator skips writing `installed_at`. Eliminates the v0.21.0-style trap where a stuck-in-installed-state mechanism needs a manual landmine #16 PATCH bump to recover. Also closes the v0.21.1 carry — `applyCommunityPluginData`'s broadened prereq gate produces no-op installs that nonetheless mark the mechanism as "installed."
>
> **Why:** Surfaced repeatedly (v0.21.0 styling 0.1.1 → 0.1.2 unstick; v0.21.1 broadened prereq gate). Architectural; would simplify recovery for any future helper that gates on a prereq. Substantial install.js change. ~2-3 stages.

> [!example]- v0.22.0 project Sub-tasks panel on task hub
> **What:** Carry from post-v0.18.2. Surface sub-tasks (folder-promoted Task Board Cards under `<task>/board/`) on the task hub note via BeaconCards. New CustomJS class `ProjectSubTasks`. Closes the v0.18.2 loop (Map filter excludes sub-tasks; this gives them their proper home).
>
> **Why:** Still relevant; first-of-its-kind UI surface; expect 1-3 inline CFs.

> [!example]- v0.21.x finance polish carries
> **What:** (a) Migration helper for pre-v0.17.0 finance entities; (b) Budget categories aggregate row; (c) PaycheckExpensesEditor "All Paid" toggle.
>
> **Why:** Carry from v0.17.0/v0.18.0/v0.18.1/v0.18.2 handoffs. Bundle-friendly with another cycle; small carries.

> [!example]- v0.21.x cross-doc line-citation drift lint
> **What:** Lint `Docs/*.md` for `[a-z\.]+\.js:\d+` patterns and assert each line still exists at that line in the cited file. Catches landmine #10 / #16-style drift before it ships.
>
> **Why:** Surfaced as v0.20.0 NEW lesson (c). Small additive harness extension; bundle-friendly with any docs-touching cycle.

---

## Recommendation

> [!success] **v0.22.0 multi-theme presets** — PRIMARY (closes user-requested wishlist; substantial schema work)
>
> **Why:** v0.21.0 + v0.21.1 closed bootstrap + consumer-convenience surfaces. Multi-theme is the next user-prioritized feature. Foundational schema work; first-of-its-kind preset-select integration. Plan reserved Y.Z.x headroom for 2-4 inline CFs (precedent: first-of-its-kind UI = 1-6 CFs; orchestration = 4-6).
>
> **Skip for now:** architectural install.js refactor (real but invasive — bundle alongside another cycle that touches install.js); project Sub-tasks panel (UI cycle; queue after multi-theme); finance polish (smaller carry; bundle later); cross-doc lint (small additive; bundle with any future docs cycle).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper/preset-asset authoring; master-driven on manifest mechanics + version bumps + harness extensions + landmine doc edits + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or schema, QUOTE the literal call/manifest shape in the design doc.
> - **Two-stage subagent review (v0.20.0 lesson d)** — spec compliance first, code quality second.
> - **Apply v0.21.1 lessons proactively:**
>   - (a) parallel-subagent dispatch with literal-contract prompts works cleanly when insertion points are 800+ lines apart
>   - (b) broadened prereq gates are a defensible per-helper choice
>   - (c) first-wins protects consumer trust; never modify a user's bound key
>   - (d) `Mod` modifier is portable; key strings store unmodified physical keys
> - **TDD discipline on harness extensions** — HK1-HK5 + CP1-CP5 (v0.21.1) + BS1-BS9 (v0.21.0) + VT/AP/SS (v0.19.0) + BB1-BB6 (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate (when CustomJS classes change OR plugin-data writes happen).
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9 / landmine #16).
> - **Reserved-headroom precedent now 8 data points: 7 UNUSED + v0.21.0 USED 5 CFs.** Decision rule: pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix / pure-additive helper-pair → UNUSED. New mechanism / new UI surface / first-of-its-kind orchestration → expect 2-6 CFs.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **In-cycle re-process bump rule (LANDMINE #16)** — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1. Six-data-point precedent + reinforced in v0.21.0 styling unstick.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs.
> - **Per-blueprint version bumps require LOCKSTEP edits** (v0.12.0 gotcha 11; for mechanisms 4-file lockstep per v0.19.0 CF-1 + landmine #16).
> - **First-wins protects consumer trust** (v0.21.1 lesson c) — never modify a binding the user has already set.
> - **Helpers-that-materialize-new-state need explicit prereq gates (v0.19.0 CF-1; v0.21.1 broadened gate)** — pick the semantic that fits the helper's failure mode.
> - **Two backup-suffix conventions (v0.19.0 lesson e)** — `.bak` for file-content overwrite (vendored themes), `.beacon-backup` for plugin-data additive merge.
> - **Vendored theme is mechanism-owned (landmine #15)** — never hand-edit `.obsidian/themes/<Name>/`.
> - **Test isolation via inline synthetic bodies (v0.20.0 lesson a)** — when a harness test reads a blueprint asset for fixture content, prefer an inline synthetic body over `fs.readFileSync` of a real path.
> - **Cross-doc line-citation drift is a real maintenance class (v0.20.0 lesson c)** — any cycle that touches a file cited as `<filename>:<lineno>` in any doc should grep all docs for that pattern.
> - **Bootstrap network posture (LANDMINE #17, v0.21.0)** — bootstrap.js is the platform's only network gateway; install.js stays filesystem-only.
> - **Canonical helper posture stack (v0.21.0 lesson d)** — atomic write + backup-on-overwrite + path-traversal validator.
> - **Defer-then-validate-then-load wizard architecture (v0.21.0 lesson e)** — when a config field gates downstream prompts/loads, gate the load behind the field's prompt+validator.
> - **Defensive augment-on-every-run for new schema fields (v0.21.0 lesson f)** — additive-only augment; never overwrite user-supplied values.
> - **Parallel-subagent dispatch (v0.21.1 lesson a)** — works cleanly when insertion points are 800+ lines apart and prompts include literal "don't touch any other line" gating.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at **11 paths** (#12 v0.21.1 expansion) — adding a 12th+ requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8 v0.21.0 amendment: install.js is filesystem-only; bootstrap.js is the network gateway).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 12th+ `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first.
> 3. Read v0.21.1 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~10-15 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (v0.22.0 multi-theme presets as primary).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step IF CustomJS classes change OR plugin-data writes happen).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.21.1 next cycle. Read Docs/prompts/2026-05-06-post-v0.21.1-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with primary recommendation (v0.22.0 multi-theme presets — schema upgrade style_settings_defaults_src single-string -> style_presets[] keyed by name; vendor headspace + ero + 4th theme alongside Baseline; wizard adds preset-select prompt), constraints carried forward, and v0.21.1 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Bootstrap harness has 36 sub-asserts (9 BS cases). Helper-cases harness has 387 sub-asserts (was 346 pre-v0.21.1; +41 from HK1-HK5 + CP1-CP5). Renderer harness has 23 cases UNCHANGED. npm dep installed: @inquirer/prompts.
3. Read v0.21.1 result writeup + CLAUDE.md status snapshot + blueprints i want.md (multi-theme entry still open) + landmines (17 entries; #12 expanded 9->11 in v0.21.1) + how.md (Convenience mechanism subsection NEW v0.21.1).
4. Surface candidates via AskUserQuestion with primary recommendation. v0.22.0 multi-theme = primary; v0.22.0 architectural install.js refactor = secondary; v0.22.0 project Sub-tasks panel = tertiary; finance polish carries / cross-doc lint = quaternary.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.21.1 lessons proactively.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget/preset-asset classes; master-driven on manifest + version-bump mechanics + docs edits + harness extensions + cycle close; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (precedent: UI 1-6 CFs; orchestration 4-6 CFs); 7-data-point reserved-headroom-UNUSED precedent (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2 + v0.20.0 + v0.21.1) for non-UI dedupe / pure adoption / pure label / pure docs / scoped-bug-fix / pure-additive helper-pair cycles still holds.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 12th+ path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits (v0.12.0 gotcha 11; for mechanisms extend to 4 files per v0.19.0 CF-1; codified as landmine #16 in v0.20.0; reinforced in v0.21.0 styling unstick); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw inline CFs + template-changing migration friction + per-entity sub-folder pattern); v0.16.0 lessons (YAML date auto-parsing; frontmatter parser variance; NBSP gotcha; renderBadge pattern; embed dedup); v0.17.0 lessons (dv-shim composition; badges[].style outline; outline-accent button pattern); v0.18.0/v0.18.1/v0.18.2 lessons (in-cycle re-process bump rule [LANDMINE #16]; pure adoption clean first-pass via API-contract quoting; trailing-whitespace template defect class [LINT SHIPPED v0.20.0]; belt-and-suspenders predicate stacking); v0.19.0 lessons (helpers materialize new state need stronger prereq contracts; shared _externalPluginsSatisfied gate; per-helper local require; two backup-suffix conventions; parallel subagents; vendored theme mechanism-owned [LANDMINE #15]); v0.20.0 NEW lessons (test isolation via inline synthetic bodies; helper-cases as generic lint host; cross-doc line-citation drift maintenance class; two-stage subagent review yields different signal at low cost; master-driven S3 is the right shape); v0.21.0 NEW lessons (first-of-its-kind orchestration cycles draw 4-6 in-cycle CFs at integration boundaries; process.exit pipe truncation [use process.exitCode]; async spawn over spawnSync; canonical helper posture stack [atomic write + backup-on-overwrite + path-traversal validator]; defer-then-validate-then-load wizard architecture; defensive augment-on-every-run for new schema fields); v0.21.1 NEW lessons (parallel-subagent dispatch with literal-contract prompts works cleanly when insertion points are 800+ lines apart; broadened prereq gate is a defensible per-helper choice; first-wins protects consumer trust; Cmd+= is the literal physical key for "Cmd++" on US keyboards).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
