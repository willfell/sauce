# Next-session handoff — post v0.111.3-CF1

**Paste this whole document into the next session as the opening prompt.**

You're picking up after v0.111.3 + v0.111.3-CF1 closed local. Workshop is at **0.111.3**, finance at **0.7.2**, base commit `da1be69`. Branch `main` is fully in sync with `origin/main` (0 ahead / 0 behind). Self-install dogfood + preflight: exit 0. Helper-cases 3374 / 0; behavioral harness 91 / 0.

## What's deployed

- **Workshop 0.111.3** — sequence of v0.110.3 (MonthlyOverview + 4 widget regression fixes) → v0.110.4 (CustomJS class file parse-error PATCH) → v0.110.5 (trailing-code-after-class PATCH) → v0.111.0 (unified context-aware FinanceNav MINOR — collapsed FinanceHubActions + FinanceNavRow → FinanceNav) → v0.111.1 (repair-template fix) → v0.111.2 (FinanceNav polish + debt entry-create bug fix) → v0.111.3 (dedupe duplicate FinanceNav fences + Defaults file detection).
- **Finance 0.7.2** — debt sub-area (v0.6.0) + MonthlyOverview band (v0.6.3) + FinanceNav unification (v0.7.0+) all SHIPPED. `MonthlyOverview` widget lives at `platform/blueprints/finance/helpers/monthly-overview.js`; migration `applyFinanceBudgetMonthlyBandInjection` in `platform/install.js`.

## Read first, in order

1. `Docs/plans/2026-06-15-v0.110.3-monthly-overview-result.md` — what shipped at v0.110.3 + 4 lessons.
2. `Docs/plans/2026-06-15-v0.111.3-CF1-monthly-overview-coverage-expansion.md` — what just closed (test coverage expansion: Dataview-proxy fidelity + perf ceiling + cross-year boundary).
3. `Docs/plans/2026-06-15-v0.111.x-enhancements-brainstorm.md` — **the 18-candidate brainstorm you'll pick the next lane from.**
4. `Docs/agent-guides/cycle-status.md` § Current — NOTE: this file is stale (still says v0.108.0 as current). Either trust git log + manifest OR fold cycle-status repair into your first lane (see brainstorm #15).
5. `Docs/agent-guides/build-test-verify.md` — preflight + release workflow.
6. `Docs/landmines.md` — non-negotiable traps.

## Outstanding decisions for the user

1. **Pick a lane from the brainstorm.** My recommendation: Bundle 1 (workshop tooling — #13 sauce-status CLI + #15 cycle-status auto-update + #12 scaffold script + #16 dev-workflow doc) ships as v0.112.0 MINOR. Then Bundle 2 (finance cohesion polish — #1 debt-payment exclusion + #3 click-through + #5 FinanceYearReview annual band) ships as v0.112.1 → v0.113.0. Alternative ordering: if MonthlyOverview math has been in the field for a week and double-counting OR missing audit-trail signal has surfaced, flip the order.
2. **Push + tag decision.** Local main matches origin/main as of `da1be69`. No tag was created for v0.111.3 or CF-1. If user wants to tag retroactively for the chain v0.110.3 → v0.111.3, ask before tagging.
3. **Brew tap formula bump.** Stale at v0.84.0 since ~27 cycles ago. Both consumer vaults rely on local-clone via `workshop_relative_path`. Brainstorm #17.
4. **cowork:doctor.** Displaced for the 9th cycle. Brainstorm #18. At some point: ship it OR explicitly retire the carry-forward.

## What you should NOT do unprompted

- **Do not push to `origin/main` or tag without user approval.** Local main = `da1be69` is the current ship point. User decides when to push + tag.
- **Do not start a cycle without the user picking a lane.** The brainstorm is open — picking turns it into a brief + design + plan.
- **Do not modify `Docs/agent-guides/cycle-status.md` § Current pointer in isolation** unless the user picks brainstorm #15 as the lane (auto-update script is the right fix).
- **Do not touch `monthly-overview.js` source.** v0.111.3-CF1 was test-only; the helper is in-field-untouched. Any modification reopens the cycle.

## Quick state check commands

```bash
# Are we in the workshop?
ls /Users/willfellhoelter/projects/repos/sauce | head

# Workshop + blueprint versions
cat platform/manifest.json | python3 -c "import json,sys;m=json.load(sys.stdin);print('workshop:',m['workshop_version']);[print(' ',b['name'],b['version']) for b in m['blueprints'] if b['name']=='finance']"

# Branch status
git status -sb
git log --oneline -5

# Harness gate (one-shot)
node platform/test/run-helper-cases.js 2>&1 | tail -3
node platform/test/run-v01103-monthly-overview.js 2>&1 | tail -3

# Full preflight
npm run release:preflight 2>&1 | grep -E "^Result:|FAIL " | tail -10
```

Expected outputs: workshop 0.111.3 / finance 0.7.2; HEAD `da1be69`; helper-cases 3374 / 0; behavioral 91 / 0; preflight exit 0.

## Open questions to ask the user at session start

1. "Which lane from the brainstorm should we open? Bundle 1 (workshop tooling) or Bundle 2 (finance polish)?"
2. "Has the MonthlyOverview band been in the field for any time? Any signal of double-counting (Budget category $ + paycheck debt $ overlap) OR audit-trail confusion (can't tell which paychecks contributed)?"
3. "Tag v0.110.3 and/or v0.111.3 retroactively, or skip tagging this chain?"
4. "Brew tap bump now, or still defer until tooling sprint?"

## Decision trees if the user is vague

- **"Just continue with whatever you think makes sense."** → Open brainstorm Bundle 1 with `superpowers:brainstorming` to confirm scope, then write brief/design/plan. Bundle 1 has the highest leverage (every future session benefits) and the lowest blast radius (tooling, not consumer-facing surfaces).
- **"Pick whatever's lowest risk."** → Bundle 1 Cycle #16 only (`Docs/agent-guides/dev-workflow.md` — pure docs, ~1h). Ships v0.111.4 PATCH.
- **"Push and tag what we have first."** → Don't touch new work. Just confirm git state, ask which tag(s) to create, then `git push` + `git tag -a` per build-test-verify.md. Then bring brainstorm back.

## Reference summary table

| Surface | File | What lives there |
|---|---|---|
| MonthlyOverview helper | `platform/blueprints/finance/helpers/monthly-overview.js` | The widget — 3 bands of cohesion math. |
| MonthlyOverview migration | `platform/install.js` (search `applyFinanceBudgetMonthlyBandInjection` + `_injectMonthlyBand`) | Body migration with 4-tier anchor priority. |
| MonthlyOverview HC tests | `platform/test/run-helper-cases.js` (search `caseV01103`) | Structural regex assertions. |
| MonthlyOverview behavioral tests | `platform/test/run-v01103-monthly-overview.js` | 91 assertions: math + DOM + anchor tiers + proxy / perf / cross-year. |
| Per-vault Debt Defaults | `spice/finance/Debt Defaults.md` (consumer vault) | Per-vault seed list. |
| Per-debt entities | `spice/finance/debts/Debt-*.md` (consumer vault) | Flat per-debt records with balance_history[]. |
| Workshop self-subscription | `ranch/platform-subscription.json` | Workshop doesn't subscribe to finance — dogfood doesn't materialize monthly-overview.js. |

End of handoff prompt.
