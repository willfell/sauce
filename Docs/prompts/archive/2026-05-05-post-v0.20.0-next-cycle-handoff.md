---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.20.0 cycle. Triggered by v0.20.0 docs polish close + tag v0.20.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.20.0-docs-polish-result.md (v0.20.0 close — read first)
  - Docs/plans/2026-05-05-v0.20.0-docs-polish-plan.md
  - Docs/plans/2026-05-05-v0.20.0-docs-polish-design.md
  - Docs/plans/2026-05-05-v0.19.0-styling-mechanism-result.md
  - Docs/prompts/2026-05-05-post-v0.19.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; cleaned to finance carry only at v0.20.0)
gates:
  - v0.20.0 closed; tag v0.20.0 pushed; workshop manifest at 0.20.0
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - T4.0 fatal eliminated; whole-suite renderer harness exits 0 for first time since v0.2.0
  - Trailing-whitespace lint shipped; 34 templates clean
  - Helper-cases harness 312 → 346 sub-asserts (+34 from TW1)
  - Landmine #16 codified (in-cycle re-process bump rule)
  - Docs/how.md "Gotchas / Lessons learned" section (10 entries)
  - Renderer harness UNCHANGED at 23 cases
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.20.x or v0.21.0 (cycle-dependent)
---

# Onboarding — post-v0.20.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults (sources for pattern ports). NOTE: from beacon's pwd these are `../../{name}/`, NOT `../{name}/`.
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.20.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, `styling@0.1.1` (all UNCHANGED in v0.20.0)
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9` (all UNCHANGED in v0.20.0)
- Tags on origin: ... `v0.18.0` `v0.18.1` `v0.18.2` `v0.19.0` **`v0.20.0`** (NEW)
- Harness: `run-helper-cases.js` 346 sub-asserts (+34 from v0.20.0 TW1 case); `run-renderer.js` 23 cases (UNCHANGED in count, but **whole-suite exits 0 for first time since v0.2.0** — selectors no longer required for clean exit).
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0).
- Landmine #12 allowlist: 6 paths; helper count: 7.
- Landmines list: **16 entries** (v0.20.0 added #16; v0.19.0 added #15).

**v0.20.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.20.0): post-v0.20.0 next-cycle handoff (THIS commit upcoming)
- `9ee0dfc` docs(v0.20.0): cycle close — workshop_version 0.19.0 -> 0.20.0 MINOR
- `2028927` docs(v0.20.0): S2 polish — count fix in how.md gotchas lead + landmine #10 install.js line drift fix
- `662f21c` docs(v0.20.0): S2 — landmine #16 + how.md gotchas section + blueprints i want.md cleanup
- `0053fb8` test(harness): v0.20.0 S1 — fix T4.0 fatal + trailing-whitespace lint for blueprint templates
- `86aa582` docs(v0.20.0): docs polish + T4.0 + landmine #16 + trailing-whitespace lint implementation plan
- `aeba6c0` docs(v0.20.0): docs polish + T4.0 + landmine #16 + trailing-whitespace lint design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.20.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -8` — expect 7 v0.20.0 cycle commits (handoff + close + S2-polish + S2 + S1 + plan + design).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `.obsidian/community-plugins.json` install-drift M + `.obsidian/themes/Baseline/` + `.obsidian/appearance.json` + `.obsidian/plugins/obsidian-style-settings/data.json` + `Docs/Meta/Scripts/cards/beacon-cards.js` install-drift M).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep -E 'v0\.(19|20)'` → `v0.19.0` + `v0.20.0` both present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.20.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.20.0"`. Confirm styling at `"0.1.1"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 346 passed, 0 failed.` (NEW: 312 → 346 in v0.20.0; +34 from TW1).
>    - **Renderer harness whole-suite NOW EXITS 0** (T4.0 fixed in v0.20.0): `node platform/test/run-renderer.js` (no selector) → exit 0; T4.0 + 7 selectors all PASS. Selectors still work individually if needed (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`, `beacon-button`).
>    - All 7 BC + 2 DA + 8 FF + 6 BB cases green; 23 cases total.
> 9. **Barebones state:** subscription pinned to `workshop_version: 0.20.0` (or 0.19.0 — barebones may need a re-pin if it lagged). Install green + idempotent.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.20.0-docs-polish-result.md`** — most recent cycle close. Captures: 0 in-cycle CFs (reserved-headroom-UNUSED 6th data point), 5 NEW v0.20.0 lessons (test isolation via inline synthetic bodies; helper-cases as a generic lint host; cross-doc line-citation drift maintenance class; two-stage subagent review yields different signal at low cost; master-driven S3 is the right shape).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.20.0 close.
> 4. **`blueprints i want.md`** — user's wishlist. Cleaned to **only** the finance personal/business expansion ask at v0.20.0. Open carry retained.
> 5. **`Docs/landmines.md`** — **16 entries** (v0.20.0 added #16 in-cycle re-process bump rule; #10 line-citation drift fixed in S2 polish).
> 6. **`Docs/how.md`** — now includes "Gotchas / Lessons learned (v0.11.0–v0.19.0)" section with 10 load-bearing entries (added in v0.20.0 S2). Section is a snapshot at v0.19.0 close + v0.20.0 codification; future cycles may extend if a substantively new lesson surfaces.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.21.0 project Sub-tasks panel on task hub (PRIMARY recommendation — adjacent to v0.18.2 closure; first-of-its-kind UI surface)
> **What:** Now that sub-tasks are correctly NOT on the Project Map (post-v0.18.2), surface them on the task page itself. New CustomJS class (or extend existing project helper) — query `<projectDir>/tasks/<Task>/board/` for cards, render as a list/grid on the task hub note via BeaconCards.
>
> **Why:** Closes the loop. Sub-tasks belong to the task; the task hub note is where they should surface. Adjacent to v0.18.2 closure (Map filter excludes sub-tasks; this cycle gives them their proper home).
>
> **Estimated scope:** 1-2 stages. Workshop bump 0.20.0 → 0.21.0 MINOR (new helper class). Renderer harness +N cases for the new helper's coverage.
>
> **Open questions for brainstorming:** Where exactly does the panel live (top of task hub note? below the task body?)? BeaconCards layout — `row` or `stacked`? Should "no sub-tasks" state render anything or be silent? Should sub-tasks be sortable / filterable from the panel?

> [!example]- v0.20.x styling polish carries (potential CFs from real-world use)
> **What:** Now that styling has shipped + been used for a day/week, real-world feedback may surface visual cohesion CFs (palette feels off in some surface, contrast issues with embedded note backgrounds, etc.). Bundle-able as a multi-CF styling@0.1.x patch sweep.
>
> **Why:** First-of-its-kind UI cycles historically pull 2-6 CFs; v0.19.0 only pulled 1 (the integration contract). Visual feedback usually surfaces over real use. Carry until user reports drift.

> [!example]- v0.21.0 styling presets (multi-style support)
> **What:** Schema upgrade — `style_settings_defaults_src` becomes `style_presets[]` keyed by name; consumer subscription declares `style_preset: "rose-pine"` (default) or `style_preset: "catppuccin"` etc. Workshop ships 2-3 presets.
>
> **Why:** Carry from v0.19.0 design Section 4 out-of-scope. Premature unless user wants a second style soon.

> [!example]- v0.21.0 FrontmatterMutator mechanism (alternative pattern extraction)
> **What:** Promote `FinanceFrontmatter.update(file, mutator)` to a shared `frontmatter-mutator` mechanism. Generic API. Mirror v0.18.0's BeaconButton-promotion playbook.
>
> **Why:** Mirrors BeaconButton thinking. Still premature — finance is the ONLY current consumer. Wait for second blueprint to surface the need.

> [!example]- v0.20.x finance polish carries
> **What:** (a) Migration helper for pre-v0.17.0 finance entities; (b) Budget categories aggregate row (Total Planned / Total Actual at bottom of editor); (c) PaycheckExpensesEditor "All Paid" toggle.
>
> **Why:** Carry from v0.17.0/v0.18.0/v0.18.1/v0.18.2 handoffs. Each is small. Bundle-friendly with another cycle if smoke matrix doesn't grow significantly.

> [!example]- v0.20.x cross-doc line-citation drift lint (carry from v0.20.0 lesson c)
> **What:** Lint `Docs/*.md` for `[a-z\.]+\.js:\d+` patterns and assert each line still exists at that line in the cited file. Catches landmine #10 / #16-style drift before it ships.
>
> **Why:** Surfaced as v0.20.0 NEW lesson (c). Small additive harness extension; bundle-friendly with any docs-touching cycle.

---

## Recommendation

> [!success] **v0.21.0 project Sub-tasks panel on task hub** — PRIMARY (closes the v0.18.2 loop; clear UX win)
>
> **Why:** Sub-tasks correctly NOT on the Project Map (v0.18.2) leaves them homeless — the task hub note is the natural surface. Closing this loop is the most coherent next step. New CustomJS class is first-of-its-kind UI surface, so expect 1+ inline CFs (precedent: v0.5.0/v0.11.0/v0.12.0/v0.6.0/v0.16.0/v0.17.0/v0.19.0 — 7-data-point UI precedent for 1-6 CFs); plan reserved 0.21.x headroom upfront.
>
> **Skip for now:** styling polish carries (wait for user to report drift); styling presets (premature); FrontmatterMutator (premature); finance polish (smaller carry; bundle later); cross-doc lint (small additive; bundle with any future docs cycle).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper authoring tasks; master-driven on manifest mechanics + version bumps + harness extensions + landmine doc edits + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or mechanism API, QUOTE the literal call shape in the design doc.
> - **Two-stage subagent review (v0.20.0 lesson d)** — spec compliance first, code quality second; both cheap; quality reviewer catches drift / count bugs / cross-doc inconsistencies that spec-only review misses.
> - **Apply v0.19.0 + v0.20.0 lessons proactively** —
>   - (a) helpers that materialize NEW state need explicit prereq gates (v0.19.0)
>   - (b) shared `_externalPluginsSatisfied` gate is reusable (v0.19.0)
>   - (c) first-of-its-kind UI cycles can land near-clean when design quotes posture + harness covers spec literally + plan flags potential CF surfaces upfront (v0.19.0)
>   - (d) per-helper local `require("fs")` (v0.19.0)
>   - (e) two backup-suffix conventions: `.bak` overwrite, `.beacon-backup` additive (v0.19.0)
>   - (f) parallel subagents for independent helpers (v0.19.0)
>   - (g) test isolation via inline synthetic bodies — decouple harness tests from blueprint churn (v0.20.0 NEW)
>   - (h) helper-cases as a generic lint host — extend cheaply via per-file walker (v0.20.0 NEW)
>   - (i) cross-doc line-citation drift is a real maintenance class — when touching a cited file, grep all docs for `<filename>:<lineno>` references (v0.20.0 NEW)
>   - (j) master-driven S3 is the right shape — version bumps + status snapshots + result writeups + tags are judgment-heavy + approval-gated (v0.20.0 NEW)
> - **TDD discipline on harness extensions** — VT/AP/SS (v0.19.0) + BB1-BB6 (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate (when CustomJS classes change).
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9 / landmine #16).
> - **First-of-its-kind UI cycle pulls 1-6 CFs** (7-data-point precedent now: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 / v0.17.0 / v0.19.0).
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2 + v0.20.0 — 6 data points now)** — pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix cycles → reserved-headroom UNUSED reliable; new mechanism / new UI surface / first-of-its-kind → expect at least 1 CF (integration contract is the most common CF surface).
> - **Decision rule:** new mechanism / new blueprint with substantial CustomJS surface → expect CFs; pure-mechanism patch OR near-clone-of-existing OR pure adoption sweep OR bug fix scoped to existing helper OR pure docs/harness → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule (LANDMINE #16, v0.20.0)** — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1. The installer's short-circuit (`install.js:223`) skips re-process when `installedEntry.version === node.sub.version`. Lockstep edits to: blueprint/mechanism manifest + `platform/manifest.json` catalogue + workshop subscription (mechanisms only) + barebones subscription. Five-data-point precedent: v0.6.0 + v0.17.0 + v0.18.0 + v0.18.1 + v0.19.0.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **dv-shim composition discipline (v0.17.0 lesson)** — when composing a hub renderer that hands a sub-container to another customJS class, the shim MUST proxy every dv method that sub-class might call (`pages` + `current` + `el` minimum).
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs.
> - **BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers** (v0.11.0 CF-1 lesson). BeaconButton callers are inside CustomJS classes, not dataviewjs blocks — no wrapper needed.
> - **Per-blueprint version bumps require LOCKSTEP edits** to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11). For mechanism bumps, lockstep extends to 4 files: workshop manifest + mechanism manifest + workshop subscription + barebones subscription (v0.19.0 CF-1 reinforcement; codified in landmine #16).
> - **BeaconCards = hubs only / bullet lists for tasks** (v0.12.0 bounded-cohesion thesis).
> - **Outline-accent action buttons go through `customJS.BeaconButton.render(...)`** (v0.18.0 lesson) — no more inline cssText + hover handlers for new buttons.
> - **InvoiceControls Save UX pattern (v0.18.0 CF-1)** — when widget Save can be the cross-file truth source, also recompute derived fields on save AND extend `disabled` state to consider derived-field drift.
> - **Trailing-whitespace template defect class (v0.18.1 CF-1; CODIFIED v0.20.0)** — caseTW1 in helper-cases now lints all blueprint .md files for `[ \t]+$`. Future blueprint authoring no longer needs manual greps; lint catches before merge.
> - **Belt-and-suspenders predicate stacking (v0.18.2 lesson)** — for filters with multiple independent signals, prefer 2 cheap predicates over 1 cleaner one when surfaces are independent.
> - **Helpers-that-materialize-new-state need explicit prereq gates (v0.19.0 CF-1)** — `applyExternalPlugins` only emits warnings; helpers that mkdir + fresh-write must short-circuit at the top via `_externalPluginsSatisfied(tp, manifest)`.
> - **Two backup-suffix conventions (v0.19.0 lesson e)** — `.bak` for file-content overwrite (vendored themes), `.beacon-backup` for plugin-data additive merge.
> - **Vendored theme is mechanism-owned (landmine #15)** — never hand-edit `.obsidian/themes/<Name>/`. Customizations route through Style Settings JSON OR `.obsidian/snippets/`.
> - **Test isolation via inline synthetic bodies (v0.20.0 lesson a)** — when a harness test reads a blueprint asset for fixture content, prefer an inline synthetic body over `fs.readFileSync` of a real path; use `synthetic://` URI prefix as sentinel.
> - **Cross-doc line-citation drift is a real maintenance class (v0.20.0 lesson c)** — any cycle that touches a file cited as `<filename>:<lineno>` in any doc should grep all docs for that pattern and update in lockstep.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at **6 paths** (#12 v0.19.0 expansion) — adding a 7th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 7th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first.
> 3. Read v0.20.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (project Sub-tasks panel as primary).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step IF CustomJS classes change).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.20.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.20.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with primary recommendation (project Sub-tasks panel on task hub), constraints carried forward, and v0.20.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Renderer harness whole-suite NOW exits 0 (T4.0 fixed in v0.20.0); selectors no longer required for clean exit. Helper-cases harness has 346 sub-asserts (v0.20.0 added 34 via TW1).
3. Read v0.20.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with primary recommendation. v0.21.0 project Sub-tasks panel on task hub = primary; styling polish carries from real-world use = secondary; styling presets multi-style = tertiary (premature); FrontmatterMutator promotion = quaternary (still premature); finance polish carries / cross-doc lint = quinary.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.19.0 + v0.20.0 lessons proactively.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget classes; master-driven on manifest + version-bump mechanics + docs edits + harness extensions + cycle close; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (7-data-point UI precedent for 1-6 inline CFs); 6-data-point reserved-headroom-UNUSED precedent (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2 + v0.20.0) for non-UI dedupe / pure adoption / pure label / pure docs / scoped-bug-fix cycles still holds.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 7th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits (v0.12.0 gotcha 11; for mechanisms extend to 4 files per v0.19.0 CF-1; codified as landmine #16 in v0.20.0); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 1-6 inline CFs + template-changing migration friction + per-entity sub-folder pattern + Templater auto-promote reusable pattern); v0.16.0 lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry); v0.17.0 lessons (dv-shim composition discipline; badges[].style outline; outline-accent action button pattern as BeaconButton candidate — DONE in v0.18.0); v0.18.0 lessons (in-cycle re-process bump rule at scale [LANDMINE #16]; mechanism-promotion-as-clean-cycle-shape; harness stub maintenance; tone palette slot reservation; cross-file UX coupling discovered late); v0.18.1 lessons (pure adoption clean first-pass via API-contract quoting; trailing-whitespace template defect class [LINT SHIPPED in v0.20.0 caseTW1]; combined cycles are bundle-friendly); v0.18.2 lessons (belt-and-suspenders predicate stacking; master-driven cycles can be very fast; carry-forward bugs queue with line numbers); v0.19.0 lessons (helpers that materialize new state need stronger prereq contracts; shared _externalPluginsSatisfied gate; first-of-its-kind UI mechanism cycle pulled exactly 1 CF; per-helper local require("fs"); two backup-suffix conventions; parallel subagents for independent helpers; vendored theme is mechanism-owned [LANDMINE #15]); v0.20.0 NEW lessons (test isolation via inline synthetic bodies; helper-cases as generic lint host; cross-doc line-citation drift maintenance class; two-stage subagent review yields different signal at low cost; master-driven S3 is the right shape).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
