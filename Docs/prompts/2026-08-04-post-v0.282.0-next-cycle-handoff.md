# Next-session prompt — loop-integrity workstream 2

Copy the block below into a fresh Claude Code session in
`/Users/willfell/Documents/GitHub/sauce`.

---

Implement **workstream 2 of the loop-integrity program**: one source of truth.

START BY READING (all on `main`):

- `Docs/superpowers/specs/2026-08-04-loop-integrity-program.md` — the program
  decomposition. Workstream 2 treats disease A (one fact, two sources, nothing
  forcing agreement). Its sketch (§ Workstream 2) is a SKETCH, not an approved
  spec — brainstorm + design first (`superpowers:brainstorming`, then a design
  doc under `Docs/superpowers/specs/`), then `superpowers:writing-plans` +
  `superpowers:test-driven-development`.
- `Docs/plans/2026-08-04-v0.282.0-board-health-sweep-result.md` — what
  workstream 1 shipped (the measurement instrument), its spec interpretations,
  and its spec interpretations.
- `Docs/agent-guides/build-test-verify.md` — release workflow. Versioning,
  tagging, the tap: AUTOMATIC. The PR title decides the bump under
  squash-merge.
- `Docs/agent-guides/delivery-board.md` — coordinator verbs, including the new
  `board-health` (v0.282.0) and `heal-epic-bindings` (v0.281.1).

## Workstream 2 scope (from the program doc)

1. Extract canonical board/atlas/slice path derivation into the delivery
   mechanism; intake and coordinator both consume it; delete intake's mirror of
   `physicalProjectPrefix` (`.agents/skills/card-intake/scripts/card-intake.js`).
2. State the board-vs-ledger authority rule explicitly; make every consumer
   obey the same one.
3. CI gate recomputing the release bump from the **PR title**, failing when it
   differs from the branch's commits (would have caught the v0.281.1
   patch-vs-minor incident). Note: editing `.github/workflows/*` needs a
   `workflow`-scoped token — Claude's OAuth can't push workflow YAML; plan for
   the user to apply that file or supply a token.

## Instrument now available

`board-health --json` ships in v0.282.0 — use it before/after any change to
prove the board's divergence classes are unchanged. The hourly launchd job can
be installed per repo with
`node scripts/autoloop/board-health-launchd.js install <repo>`.

Also relevant for workstream 3 sizing later: the sweep's untracked-member count
includes legitimate cross-clone completions (local-per-clone ledger), so read
per-project notes rather than aggregating a single number.

## Standing constraints (unchanged, learned v0.281.1 + v0.282.0)

1. The PR title decides the release bump (squash-merge; `release:plan` reports
   the pre-squash answer).
2. Consumer vaults have live writers — mtime check, overlap check, snapshot
   before any vault write.
3. `platform/test/run-sticky-notes-render-guards.js` is flaky on `main`
   (~50%, `PERF-7-HARNESS`); inside `release:preflight`. Re-run to confirm the
   flake; do NOT fold a fix into your cycle.
4. Never run two copies of `run-codex-autoloop.js` at once.
5. One workstream, one branch, one PR. No hand-versioning, no hand-tags, no
   `Co-authored-by: Claude` trailer.
6. Staging under `scripts/`: tracked files need `git add -u`, new files need
   `git add -f` — landmine #30 (`/Scripts/` ignore case-folds onto `scripts/`
   on APFS).

## State at handoff

- Workshop `v0.282.0` shipped: `board-health` verb + `Board Health.md` note +
  launchd installer + `sauce.board-health.v1`. All ten `BH-*` cases green in
  `run-codex-autoloop.js`.
- Known-open, deliberately untouched: sticky-notes flake; headspace `Loop Ops`
  tombstone residue (`GA-OPS18c`/`GA-OPS18g` — `reap` heals it); `BoardHealth`
  customjs renderer for the note body (guard block degrades gracefully).
