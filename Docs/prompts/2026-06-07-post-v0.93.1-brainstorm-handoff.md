---
purpose: Fresh-session brainstorm handoff after the v0.91.3 → v0.93.1 follow-on sprint (4 more releases on top of the v0.89.0 → v0.91.2 sprint covered by the prior audit). Supersedes the 2026-06-05 handoff. Paste the body block below into a new Claude session.
status: ready-to-paste
opened: 2026-06-07
predecessor_state: workshop @ v0.93.1; cowork @ 0.31.1; mechanism count 17
supersedes: Docs/prompts/2026-06-05-post-v0.91.2-brainstorm-handoff.md
---

# Post-v0.93.1 brainstorm handoff

The block below is the slash-style prompt to paste into a fresh Claude session. It folds in the four cycles (v0.91.3 / v0.92.0 / v0.93.0 / v0.93.1) that landed after the 2026-06-05 handoff was written, and explicitly answers a known operational confusion: why is yesterday's accuris morning-briefing still weak when defenses keep shipping?

---

## What changed since the 2026-06-05 handoff

| Cycle | Tag | What landed | Brainstorm-shape it CLOSED |
|---|---|---|---|
| v0.91.3 PATCH | 2026-06-05 | trust user-prefs over vault-config (Fix A); deferred-MCP-tool loading upfront via ToolSearch (Fix B); dataviewjs regex tightened to canonical pattern (Fix C); bundled accuris vault-state repair (vault-config fence, mcp_map normalize, ADO served_by → UUID) | Most of Shape 3 (observability + noise — ADO noise root cause; Echoes-on-headspace partially) |
| v0.92.0 MINOR | 2026-06-06 | NEW `cowork:compose-body` sub-skill + helper + 9 byte-identical golden fixtures; body-shape write-guard in 5 write-run-note sub-skills (4th defense layer); Step 14 refactor across 5 orchestrators (composer-emitted assertions piped to write-run-note) | Shape 1 — "Layer 6 body-composition deterministic backstop" |
| v0.93.0 MINOR | 2026-06-06 | NEW `cowork:sync-scheduled-jobs` user-invocable orchestrator; new helper `compose-scheduled-job-wrappers-helper.js`; new `scheduled-job-contract.json` + 12 fixtures; slash `/cowork sync-scheduled-jobs [<engagement>]` | NEW work not in the prior handoff — replaces manual scheduled-task prompt-body editing |
| v0.93.1 PATCH | 2026-06-06 | FLN-v93-8 helper accepts array-form `served_by` + `voice_notes/vibe_notes/pep_talk_style` fallback chain; FLN-v93-9 orchestrator `## Apply` section auto-pushes wrapper bodies to claude.ai scheduled-tasks MCP via `update_scheduled_task` (schedule fields preserved; idempotent re-runs) | Same scheduled-jobs-sync line |

**Shape 2 (v0.92.1 required-plugins)** — STILL SHELF. Design + plan exist (modified/untracked in working tree): `Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md` + `-plan.md`. Per v0.92.0 + v0.93.0 close lessons, target_workshop_version needs rewrite from 0.92.0 → 0.93.2 when work resumes. Still the right next-cycle candidate for closing the headspace "Semantic index not available" warning at install time.

**Shape 4 (catch-up + cleanup)** — partially landed: working-tree drift cleaned in `18f6655` ("catch up post-v0.91.3 install drift before v0.92.0"). Narrative-docs catch-up (`why.md` / `how.md` / `use.md` / `architecture.md` / `README.md`) still outstanding — now 2-4 weeks stale + missing all of v0.85.0 → v0.93.1 in the narrative.

## Operational note before the next brainstorm

The accuris morning briefing output you sampled on 2026-06-05 06:00 MT (the one with stacked `[!warning]+ ...` callouts for every kind + "deferred tools need explicit loading") **pre-dates v0.91.3**. v0.91.3 closed at 2026-06-05 evening with this specific note in the result doc:

> Tomorrow's (2026-06-06) scheduled accuris morning briefing is the live verification window for Fix A + Fix B.

Then v0.92.0 + v0.93.0 + v0.93.1 all landed on 2026-06-06. For a consumer vault to actually run on v0.93.1 surfaces, this sequence must complete:

```
brew upgrade sauce                                  # picks up Homebrew tap release of v0.93.1
cd <consumer-vault> && sauce update --bump-pins    # re-materializes all SKILL.md / helpers / contract.json
```

There's a known gap (FLN-v92-5) where consumer vaults cache `installed.workshop_path` pointing at the brew Cellar version, so direct `node /Users/willfellhoelter/projects/repos/sauce/platform/install.js --vault <consumer>` does NOT bridge the dev workspace → consumer gap pre-release. The brew chain is load-bearing.

**Verify deployment BEFORE you re-run any cowork scheduled job and treat its output as evidence of contract state.** Otherwise you'll keep debugging fixes you already shipped.

---

## READY-TO-PASTE PROMPT

```
Post-v0.93.1 next-cycle brainstorm — sauce workshop, willfellhoelter machine.

REPO: /Users/willfellhoelter/projects/repos/sauce (sauce WORKSHOP vault, branch main,
currently workshop v0.93.1, cowork @ 0.31.1). Dogfoods as its own first consumer —
NOT a consumer vault. Run the CLAUDE.md vault-identity check before any write.

CONSUMER VAULTS on this machine (Obsidian Sync):
  - Accuris: /Users/willfellhoelter/notes/sauce/accuris-sauce/
  - Headspace: /Users/willfellhoelter/notes/sauce/headspace-sauce/

ALSO RELEVANT: willfell laptop runs scheduled jobs against
  /Users/willfell/Documents/obsidian/sync/sauce/accuris-sauce/
which is a SEPARATE consumer install on a separate machine. Its
Homebrew + sauce-update state is independent of this machine.

WHAT MAKES THIS HANDOFF DIFFERENT FROM A NORMAL SESSION-START
The workshop has shipped 12 releases over 5 days (v0.89.0 → v0.93.1). Two
handoffs exist — read them BOTH, in order:

  1. Docs/prompts/2026-06-05-post-v0.91.2-brainstorm-handoff.md (prior; superseded)
  2. Docs/prompts/2026-06-07-post-v0.93.1-brainstorm-handoff.md (this — supersedes)

The new authoritative state-of-the-world doc is:

  Docs/plans/2026-06-05-post-v0.91.2-vault-and-repo-state-audit.md

Plus the four cycle-result docs that have landed since it was written:

  - Docs/plans/2026-06-05-v0.91.3-trust-prefs-and-deferred-tools-result.md
  - Docs/plans/2026-06-06-v0.92.0-body-composer-result.md
  - Docs/plans/2026-06-06-v0.93.0-scheduled-jobs-sync-result.md
  - Docs/plans/2026-06-06-v0.93.1-fln-v93-8-and-9-result.md

STEP 0 — session-start preflight (mirror Docs/prompts/SESSION-START.md):
  cd /Users/willfellhoelter/projects/repos/sauce
  git fetch origin && git status        # working tree should be near-clean (see Step 2)
  git log --oneline -10                  # HEAD should be c35af54 (v0.93.1 PATCH) or later
  jq -r .version package.json            # expect 0.93.1 (or higher if more shipped)
  jq -r .workshop_version platform/manifest.json   # expect 0.93.1 (or higher)
  npm run release:preflight 2>&1 | tail -5   # expect ALL GREEN at version-sync ok: <current>

If HEAD is NEWER than c35af54 or version is higher than 0.93.1, more cycles
shipped after this handoff was written. Read every Docs/plans/2026-06-*-v0.93+
file dated after 2026-06-07, plus the most-recent
Docs/prompts/<date>-post-v<X.Y.Z>-next-cycle-handoff.md if one exists.

STEP 1 — read the load-bearing context in order. Do NOT skim:
  1. CLAUDE.md (thin router)
  2. Docs/plans/2026-06-05-post-v0.91.2-vault-and-repo-state-audit.md (the audit)
  3. Docs/plans/2026-06-05-v0.91.3-trust-prefs-and-deferred-tools-result.md
  4. Docs/plans/2026-06-06-v0.92.0-body-composer-result.md
     — Shape 1 from the prior handoff: NEW cowork:compose-body sub-skill + helper
       + 9 byte-identical golden fixtures + write-guard layer 4
  5. Docs/plans/2026-06-06-v0.93.0-scheduled-jobs-sync-result.md
     — NEW cowork:sync-scheduled-jobs orchestrator; replaces manual scheduled-
       task prompt-body editing
  6. Docs/plans/2026-06-06-v0.93.1-fln-v93-8-and-9-result.md
     — fast-follow PATCH after first live run; auto-push to claude.ai
       scheduled-tasks MCP via update_scheduled_task tool
  7. Docs/agent-guides/cycle-status.md (verify it matches actual HEAD; the v0.93.0
     S3 close updated this — should reflect Current = 0.93.0 or 0.93.1)
  8. Docs/landmines.md (22 entries; non-negotiable)
  9. Docs/agent-guides/build-test-verify.md
 10. Docs/agent-guides/code-conventions.md
 11. Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md (modified in
     working tree; v0.92.1 / v0.93.2 candidate — STILL SHELF; closes headspace
     "Semantic index not available" warning at install time)
 12. Docs/plans/2026-06-05-v0.92.x-body-composer-and-scheduled-jobs-sync-state.md
     (state snapshot that anchored the v0.92.0 + v0.93.0 designs)

STEP 2 — confirm the working tree.
After the 18f6655 "catch up post-v0.91.3 install drift before v0.92.0" cleanup,
the pre-existing drift the prior handoff documented is GONE. Expect:
  - M Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md (shelf — being refined)
  - ?? Docs/plans/2026-06-01-v0.92.0-required-plugins-plan.md (shelf plan — never committed)
That's it. If you see anything else, surface to user before any write.

STEP 3 — VERIFY DEPLOYMENT STATE on the consumer vaults BEFORE treating any
scheduled-job output as evidence of current contract state:

  brew list sauce --versions               # should include 0.93.1 (or current)
  brew upgrade sauce 2>&1 | tail -10       # ensure latest tap merged + brew has it

  for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
               /Users/willfellhoelter/notes/sauce/headspace-sauce; do
    echo "=== $vault ==="
    jq -r '.workshop_version // "MISSING"' "$vault/ranch/platform-installed.json"
    jq -r '.contributions.cowork[]?.version // "MISSING"' \
       "$vault/ranch/claude-surface-registry.json" 2>&1 | head -3
  done

If any consumer vault shows workshop_version != 0.93.1 (or whatever HEAD is),
that vault needs `sauce update --bump-pins` BEFORE re-running scheduled jobs.

KNOWN-GOOD POST-DEPLOYMENT WORKFLOW for the willfell laptop's accuris vault
(per v0.93.0 + v0.93.1 verification posture):

  1. brew upgrade sauce
  2. cd /Users/willfell/Documents/obsidian/sync/sauce/accuris-sauce
  3. sauce update --bump-pins
  4. From claude.ai desktop: /cowork sync-scheduled-jobs accuris
     — orchestrator composes wrapper bodies AND auto-pushes them via the
       scheduled-tasks MCP (FLN-v93-9). Schedule fields preserved.
       Idempotent: re-runs report `5 unchanged / 0 updated / 0 absent`.
  5. Tomorrow's scheduled crons fire against v0.93.1-aligned wrappers + run
     against v0.93.1 SKILL.md bodies in .claude/skills/cowork/. The deferred-
     tool-loading paragraph (v0.91.3 Fix B) should eliminate the "MCP unavailable
     — deferred tools need explicit loading" failure mode the 2026-06-05 06:00 MT
     run exhibited.

STEP 4 — invoke superpowers:brainstorming.

OPEN THE BRAINSTORM WITH THE DECOMPOSITION QUESTION:

Several shapes from the prior handoff have shipped or moved. The current open
candidates split into 6 buckets. Do NOT pre-commit; explore trade-offs based on
vault evidence + the user's gut on which finding hurts most today.

  Shape A — "v0.92.1 / v0.93.2 required-plugins" PATCH (shelf-ready)
    Scope: convenience@0.3.0 adds new-tab-default-page; smart-connections-
    bridge@0.2.0 declares smart-connections as required external plugin.
    Closes headspace's "Semantic index not available" warning at install
    time. Shelf design + plan are committed (design modified; plan untracked).
    target_workshop_version needs rewrite from 0.92.0 → 0.93.2.
    Stages: ~3. Mechanism bumps but no new mechanism.

  Shape B — "v0.93.x post-deploy live verification + drift assessment"
    Scope: NOT a code cycle — operational. Run STEP 3 above on both consumer
    vaults + run the next morning-briefing fire on accuris (laptop) to verify
    v0.91.3 + v0.92.0 surfaces are actually landing. Sample the output. If
    contract holds + voice / microscope discipline holds + Echoes callout
    surfaces (headspace) — celebrate. If body still drifts, debug from there.
    Stages: 0 — but answers gate the choice of which Shape comes next.

  Shape C — "Workshop self-subscription completion + FLN bundle" PATCH
    Scope: FLN-v92-4 (subscribe workshop to remaining 7 blueprints —
    boards/products/to-do/journal/trips/finance/teams; workshop dogfood
    install was silently skipping cowork through v0.91.x) + FLN-v92-5
    (installed.workshop_path cache invalidation on workshop bump; same root
    cause as v0.75.1 _resolveWorkshopPath bug) + FLN-v92-1 (extract shared
    body-shape write-guard helper from 5 copy-pasted sub-skill sections).
    Hygiene + structural cleanup. Stages: ~3-5.

  Shape D — "Slim orchestrators" MINOR (FLN-v92-2 + FLN-v93-2)
    Scope: full orchestrator slim refactor pulling Steps 1/3/17 (pre-flight,
    engagement-resolution, post-write verify) into thin orchestrator-helper
    sub-skills. Target: each cadence orchestrator drops to ~50 lines from the
    current ~200-line dispatcher. Plus FLN-v93-2 (factor engagement-resolution
    into shared cowork:resolve-engagement sub-skill — 8 orchestrators carry
    the same prose today; drift risk growing linearly with each new
    orchestrator). Lessons #4 from v0.92.0 result + #5 from v0.93.0 cite the
    drift cost. Stages: ~6-8. Cowork MINOR.

  Shape E — "Observability + open FLN cleanup" PATCH bundle
    Scope: FLN-v93-10 (auto-create canonical scheduled tasks via MCP when
    `mcp-task-absent:<taskId>` warning fires; surfaces it as warning today
    but requires manual creation in claude.ai) + FLN-v93-11 (dry-run flag
    on Apply step) + FLN-v93-4 (dry-run flag for file-write part) + FLN-v93-5
    (cowork:audit-scheduled-jobs drift-detection skill) + FLN-v89-P0-2
    synthesize-week thin-corpus guard (still open from the original
    brainstorm-input doc) + FLN-v89-P1-3 sauce doctor D6..D8 cross-vault
    drift detection + targeted ADO warning demotion. Stages: ~4-6.

  Shape F — "Narrative-docs targeted catch-up" maintenance bundle
    Scope: why.md / how.md / use.md / architecture.md / README.md targeted
    catch-up to mention memory layer (v0.84.x), semantic retrieval (v0.87.0),
    people-cohesion + identity resolver (v0.88.0/0.89.0), discover-people +
    find-missing-people (v0.90.0/0.91.0), defense-in-depth layer cake
    (v0.91.x), body-composer (v0.92.0), sync-scheduled-jobs (v0.93.0/0.93.1).
    2-4 week staleness across 5 files. Stages: ~2-3.

RECOMMENDED SEQUENCING:
  1. Run Shape B FIRST (verification — answers "is what we've shipped actually
     landing?"). 0 stages.
  2. Then Shape A (v0.93.2 required-plugins, plan-ready, narrow, closes
     headspace gap). ~3 stages.
  3. Then either Shape C (hygiene) or Shape E (observability) based on which
     verification finding from Shape B is most painful.
  4. Shape D (slim orchestrators) is a bigger commitment — defer until
     either Shape C/E clear out smaller FLNs OR the orchestrator-drift cost
     bites again on a future cycle.

THINGS TO CARRY INTO THE BRAINSTORM:

  1. The accuris 2026-06-05 morning briefing the user sampled (stacked MCP
     warning callouts; no actual gather output) PRE-DATES v0.91.3. Don't
     interpret it as "fixes don't work" — it's evidence of pre-fix behavior.
     The 2026-06-06+ scheduled fires are the real verification window for
     v0.91.3 Fix B (deferred-tool loading).

  2. The accuris 2026-06-05 weekly review the user sampled (real GitHub PR
     delta + chat synthesis + stakeholder check) was already producing high-
     quality output BEFORE v0.92.0 body-composer shipped. That's evidence
     the orchestrator-LLM path can work; the gaps are at lower-priority
     cadences and at specific failure-class boundaries.

  3. v0.93.0 + v0.93.1 mean scheduled-task prompt-body updates are NO LONGER
     a manual copy-paste exercise. Future cycles that change SKILL.md prose
     should mention `/cowork sync-scheduled-jobs <engagement>` in their
     post-deploy verification list.

  4. The dirty working tree from the prior handoff (cowork-removed CLAUDE.md +
     dogfood drift in ranch/) is GONE — commit 18f6655 cleaned it.

  5. v0.92.0 lesson #4 + v0.93.0 lesson #5 surfaced FLN-v92-5: consumer vaults
     cache installed.workshop_path pointing at the brew Cellar version.
     Direct `node platform/install.js --vault <consumer>` doesn't bypass the
     brew chain. The brew sequence is load-bearing for consumer updates and
     this is operationally important.

  6. Headspace's missing Echoes callout is most likely still missing because
     Smart Connections plugin still isn't installed (the v0.92.1 required-
     plugins shelf design closes this DIRECTLY at install time). Verify in
     Shape B; if confirmed, prioritize Shape A.

CONVENTIONS (baked in v0.82.0+ through v0.93.1 close, NON-NEGOTIABLE):
  - Direct-push to origin/main. One commit per stage. Conventional-commits.
  - NO Co-Authored-By trailer. Explicit `git add <paths>` per stage (NEVER -A).
  - Dogfood install: node platform/install.js --vault . --auto-approve
  - Preflight gated on scripts/check-version-sync.js (package.json ==
    workshop_version == catalogue pin).
  - 22 landmines (Docs/landmines.md) non-negotiable. Most relevant for next cycle:
      - #16 in-cycle bump rule (any in-cycle revision must bump the item version)
      - #20 manifest dest substitution-token validator pattern
      - #22 .local/ override seam
      - #23 created_at authoritative (frontmatter contract)
  - FLN-v841-1 pattern: at any test-pin update stage, grep for exact-version
    pins across platform/test/ before bumping.
  - V0750-VERSION exact-pin needs bumping in lockstep with cowork manifest version.
  - DEFENSE-LAYER POSTURE (v0.91.x + v0.92.0 emergent pattern):
      * Prompt-body sentinel (user-authored cron wrapper)
      * claude_surface disambiguation row (CLAUDE.md resolvers table)
      * Verbal-commitment Notice (path + type + voice + microscope)
      * Pre-write write-guard for path (v0.91.1)
      * Pre-write write-guard for frontmatter (v0.91.2)
      * Pre-write write-guard for body-shape via composer assertions (v0.92.0)
      * Step 17b post-write misdirection probe
    When adding new contracts, decide UPFRONT which layer enforces. Body content
    needs DETERMINISTIC enforcement (helper invoked, output piped — not prose).
  - For new sub-skills/helpers: match the v0.85.0 / v0.87.0 / v0.92.0 / v0.93.0
    template — `## Inputs` / `## Pre-flight` / `## Gather` / `## Decide` /
    `## Done` / `## Harness testing` ; structured-output declaration in prose
    so HC prose-lints can assert substrings ; graceful-failure clause with
    declared error tokens ; never-throws posture.

POST-DEPLOY ACTIONS (after USER APPROVAL of any new cycle that ships):
  - Tag v<X.Y.Z>; push tag.
  - homebrew-sauce: bump Formula/sauce.rb url + sha256; PR; user merges.
  - brew update && brew upgrade sauce
  - Per consumer vault: sauce update --bump-pins
  - Per consumer vault: /cowork sync-scheduled-jobs <engagement> (v0.93.0+)
    to push refreshed wrapper bodies to claude.ai scheduled tasks.
  - If cycle introduces new external_plugins (e.g., Shape A required-plugins),
    verify .obsidian/community-plugins.json + .obsidian/plugins/<id>/* materialized
    correctly per landmine #12 mechanics.

END STATE: cycle closed on origin/main; preflight ALL GREEN; cycle-close
artifacts written per Docs/prompts/SESSION-START.md § Cycle close convention;
new handoff doc at Docs/prompts/<date>-post-v<X.Y.Z>-next-cycle-handoff.md;
consumer vaults verified clean on the new version + scheduled-task wrappers
re-synced via /cowork sync-scheduled-jobs.

START with Step 0 + Step 1 + Step 2 + Step 3 in order. Step 3 verification
output literally determines which Shape to brainstorm. THEN proceed to Step 4.
```

---

## Notes for the human reading this file

- The audit doc from 2026-06-05 is still the load-bearing baseline for VAULT EVIDENCE — it captured 5 days of cron output across both consumer vaults. The four cycle-result docs that landed since cover the WORKSHOP-SIDE changes that responded to that evidence.
- The recommended Shape order (B → A → C/E → D) is opinionated. If verification (B) shows everything is landing cleanly, the natural next step is closing the headspace SC gap via Shape A. If verification shows persistent body-content drift despite v0.92.0 shipping, that's a higher-priority debug than any new cycle.
- Shape D (slim orchestrators) is structural work that pays off the next 5+ cycles. Worth picking up after one more round of FLN cleanup so the orchestrator surfaces are simpler before being refactored.
- The required-plugins design at `Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md` is filename-dated 2026-06-01 but `target_workshop_version` needs rewriting to match wherever it lands (0.93.2 PATCH or 0.94.0 MINOR depending on sequencing).
- When this file becomes stale (after the next cycle ships its own handoff), don't delete — it's the audit-driven snapshot at this moment.
