# Next-session prompt — loop-integrity workstream 3

Copy the block below into a fresh Claude Code session in
`/Users/willfell/Documents/GitHub/sauce`.

---

Implement **workstream 3 of the loop-integrity program**: a rail that fits.

START BY READING (all on `main`):

- `Docs/superpowers/specs/2026-08-04-loop-integrity-program.md` — the program
  decomposition. Workstream 3 treats disease C (no legitimate escape hatch, so
  work leaves the rail). Its sketch (§ Workstream 3) is a SKETCH — it changes
  completion semantics, so it explicitly **needs its own brainstorm**
  (`superpowers:brainstorming`, then a design doc under
  `Docs/superpowers/specs/`), then `superpowers:writing-plans` +
  `superpowers:test-driven-development`.
- `Docs/plans/2026-08-04-v0.282.1-ws2-one-source-of-truth-result.md` — what
  workstream 2 shipped, including the `effectiveProjectionMapping` /
  `projectedRecordMapping` divergence surfaced there (a candidate input to this
  design).
- `Docs/agent-guides/delivery-board.md` — coordinator verbs + the new
  § Board vs ledger authority (v0.282.1).
- `Docs/agent-guides/build-test-verify.md` — release workflow; the PR title
  decides the bump under squash-merge, and `release:check-bump` (v0.282.1) will
  tell you locally whether title and branch agree.

## Size it from data, not guesses

Run `board-health --json` against the loop-bound repos FIRST and read
per-project untracked-member notes. The aggregate untracked count **includes
legitimate cross-clone completions** (local-per-clone ledger) — do not read it
as rail-leaving frequency. The design question is whether out-of-band batch
completion is frequent enough to warrant a sanctioned verb (provenance-carrying
completion: PR number, merge SHA, reason), whether batch-claim / variable slice
granularity supersedes it entirely, and what a cross-process write guard
covering non-coordinator writers should look like.

## Workstream 3 scope (from the program doc)

1. Sanctioned out-of-band completion carrying provenance so a batch PR (the
   `EM-4/5/6` case) is a legitimate move rather than permanent drift.
2. Cross-process write guard covering non-coordinator writers (live agent
   sessions, cross-machine sync) — the selector lock does not cover them.
3. Batch-claim / variable slice granularity — possibly supersedes item 1;
   decide with the sweep's data.

## Standing constraints (unchanged)

1. The PR title decides the release bump (squash-merge); dogfood
   `release:check-bump` before opening the PR.
2. Consumer vaults have live writers — mtime check, overlap check, snapshot
   before any vault write.
3. `platform/test/run-sticky-notes-render-guards.js` is flaky on `main`
   (~50%, `PERF-7-HARNESS`); inside `release:preflight`. Re-run to confirm the
   flake; do NOT fold a fix into your cycle.
4. Never run two copies of `run-codex-autoloop.js` at once.
5. One workstream, one branch, one PR. No hand-versioning, no hand-tags, no
   `Co-authored-by: Claude` trailer.
6. Staging under `scripts/`: tracked files need `git add -u`, new files need
   `git add -f` — landmine #30 (`/Scripts/` ignore case-folds onto `scripts/`
   on APFS).

## State at handoff

- Workshop `v0.282.1` shipped: `delivery.topology.*` (path derivation +
  `resolveSliceAuthority` + `assertProjectableStatus`), intake mirror deleted,
  `release:check-bump` + `run-release-title-gate.js` in preflight.
- The `pr-title-bump` CI job is live and gates every PR (it passed on its own
  PR #762). Pushing `.github/workflows/*` needs an **SSH push** — Claude's
  HTTPS OAuth lacks `workflow` scope; no token needs to be minted.
- Known-open, deliberately untouched: sticky-notes flake;
  `effectiveProjectionMapping`/`projectedRecordMapping` demotion divergence
  (surfaced in the ws2 result doc); board-health hourly launchd job shipped but
  not activated (Director's call); workstream 4 (durable ledger) remains
  last-and-maybe-never.
