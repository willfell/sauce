---
date: 2026-05-13
purpose: Mid-cycle handoff for v0.38.0 deploy-confidence-substrate cycle. Predecessor session closed Stage S1 (seeder library + α loader + harness). Next session executes Stages S2 → onwards via subagent-driven development against the existing plan.
predecessors:
  - Docs/plans/2026-05-12-deploy-confidence-substrate-design.md (commit 30c0ba4)
  - Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md (commit 826d9a2)
gates:
  - HEAD = d7b7e80 on main
  - npm run release:preflight exits 0
  - run-seed.js shows 16 pass · 0 fail
  - Working tree clean
required_sub_skill: superpowers:subagent-driven-development
target_artifact: continue plan execution; cycle closes at v0.38.0 tag push
---

# v0.38.0 execution handoff — Stage S2 onwards

## Project identity

- **Project name:** `sauce` (rebranded from `beacon` in v0.23.0)
- **GitHub remote:** `git@github-personal:willfell/sauce.git`
- **Workshop dev repo (this machine):** `/Users/willfellhoelter/projects/repos/sauce`
- **Single-branch workflow:** direct push to `origin/main`. No feature branches, no PRs.
- **Author:** the user. Do NOT add `Co-Authored-By: Claude` trailers.
- **Commit-message convention:** conventional-commits. Scope tag for this cycle: `(seeder,smoke,ci,release)` or subset thereof. Examples: `feat(seeder): v0.38.0 S2.1 — ...`, `test(seeder): v0.38.0 S2.3 — ...`, `ci: v0.38.0 S9 — ...`.

## State at session start

- `workshop_version`: `0.37.0` (in `platform/manifest.json`)
- `package.json` `version`: `0.36.1` (DRIFTED from workshop_version — intentional; reconciled in S12)
- Mechanisms: 10 (customjs-guard, validator, audit, nav-buttons, cards, accent-button, people-rendering, styling, convenience, platform-claude)
- Blueprints: 11 (boards, cowork, daily, journal, meetings, people, project, scratch, to-do, trips, finance)
- Tags on origin: …, v0.36.0, v0.36.1, v0.37.0
- Whole-suite preflight: green; new `run-seed.js` adds 16 sub-asserts
- Working tree: clean

## Stage progress in this cycle

| Stage | Status | HEAD |
|---|---|---|
| S0 — pre-cycle smoke | ✅ closed | (read-only) |
| S1 — seeder library + α loader + harness | ✅ closed | `d7b7e80` |
| **S2 — `sauce seed` CLI verb** | **▶ next** | — |
| S3 — programmatic (β) loader | pending | — |
| S4 — `vault_kind:"test"` + `--reset` gate | pending | — |
| S5 — `run-integration-smoke.js` | pending | — |
| S6 — `project` α-seed | pending | — |
| S7 — `daily` β-seed | pending | — |
| S8 — `meetings` α-seed | pending | — |
| S9 — `ci.yml` matrix + smoke wired | pending | — |
| S10 — `release.yml` preflight gate + version-sync helper | pending | — |
| S11 — `Docs/use.md` release-process + branch-protection sections | pending | — |
| S12 — Workshop dogfood + version bump + drift reconciliation | pending | — |
| S13 — Cycle close + tag `v0.38.0` | pending | — |

S1 commits on origin/main (chronological):
- `517dda1` — seeder library skeleton + declarative loader
- `6e00df8` — fix: path-traversal guard + empty-string YAML quote + blueprint-name validation (from S1.1 code review)
- `626b605` — run-seed.js harness + 4 HEL sub-asserts
- `82c6182` — +5 MD sub-asserts
- `fac4ef9` — +7 LD sub-asserts
- `d7b7e80` — wire run-seed.js into release:preflight

## Pre-flight checks

Run all BEFORE touching code:

```bash
cd /Users/willfellhoelter/projects/repos/sauce
git log --oneline -7
git status
grep workshop_version platform/manifest.json
grep '"version"' package.json | head -1
npm run release:preflight 2>&1 | tail -10
```

Expected:
- `git log --oneline -7` ends at `d7b7e80 chore(test): v0.38.0 S1.5 …`
- `git status` shows clean tree
- `workshop_version: "0.37.0"`
- `package.json` `version: "0.36.1"` (drift intentional)
- preflight exits 0; last harness `run-seed.js: 16 pass · 0 fail`

If preflight fails on a DIFFERENT harness than `run-seed.js`, STOP and surface to user — that's a pre-existing problem.

## Required reads (~15 minutes)

In this order:
1. `Docs/plans/2026-05-12-deploy-confidence-substrate-design.md` — the spec (274 lines). Architecture, goals, non-goals, risks.
2. `Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md` — the 13-stage plan. Skim S2-S13 headers; deep-read S2 (next stage) + S5 (smoke harness shape).
3. `CLAUDE.md` — project identity, non-negotiables, landmines reference, current status snapshot.
4. `Docs/landmines.md` — particularly #11 (module-directory invariant — enforced by `helpers.writeNote`), #12 (.obsidian/ allowlist — must NOT touch), #22 (.local/ override seam — n/a this cycle but good to know).
5. `Docs/prompts/_handoff-protocol.md` — the canonical baton-pass protocol; cycle-close handoff (S13) follows the FULL end-of-session checklist (different from this mid-cycle handoff).

## Mission

Execute Stages S2 through whatever this session has budget for. Default trajectory: S2 → S3 → S4. These three stages complete the substrate (CLI verb + β loader + reset gate) and leave the next session ready to start S5 (smoke harness). If session budget allows, push further.

**Stage S2 = 3 tasks:**
- S2.1: Create `platform/cli/cmd-seed.js` (CLI dispatcher; reads `~/.sauce/vaults.json` registry, dispatches to `platform/seeder/seeder.js`)
- S2.2: Wire `seed` verb into `platform/cli/sauce-cli.js` table + `platform/cli/cmd-help.js`
- S2.3: Add 3 S-cases to `platform/test/run-cli.js`

The plan's task blocks contain full code verbatim. Implementer subagents should not need to read the plan file — controller (you) embeds task text inline in subagent prompts.

## Cadence guidance

- **REQUIRED SUB-SKILL:** `superpowers:subagent-driven-development`. The skill prescribes: fresh implementer subagent per task → spec-compliance reviewer subagent → code-quality reviewer subagent → fix loops if needed.
- **Combined-review shortcut for trivial test-addition tasks:** when a task only adds test cases (e.g., S2.3 adds 3 S-cases to an existing harness), the test-pass output IS the spec gate. One combined `spec→quality` reviewer subagent (haiku model) is sufficient; the two-reviewer protocol is overkill. Reserve the full two-reviewer protocol for implementation tasks (S2.1, S3.1, S3.2, S4.1).
- **Model selection:** Haiku for pure transcription tasks where the plan provides full code verbatim. Sonnet for tasks needing judgment (S2.1 dispatcher because the implementer must merge with existing cmd-* patterns; S4.1 because the --reset path edits an existing function).
- **Stage close = single bundled commit per task** with conventional-commits scope. Push after each commit. Never `--no-verify`.
- **Stage close inline verification:** after each stage, run `npm run release:preflight 2>&1 | tail -10` to confirm whole-suite green. If red, do NOT proceed to the next stage; fix or surface.
- **Mid-session handoff:** if budget runs out, write a successor handoff at `Docs/prompts/<date>-v0.38.0-execution-handoff-N.md` (suffix `-2`, `-3`, etc.) following this same template, commit + push, and end the session with the slash-command block for the next chat.

## Constraints carried forward (non-negotiable)

- **Landmine #11 (module-directory invariant):** every seed write must root at `spice/<module_directory>/` for the calling blueprint. `helpers.writeNote()` enforces via `path.posix.normalize` + prefix check — already in place from S1.1 fix `6e00df8`.
- **Landmine #12 (.obsidian/ allowlist):** none of this cycle touches `.obsidian/`. If a subagent proposes to, refuse.
- **Workshop dogfoods every release:** S12 re-seeds `/Users/willfellhoelter/notes/sauce/test-vault` as the visible witness (sets `vault_kind: "test"` in its `ranch/platform-config.json` first).
- **`vault_kind:"test"` field:** new field added in this cycle to `ranch/platform-config.json`. Default absent = production-marked. Only the `--reset` codepath consults the field (S4). Backwards-compatible.
- **Zero new runtime deps.** The seeder library is pure-Node. No `npm install <pkg>` allowed.
- **No `Co-authored-by: Claude`** in any commit.
- **Direct push to `main`** — no feature branches.

## Cross-cutting gotchas

1. **package.json version drift is INTENTIONAL until S12.** `workshop_version: "0.37.0"` vs `package.json version: "0.36.1"`. S10.1 creates `scripts/check-version-sync.js`. S10.2 prepends it to `release:preflight` — at which point preflight WILL FAIL on the drift. S12 reconciles both to `0.38.0`. Between S10.2 and S12, CI is expected red. Do not "fix" the drift early.
2. **Subagent prompts must embed full task text.** Per the subagent-driven-development skill, subagents must NOT be told to read the plan file. Copy the task's "Files / Steps / Commit" block verbatim into the implementer prompt.
3. **`runCliCase` / `withTempHome` / `withTempVault` helpers in `run-cli.js`:** S2.3 uses these. The plan notes their signatures need verification by reading the top of `platform/test/run-cli.js` — they're existing helpers. If the subagent's code doesn't match the existing helper signatures, the implementer adapts.
4. **CLI verb `--vault` resolution.** `platform/cli/cmd-reinstall.js` is the precedent. New verbs honor tilde-expansion via `expandTilde`. The plan's S2.1 code block includes this verbatim — preserve it.
5. **Programmatic seed body templates may not exist for pilot blueprints.** The S7 daily β-seed `try/catch`es `ctx.helpers.renderTemplate("templates/Daily.md", …)` and falls back to a minimal stub if the template isn't present. This is intentional — the seed should not require the blueprint to ship a "Daily.md" exact match.
6. **`sauce audit --claude-surface` flag exists** but plain `sauce audit` already covers the surface walker for smoke purposes. S5 (smoke harness) calls plain `sauce audit`; that's correct.

## Slash command for the next session

Paste this into a fresh chat:

```
/superpowers:subagent-driven-development

Plan: /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md
Spec: /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-05-12-deploy-confidence-substrate-design.md
Handoff: /Users/willfellhoelter/projects/repos/sauce/Docs/prompts/2026-05-13-v0.38.0-execution-handoff.md (READ THIS FIRST)

State: HEAD d7b7e80 on main; Stages S0+S1 closed; S2 next. Working tree clean; `npm run release:preflight` exits 0 with `run-seed.js: 16 pass · 0 fail`.

Mission: execute Stages S2 → S3 → S4 (CLI verb + β loader + --reset gate). If session budget allows, push further. Mid-session handoff at /Users/willfellhoelter/projects/repos/sauce/Docs/prompts/2026-05-13-v0.38.0-execution-handoff-2.md if stopping before S13.

Working directory: /Users/willfellhoelter/projects/repos/sauce. Direct push to main. No Co-authored-by trailers. Subagent-driven-development per the handoff doc's "Cadence guidance".
```

## End-of-cycle protocol pointer

When the session that closes the cycle (after S13) runs, it MUST satisfy the full checklist in `Docs/prompts/_handoff-protocol.md` § "End-of-session checklist (DEFINITIVE)". That includes: result writeup at `Docs/plans/2026-05-13-v0.38.0-result.md` (or whatever date), `CLAUDE.md` status snapshot update with v0.38.0 entry, annotated tag `v0.38.0` pushed, next-cycle entry prompt at `Docs/prompts/<date>-post-v0.38.0-next-cycle-handoff.md`, final user message with copy-paste slash command.
