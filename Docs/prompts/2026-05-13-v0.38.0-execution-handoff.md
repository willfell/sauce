---
date: 2026-05-13
purpose: AUTONOMOUS execution of the v0.38.0 deploy-confidence-substrate cycle, end-to-end through tag push. Predecessor session closed S0+S1. Next session executes Stages S2 → S13 inclusive, then performs the full cycle-close protocol. No user check-ins; no clarification questions; no per-stage pause gates. Run to completion or recursively hand off to the next session.
predecessors:
  - Docs/plans/2026-05-12-deploy-confidence-substrate-design.md (commit 30c0ba4)
  - Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md (commit 826d9a2)
  - Docs/prompts/2026-05-13-v0.38.0-execution-handoff.md (this file; predecessor session writeup baked in)
gates:
  - HEAD = d7b7e80 on main (will advance — pre-flight asserts d7b7e80 or any descendant)
  - npm run release:preflight exits 0
  - run-seed.js shows 16 pass · 0 fail
  - Working tree clean
required_sub_skill: superpowers:subagent-driven-development
target_artifact: tag v0.38.0 on origin; tap PR opened (and merged if gh auth permits)
autonomous_execution: true
manual_intervention_authorized: false (except true blockers — see "Stop conditions")
---

# v0.38.0 execution handoff — autonomous run-to-close

> **Read this whole file before any tool call. The instructions are calibrated for an agent operating without user check-ins.**

## Project identity

- **Project name:** `sauce` (rebranded from `beacon` in v0.23.0)
- **GitHub remote:** `git@github-personal:willfell/sauce.git`
- **Workshop dev repo (this machine):** `/Users/willfellhoelter/projects/repos/sauce`
- **Single-branch workflow:** direct push to `origin/main`. No feature branches, no PRs.
- **Author:** the user. NEVER add `Co-Authored-By: Claude` trailers.
- **Commit-message convention:** conventional-commits. Scope tag for this cycle: `(seeder,smoke,ci,release)` or subset thereof. Examples: `feat(seeder): v0.38.0 S2.1 — ...`, `ci: v0.38.0 S9 — ...`, `chore(release): v0.38.0 — workshop_version 0.37.0 → 0.38.0`.

## Operating mode — read carefully

**You are running autonomously.** The user is not watching. They will check back when you're done. Do not:
- Ask the user clarifying questions (the spec + plan + this handoff are complete).
- Pause for user approval between stages.
- Stop "for the user to review" except on the explicit stop conditions below.
- Append "Want me to continue?" or "Should I proceed?" prompts.

Instead, after each stage:
1. Run `npm run release:preflight 2>&1 | tail -10`. If green, proceed immediately to the next stage.
2. If red, fix inline (debug → fix → re-run preflight). If you cannot fix in 3 attempts, halt and write a recursive handoff (see "Recursive handoff").
3. Log stage close inline in your own message stream but do not solicit feedback.

**Cycle-close criteria** (the bar for "done"):
- All stages S2-S13 complete.
- `git tag v0.38.0` annotated, pushed to origin.
- `release.yml` runs on the tag push and opens a tap PR (or fails for an attributable reason that you document).
- Result writeup committed at `Docs/plans/2026-05-13-v0.38.0-result.md`.
- CLAUDE.md status snapshot updated with v0.38.0 entry.
- Next-cycle handoff written at `Docs/prompts/<date>-post-v0.38.0-next-cycle-handoff.md`.

When cycle-close criteria are all satisfied, output a final summary message and END. Do not loop further. Do not ask "what's next?"

## State at session start

- `workshop_version`: `0.37.0` (in `platform/manifest.json`)
- `package.json` `version`: `0.36.1` (DRIFTED from workshop_version — intentional; reconciled in S12; do NOT fix early)
- Mechanisms: 10 (customjs-guard, validator, audit, nav-buttons, cards, accent-button, people-rendering, styling, convenience, platform-claude)
- Blueprints: 11 (boards, cowork, daily, journal, meetings, people, project, scratch, to-do, trips, finance)
- Tags on origin: …, v0.36.0, v0.36.1, v0.37.0
- Whole-suite preflight: green; new `run-seed.js` adds 16 sub-asserts
- Working tree: clean
- HEAD: `43bffd4` (this handoff doc commit on top of S1 close `d7b7e80`)

## Pre-flight checks

Run all BEFORE the first implementer dispatch:

```bash
cd /Users/willfellhoelter/projects/repos/sauce
git log --oneline -8
git status
grep workshop_version platform/manifest.json
grep '"version"' package.json | head -1
npm run release:preflight 2>&1 | tail -10
```

Expected:
- `git log --oneline -8` includes `43bffd4 docs(prompts): v0.38.0 mid-cycle execution handoff` and `d7b7e80 chore(test): v0.38.0 S1.5 …`
- `git status` shows clean tree
- `workshop_version: "0.37.0"`
- `package.json` `version: "0.36.1"` (drift intentional through S12)
- preflight exits 0; last harness `run-seed.js: 16 pass · 0 fail`

**If preflight fails on ANY harness, halt and write a recursive handoff. Do not attempt to fix unrelated harnesses.**

## Required reads (only at session start; ~10 minutes)

In this order:
1. `Docs/plans/2026-05-12-deploy-confidence-substrate-design.md` — the spec (274 lines).
2. `Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md` — the 13-stage plan with full task text. **You will refer to this constantly; treat it as the source-of-truth for each task's verbatim code.**
3. `CLAUDE.md` — project identity, non-negotiables, landmines reference, current status snapshot.
4. `Docs/landmines.md` — particularly #11 (module-directory invariant), #12 (.obsidian/ allowlist), #20 (read-only source vaults during migrate — doesn't apply here but good to know).
5. `Docs/prompts/_handoff-protocol.md` — § "End-of-session checklist (DEFINITIVE)" — controls the S13 close procedure.

## Stage progress in this cycle

| Stage | Status | HEAD |
|---|---|---|
| S0 — pre-cycle smoke | ✅ closed | (read-only) |
| S1 — seeder library + α loader + harness | ✅ closed | `d7b7e80` |
| **S2 — `sauce seed` CLI verb** | **▶ next** | — |
| S3 — programmatic (β) loader | pending | — |
| S4 — `vault_kind:"test"` + `--reset` gate | pending | — |
| S5 — `run-integration-smoke.js` | pending | — |
| S6 — `project` α-seed | pending | — |
| S7 — `daily` β-seed | pending | — |
| S8 — `meetings` α-seed | pending | — |
| S9 — `ci.yml` matrix + smoke wired | pending | — |
| S10 — `release.yml` preflight gate + version-sync helper | pending | — |
| S11 — `Docs/use.md` release-process + branch-protection sections | pending | — |
| S12 — Workshop dogfood + version bump + drift reconciliation | pending | — |
| S13 — Cycle close + tag `v0.38.0` | pending | — |

S1 commits on origin/main (chronological):
- `517dda1` — seeder library skeleton + declarative loader
- `6e00df8` — fix: path-traversal guard + empty-string YAML quote + blueprint-name validation
- `626b605` — run-seed.js harness + 4 HEL sub-asserts
- `82c6182` — +5 MD sub-asserts
- `fac4ef9` — +7 LD sub-asserts
- `d7b7e80` — wire run-seed.js into release:preflight
- `43bffd4` — this handoff doc

## Mission

Execute Stages S2 through S13 inclusive. Land a tagged `v0.38.0` release. Open the tap PR (release.yml automation does this on tag push; verify it ran). Then perform the full cycle-close protocol from `_handoff-protocol.md` § "End-of-session checklist (DEFINITIVE)".

## Cadence guidance (autonomous execution)

### Per-task workflow

For implementation tasks (S2.1, S2.2, S3.1, S3.2, S4.1, S5.1, S5.2, S9.1, S10.1, S10.2, S10.3, S11.1, S13.1, S13.2):

1. Dispatch implementer subagent with the task's verbatim code/steps from the plan embedded in the prompt. Do NOT instruct the subagent to read the plan file.
2. Dispatch spec-compliance reviewer (haiku unless task is non-trivial).
3. If spec ✅, dispatch code-quality reviewer (haiku). If code-quality requests changes that strengthen security/correctness (path traversal, input validation, edge cases), accept them — but do not accept stylistic refactor requests outside spec scope.
4. If reviewer requests changes, dispatch fix subagent → re-review (one cycle max; second-cycle escalates to inline-fix by controller).
5. Mark task complete in TaskUpdate.

For pure test-addition tasks (S1.2-S1.5 pattern, applies to: S2.3, S3.3, S4.2, anywhere the task only adds sub-asserts to an existing `run-*.js`):

1. Dispatch implementer (haiku — full code in plan).
2. **Skip separate spec + quality reviewers.** The test-pass output IS the spec gate. If `node platform/test/run-*.js` exits 0 with the expected pass count, the task is done. Mark complete.

For documentation tasks (S11.1, S13.1, S13.2):

1. Dispatch implementer (sonnet — judgment on prose).
2. Skip reviewers unless the doc's content is load-bearing for behavior (CLAUDE.md status updates ARE load-bearing — review spec compliance only).

### Model selection

| Task category | Model |
|---|---|
| Pure verbatim code transcription (e.g., S2.1 dispatcher — full code in plan) | haiku |
| Test-only additions (S2.3, S3.3, S4.2) | haiku |
| Editing existing files with judgment (S2.2 verb-table insert, S4.1 --reset path, S9.1 CI rewrite) | sonnet |
| Code-quality reviewers | haiku |
| Spec-compliance reviewers | haiku |
| Fix subagents | sonnet (fixes usually need judgment) |
| Documentation prose (S11.1, S13.1, S13.2) | sonnet |
| Final pre-tag smoke + cycle close | sonnet (you, the controller — not subagent) |

### Inter-stage discipline

- After EACH stage close (last task in a stage's commit), run `npm run release:preflight 2>&1 | tail -15`. Confirm green. Proceed to next stage.
- Between S10.2 and S12, `release:preflight` WILL FAIL on the version-sync gate (package.json/manifest drift). This is intentional and expected. Skip the preflight check after S10.2 and S10.3 commits ONLY. Resume after S12.3 commits the reconciliation.
- Workshop self-install after S12: run `node platform/cli/sauce-cli.js reinstall --vault $PWD` (self-install) to confirm bumped versions are accepted by the workshop's own installer.

### Subagent prompt template (use this shape)

```
You are an implementer subagent for sauce v0.38.0. Implement Task <Sn.m> ONLY. Full task text embedded — do NOT read the plan file.

## Project context
[brief: working dir, branch, HEAD, conventions, no Co-authored-by]

## Task <Sn.m> — <title>
[copy verbatim from plan: Files / Steps / Code blocks / Commit message]

## Done criteria
[copy from plan, or infer from task structure]

## Status return
ONE of: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
```

Subagents return DONE+SHA, you mark complete, dispatch next.

## Stop conditions (when to halt instead of proceeding)

Halt and write a recursive handoff (see below) ONLY if:

1. **Pre-flight fails on an unrelated harness** (i.e., something broke pre-S2 that this cycle didn't touch). Diagnose; if not trivially fixable, recursive handoff with the failure documented.
2. **A subagent reports BLOCKED on a true environment issue** that can't be resolved by re-dispatch (e.g., disk full, network down, GitHub Actions auth missing).
3. **`gh` command unavailable when needed in S13.3** for tap PR merge. The release.yml workflow opens the tap PR automatically on tag push; merging it requires `gh pr merge --merge` against `willfell/homebrew-sauce`. If `gh` is unauthenticated for that repo, log the PR number/URL and proceed to cycle close — the user merges manually later. This does NOT block cycle close; it's a final delivery step.
4. **Plan ambiguity** — a task's spec contradicts itself or the spec doc. Document the contradiction inline and pick the most-conservative interpretation. If you cannot pick safely, recursive handoff. (This is unlikely; the plan and spec were carefully aligned.)

Do NOT halt for:
- Long sessions / token budget worries (write recursive handoff at a clean stage break instead — see below).
- Optional polish / "should I add X" thinking — defer to what the plan says verbatim.
- User-facing "would you like me to…" prompts — the answer is always "yes, per plan."

## Recursive handoff (only when context budget genuinely runs out)

If you reach the end of your context window mid-cycle (not at S13 close), perform a mid-cycle handoff at a clean stage break:

1. **Pick the last completed stage** (don't leave a stage half-done).
2. **Write a successor handoff** at `Docs/prompts/<today>-v0.38.0-execution-handoff-N.md` where `N` is one greater than the highest existing `-N` suffix (this file has no suffix, so the first successor is `-2`). Mirror this template EXACTLY: same operating mode, same cadence, same stop conditions. Update:
   - `gates`: new HEAD SHA.
   - "State at session start" with current values.
   - "Stage progress" table with closed-stage SHAs.
   - "Mission" section to start at the next unstarted stage.
3. **Commit + push** the successor handoff with `docs(prompts): v0.38.0 execution handoff -N (S<X>+S<Y> closed)`.
4. **Output ONLY** the slash-command block for the next session, no other prose. The user will paste it into a fresh chat.

The recursive-handoff invariant: each session pushes one or more stages closer to S13. Eventually, one session has enough budget to close. The chain is self-perpetuating until done.

## Constraints carried forward (non-negotiable)

- **Landmine #11 (module-directory invariant):** every seed write roots at `spice/<module_directory>/`. `helpers.writeNote()` enforces via `path.posix.normalize` + prefix check (already in `6e00df8`).
- **Landmine #12 (.obsidian/ allowlist):** none of this cycle touches `.obsidian/`. If a subagent proposes to, refuse.
- **Workshop dogfoods every release:** S12.2 re-seeds `/Users/willfellhoelter/notes/sauce/test-vault`. The visual-Obsidian-verify step (S12.2 step 3) is best-effort — skip the manual Obsidian-open step and document as `# Visual verification deferred to user` in the S12 commit message.
- **`vault_kind:"test"` field:** new field added to `ranch/platform-config.json`. Default absent = production-marked. Only the `--reset` codepath consults the field. Backwards-compatible.
- **Zero new runtime deps.** Pure-Node only. No `npm install <pkg>`.
- **No `Co-authored-by: Claude`** in any commit.
- **Direct push to `main`** — no feature branches.

## Cross-cutting gotchas

1. **package.json version drift is INTENTIONAL until S12.** `workshop_version: "0.37.0"` vs `package.json version: "0.36.1"`. S10.1 creates `scripts/check-version-sync.js`. S10.2 prepends it to `release:preflight` — at that point preflight WILL FAIL on the drift. **Between S10.2 and S12.3, SKIP the inter-stage preflight check** (or expect it to fail at the version-sync line specifically; if it fails ELSEWHERE, that's a real bug). S12.3 reconciles both to `0.38.0` and preflight goes green.
2. **CI is expected red between S10.2 and S12.3.** Tag pushes during this window would be blocked by `release.yml`'s new `preflight` job; that's correct. Don't tag until S12.3.
3. **`runCliCase` / `withTempHome` / `withTempVault` helpers in `run-cli.js`:** S2.3 uses these. The plan notes their signatures need verification by reading the top of `platform/test/run-cli.js` — they're existing helpers. If the subagent's code doesn't match the existing helper signatures, the implementer adapts inline.
4. **CLI verb `--vault` resolution.** `platform/cli/cmd-reinstall.js` is the precedent. New verbs honor tilde-expansion via `expandTilde`. S2.1's code block includes this verbatim — preserve it.
5. **Programmatic seed body templates may not exist for pilot blueprints.** S7's daily β-seed `try/catch`es `ctx.helpers.renderTemplate("templates/Daily.md", …)` and falls back to a minimal stub. The exact template filename may differ from `Daily.md` — S7.1 inspects to find the actual filename; S7.2 updates the seed to match.
6. **Smoke harness expectations on file counts:** S5.1 hardcodes `3 / 30 / 4` for `project / daily / meetings`. If the actual seed counts differ (e.g., daily generates 30 by design), update the smoke's `minNotes` constants to match.
7. **Manifests at HEAD use snake_case for blueprint/module/path fields.** When editing or reading manifests, preserve exact field-name spelling.
8. **`sauce audit --claude-surface` flag exists** but plain `sauce audit` already covers the surface walker for smoke purposes. S5 (smoke harness) calls plain `sauce audit`; that's correct.
9. **Linux CI may surface latent macOS-only assumptions** (S9 step 5). If S9's first run is red on Linux but green on macOS, debug inline as a sub-task `fix(ci): v0.38.0 S9.4 — Linux <root cause>`. Do NOT remove Linux from the matrix.
10. **Tag push triggers release.yml.** `release.yml` (modified in S10.3) first runs preflight; if green, opens a tap PR via `peter-evans/create-pull-request@v6` against `willfell/homebrew-sauce` using `TAP_PR_TOKEN` secret. You watch the tag-push result via `gh run watch` if `gh` is authenticated, OR poll via `gh run list --workflow=release.yml --limit=1` and read status. If `gh` unavailable, manually verify by visiting https://github.com/willfell/sauce/actions in stdout reporting (user reads).

## End-of-cycle protocol (when S13.4 completes successfully)

After tag push + tap PR open, perform the full checklist from `Docs/prompts/_handoff-protocol.md` § "End-of-session checklist (DEFINITIVE)":

1. **Result writeup** at `Docs/plans/2026-05-13-v0.38.0-result.md`. Required sections: frontmatter, abstract success callout, what shipped table, surprises (if any), pacing rhythm, cumulative file-level diff summary, state snapshot table, next-cycle handoff section.
2. **CLAUDE.md status snapshot** updated with v0.38.0 entry. Update: Cycle order line; Workshop version line (`0.37.0` → `0.38.0`); add v0.38.0 summary block in the standard form (lead with `**v0.38.0 summary (deploy-confidence substrate, CLOSED 2026-05-XX):**`); harness count line (`Nine` → `Eleven` load-bearing harnesses with run-seed.js + run-integration-smoke.js added); whole-suite total bumped from `~1145` to actual.
3. **Annotated tag** — already done in S13.3 (verify it's pushed).
4. **Next-cycle handoff** at `Docs/prompts/2026-05-13-post-v0.38.0-next-cycle-handoff.md`. Conform to `_handoff-protocol.md` template (NOT this autonomous-execution template — that was for THIS execution; the next cycle is presumably a normal brainstorm-driven cycle). Point at the design's "How this composes with future cycles" section as the menu of next-cycle candidates.
5. **Final commit + push** bundling the result writeup + CLAUDE.md update + next-cycle handoff: `chore(release): v0.38.0 CLOSED — result writeup + status snapshot + next-cycle handoff`.

When all five items committed and pushed, the cycle is closed. Output a final summary message (commit SHAs, tag confirmation, tap PR URL if known) and END.

## Slash command for the autonomous session

The user pastes this into a fresh chat to start the autonomous run:

```
/superpowers:subagent-driven-development

AUTONOMOUS RUN: Execute v0.38.0 deploy-confidence-substrate cycle end-to-end. No user check-ins; no clarification questions; no per-stage pauses. Run through tag v0.38.0 push + cycle close, OR recursively hand off at a clean stage break if context budget runs out.

Plan: /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-05-12-v0.38.0-deploy-confidence-substrate-plan.md
Spec: /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-05-12-deploy-confidence-substrate-design.md
Handoff (READ THIS FIRST): /Users/willfellhoelter/projects/repos/sauce/Docs/prompts/2026-05-13-v0.38.0-execution-handoff.md

State: HEAD 43bffd4 on main; Stages S0+S1 closed; S2 next. Working tree clean; `npm run release:preflight` exits 0 with `run-seed.js: 16 pass · 0 fail`.

Mission: complete Stages S2-S13, tag v0.38.0, perform full cycle-close protocol (result writeup, CLAUDE.md update, next-cycle handoff). Halt only on the explicit stop conditions in the handoff doc's "Stop conditions" section. If context budget runs out mid-cycle, write a recursive handoff per the handoff doc's "Recursive handoff" section and end with only the slash-command block.

Working directory: /Users/willfellhoelter/projects/repos/sauce. Direct push to main. No Co-authored-by trailers.
```

## A note to the next agent (you)

You are continuing a chain of sessions whose collective output is one cycle's worth of work. The user has authorized autonomous execution. They are not watching. Your job is to make as much progress as your context budget allows, then either close the cycle or hand off cleanly.

Do not ask permission. Do not ask for clarification. Do not pause "to confirm". The spec, plan, and this handoff are complete; everything you need is on disk. If you find yourself wanting to ask a question, re-read the relevant plan section — the answer is there.

If you're tempted to "polish" or "improve" something beyond what the plan says, don't. Implement exactly what's planned. Code quality is bounded by the spec; deviations are accepted only when fixing a CRITICAL bug found by code review (S1.1 set the precedent — path-traversal guard hardened beyond literal spec text because the spec's intent was clearly to prevent escape).

End your session with one of:
- **Cycle closed:** brief summary + commit SHAs + tag + tap PR URL.
- **Recursive handoff:** the slash-command block from your successor handoff doc. Nothing else.

Good luck.
