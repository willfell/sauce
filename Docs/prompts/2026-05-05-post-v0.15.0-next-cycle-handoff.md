---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.15.0 cycle. Triggered by v0.6.0 trips blueprint cycle close + tag v0.15.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-04-v0.6.0-trips-blueprint-result.md (v0.6.0 close — read first)
  - Docs/plans/2026-05-04-v0.6.0-trips-blueprint-design.md
  - Docs/plans/2026-05-04-v0.6.0-trips-blueprint-plan.md
  - Docs/plans/2026-05-04-v0.14.0-date-aware-nav-buttons-result.md
  - Docs/plans/2026-05-04-v0.13.0-journal-blueprint-result.md
  - Docs/prompts/2026-05-04-post-v0.14.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist of future blueprints — load-bearing for next-cycle pick)
gates:
  - v0.6.0 closed; tag v0.15.0 pushed; workshop manifest at 0.15.0
  - Path migration 2026-05-05 — workshop renamed `poc-vault → beacon`; barebones renamed `tmp-test-barebones-vault → barebones-beacon-poc`; accuris-mirror renamed `tmp-acc-vault → accuris-beacon-poc`. Both consumer `platform-config.json` files updated to `workshop_relative_path: ../beacon`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - nav-buttons mechanism at 2.5.1 (BUMPED in v0.6.0: 2.5.0 → 2.5.1 PATCH ADDITIVE — ICONS entries `trips` plane + `trip` location-pin)
  - cards mechanism at 0.2.1 (UNCHANGED in v0.6.0)
  - trips blueprint at 0.1.4 (NEW in v0.6.0; FOUR in-cycle CFs trips 0.1.0 → 0.1.4)
  - daily blueprint at 0.2.1 (UNCHANGED); journal at 0.1.0 (UNCHANGED); meetings at 0.2.0 (UNCHANGED); project at 1.3.2 (UNCHANGED); to-do at 0.1.2 (UNCHANGED); boards at 0.1.0 (UNCHANGED)
  - Helper-cases harness 255 sub-asserts (UNCHANGED in v0.6.0; pure blueprint addition with zero installer surface)
  - Renderer harness 8 cases (UNCHANGED in v0.6.0; trips uses BeaconCards via field-function API; no new selectors)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval — user picks)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.15.x or v0.16.0 (cycle-dependent)
---

# Onboarding — post-v0.15.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (LOAD-BEARING — paths CHANGED 2026-05-05)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git` — personal account `willfellhoelter@gmail.com`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (renamed 2026-05-05 from `poc-vault`)
> - **Sibling vaults (renamed 2026-05-05):**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked) — was `tmp-test-barebones-vault`
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level; mechanisms-only posture preserved) — was `tmp-acc-vault`
>   - `../accuris/` — production reference vault (NOT a v0.x consumer; source of pattern ports)
>   - `../headspace/` — personal reference vault (source of trips / finance pattern ports per `blueprints i want.md`)
>   - `../ero/` — business reference vault (source of business-finance / invoicing pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).
> - **Path migration verification:** any harness command from the workshop dir uses `../barebones-beacon-poc/...` (NOT `../tmp-test-barebones-vault/...`). Both consumer `platform-config.json:workshop_relative_path` now point at `../beacon`.

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.15.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, **`nav-buttons@2.5.1`** (BUMPED in v0.6.0: 2.5.0 → 2.5.1 PATCH ADDITIVE — `trips` plane + `trip` location-pin ICONS entries), `cards@0.2.1`
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.0`, `meetings@0.2.0`, `project@1.3.2`, `to-do@0.1.2`, **`trips@0.1.4`** (NEW in v0.6.0 — per-trip ecosystem under `beacon/trips/<slug>/` with atlas + 5 sections + Trip Board; three CustomJS classes; eight templates; folder-promote cards via Templater)
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` `v0.11.0` `v0.12.0` `v0.13.0` `v0.14.0` **`v0.15.0`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED in v0.6.0); `run-renderer.js` 8 cases (UNCHANGED in v0.6.0); all 3 harnesses green (with one pre-existing T4.0 fatal — see pre-flight #7)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 3 paths; helper count: 4
- Filename pattern `<Name>-YYYY-MM-DD` invariant (gotcha 8) load-bearing across two renderer call sites
- Mobile-aware visual design as renderer-level posture (gotcha 7)
- In-cycle re-process bump rule (gotcha 9) — TWO precedent shapes now established:
  - **Single-stage clean** (v0.13.0 + v0.14.0): design phase identifies STRONG precedent + ZERO novel mechanism surface beyond the named change → reserved 0.Y.x headroom UNUSED
  - **First-of-its-kind blueprint** (v0.5.0 + v0.11.0 + v0.12.0 + v0.6.0): substantial new entity-ecosystem surface OR new mechanism → reserved 0.Y.x headroom USED 2-6 times across inline CFs; design phase locks SHAPE, CF iterations refine FEEL
- NEW from v0.6.0: API-contract quoting discipline (CF-1 lesson); template-changing CFs cause migration friction (existing entities retain old body); per-entity sub-folder pattern lives within landmine #11 (e.g. `trips/<slug>/board/<task>/` mirrors project's `<slug>/tasks/<task>/`); Templater auto-promote pattern reusable across blueprints (Trip Board Card mirrors project Kanban Card, simplified)

**v0.6.0 commits in order (top-down — to be confirmed via git log post-cycle-close):**
- `<TBD>` docs(v0.6.0): cycle close — trips blueprint result + status snapshot + post-v0.15.0 handoff (THIS commit upcoming)
- `5af32d9` feat(trips): v0.6.0 CF-4 — atlas additional-row hide + sections cards groups + Trip Board mechanism; trips 0.1.3 -> 0.1.4
- `90c85fa` feat(trips): v0.6.0 CF-3 — hub groups + section cards + multi-row nav + bare templates; trips 0.1.2 -> 0.1.3
- `090fc8e` feat(trips): v0.6.0 CF-2 — New Section button on trip atlas; trips 0.1.1 -> 0.1.2
- `006cc0b` fix(trips): v0.6.0 CF-1 — TripsHubCards BeaconCards API + atlas filter; trips 0.1.0 -> 0.1.1
- `dcd51e1` chore(workshop,trips): v0.6.0 S2 — workshop 0.14.0 -> 0.15.0; nav-buttons 2.5.0 -> 2.5.1; trips@0.1.0 added
- `ce1edf7` feat(trips,nav-buttons): v0.6.0 S1 — trips@0.1.0 blueprint authoring + nav-buttons 2.5.0 -> 2.5.1 ICONS
- `1d7db51` docs(v0.6.0): trips blueprint design + implementation plan

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.6.0 baseline
> 1. **Confirm session CWD:**
>    ```bash
>    pwd  # should be /Users/willfell/Documents/obsidian/sync/workshop/beacon
>    ```
>    If NOT in `beacon/` — STOP. Restart Claude from the canonical path.
> 2. **Git head + recent commits:**
>    ```bash
>    git log --oneline -10
>    ```
>    Expected (top-down): a `docs(v0.6.0): cycle close` commit + `5af32d9` CF-4 + `90c85fa` CF-3 + `090fc8e` CF-2 + `006cc0b` CF-1 + `dcd51e1` S2 + `ce1edf7` S1 + `1d7db51` design+plan + below: `286e4db` post-v0.14.0 handoff + `dfc5bb9` v0.14.0 close.
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.15` → `v0.15.0` present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js \
>        Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.15.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.15.0"`. Confirm `nav-buttons` catalogue line shows `"version": "2.5.1"`. Confirm `trips` blueprint line shows `"version": "0.1.4"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`) for regression sweeps. **Pre-existing T4.0 fatal carried forward 6 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors. Carry-forward as a candidate cleanup for a v0.15.x polish cycle.
>    - All 6 BC cases green; both DA cases green.
> 9. **Barebones state (NEW PATH — `barebones-beacon-poc`):**
>    ```bash
>    grep -E '"(nav-buttons|trips)"' ../barebones-beacon-poc/Docs/Meta/platform-subscription.json
>    diff <(jq -S '.installed' ../barebones-beacon-poc/Docs/Meta/platform-installed.json) <(jq -S '{mechanisms, blueprints}' ../barebones-beacon-poc/Docs/Meta/platform-subscription.json)
>    ```
>    Expected: subscription pinned to `nav-buttons@2.5.1` + `trips@0.1.4`; pre-flight diff installed.json vs subscription should be clean post-v0.6.0.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`. Same for accuris-beacon-poc.

If any pre-flight fails, STOP and surface to user via `AskUserQuestion`.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol. END-OF-SESSION CHECKLIST IS LOAD-BEARING.
> 2. **`Docs/plans/2026-05-04-v0.6.0-trips-blueprint-result.md`** — most recent cycle close. Captures (a) FOUR in-cycle CFs (CF-1 BeaconCards API mismatch; CF-2 New Section action button; CF-3 hub groups + new TripSectionsCards class + bare templates + multi-row nav; CF-4 atlas additional-row hide + sections cards groups + Trip Board mechanism); (b) THIRTEEN manual smokes A-Q across CF iterations; (c) the contrast with v0.13.0 + v0.14.0 single-stage clean cycles — first-of-its-kind blueprints draw substantial smoke-time feedback; (d) API-contract quoting discipline; (e) Templater auto-promote pattern reusable across entity blueprints; (f) per-entity sub-folder pattern lives within landmine #11.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.6.0 close. Path migration note 2026-05-05.
> 4. **`blueprints i want.md`** — load-bearing for next-cycle pick. User's wishlist (post v0.6.0 close):
>    - **REMOVE: trips (line 4-5) — DONE in v0.6.0.**
>    - **REMOVE: date-aware nav buttons (line 73) — DONE in v0.14.0.**
>    - **REMOVE: journal (line 14-17) — DONE in v0.13.0.**
>    - KEEP: finance (line 6-13; hybrid personal+business; needs design-first round)
>    - KEEP: styling (line 18-67; user explicitly DEFERRED)
>    - KEEP: meeting cards on daily (line 71; bounded-cohesion thesis applies — likely already-satisfied as of v0.12.0; needs verification)
>    - KEEP: new-meeting-button parity (line 72; small contained refactor)
> 5. **`Docs/landmines.md`** — 14 entries unchanged in v0.6.0.
> 6. **`Docs/how.md`** — read the optional manifest fields callouts; cards mechanism context not yet codified there (candidate for a v0.5.1 docs cycle); v0.6.0 lessons not yet codified either.
> 7. **`Docs/plans/2026-05-04-v0.6.0-trips-blueprint-design.md`** + plan — most recent design + plan structure (use as template if next cycle is similarly entity-ecosystem shape like Finance).
> 8. **`Docs/plans/2026-05-04-v0.5.0-project-blueprint-result.md`** — closest precedent for a per-entity-ecosystem cycle BEFORE trips. Read alongside v0.6.0 result if next cycle is Finance (very similar shape).
> 9. **(If you pick Finance)** Reference reads:
>    - `../headspace/Finance/Finance.md` — personal-finance source pattern.
>    - `../ero/Finance/Finance-Hub.md` — business-finance / invoicing source pattern.
>    - `platform/blueprints/trips/manifest.json` + helpers + templates — closest near-clone manifest shape (per-entity ecosystem under module_directory).
> 10. **(If you pick new-meeting-button parity)** Reference reads:
>     - `platform/blueprints/project/helpers/project-nav-buttons.js` (the canonical `_promptForProjectName` + `_createProject` pattern).
>     - `platform/blueprints/meetings/Docs/Meta/Scripts/meetings/new-meeting-button.js` (current divergent shape).
>     - `platform/blueprints/trips/helpers/trip-nav-buttons.js` (the most-recent canonical pattern — `_promptForTripDetails` + `_createTrip`).
> 11. **(If you pick meeting cards on daily)** Reference reads:
>     - `platform/blueprints/daily/Docs/Meta/Scripts/daily/space-daily-dashboard.js` — meetings panel; verify whether it ALREADY uses BeaconCards (v0.12.0 should have migrated it). If yes — surface to user for closure (no cycle needed).

---

## Candidate cycles (pick ONE)

> [!example]- v0.7.0 / v0.16.0 finance blueprint **(PRIMARY recommendation)**
> **What:** ship a `finance` blueprint covering BOTH personal day-to-day budgeting (mirroring `../headspace/Finance/Finance.md`) AND business invoicing (mirroring `../ero/Finance/Finance-Hub.md`). User explicitly notes: "we can incorporate one financial blueprint that can handle the day to day budgeting / paycheck tracking, as well as have an invoicing system."
>
> **Why:** highest-utility user-facing surface from the wishlist post-trips. v0.6.0 just shipped the entity-ecosystem pattern (per-entity sub-folder + atlas + sections + per-entity board); finance is the natural next entity-ecosystem cycle. Same recipe applies — per-entity under `beacon/finance/<slug>/` (or split into `beacon/finance-personal/<slug>/` + `beacon/finance-business/<slug>/` depending on brainstorm decision).
>
> **Estimated scope:** 2-3 stages with reserved 0.Y.x headroom for 3-5 inline CFs (per v0.6.0 first-of-its-kind pattern). Workshop bump 0.15.0 → 0.16.0 MINOR (or 0.7.0 if you prefer wishlist-letter alignment).
>
> **Open questions for brainstorming:**
> - Hybrid shape: one blueprint with two surfaces, or two blueprints (`finance-personal` + `finance-business`)? User suggested ONE blueprint that handles both.
> - Recurring entries (monthly bills, biweekly paychecks) — Templater template? Background CustomJS class with auto-create on daily-note open?
> - Invoicing: per-invoice note files? Frontmatter-driven invoice ledger? PDF export integration (likely out of scope)?
> - Does it use BeaconCards (and what layout) for the hub view? Bounded-cohesion thesis applies — invoice ledger = hub-style YES; line-item bullet list = bullet list NO.
> - Bills aging / paid-status — frontmatter status field with a Dataview-driven view?
> - Per-entity (per-month? per-paycheck-period?) sub-folder pattern? Or flat budget/invoice notes?
> - Reuse Trip Board's auto-promote Templater pattern for invoice line items? Or simpler bullet-list?

> [!example]- v0.15.x.y new-meeting-button parity **(RUNNER-UP — small mechanical cycle)**
> **What:** harmonize `new-meeting-button.js` with `new-project-button.js` AND `new-trip-button.js` (now that trips ships). User explicitly wants "the new meeting button to reflect the pattern used for the new project button" (`blueprints i want.md` line 72). v0.6.0's `_promptForTripDetails` + `_createTrip` is now the most-recent canonical pattern for entity-create dialogs.
>
> **Why:** small, contained refactor. Improves UX consistency across entity-blueprints. Pre-investigation needed before scope can be locked. Good "back to clean cycle" candidate after v0.6.0's CF-heavy shape.
>
> **Estimated scope:** 0.5–1 stage. Read all 3 classes side-by-side, surface divergences, present a brainstorm checklist of what to harmonize, ship a meeting blueprint MINOR bump (`meetings@0.2.0 → 0.3.0`). Workshop bump 0.15.0 → 0.15.1 PATCH (or 0.16.0 MINOR if user-visible behavior changes).
>
> **Open questions for brainstorming:**
> - Side-by-side diff first — what divergences exist? UX flow? File-creation? Post-create-open?
> - Risk of regression on existing meetings: harmonize via opt-in setting, or rip-and-replace with explicit cycle bump?
> - Could this be bundled with a small docs polish (T4.0 cleanup + v0.5.1 lessons) for a single PATCH cycle?

> [!example]- v0.15.x.y meeting cards on daily — verification candidate
> **What:** verify whether `SpaceDailyDashboard`'s meetings panel already renders meetings as BeaconCards. v0.12.0 migrated MeetingsHubCards to BeaconCards (bounded-cohesion: hubs YES) AND v0.12.0's S3.4.x sub-stages reverted the daily-dashboard tasks panel from cards back to bullets (bounded-cohesion: tasks NO). The meetings-on-daily panel is the ambiguous middle case.
>
> **Why:** this candidate may be **already-satisfied** as of v0.12.0. Quick verify: read `space-daily-dashboard.js` and check whether the meetings panel calls BeaconCards. If yes — surface to user for closure (no cycle needed). If no — small bounded-cohesion-thesis-driven cycle.
>
> **Estimated scope:** 0–1 stage depending on verification outcome. If a cycle is needed: 1 stage; daily blueprint MINOR bump (`daily@0.2.1 → 0.3.0`); workshop bump 0.15.0 → 0.15.1 PATCH or 0.16.0 MINOR.

> [!example]- v0.15.x T4.0 lazy-scaffold harness cleanup + v0.5.1 docs cycle bundled
> **What:** fix the pre-existing T4.0 fatal in `node platform/test/run-renderer.js` (no selector). Hardcoded `kanban-board.md` path was deleted in v0.2.0 boards cycle; the harness has been silently broken since. v0.12.0 + v0.13.0 + v0.14.0 + v0.6.0 all chose to flag rather than fix.
>
> Bundle with a v0.5.1 docs cycle that lifts v0.11.0 + v0.12.0 + v0.13.0 + v0.14.0 + v0.6.0 lessons into `Docs/how.md`:
> - (a) BeaconCards-via-customjs-guard wrapping pattern
> - (b) ICONS-map invariant + adding an Nth entry pattern
> - (c) renderer-installer pair-invariant (validateAndResolve branch per action_type)
> - (d) dual-source-of-truth gap (per-blueprint version bumps need lockstep manifest pair edits)
> - (e) bounded-cohesion thesis (BeaconCards = hubs only; bullet lists for tasks)
> - (f) plan-correction patterns (path prefix + placeholder discipline + workshop self-sub blueprints[] empty + barebones not git-tracked)
> - (g) v0.14.0 lessons — helper-extraction-string-not-moment; regex precedent reuse across call sites; zero-config / always-on default posture; TDD-first commit pattern
> - (h) **NEW v0.6.0 lessons** — API-contract quoting; first-of-its-kind cycles draw inline CFs; template-changing migration friction; per-entity sub-folder pattern within landmine #11; Templater auto-promote reusable pattern
>
> **Why:** restores `run-renderer.js` to a clean exit; removes "known-broken-but-flagged" friction. Lifts institutional memory currently captured only in result writeups into the load-bearing how.md. Small fix; high cleanup value.
>
> **Estimated scope:** 1 stage (0.5 harness + 0.5 docs). Workshop bump 0.15.0 → 0.15.1 PATCH (or no bump — harness + docs only).

> [!example]- v0.15.x.y styling blueprint (deferred per user)
> **What:** ship a `styling` blueprint that bundles theme + CSS-snippet + Style Settings plugin config + Baseline community theme as a one-shot install. Codifies the per-attribute style-settings JSON the user has captured at `blueprints i want.md` lines 22-67.
>
> **NOTE:** USER EXPLICITLY DEFERRED — "this is something that will probably need to be done down the road rather than now as I'd rather get blueprints in there, then refine onboarding with something like this that handles plugins, styling, etc." Don't start this without explicit user reversal.

> [!example]- v0.4.3 mobile install path investigation (deferred)
> **What:** feasibility study for retiring landmine #8 (desktop-only installer). Currently `platform/install.js` uses `require("fs")` directly; mobile Obsidian doesn't expose Node's `fs`. Would require Obsidian Sync delivery + vault-adapter-only writes + a pure-Obsidian execution path.
> **Why:** opens beacon to Obsidian mobile users. Not a blueprint cycle; design + spike work.

---

## Recommendation

> [!success] **v0.7.0 / v0.16.0 Finance blueprint** — RECOMMENDED PRIMARY
> v0.6.0 just shipped the entity-ecosystem pattern (per-entity sub-folder + atlas + sections + per-entity Kanban board with auto-promote cards). Finance is the natural next entity-ecosystem cycle and the highest-utility item left on the wishlist. Same recipe applies; v0.6.0's lessons (API-contract quoting, design-locks-shape-CFs-refine-feel, Templater auto-promote reusable pattern) directly inform Finance's design.
>
> **Runner-up: v0.15.x.y new-meeting-button parity** — small contained refactor; "back to clean cycle" candidate after v0.6.0's CF-heavy shape; pre-investigation needed but bounded scope.
>
> **Skip for now (defer to dedicated cycles):**
> - v0.15.x.y meeting cards on daily — VERIFICATION CANDIDATE; quick read-and-confirm before opening a cycle
> - v0.15.x T4.0 + v0.5.1 docs polish — runner-up for a polish PATCH cycle; defer until user signals docs-cleanup priority
> - v0.15.x.y styling — USER EXPLICITLY DEFERRED
> - v0.4.3 mobile install — large standalone spike

---

## Cadence guidance (mirror v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 for entity-ecosystem cycles; mirror v0.13.0 / v0.14.0 for clean single-stage)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval — user picks.
> - Per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting (NEW v0.6.0 lesson):** when designing a class that calls an existing mechanism (BeaconCards, customjs-guard, etc.), QUOTE the literal call shape in the design doc — don't describe abstractly. Include `customJS.<X>.render(...)` example with concrete options. Avoids CF-1-style API mismatches.
> - **TDD discipline on harness extensions** — renderer-harness BC1-BC6 precedent (v0.12.0 S1) and DA1-DA2 precedent (v0.14.0 T1.2): write failing case, commit, then implement. Two-cycle precedent for TDD-first commit pattern.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian numbered as a SMOKE STEP (NEW v0.6.0 reinforcement):** for cycles introducing NEW CustomJS classes, the Cmd+R step gets its own numbered smoke task in the plan (not just a callout) — gotcha 2 codified.
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **Inline-CF-extension pattern** (v0.5.0 + v0.11.0 + v0.12.0 + v0.6.0 — FOUR cycles strong now): when user surfaces polish items at S2/S3 close OR mid-smoke, address as CF-N sub-stages with their own commits BEFORE cycle close. v0.6.0 was the heaviest yet (4 CFs covering 13 user-flagged items). Reserve `Y.Z.x` headroom upfront on any first-of-its-kind blueprint cycle.
> - **Reserved-headroom-USED pattern (v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 — FOUR data points):** for entity-ecosystem cycles, expect 2-6 in-cycle CFs. Plan headroom upfront. Don't fight the inline-CF pattern — design phase locks SHAPE; CFs refine FEEL.
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0 — two data points):** for cycles where the design phase identifies a STRONG precedent + ZERO novel mechanism surface beyond the named change, single-stage is the right shape and the reserved 0.Y.x headroom goes unused.
> - **Decision rule:** entity-ecosystem cycle (new blueprint with substantial CustomJS surface) → expect CFs; pure-mechanism patch OR near-clone-of-existing → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints. Helpers + template content can use `{{xxx}}` tokens (`substituteLenient` resolves at install time).
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts only. Project + Trips artifacts are slug-routed.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9) — when reusing in-cycle staged work at version X, in-cycle revisions to that work must bump X+1 to force consumer re-process. v0.6.0 went through FOUR in-cycle bumps (trips 0.1.0 → 0.1.4) — pattern is now strongly established for entity-ecosystem cycles.
> - **Verify command ids against runtime** (gotcha 10) — when an `invoke_command` action ships, verify the actual command id via `Object.keys(app.commands.commands).filter(...)` in Obsidian DevTools. Don't trust naming conventions.
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2 codified as numbered smoke step (v0.6.0 reinforcement).
> - Module-directory invariant under `beacon/` (#11) — codified at installer level. Per-entity sub-folders (`<slug>/board/<task>/`, `<slug>/tasks/<task>/`) live within the invariant — they're per-entity instances, not new sub-modules.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.
> - **v0.6.0 NEW lesson — API-contract quoting:** existing mechanism API contracts must be QUOTED literally in design docs, not described abstractly. Include `customJS.<X>.render(...)` call shape concrete example. CF-1's BeaconCards items-array vs field-function-API mismatch shipped because the design described semantics but didn't quote the call shape.
> - **v0.6.0 NEW lesson — first-of-its-kind blueprint cycles draw 3-5 inline CFs:** plan headroom; design locks SHAPE, CFs refine FEEL.
> - **v0.6.0 NEW lesson — template-changing CFs cause migration friction:** existing entities retain old template body content because templates are read at creation time. Design templates as bare-as-possible from the start to reduce CF rewrite surface.
> - **v0.6.0 NEW lesson — per-entity sub-folder pattern stays within landmine #11:** sub-folders like `<slug>/board/<task>/` mirror project's `<slug>/tasks/<task>/`; both are per-entity instances within the module's one directory.
> - **v0.6.0 NEW lesson — Templater auto-promote pattern is reusable:** Trip Board Card's `tp.file.move` mirror of project Kanban Card (simplified — no workstreams, no sub-tasks) is a reusable foundation for any future entity blueprint that needs per-task folders.

---

## Cross-cutting gotchas codified during recent cycles (READ BEFORE STARTING)

> [!warning] Gotchas surfaced 2026-05-03 / 2026-05-04 / 2026-05-05 — load-bearing for any cycle
>
> Gotchas 1-11 unchanged from post-v0.14.0 handoff. v0.6.0 added NO new numbered gotchas but reinforced multiple:
> - **Gotcha 2 (Cmd+R after Node-harness install)** — codified as numbered smoke step for cycles with new CustomJS classes (was a callout; now a step).
> - **Gotcha 9 (in-cycle re-process bump)** — FOUR-data-point evidence base now (v0.5.0 + v0.11.0 + v0.12.0 + v0.6.0); decision rule established for "single-stage clean" vs "first-of-its-kind expects CFs."
> - **Gotcha 10 (verify command ids)** — unchanged but reminder that ANY new `invoke_command` action requires runtime verification.
> - **Gotcha 11 (dual-source-of-truth lockstep)** — held across all 4 of v0.6.0's CF bumps with zero misfires.
>
> NEW patterns (v0.6.0, may graduate to numbered gotchas in future cycles):
> - **API-contract quoting** — see "Cadence guidance" above + "Constraints carried forward" above.
> - **Migration friction on template-changing CFs** — see same.
> - **Per-entity sub-folder pattern within landmine #11** — see same.
> - **Templater auto-promote pattern reusable across entity blueprints** — see same.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (NOT `poc-vault`).
> 2. Run all 10 pre-flight checks. Surface discrepancies first (especially the T4.0 fatal — known issue carried forward six cycles now; flag in advance).
> 3. Read the v0.6.0 result writeup + CLAUDE.md status snapshot + `blueprints i want.md` + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present the candidates above + recommendation; lock the choice before opening brainstorming. Reference `blueprints i want.md` annotations: trips DONE in v0.6.0; journal DONE in v0.13.0; date-aware DONE in v0.14.0; Finance + new-meeting-button parity + meeting-cards-on-daily + T4.0+docs are open candidates; styling explicitly deferred.
> 5. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions listed for that candidate. **Apply API-contract quoting discipline** in the design phase (quote any existing mechanism call shapes literally).
> 6. After brainstorming approval, invoke `de:writing-plans` to author the implementation plan.
> 7. After plan approval, invoke `de:subagent-driven-development` (same session) OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at S2/S3 close (with Cmd+R prerequisite as a NUMBERED step). Plan reserved 0.Y.x headroom for inline CFs IF the cycle is first-of-its-kind entity-ecosystem.
> 9. Cycle close: result writeup + CLAUDE.md status snapshot + commit + tag + push branch + tag + post-<cycle> next-cycle handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.15.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.15.0-next-cycle-handoff.md FIRST — the full onboarding doc with project identity (NEW PATHS as of 2026-05-05: workshop = beacon, barebones = barebones-beacon-poc, accuris-mirror = accuris-beacon-poc), 10 pre-flight checks, candidate cycles with recommendation (Finance blueprint primary; new-meeting-button parity runner-up), constraints carried forward, and cross-cutting gotchas (still 11 numbered + 4+ patterns).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (NOT poc-vault — restart Claude if wrong path).
2. Run all 10 pre-flight checks. Note the pre-existing T4.0 fatal on `node platform/test/run-renderer.js` (no selector) — known issue carried forward 6 cycles; use individual selectors (empty, malformed, unknown-action, beacon-cards, date-aware) for regression sweeps.
3. Read v0.6.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md (load-bearing for next-cycle pick) + landmines + how.md.
4. Surface the candidate cycles via AskUserQuestion with the recommendation. Get pick before brainstorming. Reference `blueprints i want.md` annotations: trips DONE in v0.6.0; journal DONE in v0.13.0; date-aware DONE in v0.14.0; Finance is the highest-utility remaining wishlist item; new-meeting-button parity + meeting-cards-on-daily are small candidates; styling explicitly deferred by user.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. **Apply API-contract quoting discipline** — for any class calling existing mechanisms, QUOTE the literal call shape in the design doc.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian as a NUMBERED smoke step (gotcha 2 v0.6.0 codification) when CustomJS classes are added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind entity-ecosystem (v0.5.0/v0.11.0/v0.12.0/v0.6.0 four-data-point precedent for 2-6 inline CFs); plan single-stage IF near-clone OR pure-mechanism (v0.13.0/v0.14.0 two-data-point precedent for clean cycles).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; filename pattern <Name>-YYYY-MM-DD invariant (gotcha 8) for date-routed daily artifacts (NOT for entity-routed like project/trips); verify invoke_command command ids against live runtime (gotcha 10); for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); v0.6.0 NEW lessons — API-contract quoting + first-of-its-kind cycles draw 3-5 inline CFs + template-changing migration friction + per-entity sub-folder pattern within landmine #11 + Templater auto-promote reusable pattern.

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
