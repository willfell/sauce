---
purpose: Resume-prompt + state snapshot for the session that picks up after v0.101.0 ships + deploys. The verbal-feedback-everywhere cycle (v0.101.0) put the prose-first feedback channel on ALL FIVE briefs — capture v=4 (tap + typing box everywhere) + per-cadence satisfaction {day, cadence, useful} + learned_weights schema 5 (purely additive) + generalized prose ingest (source_cadence tagging, dedup, downrank-skip symmetry). Successor is the cowork:doctor cycle (embedded, 4 checks — shape locked at the 2026-06-11 brainstorm; displaced ONE cycle by v0.101.0; its self-grading digest now reads PER-CADENCE satisfaction).
load_when: Starting a fresh session on/after 2026-06-13 to pick the next cycle after v0.101.0.
status: authoritative as of 2026-06-13 (v0.101.0 S3 cycle-close + this commit)
prior_cycle: v0.101.0 verbal feedback everywhere MINOR
result_doc: Docs/plans/2026-06-13-v0.101.0-verbal-feedback-everywhere-result.md
design_doc: Docs/plans/2026-06-12-v0.101.0-verbal-feedback-everywhere-design.md
vision: Docs/cowork-vision.md
ecosystem: Docs/cowork-ecosystem.md
---

# Post-v0.101.0 next-cycle handoff

This doc is the session-start reference for the FIRST conversation after v0.101.0 closes + deploys. The verbal-feedback-everywhere cycle shipped three moves in one MINOR:

1. **Capture v=4 — one shape on all five briefs.** One-tap `Useful: [ ] yes [ ] no` + the free-text typing box on top of EVERY brief's feedback section; EOD keeps its per-item lists + knobs; the 4 non-EOD cadences render ONE collapsed `Kinds — quick ticks` checklist (old checkboxes demoted to garnish). Legacy `rating-block` marker EMISSION retired (parser retained forever); write-guard accepts EITHER marker; upgrade-day kind ticks preserved from legacy priors.
2. **Per-cadence satisfaction (schema 5).** Entries become `{day, cadence, useful}` — up to five taps a day coexist; same-(day, cadence) overwrite; day-window trim (30) + 200-entry cap; prose WINS over tap on disagreement. `learned_weights` 4 → 5 is PURELY ADDITIVE (NO reset; counters intact; old entries normalize to `cadence: "eod-review"`). Graduation rules untouched.
3. **Prose ingest generalized.** Up to five typing boxes per day; intents tagged `source_cadence`; dedup ONE application per (day, intent, kind, entity); NEW `satisfaction` intent + `satisfaction-series` target; downrank-kind-no-entity = kind SKIP (`applyIntentKindTicks` renamed `applyIntentKindObservations`).

No new scheduled job; contract 0.35.1 UNCHANGED; zero new substitution tokens; mechanism count 17 unchanged.

Successor is the **cowork:doctor** cycle — shape locked at the 2026-06-11 brainstorm (embedded in the nightly reconciler, 4 checks, no new scheduled job, no contract bump), displaced ONE cycle by v0.101.0 (deliberately: the doctor's satisfaction-trend KPI is 5× richer now that taps + prose flow from all five cadences).

## State verification (run first)

```bash
cd /Users/willfellhoelter/projects/repos/sauce
git fetch origin && git status
git log --oneline -5
git rev-parse v0.101.0^{commit}                              # Expect: tag present
jq -r .version package.json                                  # Expect 0.101.0
jq -r .workshop_version platform/manifest.json               # Expect 0.101.0
jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # Expect 0.40.0
jq -r .version platform/blueprints/cowork/manifest.json      # Expect 0.40.0
jq -r '.contract_version' platform/blueprints/cowork/data/scheduled-job-contract.json # Expect 0.35.1 (UNCHANGED)
jq -r '.properties.schema_version.enum' platform/blueprints/cowork/data/schemas/eod-review@1.0.0.json   # Expect ["1.0.0","1.1.0","1.2.0","1.3.0","1.4.0"]
for s in morning-briefing midday-tripwire weekly-review monthly-review; do
  jq -r '.properties.schema_version.enum, (.properties.feedback_capture != null)' \
    "platform/blueprints/cowork/data/schemas/$s@1.0.0.json"   # Expect ["1.0.0","1.1.0"] + true × 4
done
brew list sauce --versions                                   # Expect 0.101.0
for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
             /Users/willfellhoelter/notes/sauce/headspace-sauce; do
  echo "=== $vault ==="
  jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
  jq -r '.blueprints[] | select(.name == "cowork") | "\(.name): \(.version)"' "$vault/ranch/platform-subscription.json"
done
```

Expected: HEAD on origin/main; tag `v0.101.0` present; brew sauce 0.101.0; both consumer vaults at workshop_version 0.101.0 + cowork 0.40.0; scheduled-job-contract `contract_version` 0.35.1 (UNCHANGED — zero new tokens; landmine #20 doesn't fire); eod-review sidecar schema enum runs through `1.4.0` (and `feedback_capture.sentinel_version` enum carries `"v=4"`); the 4 non-EOD sidecar schemas carry enum `["1.0.0","1.1.0"]` + the optional slim `feedback_capture` `{sentinel_version: ["v=4"], kinds_listed[]}`; feedback-capture marker `v=4` (parsers tolerate v=1 through v=4; rating-block = legacy corpus only); `learned_weights` `schema_version: 5` AFTER the first post-deploy 03:00 reconciler run per vault — **counters INTACT (additive migration; NO reset, unlike v0.99.0's)**, satisfaction entries carrying `cadence`.

**Post-deploy alignment:** ONE `align-scheduled-jobs` run per vault, run by the user from Cloud Cowork (the real surface — `/cowork sync-scheduled-jobs` does NOT exist as a slash command, per v0.98.2 Discovery 7).

## Arc

| Cycle | Scope | Slot |
| --- | --- | --- |
| **v0.101.0** ⭐ | **verbal feedback everywhere MINOR** — capture v=4 on all 5 cadences (tap + typing box on top everywhere; EOD per-item lists; non-EOD collapsed kind checklist; rating-block emission retired, parser forever; either-marker write-guard); per-cadence satisfaction `{day, cadence, useful}` + schema 5 (purely additive — no reset); generalized prose ingest (source_cadence, dedup per (day, intent, kind, entity), prose-over-tap, `satisfaction` intent, downrank-kind-no-entity = kind skip; `applyIntentKindObservations` rename); helper-cases 2455/0 (+85; 24 new HC-V1010 cases) | **SHIPPED 2026-06-13** |
| **cowork:doctor** (was slotted "v0.99.1"; displaced ONE cycle by v0.101.0) | **MINOR** — EMBEDDED in the nightly reconciler (shape locked 2026-06-11); 4 checks: (1) wrapper/sidecar version-drift vs installed; (2) sidecar schema conformance over a rolling window; (3) align-audit staleness (`scheduled-jobs.md` last_synced vs installed); (4) self-grading digest — **satisfaction trend NOW PER-CADENCE** (this is the v0.101.0 KPI upgrade: grade morning vs midday vs EOD separately) + top entity-weight movers. Anomalies → one collapsed callout in next morning brief; details → reconciler log. Plus the memory-engine-jobs out-of-contract ecosystem note | NEXT (brainstorm at cycle open) |
| **Then** | Cross-machine lockfiles + path-relative wrappers (BUNDLED — half-fixes rejected at the 2026-06-11 brainstorm Q6) | queued |
| **Parked / gated** | v0.95.1-migrator (until a real upgrade path needs it); weekly/monthly per-item ticks (demand-gated — v=4 already gives them tap + prose + kind ticks); workshop-wide strict-mode sweep; formula-constant tuning (data-gated on the doctor's digest); per-cadence rendering knobs (NEW — gated on doctor evidence that a cadence-level fix is needed); weekly/monthly Step 7 example-path fix (v0.101.0 Discovery 5 — `spice/cowork/daily/...` examples contradict the week/month-tier path headers; PATCH-sized) | parked |

## Open questions for the doctor brainstorm

The doctor's SHAPE is locked (embedded, 4 checks — 2026-06-11 brainstorm Q4). These remain open and should be resolved before the design is locked (carried verbatim from `Docs/prompts/2026-06-12-post-v0.99.0-next-cycle-handoff.md` § "Open questions for the v0.99.1 doctor brainstorm"):

1. **Where does the doctor write its detail log?** A `[doctor]`-tagged section in the existing `reconciler-log.md` (free — the reconciler already owns the write path; capped-30 contract already exists) vs a NEW `memory/<eng>/doctor-log.md` (separable history; another runtime-created file). Lean: reconciler-log entry — fewer files, and the morning-brief callout is the user surface anyway.

2. **Does the digest render daily or only on anomaly?** Daily digest = a standing satisfaction-trend pulse in every morning brief (reading-load cost — v0.98.0's reduction arc pushes against it). Anomaly-only = silent when green (the v0.96.1 heartbeat posture: silent on green). Lean: anomaly-only for checks 1-3; the self-grading digest (check 4) may deserve a weekly slot instead of daily.

3. **How does version-drift detection read wrapper stamps?** The cloud wrapper body isn't directly readable at reconcile time from the vault — candidates: read the sidecar's `generator`/`generated_by` stamp from yesterday's notes (what version COMPOSED the note — indirect but already on disk) vs a self-stamp clause in the wrapper that writes its own version into the sidecar at fire time (direct but needs a wrapper-template change + align run to deploy). Resolve against what's actually present in the live sidecars (verify-before-design).

NEW context for the brainstorm since those were written: check 4's input is now the PER-CADENCE series — the digest design should decide whether it reports per-cadence trends individually, the worst cadence, or a blended trend with per-cadence drill-down in the detail log.

## Carry-forward items (ready-to-go work)

- **`cowork:doctor` (NEXT).** 4 checks locked. Motivating data points: v0.98.2 Discoveries 5 (sidecar write-guard bypass) / 7 (operational docs named a nonexistent surface) / 8 (4-version wrapper staleness, silent); v0.99.0 Discovery 4 (external-plugin index drift — index-presence is doctor-checkable); divergent align-audit body formats between vaults; stale accuris cloud task NAMES. NEW from v0.101.0: per-cadence satisfaction is the digest's primary input.
- **Memory-engine jobs out-of-contract note.** `capture-tick` / `synthesize-day` / `synthesize-week` documented as DELIBERATELY out-of-contract — ecosystem-doc note lands in the doctor cycle alongside the staleness warning.
- **Lockfiles + path-relative wrappers.** Bundled cycle, queued behind doctor.
- **v0.95.1-migrator.** Still parked; design + plan execution-ready; ship-on-demand.
- **Weekly/monthly per-item ticks.** Demand-gated; additive when wanted.
- **Workshop-wide strict-mode sweep.** Per-touch rule held through v0.99.0 + v0.101.0; the rest remain. PATCH-sized hygiene.
- **Formula-constant tuning.** Data-gated — the doctor's per-cadence digest is the instrument that makes tuning evidence-based; do not tune blind.
- **Per-cadence rendering knobs.** Gated on doctor evidence — `source_cadence` is data accrual only this cycle.
- **Weekly/monthly Step 7 example-path fix.** v0.101.0 Discovery 5; PATCH-sized OI prose fix.

## Decision branch for the next session

**Default (recommended): brainstorm cowork:doctor.** The design is already heavily constrained (embedded, 4 checks, no new job, no contract bump — Q4 of the 2026-06-11 brainstorm), so the brainstorm is short: resolve the 3 open questions above (plus the per-cadence digest-shape question), then design + plan. Per-cadence satisfaction data starts accumulating from the v0.101.0 deploy, so every day before the doctor ships is observability data the platform collects but cannot yet read.

**Alternative: hold for first-fires.** Let v0.101.0 run 2-3 days; verify v=4 renders on all five briefs, the additive schema-5 migration fired once (counters intact), per-cadence satisfaction populates, no double-applied intents in `feedback-deltas.md`. The doctor brainstorm then has live multi-cadence data shapes to design against. This costs little — the doctor's check 4 reads exactly these artifacts.

**Alternative: v0.101.0.x PATCH** — only if first-fire grading surfaces a v=4/guard/dedup/migration defect.

## Resume prompt (paste into fresh session)

```
Resume sauce workshop work post-v0.101.0 close (2026-06-13).

REPO: /Users/willfellhoelter/projects/repos/sauce (workshop vault, NOT a
consumer). Run vault-identity check per CLAUDE.md before any write.

AUTHORITATIVE STATE DOC (READ FIRST, END-TO-END):
  Docs/prompts/2026-06-13-post-v0.101.0-next-cycle-handoff.md

ALSO LOAD-BEARING (read in order after the state doc):
  1. Docs/plans/2026-06-13-v0.101.0-verbal-feedback-everywhere-result.md
     (result doc — what shipped, 5 discoveries/lessons, doctor seeds)
  2. Docs/plans/2026-06-12-v0.101.0-verbal-feedback-everywhere-design.md
     (design — § Decisions locked at brainstorm Q1-Q7; § Contracts in
     detail C1-C6; § S1.0 audit)
  3. Docs/cowork-ecosystem.md (post-v0.101.0 ground truth: § 5.1 v=4 on
     the 4 non-EOD cadences; § 5.2 EOD v=4; § 5.3 one-emitted-marker
     story; § 7.2.7 schema 5 + per-cadence satisfaction; § 9 pipeline)
  4. Docs/prompts/2026-06-12-post-v0.99.0-next-cycle-handoff.md
     (predecessor handoff — the doctor's 3 open questions originate here)
  5. Docs/cycle-history.md § v0.101.0 (cycle narrative)
  6. Docs/cowork-vision.md (v0.101.0 row SHIPPED; § 2 dimension 6
     Quality observability is the doctor cycle's target)
  7. Docs/agent-guides/cycle-status.md (live workshop catalogue at 0.101.0)
  8. Docs/landmines.md (22 non-negotiable traps)

STATE VERIFICATION (per the state doc § "State verification"):
  git log --oneline -5
  git rev-parse v0.101.0^{commit}
  jq -r .version package.json                                  # 0.101.0
  jq -r .workshop_version platform/manifest.json               # 0.101.0
  jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # 0.40.0
  jq -r '.contract_version' platform/blueprints/cowork/data/scheduled-job-contract.json # 0.35.1 UNCHANGED
  jq -r '.properties.schema_version.enum' platform/blueprints/cowork/data/schemas/eod-review@1.0.0.json  # ["1.0.0","1.1.0","1.2.0","1.3.0","1.4.0"]
  for s in morning-briefing midday-tripwire weekly-review monthly-review; do
    jq -r '.properties.schema_version.enum, (.properties.feedback_capture != null)' \
      platform/blueprints/cowork/data/schemas/$s@1.0.0.json    # ["1.0.0","1.1.0"] + true
  done
  brew list sauce --versions                                   # 0.101.0
  for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
               /Users/willfellhoelter/notes/sauce/headspace-sauce; do
    echo "=== $vault ==="
    jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
    jq -r '.blueprints[] | select(.name == "cowork") | "\(.name): \(.version)"' "$vault/ranch/platform-subscription.json"
  done

EXPECTED STATE:
  HEAD: post-v0.101.0 close. Tag v0.101.0 present. brew sauce: 0.101.0.
  Both consumer vaults (BOTH LOCAL): workshop 0.101.0 + cowork 0.40.0.
  scheduled-job-contract: 0.35.1 UNCHANGED (zero new tokens).
  eod-review schema enum runs through "1.4.0"; 4 non-EOD schemas carry
  enum ["1.0.0","1.1.0"] + optional slim feedback_capture.
  Feedback marker v=4 on ALL FIVE briefs (parsers tolerate v=1..v=4;
  rating-block = legacy corpus only; write-guard accepts either marker).
  learned_weights: schema_version 5 AFTER the first post-deploy 03:00
  reconciler run per vault — ADDITIVE migration, counters INTACT
  (NO reset, unlike v0.99.0). Satisfaction entries carry cadence;
  old entries normalized to cadence: "eod-review". ONE
  align-scheduled-jobs run per vault from Cloud Cowork done
  post-deploy (NOT /cowork sync-scheduled-jobs — that slash command
  does not exist).

TASK FOR THIS SESSION (default): brainstorm cowork:doctor (displaced
one cycle by v0.101.0; shape stays LOCKED from the 2026-06-11
brainstorm Q4): EMBEDDED in the nightly reconciler; 4 checks; no new
scheduled job; no contract bump. THREE open questions to resolve at
brainstorm (carried verbatim from the post-v0.99.0 handoff):

  (1) Detail-log location — [doctor]-tagged reconciler-log entry
      (lean) vs new memory/<eng>/doctor-log.md file?

  (2) Digest cadence — anomaly-only for checks 1-3 (heartbeat
      posture: silent on green); daily vs weekly slot for the
      self-grading digest (check 4)? Reading-load arc pushes
      against daily.

  (3) Version-drift stamp source — sidecar generator/generated_by
      stamp (already on disk) vs wrapper self-stamp clause (needs
      template change + align run)? Verify live sidecar shapes
      before designing (verify-before-design rule).

  PLUS new since v0.101.0: check 4's input is PER-CADENCE — decide
  whether the digest reports per-cadence trends, the worst cadence,
  or a blend with per-cadence drill-down in the detail log.

THE 4 LOCKED CHECKS:
  1. wrapper/sidecar version-drift vs installed version
  2. sidecar schema conformance over a rolling window
  3. align-audit staleness (scheduled-jobs.md last_synced vs installed)
  4. self-grading digest — PER-CADENCE satisfaction trend
     (accumulating since the v0.101.0 deploy) + top entity-weight movers
  Anomalies → ONE collapsed callout in next morning brief;
  details → reconciler log.

DOCTOR DATA POINTS (motivating evidence):
  - v0.98.2 Discovery 5: sidecar write-guard bypassed at fire time
  - v0.98.2 Discovery 7: ops docs named a nonexistent slash command
  - v0.98.2 Discovery 8: wrappers ran 4 versions stale, silently
  - v0.99.0 Discovery 4: external-plugin index drift (realclaudian
    delisted upstream) — index-presence is doctor-checkable
  - divergent align-audit body formats between vaults
  - stale accuris cloud task NAMES

Apply v0.101.0 lessons: (1) enumerate EVERY fixture root in the S1.0
audit (platform/test/fixtures/ AND helpers/fixtures/); delete
consumer-less fixture dirs; (2) when new code starts PARSING a
previously-opaque field, audit every fixture that populates it
(impossible data is latent until interpreted); (3) keep the per-task
two-stage review (5 substantive catches this cycle); (4) fix contract
discrepancies STRUCTURALLY (either-marker guard), not with prose
carve-outs; (5) Step-7 example-path contradiction in weekly/monthly
OIs is a parked PATCH candidate.

OPEN ALTERNATIVES (if user redirects):
  - Hold for first-fires (2-3 days): verify v=4 renders on all five
    briefs, additive schema-5 migration (counters INTACT), per-cadence
    satisfaction populating, no double-applied intents — then the
    doctor brainstorm designs against live multi-cadence shapes.
  - v0.101.0.x PATCH — only if first-fire grading surfaces a
    v=4/guard/dedup/migration defect.

SAUCE CONVENTIONS (NON-NEGOTIABLE):
  - Direct-push to origin/main only after S4 user-approval gate.
  - One commit per stage; conventional commits; explicit git add <paths>
    (NEVER -A).
  - NO Co-Authored-By trailer in commit messages.
  - Vault-identity check before any write.
  - 22 landmines (Docs/landmines.md) apply.

START with the state verification commands above. Report what you find.
Then begin the cowork:doctor brainstorm (or alternative path if user
redirects).
```

## Cross-references

- **v0.101.0 result doc:** `Docs/plans/2026-06-13-v0.101.0-verbal-feedback-everywhere-result.md` — what shipped + 5 discoveries/lessons + post-deploy operational sequence
- **v0.101.0 design:** `Docs/plans/2026-06-12-v0.101.0-verbal-feedback-everywhere-design.md` — § Decisions locked at brainstorm (Q1-Q7); § Contracts in detail (C1-C6); § S1.0 audit
- **v0.101.0 plan:** `Docs/plans/2026-06-12-v0.101.0-verbal-feedback-everywhere-plan.md`
- **Post-v0.99.0 handoff (predecessor):** `Docs/prompts/2026-06-12-post-v0.99.0-next-cycle-handoff.md` — origin of the doctor's 3 open questions (§ "Open questions for the v0.99.1 doctor brainstorm")
- **v0.99.0 result doc (prior feedback cycle):** `Docs/plans/2026-06-12-v0.99.0-sparse-signal-feedback-result.md` — Discovery 4 + the engagement-gate/satisfaction groundwork this cycle generalized
- **Ecosystem walkthrough (post-v0.101.0 ground truth):** `Docs/cowork-ecosystem.md` — § 5.1 v=4 non-EOD shape; § 5.2 EOD v=4; § 5.3 one-emitted-marker story; § 7.2.7 schema 5; § 9 per-cadence pipeline
- **Cycle history v0.101.0:** `Docs/cycle-history.md` § v0.101.0
- **Live cycle status:** `Docs/agent-guides/cycle-status.md` (workshop 0.101.0)
- **North Star:** `Docs/cowork-vision.md` (v0.101.0 row SHIPPED; § 2 dimension 6 Quality observability frames the doctor cycle)
- **Cloud Cowork aligner:** `Docs/cloud-cowork/align-scheduled-jobs.skill.md` (the real post-deploy sync surface)
- **install.md Upgrading section:** `commands/install.md` (v0.101.0 deploy note: schema-5 ADDITIVE — no reset; v=4 on all five briefs; align once per vault)
- **Landmines:** `Docs/landmines.md` (22 entries; #20 lockstep — contract DATA unchanged at 0.35.1; #11 module-directory invariant — all v0.101.0 writes confined to `spice/cowork/`)
