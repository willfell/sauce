---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.16.0 cycle. Triggered by v0.16.0 finance blueprint cycle close + tag v0.16.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.16.0-finance-blueprint-result.md (v0.16.0 close — read first)
  - Docs/plans/2026-05-05-v0.16.0-finance-blueprint-design.md
  - Docs/plans/2026-05-05-v0.16.0-finance-blueprint-plan.md
  - Docs/plans/2026-05-04-v0.6.0-trips-blueprint-result.md
  - Docs/prompts/2026-05-05-post-v0.15.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; finance line 3-10 now DONE)
gates:
  - v0.16.0 closed; tag v0.16.0 pushed; workshop manifest at 0.16.0
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - cards mechanism at 0.2.2 (BUMPED in v0.16.0 from 0.2.1 PATCH ADDITIVE — `success` tone `#16a34a` green)
  - nav-buttons mechanism at 2.5.2 (BUMPED in v0.16.0 from 2.5.1 PATCH ADDITIVE — `finance` Lucide wallet ICONS entry)
  - finance blueprint at 0.1.4 (NEW in v0.16.0; FOUR in-cycle CFs finance 0.1.0 → 0.1.4)
  - All other blueprints UNCHANGED
  - Helper-cases harness 255 sub-asserts (UNCHANGED in v0.16.0)
  - Renderer harness 9 cases (UP from 8 in v0.6.0; +1 BC7 success-tone TDD-first)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.16.x or v0.17.0 (cycle-dependent)
---

# Onboarding — post-v0.16.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `../accuris/` / `../headspace/` / `../ero/` — production reference vaults (sources for pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.16.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, **`nav-buttons@2.5.2`** (BUMPED in v0.16.0: 2.5.1 → 2.5.2 PATCH ADDITIVE — `finance` ICONS entry), **`cards@0.2.2`** (BUMPED in v0.16.0: 0.2.1 → 0.2.2 PATCH ADDITIVE — `success` tone)
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.0`, `meetings@0.2.0`, `project@1.3.2`, `to-do@0.1.2`, `trips@0.1.4`, **`finance@0.1.4`** (NEW in v0.16.0)
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` `v0.11.0` `v0.12.0` `v0.13.0` `v0.14.0` `v0.15.0` **`v0.16.0`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED in v0.16.0); `run-renderer.js` 9 cases (UP from 8 in v0.6.0; BC7 NEW); pre-existing T4.0 fatal still carried (use selectors)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 3 paths; helper count: 4

**v0.16.0 commits in order (top-down):**
- `<TBD>` docs(v0.16.0): cycle close — finance blueprint result + status snapshot + post-v0.16.0 handoff (THIS commit upcoming)
- `473e2c7` fix(finance): v0.16.0 CF-4 — defensive truthy paid-expense check; finance 0.1.3 -> 0.1.4
- `d0a2c25` fix(finance): v0.16.0 CF-3 — Budget + Paycheck per-entity sub-folders; finance 0.1.2 -> 0.1.3
- `5abb94d` fix(finance): v0.16.0 CF-2 — date-tolerant FinanceStatus + entity-page status badges + Time-Log embed dedup; finance 0.1.1 -> 0.1.2
- `67e5e3b` fix(finance): v0.16.0 CF-1 — top hub action row + area-link row + remove # Finance heading; finance 0.1.0 -> 0.1.1
- `f3d9d3e` feat(finance): v0.16.0 S2 — finance@0.1.0 blueprint authored — 9 helpers + 10 templates + manifest
- `e2663e5` feat(cards,nav-buttons): v0.16.0 S1 — cards 0.2.1 -> 0.2.2 success tone + nav-buttons 2.5.1 -> 2.5.2 finance icon; workshop 0.15.0 -> 0.16.0; finance@0.1.0 forward-declared
- `138188f` docs(v0.16.0): finance blueprint implementation plan
- `c6e9efd` docs(v0.16.0): finance blueprint design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.16.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -12` — expect 8 cycle commits + 4 prior (post-v0.15.0 handoff + v0.6.0 close).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `blueprints i want.md`).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.16` → `v0.16.0` present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.16.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.16.0"`. Confirm `cards` catalogue line shows `"version": "0.2.2"`, `nav-buttons` shows `"2.5.2"`, `finance` shows `"0.1.4"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`) for regression sweeps. **T4.0 carried-forward 7 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors.
>    - All 7 BC cases green (BC7 NEW); both DA cases green.
> 9. **Barebones state:** subscription pinned to `cards@0.2.2`, `nav-buttons@2.5.2`, `finance@0.1.4`. Install green + idempotent.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.16.0-finance-blueprint-result.md`** — most recent cycle close. Captures: 4 in-cycle CFs (top hub action row → date-tolerant + entity-page badges → per-entity sub-folder symmetry → defensive paid-truthy); 12 manual smokes user-confirmed; 6 NEW v0.16.0 lessons (YAML date auto-parsing, parser-variance booleans, NBSP gotcha, renderBadge pattern, embed dedup, per-entity sub-folder symmetry).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.16.0 close.
> 4. **`blueprints i want.md`** — user's wishlist (post v0.16.0 close):
>    - **REMOVE: finance (line 3-10) — DONE in v0.16.0** as a foundational blueprint; v0.17.0 candidate is widget-driven UIs ON TOP of finance.
>    - REMOVE: trips (line 4-5) — DONE in v0.6.0
>    - REMOVE: date-aware nav buttons (line 73) — DONE in v0.14.0
>    - REMOVE: journal (line 14-17) — DONE in v0.13.0
>    - KEEP: styling (line 18-67; explicitly DEFERRED)
>    - KEEP: meeting cards on daily (line 71; verify whether v0.12.0 already satisfied)
>    - KEEP: new-meeting-button parity (line 72; small contained refactor)
> 5. **`Docs/landmines.md`** — 14 entries unchanged in v0.16.0; FIVE new candidate gotchas surfaced (see status snapshot).
> 6. **`Docs/how.md`** — six cycles of lessons NOT YET codified (v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0); candidate for a docs polish cycle.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.17.0 finance widget UIs **(PRIMARY recommendation)**
> **What:** ship widget-driven UIs ON the finance entity pages so users never manually edit frontmatter. Closes the user's foundational ask from v0.16.0 smoke time:
> - **BudgetCategoriesEditor** — Add Category / Edit Category / Delete Category buttons on each Budget page. Each row: name + planned + actual numeric inputs. Mutates `categories[]` frontmatter via vault read+write.
> - **PaycheckExpensesEditor** — Add Expense / Toggle Paid / Delete Expense buttons on each Paycheck page. Each row: item + amount + category + paid toggle + url. Mutates `expenses[]` frontmatter.
> - **InvoiceTimeLogEditor** — Add Time Entry / Edit Entry / Delete Entry buttons on the Time-Log file. Each row: date + start + end + hours (auto-computed) + description. Auto-sums `total_hours` + propagates to invoice atlas's `hours` + `amount` (rate × hours; rate configurable).
> - **InvoiceSubmitToggle** — single button on Invoice atlas: "Mark Submitted" sets `submitted_date` to today; "Unmark" clears it.
>
> **Why:** highest-utility UX work post-v0.16.0. v0.16.0 surfaced the foundational pain (NBSP gotcha + YAML inline-flow parser variance + general manual-frontmatter brittleness). Widget UIs are the canonical fix; user explicitly flagged.
>
> **Estimated scope:** 3-4 widget classes + per-entity helpers + frontmatter-mutation helper (shared `FinanceFrontmatter.update(file, mutator)` ?). 2-3 stages with reserved 0.17.x headroom. Workshop bump 0.16.0 → 0.17.0 MINOR.
>
> **Open questions for brainstorming:**
> - Single shared `FinanceFrontmatter` mutation helper, or per-widget mutation logic?
> - Edit-in-place modal/popover vs inline form? (Modal mirrors NewXButton overlay shape; inline form is more discoverable but more state to manage)
> - How does the user-feedback "main buttons within the finance hub looks, just mediocre" tie in? Bundle hub button styling polish into v0.17.0, or separate visual cycle?
> - Integrated nav for Budget + Paycheck context (mirroring InvoiceNavButtons) — bundle here, or separate small cycle?

> [!example]- v0.16.x.y new-meeting-button parity **(RUNNER-UP)**
> **What:** harmonize `new-meeting-button.js` with `new-project-button.js` AND `new-trip-button.js` AND now `new-budget/paycheck/invoice-button.js` (new in v0.16.0 — quad of overlay-dialog precedents). Small contained refactor.
>
> **Why:** small, contained refactor; "back to clean cycle" candidate after v0.16.0's CF-heavy shape (matches v0.13.0/v0.14.0 single-stage clean precedent if precedent + zero-novel-mechanism applies).
>
> **Estimated scope:** 0.5–1 stage. Workshop bump 0.16.0 → 0.16.1 PATCH (or 0.17.0 MINOR if user-visible behavior changes).

> [!example]- v0.16.x docs polish + T4.0 cleanup
> **What:** fix the 7-cycles-old run-renderer.js no-selector fatal + lift v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 lessons into `Docs/how.md` + `Docs/landmines.md`. Six cycles of institutional memory captured only in result writeups.
>
> **Why:** restores `run-renderer.js` to clean exit; codifies lessons for future cycles.
>
> **Estimated scope:** 1 stage. Workshop bump 0.16.0 → 0.16.1 PATCH (or no bump — harness + docs only).

> [!example]- v0.16.x.y meeting cards on daily — verification candidate
> **What:** verify whether `SpaceDailyDashboard`'s meetings panel already renders meetings as BeaconCards (v0.12.0 should have migrated). Quick read-and-confirm; close out candidate without a cycle if already satisfied.

> [!example]- v0.16.x.y styling blueprint (deferred per user)
> **What:** ship a `styling` blueprint bundling theme + CSS-snippet + Style Settings + Baseline community theme. USER EXPLICITLY DEFERRED in v0.6.0 cycle; not yet reversed.

---

## Recommendation

> [!success] **v0.17.0 Finance widget UIs** — RECOMMENDED PRIMARY
> User explicitly flagged "no user should ever manually edit a frontmatter" + "consistent utilization of nav buttons for each section" + "main buttons within the finance hub looks, just mediocre" during v0.16.0 smoke time. v0.16.0 shipped the data foundation; v0.17.0 ships the UX layer. Likely 3-5 inline CFs given first-of-its-kind widget pattern (no prior beacon precedent for in-place frontmatter mutation widgets).
>
> **Runner-up: v0.16.x.y new-meeting-button parity** — small contained refactor; "back to clean cycle" candidate.
>
> **Skip for now:** docs polish + T4.0 (defer until docs-cleanup priority signaled); meeting cards on daily (verification only); styling (deferred by user).

---

## Cadence guidance (mirror v0.6.0 / v0.5.0 / v0.11.0 / v0.12.0 / v0.16.0 for entity-ecosystem cycles)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget-authoring + frontmatter-mutation helper tasks; master-driven on manifest + version-bump mechanics.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper (e.g., `FinanceFrontmatter.update`), QUOTE the literal call shape in the design doc.
> - **Apply v0.16.0 lessons proactively** — write any new entity creation helper to (a) quote ALL date frontmatter values on write; (b) embed `customJS.FinanceStatus.renderBadge` (or analogue) on entity body; (c) suppress duplicate nav-buttons in embeds via `dv.container.closest(".markdown-embed")`.
> - **TDD discipline on harness extensions** — BC7 success-tone precedent (v0.16.0 T1.1) + BC1-BC6 (v0.12.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **Reserved-headroom-USED pattern (v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 — FIVE-data-point precedent):** for entity-ecosystem + first-of-its-kind UI cycles, expect 3-6 inline CFs. Plan headroom upfront.
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0):** for cycles with strong precedent + zero novel mechanism surface, single-stage is the right shape.
> - **Decision rule:** entity-ecosystem cycle (new blueprint with substantial CustomJS surface) → expect CFs; pure-mechanism patch OR near-clone-of-existing → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9) — when reusing in-cycle staged work at version X, in-cycle revisions must bump X+1. v0.16.0 was FOUR in-cycle bumps (finance 0.1.0 → 0.1.4) — pattern strongly established.
> - **Verify command ids against runtime** (gotcha 10) — when an `invoke_command` action ships, verify the actual command id via `Object.keys(app.commands.commands).filter(...)` in DevTools.
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2 codified as numbered smoke step.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders (e.g., `<YYYY-MM>/`, `<YYYY-MM-DD>/`, `<slug>/board/<task>/`) live within the invariant — they're per-entity instances, not new sub-modules.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.
> - **v0.16.0 NEW lessons (apply proactively):**
>   - **YAML date auto-parsing** — quote dates on write; tolerant parse on read.
>   - **Frontmatter parser variance for booleans** — centralize coercion (boolean OR string-"true").
>   - **NBSP gotcha** — manual frontmatter editing introduces invisible-character traps; widget-driven mutation is the canonical fix.
>   - **renderBadge() pattern** — status pills on entity NOTE pages via dataviewjs block embedded by NewXButton creation helper.
>   - **Embed dedup via `dv.container.closest(".markdown-embed")`** — suppress duplicated nav-buttons when embedded.
>   - **Per-entity sub-folder symmetry** — when one sub-area uses an entity-ecosystem layout, all sub-areas mirror it from the start.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first (especially the T4.0 fatal — known, carried 7 cycles now).
> 3. Read v0.16.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation; lock the choice before opening brainstorming. Reference `blueprints i want.md` annotations: finance DONE at v0.16.0 (FOUNDATION); v0.17.0 candidate is widgets ON TOP of finance.
> 5. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions. **Apply v0.16.0 lessons** in design phase: quote API contracts, plan for YAML auto-parse, plan for boolean parser variance, embed renderBadge pattern, plan embed dedup if any new template-embedded note shape lands.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at S2/S3 close (Cmd+R as numbered step). Plan reserved 0.Y.x headroom for inline CFs IF first-of-its-kind.
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.16.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.16.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with recommendation (Finance widget UIs primary; new-meeting-button parity runner-up), constraints carried forward, and v0.16.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Note T4.0 carried 7 cycles; use selectors.
3. Read v0.16.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with recommendation. Reference blueprints i want.md annotations: finance foundation DONE at v0.16.0; v0.17.0 candidate is widget-driven UIs ON TOP of finance (no manual frontmatter editing).
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.16.0 lessons proactively (date auto-parsing, parser variance, renderBadge, embed dedup, per-entity sub-folder symmetry).
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on widget classes + frontmatter-mutation helper; master-driven on manifest + version-bump mechanics; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind UI cycle (v0.5.0/v0.11.0/v0.12.0/v0.6.0/v0.16.0 FIVE-data-point precedent for 2-6 inline CFs).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 3-5 inline CFs + template-changing migration friction + per-entity sub-folder pattern within landmine #11 + Templater auto-promote reusable pattern); v0.16.0 NEW lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
