---
date: 2026-05-06
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.22.0 cycle. Triggered by v0.22.0 inside-vault bootstrap close + tag v0.22.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-06-v0.22.0-inside-vault-bootstrap-result.md (v0.22.0 close — read first)
  - Docs/plans/2026-05-06-v0.22.0-inside-vault-bootstrap-plan.md
  - Docs/plans/2026-05-06-v0.22.0-inside-vault-bootstrap-design.md
  - Docs/plans/2026-05-06-v0.21.1-consumer-convenience-result.md
  - Docs/prompts/2026-05-06-post-v0.21.1-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; multi-theme entry from 2026-05-06 still open)
gates:
  - v0.22.0 closed; tag v0.22.0 pushed; workshop manifest at 0.22.0
  - install.sh shipped + curl one-liner usable
  - beacon CLI shipped (4 verbs: bootstrap/update/status/wizard)
  - Beacon/ inside-vault layout default for fresh consumer vaults
  - Landmine #18 codified
  - 2 new harnesses (run-cli.js + run-install-sh.js); run-bootstrap 36 → 45
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.22.x or v0.23.0 (cycle-dependent)
---

# Onboarding — post-v0.22.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + smoke target (NOT git-tracked; sibling-of-workshop layout)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level; sibling-of-workshop layout)
>   - `../scratch/1/` — fresh-vault Phase C smoke target (created 2026-05-06; sibling-of-workshop layout)
>   - `../scratch/2/` — **NEW v0.22.0 inside-vault layout smoke target** (curl-bootstrap + Beacon/ inside the vault)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults (sources for pattern ports). NOTE: from beacon's pwd these are `../../{name}/`, NOT `../{name}/`.
> - **Distribution model (post-v0.22.0):** TWO LAYOUTS COEXIST — (1) sibling-of-workshop (legacy POC vaults; `workshop_relative_path: "../beacon"`); (2) inside-vault `Beacon/` (v0.22.0+ default for fresh curl-bootstrapped vaults; `workshop_relative_path: "Beacon"`). Stub md5 invariant `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.22.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.21.0` `v0.21.1` **`v0.22.0`** (NEW)
- Harness: `run-bootstrap.js` 45 sub-asserts (was 36; +9 from BS10-BS13); **`run-cli.js` 13 sub-asserts (NEW; first CLI-dispatcher harness; 10 cases C1-C10)**; **`run-install-sh.js` 10 sub-asserts (NEW; first bash-driver harness; 6 cases I1-I6)**; `run-helper-cases.js` 387 sub-asserts UNCHANGED; `run-renderer.js` 23 cases UNCHANGED.
- Allowlist (landmine #12): **11 paths** UNCHANGED in v0.22.0 (CLI doesn't touch `.obsidian/`).
- Landmines list: **18 entries** (v0.22.0 added #18 — inside-vault `Beacon/` is git-managed; mirrors #15).
- Helper count: 9 allowlist-touching UNCHANGED.
- **NEW v0.22.0 surface artifacts:** `install.sh` (repo root, chmod 0755) + `platform/cli/{beacon-cli, cmd-*}.js` + `platform/visual/*.js` + `Beacon/Scripts/{activate.sh, beacon}` (install-time generated per consumer).

**v0.22.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.22.0): post-v0.22.0 next-cycle handoff (THIS commit upcoming)
- `b8ed25d` docs(v0.22.0): cycle close — workshop_version 0.21.1 -> 0.22.0 MINOR
- `e4a67d2` docs(v0.22.0): S4 — install.md + landmine #18 + CLAUDE/how/use updates
- `f56c7fc` fix(install): v0.22.0 S3 CF — install.sh defensive hardening
- `96c4a91` feat(install): v0.22.0 S3 — install.sh remote bootstrap orchestrator
- `7d1a3b4` feat(cli): v0.22.0 S2 — beacon CLI surface + bootstrap.js phase refactor
- `f85d0ac` test(v0.22.0): S1 fixes — withTempVault type-guard + per-case try/catch + harness hygiene
- `9a55e1e` test(v0.22.0): S1 TDD-first — failing harness baseline
- `5247e39` docs(v0.22.0): inside-vault bootstrap implementation plan
- `9dce392` docs(v0.22.0): inside-vault bootstrap + lean CLI design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.22.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -12` — expect 10 v0.22.0 cycle commits.
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `.obsidian/` install-drift `M` markers).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep -E 'v0\.22'` → `v0.22.0` annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.22.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.22.0"`.
> 7. **install.sh is executable:** `ls -la install.sh | awk '{print $1}'` → starts with `-rwx`.
> 8. **CLI dispatcher exists:** `test -f platform/cli/beacon-cli.js && echo OK`.
> 9. **npm deps installed:** `test -d node_modules/@inquirer/prompts && echo OK` → OK.
> 10. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 11. **All 5 harnesses:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 13 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 10 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0; T4.0 + 7 selectors all PASS.
> 12. **Inside-vault smoke target persists:** `ls /Users/willfell/scratch/2/Beacon/Scripts/activate.sh` should exist (from v0.22.0 S5 manual smoke). Optional: `cd /Users/willfell/scratch/2 && source Beacon/Scripts/activate.sh && beacon status` to verify the CLI still resolves.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-06-v0.22.0-inside-vault-bootstrap-result.md`** — most recent cycle close. 9 NEW v0.22.0 lessons + reserved-headroom-USED 8th data point + 7 carry-forwards.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.22.0 close.
> 4. **`blueprints i want.md`** — user's wishlist. Multi-theme entry still open.
> 5. **`Docs/landmines.md`** — 18 entries; #18 NEW in v0.22.0 (inside-vault Beacon/ is git-managed).
> 6. **`Docs/install.md`** — NEW user-facing install reference; sync-exclusion guides; CLI verb reference.
> 7. **`Docs/how.md`** — now includes "CLI surface (v0.22.0+)" section.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.22.x `--non-interactive` flag propagation + CI/headless install (PRIMARY recommendation if quick win desired)
> **What:** Propagate `install.sh --non-interactive` through to the node CLI; teach `cmd-bootstrap.js` + `runFirstRunWizard` to honor it by auto-accepting all defaults. Enables `curl ... | bash -s -- --non-interactive --overwrite` to run end-to-end without any prompts. Required for CI / headless installs / scripted provisioning. Ships also: `install.sh --vault=PATH` equals form (IMP-5 from v0.22.0 S3 quality review).
>
> **Why:** Closes v0.22.0 NEW lesson (e). Small, well-scoped patch cycle. Highest user-leverage carry-forward. ~1-2 stages, expected clean (pure flag-propagation; precedent: 7-data-point UNUSED-headroom).
>
> **Estimated scope:** PATCH bump 0.22.0 → 0.22.1. Touches install.sh + beacon-cli.js dispatcher + cmd-bootstrap.js + bootstrap-lib/wizard.js (add nonInteractive default-acceptance branch). Harness extension: maybe 1-2 new run-install-sh cases for the propagation + 1-2 new run-cli cases for the wizard's nonInteractive defaults branch.

> [!example]- v0.22.x fresh-clone dirty-tree fix (good companion to non-interactive)
> **What:** Investigate why `beacon status` reports `dirty` on a freshly-bootstrapped Beacon/. Likely `npm install --omit=dev` reconciles `package-lock.json` against the just-cloned tree. Two paths: (a) make `package-lock.json` deterministic across npm versions; (b) add post-install untracked patterns to a workshop-side `.gitignore` so they don't show as dirty.
>
> **Why:** Closes v0.22.0 NEW lesson (f). Small. Bundle with the non-interactive cycle for a single PATCH that closes the two highest-leverage v0.22.0 carries.

> [!example]- v0.23.0 multi-theme presets (carried from v0.21.1 handoff)
> **What:** Schema upgrade from `style_settings_defaults_src` (single string) to `style_presets[]` keyed by name. Each preset entry: `{ name, theme_src, style_settings_src, appearance_overrides }`. Consumer subscription declares `style_preset: "baseline"` (default) or `"headspace"` / `"ero"` / `"<fourth>"`. Workshop ships 4 presets. Wizard adds preset-select prompt during first-run.
>
> **Why:** User-requested 2026-05-06 in `blueprints i want.md`. Foundational; substantial schema upgrade. Was the post-v0.21.1 PRIMARY recommendation; user pivoted to bootstrap orchestration in v0.22.0; now back on the table.
>
> **Estimated scope:** 3-4 stages. Workshop bump 0.22.x → 0.23.0 MINOR. New schema field at workshop manifest level; styling helpers extended to dispatch on preset; wizard adds preset prompt. **First-of-its-kind UI surface; expect 2-4 in-cycle CFs** at preset-select integration boundaries.

> [!example]- v0.23.0 architectural fix — install.js doesn't write installed_at when helpers no-op
> **What:** Refactor install.js so helpers can signal NO_OP_DUE_TO_MISSING_PREREQ back to the orchestrator + the orchestrator skips writing `installed_at`. Eliminates the v0.21.0-style trap where a stuck-in-installed-state mechanism needs a manual landmine #16 PATCH bump to recover. Also closes the v0.21.1 carry — `applyCommunityPluginData`'s broadened prereq gate produces no-op installs that nonetheless mark the mechanism as "installed."
>
> **Why:** Surfaced repeatedly (v0.21.0 styling 0.1.1 → 0.1.2 unstick; v0.21.1 broadened prereq gate). Architectural; would simplify recovery for any future helper that gates on a prereq. Substantial install.js change. ~2-3 stages.

> [!example]- v0.23.0 project Sub-tasks panel on task hub
> **What:** Carry from post-v0.18.2. Surface sub-tasks (folder-promoted Task Board Cards under `<task>/board/`) on the task hub note via BeaconCards. New CustomJS class `ProjectSubTasks`.
>
> **Why:** Still relevant; first-of-its-kind UI surface; expect 1-3 inline CFs.

> [!example]- v0.22.x finance polish carries
> **What:** (a) Migration helper for pre-v0.17.0 finance entities; (b) Budget categories aggregate row; (c) PaycheckExpensesEditor "All Paid" toggle.
>
> **Why:** Carry from v0.17.0/v0.18.0/v0.18.1/v0.18.2 handoffs. Bundle-friendly with another cycle; small carries.

> [!example]- v0.22.x cross-doc line-citation drift lint
> **What:** Lint `Docs/*.md` for `[a-z\.]+\.js:\d+` patterns and assert each line still exists at that line in the cited file. Catches landmine #10 / #16-style drift before it ships.
>
> **Why:** Surfaced as v0.20.0 NEW lesson (c). Small additive harness extension; bundle-friendly with any docs-touching cycle.

---

## Recommendation

> [!success] **v0.22.x `--non-interactive` propagation + dirty-tree fix** — PRIMARY (closes 2 highest-leverage v0.22.0 carries; small clean cycle)
>
> **Why:** Both v0.22.0 NEW lessons (e) + (f) are real user-facing rough edges that emerged in the manual smoke. Both are small, well-scoped, and bundleable into a single PATCH cycle. Multi-theme presets (v0.23.0) is the bigger first-of-its-kind cycle; queuing it AFTER closing the v0.22.0 carries keeps the cadence tight.
>
> **Skip for now:** v0.23.0 multi-theme presets (queue after v0.22.x); architectural install.js refactor (real but invasive — bundle alongside another cycle that touches install.js); project Sub-tasks panel (UI cycle); finance polish (smaller carry); cross-doc lint (small additive).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget/helper/preset-asset authoring; master-driven on manifest mechanics + version bumps + harness extensions + landmine doc edits + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or schema, QUOTE the literal call/manifest shape in the design doc.
> - **Two-stage subagent review (v0.20.0 lesson d)** — spec compliance first, code quality second.
> - **Apply v0.22.0 lessons proactively:**
>   - (a) per-shell source-activation conventions match Python venv / nvm / pyenv; never auto-edit shell rc files
>   - (b) two coexisting layouts (sibling-of-workshop + inside-vault) — both must keep working
>   - (c) `bootstrapCtxFromArgs` pattern reusable for any future verb that creates state
>   - (d) bash-driver harness shim posture (PATH-prefixed shims; `${process.execPath}` for node passthrough; macOS `/bin/bash` PATH inclusion)
>   - (e) bash-side flags don't auto-propagate to node CLI — pass through explicitly
>   - (h) plan-quoted bug → fix helper-side, preserve plan-of-record
>   - (i) `set -euo pipefail` + trap + INSTALL_OK gate-flag for half-state cleanup
> - **TDD discipline on harness extensions** — BS/VT/AP/SS (v0.19.0) + BB1-BB6 (v0.18.0) + FF1-FF8 (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0) + HK1-HK5 + CP1-CP5 (v0.21.1) + BS9 (v0.21.0) + **BS10-BS13 + C1-C10 + I1-I6 (v0.22.0)**: write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate (when CustomJS classes change OR plugin-data writes happen).
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9 / landmine #16).
> - **Reserved-headroom precedent now 9 data points: 7 UNUSED + v0.21.0 USED 5 CFs + v0.22.0 USED 1 CF.** Decision rule: pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix / pure-additive helper-pair / non-interactive-flag-propagation → UNUSED. New mechanism / new UI surface / first-of-its-kind orchestration → expect 1-6 CFs.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **In-cycle re-process bump rule (LANDMINE #16)** — when reusing in-cycle staged work at version X, in-cycle revisions MUST bump X+1.
> - **Verify command ids against runtime** (gotcha 10).
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2.
> - **API-contract quoting (v0.6.0 lesson)** — existing mechanism API contracts must be QUOTED literally in design docs.
> - **Per-blueprint version bumps require LOCKSTEP edits** (v0.12.0 gotcha 11; for mechanisms 4-file lockstep per v0.19.0 CF-1 + landmine #16).
> - **First-wins protects consumer trust** (v0.21.1 lesson c) — never modify a binding the user has already set.
> - **Helpers-that-materialize-new-state need explicit prereq gates (v0.19.0 CF-1; v0.21.1 broadened gate)** — pick the semantic that fits the helper's failure mode.
> - **Two backup-suffix conventions (v0.19.0 lesson e)** — `.bak` for file-content overwrite (vendored themes); `.beacon-backup` for plugin-data additive merge.
> - **Vendored theme is mechanism-owned (landmine #15)** — never hand-edit `.obsidian/themes/<Name>/`.
> - **Test isolation via inline synthetic bodies (v0.20.0 lesson a)** — when a harness test reads a blueprint asset for fixture content, prefer an inline synthetic body over `fs.readFileSync` of a real path. **Bash-driver harnesses extend this to PATH-prefixed shim scripts (v0.22.0 lesson d).**
> - **Cross-doc line-citation drift is a real maintenance class (v0.20.0 lesson c)** — any cycle that touches a file cited as `<filename>:<lineno>` in any doc should grep all docs for that pattern.
> - **Bootstrap network posture (LANDMINE #17, v0.21.0)** — bootstrap.js is the platform's only network gateway; install.js stays filesystem-only. **v0.22.0 EXTENDS this:** install.sh is the second network gateway (curl + git clone + npm install); CLI verbs (`beacon update`) are the third (git fetch).
> - **Canonical helper posture stack (v0.21.0 lesson d)** — atomic write + backup-on-overwrite + path-traversal validator.
> - **Defer-then-validate-then-load wizard architecture (v0.21.0 lesson e)** — when a config field gates downstream prompts/loads, gate the load behind the field's prompt+validator.
> - **Defensive augment-on-every-run for new schema fields (v0.21.0 lesson f)** — additive-only augment; never overwrite user-supplied values.
> - **Parallel-subagent dispatch (v0.21.1 lesson a)** — works cleanly when insertion points are 800+ lines apart and prompts include literal "don't touch any other line" gating.
> - **Inside-vault `Beacon/` is git-managed (LANDMINE #18, v0.22.0)** — never hand-edit; recovery via `beacon update --force`. Mirrors #15 vendored-theme posture.
> - **`bootstrapCtxFromArgs` pattern (v0.22.0 lesson c)** — bootstrap is the one verb that creates state; dispatcher must tolerate config absence for THIS verb only.
> - **Per-shell source-activation (v0.22.0 lesson a)** — never auto-edit shell rc files; per-shell `source` is the canonical posture (matches Python venv / nvm / pyenv).
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders live within the invariant.
> - `.obsidian/` allowlist at **11 paths** (#12 v0.21.1 expansion) — adding a 12th+ requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8 v0.21.0 amendment: install.js is filesystem-only; bootstrap.js + install.sh + CLI verbs are the network gateways).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory (this cycle codified `Beacon/` as second sanctioned), adding 12th+ `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.

---

## End-of-session protocol

> [!success] Before this session ends, the agent MUST follow `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (if workshop_version bumped) — REQUIRES user approval.
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 12 pre-flight checks. Surface discrepancies first.
> 3. Read v0.22.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + install.md + how.md (~10-15 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + recommendation (v0.22.x `--non-interactive` + dirty-tree fix as primary).
> 5. Invoke `de:brainstorming` to scope the locked cycle.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at stage close (Cmd+R as numbered step IF CustomJS classes change OR plugin-data writes happen).
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.22.0 next cycle. Read Docs/prompts/2026-05-06-post-v0.22.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 12 pre-flight checks, candidate cycles with primary recommendation (v0.22.x --non-interactive flag propagation + fresh-clone dirty-tree fix; small bundleable PATCH cycle closing the 2 highest-leverage v0.22.0 carries), constraints carried forward, and v0.22.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 12 pre-flight checks. Bootstrap harness has 45 sub-asserts (was 36 pre-v0.22.0; +9 from BS10-BS13). NEW run-cli.js harness has 13 sub-asserts (10 cases C1-C10). NEW run-install-sh.js harness has 10 sub-asserts (6 cases I1-I6). Helper-cases harness has 387 sub-asserts UNCHANGED. Renderer harness 23 cases UNCHANGED. install.sh exists at repo root; platform/cli/* + platform/visual/* shipped.
3. Read v0.22.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md (multi-theme entry still open) + landmines (18 entries; #18 NEW v0.22.0 inside-vault Beacon/ git-managed) + install.md (NEW v0.22.0 user-facing reference) + how.md (CLI surface section NEW v0.22.0).
4. Surface candidates via AskUserQuestion with primary recommendation. v0.22.x non-interactive + dirty-tree = primary; v0.23.0 multi-theme = secondary; v0.23.0 architectural install.js refactor = tertiary; v0.23.0 project Sub-tasks panel = quaternary; finance polish carries / cross-doc lint = quinary.
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.22.0 lessons proactively.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget/preset-asset classes; master-driven on manifest + version-bump mechanics + docs edits + harness extensions + cycle close; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (precedent: UI 1-6 CFs; orchestration 4-6 CFs); 9-data-point reserved-headroom precedent (7 UNUSED + v0.21.0 5 USED + v0.22.0 1 USED).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 12th+ path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits (v0.12.0 gotcha 11; for mechanisms extend to 4 files per v0.19.0 CF-1; codified as landmine #16 in v0.20.0); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); outline-accent action buttons go through customJS.BeaconButton.render (v0.18.0); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw inline CFs + template-changing migration friction + per-entity sub-folder pattern); v0.16.0 lessons (YAML date auto-parsing; frontmatter parser variance; NBSP gotcha; renderBadge pattern; embed dedup); v0.17.0 lessons (dv-shim composition; badges[].style outline; outline-accent button pattern); v0.18.0/v0.18.1/v0.18.2 lessons (in-cycle re-process bump rule [LANDMINE #16]; pure adoption clean first-pass via API-contract quoting; trailing-whitespace template defect class [LINT SHIPPED v0.20.0]; belt-and-suspenders predicate stacking); v0.19.0 lessons (helpers materialize new state need stronger prereq contracts; shared _externalPluginsSatisfied gate; per-helper local require; two backup-suffix conventions; parallel subagents; vendored theme mechanism-owned [LANDMINE #15]); v0.20.0 NEW lessons (test isolation via inline synthetic bodies; helper-cases as generic lint host; cross-doc line-citation drift maintenance class; two-stage subagent review yields different signal at low cost; master-driven S3 is the right shape); v0.21.0 NEW lessons (first-of-its-kind orchestration cycles draw 4-6 in-cycle CFs at integration boundaries; process.exit pipe truncation [use process.exitCode]; async spawn over spawnSync; canonical helper posture stack [atomic write + backup-on-overwrite + path-traversal validator]; defer-then-validate-then-load wizard architecture; defensive augment-on-every-run for new schema fields); v0.21.1 NEW lessons (parallel-subagent dispatch with literal-contract prompts works cleanly when insertion points are 800+ lines apart; broadened prereq gate is a defensible per-helper choice; first-wins protects consumer trust; Cmd+= is the literal physical key for "Cmd++" on US keyboards); v0.22.0 NEW lessons (per-shell source-activation matches Python venv / nvm / pyenv conventions; inside-vault Beacon/ coexists with lowercase beacon/<module>/; bootstrapCtxFromArgs is right shape for state-creating verb; bash-driver harnesses use PATH-prefixed shim scripts; --non-interactive flag is bash-side-only [v0.22.x candidate]; fresh-clone dirty-tree [v0.22.x candidate]; banner version reflects live manifest at clone time; TDD baseline plan-quoted bugs fix helper-side preserve plan-of-record; set -euo pipefail + trap + INSTALL_OK gate-flag canonical for half-state cleanup).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
