---
date: 2026-05-06
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.23.0 cycle. Triggered by v0.23.0 close + tag v0.23.0 on origin/main + GitHub repo rename willfell/beacon → willfell/sauce.
predecessors:
  - Docs/plans/2026-05-06-v0.23.0-sauce-rebrand-result.md (most recent close — read first)
  - Docs/plans/2026-05-06-v0.23.0-sauce-rebrand-plan.md
  - Docs/plans/2026-05-06-v0.23.0-sauce-rebrand-design.md
  - Docs/prompts/2026-05-06-post-v0.22.1-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol)
gates:
  - v0.23.0 closed; tag v0.23.0 pushed; workshop manifest at 0.23.0
  - Workshop clone Beacon/ → pantry/ shipped
  - CLI binary beacon → sauce shipped
  - Brand "Beacon" → "Sauce" rebranded across docs + CLAUDE.md + ASCII banner + runtime banner
  - Env vars BEACON_* → SAUCE_*
  - GitHub repo renamed willfell/beacon → willfell/sauce (auto-redirect preserved)
  - macOS APFS Daily Notes acceptance gate PASSED
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.24.0
---

# Onboarding — post-v0.23.0 next-cycle selection

> [!info] Project identity (post-v0.23.0)
> - **Project name:** `sauce` (rebranded from `beacon` in v0.23.0)
> - **GitHub remote:** `git@github-personal:willfell/sauce.git` (HTTPS: `https://github.com/willfell/sauce`)
> - **Local workshop dev path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (host filesystem path; not yet renamed — could rename to `/workshop/sauce` as a v0.23.x housekeeping cycle)
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target (legacy sibling-of-workshop layout; `../beacon` workshop_relative_path)
>   - `../accuris-beacon-poc/` — secondary consumer (legacy layout; pre-existing version-pin drift on nav-buttons + project)
>   - `../scratch/2/`, `../scratch/3/`, `../scratch/4/` — historical v0.22.x smoke targets (preserved as-is; defunct after v0.23.0 rename)
>   - `../scratch/5/` — v0.23.0 smoke target (post-rename: `pantry/` workshop clone + `beacon/` modules + `Docs/Meta/` runtime; ALL 8 blueprints + 8 mechanisms installed; clean idempotent)
>   - `/Users/willfell/Documents/obsidian/sync/{accuris,headspace,ero}/` — production reference vaults

---

## Workshop state at session start

- `workshop_version: 0.23.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, `beacon-button@0.1.0`, `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.22.0` `v0.22.1` `v0.23.0` (NEW)
- Harness counts: bootstrap 45 / cli 20 / install-sh 14 / helper-cases 387 / renderer 23 — all green
- Allowlist (landmine #12): **11 paths** UNCHANGED in v0.23.0
- Landmines: **18 entries** UNCHANGED (no new landmine; #11 + #18 wording amended)
- Helper count: 9 UNCHANGED

**v0.23.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.23.0): post-v0.23.0 next-cycle handoff (THIS commit)
- `<RESULT>` docs(v0.23.0): result writeup
- `8f76296` v0.23.0 (annotated tag — post cycle close)
- `497be99` docs(v0.23.0): cycle close — workshop_version 0.22.1 → 0.23.0 MINOR (Sauce rebrand)
- `8b5cec0` feat(v0.23.0): S2 — Sauce rebrand + Beacon→pantry + beacon→sauce CLI
- `4c86b36` test(v0.23.0): S1 TDD baseline — failing harness for sauce/pantry/sauce-cli
- `6d822a0` docs(v0.23.0): sauce rebrand implementation plan
- `095eeb2` docs(v0.23.0): sauce rebrand + workshop-clone rename design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git remote -v` → `origin git@github-personal:willfell/sauce.git`
> 3. `git log --oneline -10` — expect ~8 v0.23.0 cycle commits + handoff at top.
> 4. `git status` → only pre-existing drift (`.obsidian/*` + `Docs/Meta/Scripts/cards/beacon-cards.js` + `*.bak` files).
> 5. `git ls-remote --tags origin | grep v0.23.0` → 2 lines (annotated tag + dereference).
> 6. **Stub md5 invariant** (4 paths must match): `md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js` → all `a39257da1dd49ae4481e5cd0a42bdac4`.
> 7. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.23.0"`.
> 8. `ls platform/cli/sauce-cli.js` → exists. `ls platform/cli/beacon-cli.js` → does NOT exist (renamed).
> 9. `grep -n 'BEACON_VAULT\|BEACON_REPO_URL' platform/cli/sauce-cli.js install.sh platform/bootstrap.js` → empty (no BEACON_* env vars left in active code).
> 10. Workshop self-install green + idempotent: `node platform/test/run-install.js .` → exit 0.
> 11. **All 5 harnesses:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 20 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0.
> 12. **Verify scratch/5 still works** (optional but recommended): `cd /Users/willfell/scratch/5 && source pantry/Scripts/activate.sh && sauce status` → "Drift: none".

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-06-v0.23.0-sauce-rebrand-result.md`** — most recent cycle close. NEW lessons (a-h); reserved-headroom precedent extends to 10 data points.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.23.0 close.
> 4. **`Docs/install.md`** — read the `[!success]` callout (v0.23.0 resolved); upgrade recipe section.
> 5. **`Docs/landmines.md`** — 18 entries; #11 + #18 amended in v0.23.0; #18 references `pantry/` + `Beacon/` historical alias.
> 6. **`blueprints i want.md`** — pending feature carries (multi-theme deferred again).

---

## 🔴 v0.24.0 PRIMARY: Tree 3 rename + `beacon-button` mechanism rename

> [!warning] Sequenced as v0.24.0 per multi-cycle naming destination locked at v0.23.0 brainstorm.

**What:** Rename consumer-side runtime plumbing dir from `Docs/Meta/` → `ranch/`. Bundle `beacon-button` mechanism rename (final name TBD — candidates: `accent-button`, `sauce-button`, sauce-themed). Touch every consumer config + every blueprint manifest's path-variable references.

**Why:** Continues the multi-cycle Sauce rebrand. v0.23.0 nailed Tree 1 (workshop clone); v0.24.0 nails Tree 3 (runtime plumbing). v0.25.0 will nail Tree 2 (modules namespace).

### v0.24.0 cycle scope

**Files affected (rough estimate; finalize in design):**
- Every consumer's `Docs/Meta/platform-config.json` → `ranch/platform-config.json` (or rename `Docs/Meta` dir wholesale + update path variables)
- Every blueprint manifest's `templates_path` / `scripts_path` / `views_path` variable references
- `platform/install.js` helper paths (templates, scripts, views materialization destinations)
- The thin stub `Docs/Meta/Templater/platformInstall.js` location — moves to `ranch/Templater/platformInstall.js`. Stub body content-static (md5 invariant `a39257da1dd49ae4481e5cd0a42bdac4` — only the FILE LOCATION changes).
- All 5 harnesses: path expectations
- Documentation: `Docs/install.md`, `Docs/use.md`, `Docs/how.md`, `Docs/landmines.md`, `CLAUDE.md`
- `beacon-button` mechanism source dir at `platform/mechanisms/beacon-button/` → renamed dir + class file rename (`BeaconButton` class → `AccentButton` or sauce-themed)
- All call-sites of `customJS.BeaconButton.render(...)` → `customJS.AccentButton.render(...)` (or whatever the chosen name is)

**Open design questions for v0.24.0 brainstorm:**
1. Final `beacon-button` rename name (candidates: `accent-button`, `dollop-button`, `sauce-button`, ...)?
2. Migration story: same as v0.23.0 (no migration tooling; manual `mv Docs/Meta ranch` + sed on consumer configs)?
3. Stub location migration: does `Docs/Meta/Templater/platformInstall.js` move to `ranch/Templater/platformInstall.js` mid-cycle? How does `install.js` resolve the new path? (Stub at known location; install.js reads `workshop_relative_path` from `<vault>/<runtime>/platform-config.json` — chicken-and-egg without intermediate state.)
4. Cycle shape: 3 stages (TDD harness → atomic mass rename → smoke + close)? Or 4 (split runtime rename from mechanism rename)?

**Cycle shape estimate:** 3-4 stages. MINOR bump (BREAKING for any v0.23.x consumer; one-time `mv` migration documented). Reserved-headroom: expect 2-4 CFs (wide-touching mechanical, similar shape to v0.23.0).

---

## Other candidate cycles (queued AFTER v0.24.0)

> [!example]- v0.25.0 Tree 2 module namespace rename
> `beacon/<module>/` → `spice/<module>/`. The most call-site-heavy of the rebrand sequence. Every blueprint manifest's `module_directory` field (8 blueprints), every Templater `folder_prefix` value, every wikilink across templates, every CustomJS path-check (`pathParts[tripsIdx-1] !== "beacon"` in trip helpers, ProjectsHubCards filter, etc.). High-risk; substantial reserved-headroom.

> [!example]- Local workshop dev path rename
> `/Users/willfell/Documents/obsidian/sync/workshop/beacon` → `/workshop/sauce`. Pure host-filesystem operation; doesn't affect repo content. Single-task housekeeping. Could bundle with v0.24.0 or stand alone.

> [!example]- Multi-theme presets (DEFERRED again)
> Carried from v0.21.1 → v0.22.0 → v0.22.1 → v0.23.0. v0.26.0+ candidate.

> [!example]- Architectural install.js refactor
> Helpers signal NO_OP_DUE_TO_MISSING_PREREQ to orchestrator. Eliminates landmine #16 unstick PATCH bumps. Substantial install.js change.

> [!example]- Project Sub-tasks panel on task hub
> Carry from post-v0.18.2. UI cycle.

> [!example]- Finance polish carries
> Migration helper for pre-v0.17.0 entities; budget categories aggregate row; PaycheckExpensesEditor "All Paid" toggle.

> [!example]- Dogfood drift housekeeping (NEW v0.23.0 carry)
> `Docs/Meta/Scripts/cards/beacon-cards.js` cards@0.2.2 → 0.2.3 outline-pill content drift in tree at v0.23.0 session start; deferred. Small `chore(workshop): resync materialized cards mechanism` commit.

> [!example]- `BEACON_*` shell-rc honor-both shim (NEW v0.23.0 carry)
> If users complain about lost shell-rc exports, dispatcher could honor both `SAUCE_VAULT || BEACON_VAULT`. Currently install.md upgrade recipe documents the manual update; user-driven decision whether to add the shim.

---

## Recommendation

> [!success] v0.24.0 — Tree 3 runtime rename + `beacon-button` mechanism rename — PRIMARY
>
> Brainstorm should:
> 1. Lock the `beacon-button` mechanism's new name via `AskUserQuestion` (candidates: `accent-button`, `dollop-button`, `sauce-button`, free-text).
> 2. Lock the migration story (default: no tooling; manual `mv` + sed recipe).
> 3. Lock cycle shape (3 vs 4 stages).
> 4. Lock the stub-location migration (the chicken-and-egg between `Docs/Meta/Templater/platformInstall.js` location and `install.js`'s discovery — likely needs a transitional grace period or explicit `--from-legacy` flag).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock `beacon-button` rename name + migration story + stub-migration mechanics.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on the rename mechanics (find-and-replace across consumer configs + manifests + paths) + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any path string that changes, QUOTE the literal in the design.
> - **Two-stage subagent review** (v0.20.0 + v0.22.1 + v0.23.0 reaffirmation) — spec compliance first, code quality second. v0.23.0 caught 3 CFs pre-commit; same posture for v0.24.0.
> - **Manual smoke is the close gate.** Fresh `<vault>/scratch/6/` curl-bootstrap with v0.24.0 install.sh, opening Daily Notes successfully + verifying runtime artifacts land at `<vault>/ranch/...`.
> - Push to origin/main after each stage commit; annotated tag at cycle close (REQUIRES user approval).
> - **Reserved-headroom precedent now 10 data points (post-v0.23.0).** Decision rule: pure adoption / pure dedupe / pure label / pure docs / scoped-bug-fix / pure-additive helper-pair → reliably UNUSED EXCEPT when the two-stage subagent review surfaces a latent issue. New mechanism / new UI surface / first-of-its-kind orchestration / wide-touching mechanical-rename → expect 1-6 CFs. **v0.24.0 is wide-touching mechanical similar to v0.23.0 — expect 2-4 CFs from missed call-sites + chicken-and-egg stub-migration friction.**

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; reaffirmed v0.22.1 + v0.23.0)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **Module-directory invariant under `beacon/` (#11)** — UNCHANGED in v0.24.0; planned rename to `spice/<module>/` queues for v0.25.0 (don't bundle into v0.24.0; keep cycles bounded).
> - **Inside-vault clone is git-managed (#18)** — `pantry/` is the v0.23.0+ name; preserve.
> - **Allowlist 11 paths (#12)** — adding 12th+ requires explicit user approval. v0.24.0 may need allowlist updates if `ranch/` introduces new managed-write paths.
> - **Stub content-static (#13)** — never re-edit any `Docs/Meta/Templater/platformInstall.js` body. v0.24.0 MOVES the stub location (`Docs/Meta/Templater/` → `ranch/Templater/`); the BODY stays content-static. Stub md5 invariant `a39257da1dd49ae4481e5cd0a42bdac4` preserved post-move.
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **`.beacon-backup` suffix preserved** (v0.23.0 lesson a) — landmine #12 mechanic #2 backup-suffix is platform-version-stable; brand-rename does NOT cascade to backup-file names. Same for v0.24.0 — `.ranch-backup` would silently miss prior backups.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory (v0.24.0 codifies a NEW sanctioned top-level: `ranch/` replacing `Docs/Meta/`).

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. `git remote -v` showing `willfell/sauce`).
> 2. Read v0.23.0 result writeup (NEW lessons a-h; reserved-headroom 10 data points).
> 3. Surface v0.24.0 as PRIMARY recommendation via `AskUserQuestion`. Include `beacon-button` rename name candidates (`accent-button` / `dollop-button` / `sauce-button` / free-text).
> 4. Once name + scope are locked, brainstorm: stub-migration mechanics, harness updates needed, cycle shape, reserved-headroom estimate.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.23.0 next cycle. Read Docs/prompts/2026-05-06-post-v0.23.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity (post-Sauce-rebrand: project name "sauce", GitHub repo at github.com/willfell/sauce, CLI binary `sauce`, env vars SAUCE_*, workshop clone at <vault>/pantry/), pre-flight checks, the 🔴 PRIMARY recommendation (v0.24.0 Tree 3 rename Docs/Meta/ → ranch/ + beacon-button mechanism rename), constraints carried forward, and the v0.23.0 NEW lessons (a-h).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (host filesystem path; not yet renamed to /workshop/sauce — could bundle with v0.24.0 or stand-alone housekeeping).
2. Run pre-flight checks. Workshop_version 0.23.0; remote git@github-personal:willfell/sauce.git. Bootstrap 45/0, cli 20/0, install-sh 14/0, helper-cases 387/0, renderer green.
3. Read v0.23.0 result writeup + CLAUDE.md status snapshot + landmines (18 entries; #11 + #18 amended in v0.23.0).
4. Surface candidates via AskUserQuestion. v0.24.0 = PRIMARY (Tree 3 runtime rename + beacon-button mechanism rename). v0.25.0 (Tree 2 modules) DEFERRED behind v0.24.0. Local workshop dev path rename (host filesystem) — bundle or standalone? Multi-theme presets DEFERRED again. Architectural install.js refactor + project Sub-tasks panel + finance polish + cross-doc lint + dogfood drift housekeeping queued.
5. Once locked, brainstorm beacon-button new name + migration story + stub-location migration mechanics + cycle shape.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on the rename mechanics + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate — fresh-vault scratch/6 curl-bootstrap on macOS APFS + Daily Notes successful open + ranch/ runtime artifacts visible. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13) — but stub LOCATION moves Docs/Meta/Templater → ranch/Templater in v0.24.0 (md5 invariant preserved on body); landmine #12 allowlist expansion (12th+ path) requires approval; landmine #11 module-directory invariant UNCHANGED in v0.24.0 (only Tree 3 changes; beacon/<module>/ namespace stays); two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 + v0.23.0 reaffirmation); v0.23.0 lessons (.beacon-backup suffix preserved; sed regex word-boundary discipline; multi-cycle naming locked at brainstorm; runtime banner platform/visual/banner.js is brand surface).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
