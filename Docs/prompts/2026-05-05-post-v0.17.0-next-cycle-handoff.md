---
date: 2026-05-05
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.17.0 cycle. Triggered by v0.17.0 finance-widgets cycle close + tag v0.17.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-05-v0.17.0-finance-widgets-result.md (v0.17.0 close — read first)
  - Docs/plans/2026-05-05-v0.17.0-finance-widgets-design.md
  - Docs/plans/2026-05-05-v0.17.0-finance-widgets-plan.md
  - Docs/plans/2026-05-05-v0.16.0-finance-blueprint-result.md
  - Docs/prompts/2026-05-05-post-v0.16.0-next-cycle-handoff.md (predecessor handoff — structural template)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist; finance widget UIs now DONE)
gates:
  - v0.17.0 closed; tag v0.17.0 pushed; workshop manifest at 0.17.0
  - Path layout 2026-05-05 — workshop = `beacon`; barebones = `barebones-beacon-poc`; accuris-mirror = `accuris-beacon-poc`. Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4`.
  - cards mechanism at 0.2.3 (BUMPED in v0.17.0 from 0.2.2 PATCH ADDITIVE — `badges[].style: "outline"` for Linear/GitHub status pills)
  - finance blueprint at 0.2.6 (BUMPED in v0.17.0 from 0.1.4 — 7 new CustomJS classes; FIVE in-cycle CFs)
  - All other blueprints + mechanisms UNCHANGED
  - Helper-cases harness 255 sub-asserts (UNCHANGED in v0.17.0)
  - Renderer harness 17 cases (UP from 9 in v0.16.0; +8 FF1-FF8 under new `finance` selector)
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.17.x or v0.18.0 (cycle-dependent)
---

# Onboarding — post-v0.17.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (paths from 2026-05-05 migration unchanged)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target + manual-smoke target (NOT git-tracked)
>   - `../accuris-beacon-poc/` — secondary consumer (deliberate-lag at subscription level)
>   - `../accuris/` / `../headspace/` / `../ero/` — production reference vaults (sources for pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.17.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, **`cards@0.2.3`** (BUMPED in v0.17.0: 0.2.2 → 0.2.3 PATCH ADDITIVE — `badges[].style: "outline"`)
- Blueprints: `boards@0.1.0`, `daily@0.2.1`, `journal@0.1.0`, `meetings@0.2.0`, `project@1.3.2`, `to-do@0.1.2`, `trips@0.1.4`, **`finance@0.2.6`** (BUMPED in v0.17.0: 0.1.4 → 0.2.6 — 7 new CustomJS classes + 5 CFs)
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` `v0.11.0` `v0.12.0` `v0.13.0` `v0.14.0` `v0.15.0` `v0.16.0` **`v0.17.0`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED in v0.17.0); `run-renderer.js` 17 cases (+8 FF1-FF8 NEW under `finance` selector); pre-existing T4.0 fatal still carried (use selectors)
- Module-directory invariant codified at the installer level (T-FIX1 from v0.3.0)
- Landmine #12 allowlist: 3 paths; helper count: 4

**v0.17.0 commits in order (top-down):**
- `<TBD>` docs(v0.17.0): cycle close — finance-widgets result + status snapshot + post-v0.17.0 handoff (THIS commit upcoming)
- `6208abb` fix(finance): v0.17.0 CF-5 — drop ## Notes from entity templates; finance 0.2.5 -> 0.2.6
- `740226d` fix(finance): v0.17.0 CF-4 — paycheck-some-paid-as-InProgress + InvoiceControls summary strip + outline-style action buttons + drop entity H1; finance 0.2.4 -> 0.2.5
- `80bdccc` fix(finance): v0.17.0 CF-3 — drop redundant H1 from Budgets/Paychecks/Invoices area hub content; finance 0.2.3 -> 0.2.4
- `6e1ecbc` fix(finance,cards): v0.17.0 CF-2 — empty pending + NewX restyle + cards pill; cards 0.2.2 -> 0.2.3; finance 0.2.2 -> 0.2.3
- `bc5dee2` fix(finance): v0.17.0 CF-1 — visual cohesion + Pending sub-section bug fix; finance 0.2.1 -> 0.2.2
- `605c77a` feat(finance): v0.17.0 S2 — 4 widgets + template integration + NewXButton lockstep; finance 0.2.0 -> 0.2.1
- `e5d7c41` test(finance): v0.17.0 S2 T2.7 — FF4-FF8 widget renderer cases + makeEl addEventListener noop
- `0ed451a` feat(finance): v0.17.0 S1 — FinanceFrontmatter + Budget/PaycheckNavButtons + hub polish; finance 0.1.4 -> 0.2.0; workshop 0.16.0 -> 0.17.0
- `ce32316` test(finance): v0.17.0 S1 T1.5 — FF1/FF2/FF3 renderer cases for BudgetNavButtons + FinanceHubCards
- `94efc62` docs(v0.17.0): finance widget UIs implementation plan
- `4493ac3` docs(v0.17.0): finance widget UIs design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.17.0 baseline
> 1. **Confirm session CWD:** `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. **Git head + recent commits:** `git log --oneline -14` — expect 12 cycle commits + 2 prior (v0.16.0 close + post-v0.16.0 handoff).
> 3. **Working tree:** `git status` → clean (or carry-forward `.bak` artifacts + `blueprints i want.md`).
> 4. **Tags on origin:** `git ls-remote --tags origin | grep v0.17` → `v0.17.0` present + annotated.
> 5. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 platform/installer-stub.js Docs/Meta/Templater/platformInstall.js \
>        ../barebones-beacon-poc/Docs/Meta/Templater/platformInstall.js \
>        ../accuris-beacon-poc/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 6. **Workshop manifest at 0.17.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.17.0"`. Confirm `cards` catalogue line shows `"version": "0.2.3"`, `finance` shows `"0.2.6"`.
> 7. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 8. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`) for regression sweeps. **T4.0 carried-forward 8 cycles now:** `node platform/test/run-renderer.js` (no selector) STILL fatals on a hardcoded `kanban-board.md` path deleted in v0.2.0. Continue using selectors.
>    - All 7 BC + 2 DA + 8 FF cases green.
> 9. **Barebones state:** subscription pinned to `cards@0.2.3`, `finance@0.2.6`. Install green + idempotent.
> 10. **Path migration sanity:** `cat ../barebones-beacon-poc/Docs/Meta/platform-config.json | grep workshop_relative_path` → `"workshop_relative_path": "../beacon"`.

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~15-20 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol.
> 2. **`Docs/plans/2026-05-05-v0.17.0-finance-widgets-result.md`** — most recent cycle close. Captures: 5 in-cycle CFs (visual cohesion + Pending bug fix → empty pending + NewX restyle + cards pill → drop area hub H1 → paycheck status rule fix + InvoiceControls summary + outline action buttons + drop entity H1 → drop ## Notes); 7 new CustomJS classes; 6 NEW v0.17.0 lessons (dv-shim composition discipline, badges[].style outline, outline-accent button pattern as BeaconButton candidate, FrontmatterMutator candidate, first-of-its-kind UI cycle pulls 5 CFs (6-data-point precedent), visual cohesion as CF magnet).
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.17.0 close.
> 4. **`blueprints i want.md`** — user's wishlist (post v0.17.0 close):
>    - REMOVE: finance widget UIs — DONE in v0.17.0
>    - REMOVE: finance foundation — DONE in v0.16.0
>    - REMOVE: trips — DONE in v0.6.0
>    - REMOVE: date-aware nav buttons — DONE in v0.14.0
>    - REMOVE: journal — DONE in v0.13.0
>    - KEEP: styling (line 18-67; explicitly DEFERRED)
>    - KEEP: meeting cards on daily (line 71; verify whether v0.12.0 already satisfied)
>    - KEEP: new-meeting-button parity (line 72; small contained refactor)
> 5. **`Docs/landmines.md`** — 14 entries unchanged in v0.17.0; ONE new candidate gotcha surfaced (dv-shim composition discipline; CF-1 root cause).
> 6. **`Docs/how.md`** — seven cycles of lessons NOT YET codified (v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.17.0); candidate for a docs polish cycle.

---

## Candidate cycles (pick ONE)

> [!example]+ v0.18.0 BeaconButton mechanism promotion **(PRIMARY recommendation if pattern-extraction work feels right)**
> **What:** Promote the outline-accent action button pattern (currently duplicated across NewBudget/Paycheck/InvoiceButton + 3 widget "+ Add X" buttons + InvoiceControls Save/Submit + project's `_renderActionButton`) into a shared `BeaconButton` mechanism. NEW mechanism (first new mechanism in many cycles).
>
> **Why:** Pattern is now well-established across 7+ call sites. Promoting to a shared mechanism would dedupe ~50+ LOC and lock the design before more blueprints copy-paste it. Maps to v0.17.0 lesson #3 ("outline-accent action button pattern → BeaconButton mechanism candidate").
>
> **Estimated scope:** 1-2 stages. Workshop bump 0.17.0 → 0.18.0 MINOR (new mechanism = MINOR per project convention). Helper-cases harness +N cases for the new mechanism API.
>
> **Open questions for brainstorming:** API shape (`BeaconButton.render(parent, opts)` returning the button el? or `BeaconButton.create(opts)` returning HTML string?); icon-as-key vs icon-as-svg-string; tone palette extension (accent/neutral/danger?); hover behavior parameterization.

> [!example]- v0.17.x.y new-meeting-button parity **(RUNNER-UP)**
> **What:** Harmonize `new-meeting-button.js` with the now-fully-mature overlay-dialog quad (NewProject + NewTrip + NewBudget/Paycheck/Invoice). Small contained refactor.
>
> **Why:** Small, contained refactor; "back to clean cycle" candidate after v0.17.0's CF-heavy shape. Matches v0.13.0/v0.14.0 single-stage clean precedent if precedent + zero-novel-mechanism applies.
>
> **Estimated scope:** 0.5–1 stage. Workshop bump 0.17.0 → 0.17.1 PATCH (or 0.18.0 MINOR if user-visible behavior changes).

> [!example]- v0.17.x docs polish + T4.0 cleanup
> **What:** Fix the 8-cycles-old run-renderer.js no-selector fatal + lift v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0 / v0.6.0 / v0.16.0 / v0.17.0 lessons into `Docs/how.md` + `Docs/landmines.md`. Seven cycles of institutional memory captured only in result writeups; `dv-shim composition discipline` from v0.17.0 CF-1 specifically a strong landmine candidate.
>
> **Why:** Restores `run-renderer.js` to clean exit; codifies lessons for future cycles.
>
> **Estimated scope:** 1 stage. Workshop bump 0.17.0 → 0.17.1 PATCH (or no bump — harness + docs only).

> [!example]- v0.17.x.y FrontmatterMutator mechanism (alternative to BeaconButton)
> **What:** Promote `FinanceFrontmatter.update(file, mutator)` to a shared `frontmatter-mutator` mechanism. Generic API (no finance-specific semantics).
>
> **Why:** Mirrors BeaconButton thinking. But finance is currently the ONLY blueprint using widget-driven mutation — promotion may be premature (wait for second blueprint to surface the need). Defer until trip status mutation, project board card state, or similar surfaces.

> [!example]- v0.17.x.y meeting cards on daily — verification candidate
> **What:** Verify whether `SpaceDailyDashboard`'s meetings panel already renders meetings as BeaconCards (v0.12.0 should have migrated). Quick read-and-confirm; close out without a cycle if already satisfied.

> [!example]- v0.17.x.y styling blueprint (deferred per user)
> **What:** Ship a `styling` blueprint bundling theme + CSS-snippet + Style Settings + Baseline community theme. USER EXPLICITLY DEFERRED in v0.6.0 cycle; not yet reversed.

---

## Recommendation

> [!success] **v0.18.0 BeaconButton mechanism promotion** OR **v0.17.x.y new-meeting-button parity** — DUAL TOSS-UP
>
> **If you want to extract a v0.17.0 lesson into a durable mechanism:** v0.18.0 BeaconButton. Pattern is mature across 7+ call sites. First new mechanism in many cycles. Substantial cycle (+harness coverage).
>
> **If you want a clean single-stage cycle to recover from v0.17.0's CF-heavy shape:** v0.17.x.y new-meeting-button parity. Zero novel surface; matches v0.13.0/v0.14.0 reserved-headroom-UNUSED clean-cycle precedent.
>
> **Skip for now:** docs polish + T4.0 (defer until docs-cleanup priority signaled — but `dv-shim composition discipline` is worth codifying soon); FrontmatterMutator (premature; wait for second blueprint); meeting cards on daily (verification only); styling (deferred by user).

---

## Cadence guidance (mirror v0.6.0 / v0.5.0 / v0.11.0 / v0.12.0 / v0.16.0 / v0.17.0 for entity-ecosystem + UI cycles)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval.
> - Per-task subagent dispatch on widget-authoring + helper tasks; master-driven on manifest + version-bump mechanics.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0 lesson) — for any NEW shared helper or mechanism API, QUOTE the literal call shape in the design doc.
> - **Apply v0.17.0 lessons proactively** — (a) when composing hub renderer dv-shims, proxy `pages` + `current` + `el` minimum; (b) for status-bearing blueprints, opt into `badges[].style: "outline"`; (c) when authoring action buttons, mirror project `_renderActionButton` (or use BeaconButton if v0.18.0 ships it).
> - **TDD discipline on harness extensions** — FF1-FF8 precedent (v0.17.0) + BC1-BC7 (v0.12.0/v0.16.0) + DA1-DA2 (v0.14.0): write failing case, commit, then implement.
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian** as a numbered smoke step when introducing/changing CustomJS classes (gotcha 2 v0.6.0 codification).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **First-of-its-kind cycle pulls 2-6 CFs (6-data-point precedent: v0.5.0 / v0.11.0 / v0.12.0 / v0.6.0 / v0.16.0 / v0.17.0).**
> - **Reserved-headroom-UNUSED pattern (v0.13.0 + v0.14.0):** for cycles with strong precedent + zero novel mechanism surface, single-stage is the right shape.
> - **Decision rule:** new mechanism / new blueprint with substantial CustomJS surface → expect CFs; pure-mechanism patch OR near-clone-of-existing → expect single-stage clean.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints.
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile.
> - **In-cycle re-process bump rule** (gotcha 9) — when reusing in-cycle staged work at version X, in-cycle revisions must bump X+1. v0.17.0 was SIX in-cycle bumps (finance 0.1.4 → 0.2.6) — pattern strongly established.
> - **Verify command ids against runtime** (gotcha 10) — when an `invoke_command` action ships, verify the actual command id via `Object.keys(app.commands.commands).filter(...)` in DevTools.
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes — gotcha 2 codified as numbered smoke step.
> - **NEW v0.17.0 candidate gotcha (codify in landmines):** **dv-shim composition discipline** — when a hub renderer hands a sub-container to another customJS class, the shim MUST proxy every dv method that sub-class might call (`pages` + `current` + `el` minimum). A `{container: sub}`-only shim breaks any sub-renderer that calls `dv.pages(...)`.
> - Module-directory invariant under `beacon/` (#11). Per-entity sub-folders (e.g., `<YYYY-MM>/`, `<YYYY-MM-DD>/`, `<slug>/board/<task>/`) live within the invariant.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.
> - **v0.17.0 NEW lessons (apply proactively):**
>   - **dv-shim composition discipline** — proxy `pages` + `current` + `el` from parent dv when sub-cards delegate to BeaconCards.
>   - **`badges[].style: "outline"` on BeaconCards** — Linear/GitHub status pill aesthetic; opt in for status-bearing blueprints.
>   - **Outline-accent action button pattern** — mirror project `_renderActionButton` until BeaconButton mechanism ships.
>   - **`FinanceFrontmatter.update(file, mutator)` API** — generic mutation pattern; promotion candidate.
>   - **First-of-its-kind UI cycles draw 5 CFs** — 6-data-point precedent; reserve Y.Z.x headroom upfront.
>   - **Visual cohesion is CF-heavy work** — three of five v0.17.0 CFs were primarily styling.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD is `/Users/willfell/Documents/obsidian/sync/workshop/beacon`.
> 2. Run all 10 pre-flight checks. Surface discrepancies first (especially the T4.0 fatal — known, carried 8 cycles now).
> 3. Read v0.17.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md (~15-20 min).
> 4. **Surface the cycle pick to user via `AskUserQuestion`** — present candidates above + dual recommendation (BeaconButton mechanism vs new-meeting-button parity); lock the choice before opening brainstorming.
> 5. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions. **Apply v0.17.0 lessons** in design phase: quote API contracts, dv-shim composition rule for any new hub composite, plan for outline status pills if status-bearing.
> 6. After brainstorming approval, invoke `de:writing-plans`.
> 7. After plan approval, invoke `de:subagent-driven-development` OR ask user to start a parallel session with `de:executing-plans`.
> 8. Manual Obsidian smokes at S2/S3 close (Cmd+R as numbered step). Plan reserved 0.Y.x headroom for inline CFs IF first-of-its-kind.
> 9. Cycle close: result + CLAUDE.md status snapshot + commit + tag + push + post-<cycle> handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.17.0 next cycle. Read Docs/prompts/2026-05-05-post-v0.17.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity, 10 pre-flight checks, candidate cycles with dual recommendation (BeaconButton mechanism promotion primary; new-meeting-button parity runner-up), constraints carried forward, and v0.17.0 NEW lessons.

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon.
2. Run all 10 pre-flight checks. Note T4.0 carried 8 cycles; use selectors (empty, malformed, unknown-action, beacon-cards, date-aware, finance) for renderer harness sweeps. Renderer harness has 17 cases (8 finance NEW in v0.17.0).
3. Read v0.17.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md + landmines + how.md.
4. Surface candidates via AskUserQuestion with dual recommendation. v0.18.0 BeaconButton mechanism = pattern-extraction work (substantial); v0.17.x.y new-meeting-button parity = clean-cycle (single-stage).
5. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown. Apply v0.17.0 lessons proactively: (a) dv-shim composition discipline (proxy pages+current+el); (b) outline status pill for status-bearing blueprints; (c) outline-accent action buttons via project _renderActionButton (or BeaconButton if v0.18.0 ships it); (d) reserved-headroom upfront for first-of-its-kind cycles.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on helper/widget classes; master-driven on manifest + version-bump mechanics; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian numbered smoke step when CustomJS classes added/changed. Pre-flight diff barebones installed.json vs subscription. Plan reserved Y.Z.x headroom IF cycle is first-of-its-kind (6-data-point precedent for 2-6 inline CFs).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line + barebones sub (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis); v0.6.0 lessons (API-contract quoting + first-of-its-kind cycles draw 3-5 inline CFs + template-changing migration friction + per-entity sub-folder pattern + Templater auto-promote reusable pattern); v0.16.0 lessons (YAML date auto-parsing — quote on write + tolerant parse on read; frontmatter parser variance for booleans — centralize coercion; NBSP gotcha — widgets are the canonical fix; renderBadge pattern; embed dedup via closest(".markdown-embed"); per-entity sub-folder symmetry); v0.17.0 NEW lessons (dv-shim composition discipline; badges[].style outline; outline-accent action button pattern as BeaconButton candidate; FrontmatterMutator candidate; first-of-its-kind UI cycle pulls 5 CFs; visual cohesion as CF magnet).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
