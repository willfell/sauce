# Post-v0.85.0 — next cycle handoff

**You are starting a fresh cycle.** This doc captures state inherited from v0.85.0's close (2026-06-02) and the candidate work for the next cycle. Per the canonical session-start recipe in `Docs/prompts/SESSION-START.md`.

---

## State inherited

**Workshop:** `0.85.0` at HEAD (S12 cycle-close commit). Branch `main`. cowork `0.24.0`. Engagement-types schema `0.5.0`.

**Versions shipped (v0.85.0):**
- `workshop_version: 0.85.0` (in `platform/manifest.json`, `package.json`, `ranch/platform-subscription.json`)
- `cowork@0.24.0` — MINOR; NEW sub-skill `cowork:read-memory` + NEW orchestrator `cowork:synthesize-week` + 2 NEW pure helpers (`read-memory-helper.js`, `compose-memory-callouts.js`) + NEW canonical type `cowork-weekly-synthesis` + rule_fragment + customization-contract STOCK row + activity-feed `_DEFAULT_BLUEPRINTS` entry; morning-briefing step 3a byte-identical refactor.
- Engagement-type schema: 0.5.0 (additive `synthesize_week: true` default on all 3 standard types).
- No mechanism bumps; no other blueprint bumps.

**Harnesses:** 24 total (unchanged from v0.84.1). Preflight `version-sync ok: 0.85.0` ALL GREEN. **864 passed / 0 failed** in cowork-smoke.

**Recent commits (newest first, condensed):**
- `(S12)`: docs(workshop): v0.85.0 S12 — cycle-close artifacts (result, handoff, history, status, install.md upgrade, landmines #16 touch)
- `27ae6a8` (S11): chore(workshop): v0.85.0 S11 — version bumps (workshop 0.84.4 -> 0.85.0)
- `bd10160` (S10 polish): fix(cowork): v0.85.0 S10 polish — restore proper naming_pattern escape
- `bb984f9` (S10): feat(cowork): v0.85.0 S10 — cowork-weekly-synthesis canonical type + contract + activity-feed
- `729d013` (S9): feat(cowork): v0.85.0 S9 — engagement-types v0.5.0 + onboard synthesize_week cadence
- `c1daaa4` (S8 polish): test(cowork): v0.85.0 S8 polish — remove superseded HC-V0840-C1/C2
- `51995e9` (S8): feat(cowork): v0.85.0 S8 — composeMemoryCallouts helper + morning-briefing refactor
- `77c6d57` (S7): feat(cowork): v0.85.0 S7 — NEW cowork:synthesize-week orchestrator (Tier 2)
- `c2ac5c2` (S6 polish parser): fix(cowork): v0.85.0 S6 polish — read-memory-helper parser correctness
- `b18d3eb` (S6 polish V0750): test(cowork): v0.85.0 S6 polish — V0750-VERSION pin 0.23.0 -> 0.24.0
- `63c2bfa` (S6): feat(cowork): v0.85.0 S6 — NEW cowork:read-memory sub-skill + parser helper
- `b6a283f` (S5 polish): test(cowork): v0.85.0 S5 polish — tighten E2 + regex hardening
- `7ade9b4` (S5): test(cowork): v0.85.0 S5 — RED HC-V0850 A/B/C/D/E/F + HC-V0740-1 pin bump 0.4.0->0.5.0
- `862c3a2` (S3 polish): style(cowork): v0.85.0 S3 polish — date-token + indent + NEW-prefix parity
- `48e7203` (S3): feat(cowork): v0.85.0 S3 — atomic-note Memory log backlink footer
- `c2649a8` (S2): feat(cowork): v0.85.0 S2 — visible Memory section on Cowork.md hub
- `a7b61c5` (S1): fix(cli): v0.85.0 S1 — FLN-v83-2 handleBumpPins blueprint-pin handling
- `1e8d7f8` (S0 plan): docs(plans): v0.85.0 plan

**Mid-cycle cohabitation note.** Three polish PATCHes shipped from another chat in parallel between S1 and S2: `aa9e6fd` (v0.84.2 SDD canonical sync), `f116c3e` (v0.84.3 header-pills), `4f8082c` (v0.84.4 polish-pills). These were daily-blueprint-scoped + landed cleanly alongside this cycle's cowork-blueprint-scoped work. The workshop_version moved 0.84.1 → 0.84.4 mid-cycle; S11 absorbed the bump 0.84.4 → 0.85.0 in a single lockstep edit.

---

## What shipped in v0.85.0 (summary)

**Tier 2 of the cowork continuous-memory architecture + the load-bearing read-memory primitive that makes v0.86.0's cross-orchestrator wire-through mechanical (MINOR).**

The architectural pivot: morning-briefing's prose-read step 3a from v0.84.0 is now a thin shim that calls a NEW structured-output sub-skill `cowork:read-memory` twice (yesterday day + today tick) and composes output via a NEW pure helper `composeMemoryCallouts`. Output is byte-identical to v0.84.0 (golden-fixture HC-V0850-C3..C5 asserts byte equality). Downstream consumers (the 4 atomic-note orchestrators still owed wire-through) will now consume a structured `{ yesterday, today_ticks, week_synthesis, carry_forward }` shape instead of re-implementing inline file-read + parse logic per orchestrator. NEW orchestrator `cowork:synthesize-week` (Friday 17:00) reads the week-window via `cowork:read-memory`; composes a voice-applied weekly-pattern paragraph (≤300 words) + ≤5 carry-forward bullets; writes `spice/cowork/memory/<engagement>/YYYY/MM-Month/YYYY-Www/synthesis.md`; idempotent re-fires within the same ISO week replace body while preserving `created_at`. Engagement-types schema bumped 0.4.0 → 0.5.0 (additive `synthesize_week`). NEW canonical type `cowork-weekly-synthesis` + rule_fragment + customization-contract STOCK row + activity-feed `_DEFAULT_BLUEPRINTS` entry. cowork 0.23.0 → 0.24.0.

**4 polish-prelude items folded** from would-be v0.84.2: §0.1 FLN-v83-2 `handleBumpPins` defensive hardening (ancestry-walk preference over stale `installed.workshop_path` Cellar keg — CLOSED, validation owed at deploy); §0.2 visible `## Memory` section on Cowork.md hub; §0.3 atomic-note `[!quote]- Memory log` backlink footer on MB / midday-tripwire / eod-review; §0.4 headspace eod-review monochrome-callout post-deploy validation noted (no commit unless regression).

7 NEW lessons documented (structured-output sub-skill API decouples downstream; helper-extract + golden-fixture is canonical for byte-identical refactors; polish-prelude fold works with tight per-item HC; mid-cycle cross-chat cohabitation when scopes are disjoint; parser correctness against real-world fixtures BEFORE downstream contracts depend on it; prior-cycle assertion supersedure in the same stage; test-pin escape hygiene for JSON-stored regex naming_patterns). FLN-v83-2 CLOSED. FLN-v85-1/2/3 surfaced.

Full narrative: `Docs/plans/2026-06-02-v0.85.0-tier-2-and-read-memory-result.md`.

---

## Open carry-forwards from v0.85.0 (newly surfaced)

### FLN-v85-1 — deploy-time validation of FLN-v83-2 hardening on accuris/headspace

v0.85.0 §0.1 ships defensive hardening to `handleBumpPins`'s `_resolveWorkshopPath` (ancestry-walk preference over stale `installed.workshop_path`). Design's "skips when N-2+ behind" hypothesis was disconfirmed during S1 investigation; the real-world bug pattern is stale Cellar keg paths, not version-skip distance. **Real-world validation on accuris/headspace is still owed.** If `sauce update --bump-pins` still silently no-ops cowork on the stale-Cellar scenario after v0.85.0 deploys, S1 hardening is insufficient and a stronger fix is needed (probably refusing to use `installed.workshop_path` at all + always preferring ancestry walk).

**Candidate:** PATCH cycle if regression confirmed. Validate first; don't pre-engineer.

### FLN-v85-2 — formalize prior-cycle assertion supersedure

When a refactor obsoletes prose checks (e.g., HC-V0840-C1/C2 obsoleted by v0.85.0 S8's morning-briefing refactor), retire them in the SAME stage rather than leaving them RED across cycle boundaries. Codification candidate for `Docs/agent-guides/build-test-verify.md` § Writing HC cases or `Docs/agent-guides/code-conventions.md`.

**Candidate:** doc-only PATCH; or fold into a process-cycle.

### FLN-v85-3 — test-pin escape hygiene for JSON-stored regex naming_patterns

S10's rule-engine `naming_pattern` requires escaped (`^synthesis\\.md$`) JSON form to round-trip correctly. The S5 smoke regex was authored assuming unescaped. The S10 polish commit `bd10160` hardened the assertion to accept BOTH escaped + unescaped JSON forms. Future rule_fragment cycles should pre-flight assertion regexes against both forms at S0 plan time — not at S7 preflight failure time.

**Candidate:** plan-template addition or a `Docs/agent-guides/build-test-verify.md` § Writing HC cases extension.

### v0.85.0 §0.4 — headspace eod-review monochrome-callout post-deploy validation

Still owed at v0.85.0 deploy. If callouts in eod-review notes render monochrome on headspace, file a regression (would indicate `sauce-callouts.css` is not applied or `--callout-color` per-type variables are not overriding). No commit unless regression.

---

## Carry-forwards still open from prior cycles (priority-ranked)

### Highest-value follow-ups

1. **FLN-v83-1 (still open) — orchestrator SKILL.md prose-consistency lint.** No HC case asserts that each cowork orchestrator's Step 2 contains BOTH (a) canonical engagement-type materialized-path Read AND (b) a not-found guard. Add a `run-cowork-smoke.js` prose-lint case per orchestrator. ~1-2 stages. MINOR posture.

2. **FLN-v841-1 (partially closed — exact-pin equality helper).** HC-V0740-1 engagement-type schema exact-pin was bumped 0.4.0 → 0.5.0 in v0.85.0 lockstep, closing one of the two remaining exact-pin sites. V0750-VERSION cowork-pin remains an exact-pin candidate for a future `versionEquals()` helper refactor in `semver-helper.js`. ~2 stages. PATCH posture.

3. **FLN-v84-2 (REAFFIRMED — v0.86.0 MINOR primary candidate): cross-orchestrator memory wire-through.** v0.85.0 shipped the `cowork:read-memory` primitive; v0.86.0 wires midday-tripwire / eod-review / weekly-review / monthly-review to it via the same pre-flight 3a + body composition pattern morning-briefing got in v0.84.0/0.85.0. Mechanical per-orchestrator copy-and-adapt now that the structured-output API is load-bearing. Strict-sequence per `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md`.

4. **Post-deploy required: re-run `cowork:onboard-scheduled-jobs`** on consumer vaults to register the new `synthesize_week` cadence in Cowork.md after `sauce update --bump-pins` to v0.85.0.

5. **FLN-v79-1 (still open) — VISUAL verification of WS-C (sauce-callouts.css).** Still owed by user.

6. **FLN-v82-1 / FLN-v82-2 / FLN-v82-3:** CLOSED prior to v0.85.0 (engagement-type accessibility, ranch/rules/cowork.json dedup, parseYamlIsh hyphen-key support).

7. **FLN-v821-2 / FLN-v821-3 / FLN-v821-2:** doc-only / plan-template codification still open.

### Long-term carry-forwards

8. **FLN-v84-3** — memory retention / compression policy (design-only until compression logic is scoped).
9. **FLN-v84-4** — Smart Connections semantic retrieval over memory notes.
10. **FLN-v842-1** — `sauce update --bump-pins` as implicit default. PATCH-cycle candidate.
11. **FLN-v79-6** — `classifyGap` resolution heuristics tight; expand as new MCPs surface.
12. **FLN-v80-3** — result-doc structure stability worth codifying as a skill.
13. **v0.79+/v0.80+ vertical-slice carry-forwards** — full list in `Docs/agent-guides/cycle-status.md` § In-flight queue.

### Lower-value cleanup queue

See `Docs/agent-guides/cycle-status.md` § In-flight / next-candidate queue for the full backlog (hub-nav extraction, claude_surface[] wave 3, FLN-v66-1, FLN-v66-5, FLN-v64-6, etc).

---

## Recommended next-cycle: v0.86.0 MINOR — cross-orchestrator memory wire-through

**Per `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md` + `Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md` § v0.86.0.**

Apply the v0.85.0 morning-briefing pre-flight 3a + body composition pattern to the 4 remaining atomic-note orchestrators:

- `cowork:midday-tripwire` — pre-flight read-memory call; inject memory-context callout into the tripwire body (gate on `tripwire_aspects.length > 0` posture preserved).
- `cowork:eod-review` — pre-flight read-memory call; weave today's tick stream + yesterday's synthesis into the EOD narrative.
- `cowork:weekly-review` — pre-flight read-memory call + week-synthesis read; reference Friday's `synthesis.md` (if present) in the weekly hub narrative.
- `cowork:monthly-review` — pre-flight read-memory call + 4-week-synthesis aggregation; reference all 4 weekly `synthesis.md` files (if present) in the monthly hub narrative.

Each orchestrator's diff is small: insert two `cowork:read-memory` invocations at pre-flight + one `composeMemoryCallouts` call in body composition (copy-and-adapt from morning-briefing/SKILL.md step 3a). Null-data gating applies — when a memory file is absent, the step skips cleanly.

**Scope:** ~5-7 stages.
- S0: design + plan + RED HC-V0860-A/B/C/D groups (one set per orchestrator).
- S1..S4: wire each of the 4 orchestrators (one stage per).
- S5: lockstep version bumps (workshop 0.85.0 → 0.86.0; cowork 0.24.0 → 0.25.0).
- S6: cycle-close.

**Version posture:** Workshop 0.85.0 → 0.86.0 (MINOR — net-new orchestrator surface contracts). cowork 0.24.0 → 0.25.0 (MINOR — additive SKILL.md content on existing orchestrators; consumers must re-bump pin to pick up).

**Risk:** parser correctness regressions are now load-bearing for 4 more call-sites. v0.85.0's S6 polish caught 5 parser defects against real-world synthesize-day + capture-tick output shapes; the v0.86.0 cycle should re-grep real-world fixtures from accuris/headspace memory dirs as part of S0 plan-time discovery and add A1..A4 / B1..B5 style fixtures BEFORE wiring (not after).

---

## Ready-to-paste prompt for the next session

Below is the prompt to start the recommended next cycle.

```
Post-v0.85.0 next cycle — run on this machine.

REPO: /Users/willfellhoelter/projects/repos/sauce (sauce WORKSHOP vault, branch main,
currently workshop v0.85.0). Dogfoods as its own first consumer — NOT a consumer vault.
Run the CLAUDE.md vault-identity check before any write.

STEP 0 — session-start recipe:
  cd /Users/willfellhoelter/projects/repos/sauce
  git fetch origin && git status     # expect clean tree on main
  git log --oneline -5               # HEAD should be S12 cycle-close commit (v0.85.0)
  jq -r .workshop_version ranch/platform-installed.json   # expect 0.85.0

Then follow Docs/prompts/SESSION-START.md (canonical session-start recipe). Read in order:
  - CLAUDE.md (thin router)
  - Docs/agent-guides/cycle-status.md (live state at v0.85.0)
  - Docs/prompts/2026-06-02-post-v0.85.0-next-cycle-handoff.md (THIS doc)
  - Docs/landmines.md (22 entries; always non-negotiable)
  - Docs/agent-guides/build-test-verify.md
  - Docs/agent-guides/code-conventions.md
  - Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md
  - Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md § v0.86.0

STEP 1 — Brainstorm v0.86.0 (cross-orchestrator memory wire-through):

The PRIMARY recommendation is v0.86.0 MINOR — cross-orchestrator memory wire-through
(FLN-v84-2). Apply the v0.85.0 morning-briefing step 3a + composeMemoryCallouts
pattern to:
  - cowork:midday-tripwire
  - cowork:eod-review
  - cowork:weekly-review
  - cowork:monthly-review

Each diff is small: 2 cowork:read-memory invocations at pre-flight + 1
composeMemoryCallouts call in body. Null-data gating preserves backward-compat.

~5-7 stages. Workshop 0.85.0 → 0.86.0; cowork 0.24.0 → 0.25.0.

POST-DEPLOY TASKS (do before any code work if not already done):
  - sauce update --bump-pins on all consumer vaults (workshop 0.84.x → 0.85.0;
    cowork 0.23.0 → 0.24.0). v0.85.0 §0.1 should resolve the v0.83.0/v0.84.0
    jq workaround scenario — but VALIDATE that --bump-pins actually bumps cowork
    on accuris/headspace (FLN-v85-1). If it still skips, file the regression
    + fall back to manual jq edit per Docs/install.md v0.84.0 upgrade section.
  - Re-run cowork:onboard-scheduled-jobs on each consumer vault to register
    the new synthesize_week cadence in Cowork.md.
  - Friday afternoon: verify cowork:synthesize-week fires correctly + writes
    spice/cowork/memory/<engagement>/YYYY/MM-Month/YYYY-Www/synthesis.md.
  - Verify morning-briefing output is byte-identical to a v0.84.4 reference fire
    (golden-fixture HC-V0850-C3..C5 should already guarantee this, but eyeball
    the first post-deploy fire to confirm).
  - §0.4: VISUAL verification of eod-review callouts on headspace (monochrome
    regression check).

CONVENTIONS (baked in from v0.82.0+ through v0.85.0 close):
- Direct-push to origin/main. One commit per stage. Conventional-commits format.
- NO Co-Authored-By trailer. Explicit git add <paths> (NEVER git add -A).
- Dogfood install: node platform/install.js --vault . --auto-approve
- After ANY SKILL.md/manifest change, re-materialize and stage regenerated ranch/ +
  .claude/ in the same stage commit.
- Preflight gated on scripts/check-version-sync.js (package.json version ==
  workshop_version).
- Get USER APPROVAL before pushing any annotated tag.

END STATE: cycle committed on origin/main, preflight ALL GREEN, cycle-close artifacts
written per canonical convention (result, handoff, cycle-status, cycle-history,
install.md upgrade section, landmines #16 touch if applicable).
```

---

## References

- v0.85.0 design + plan + result: `Docs/plans/2026-06-02-v0.85.0-tier-2-and-read-memory-{design,plan,result}.md`
- Sequencing decision: `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md`
- v0.86–v0.89 scope sketches: `Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md`
- v0.84.0 result (Tier 0 + Tier 1 predecessor): `Docs/plans/2026-06-01-v0.84.0-cowork-memory-layer-result.md`
- v0.84.0 handoff: `Docs/prompts/2026-06-02-post-v0.84.0-next-cycle-handoff.md`
- Cycle-history: `Docs/cycle-history.md`
- Cycle-status: `Docs/agent-guides/cycle-status.md`
- Landmines: `Docs/landmines.md`
- Customization contract: `Docs/agent-guides/cowork-customization-contract.md`
- Build-test-verify guide: `Docs/agent-guides/build-test-verify.md`
- Code conventions: `Docs/agent-guides/code-conventions.md`
- Session-start recipe: `Docs/prompts/SESSION-START.md`
- Install upgrade section: `Docs/install.md` § Upgrading from v0.84.x to v0.85.0
