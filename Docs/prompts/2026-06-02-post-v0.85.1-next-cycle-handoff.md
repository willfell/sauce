# Post-v0.85.1 — next cycle handoff

**You are starting a fresh cycle.** This doc captures state inherited from v0.85.1's close (2026-06-02) and the candidate work for the next cycle. Per the canonical session-start recipe in `Docs/prompts/SESSION-START.md`.

---

## State inherited

**Workshop:** `0.85.1` at HEAD (S5 cycle-close commit). Branch `main`. cowork `0.24.1`. Engagement-types schema `0.5.0` (unchanged from v0.85.0).

**Versions shipped (v0.85.1):**
- `workshop_version: 0.85.1` (in `platform/manifest.json`, `package.json`, `ranch/platform-subscription.json`)
- `cowork@0.24.1` — PATCH; SKILL.md prose change in `synthesize-week/SKILL.md` schedule line + `onboard-scheduled-jobs/SKILL.md` cadence walk entry. No new install surface; no new sub-skills, orchestrators, helpers, canonical types, or rule_fragments.
- Engagement-types schema: unchanged at `0.5.0`.
- No mechanism bumps; no other blueprint bumps.

**Harnesses:** 24 total (unchanged from v0.85.0). Preflight `version-sync ok: 0.85.1` ALL GREEN. **868 passed / 0 failed** in smoke (delta +4 from v0.85.0's 864).

**Recent commits (newest first, condensed):**
- `(S5)`: docs(workshop): v0.85.1 S5 — cycle-close artifacts
- `822edf8` (S4): chore(workshop): v0.85.1 S4 — version bumps (workshop 0.85.0 -> 0.85.1)
- `eb408ea` (S3): feat(cli): v0.85.1 S3 — active-pantry drift warning
- `6605601` (S2): feat(cowork): v0.85.1 S2 — synthesize-week default cron 0 17 -> 30 17
- `0b1df7b` (S1 polish): test(cli): v0.85.1 S1 polish — pin --workshop-path in HC-V0850-G1
- `77558e1` (S1): fix(cli): v0.85.1 S1 — _resolveWorkshopPath generalization + workshop_path auto-populate
- `5b8e501` (S0 plan): docs(plans): v0.85.1 deploy-mechanics-and-cron-fix design + plan

---

## What shipped in v0.85.1 (summary)

**PATCH retiring v0.85.0's deploy-time friction (FLN-v85-1) + fixing synthesize-week's Friday-data-drop default cron.** v0.85.0's §0.1 defensive hardening turned out to be insufficient for the active-pantry deploy layout (no `libexec/`-named ancestor); v0.85.1 generalizes `_resolveWorkshopPath` via a new `_isValidWorkshopRoot()` helper that walks for ANY ancestor with a parseable `platform/manifest.json` + `workshop_version` field present — covers brew Cellar + active-pantry + in-vault pantry uniformly. `installed.workshop_path` auto-populated on first successful resolve so future runs short-circuit. Synthesize-week default cron `0 17 * * 5` → `30 17 * * 5` (Friday 17:30, after synthesize-day's 17:20 — captures Friday data in the week roll-up). NEW `_warnIfActivePantryDrift()` helper in `cmd-update.js` fires a stderr Notice with the suggested `git pull --ff-only origin main` when active-pantry checkout is N-behind `origin/main`. 4 pre-existing test fixtures (caseV0760B2/B3/B4 + 6 `_bpRun` callers + HC-V0850-G1 polish) updated for hermeticity now that the generalized resolver accepts the real sauce-repo ancestry as a valid workshop root. cowork 0.24.0 → 0.24.1 (PATCH; SKILL.md prose). 4 NEW lessons documented (design-doc hypothesis insufficient until real-world deploy surfacing; test-fixture hermeticity when a resolver becomes MORE permissive; cron-default design must walk the full cadence-graph not just the upstream slot; auto-populate-to-disk pattern for runtime path resolution). **FLN-v85-1 CLOSED.** FLN-v85-4 surfaced.

Full narrative: `Docs/plans/2026-06-02-v0.85.1-deploy-mechanics-and-cron-fix-result.md`.

---

## Open carry-forwards from v0.85.1 (newly surfaced)

### FLN-v85-4 — scheduled-job cron migration hints

v0.85.1 §3 changed synthesize-week's default cron from `0 17 * * 5` to `30 17 * * 5`. Existing scheduled jobs registered at v0.85.0 onboard via `cowork:onboard-scheduled-jobs` (e.g., `cowork-synthesize-week-accuris`) carry the old `0 17` cron and **will NOT auto-update**. Consumer must hand-edit each `cowork-synthesize-week-<engagement>` task's cron field via the scheduled-tasks MCP. Future cycles that change a default cron value (or add/move a cadence) should ship a migration hint (e.g., an idempotent `cowork:migrate-cron <old> <new>` skill that walks existing tasks via the MCP) or document the manual one-liner more prominently than the install.md § Upgrading note.

**Candidate:** small skill cycle (probably PATCH-shaped) once v0.86.0's cross-orchestrator wire-through is done. Could also fold into the FLN-v842-1 "`--bump-pins` implicit default" cycle.

### FLN-v85-2 (still open from v0.85.0) — formalize prior-cycle assertion supersedure

When a refactor obsoletes prose checks, retire them in the SAME stage rather than leaving them RED across cycle boundaries. Codification candidate for `Docs/agent-guides/build-test-verify.md` § Writing HC cases.

**Candidate:** doc-only PATCH; or fold into a process-cycle.

### FLN-v85-3 (still open from v0.85.0) — test-pin escape hygiene for JSON-stored regex naming_patterns

Smoke regex assertions on JSON-stored naming_patterns must accept both escaped + unescaped forms (the rule-engine wants escaped `^foo\\.md$`; pragmatic readers may use either). Future rule_fragment cycles should pre-flight assertion regexes against both forms at S0 plan time.

**Candidate:** plan-template addition or `Docs/agent-guides/build-test-verify.md` § Writing HC cases extension.

### v0.85.0 §0.4 (still owed from v0.85.0) — headspace eod-review monochrome-callout post-deploy validation

If callouts in eod-review notes render monochrome on headspace, file a regression (would indicate `sauce-callouts.css` is not applied or `--callout-color` per-type variables are not overriding). No commit unless regression.

---

## Carry-forwards still open from prior cycles (priority-ranked)

### Highest-value follow-ups

1. **FLN-v84-2 (REAFFIRMED — v0.86.0 MINOR primary candidate): cross-orchestrator memory wire-through.** v0.85.0 shipped the `cowork:read-memory` primitive; v0.86.0 wires midday-tripwire / eod-review / weekly-review / monthly-review to it via the same pre-flight 3a + body composition pattern morning-briefing got in v0.84.0/0.85.0. Mechanical per-orchestrator copy-and-adapt now that the structured-output API is load-bearing. Strict-sequence per `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md`.

2. **FLN-v83-1 (still open) — orchestrator SKILL.md prose-consistency lint.** No HC case asserts that each cowork orchestrator's Step 2 contains BOTH (a) canonical engagement-type materialized-path Read AND (b) a not-found guard. Add a `run-cowork-smoke.js` prose-lint case per orchestrator. ~1-2 stages. MINOR posture.

3. **FLN-v841-1 (partially closed — exact-pin equality helper).** HC-V0740-1 engagement-type schema exact-pin was bumped in v0.85.0 lockstep, closing one of the two remaining exact-pin sites. V0750-VERSION cowork-pin (bumped again 0.24.0 → 0.24.1 in v0.85.1 S2) remains an exact-pin candidate for a future `versionEquals()` helper refactor in `semver-helper.js`. ~2 stages. PATCH posture.

4. **Post-deploy required:** for v0.85.1 specifically, hand-adjust each existing `cowork-synthesize-week-<engagement>` task's cron field (`0 17 * * 5` → `30 17 * * 5`) via scheduled-tasks MCP after `sauce update --bump-pins`. See `Docs/install.md` § Upgrading from v0.85.0 to v0.85.1.

5. **FLN-v79-1 (still open) — VISUAL verification of WS-C (sauce-callouts.css).** Still owed by user.

### Long-term carry-forwards

6. **FLN-v84-3** — memory retention / compression policy (design-only until compression logic is scoped).
7. **FLN-v84-4** — Smart Connections semantic retrieval over memory notes.
8. **FLN-v842-1** — `sauce update --bump-pins` as implicit default. PATCH-cycle candidate.
9. **FLN-v79-6** — `classifyGap` resolution heuristics tight; expand as new MCPs surface.
10. **FLN-v80-3** — result-doc structure stability worth codifying as a skill.
11. **FLN-v821-1 / FLN-v821-2 / FLN-v821-3** — doc-only / plan-template codification still open.
12. **v0.79+/v0.80+ vertical-slice carry-forwards** — full list in `Docs/agent-guides/cycle-status.md` § In-flight queue.

### Lower-value cleanup queue

See `Docs/agent-guides/cycle-status.md` § In-flight / next-candidate queue for the full backlog (hub-nav extraction, claude_surface[] wave 3, FLN-v66-1, FLN-v66-5, FLN-v64-6, etc).

---

## Recommended next-cycle: v0.86.0 MINOR — cross-orchestrator memory wire-through

**Per `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md` + `Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md` § v0.86.0.**

Apply the v0.85.0 morning-briefing pre-flight 3a + body composition pattern to the 4 remaining atomic-note orchestrators:

- `cowork:midday-tripwire` — pre-flight read-memory call; inject memory-context callout into the tripwire body (gate on `tripwire_aspects.length > 0` posture preserved).
- `cowork:eod-review` — pre-flight read-memory call; weave today's tick stream + yesterday's synthesis into the EOD narrative.
- `cowork:weekly-review` — pre-flight read-memory call + week-synthesis read; reference Friday's `synthesis.md` (if present) in the weekly hub narrative.
- `cowork:monthly-review` — pre-flight read-memory call + 4-week-synthesis aggregation; reference all 4 weekly `synthesis.md` files (if present) in the monthly hub narrative.

Each orchestrator's diff is small: insert two `cowork:read-memory` invocations at pre-flight + one `composeMemoryCallouts` call in body composition (copy-and-adapt from morning-briefing/SKILL.md step 3a). Null-data gating applies — when a memory file is absent, the step skips cleanly.

**Scope:** ~5-7 stages.
- S0: design + plan + RED HC-V0860-A/B/C/D groups (one set per orchestrator).
- S1..S4: wire each of the 4 orchestrators (one stage per).
- S5: lockstep version bumps (workshop 0.85.1 → 0.86.0; cowork 0.24.1 → 0.25.0).
- S6: cycle-close.

**Version posture:** Workshop 0.85.1 → 0.86.0 (MINOR — net-new orchestrator surface contracts). cowork 0.24.1 → 0.25.0 (MINOR — additive SKILL.md content on existing orchestrators; consumers must re-bump pin to pick up).

**Risk:** parser correctness regressions are now load-bearing for 4 more call-sites. v0.85.0's S6 polish caught 5 parser defects against real-world synthesize-day + capture-tick output shapes; the v0.86.0 cycle should re-grep real-world fixtures from accuris/headspace memory dirs as part of S0 plan-time discovery and add A1..A4 / B1..B5 style fixtures BEFORE wiring (not after).

---

## Ready-to-paste prompt for the next session

Below is the prompt to start the recommended next cycle.

```
Post-v0.85.1 next cycle — run on this machine.

REPO: /Users/willfellhoelter/projects/repos/sauce (sauce WORKSHOP vault, branch main,
currently workshop v0.85.1). Dogfoods as its own first consumer — NOT a consumer vault.
Run the CLAUDE.md vault-identity check before any write.

STEP 0 — session-start recipe:
  cd /Users/willfellhoelter/projects/repos/sauce
  git fetch origin && git status     # expect clean tree on main
  git log --oneline -5               # HEAD should be S5 cycle-close commit (v0.85.1)
  jq -r .workshop_version ranch/platform-installed.json   # expect 0.85.1

Then follow Docs/prompts/SESSION-START.md (canonical session-start recipe). Read in order:
  - CLAUDE.md (thin router)
  - Docs/agent-guides/cycle-status.md (live state at v0.85.1)
  - Docs/prompts/2026-06-02-post-v0.85.1-next-cycle-handoff.md (THIS doc)
  - Docs/landmines.md (22 entries; always non-negotiable)
  - Docs/agent-guides/build-test-verify.md
  - Docs/agent-guides/code-conventions.md
  - Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md
  - Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md § v0.86.0

STEP 1 — Brainstorm v0.86.0 (cross-orchestrator memory wire-through):

The PRIMARY recommendation is v0.86.0 MINOR — cross-orchestrator memory wire-through
(FLN-v84-2). Apply the v0.85.0 morning-briefing step 3a + composeMemoryCallouts
pattern to:
  - cowork:midday-tripwire
  - cowork:eod-review
  - cowork:weekly-review
  - cowork:monthly-review

Each diff is small: 2 cowork:read-memory invocations at pre-flight + 1
composeMemoryCallouts call in body. Null-data gating preserves backward-compat.

~5-7 stages. Workshop 0.85.1 → 0.86.0; cowork 0.24.1 → 0.25.0.

POST-DEPLOY TASKS (do before any code work if not already done):
  - sauce update --bump-pins on all consumer vaults (workshop 0.85.0 → 0.85.1;
    cowork 0.24.0 → 0.24.1). v0.85.1 §1 generalization should resolve cleanly
    across all 3 deploy layouts now (brew Cellar + active-pantry + in-vault).
    No more --workshop-path flag dance.
  - For each existing cowork-synthesize-week-<engagement> task, hand-adjust
    cron from `0 17 * * 5` to `30 17 * * 5` via scheduled-tasks MCP
    (FLN-v85-4 / install.md § Upgrading from v0.85.0 to v0.85.1).
  - Verify the next Friday afternoon's cowork:synthesize-week fire at 17:30
    (after synthesize-day's 17:20) actually captures Friday data in the
    week roll-up at spice/cowork/memory/<engagement>/YYYY/MM-Month/YYYY-Www/synthesis.md.
  - §0.4 (v0.85.0 carry-forward): VISUAL verification of eod-review callouts
    on headspace (monochrome regression check).

CONVENTIONS (baked in from v0.82.0+ through v0.85.1 close):
- Direct-push to origin/main. One commit per stage. Conventional-commits format.
- NO Co-Authored-By trailer. Explicit git add <paths> (NEVER git add -A).
- Dogfood install: node platform/install.js --vault . --auto-approve
- After ANY SKILL.md/manifest change, re-materialize and stage regenerated ranch/ +
  .claude/ in the same stage commit.
- Preflight gated on scripts/check-version-sync.js (package.json version ==
  workshop_version).
- Get USER APPROVAL before pushing any annotated tag.

END STATE: cycle committed on origin/main, preflight ALL GREEN, cycle-close artifacts
written per canonical convention (result, handoff, cycle-status, cycle-history,
install.md upgrade section, landmines #16 touch if applicable).
```

---

## References

- v0.85.1 design + plan + result: `Docs/plans/2026-06-02-v0.85.1-deploy-mechanics-and-cron-fix-{design,plan,result}.md`
- v0.85.0 result (predecessor): `Docs/plans/2026-06-02-v0.85.0-tier-2-and-read-memory-result.md`
- v0.85.0 handoff: `Docs/prompts/2026-06-02-post-v0.85.0-next-cycle-handoff.md`
- Sequencing decision: `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md`
- v0.86–v0.89 scope sketches: `Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md`
- v0.84.0 result (Tier 0 + Tier 1 grandparent): `Docs/plans/2026-06-01-v0.84.0-cowork-memory-layer-result.md`
- Cycle-history: `Docs/cycle-history.md`
- Cycle-status: `Docs/agent-guides/cycle-status.md`
- Landmines: `Docs/landmines.md`
- Customization contract: `Docs/agent-guides/cowork-customization-contract.md`
- Build-test-verify guide: `Docs/agent-guides/build-test-verify.md`
- Code conventions: `Docs/agent-guides/code-conventions.md`
- Session-start recipe: `Docs/prompts/SESSION-START.md`
- Install upgrade section: `Docs/install.md` § Upgrading from v0.85.0 to v0.85.1
