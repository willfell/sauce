---
date: 2026-05-03
purpose: copy-paste prompt for a fresh agent session to resume v0.1.1 S3 (and beyond — S4, then v0.1.2)
related:
  - 2026-05-03-v0.1.1-S2-close-and-v0.1.2-handoff.md
---

# Prompt — resume v0.1.1 S3, then S4, then v0.1.2

Copy everything inside the fenced block below into a fresh agent session. The agent has zero conversation context; this is self-contained.

---

```
I'm continuing work on the vault platform project from /Users/willfell/Documents/obsidian/sync/workshop/poc-vault. Previous session closed v0.1.1 S2 fully (registry-driven nav-buttons v2.0.0 + ledger-prune fix-up + headless renderer harness). S3 (project blueprint v0.2.0 + Dataview kanban template) is next, then S4 (barebones regression sweep), then v0.1.2 (multi-vault automation via git-based pull) which is fully designed but not yet planned. Don't write any code yet — read first, then we'll align.

## Read in this order before responding

1. `CLAUDE.md` (workshop root) — vault identity + non-negotiables.
2. `Docs/Index.md` — entry point.
3. `Docs/why.md`, `Docs/how.md`, `Docs/landmines.md` — purpose, architecture, traps. Landmines is non-negotiable.
4. `Docs/plans/2026-05-03-v0.1.1-S2-close-and-v0.1.2-handoff.md` — THE LOAD-BEARING DOC. Captures everything done so far across both cycles, every locked decision, every side-quest, every open thread. Read in full.
5. `Docs/plans/2026-05-03-registry-driven-nav-buttons-design.md` — v0.1.1 design (registry-driven nav-buttons). Note the manifest's `template_source: "kanban-board.md"` is BASENAME ONLY — the installer prepends `${content_path}/${source-name}/` automatically. Don't include `content/` prefix.
6. `Docs/plans/2026-05-03-registry-driven-nav-buttons-plan.md` — v0.1.1 implementation plan. Stage 3 (T3.1–T3.6) is what's next; Stage 4 (T4.1–T4.10) follows.
7. `Docs/plans/2026-05-03-multi-vault-automation-design.md` — v0.1.2 design (git-based pull, thin stub bootstrap). Approved; awaiting `de:writing-plans` after v0.1.1 closes.
8. `Docs/plans/execution-logs/2026-05-03-registry-driven-nav-buttons/` — every implementer's per-task log (T1.1 through T2.5-T2.6-T2.7-headless-renderer-harness). Skim at least the most recent few to see the rhythm.

Then peek at the live state:

- `platform/manifest.json` — workshop_version is 0.4.0. nav-buttons mechanism at 2.0.0. project blueprint still at 0.1.0 (S3 will bump it).
- `platform/mechanisms/nav-buttons/space-nav-buttons.js` — v2.0.0 thin renderer (~205 LOC).
- `platform/blueprints/project/manifest.json` — current 0.1.0 manifest. S3 will add `nav_buttons[]` + a `files[]` entry for the kanban template.
- `Docs/Meta/platform-subscription.json` (workshop) — workshop currently subscribes to nav-buttons@2.0.0 only; project was DROPPED for renderer-dogfood-purity.
- `platform/test/run-install.js` — install harness (~280 LOC). Use this for S3 install dogfood; `node platform/test/run-install.js .` runs the workshop install headless.
- `platform/test/run-renderer.js` — renderer harness (~270 LOC). Use this for renderer smoke; `node platform/test/run-renderer.js` runs all three S2 tests headless.

## Where we are right now

v0.1.1 progress:
- S1: ✅ closed — installer extensions (`applyNavButtons`, `validateAndResolve`, `pruneNavButtonsRegistry`, `pruneInstalledLedger`, `content_path` variable, nit-4 prune-shape failure-loud fix). workshop_version 0.4.0. Bootstrap copies × 3 in sync.
- S2: ✅ closed — nav-buttons v2.0.0 thin renderer (full rewrite). Workshop dropped project subscription. Headless renderer harness verifies T2.5/T2.6/T2.7 without Obsidian.
- S3: ⏳ next — project blueprint v0.2.0 + Dataview-driven kanban template + first registry contribution.
- S4: ⏳ after S3 — barebones regression sweep; some smokes may still need Obsidian (specifically T4.6 /new-project which is Templater-driven; rest can run via the harnesses).

v0.1.2 (multi-vault automation): designed; not planned/built. Ships AFTER v0.1.1 closes.

## What I want from you in this session

1. Read the files above before saying anything substantive. Confirm by referencing specifics.
2. Use the `de:executing-plans` or `de:subagent-driven-development` skill to drive S3. Per the project rhythm: bundle implementer work, spec review, quality review (in parallel where safe), surface to me only at stage boundaries or at design-doc decision points (CLAUDE.md "ask before" gates).
3. Use the headless harnesses (`run-install.js` + `run-renderer.js`) for dogfood — DO NOT propose Obsidian-in-the-loop testing for anything they cover. If a S4 smoke can't be covered by the harnesses, surface it and we'll decide whether to build a third harness or accept it as a manual smoke.
4. Don't bump `workshop_version` without asking (CLAUDE.md non-negotiable).
5. Don't hand-edit `Docs/Meta/platform-installed.json` (S2 surfaced this; remediated via `pruneInstalledLedger`).
6. Don't touch `.obsidian/` directories.
7. Each implementer dispatch logs to `Docs/plans/execution-logs/2026-05-03-registry-driven-nav-buttons/T<task>-<slug>.md`. Multi-task bundles can share one log if the work is tightly coupled.
8. Ask one clarifying question at a time via AskUserQuestion when needed; the handoff doc anticipates many.

## Constraints

- No git commits this cycle (v0.1.2 introduces git; not yet).
- Bootstrap-copy discipline holds for v0.1.1: when `platform/install.js` changes, re-sync the 3 runtime copies in poc-vault, tmp-acc-vault, tmp-test-barebones-vault. (Retires in v0.1.2 S3.)
- Workshop self-install must remain green at every stage close. Use the install harness to verify.
- Mobile (iOS) is unsupported.
- `template_source` in `nav_buttons[]` declarations is BASENAME ONLY (`"kanban-board.md"`, not `"content/kanban-board.md"`). The installer's path-rewrite would otherwise produce a doubled `content/` segment.
- Workshop's project subscription stays DROPPED through v0.1.2. Project regression dogfood lives in barebones.

## Pacing preference

Stage-level checkpoints. Bundle implementer work; surface only at stage / decision boundaries. The two side-quest harnesses (install + renderer) are load-bearing — don't bypass them. If you find a behavior the harnesses can't cover, surface that as a tooling decision.

## Suggested first move

1. Read the files above.
2. Use `de:executing-plans` (or `de:subagent-driven-development`) to drive Stage 3.
3. Dispatch the S3 implementer for the bundle: kanban-board.md content file + project manifest update (version, nav_buttons, files entry, depends_on bump) + workshop manifest blueprints[] entry bump. Plan tasks T3.1, T3.2, T3.3.
4. Workshop dogfood via `run-install.js` (T3.4) — but workshop doesn't subscribe to project, so this verifies the manifest change doesn't break anything; it doesn't install project anywhere.
5. Negative test (T3.6) — version range satisfaction. T3.5 (malformed entry) was deferred to S4 since workshop won't install project.
6. Spec review + quality review.
7. Promote to S4 (barebones regression sweep).
8. After S4 closes + result writeup, invoke `de:writing-plans` for v0.1.2 (the design at `Docs/plans/2026-05-03-multi-vault-automation-design.md` is approved and ready).

Begin by confirming what you've read and then asking your first clarifying question (if any).
```

---

## Notes for the human (not part of the prompt above)

- The handoff doc (`2026-05-03-v0.1.1-S2-close-and-v0.1.2-handoff.md`) is the single load-bearing source. If anything in the prompt above conflicts with the handoff, the handoff wins.
- If you want to skip ahead to v0.1.2 (e.g., abandon S3/S4 and pivot), edit the prompt to say so explicitly. The agent will follow what you write.
- If the new agent burns context reading too many files, you can trim the read list to: handoff doc + design doc + plan doc + state inspection. Skip the conceptual triple (why/how/landmines) since they're already encoded in the handoff's "End goal" + non-negotiables sections.
- The two harnesses are stable. If you change install.js or space-nav-buttons.js significantly, both harnesses might need small updates; the renderer harness in particular faked a small `dv` shape that could need extending if the renderer ever needs a richer Dataview API.
