---
date: 2026-05-04
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.12.0 cycle. Triggered by v0.12.0 cycle close + tag v0.12.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-04-v0.12.0-cards-cohesion-result.md (v0.12.0 close — read first)
  - Docs/plans/2026-05-04-v0.12.0-cards-cohesion-design.md (design + locked decisions)
  - Docs/plans/2026-05-04-v0.12.0-cards-cohesion-plan.md (plan)
  - Docs/plans/2026-05-04-cards-mechanism-fit-audit.md (migration roadmap; READY/EXTEND audit — load-bearing for the tasks-as-bullets revert)
  - Docs/plans/2026-05-04-v0.11.0-cards-mechanism-result.md (BeaconCards 0.1.x foundation)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol — load-bearing)
  - blueprints i want.md (user's wishlist of future blueprints — load-bearing for next-cycle pick)
gates:
  - v0.12.0 closed; tag v0.12.0 pushed; workshop manifest at 0.12.0
  - cards mechanism at 0.2.1 (subtitle polymorphism + badges[].icon API in 0.2.0; visual polish PATCH in 0.2.1)
  - meetings blueprint at 0.2.0 (MeetingsHubCards thin wrapper around BeaconCards; row-layout + async pre-fetch adapter)
  - daily blueprint at 0.2.1 (SpaceDailyDashboard meetings panel migrated to BeaconCards; tasks panel reverted to bullet list per audit-predicted regression)
  - Helper-cases harness 255 sub-asserts (UNCHANGED in v0.12.0; cards mechanism is renderer-only)
  - Renderer harness +6 BC cases (BC1-BC6 for subtitle polymorphism + badges[].icon + synthetic-page)
  - Stub md5 invariant unchanged at `a39257da1dd49ae4481e5cd0a42bdac4` across all 4 paths
  - Cohesion thesis bounded — BeaconCards = right primitive for hub-style listings; bullet lists = right primitive for compact at-a-glance task panels
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval — user picks)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.12.x or v0.6.0 (cycle-dependent)
---

# Onboarding — post-v0.12.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (load-bearing)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git` — personal account `willfellhoelter@gmail.com`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/poc-vault`
> - **Sibling vaults:**
>   - `../tmp-test-barebones-vault/` — primary regression target + manual-smoke target
>   - `../tmp-acc-vault/` — secondary consumer (deliberate-lag at subscription level; mechanisms-only posture preserved)
>   - `../accuris/` — production reference vault (NOT a v0.x consumer; source of pattern ports)
>   - `../headspace/` — personal reference vault (source of journal / trips / finance pattern ports per `blueprints i want.md`)
>   - `../ero/` — business reference vault (source of business-finance / invoicing pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` UNCHANGED across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.12.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.3.0`, **`cards@0.2.1`** (BUMPED in v0.12.0: 0.1.1 → 0.2.0 API extension → 0.2.1 visual polish)
- Blueprints: `boards@0.1.0`, **`daily@0.2.1`** (BUMPED in v0.12.0: 0.1.4 → 0.2.0 → 0.2.1 tasks-as-bullets revert), **`meetings@0.2.0`** (BUMPED in v0.12.0: 0.1.2 → 0.2.0 thin BeaconCards wrapper), `project@1.3.2`, `to-do@0.1.2`
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0` `v0.8.0` `v0.8.1` `v0.9.0` `v0.10.0` `v0.11.0` **`v0.12.0`** (NEW)
- Harness: `run-helper-cases.js` 255 sub-asserts (UNCHANGED — cards mechanism is renderer-only); `run-renderer.js` +6 BC cases (BC1-BC6); all 3 harnesses green (with one pre-existing T4.0 fatal — see pre-flight #7)
- Module-directory invariant codified at the installer level (T-FIX1)
- Landmine #12 allowlist: 3 paths; helper count: 4
- Filename pattern `<Name>-YYYY-MM-DD` invariant (gotcha 8) — applies to date-routed daily artifacts only; project artifacts are slug-routed
- Mobile-aware visual design as renderer-level posture (gotcha 7)
- In-cycle re-process bump rule (gotcha 9) — reinforced again by TWO inline-CF sub-stages in v0.12.0 (cards@0.2.1 + daily@0.2.1)
- NEW from v0.12.0: BeaconCards 0.2.0 API extension (`subtitle` polymorphism `string | null | { text, secondaryText }`; `badges[].icon` for inline-SVG chips; synthetic-page pattern codified in docstring); MeetingsHubCards rewritten ~159 LOC → ~111 LOC as thin BeaconCards wrapper; SpaceDailyDashboard meetings panel via BeaconCards; SpaceDailyDashboard tasks panel reverted to flat `<ul>` (cohesion-thesis bound for hubs only); v0.12.0 S2.2 dual-source-of-truth gap surfaced + codified (per-blueprint version bumps need lockstep edits to BOTH blueprint manifest AND workshop catalogue line)

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.12.0 baseline
> 1. **Git head + recent commits:**
>    ```bash
>    git -C /Users/willfell/Documents/obsidian/sync/workshop/poc-vault log --oneline -11
>    ```
>    Expected (top-down): `81acef2` v0.12.0 status snapshot update; `0bfdfb7` v0.12.0 cycle close result writeup; `c356f85` S3.4.1 daily 0.2.0 → 0.2.1 tasks-as-bullets revert; `59bf62f` S3 SpaceDailyDashboard panels migrated; `8bbdd05` S3 daily 0.1.4 → 0.2.0 manifest bump; `208b2fe` S2.4.1 cards 0.2.0 → 0.2.1 visual polish; `1d897b7` S2 MeetingsHubCards migrated; `b7ab2e3` S2 meetings 0.1.2 → 0.2.0 manifest bump; `19a8aed` S1 workshop bump 0.11.0 → 0.12.0 + cards 0.1.1 → 0.2.0; `e1e01a8` S1 BeaconCards 0.2.0 implementation; `b72801c` S1 failing BC1-BC6 renderer cases.
> 2. **Working tree:** `git status` → clean (or just `.bak` artifacts).
> 3. **Tags on origin:** `git ls-remote --tags origin | grep v0.12` → `v0.12.0` present + annotated.
> 4. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/platform/installer-stub.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-acc-vault/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 5. **Workshop manifest at 0.12.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.12.0"`.
> 6. **Workshop self-install green + idempotent:** `node platform/test/run-install.js .` → exit 0; second run → 0 new history entries.
> 7. **Harnesses:**
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 255 passed, 0 failed.`
>    - Renderer harness — use individual selectors (`empty`, `malformed`, `unknown-action`, `beacon-cards`) for regression sweeps. **NOTE the pre-existing T4.0 fatal:** `node platform/test/run-renderer.js` (no selector) fatals on a hardcoded `kanban-board.md` path deleted way back in v0.2.0 boards cycle. v0.12.0 chose to flag rather than fix; carry forward as a candidate cleanup for v0.12.x.
>    - All 6 BC cases (`beacon-cards-stacked-subtitle-string`, `beacon-cards-stacked-subtitle-object`, `beacon-cards-row-subtitle-object`, `beacon-cards-badges-with-icon`, `beacon-cards-badges-without-icon`, `beacon-cards-synthetic-page`) green.
> 8. **Barebones state:**
>    ```bash
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Scripts/cards/ 2>&1
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Scripts/meetings/ 2>&1
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Scripts/daily/ 2>&1
>    head -2 /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Scripts/meetings/meetings-hub-cards.js
>    head -2 /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Scripts/daily/space-daily-dashboard.js
>    grep -E '"(cards|meetings|daily)"' /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/platform-subscription.json
>    ```
>    Expected: `beacon-cards.js` present in cards/; `meetings-hub-cards.js` header docstring shows v0.2.0 "Thin wrapper around BeaconCards"; `space-daily-dashboard.js` header docstring shows v0.2.1 "tasks panel reverted from BeaconCards to bullet"; subscription pinned to `cards@0.2.1`, `meetings@0.2.0`, `daily@0.2.1`.

If any pre-flight fails, STOP and surface to user via `AskUserQuestion`.

---

## Required reads (~15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol. END-OF-SESSION CHECKLIST IS LOAD-BEARING.
> 2. **`Docs/plans/2026-05-04-v0.12.0-cards-cohesion-result.md`** — most recent cycle close. Captures (a) the TWO inline-CF sub-stages (cards@0.2.1 + daily@0.2.1); (b) the dual-source-of-truth gap (S2.2 → S2.3); (c) the bounded-cohesion thesis (hubs-yes / tasks-no); (d) audit-prediction validated (cards-mechanism-fit audit's "EXTEND with caution" verdict on tasks panel held); (e) TDD-first on renderer harness extensions. Read first to inherit cadence + lessons.
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.12.0 close.
> 4. **`blueprints i want.md`** — load-bearing for next-cycle pick. User's wishlist (journal / trips / finance / styling). Annotated with location of source patterns in headspace + ero vaults.
> 5. **`Docs/landmines.md`** — 14 entries unchanged.
> 6. **`Docs/how.md`** — read the optional manifest fields callouts; cards mechanism context not yet codified there (candidate for v0.5.1 docs cycle).
> 7. **`Docs/plans/2026-05-04-v0.12.0-cards-cohesion-design.md`** + plan — most recent design + plan structure (use as template if you reach those phases).
> 8. **`Docs/plans/2026-05-04-cards-mechanism-fit-audit.md`** — migration roadmap; tasks panel "EXTEND with caution" verdict was load-bearing in v0.12.0's S3.4.1 revert. Read if next cycle touches a card-style surface.
> 9. **(If you pick v0.6.0 journal blueprint)** Reference reads:
>     - `../headspace/Timestamps/Journal/Daily Journal, 2026-05-04-Monday.md` — pattern source.
>     - `platform/blueprints/daily/manifest.json` — closest-existing-pattern manifest (date-routed YYYY/MM-MMMM/ structure).
>     - `platform/blueprints/to-do/manifest.json` — runTemplaterTemplate + templater_folder_templates pattern.
> 10. **(If you pick v0.4.x.y dual-source-of-truth installer guard)** Reference reads:
>     - `platform/install.js` — see `installItem()` subscription-pin compare + manifest catalogue load.
>     - v0.12.0 result writeup Lessons learned #1 — surfaces the gap.

---

## Candidate cycles (pick ONE)

> [!example]- v0.6.0 journal blueprint **(Strong candidate; PRIMARY recommendation)**
> **What:** ship a NEW `journal` blueprint mirroring headspace's `Timestamps/Journal/Daily Journal, YYYY-MM-DD-DayOfWeek.md` pattern. Materializes under `beacon/journal/YYYY/MM-MMMM/Journal-YYYY-MM-DD.md` (daily-blueprint-style routing; filename pattern `<Name>-YYYY-MM-DD` invariant per gotcha 8). User-described scope: "fairly straight forward, simple to do."
>
> **Why:** continues the platform's blueprint catalog growth on the most user-visible axis. v0.12.0 foundations (BeaconCards 0.2.x, bounded-cohesion thesis, runTemplaterTemplate split-field schema, templater_folder_templates) all stable. Lowest-friction NEW blueprint cycle since v0.4.0 to-do. Smallest scope of the four wishlist candidates per user's annotation.
>
> **Estimated scope:** 1 stage. New blueprint manifest + 1 Templater template body + maybe a thin `JournalNavButton` action. No new mechanism work; no installer extension. Workshop bump 0.12.0 → 0.13.0 MINOR (new blueprint).
>
> **Open questions for brainstorming:**
> - Entry UX: nav-button action `runTemplaterTemplate`-style (mirrors to-do) or core-plugin override (mirrors daily)?
> - Does the journal note get a hub-style aggregator, or is it leaf-only?
> - SpaceDailyDashboard: does daily get a third panel ("Today's Journal")? If so, BeaconCards `layout: "stacked"` with `subtitle: { text: "<excerpt>", secondaryText: ... }`? Or a single inline link?
> - Frontmatter shape — minimal (just `created`, `tags`)? Match headspace's pattern verbatim?
> - Filename pattern: `Journal-YYYY-MM-DD.md` (gotcha 8 invariant) versus headspace's `Daily Journal, YYYY-MM-DD-Monday.md` (rejects gotcha 8 — has space + comma + DOW).

> [!example]- v0.6.x trips blueprint
> **What:** ship a `trips` blueprint mirroring `../headspace/boards/trips/Trips.md`. User wants per-trip "ecosystem" with sub-sections for flights + packing + per-trip board. Likely uses sub-module directory pattern under `beacon/trips/<trip-slug>/{flights, packing, board}.md`.
>
> **Why:** biggest scope of the wishlist candidates; first cycle that exercises module-sub-directory patterns (current invariant is one directory per blueprint). Codifies the "ecosystem of files per top-level entity" pattern that finance + future entity-blueprints would inherit.
>
> **Estimated scope:** 2-3 stages. New blueprint manifest with sub-folder routing. May surface a sub-module-directory installer extension. Workshop bump 0.12.0 → 0.13.0 MINOR.
>
> **Open questions for brainstorming:**
> - Sub-module shape: `beacon/trips/2026-Madrid/{flights, packing, board}.md` flat or `beacon/trips/2026-Madrid/sections/{flights, packing}/...`?
> - Entry UX: `New Trip` button → Templater prompt for name + dates → scaffolds the directory tree?
> - Does the per-trip board reuse the boards@0.1.0 blueprint primitives (Kanban-plugin board), or is it a BeaconCards row-layout board?
> - How does landmine #11 (one directory per blueprint) interact with sub-module routing? Codify a sub-invariant or stretch the existing one?

> [!example]- v0.6.x.y finance blueprint (personal + business hybrid)
> **What:** ship a `finance` blueprint covering BOTH personal day-to-day budgeting (mirroring `../headspace/Finance/Finance.md`) AND business invoicing (mirroring `../ero/Finance/Finance-Hub.md`). User explicitly notes "we can incorporate one financial blueprint that can handle the day to day budgeting / paycheck tracking, as well as have an invoicing system."
>
> **Why:** highest-utility for user's daily flow; touches budgets, paychecks, bills, and ERO-style invoices in a unified surface. Bigger scope than journal; needs design-first pass to reconcile personal-vs-business hybrid shape.
>
> **Estimated scope:** 2-3 stages. Likely needs a brainstorming-then-design-then-design-v2 round given hybrid scope. Could split into v0.6.x personal-finance + v0.6.x.y business-finance/invoicing sub-cycles. Workshop bump 0.12.0 → 0.13.0 MINOR.
>
> **Open questions for brainstorming:**
> - Hybrid shape: one blueprint with two surfaces, or two blueprints (`finance-personal` + `finance-business`)?
> - Recurring entries (monthly bills, biweekly paychecks) — Templater template? Background CustomJS class with auto-create?
> - Invoicing: per-invoice note files? Frontmatter-driven invoice ledger? PDF export integration (likely out of scope)?
> - Does it use BeaconCards (and what layout) for the hub view?
> - Bills aging / paid-status — frontmatter status field with a Dataview-driven view?

> [!example]- v0.6.x.y styling blueprint (deferred per user)
> **What:** ship a `styling` blueprint that bundles theme + CSS-snippet + Style Settings plugin config + Baseline community theme as a one-shot install. Codifies the per-attribute style-settings JSON the user has captured at `blueprints i want.md` lines 22-67.
>
> **Why:** completes the "set up a new beacon vault from zero in one install" UX. Touches CSS snippets directory + plugin data files for community-theme + Style Settings.
>
> **Estimated scope:** 2-3 stages; likely needs a new installer helper for community-theme installation + CSS-snippet drops + Style Settings plugin data merge (4th allowlist path? `style-settings/data.json` if the plugin uses it). Workshop bump 0.12.0 → 0.13.0 MINOR.
>
> **NOTE:** USER EXPLICITLY DEFERRED — "this is something that will probably need to be done down the road rather than now as I'd rather get blueprints in there, then refine onboarding with something like this that handles plugins, styling, etc." Don't start this without explicit user reversal.
>
> **Open questions for brainstorming:**
> - Installer support for CSS snippets — new helper analog to applyExternalPlugins?
> - Style Settings plugin data path — does it land in landmine #12 allowlist? Currently 3 paths; this would push to 4 (requires user approval).
> - Community theme distribution — fetch from the community-theme registry at install time, or bundle JSON locally?

> [!example]- v0.12.x T4.0 lazy-scaffold harness cleanup
> **What:** fix the pre-existing T4.0 fatal in `node platform/test/run-renderer.js` (no selector). Hardcoded `kanban-board.md` path was deleted in v0.2.0 boards cycle; the harness has been silently broken since. v0.12.0 chose to flag rather than fix.
>
> **Why:** restores `run-renderer.js` to a clean exit; removes one piece of "known-broken-but-flagged" friction from the harness suite. Small fix.
>
> **Estimated scope:** 0.5 stage. Pure harness cleanup; either remove the T4.0 case or update the path to a still-existing fixture. Workshop bump 0.12.0 → 0.12.1 PATCH (or no bump — harness-only).
>
> **Open questions for brainstorming:**
> - Remove T4.0 entirely (was the v0.2.0 boards-cycle leftover) or repoint to a still-current fixture?
> - Bundle with a docs cycle (v0.5.1 codification) for a single "polish" cycle?

> [!example]- v0.5.1 docs cycle: codify customjs-guard-wrap-BeaconCards + ICONS-map + pair-invariant + dual-source-of-truth
> **What:** pure docs. Document in `Docs/how.md`:
> - (a) any BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (project blueprint's ProjectNotesCards + ProjectReferencedByCards as the canonical pattern)
> - (b) the canonical ICONS map keys list (Surprise F from v0.5.0 follow-up)
> - (c) the renderer-installer pair-invariant (any new action_type needs validateAndResolve branch)
> - (d) the dual-source-of-truth gap from v0.12.0 (per-blueprint version bumps need lockstep BOTH manifest edits)
> - (e) the bounded-cohesion thesis (BeaconCards = hubs only; bullet lists for tasks)
>
> **Why:** institutional memory; the v0.11.0 CF-1 + v0.12.0 S2.2 lessons are currently captured only in result writeups. Lifting them into how.md prevents re-discovery friction.
>
> **Estimated scope:** 0.5 stage. Pure docs. Workshop bump 0.12.0 → 0.12.1 PATCH (or no bump — docs-only).
>
> **Open questions for brainstorming:**
> - Where in `Docs/how.md` does this live? Renderer subsection? Standalone "BeaconCards usage" section?
> - Bundle with v0.12.x T4.0 cleanup as a single docs+harness cleanup cycle?

> [!example]- v0.4.3 mobile install path investigation
> **What:** feasibility study for retiring landmine #8 (desktop-only installer). Currently `platform/install.js` uses `require("fs")` directly; mobile Obsidian doesn't expose Node's `fs`. Would require Obsidian Sync delivery + vault-adapter-only writes + a pure-Obsidian execution path.
> **Why:** opens beacon to Obsidian mobile users. Not a blueprint cycle; design + spike work. Renderer + class layer is already mobile-aware (gotcha 7); installer is the last desktop-only frontier.
> **Estimated scope:** 1 stage spike (no version bump if pure investigation; design output only). Could split into v0.4.3 design + v0.4.4 implementation cycles.
> **Open questions for brainstorming:**
> - Path 1 — fully replace `fs` with vault adapter calls (every read + write through `app.vault.adapter`). Big lift; touches every helper.
> - Path 2 — keep `fs` for the workshop side; build a SEPARATE Obsidian-side install path that consumers use. Two installers; risks divergence.
> - Path 3 — consumers receive prebuilt pack via Obsidian Sync; install becomes a one-shot apply. Simpler runtime; more complex build pipeline.

---

## Recommendation

> [!success] **v0.6.0 journal blueprint** — RECOMMENDED PRIMARY
> User's `blueprints i want.md` is now the canonical source for next-blueprint candidates. Journal is annotated as "fairly straight forward, simple to do" — smallest scope of the four wishlist candidates. v0.12.0 foundations (cards 0.2.1 mechanism, bounded-cohesion thesis, runTemplaterTemplate split-field schema, daily-blueprint date routing) all directly applicable. Lowest-friction NEW blueprint since v0.4.0 to-do.
>
> **Runner-up: v0.12.x T4.0 lazy-scaffold harness cleanup + v0.5.1 docs cycle bundled** — small "polish" cycle; restores `run-renderer.js` to clean exit AND lifts v0.11.0 + v0.12.0 lessons into how.md. Half-stage each; could bundle as one PATCH cycle (0.12.0 → 0.12.1) before the next NEW-blueprint cycle.
>
> **Skip for now (defer to dedicated cycles):** v0.6.x trips (biggest scope; design-heavy; better as a v0.7.0 spike), v0.6.x.y finance (hybrid scope needs design-first round; better paired with trips after journal lands), v0.6.x.y styling (USER EXPLICITLY DEFERRED), v0.4.3 mobile installer (large standalone spike).

---

## Cadence guidance (mirror v0.4.2 / v0.5.0 / v0.11.0 / v0.12.0)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel session)** after plan approval — user picks.
> - Per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - TDD discipline on harness extensions (renderer-harness BC1-BC6 precedent in v0.12.0 S1 — write failing case, commit, then implement).
> - Manual Obsidian smokes as the final gate.
> - Push to origin/main after each stage commit; annotated tag at cycle close.
> - **Cmd+R Obsidian after every Node-harness install when new CustomJS classes are introduced** OR when class bodies changed (v0.12.0 rewrote both MeetingsHubCards + SpaceDailyDashboard; reinforces the rule).
> - **Pre-flight: diff barebones platform-installed.json vs subscription** to surface stale-install pitfalls (gotcha 9).
> - **Inline-CF-extension pattern (v0.5.0 + v0.11.0 + v0.12.0 precedent — now THREE cycles strong):** when user surfaces polish items at S2/S3 close, address as S2.x/S3.x sub-stages with their own commits BEFORE cycle close, rather than deferring to a follow-up cycle. Use this pattern when user explicitly says "iron it all out before moving on." v0.12.0 was a TWO-inline-CF cycle (cards@0.2.1 visual polish + daily@0.2.1 tasks-as-bullets revert). Reserve `Y.Z.x` headroom upfront on any cycle expected to draw user feedback at smoke time.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), with `{{module_directory}}` overlay for blueprints. Helpers + template content can use `{{xxx}}` tokens (`substituteLenient` resolves at install time).
> - **Filename pattern `<Name>-YYYY-MM-DD` invariant** (gotcha 8) — applies to date-routed daily artifacts only. Project artifacts are slug-routed; gotcha 8 does NOT apply to project files. Date-extraction in CustomJS classes uses regex `\d{4}-\d{2}-\d{2}`.
> - **Mobile-aware visual design** (gotcha 7) — every CustomJS class designed for desktop AND mobile. Adaptive layouts, touch-friendly targets, no hover-only affordances. BeaconCards (chip-collapse on small screens; row-layout adapts) + MeetingsHubCards (post-v0.12.0 thin wrapper) are the new pattern references. Installer remains desktop-only (landmine #8 unchanged).
> - **In-cycle re-process bump rule** (gotcha 9) — when reusing in-cycle staged work at version X, in-cycle revisions to that work must bump X+1 to force consumer re-process. Pre-flight diff installed.json vs subscription before assuming a no-op. **Three-cycle precedent** (v0.5.0 + v0.11.0 + v0.12.0): reserve `Y.Z.x` headroom upfront on any cycle expected to draw user feedback at smoke time.
> - **Verify command ids against runtime** (gotcha 10) — when an `invoke_command` action ships, verify the actual command id via `Object.keys(app.commands.commands).filter(...)` in Obsidian DevTools. Don't trust naming conventions.
> - **Cmd+R Obsidian post-Node-harness-install** when introducing OR rewriting CustomJS classes.
> - Module-directory invariant under `beacon/` (#11) — codified at installer level via T-FIX1's mkdir.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfoods every release.
> - Desktop-only installer (#8).
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path, editing a consumer's `platform-installed.json` by hand.
> - **v0.5.0 lesson:** when porting CustomJS classes that hardcode template paths, USE `{{templates_path}}` token (installer substitutes at install time) NOT hardcoded `Docs/Meta/Templates`.
> - **v0.11.0 lesson:** for any new renderer action_type, ALSO add a `validateAndResolve` branch in install.js OR placeholders won't substitute at install time (carried forward from v0.5.0 CF-1).
> - **v0.11.0 lesson:** BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers; never direct `customJS.BeaconCards.render(dv, ...)` callsite (landmines #1, #2 cold-load TDZ). Ship a thin wrapper class per use-site that internally calls BeaconCards. Canonical examples: `ProjectNotesCards` + `ProjectReferencedByCards` + (v0.12.0) `MeetingsHubCards`.
> - **v0.11.0 lesson:** for row-layout cards, `icon` getter returns inline SVG HTML; `meta` getter returns HTML rendered right of title row; `subtitle` indents past the icon. Match accuris Planning-Board pattern.
> - **v0.12.0 lesson (NEW): dual-source-of-truth for blueprint versions.** Per-blueprint version bumps require lockstep edits to BOTH the blueprint's own `manifest.json` AND the workshop catalogue line in `platform/manifest.json` (~line 20 area). The installer reads versions from the workshop catalogue (NOT the blueprint manifest) for subscription-pin compares. Missing the catalogue edit produces a `subscription pins X@Y but workshop has Z` skip — silently masquerades as a no-op. Future cycles should bake a "manifest-pair edit" check into any "bump X" task.
> - **v0.12.0 lesson (NEW): bounded-cohesion thesis — BeaconCards is right for hubs, NOT for tasks.** BeaconCards is the right primitive for hub-style listings (meetings hub, project hub, daily meetings panel) where each item is a real file with title/subtitle/meta worth surfacing visually. Bullet lists remain the right primitive for compact at-a-glance task panels where each line item is just text + click-to-jump. Don't reach for BeaconCards for inline task / checklist / queue surfaces. Audit-flagged "EXTEND with caution" verdicts warrant a brainstorm gate BEFORE migrating.

---

## Cross-cutting gotchas codified during recent cycles (READ BEFORE STARTING)

> [!warning] Gotchas surfaced 2026-05-03 / 2026-05-04 — load-bearing for any cycle touching runTemplaterTemplate, CustomJS classes, invoke_command, new renderer action_types, or BeaconCards
>
> **Gotcha 1: Bracket-escape gap on runTemplaterTemplate folder/filename.** RESOLVED at v0.4.2 S1 — split-field schema makes literal text never reach `moment.format()`. Manifests still using legacy `folder` / `filename` fields are silently ignored (BREAKING). Future blueprints inherit the safety for free.
>
> **Gotcha 2: Node-harness install + Obsidian's plugin auto-rescan.** `platform/test/run-install.js` writes via `fs` (Node), bypassing Obsidian's vault adapter. Obsidian's CustomJS plugin watches the vault adapter for change events to auto-rescan its jsFolder. After a Node-harness install adds new files OR rewrites class bodies, the user MUST Cmd+R Obsidian to force plugin re-init. Smoke checklists for any cycle adding OR rewriting CustomJS classes should explicitly call out the Cmd+R step.
>
> **Gotcha 3: T-FIX1 mkdir module_directory at install time** (v0.3.0 codified). Every blueprint with `module_directory` set gets that directory mkdir'd between `applyPreInstall` and `files[]` loop. Mechanisms exempt.
>
> **Gotcha 4: Force re-process via installed.json edit (CLAUDE.md ask-before-acting).** `install.js:111` short-circuits per-item install when `subscribed.version === installed.version`. Tests/cycles that need to exercise behavior INSIDE `installItem()` must triple-bump versions OR drop the entry from `<consumer>/Docs/Meta/platform-installed.json:installed[]` (CLAUDE.md ask-before-acting; user-approved).
>
> **Gotcha 5: User-override preserved on folder_templates + nav-button registry overwrites.** `applyTemplaterFolderTemplates` preserves user-customized template paths. `applyNavButtons` REPLACES the contributions[<source>] array on each install. Existing `folder_templates` entries from prior installs may persist as user-overrides — cosmetic clutter but functionally inert.
>
> **Gotcha 6: Mid-cycle redesign methodology.** When smokes surface a misalignment with the user's mental model, ACCEPT the redesign rather than ship the wrong primitive. Workflow: pause SDD → invoke `de:brainstorming` → present revised design → get approval → invoke `de:writing-plans` → resume SDD. Used four times now (v0.3.2 v1→v2; v0.4.2 v1→v2; v0.5.0 inline S2.x sub-stages; v0.12.0 S3.4.1 tasks-as-bullets revert). Reinforced via v0.11.0's SIX-bump iteration. Pattern is reproducible.
>
> **Gotcha 7: Mobile-aware visual design as platform value (v0.4.2; reinforced v0.5.0 + v0.11.0 + v0.12.0).** Every renderer (CustomJS class) MUST be designed for both desktop AND mobile. Adaptive layouts, touch-friendly targets, no hover-only affordances. BeaconCards 0.2.x (chip-collapse on small screens; row-layout adapts; subtitle two-line-wrap word-break preserved) is the canonical pattern reference. Installer remains desktop-only (landmine #8 unchanged); the visual contract is what changes.
>
> **Gotcha 8: Filename pattern `<Name>-YYYY-MM-DD` invariant (v0.4.2).** All date-routed daily artifacts use date-suffix; hyphen-only separator; no spaces. Date-extraction in CustomJS classes uses regex `\d{4}-\d{2}-\d{2}`. **NOTE:** project artifacts are slug-routed (NOT date-routed) — gotcha 8 does NOT apply to project files.
>
> **Gotcha 9: In-cycle re-process bump rule (v0.4.2; reinforced v0.5.0 + v0.11.0 + v0.12.0).** When reusing in-cycle work staged at version X, in-cycle revisions to that work must bump to version X+1 to force consumer re-process. v0.5.0 was a SIX-bump cycle (project 0.3.0 → 1.2.2). v0.11.0 was a SIX-bump cycle. v0.12.0 was a TWO-inline-CF cycle (cards@0.2.1 + daily@0.2.1). **Pattern is now strong evidence over THREE consecutive cycles:** any cycle with anticipated polish/feedback should reserve "Y.Z.x" headroom upfront. Pre-flight diff installed.json vs subscription before assuming a no-op.
>
> **Gotcha 10: Verify invoke_command command ids against runtime (v0.4.2).** When designing an invoke_command action, verify the actual command id via Obsidian DevTools (`Object.keys(app.commands.commands).filter(k => k.includes('<keyword>'))`). Don't trust naming conventions or Obsidian docs; use the live runtime as truth.
>
> **Gotcha 11 (NEW v0.12.0): Dual-source-of-truth for blueprint versions.** Per-blueprint version bumps require lockstep edits to BOTH (a) the blueprint's own `manifest.json` AND (b) the workshop catalogue line at `platform/manifest.json` (~line 20 — the `blueprints[]` entry's `version` field). The installer reads versions from the workshop catalogue (NOT the blueprint manifest) for subscription-pin compares. v0.12.0 S2.2 missed the catalogue edit; first barebones install run skipped meetings with `subscription pins meetings@0.2.0 but workshop has 0.1.2` — surfaced as a silent no-op. Caught + fixed in S2.3. **Action item:** any "bump <blueprint>" task must include both edits as a single edit pair. Future systemic fix candidate (v0.12.x?): single-source-of-truth schema where workshop catalogue versions are derived from blueprint manifests at install time.
>
> **NEW pattern (v0.5.0, not yet a numbered gotcha): renderer dispatcher action_types must have matching install.js validateAndResolve branches.** v0.5.0 surfaced openLink as dead-code installer-side — `target` literal `{{module_directory}}` was reaching the registry unresolved because the openLink branch didn't substitute. Landed CF-1. **Lesson:** for any new renderer action_type, ALSO add a `validateAndResolve` branch in install.js OR placeholders won't substitute at install time.
>
> **NEW pattern (v0.11.0, not yet a numbered gotcha): BeaconCards-via-customjs-guard wrapping is the canonical dataviewjs callsite.** Direct `customJS.BeaconCards.render(dv, ...)` from a `dataviewjs` block violates landmines #1/#2 (cold-load TDZ on `customJS`). The cohesion pattern: ship a thin wrapper class per use-site that internally calls BeaconCards, then invoke it via `dv.view("Docs/Meta/Views/customjs-guard", { class: "<WrapperName>" })`. v0.11.0 examples: `ProjectNotesCards` + `ProjectReferencedByCards`. v0.12.0 example: `MeetingsHubCards` rewritten as canonical thin wrapper (~159 → ~111 LOC). Carry forward to every future BeaconCards consumer.
>
> **NEW posture (v0.12.0): BeaconCards = hubs only; bullet lists for tasks.** v0.12.0 S3.4.1 reverted SpaceDailyDashboard tasks panel from BeaconCards to a flat `<ul>`, validating the cards-mechanism-fit audit's "EXTEND with caution: card grid for tasks would be visually heavier than the current at-a-glance list" verdict. **Posture:** BeaconCards is the right primitive for hub-style listings where each item is a real file with title/subtitle/meta worth surfacing visually. Bullet lists remain the right primitive for compact at-a-glance task panels (text + click-to-jump). Don't reach for BeaconCards for inline task / checklist / queue surfaces. Audit-flagged "EXTEND with caution" verdicts warrant a brainstorm gate BEFORE migrating.

---

## Suggested first move

> [!todo] Recommended path
> 1. Run all 8 pre-flight checks. Surface discrepancies first (especially the T4.0 fatal — known issue, flag in advance).
> 2. Read the v0.12.0 result writeup + CLAUDE.md status snapshot + `blueprints i want.md` + landmines + how.md (~15 min).
> 3. **Surface the cycle pick to user via `AskUserQuestion`** — present the 7 candidates above + recommendation; lock the choice before opening brainstorming. Note `blueprints i want.md` annotations (journal = "fairly straight forward, simple to do"; styling = "down the road"; trips/finance = bigger scope).
> 4. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions listed for that candidate.
> 5. After brainstorming approval, invoke `de:writing-plans` to author the implementation plan.
> 6. After plan approval, invoke `de:subagent-driven-development` (same session) OR ask user to start a parallel session with `de:executing-plans`.
> 7. Manual Obsidian smokes at S2/S3 close (with Cmd+R prerequisite — every cycle from here on out, given BeaconCards is now in play).
> 8. Cycle close: result writeup + CLAUDE.md status snapshot + commit + tag + push branch + tag + post-<cycle> next-cycle handoff. Per `_handoff-protocol.md`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.12.0 next cycle. Read Docs/prompts/2026-05-04-post-v0.12.0-next-cycle-handoff.md FIRST — the full onboarding doc with project identity, 8 pre-flight checks, 7 candidate cycles with recommendation (v0.6.0 journal blueprint primary; v0.12.x T4.0 cleanup + v0.5.1 docs cycle bundled runner-up), constraints carried forward, and cross-cutting gotchas (now 11 numbered + 3 patterns).

After reading the handoff doc:
1. Run all 8 pre-flight checks. Note the pre-existing T4.0 fatal on `node platform/test/run-renderer.js` (no selector) — known issue carried forward from v0.12.0; use individual selectors for regression sweeps.
2. Read v0.12.0 result writeup + CLAUDE.md status snapshot + blueprints i want.md (load-bearing for next-cycle pick) + landmines + how.md.
3. Surface the 7 cycle candidates via AskUserQuestion with the recommendation. Get pick before brainstorming. Reference `blueprints i want.md` annotations: journal = "fairly straight forward, simple to do" (PRIMARY); trips + finance = bigger scope; styling = explicitly deferred by user.
4. Once locked, brainstorm: scope, locked decisions, open questions resolution, version bump, stage breakdown.
5. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks; stage-close AskUserQuestion checkpoints. TDD on harness extensions (renderer-harness BC1-BC6 precedent in v0.12.0 S1). Push to origin/main after each stage commit; annotated tag at cycle close. Cmd+R Obsidian after Node-harness install when CustomJS classes appear OR class bodies change. Pre-flight diff barebones installed.json vs subscription. Inline-CF-extension pattern (v0.5.0 + v0.11.0 + v0.12.0 precedent — three cycles strong): when user surfaces polish at S2/S3 close, address as S2.x/S3.x sub-stages before cycle close. Reserve Y.Z.x headroom upfront — v0.12.0 was a TWO-inline-CF cycle.

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to 4th path) requires approval; editing barebones platform-installed.json by hand requires approval; mobile-aware visual design (gotcha 7) for any new CustomJS class; filename pattern <Name>-YYYY-MM-DD invariant (gotcha 8) for date-routed daily artifacts (NOT for project artifacts); verify invoke_command command ids against live runtime (gotcha 10); for any new renderer action_type, ALSO add validateAndResolve branch in install.js (v0.5.0 lesson); BeaconCards usage from dataviewjs MUST go through customjs-guard wrappers (v0.11.0 CF-1 lesson — never direct callsite); per-blueprint version bumps require LOCKSTEP edits to BOTH blueprint manifest AND workshop catalogue line (v0.12.0 gotcha 11); BeaconCards = hubs only / bullet lists for tasks (v0.12.0 bounded-cohesion thesis).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
