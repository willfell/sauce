---
purpose: Resume-prompt + state snapshot for the session that picks up after v0.98.2 ships + deploys. The feedback-loop-closure cycle (v0.98.2) lands the third and CLOSING cycle of the v0.98.x substance arc — reconciler ingest + downvote capture + voice proposals + learned_weights schema 3 (nested entity maps). Successor v0.99.0+ shifts focus to observability + cross-machine durability now that the compounding loop is live.
load_when: Starting a fresh session on/after 2026-06-11 to pick the next cycle after v0.98.2.
status: authoritative as of 2026-06-11 (v0.98.2 S3 cycle-close + this commit)
prior_cycle: v0.98.2 feedback-loop closure MINOR
result_doc: Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-result.md
design_doc: Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-design.md
vision: Docs/cowork-vision.md
ecosystem: Docs/cowork-ecosystem.md
---

# Post-v0.98.2 next-cycle handoff

This doc is the session-start reference for the FIRST conversation after v0.98.2 closes + deploys. The feedback-loop-closure cycle (v0.98.2) shipped the third move of the v0.98.x substance arc:

1. **v0.98.0** — synopsis-density rewrite (predictive lead, collapsed per-kind, closing removed)
2. **v0.98.1** — questionnaire expansion + free-text capture (per-item ticks + knobs + fenced free-text; v=1 sentinel)
3. **v0.98.2** ⭐ — reconciler ingest + downvote capture + voice proposals (v=2 sentinel; learned_weights schema 3; nested per-kind entity maps; tick-to-approve voice updates; audit trail)

The v0.98.x arc is now CLOSED. The compounding-assistant loop is LIVE: ticks → reconciler rollup → entity-weight deltas → tomorrow's consumption thresholds. Successor (v0.99.0+) shifts to observability + cross-machine durability.

## State verification (run first)

```bash
cd /Users/willfellhoelter/projects/repos/sauce
git fetch origin && git status
git log --oneline -5
git rev-parse v0.98.2^{commit}                              # Expect: tag present
jq -r .version package.json                                  # Expect 0.98.2
jq -r .workshop_version platform/manifest.json               # Expect 0.98.2
jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # Expect 0.38.0
jq -r .version platform/blueprints/cowork/manifest.json      # Expect 0.38.0
jq -r '.contract_version' platform/blueprints/cowork/data/scheduled-job-contract.json # Expect 0.35.1 (UNCHANGED)
jq -r '.properties.schema_version.enum' platform/blueprints/cowork/data/schemas/eod-review@1.0.0.json   # Expect ["1.0.0","1.1.0","1.2.0"]
brew list sauce --versions                                   # Expect 0.98.2
for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
             /Users/willfellhoelter/notes/sauce/headspace-sauce; do
  echo "=== $vault ==="
  jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
  jq -r '.blueprints[] | select(.name == "cowork") | "\(.name): \(.version)"' "$vault/ranch/platform-subscription.json"
done
```

Expected: HEAD on origin/main; tag `v0.98.2` present; brew sauce 0.98.2; both consumer vaults at workshop_version 0.98.2 + cowork 0.38.0; scheduled-job-contract `contract_version` 0.35.1 (UNCHANGED — landmine #20 lockstep applies on contract DATA shape; this cycle is OI-content-only); eod-review sidecar schema enum carries `1.2.0`.

**Accuris catch-up note:** the local accuris vault sat at 0.98.0 / 0.36.0 (updates run on the work machine). ONE `sauce update --bump-pins` on the local copy catches BOTH v0.98.1 AND v0.98.2 deltas in a single pass. Confirm both `compose-feedback-capture-helper.js` (v0.98.1) AND `ingest-feedback-helper.js` (v0.98.2) are materialized post-update.

## User vision verbatim (2026-06-10 brainstorm)

The north star for the v0.98.x substance arc. Now fully satisfied across the three cycles:

> "just saying thumbs up isn't enough. what if we added a section for didn't like? what if there was a section where i could provide feedback, and another job took what i liked, what i didn't like, my feedback, then updated the relevant files so that all scheduled jobs would then adjust the next day?"

- "a section for didn't like" → v0.98.2 v=2 Rail L per-kind Didn't-like list (same item-IDs as Mattered list, section-context state machine in parser)
- "a section where i could provide feedback" → v0.98.1 fenced `` ```feedback…``` `` free-text block
- "another job took what i liked, what i didn't like, my feedback" → v0.98.2 `cowork:ingest-feedback` (deterministic rollup + inline LLM intent extraction in the existing nightly reconciler)
- "updated the relevant files so that all scheduled jobs would then adjust the next day" → v0.98.2 learned_weights schema 3 + microscope appends + voice proposals + next-morning consumption thresholds

## v0.98.x arc — substance, not structure (CLOSED)

| Cycle | Scope | Slot |
| --- | --- | --- |
| **v0.98.0** | synopsis-density rewrite MINOR — brief lead `[!info]+ <per-cadence title>` OPEN by default, ≤80-word predictive; per-kind callouts collapsed; bottom closing REMOVED | **SHIPPED 2026-06-10** |
| **v0.98.1** | questionnaire expansion + free-text capture MINOR — stable item-IDs + per-item ticks + per-kind frequency knobs + fenced free-text + sentinel v=1 | **SHIPPED 2026-06-11** |
| **v0.98.2** ⭐ | **feedback-loop closure MINOR** — Rail L v=2 (+ Didn't-like list); reconciler ingest steps 3 (BOTH sentinels) / 3.5 (deterministic rollup) / 3.6 (inline LLM intents) / 5.5 (non-weight deltas) / 5.6 (voice-proposal sweep); learned_weights schema 2 → 3 nested per-kind entity maps; voice changes PROPOSED (never auto-applied), tick-to-approve in next morning brief; coverage queue + feedback-deltas audit log; eod-review sidecar 1.2.0 with items[] identity registry | **SHIPPED 2026-06-11** |
| **v0.98.2.x PATCH** | (Reserved) — `cowork:doctor` (if dogfood week surfaces sidecar conformance anomalies per result-doc Discovery #5); per-item ticks on weekly/monthly; strict-mode adoption for helpers (per Discovery #2) | OPTIONAL (PATCH) — depends on first-week grading |
| **v0.99.0+** | Cross-machine wrappers + `cowork:doctor` observability skill + entity-weight live-grading after 7 days + per-item ticks on weekly/monthly (future) + strict-mode adoption + the v0.95.1-migrator carry-forward | NEXT (queued) |

## Open questions for the v0.99.0+ brainstorm

The v0.99.0 brainstorm should resolve these before the design is locked:

1. **Entity-weight live-grading results (seed: 7-day post-deploy data).** After 7 days of dogfood, do `per_kind.<kind>.per_person.<name>.weight` values move in directions matching the user's qualitative read? If not, the +0.05 / −0.10 / decay constants need re-tuning. Open question: ship the tuning as a v0.98.2.x PATCH or fold into v0.99.0 alongside `cowork:doctor`? Tuning IS data-driven; doctor lets you see the data first.

2. **`cowork:doctor` scope (incl. the sidecar-validation-bypass discovery #5).** Per the v0.98.2 result-doc Discovery #5: the live headspace 2026-06-10 sidecar's `coverage_gap: []` shape violation slipped past the deterministic write-guard at fire time. The doctor should at minimum scan sidecar conformance across the corpus and emit anomaly callouts. Open scope questions: (a) Does the doctor ALSO grade learned_weights movement (closes Q1 above)? (b) Does it surface in-brief (collapsed callout) or as a separate `cowork:doctor` cadence? (c) Where does it WRITE observations — `memory/<eng>/doctor-log.md` or somewhere else? (d) Does it run on the existing reconciler cadence (free) or as a 6th scheduled job (independent fire time)?

3. **Cross-machine wrapper lockfiles.** Wrappers still bake `/Users/willfellhoelter` paths at sync time; two machines online when a cron fires race-condition (first-to-write wins). Open question: lockfile semantics + path-relative wrappers in one cycle, or split (lockfile in v0.99.0; path-relative in v0.99.1)? Lean: bundle them. Lockfile-without-relative-paths is a half-fix; relative-paths-without-lockfile leaves the race.

4. **v0.95.1-migrator carry-forward.** Still parked from v0.96.0+ — the `sauce update --migrate-config` cross-machine path that handles pre-v0.95.0 vault-config.md shapes hasn't been needed in production. Open question: ship preemptively in v0.99.0+ (defensive), or wait until a real-world cross-machine consumer upgrade requires it? Lean: defer until needed. The design + plan are execution-ready; ship-on-demand is fine.

## Carry-forward items (ready-to-go work)

### v0.98.2.x PATCH candidates

- **`cowork:doctor` observability skill.** Surfaced concretely by Discovery #5 — sidecar-validation-bypass at fire time. Architecturally fits inside the existing reconciler cadence (new Step 6 or a parallel runner). PATCH-sized if scoped to sidecar conformance only; MINOR if it also grades learned_weights movement.
- **Per-item ticks on weekly / monthly cadences.** v0.98.2's Rail L v=2 is EOD-only. Additive extension if post-deploy data shows demand.
- **Strict-mode adoption for helpers.** Discovery #2 (implicit global from missing `let m;`) would have been caught at parse time with `"use strict"`. Workshop-wide audit + adopt across all helpers. PATCH-sized hygiene.

### v0.99.0+ items

- **Cross-machine wrappers.** Vision § 2 dimension 8 (Cross-machine consistency, current grade C). v0.99.0+ candidate; see Q3 above.
- **`cowork:doctor` observability skill.** Vision § 2 dimension 6 (Quality observability, current grade F). v0.99.0+ candidate; see Q2 above.
- **Entity-weight live-grading after 7 days.** First time the platform can grade itself empirically — entity-weight maps populate over the first 7-30 days post-deploy. v0.99.0+ candidate; see Q1 above.
- **Per-item ticks on weekly / monthly cadences.** Promoted from PATCH-candidate if dogfood data shows demand.
- **Strict-mode adoption for helpers.** Promoted from PATCH-candidate; workshop-wide hygiene.
- **v0.95.1-migrator carry-forward.** Parked from v0.96.0+. v0.99.0+ candidate when a real-world consumer-vault upgrade path requires it; see Q4 above.

## Decision branch for the next session

**Default (recommended): brainstorm v0.99.0 — observability + cross-machine durability.** Resolve the 4 open questions above before committing the design. MINOR sized; the compounding loop is live, so cycle #1 post-loop is observability (grade what we shipped) before cross-machine (durability without lockfiles is fragile).

**Alternative: brainstorm v0.98.2.x PATCH** — only viable if first post-deploy 48h has surfaced sidecar conformance anomalies (Discovery #5 class) OR voice-proposal volume is too high/low (LLM intent-extraction tuning). Faster than v0.99.0 but lower leverage.

**Alternative: hold for first-week grading data.** No new cycle; let the v0.98.2 deploy run for 7 days while collecting entity-weight movement + voice-proposal volume + sidecar conformance data. Then v0.99.0 brainstorm has empirical inputs. Lean: this is the right call if the user can defer; the v0.98.x arc just closed and the post-loop empirical signal is unprecedented.

## Resume prompt (paste into fresh session)

```
Resume sauce workshop work post-v0.98.2 close (2026-06-11).

REPO: /Users/willfellhoelter/projects/repos/sauce (workshop vault, NOT a
consumer). Run vault-identity check per CLAUDE.md before any write.

AUTHORITATIVE STATE DOC (READ FIRST, END-TO-END):
  Docs/prompts/2026-06-11-post-v0.98.2-next-cycle-handoff.md

ALSO LOAD-BEARING (read in order after the state doc):
  1. Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-result.md
     (result doc — what shipped, 6 discoveries/lessons, 4 open v0.99.0
     brainstorm questions)
  2. Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-design.md
     (design — § Absorption discoveries; § Architecture overview;
     § Out-of-scope guards; § Capture v=2 contract)
  3. Docs/cowork-ecosystem.md (post-v0.98.2 ground-truth walkthrough:
     § 7.2.4 SHIPPED; § 7.2.5 user-preferences.md frontmatter +
     schema 3; § 9 reconciler ingest pipeline)
  4. Docs/plans/2026-06-11-v0.98.1-questionnaire-capture-{design,result}.md
     (immediate predecessor — capture surface)
  5. Docs/cycle-history.md § v0.98.2 (cycle narrative)
  6. Docs/cowork-vision.md (long-term direction + locked decisions;
     v0.98.2 row marked SHIPPED at v0.98.2 close; § 2 dimensions 3/6/8
     for v0.99.0+ targets)
  7. Docs/agent-guides/cycle-status.md (live workshop catalogue at 0.98.2)
  8. Docs/landmines.md (22 non-negotiable traps)

STATE VERIFICATION (per the state doc § "State verification"):
  git log --oneline -5
  git rev-parse v0.98.2^{commit}
  jq -r .version package.json                                  # 0.98.2
  jq -r .workshop_version platform/manifest.json               # 0.98.2
  jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # 0.38.0
  jq -r '.contract_version' platform/blueprints/cowork/data/scheduled-job-contract.json # 0.35.1 UNCHANGED
  jq -r '.properties.schema_version.enum' platform/blueprints/cowork/data/schemas/eod-review@1.0.0.json  # ["1.0.0","1.1.0","1.2.0"]
  brew list sauce --versions                                   # 0.98.2
  for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
               /Users/willfellhoelter/notes/sauce/headspace-sauce; do
    echo "=== $vault ==="
    jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
    jq -r '.blueprints[] | select(.name == "cowork") | "\(.name): \(.version)"' "$vault/ranch/platform-subscription.json"
  done

EXPECTED STATE:
  HEAD: post-v0.98.2 close.
  Tag v0.98.2 present.
  brew sauce: 0.98.2.
  Both consumer vaults: workshop_version 0.98.2 + cowork 0.38.0.
  scheduled-job-contract: contract_version 0.35.1 (UNCHANGED — landmine
  #20 applies on contract DATA shape; this cycle was OI-content-only).
  eod-review sidecar schema enum: ["1.0.0","1.1.0","1.2.0"] (additive;
  v=2 sentinel + items[] + ambiguous_items[]).
  Accuris LOCAL copy may have needed a two-version catch-up
  (0.98.0 → 0.98.2 in one --bump-pins invocation; verify both
  compose-feedback-capture-helper.js AND ingest-feedback-helper.js are
  materialized).

TASK FOR THIS SESSION (default): brainstorm v0.99.0 — observability +
cross-machine durability per the v0.98.2 result doc § Carry-forward.

The recommended next cycle is v0.99.0 with FOUR open questions to
resolve at brainstorm:

  (1) Entity-weight live-grading results — after 7 days of dogfood,
      tune +0.05 / −0.10 / decay constants? PATCH or fold into v0.99.0?

  (2) cowork:doctor scope — minimum (sidecar conformance only) vs
      max (also grade learned_weights movement + log location +
      embed cadence vs separate fire)? Concretely seeded by the
      result-doc Discovery #5 sidecar-validation-bypass.

  (3) Cross-machine wrapper lockfiles + path-relative wrappers —
      bundle in one cycle or split v0.99.0 / v0.99.1?

  (4) v0.95.1-migrator carry-forward — ship preemptively or
      defer-until-needed? Lean: defer.

USER VISION (verbatim, 2026-06-10 — now fully satisfied across v0.98.x):
  "just saying thumbs up isn't enough. what if we added a section for
   didn't like? what if there was a section where i could provide
   feedback, and another job took what i liked, what i didn't like, my
   feedback, then updated the relevant files so that all scheduled jobs
   would then adjust the next day?"

Invoke superpowers:brainstorming with the brief:

  "Brainstorm v0.99.0 — observability + cross-machine durability.
   Resolve the 4 open questions: (a) entity-weight live-grading
   tuning (PATCH or fold); (b) cowork:doctor scope (minimum vs max;
   embed vs separate fire); (c) cross-machine wrapper lockfiles +
   path-relative wrappers (bundle vs split); (d) v0.95.1-migrator
   ship-vs-defer.

   The v0.98.x compounding loop is LIVE. v0.99.0 is the first cycle
   that can GRADE what we shipped (cowork:doctor) + GUARANTEE durability
   across machines (cross-machine wrappers). Vision dimensions advanced:
   6 (Quality observability, current grade F → B target); 8 (Cross-
   machine consistency, current grade C → A target).

   Apply v0.98.2 lessons: (1) plan fixture hashes must be computed not
   invented (mark placeholders with compute-me); (2) when rewriting
   PART of a function, grep the remainder for identifiers the removed
   lines declared — strict-mode adoption queued; (3) intentionally-
   parallel parser implementations need cross-reference comments;
   (4) verify NAMED helper signatures at PLAN-time (extends the
   verify-helpers-before-design rule to plans); (5) deterministic
   gates are advisory unless gate-invocation is OBSERVABLE
   (motivates cowork:doctor); (6) new substitution tokens must be
   registered in KNOWN_SUBSTITUTION_TOKENS allowlist in the same
   commit."

OPEN ALTERNATIVES (if user redirects):
  - v0.98.2.x PATCH (cowork:doctor minimum, or LLM intent-extraction
    tuning, or strict-mode adoption) — only if first post-deploy 48h
    surfaced sidecar anomalies OR voice-proposal volume drift
  - Hold for first-week grading data — no new cycle; let v0.98.2
    deploy run 7 days while collecting empirical signal. Then v0.99.0
    brainstorm has data inputs.

SAUCE CONVENTIONS (NON-NEGOTIABLE):
  - Direct-push to origin/main only after S4 user-approval gate.
  - One commit per stage; conventional commits; explicit git add <paths>
    (NEVER -A).
  - NO Co-Authored-By trailer in commit messages.
  - Vault-identity check before any write.
  - 22 landmines (Docs/landmines.md) apply.

START with the state verification commands above. Report what you find.
Then begin the v0.99.0 brainstorm (or alternative path if user redirects).
```

## Cross-references

- **v0.98.2 result doc:** `Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-result.md` — what shipped + 6 discoveries/lessons + 4 v0.99.0+ open questions + post-deploy operational sequence
- **v0.98.2 design:** `Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-design.md` — § Absorption discoveries; § Architecture overview; § Capture v=2 contract; § Out-of-scope guards
- **v0.98.2 plan:** `Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-plan.md`
- **v0.98.1 design/result:** `Docs/plans/2026-06-11-v0.98.1-questionnaire-capture-{design,result}.md` — capture surface predecessor
- **Ecosystem walkthrough (post-v0.98.2 ground truth):** `Docs/cowork-ecosystem.md` — § 7.2.4 SHIPPED; § 7.2.5 user-preferences.md frontmatter + schema 3; § 9 reconciler ingest pipeline
- **Cycle history v0.98.2:** `Docs/cycle-history.md` § v0.98.2
- **Live cycle status:** `Docs/agent-guides/cycle-status.md` (workshop 0.98.2)
- **North Star:** `Docs/cowork-vision.md` (v0.98.2 row marked SHIPPED at v0.98.2 close; § 2 quality bar dimensions 3/6/8 frame the v0.99.0+ targets)
- **Prior next-cycle handoff (post-v0.98.1):** `Docs/prompts/2026-06-11-post-v0.98.1-next-cycle-handoff.md`
- **install.md Upgrading section:** `commands/install.md` (4 user-content preservation safeguards UNCHANGED from v0.98.1; v0.98.2 deploy note added)
- **Landmines:** `Docs/landmines.md` (22 entries; #20 lockstep — contract DATA unchanged at 0.35.1; #11 module-directory invariant — all v0.98.2 writes confined to `spice/cowork/`)
