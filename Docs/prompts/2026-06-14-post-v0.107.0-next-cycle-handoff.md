---
date: 2026-06-14
kind: post-cycle-handoff
predecessor: v0.107.0 finance defaults + groups + budget summary
---

# Post-v0.107.0 handoff — next cycle pickup

## What just shipped

v0.107.0 — finance defaults + groups + BudgetSummary, with one in-cycle CF-1 PATCH (finance 0.5.0 → 0.5.1; install posture fix for headless YAML mutation).

- workshop 0.106.0.1 → **0.107.0** MINOR
- finance 0.4.0 → **0.5.1** (MINOR + 1 CF)
- entity-create 0.6.0 → **0.7.0** MINOR ADDITIVE (`seed_from_defaults` schema field)

## Manual smoke checklist for the user (ordered)

Run on **headspace** vault first (it's the only consumer with active budget data).

1. **Cmd+R Obsidian** in the headspace vault. Should load 16 finance CustomJS classes (13 existing + 3 new: BudgetDefaultsEditor, PaycheckDefaultsEditor, BudgetSummary).
2. **Open `spice/finance/Budget Defaults.md`**. `BudgetDefaultsEditor` renders with empty state ("No groups yet. Click + Add Group."). Two panes visible.
3. **Add 4 groups via dialog**: Essential / Extra / Neutral / Travel. Each group should appear in the Groups pane with `✎ ↑ ↓ ×` controls. Try reorder ↑↓ — order persists in `groups[]` frontmatter.
4. **Add 5 categories** with the Group dropdown (one per group + an extra). The dropdown must show the 4 groups you just added.
5. **Try deleting a group that has categories** — dialog should block and offer "Reassign N categories to:" picker. Cancel = group stays.
6. **Open `spice/finance/Paycheck Defaults.md`**. Add 3 expenses (e.g., Rent / Phone / CC payoff). For one, type `Re` in the Category field — Budget Defaults categories should autocomplete.
7. **Click `+ New Budget` on `Budgets.md` for July 2026**. File should scaffold at `spice/finance/budgets/2026-07/Budget-2026-07.md` with `categories[]` = full snapshot of 5 + `groups[]` = snapshot of 4 + each `actual: 0`.
8. **Open the July budget**. `BudgetSummary` renders 3 bands at the top: PLANNED/ACTUAL/VARIANCE numbers, day-of-month progress + spent% bar + pace indicator, per-group mini-cards. Categories editor below renders grouped `<details>` sections with per-group SUBTOTAL line.
9. **Add a category mid-budget** — the Group dropdown must show the BUDGET'S `groups[]` snapshot, NOT the current defaults' groups. Rename a group in defaults → reopen July budget → defaults rename does NOT propagate (snapshot semantics intact).
10. **Edit a category's `actual` to $500** — `BudgetSummary` recomputes; per-group card color may flip; sub-total line updates.
11. **Open existing `spice/finance/budgets/2026-06/Budget-2026-06.md`**. **The 4 original categories must be intact** (Taco/Gas/Restaurants/Subscriptions); all under auto-injected "Unassigned" group; planned + actual UNCHANGED from before. `BudgetSummary` should render with empty Band 3 (since `groups[]` is empty on this old budget).
12. **Verify the snapshot exists at** `spice/finance/budgets/.sauce-backup/<timestamp>/spice/finance/budgets/2026-06/Budget-2026-06.md` (the pre-migration copy). Actually the path is at the **vault root**: `<vault>/.sauce-backup/<timestamp>/spice/finance/budgets/2026-06/Budget-2026-06.md`.
13. **Click `+ New Paycheck`** for a pay period. Expenses pre-populated; each carries `paid: false`. The `PaycheckExpensesEditor` Category input autocompletes from Budget Defaults' category names.
14. **Re-run `sauce install`** (or `node platform/install.js --vault <vault> --auto-approve`). Log shows `"3 budgets scanned, 3 snapshotted, 0 touched, 0 categories backfilled"` (idempotent). Defaults files preserved.
15. **Top of `Budgets.md` hub** — small ⚙ Defaults link appears next to `+ New Budget`. Same on `Paychecks.md`.

If anything looks off, surface it for v0.107.x patches.

## Open carry-forwards (queued)

### High value (most-aligned with user's stated goals)

1. **Debt envelope row in `BudgetSummary`** — reads `monthly_payoff_target_usd` from `spice/cowork/context/<engagement>/finance-guide.md` and shows `$X going to debt this month vs target $Y` on every budget. This is the BIG missing piece for the user's "Understand my plan to attack Credit Debt" goal. Highest-value v0.107.x or v0.108.0 candidate.

2. **Cowork morning-brief surface** — morning briefing reads current Budget's `BudgetSummary` computation (totals, pace, top overruns) and renders an accountability line in the briefing. Closes the "hold me accountable" loop end-to-end with the briefings the user already uses.

### Medium value

3. **Top-3 overruns + top-3 underruns row in `BudgetSummary`** — proactive signal for which categories deserve attention.
4. **"+ From Defaults" picker in `BudgetCategoriesEditor`** — when a user trimmed a category early in the month and then needs to add it back, this picker shows Budget Defaults' categories not already in the current budget. One-click add.
5. **"Bulk reassign Unassigned via name-match against Budget Defaults"** — for the v0.107.0 migrated budgets (currently all categories under "Unassigned"), name-match against current Budget Defaults to auto-reassign groups in bulk. Then user manually fixes mismatches.

### Low priority / housekeeping

6. **Promote `FinanceFrontmatter` to a shared `frontmatter-mutator` mechanism** — finally has 2 consumers (finance, plus the new BudgetDefaultsEditor/PaycheckDefaultsEditor pattern). v0.17.0 queued this; v0.107.0 added the second consumer signal.
7. **Per-engagement defaults** (Q2 option B from brainstorm) — useful if the user starts tracking LLC vs personal separately. Deferred until that need surfaces.
8. **`cowork:doctor`** — displaced for the SEVENTH cycle now. KPI is very rich. Probably warrants a cycle soon.

## Open infrastructure issue — brew tap is stale

The user's brew tap `/Users/willfellhoelter/projects/repos/homebrew-sauce` has a formula at `v0.84.0`. The user's local `/opt/homebrew/Cellar/sauce/0.106.0.1/` was somehow built without going through the tap formula bump.

**What this means for v0.107.0:**
- Both consumer vaults (accuris + headspace) had their `ranch/platform-config.json` `workshop_relative_path` temporarily redirected to `/Users/willfellhoelter/projects/repos/sauce` (the local workshop dev clone) for this deploy.
- The deploy succeeded; v0.107.0 + finance 0.5.1 are materialized in both vaults; the migration ran on headspace; pre-migration snapshots exist.
- If the user `brew upgrade sauce` later (assuming the tap formula gets bumped), they should consider restoring `workshop_relative_path` to `/opt/homebrew/opt/sauce/libexec` on each consumer. Until then, the local dev clone is the authoritative workshop for these two consumers.

**v0.108.0 candidate (housekeeping):** Bump brew tap formula to v0.107.0 (URL = `https://github.com/willfell/sauce/archive/refs/tags/v0.107.0.tar.gz`; recompute SHA256). Possibly also bump the tap formula whenever a cycle closes — the v0.84.0 staleness suggests the bump-on-release habit has lapsed.

## What's the next cycle

The user's clearest-impact follow-up is **v0.108.0 — debt envelope + cowork accountability surface**. That ships the second-half of the brainstorm vision (the "Know my limit on spending every month" + "Hold me accountable" goals from the original ask).

If you'd prefer to take the cowork:doctor displacement now (it's been seven cycles), v0.108.0 cowork:doctor is also justified.

Either is acceptable. The user picks at cycle-start.

End of handoff.
