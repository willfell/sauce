---
date: 2026-05-07
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.24.0 cycle. Triggered by v0.24.0 close + tag v0.24.0 on origin/main (Tree 3 runtime rename `Docs/Meta/` → `ranch/` + `beacon-button` mechanism rename to `accent-button`).
predecessors:
  - Docs/plans/2026-05-07-v0.24.0-tree3-and-accent-button-result.md (most recent close — read first)
  - Docs/plans/2026-05-07-v0.24.0-tree3-and-accent-button-plan.md
  - Docs/plans/2026-05-07-v0.24.0-tree3-and-accent-button-design.md
  - Docs/prompts/2026-05-06-post-v0.23.0-next-cycle-handoff.md (predecessor handoff)
  - Docs/prompts/_handoff-protocol.md (canonical baton-pass protocol)
gates:
  - v0.24.0 closed; tag v0.24.0 pushed; workshop manifest at 0.24.0
  - Tree 3 runtime plumbing rename Docs/Meta/ → ranch/ shipped (workshop self + barebones-beacon-poc + accuris-beacon-poc)
  - beacon-button mechanism → accent-button (BeaconButton class → AccentButton); 15 call-sites migrated
  - New stub md5 invariant ea23aa812503bfca66359d3b2b239ba8 (was a39257da1dd49ae4481e5cd0a42bdac4 from v0.1.2 → v0.23.0)
  - Allowlist #12 expanded 11 → 12 paths (.obsidian/plugins/customjs/data.json) for new applyCustomJsSettings helper
  - Helper count 9 → 10
  - macOS APFS Daily Notes acceptance gate PASSED + post-applyCustomJsSettings Cmd+R verified SpaceNavButtons + SpaceDailyDashboard render
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:subagent-driven-development OR de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.25.0 (or other)
---

# Onboarding — post-v0.24.0 next-cycle selection

> [!info] Project identity (post-v0.24.0)
> - **Project name:** `sauce` (rebranded in v0.23.0)
> - **GitHub remote:** `git@github-personal:willfell/sauce.git`
> - **Local workshop dev path:** `/Users/willfell/Documents/obsidian/sync/workshop/beacon` (host filesystem path; not yet renamed; standalone v0.24.x housekeeping carry)
> - **Sibling vaults:**
>   - `../barebones-beacon-poc/` — primary regression target (legacy sibling-of-workshop layout; `../beacon` workshop_relative_path; ranch/ runtime now post-v0.24.0)
>   - `../accuris-beacon-poc/` — secondary consumer (legacy layout; pre-existing version-pin drift on nav-buttons + project)
>   - `../scratch/2/` … `../scratch/5/` — historical smoke targets
>   - `../scratch/6/` — v0.24.0 smoke target (post-rename: `pantry/` workshop clone + `beacon/` modules + `ranch/` runtime; ALL 8 blueprints + 8 mechanisms installed; CF-3 closed via applyCustomJsSettings)

---

## Workshop state at session start

- `workshop_version: 0.24.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.5.2`, `cards@0.2.3`, **`accent-button@0.1.0`** (renamed from `beacon-button` in v0.24.0; class `BeaconButton` → `AccentButton`), `styling@0.1.2`, `convenience@0.1.0`
- Blueprints: `boards@0.1.0`, `daily@0.2.2`, `journal@0.1.1`, `meetings@0.2.1`, `project@1.3.5`, `to-do@0.1.4`, `trips@0.1.4`, `finance@0.2.9`
- Tags on origin: ... `v0.22.0` `v0.22.1` `v0.23.0` `v0.24.0` (NEW)
- Harness counts: bootstrap 45 / cli 20 / install-sh 14 / helper-cases 387 / renderer 23 — all green
- Allowlist (landmine #12): **12 paths** (added `.obsidian/plugins/customjs/data.json` in v0.24.0 CF-3)
- Landmines: **18 entries** (UNCHANGED count; #12 + #13 wording amended in v0.24.0)
- Helper count: **10** (v0.24.0 added applyCustomJsSettings)
- **Stub md5 invariant: `ea23aa812503bfca66359d3b2b239ba8`** (NEW in v0.24.0; was `a39257da1dd49ae4481e5cd0a42bdac4` v0.1.2 → v0.23.0)

**v0.24.0 commits in order (top-down):**
- `<HANDOFF>` docs(v0.24.0): post-v0.24.0 next-cycle handoff (THIS commit)
- `4ead196` docs(v0.24.0): cycle close + applyCustomJsSettings helper + landmine #12 11 → 12 paths
- `b2bdada` fix(v0.24.0): S2 follow-up — sed modifications missed in atomic commit
- `494d29e` feat(v0.24.0): S2 — Tree 3 rename Docs/Meta -> ranch + beacon-button -> accent-button
- `09879f6` test(v0.24.0): S1 TDD-RED baseline — accent-button + ranch/ harness expectations
- `6d2604b` docs(v0.24.0): tree 3 + accent-button rename implementation plan
- `e404588` docs(v0.24.0): tree 3 + accent-button rename design

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo]
> 1. `pwd` → `/Users/willfell/Documents/obsidian/sync/workshop/beacon`
> 2. `git remote -v` → `origin git@github-personal:willfell/sauce.git`
> 3. `git log --oneline -10` — expect ~7 v0.24.0 cycle commits + handoff at top
> 4. `git status` → only pre-existing drift (`.obsidian/*`, `ranch/Scripts/cards/beacon-cards.js` cards@0.2.2→0.2.3 dogfood drift, `*.bak` files, `blueprints i want.md` minor)
> 5. `git ls-remote --tags origin | grep v0.24.0` → 2 lines (annotated tag + dereference)
> 6. **Stub md5 invariant** (4 paths must match new value `ea23aa812503bfca66359d3b2b239ba8`): `md5 platform/installer-stub.js ranch/Templater/platformInstall.js ../barebones-beacon-poc/ranch/Templater/platformInstall.js ../accuris-beacon-poc/ranch/Templater/platformInstall.js`
> 7. `grep workshop_version platform/manifest.json` → `"workshop_version": "0.24.0"`
> 8. `ls platform/mechanisms/accent-button/` → `manifest.json` + `accent-button.js`. `ls platform/mechanisms/beacon-button/` → does NOT exist
> 9. `grep -rE 'BeaconButton|beacon-button' platform/blueprints/` → empty (only deliberate historical references in mechanism manifest description + run-renderer.js comment + how.md L209 + landmines.md #13)
> 10. **All 5 harnesses GREEN:**
>    - `node platform/test/run-bootstrap.js` → `Result: 45 passed, 0 failed.`
>    - `node platform/test/run-cli.js` → `Result: 20 passed, 0 failed.`
>    - `node platform/test/run-install-sh.js` → `Result: 14 passed, 0 failed.`
>    - `node platform/test/run-helper-cases.js | tail -3` → `Result: 387 passed, 0 failed.`
>    - `node platform/test/run-renderer.js` (no selector) → exit 0

If any pre-flight fails, STOP and surface to user.

---

## Required reads (~10-15 minutes)

> [!todo] In this order
> 1. **`Docs/prompts/_handoff-protocol.md`** — canonical baton-pass protocol
> 2. **`Docs/plans/2026-05-07-v0.24.0-tree3-and-accent-button-result.md`** — most recent cycle close. NEW lessons (a–h); reserved-headroom precedent extends to 11 data points (third MIXED case)
> 3. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.24.0 close
> 4. **`Docs/install.md`** — read the v0.23.x → v0.24.0 upgrade section + applyCustomJsSettings note
> 5. **`Docs/landmines.md`** — 18 entries; #12 (11 → 12 paths; v0.24.0 history appended) + #13 (new stub md5 + Tree N rename history) amended in v0.24.0
> 6. **`blueprints i want.md`** — pending feature carries (multi-theme deferred again)

---

## 🔴 v0.25.0 PRIMARY: Tree 2 module namespace rename `beacon/<module>/` → `spice/<module>/`

> [!warning] Final of three multi-cycle Sauce rebrand renames. v0.23.0 nailed Tree 1 (workshop clone); v0.24.0 nailed Tree 3 (runtime); v0.25.0 nails Tree 2 (modules).

**What:** Rename consumer-side blueprint module namespace `<vault>/beacon/` → `<vault>/spice/`. Most call-site-heavy of the three: every blueprint manifest's `module_directory` field (8 blueprints), every Templater path, every wikilink across ~50+ templates, every CustomJS path-check.

**Why:** Final piece of the multi-cycle Sauce rebrand. Locked at v0.23.0 brainstorm.

### v0.25.0 cycle scope (rough estimate; finalize in design)

**Files affected:**
- 8 blueprint manifest `module_directory` fields (`beacon` prefix → `spice`)
- ~50+ template files with wikilinks like `[[beacon/<module>/...]]`
- CustomJS path-checks (`pathParts[tripsIdx-1] !== "beacon"` in trip helpers, `ProjectsHubCards.filter` etc.)
- Blueprint helpers' `dv.pages("beacon/...")` calls
- Per-blueprint nav-button `folder_prefix` values (`beacon/<module>/` → `spice/<module>/`)
- Documentation: install.md migration recipe, use.md, how.md, landmines.md #11 (the module-directory invariant)
- All 5 harnesses' fixture configs

**Open design questions for v0.25.0 brainstorm:**
1. Migration story: same as v0.23.0 + v0.24.0 (manual `mv beacon spice` + sed on consumer configs + sed on user-authored content if any)?
2. User-authored content under `<vault>/beacon/` (cards, journal entries, projects, etc.) — `mv` is safe, but wikilinks INSIDE those files referencing `[[beacon/<module>/...]]` need bulk update OR Obsidian's wikilink resolver tolerates the rename via auto-redirect.
3. Cycle shape: 3 stages (TDD harness → atomic mass rename → smoke + close)?
4. Module-directory invariant codification: landmine #11 currently forecasts `spice/<module>/` rename in v0.25.0; finalize wording.

**Cycle shape estimate:** 3-4 stages. MINOR bump. Reserved-headroom: expect 2-4 review-surfaced + acceptance-gate-surfaced CFs (matching v0.24.0 11-data-point pattern). v0.24.0 lessons (a)–(c) directly applicable.

---

## Other candidate cycles (queued AFTER v0.25.0)

> [!example]- Multi-theme presets (DEFERRED again — 5th time)
> Carried from v0.21.1 → v0.22.0 → v0.22.1 → v0.23.0 → v0.24.0. v0.26.0+ candidate.

> [!example]- Architectural install.js refactor
> Helpers signal NO_OP_DUE_TO_MISSING_PREREQ to orchestrator. Eliminates landmine #16 unstick PATCH bumps. Substantial install.js change.

> [!example]- Project Sub-tasks panel on task hub
> Carry from post-v0.18.2. UI cycle.

> [!example]- Finance polish carries
> Migration helper for pre-v0.17.0 entities; budget categories aggregate row; PaycheckExpensesEditor "All Paid" toggle.

> [!example]- Local workshop dev path rename
> `/Users/willfell/Documents/obsidian/sync/workshop/beacon` → `/workshop/sauce`. Pure host-filesystem rename. Standalone v0.24.x or v0.25.x housekeeping (could bundle into v0.25.0 to match v0.23.0/v0.24.0 cadence).

> [!example]- Dogfood drift housekeeping (carry from v0.23.0 + v0.24.0)
> `ranch/Scripts/cards/beacon-cards.js` cards@0.2.2 → 0.2.3 outline-pill content drift in tree at v0.24.0 close. Small `chore(workshop): resync materialized cards mechanism` commit.

> [!example]- `BEACON_*` shell-rc honor-both shim (carry from v0.23.0)
> If users complain about lost shell-rc exports, dispatcher could honor both `SAUCE_VAULT || BEACON_VAULT`.

---

## Recommendation

> [!success] v0.25.0 — Tree 2 module namespace rename — PRIMARY
>
> Brainstorm should:
> 1. Lock the migration story (default: manual `mv` + sed; same as v0.23.0/v0.24.0 posture).
> 2. Lock cycle shape (3-4 stages).
> 3. Apply v0.24.0 lessons (a)–(c) at design-time:
>    - Pre-flight `grep -rE 'path\.join.*"<oldpart>"\s*,\s*"<oldsubpart>"'` scan to catch multi-arg path.join patterns.
>    - sed scope MUST include source dirs (mechanisms + blueprints + templates + jsdoc), not just infrastructure code.
>    - Audit every `.obsidian/plugins/<id>/data.json:<field>` for path values referencing `beacon/<module>/`.
> 4. Decide if local workshop dev path rename bundles in (v0.23.0 deferred; v0.24.0 deferred again; cumulating).

---

## Cadence guidance

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to lock migration story + cycle shape.
> - **`de:writing-plans`** after brainstorm approval.
> - **`de:subagent-driven-development` (same session) OR `de:executing-plans` (parallel)** after plan approval.
> - Master-driven on the rename mechanics + harness updates + version bumps + cycle close.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - **API-contract quoting** (v0.6.0) — for any path string that changes, QUOTE the literal in the design.
> - **Two-stage subagent review** (v0.20.0 + reaffirmed v0.22.1 + v0.23.0 + v0.24.0) — spec compliance first, code quality second.
> - **Manual smoke is the close gate.** Fresh `<vault>/scratch/7/` curl-bootstrap with v0.25.0 install.sh, opening Daily Notes successfully + verifying runtime artifacts land at `<vault>/spice/...`.
> - Push to origin/main after each stage commit; annotated tag at cycle close (REQUIRES user approval).
> - **Reserved-headroom precedent now 11 data points (post-v0.24.0).** v0.25.0 = wide-touching mechanical-rename similar to v0.23.0 + v0.24.0 — expect 2-4 CFs from missed call-sites + foundational plugin data.json migrations.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics).
> - Substitution variables only from `platform-config.json:variables` (#9).
> - **In-cycle re-process bump rule (LANDMINE #16)**.
> - **API-contract quoting (v0.6.0)** + **two-stage subagent review (v0.20.0; reaffirmed v0.22.1 + v0.23.0 + v0.24.0)**.
> - **Per-blueprint version bumps require LOCKSTEP edits**.
> - **Module-directory invariant (#11)** — `beacon/<module>/` will be renamed to `spice/<module>/` in v0.25.0; the INVARIANT (one-module-dir-per-blueprint, all-runtime-content-under-module-dir, no-cross-module-writes) does NOT change.
> - **Inside-vault clone is git-managed (#18)** — `pantry/` is the v0.23.0+ name; preserve.
> - **Allowlist 12 paths (#12)** — adding 13th+ requires explicit user approval.
> - **Stub content-static (#13)** — never re-edit any `ranch/Templater/platformInstall.js` body. Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` post-v0.24.0. Tree 2 rename in v0.25.0 does NOT change the stub body (stub reads `ranch/platform-config.json` regardless of module-dir name).
> - **Bootstrap network posture (#17)** — install.sh + CLI verbs are network gateways; install.js stays filesystem-only.
> - **`.beacon-backup` suffix preserved** (v0.23.0 lesson a; v0.24.0 reaffirmation) — backup-suffix conventions are platform-version-stable; brand-rename does NOT cascade.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, allowlist expansion (13th path), creating top-level vault directory (v0.25.0 codifies a NEW sanctioned top-level: `spice/` replacing `beacon/<module>/`).

---

## End-of-session protocol

> [!success] Per `Docs/prompts/_handoff-protocol.md`'s end-of-session checklist:
> 1. Result writeup at `Docs/plans/<date>-<cycle>-result.md`.
> 2. CLAUDE.md status snapshot updated.
> 3. Cycle-close commit + push.
> 4. Annotated tag at cycle close (REQUIRES user approval).
> 5. Next entry prompt at `Docs/prompts/<date>-post-<cycle>-next-cycle-handoff.md`.
> 6. Commit + push next entry prompt.
> 7. Final user message: copy-paste slash command + one-paragraph status summary.

---

## Suggested first move

> [!todo] Recommended path
> 1. Verify session CWD + run all pre-flight checks (incl. stub md5 + harness counts).
> 2. Read v0.24.0 result writeup (NEW lessons a–h; reserved-headroom 11 data points; CF-3 helper pattern).
> 3. Surface v0.25.0 as PRIMARY recommendation via `AskUserQuestion`. Apply v0.24.0 lessons at design-time.
> 4. Once scope is locked, brainstorm: migration mechanics, harness updates needed, cycle shape, reserved-headroom estimate, foundational plugin data.json audit.
> 5. After brainstorming approval, invoke `de:writing-plans`.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.24.0 next cycle. Read Docs/prompts/2026-05-07-post-v0.24.0-next-cycle-handoff.md FIRST — full onboarding doc with project identity (Sauce platform; sauce/pantry/ranch + beacon/<module>/ stays until v0.25.0; CLI binary `sauce`; env vars SAUCE_*), pre-flight checks (workshop_version 0.24.0; stub md5 ea23aa812503bfca66359d3b2b239ba8 across 4 paths; allowlist 12 paths; helper count 10; bootstrap 45/0, cli 20/0, install-sh 14/0, helper-cases 387/0, renderer green), the 🔴 PRIMARY recommendation (v0.25.0 Tree 2 rename `beacon/<module>/` → `spice/<module>/`), constraints carried forward, and the v0.24.0 NEW lessons (a-h).

After reading the handoff doc:
1. Verify pwd is /Users/willfell/Documents/obsidian/sync/workshop/beacon (host filesystem path; standalone rename carry from v0.24.0).
2. Run pre-flight checks. Workshop_version 0.24.0; remote git@github-personal:willfell/sauce.git; new stub md5 ea23aa812503bfca66359d3b2b239ba8.
3. Read v0.24.0 result writeup + CLAUDE.md status snapshot + landmines (18 entries; #12 11→12 paths + #13 new md5 amended in v0.24.0).
4. Surface candidates via AskUserQuestion. v0.25.0 = PRIMARY (Tree 2 module-namespace rename — final of three multi-cycle Sauce rebrand renames). Other queues: architectural install.js refactor, project Sub-tasks panel, finance polish, multi-theme presets DEFERRED again, dogfood drift housekeeping carry, local workshop dev path rename carry.
5. Once locked, brainstorm migration story + cycle shape + foundational plugin data.json audit.
6. After brainstorming approval, invoke de:writing-plans.

Pacing: master-driven on the rename mechanics + harness updates + version bumps + cycle close. Stage-close AskUserQuestion checkpoints. Manual smoke is the close gate — fresh-vault scratch/7 curl-bootstrap on macOS APFS + Daily Notes successful open + spice/ runtime artifacts visible. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Apply v0.24.0 lessons at design-time:
- Pre-flight `grep -rE 'path\.join.*"<oldpart>"\s*,\s*"<oldsubpart>"'` scan (lesson a).
- sed scope MUST include source dirs (mechanisms + blueprints + templates + jsdoc), not just infrastructure code (lesson b).
- Audit every `.obsidian/plugins/<id>/data.json:<field>` for path values referencing `beacon/<module>/` (lesson c) — though Tree 2 may not have any (jsFolder is for scripts, not modules).
- After git mv, ALWAYS run `git add -u` separately + verify `git diff --cached --stat` (lesson e).

Constraints carried forward: workshop_version bumps require approval; force-push + history rewrite + tag deletion require approval; stub body untouchable (#13; new md5 ea23aa812503bfca66359d3b2b239ba8 holds across v0.25.0 — Tree 2 doesn't change stub body); landmine #12 allowlist expansion (13th+ path) requires approval; landmine #11 module-directory invariant: namespace renames `beacon/` → `spice/`, but the one-module-per-blueprint rule does NOT change; two-stage spec-then-quality subagent review (v0.20.0 + v0.22.1 + v0.23.0 + v0.24.0 reaffirmation).

End-of-session protocol per Docs/prompts/_handoff-protocol.md MUST be followed.

Begin with pre-flight checks.
```

End of onboarding.
