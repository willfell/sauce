---
date: 2026-05-04
purpose: Onboard the next session to select + brainstorm + plan + execute the post-v0.3.0 cycle. Triggered by v0.3.0 cycle close at commit 3a5c271 + tag v0.7.0 on origin/main.
predecessors:
  - Docs/plans/2026-05-04-v0.3.0-daily-blueprint-result.md (v0.3.0 close — read first)
  - Docs/plans/2026-05-04-v0.3.0-daily-blueprint-plan.md (v0.3.0 plan)
  - Docs/plans/2026-05-04-v0.2.0-boards-blueprint-result.md (v0.2.0 close)
gates:
  - v0.3.0 closed at 3a5c271; tag v0.7.0 pushed; workshop manifest at 0.7.0
  - barebones materialized: beacon/daily/ exists; .obsidian/daily-notes.json wired; SpaceDailyDashboard graceful-empty silent
  - landmine #11 codified at installer level (T-FIX1 mkdir mechanic); landmine #12 allowlist at 3 paths
required_sub_skill: de:brainstorming (primary; opens cycle); de:writing-plans (post brainstorm); de:executing-plans (post plan approval)
target_artifact: Docs/plans/<date>-<cycle>-design.md → Docs/plans/<date>-<cycle>-plan.md → execute → Docs/plans/<date>-<cycle>-result.md → tag v0.7.x or v0.4.0 (cycle-dependent)
---

# Onboarding — post-v0.3.0 next-cycle selection (brainstorm → plan → execute)

> [!info] Project identity (load-bearing)
> - **Project name:** `beacon`
> - **GitHub remote:** `git@github-personal:willfell/beacon.git` (HTTPS: `https://github.com/willfell/beacon`) — personal account `willfellhoelter@gmail.com`
> - **Local workshop path:** `/Users/willfell/Documents/obsidian/sync/workshop/poc-vault`
> - **Sibling vaults:**
>   - `../tmp-test-barebones-vault/` — primary regression target + manual-smoke target
>   - `../tmp-acc-vault/` — secondary consumer (deliberate-lag at subscription level; mechanisms-only posture preserved)
>   - `../accuris/` — production reference vault (NOT a v0.x consumer; source of pattern ports)
> - **Distribution model:** stub-dispatch (post-v0.1.2). Stub md5 `a39257da1dd49ae4481e5cd0a42bdac4` across all 4 paths. NEVER re-edit the stub (landmine #13).

---

## Mission

Pick the next cycle, brainstorm + design + plan + execute it.

**Workshop state at session start:**
- `workshop_version: 0.7.0`
- Mechanisms: `customjs-guard@1.0.0`, `validator@0.1.1`, `audit@0.1.1`, `nav-buttons@2.0.0`
- Blueprints: `boards@0.1.0`, `daily@0.1.0`, `project@0.3.0`
- Tags on origin: `v0.4.1` `v0.5.0` `v0.6.0` `v0.7.0`
- Harness: `run-helper-cases.js` 172 sub-asserts; all 3 harnesses green
- Module-directory invariant codified at the installer level (T-FIX1)
- Landmine #12 allowlist: 3 paths (templater-obsidian, slash-commander, daily-notes)

---

## Pre-flight checks (do these BEFORE picking a cycle)

> [!todo] Verify state matches expected post-v0.3.0 baseline
> 1. **Git head + recent commits:**
>    ```bash
>    git -C /Users/willfell/Documents/obsidian/sync/workshop/poc-vault log --oneline -5
>    ```
>    Expected (top-down): `3a5c271` (v0.3.0 S2 close), `a61908c` (v0.3.0 S1), `0008b28` (v0.3.0 plan-writing handoff), `f9264a5` (v0.3.0 design), `6c56d7f` (v0.2.0 close).
> 2. **Working tree:** `git status` → clean.
> 3. **Tags on origin:** `git ls-remote --tags origin` → `v0.4.1`, `v0.5.0`, `v0.6.0`, `v0.7.0` all present + annotated.
> 4. **Stub md5 invariant (4 paths must match):**
>    ```bash
>    md5 /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/platform/installer-stub.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/poc-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/Docs/Meta/Templater/platformInstall.js \
>        /Users/willfell/Documents/obsidian/sync/workshop/tmp-acc-vault/Docs/Meta/Templater/platformInstall.js
>    ```
>    Expected: all 4 = `a39257da1dd49ae4481e5cd0a42bdac4`.
> 5. **Workshop manifest at 0.7.0:** `grep workshop_version platform/manifest.json` → `"workshop_version": "0.7.0"`.
> 6. **Workshop self-install green:** `node platform/test/run-install.js .` → exit 0; idempotent (0 new history entries).
> 7. **Helper-cases harness 172 green:** `node platform/test/run-helper-cases.js` → `Result: 172 passed, 0 failed.`
> 8. **Barebones daily blueprint state:**
>    ```bash
>    ls /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/beacon/daily
>    cat /Users/willfell/Documents/obsidian/sync/workshop/tmp-test-barebones-vault/.obsidian/daily-notes.json
>    ```
>    Expected: `beacon/daily/` exists (possibly with subdirectories `2026/05-May/...` containing the user's manual-smoke test daily notes); `daily-notes.json` shape `{folder: "beacon/daily", format: "YYYY/MM-MMMM/YYYY-MM-DD-dddd", template: "Docs/Meta/Templates/Daily Note.md"}`.

If any pre-flight fails, STOP and surface to user via `AskUserQuestion`.

---

## Required reads (~15 minutes)

> [!todo] In this order
> 1. **`Docs/plans/2026-05-04-v0.3.0-daily-blueprint-result.md`** — most recent cycle close. State snapshot, surprises, pacing rhythm. Read first to inherit cadence.
> 2. **`CLAUDE.md`** — non-negotiables. Status snapshot at v0.3.0 close (lines ~115). Cycle order shows v0.3.0 ✅; next TBD.
> 3. **`Docs/landmines.md`** — 14 entries. Pay attention to #11 (codified by T-FIX1) + #12 (3-path allowlist).
> 4. **`Docs/plans/2026-05-04-v0.3.0-daily-blueprint-plan.md`** — most recent plan structure (use as plan-writing template if you reach that phase).
> 5. **(If you pick v0.3.x meetings or to-do)** Reference reads:
>     - `/Users/willfell/Documents/obsidian/sync/accuris/Extras/Templates/Template, Meeting.md` (meetings template)
>     - `/Users/willfell/Documents/obsidian/sync/accuris/Timestamps/Meetings/...` (sample meeting notes)
>     - `/Users/willfell/Documents/obsidian/sync/accuris/Extras/Scripts/space-daily-dashboard.js` (already studied; references `Timestamps/Meetings` + `Timestamps/ToDo`).
> 6. **(If you pick v0.4.0 nav-button action types)** Reference reads:
>     - `platform/install.js` `applyNavButtons` at line ~843 + `validateAndResolve` at line ~945 (current `createFromTemplate` action handling)
>     - `Docs/Meta/Scripts/nav-buttons/space-nav-buttons.js` (renderer; current click-handler dispatch)

---

## Candidate cycles (pick ONE)

> [!example]- v0.3.1 to-do blueprint **(RECOMMENDED)**
> **What:** ship `to-do` blueprint at `beacon/to-do/` with date-routed task notes. Feeds `SpaceDailyDashboard` tasks panel — currently silent because `beacon/to-do/` doesn't exist.
> **Why first:** smallest sibling that makes the daily dashboard useful immediately. Validates v0.3.0's "siblings tolerated absent" design retroactively. Uses existing infrastructure (module-directory mkdir from T-FIX1; nav-buttons + customjs-guard mechanisms; existing nav_buttons[] action types).
> **Estimated scope:** 1-stage cycle. Manifest + content/template + post_install notice. Probably no schema additions. Workshop bump 0.7.0 → 0.7.1 (patch — additive blueprint only).
> **Open questions for brainstorming:**
> - Single per-day to-do file (`beacon/to-do/YYYY-MM-DD.md`) OR multiple per-day (one per task)?
> - Schema: simple checklist (`- [ ] task`) OR per-task frontmatter (due, priority, project)?
> - Entry UX: nav-button "+ Task" / Templater hotkey / Slash Commander / multiple?
> - SpaceDailyDashboard reads tasks via `dv.pages('"beacon/to-do"').file.tasks.where(t => !t.completed)` — confirm the to-do schema produces task lines that match.
>
> **Cycle close adds:** tag `v0.7.1` annotated.

> [!example]- v0.3.2 meetings blueprint
> **What:** ship `meetings` blueprint at `beacon/meetings/` with date-routed meeting notes. Feeds `SpaceDailyDashboard` meetings panel.
> **Why later (or alongside to-do):** more complex than to-do — meeting notes have richer schema (attendees, summary, agenda, action items). Probably 2-stage cycle. Cross-references with people-notes (out of scope for now).
> **Estimated scope:** 2-stage. Manifest + template (port from accuris's `Template, Meeting.md`) + maybe a `MeetingsHub` view + post_install notice. Workshop bump 0.7.0 → 0.7.2 (or 0.8.0 if pulled in alongside other work).
> **Open questions for brainstorming:**
> - Verbatim accuris port OR redesigned schema?
> - Templater hotkey for "+ Meeting" creation? Slash Commander binding?
> - Auto-link meeting note to today's daily note via dataview query?
> - SpaceDailyDashboard's meetings query: `dv.pages('"beacon/meetings"').where(p => p.file.name.startsWith(today))` — pattern requires meeting note filenames prefixed with `YYYY-MM-DD`.

> [!example]- v0.2.1 project migration to `beacon/projects/`
> **What:** retire project blueprint's `module_directory: "projects-legacy"` stopgap; migrate content from current `boards/planning/<slug>/` (TWO-axis mis-location: wrong namespace + wrong module dir) to canonical `beacon/projects/<slug>/`.
> **Why important:** long-deferred from v0.2.0 close. Pays down architectural debt. Aligns project with the codified module-directory invariant.
> **Estimated scope:** 1-2 stage. Pure refactor. project bumps 0.3.0 → 1.0.0 (BREAKING). Need a migration strategy for existing project notes in consumer vaults (`pre_install` move? installer migration helper? user-facing manual ritual?). Workshop bump 0.7.0 → 0.8.0 (minor — BREAKING for project but additive contract for workshop).
> **Open questions for brainstorming:**
> - Migration mechanic: extend `pre_install[]` with a `move` action type? OR document as user-facing manual step?
> - How to handle accuris's existing project notes (when accuris migrates)? Out of scope for v0.2.1?
> - Renderer impact: nav-buttons registry's `target` paths need to be re-resolved.

> [!example]- v0.4.0 invoke_command nav-button action type + Today button
> **What:** extend nav-buttons schema with new `action.type: "invoke_command"` (alongside existing `createFromTemplate`); add a "Today" nav-button to the daily blueprint that fires `daily-notes:goto-today`.
> **Why:** addresses the smoke-A user observation ("did the daily note button make it into the nav buttons?"). Generalizes nav-buttons to surface any registered Obsidian command, not just Templater-template instantiations. Unlocks future blueprints that want to fire arbitrary commands.
> **Estimated scope:** 1-stage cycle. Pure infrastructure + small daily manifest update. nav-buttons mechanism bumps 2.0.0 → 2.1.0 (minor); daily blueprint bumps 0.1.0 → 0.1.1; workshop bumps 0.7.0 → 0.8.0.
> **Open questions for brainstorming:**
> - Action validation: how does `applyNavButtons` know the command_id is real? (Obsidian command palette is per-runtime; can't statically validate.)
> - Renderer impact: `space-nav-buttons.js` needs new dispatch for `invoke_command` (calls `app.commands.executeCommandById(...)`).
> - Registry shape: stays the same? Add new `action: { type, command_id }` shape next to existing `{ type: "createFromTemplate", target, template_source }`.
> - Should the Today button be in daily blueprint's manifest OR a workshop-level "platform-default" registration?

> [!example]- v0.3.x Calendar plugin display preferences
> **What:** ship `core_plugin_settings` for Calendar plugin's `data.json` (weekly-notes, weekStart, wordsPerDot defaults).
> **Why:** small. Proves `core_plugin_settings[]` generalizes to non-daily-notes core plugin data files. Adds Calendar's data.json to landmine #12 allowlist (4th path — needs explicit user approval).
> **Estimated scope:** 1-stage cycle. Daily manifest gets a second `core_plugin_settings` entry. Workshop bump 0.7.0 → 0.7.1 OR 0.8.0 depending on allowlist gate.
> **Open questions for brainstorming:**
> - Worth the allowlist expansion? Calendar's defaults are mostly cosmetic.
> - Better: defer until a real preference need surfaces.
> **Recommendation:** SKIP for now; defer indefinitely.

---

## Recommendation

> [!success] **v0.3.1 to-do blueprint** — opens with brainstorming
> Smallest cycle. Highest validation value (makes the daily dashboard useful). Uses already-codified infrastructure. Sets up v0.3.2 meetings naturally.
>
> If user prefers to address the smoke-A observation directly: **v0.4.0 invoke_command** is the runner-up — small, principled, addresses a user-surfaced gap.

---

## Cadence guidance (mirror v0.3.0 / v0.2.0)

> [!info] Recommended pacing
> - **Open with `de:brainstorming`** to scope the cycle + lock decisions.
> - **`de:writing-plans`** after brainstorming approval.
> - **`de:executing-plans` (subagent-driven)** after plan approval.
> - Per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks.
> - Stage-close `AskUserQuestion` checkpoints before commit + push.
> - TDD discipline on harness extensions (mirror v0.3.0's C1-C5 / M6 case structure).
> - Manual Obsidian smokes as the final gate (1-3 smokes depending on cycle).
> - Push to origin/main after each stage commit; annotated tag at cycle close.

---

## Constraints carried forward (non-negotiable)

> [!warning] CLAUDE.md non-negotiables + landmines
> - No personal content in workshop.
> - JSON only (#6).
> - Failure-loud posture (#12 mechanics for any new `.obsidian/` path).
> - Substitution variables only from `platform-config.json:variables` (#9), EXCEPT `{{module_directory}}` overlay for blueprints (codified v0.2.0 T1.2).
> - Module-directory invariant under `beacon/` (#11) — codified at installer level via T-FIX1's mkdir.
> - `.obsidian/` allowlist at 3 paths (#12) — adding a 4th requires explicit user approval + same 4 safety mechanics.
> - Stub content-static (#13) — never re-edit any `Docs/Meta/Templater/platformInstall.js`.
> - `gitState()` best-effort (#14) — null-tolerant.
> - Workshop dogfods every release.
> - Desktop-only.
> - **Ask before:** workshop_version bump, force-push, history rewrite, tag deletion, modifying any consumer's stub body, editing `platform/installer-stub.js`, creating top-level vault directory, adding 4th `.obsidian/` allowlist path.

---

## Suggested first move

> [!todo] Recommended path
> 1. Run all 8 pre-flight checks. Surface discrepancies first.
> 2. Read the v0.3.0 result writeup + CLAUDE.md status snapshot + landmines (~15 min).
> 3. **Surface the cycle pick to user via `AskUserQuestion`** — present the 5 candidates above + recommendation; lock the choice before opening brainstorming.
> 4. Invoke `de:brainstorming` to scope the locked cycle + answer the open questions listed for that candidate.
> 5. After brainstorming approval, invoke `de:writing-plans` to author the implementation plan.
> 6. After plan approval, invoke `de:executing-plans` (subagent-driven).
> 7. Manual Obsidian smokes at S2 close.
> 8. Cycle close: result writeup + CLAUDE.md status snapshot + commit + tag + push branch + tag.

---

## Slash command for the next session

```
/de:brainstorming

Open the post-v0.3.0 next cycle. Read Docs/prompts/2026-05-04-post-v0.3.0-next-cycle-handoff.md FIRST — it's the full onboarding doc with project identity, pre-flight checks, candidate cycles (5 options with recommendation), constraints carried forward, and pacing guidance.

After reading the handoff doc:
1. Run all 8 pre-flight checks.
2. Read the v0.3.0 result writeup + CLAUDE.md + landmines.md.
3. Surface the 5 cycle candidates to me via AskUserQuestion with the recommendation (v0.3.1 to-do as primary; v0.4.0 invoke_command as runner-up). Get my pick before brainstorming opens.
4. Once locked, brainstorm the chosen cycle: scope, locked decisions, open questions resolution, version bump, stage breakdown.
5. After brainstorming approval, invoke de:writing-plans.

Pacing: per-task subagent dispatch on installer-extending + blueprint-authoring + writeup tasks; master-driven mechanical tasks; stage-close AskUserQuestion checkpoints. TDD on harness extensions. Push to origin/main after each stage commit; annotated tag at cycle close.

Constraints carried forward: workshop_version bumps require my approval; force-push + history rewrite + tag deletion still require my approval; stub body untouchable (landmine #13); landmine #12 allowlist expansion (to a 4th path) requires my approval.

Begin with pre-flight checks.
```

End of onboarding.
