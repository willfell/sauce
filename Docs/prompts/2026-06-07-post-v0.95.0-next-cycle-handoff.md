---
purpose: Resume-prompt + state snapshot for the session that picks up after v0.95.0 ships + deploys. The cowork-spine MINOR cycle (this one) closes the cohesion + composability surface; next moves are v0.94.1/v0.94.2 Recoverability/Generalizability PATCHes, v0.95.0.x voice/bootstrap polish, v0.95.1 vault-config migrator, then v0.96.0+ memory write-side.
load_when: Starting a fresh session on/after 2026-06-08 to pick the next cycle after v0.95.0.
status: authoritative as of 2026-06-07 (S3 cycle-close)
prior_cycle: v0.95.0 cowork-spine MINOR
prior_state_doc: Docs/plans/2026-06-07-post-v0.93.3-brainstorm-state.md
result_doc: Docs/plans/2026-06-07-v0.95.0-cowork-spine-result.md
vision: Docs/cowork-vision.md
---

# Post-v0.95.0 next-cycle handoff

This doc is the session-start reference for the FIRST conversation after v0.95.0 closes + deploys. The cowork-spine cycle landed the cohesion sweep + knob ergonomics refactor; the follow-up cycles cascade voice/bootstrap polish, the vault-config migrator, the memory write-side, and cross-machine wrappers.

## State verification (run first)

```bash
cd /Users/willfellhoelter/projects/repos/sauce
git fetch origin && git status
git log --oneline -5
git rev-parse v0.95.0^{commit}
jq -r .version package.json                                # Expect 0.95.0
jq -r .workshop_version platform/manifest.json             # Expect 0.95.0
jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # Expect 0.32.0
brew list sauce --versions                                 # Expect 0.95.0
for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
             /Users/willfellhoelter/notes/sauce/headspace-sauce; do
  echo "=== $vault ==="
  jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
  jq -r '.blueprints[] | select(.name == "cowork") | "\(.name): \(.version)"' "$vault/ranch/platform-subscription.json"
done
```

Expected: HEAD on origin/main; tag `v0.95.0` present; brew sauce 0.95.0; both consumer vaults at workshop_version 0.95.0 + cowork 0.32.0.

## Resequenced arc (per `Docs/cowork-vision.md` § 5)

| Cycle | Scope | Slot |
|---|---|---|
| **v0.94.1** | Echoes-warning reword — `[!warning]+ Semantic index not available` → user-friendly phrasing | NEXT (PATCH) — small, fast, low-risk |
| **v0.94.2** | Platform de-personalization — accuris fixture identifiers in platform/test/fixtures + SKILL.md prose | NEXT (PATCH) — hygiene |
| **v0.95.0.x PATCH** | Voice/bootstrap polish — bootstrap-vault stage-gated context fill for brand-voice.md / people.md / finance-guide.md | NEXT (MINOR-PATCH bundle) — depends on v0.95.0 7-day observability window |
| **v0.95.1** | `sauce update --migrate-config` — adds empty `overrides: {}` to every engagement + drops v0.95.0 backward-compat reads | NEXT (PATCH) — closes the v0.95.0 backward-compat one-cycle window |
| **v0.96.0+** | Memory write-side (pattern accrual) + cross-machine wrappers + `cowork:doctor` + `cowork:audit-cohesion` | LATER — the compounding-assistant frontier |

## 7-day quality observability window (post-v0.95.0 deploy)

Per design § Risks and result doc § Quality observability window: grade 7 days of consumer-vault atomic notes against the vision § 2 dimensions. Watch for:

- Orchestrator behavior on slimmed bodies — does the LLM execute the priority loop reliably with ~150 fewer lines of imperative prose?
- `plan-dispatch` invocation log — does the orchestrator's pre-flight log show the new sub-skill READ? (Should appear at step 3b in atomic-note output's Notice trail.)
- Atomic-note output shape — unchanged from v0.94.x? (Frontmatter type / title / summary / SpaceNavButtons / [!info]- / [!example]+ / [!tip] all stable?)
- Defense-in-depth fires — any `failed:contract-violation:*` Notices in the wild?
- New behavior probes — try adding an `overrides: { render_aspects: { finance_block: "skip" } }` block to one engagement's vault-config.md record and confirm next morning-briefing skips Finance.

If any regression, file v0.95.0.x PATCH candidate (defense-in-depth strengthening, prose adjustment, or knob-composition refinement).

## Decision branch for the next session

**Default: brainstorm v0.94.1 (Echoes-warning reword)** — small, fast, low-risk. Closes the most-visible Recoverability scar (cold-corpus Echoes warning callout) per cowork-vision § 2 dimension 7. Touches `cowork:gather-semantic-related` + `cowork:gather-semantic-memory` + a handful of fixtures. PATCH-sized; could ship same-day.

**Alternative: brainstorm v0.94.2 (Platform de-personalization)** — hygiene pass on accuris-specific identifiers in `platform/test/fixtures/` + SKILL.md prose. Pure refactor (no behavior delta). Generalizability dimension.

**Alternative: brainstorm v0.95.0.x (voice/bootstrap polish)** — bootstrap-vault stage-gated context fill prompts for brand-voice.md / people.md / finance-guide.md when each render_aspect is `include`. Closes the headspace-vs-accuris data-richness gap. Depends on the 7-day observability window completing first.

**Alternative: brainstorm v0.95.1 (vault-config migrator)** — `sauce update --migrate-config` adds `overrides: {}` block to every engagement + drops backward-compat reads. PATCH-sized. Closes the v0.95.0 one-cycle window.

## Resume prompt (paste into fresh session)

```
Resume sauce workshop work post-v0.95.0 close (2026-06-07).

REPO: /Users/willfellhoelter/projects/repos/sauce (workshop vault, NOT a
consumer). Run vault-identity check per CLAUDE.md before any write.

AUTHORITATIVE STATE DOC (READ FIRST, END-TO-END):
  Docs/prompts/2026-06-07-post-v0.95.0-next-cycle-handoff.md

ALSO LOAD-BEARING (read in order after the state doc):
  1. Docs/plans/2026-06-07-v0.95.0-cowork-spine-result.md
     (result doc — what shipped, surfaces hit, 6 NEW lessons)
  2. Docs/cycle-history.md § v0.95.0 (cycle narrative)
  3. Docs/cowork-vision.md (long-term direction + locked decisions)
  4. Docs/agent-guides/cycle-status.md (live workshop catalogue at 0.95.0)
  5. Docs/agent-guides/cowork-orchestrator-template.md (NEW v1.0.0)
  6. Docs/landmines.md (22 non-negotiable traps)

STATE VERIFICATION (per the state doc § "State verification"):
  git log --oneline -5
  git rev-parse v0.95.0^{commit}
  jq -r .version package.json                                  # 0.95.0
  jq -r .workshop_version platform/manifest.json               # 0.95.0
  jq -r '.blueprints[] | select(.name == "cowork") | .version' platform/manifest.json   # 0.32.0
  brew list sauce --versions                                   # 0.95.0
  for vault in /Users/willfellhoelter/notes/sauce/accuris-sauce \
               /Users/willfellhoelter/notes/sauce/headspace-sauce; do
    echo "=== $vault ==="
    jq -r '.workshop_version' "$vault/ranch/platform-subscription.json"
  done

EXPECTED STATE:
  HEAD: post-v0.95.0 close.
  Tag v0.95.0 present.
  brew sauce: 0.95.0.
  Both consumer vaults: workshop_version 0.95.0 + cowork 0.32.0.

TASK FOR THIS SESSION (default): brainstorm v0.94.1 — Echoes-warning reword.

The recommended next cycle is v0.94.1: reword the cold-corpus
`[!warning]+ Semantic index not available` callout to user-friendly
phrasing ("Semantic memory warming up — Echoes will appear once Smart
Connections has indexed (typically 10-30 min on first run)"). Touches
cowork:gather-semantic-related + cowork:gather-semantic-memory + a
handful of fixtures. PATCH-sized.

Invoke superpowers:brainstorming with the brief:

  "Brainstorm v0.94.1 — Echoes-warning reword. The current callout
   `[!warning]+ Semantic index not available` reads as scary-error
   instead of cold-corpus-still-warming-up. Reword to user-friendly
   phrasing. Decide: (1) exact reword wording; (2) which sub-skill
   files emit the callout vs reference it; (3) which fixtures need
   regeneration; (4) is the wording per-engagement-configurable or
   global; (5) does it stay [!warning]+ or shift to [!info]- /
   [!note]; (6) backward-compat fixture deltas. Apply v0.93.3+
   lessons: TDD against real path, per-cycle VERSION pin sweep,
   ranch lockstep, surface scope adaptations explicitly."

OPEN ALTERNATIVES (if user redirects):
  - v0.94.2 — Platform de-personalization (accuris-specific identifiers
    in platform/test/fixtures + SKILL.md prose; pure refactor)
  - v0.95.0.x — voice/bootstrap polish (bootstrap-vault stage-gated
    context fill — depends on 7-day observability window)
  - v0.95.1 — vault-config migrator (`sauce update --migrate-config`
    adds `overrides: {}` to engagements + drops backward-compat reads)

SAUCE CONVENTIONS (NON-NEGOTIABLE):
  - Direct-push to origin/main only after S4 user-approval gate.
  - One commit per stage; conventional commits; explicit git add <paths>
    (NEVER -A).
  - NO Co-Authored-By trailer in commit messages.
  - Vault-identity check before any write.
  - 22 landmines (Docs/landmines.md) apply.

START with the state verification commands above. Report what you find.
Then begin the v0.94.1 brainstorm (or alternative path if user redirects).
```

## Cross-references

- **v0.95.0 result doc:** `Docs/plans/2026-06-07-v0.95.0-cowork-spine-result.md` — what shipped + 6 NEW lessons
- **v0.95.0 design:** `Docs/plans/2026-06-07-v0.95.0-cowork-spine-design.md`
- **v0.95.0 plan:** `Docs/plans/2026-06-07-v0.95.0-cowork-spine-plan.md`
- **Cycle history v0.95.0:** `Docs/cycle-history.md` § v0.95.0
- **Live cycle status:** `Docs/agent-guides/cycle-status.md`
- **North Star:** `Docs/cowork-vision.md`
- **Canonical orchestrator template:** `Docs/agent-guides/cowork-orchestrator-template.md` (NEW v1.0.0)
- **Prior state doc:** `Docs/plans/2026-06-07-post-v0.93.3-brainstorm-state.md`
- **Landmines:** `Docs/landmines.md`
