---
context: post-v0.119.0 cycle close
authored_at: 2026-06-16
load_when: starting the next cycle on this branch
---

# Post-v0.119.0 next-cycle handoff

## State

- Workshop on `0.119.0`, deployed via brew tap PR + per-vault `sauce install` on accuris +
  headspace + ero (2026-06-16, pending Phase 8 deployment).
- to-do blueprint on `0.7.0`. Sentinel additive + markdown rendering both verified live.
- Harness count **34** (32 → 34 this cycle: `run-customjs-contract.js` +
  `run-todo-markdown-render.js`).
- Two new preflight defenses in place: `run-customjs-contract.js` (detect-by-shape
  static-vs-instance contract scanner) + `lint-display-markers.js` (baseline-aware anchor lint).
- Two new landmines: #27 (cycle scope discipline — cap at 3 distinct surfaces) + #28 (verify
  dispatcher / loader contracts before designing against them).

## Top candidates for the next cycle

1. **Meetings dialog feature** (deferred from v0.119.0 triage). User asked for: prompts-for-attendees
   field on `+ New Meeting`; in-meeting `+ New Task` button that creates a task within the meeting
   (currently manual). Likely scope: to-do 0.8.0 + meetings 0.9.0. Design needed before plan.
2. **todo:doctor / cowork:doctor doctor-pattern cycle**. cowork:doctor has been queued five-plus
   cycles. Pair with todo:doctor (postmortem item #8) and design ONE shared doctor pattern.
   Could be its own MINOR.
3. **Live-render recurring tasks (postmortem item #5 full version).** Additive sentinel closes
   the immediate bug; live-render replaces materialization entirely with a Dataview view that
   reads the registry on every render. Bigger mental shift. Defer until v0.7.0 has soaked.
4. **Seed-subscription bump pattern codification.** Phase 5 of v0.119.0 surfaced a gap: future
   cycles bumping a blueprint version need to bump the seed-vault subscription pin in the SAME
   plan task. Document in `Docs/agent-guides/migration-regression-net.md`. PATCH-sized.
5. **Anchor-lint baseline shrink-over-time.** Refactor any parser flagged in the 59-entry
   baseline of `scripts/lint-display-markers-baseline.json` to use stable anchors (HTML
   comments, frontmatter, block-ids). Baseline regenerates organically each cycle.

## Stale-state lookout

- The 6 placeholder smoke checklists need filling in. The `daily.md`, `meetings.md`,
  `scratch.md`, `people.md`, `project.md`, `cowork.md` files all still say
  `(TODO: list <blueprint>'s template files)`. Treat them as active TODOs to be picked up by
  the next cycle that touches each blueprint.
- `lint-display-markers-baseline.json` grandfathers existing display-marker matches. As cycles
  refactor parsers to stable anchors, the baseline can be regenerated; ideally the baseline
  shrinks over time.
- The plan + design + result trio for v0.119.0 lives at
  `Docs/plans/2026-06-16-v0.119.0-todo-hotfix-thrash-defenses-{design,plan,result}.md` — read
  these if context on the verification-net architecture is needed.
