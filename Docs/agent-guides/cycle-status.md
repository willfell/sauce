---
purpose: Live platform state. Workshop version, mechanism catalogue, blueprint catalogue, harness count, landmines summary, in-flight queue. Updated at every cycle close.
load_when: Starting a session, picking the next cycle, or sanity-checking the current state.
---

# Cycle status (live)

> Closed-cycle narratives are in `Docs/cycle-history.md`. This guide carries only the live pointers. Update both this file AND `Docs/cycle-history.md` at every cycle close (see [build-test-verify.md](build-test-verify.md) § Cycle-close artifacts).

## Current

- **Workshop version:** `0.102.0` (closed 2026-06-13)
- **Most recent cycle:** v0.102.0 project workspace MINOR — three platform moves in one cycle. **(1) Sections.** Doc-notes get foldered under `docs/<section-slug>/`; two platform defaults ship (`Knowledge`, `Notes`); per-project override via `sections: []` in project frontmatter; `section:` doc-note frontmatter mirrors folder (folder wins on conflict). Tags do orthogonal categorization. **(2) Confluence-style Docs Hub.** NEW `ProjectDocsSections` helper renders one labeled bucket per section, each as a BeaconCards row list (v0.100.0 row aesthetic preserved); per-bucket `+ New <Section>` button via the NEW EntityCreate `presetPrompts` capability — no section picker on click; trailing `Unfiled` bucket catches orphans. **(3) Project ↔ Meetings link.** Singular `project: "[[ProjectName]]"` field on meeting frontmatter (optional); project note grows `## Meetings` panel above the BacklinkPanel (top 5 + view-all expander + `+ New meeting for this project` button via `presetPrompts`); NEW `ProjectMeetingsPanel` helper; meeting hub rows gain a project pill when set. Plus an adjacent install fix: NEW `applyVaultDefaultPaths` codifies Obsidian "Files & Links" defaults (`spice/resources/{notes,attachments}/`) so new notes + pasted attachments stop scattering at vault root; `platform-claude` 0.1.3 PATCH adds `spice/resources/` to DIRECTORY_MAP_SEEDS. NEW `applyProjectSectionsMigration` installer step (Strategy A auto-migration): flat `docs/<doc>.md` → `docs/knowledge/<doc>.md` + `section: "Knowledge"` frontmatter; pre-existing doc-note subfolders register as custom sections in parent project's `sections[]`. Idempotent. workshop 0.101.1 → **0.102.0**; project 1.15.2 → **1.16.0**; meetings 0.7.0 → **0.8.0**; entity-create 0.4.0 → **0.5.0**; platform-claude 0.1.2 → **0.1.3**; cowork 0.40.0 UNCHANGED; contract 0.35.1 UNCHANGED; mechanism count UNCHANGED at 17. helper-cases 2457 → **2573 / 0** (+116 sub-asserts; new HC families EC-1..3 / PDS-1..9 / PMP-1..7 / PROJ-MAN-1..4 / PROJ-TPL-1..3 / MEET-1..2 / PSM-1..4 / VDP-1..3 / CLAUDE-MD-RES-1); cowork-smoke 954 / 0; claude-surface **217 / 0** (+1 for `spice/resources/` row); bootstrap 85 / 0; integration-smoke 36 / 0; wiki-to-docs 30 / 0; preflight exit 0; `version-sync ok: 0.102.0`. **5 lessons** (see result doc): workshop manifest catalogue drifts from per-mechanism manifests — self-install caught the gap at S6 (codified as landmine #24); `slugify` is a derive primitive, not a Jinja pipe; `{{this.project_slug}}` is NOT in the substitution catalogue (use `{{current_file.frontmatter.<field>}}`); consumer regression is POST-release per build-test-verify.md (workshop_relative_path points at brew sauce); lockstep version-pin updates across tests are established pattern, not scope creep (4 lockstep events across 5 commits). **v0.102.1 search fast-follow is NEXT** (in-page filter + dynamic tag chips + scoped-search button on `ProjectDocsSections` — design Section 13; 1-2 days post-deploy); **v0.103.0 cowork:doctor** displaced ONE cycle by v0.102.0 (shape stays locked from 2026-06-11 brainstorm). **Action required post-deploy:** `brew upgrade sauce` → per consumer vault (accuris + headspace, BOTH LOCAL): `sauce update --bump-pins && sauce install`. Install runs `applyProjectSectionsMigration` (filesystem migration) + `applyVaultDefaultPaths` (Obsidian defaults). Idempotent. No cloud-sync needed. See `Docs/cycle-history.md` § v0.102.0 + `Docs/plans/2026-06-13-v0.102.0-project-workspace-result.md` + `Docs/prompts/2026-06-13-post-v0.102.0-next-cycle-handoff.md`.

- **Workshop version (previous):** `0.101.1` (closed 2026-06-13) — v0.101.1 PATCH (same day as v0.101.0) — project Docs hub "+ New Doc" button was MISSING on every project built from the pre-0.101.1 template: the entity-create:doc-note block used a broken `AccentButton` guard dispatch (guard fed `dv`, not `dv.container`, to `AccentButton.render` → `createEl` threw → button vanished). Canonicalized the template to `customJS.EntityCreate.render(dv, {instance})` (the form all 6 other blueprints use) + NEW `applyDocsHubButtonRepair` installer step heals existing broken `docs/Docs.md` in place (`applyDocsBackfill` is create-if-absent). Broken in 13/20 accuris + 6/7 headspace hubs; surfaced once v0.100.0 made rows the primary nav. `R-WIKI-1` rewritten (it had locked in the broken form since v0.50.0). project 1.15.1 → 1.15.2; helper-cases **2457 / 0**; wiki-to-docs **30 / 0**. See `Docs/cycle-history.md` § v0.101.1.

- **Workshop version (previous):** `0.101.0` (closed 2026-06-13) — v0.101.0 verbal feedback everywhere MINOR — the prose-first feedback channel v0.99.0 built for EOD now lives on ALL FIVE briefs. **(1) Capture v=4, one shape everywhere:** one-tap `Useful: [ ] yes [ ] no` + free-text typing box on top of every brief's feedback section; EOD keeps its exact per-item Mattered/Didn't-like lists + knobs; the 4 non-EOD cadences render ONE collapsed `Kinds — quick ticks` checklist (old kind checkboxes demoted to garnish; same `{{$rating_kind_lines}}` source — ZERO new tokens). Legacy `rating-block` marker EMISSION retired (parser retained forever); `write-atomic-note-helper.js` guard accepts EITHER marker (NEW `FEEDBACK_SENTINEL`; failure token unchanged) — also resolves STRUCTURALLY the v0.99.0-era EOD discrepancy (OI said pass `surfaced_kinds_for_rating`; EOD body had no rating marker). Upgrade-day transition preserves legacy kind ticks into the new checklist via `parseRatingCallout`. **(2) Per-cadence satisfaction (schema 5):** entries become `{day, cadence, useful}` — up to five taps/day coexist; same-(day, cadence) overwrite; day-window trim (30) + 200-entry cap; prose WINS over tap on disagreement; `normalizeLearnedWeightsV5` is PURELY ADDITIVE (NO reset — counters intact; schema-4 entries gain `cadence: "eod-review"`); graduation rules untouched. **(3) Prose ingest generalized:** up to five typing boxes/day, intents tagged `source_cadence`; dedup ONE application per (day, intent, kind, entity); NEW `satisfaction` intent + `satisfaction-series` target; downrank-kind-no-entity = kind SKIP (uprank/downrank symmetry; `applyIntentKindTicks` → `applyIntentKindObservations` rename). cowork 0.39.0 → 0.40.0; workshop 0.100.1 → 0.101.0; eod sidecar enum + `"1.4.0"` / sentinel + `"v=4"`; 4 non-EOD schemas gain enum `["1.0.0","1.1.0"]` + optional slim `feedback_capture {sentinel_version, kinds_listed[]}`; **contract 0.35.1 UNCHANGED** (zero new tokens); mechanism count UNCHANGED at 17. helper-cases 2370 → **2455 / 0** (+85 sub-asserts; 24 new HC-V1010 cases); cowork-smoke 954 / 0; claude-surface 216 / 0; bootstrap 85 / 0; integration-smoke 36 / 0; preflight exit 0; `version-sync ok: 0.101.0`. **5 discoveries** (see result doc): fixture-audit scope gap (goldens live under `helpers/fixtures/` too — 1 consumed golden regen + 5 unconsumed dirs DELETED); impossible-date fixture latent until code parsed the field (HC-V0990-SCHEMA-A6 `"2026-05-32"`); two-stage review caught 5 substantive issues; either-marker guard resolved the EOD discrepancy structurally; weekly/monthly Step-7 example paths contradict tier headers (parked PATCH). **cowork:doctor displaced ONE cycle — NEXT; its self-grading digest now reads PER-CADENCE satisfaction.** **Action required post-deploy:** brew upgrade → per vault (headspace + accuris, BOTH LOCAL): Safeguard-1 snapshot → `sauce update --bump-pins` → verify 0.101.0/0.40.0 → ONE `align-scheduled-jobs` run per vault from Cloud Cowork. Schema-5 migration fires at the first post-update 03:00 run — ADDITIVE, counters INTACT. See `Docs/cycle-history.md` § v0.101.0 + `Docs/plans/2026-06-13-v0.101.0-verbal-feedback-everywhere-result.md` + `Docs/prompts/2026-06-13-post-v0.101.0-next-cycle-handoff.md`.

- **Workshop version (previous):** `0.100.1` (closed 2026-06-12) — v0.100.1 docs-hub click hotfix PATCH (same day as v0.100.0) — docs-hub row click threw `e.indexOf is not a function`: `target: (p) => p.file.link` passed a Dataview Link OBJECT to `openLinkText` (string-only API). Preserved-from-old-code line; broken-on-click since v0.50.0, masked. Target override dropped (BeaconCards default `p.file.path`); PDC-10 regression net; project 1.15.0 → 1.15.1; helper-cases **2370 / 0** (+1). Lesson: "in-production behavior" is not a safe preserve unless the line's runtime contract was verified. See `Docs/cycle-history.md` § v0.100.1.
- **Workshop version (previous):** `0.100.0` (closed 2026-06-12) — v0.100.0 project visual polish MINOR — small visual-only cycle on the project blueprint (1.14.0 -> 1.15.0). Two helpers: `project-docs-cards.js` converts the Docs hub from a BeaconCards grid to `layout: "row"` list rows with `created <date> / edited <relative>` right-side meta (fixed latent `p.created` dead-key bug); `project-nav-buttons.js` delegates the project nav row to `customJS.AccentButton.render` (accent style matching New Doc/New Note; full-width; divider margin raised). No cowork surface touched; no schema delta; no scheduled-job-contract bump (0.35.1 holds); mechanism count UNCHANGED at 17. **3 discoveries:** (1) stale guide command -- `build-test-verify.md` Self-install used `node platform/install.js .`; actual signature is `--vault . --auto-approve` -- FIXED in S5; (2) rules-regen is blueprint-version-gated (benign large diff; not mass deletion); (3) FA3-PROJ-1 version-floor is an enumerated alternation (carry-forward: numeric compare). helper-cases 2362 -> **2369 / 0** (+7: PDC-3 1->2 asserts; PDC-8 x3; PDC-9 x1; PNB-WRAP-2/3 deleted -2; PNB-ACC-1..3 x4). Visual verification deferred to post-deploy human check. See `Docs/cycle-history.md` S v0.100.0 + `Docs/plans/2026-06-12-v0.100.0-project-visual-polish-result.md`.

- **Workshop version (previous):** `0.99.0` (closed 2026-06-12) -- v0.99.0 sparse-signal feedback MINOR -- engagement-gated learning days (ENGAGED/TAP-ONLY/SILENT at Step 3.4); prose-first capture v=3 (tap + fence on top; kind-prefix scoping; uprank prose as tick); satisfaction rolling-30 series + learned_weights schema 4 (silence-reset migration; warmup_until retired; graduation = 7 engaged days AND 7 ticks+skips); convenience 0.4.1 (realclaudian delisted upstream). See `Docs/cycle-history.md` S v0.99.0.

- **Workshop version (previous):** `0.98.2` (closed 2026-06-11) — v0.98.2 feedback-loop closure MINOR — closes the v0.98.x substance arc and the user's 2026-06-10 vision in full ("section for didn't like" + "section where i could provide feedback" + "another job took what i liked, what i didn't like, my feedback" + "updated the relevant files so that all scheduled jobs would then adjust the next day"). Three structural moves landed in one cycle: (1) **Capture completes (v=2)** — Rail L gains the explicit per-item DOWNVOTE list ("Didn't like:") sharing the same `^item-<kind>-<7hex>` block-IDs as the Mattered list; sentinel bumps v=1 → v=2 (tolerant parser accepts both); EOD sidecar 1.1.0 → 1.2.0 (additive — adds `feedback_capture.items[]` identity registry + `ambiguous_items[]`). (2) **Ingest inside the existing nightly reconciler** (no new scheduled job; no contract bump) — Step 3 of `reconcile-cowork` extended to parse BOTH `rating-block` AND `feedback-capture` (v=1 + v=2) sentinels, RESTORES EOD signal to per-kind learning (v0.98.1's sentinel switch had silently broken it; absorption discovery #2); NEW Step 3.5 deterministic rollup via `ingest-feedback-helper.js` contract (mattered +0.05 / didn't-like −0.10 entity delta; knob ±0.05 post-formula re-clamped; floor-set 0.10 for hard-suppression intents); NEW Step 3.6 INLINE LLM free-text intent extraction (in the reconciler session — no separate API call, no model pin) with deterministic validation rules (reject unknown intents, non-verbatim source quotes, out-of-allowlist write targets, kindless `learned_weights` intents); Steps 4-5 extended for learned_weights schema 2 → 3 in-place additive migration on `user-preferences.md` frontmatter (nested per-kind entity maps `per_kind.<kind>.per_person/per_channel/per_topic`); NEW Step 5.5 applies non-weight deltas (microscope APPEND-only; voice corrections PROPOSED to `memory/<eng>/voice-proposals.md`; coverage gaps queued to `memory/<eng>/coverage-queue.md`; everything audit-logged to `memory/<eng>/feedback-deltas.md` with source quote); NEW Step 5.6 sweeps voice-proposal approvals from yesterday's morning brief (tick-to-approve → applied + audit-log; untouched past 7-day expiry → expired). (3) **Tomorrow's brief consumes the new weights** — morning-briefing + eod-review OIs gain consumption-thresholds clause (≥1.20 lead section; ≤0.50 cap to one line; ≤0.25 omit unless hard signal); voice-proposals callout renders only when proposals are pending. **NEW files:** `ingest-feedback-helper.js` (deterministic core ~290 LOC) + `ingest-feedback/SKILL.md` shim (blueprint source first per v0.98.1 lesson #4). **Architectural posture preserved:** deterministic-helpers-for-shape, OI-prose-for-voice. The reconciler stays pure-MCP at fire time (per its v0.97.1 posture); helper is the testable reference impl; LLM proposes, deterministic layer disposes. Voice/personality changes PROPOSED, never auto-applied — tick is the consent. Tasks-plugin trailing-annotation tolerance (`✅ YYYY-MM-DD`, `📅 YYYY-MM-DD`) added on EVERY tick line, including `parseRatingCallout` kind-checkbox lines. cowork@0.37.0 → 0.38.0 MINOR; workshop 0.98.1 → 0.98.2 MINOR; **contract_version UNCHANGED at 0.35.1** (OI-content-only change; landmine #20 doesn't fire); **learned_weights schema 2 → 3** (nested per-kind entity maps; in-place additive). **6 cycle discoveries** (see result doc): (1) plan fixture hashes must be COMPUTED not invented (plan flagged with compute-me marker — worked as designed); (2) S1.3 tick-block rewrite orphaned `let m;` declaration (implicit global in unchanged knob loop) → S1.3.1 hotfix `4ea47e7` — strict-mode adoption queued; (3) intentionally-parallel parser implementations (`_parsePrior` + `parseFeedbackCapture`) need cross-reference comments (added at S1.4); (4) `validateSidecar`'s real signature is `(payload, schemaPath)` not `(payload, cadence)` — plan one-liner adapted at S1.6 (verify-helpers-before-design rule extends to plans); (5) PRE-EXISTING: live headspace 2026-06-10 eod-review sidecar carries `coverage_gap: []` array vs schema's object shape — write-guard bypassed at fire time; queued for `cowork:doctor` observability (v0.98.2.x / v0.99.0+); (6) new shared-clause substitution tokens (`{{$voice_proposals_count}}`, `{{$voice_proposal_lines}}`) must be registered in HC-V0970-T-3 `KNOWN_SUBSTITUTION_TOKENS` allowlist (added at S1.5). **18 + 5 = 23 new HC sub-asserts** (HC-V0982-CAPTURE-A1..A6 + HC-V0982-PARSE-A1..A4 + HC-V0982-INGEST-A1..A8 + 5 EODITEMS fixture sub-asserts closing v0.98.1's end-to-end carry-forward). Final harness: helper-cases **2336 / 0** (+23); cowork-smoke **954 / 0**; claude-surface **216 / 0** (+1 ingest-feedback shim); integration-smoke **36 / 0**; cli **132 / 0**; bootstrap **85 / 0**. Per-cycle VERSION-pin sweep: ~25 hardcoded `"0.98.1"` / `"0.37.0"` assertion values bumped to `"0.98.2"` / `"0.38.0"`; ranch/platform-subscription.json workshop + cowork (blueprints[].cowork.version) lockstep per v0.98.1 lesson #5. **Action required post-deploy:** `sauce update --bump-pins` per consumer vault (materializes NEW `ingest-feedback-helper.js` + NEW `ingest-feedback/SKILL.md` shim + updated `compose-feedback-capture-helper.js` / `learn-from-checks-helper.js` / `write-atomic-note-helper.js` + updated `reconcile-cowork.md` / `eod-review.md` / `morning-briefing.md` / `_shared-clauses.md` OI + eod-review sidecar schema enum gains `1.2.0`). **Accuris two-version catch-up:** local accuris vault sits at 0.98.0 / 0.36.0; ONE `sauce update --bump-pins` catches BOTH v0.98.1 AND v0.98.2 deltas in a single pass; verify both `compose-feedback-capture-helper.js` (v0.98.1) AND `ingest-feedback-helper.js` (v0.98.2) materialize. THEN `/cowork sync-scheduled-jobs` once per vault from claude.ai's Cowork UI (contract STAYS at 0.35.1; new substitution tokens flow through). **Carry-forward:** v0.98.2.x PATCH candidates (`cowork:doctor` sidecar conformance per Discovery #5; per-item ticks on weekly/monthly; strict-mode adoption for helpers per Discovery #2); v0.99.0+ next (cross-machine wrappers + `cowork:doctor` full scope + entity-weight live-grading after 7 days + v0.95.1-migrator carry-forward). v0.98.x substance arc CLOSED. See `Docs/cycle-history.md` § v0.98.2 + `Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-{design,plan,result}.md` + `Docs/prompts/2026-06-11-post-v0.98.2-next-cycle-handoff.md`.

- **Workshop version (previous):** `0.98.1` (closed 2026-06-11) — v0.98.1 questionnaire expansion + free-text capture MINOR — replaces the EOD-only Rail L coarse kind-checkbox rating block with a richer capture surface: stable per-item `^item-<kind>-<7-char-sha>` block-IDs emitted by compose-body; per-item ticks pointing at those block-IDs via wikilinks; per-kind 3-position frequency knobs (`less / same / more`); tagged fenced `` ```feedback…``` `` free-text block; `<!-- cowork:feedback-capture v=1 -->` sentinel. Capture-only — no `learned_weights` writes, no voice updates (v0.98.2 closes the loop with ingest). cowork@0.36.0 → 0.37.0 MINOR; workshop 0.98.0 → 0.98.1 MINOR; eod-review sidecar schema 1.0.0 → 1.1.0 additive; contract_version UNCHANGED at 0.35.1. **12 new HC sub-asserts** (HC-V0981-FEEDBACK-A1..A8 + HC-V0981-PARSEFEEDBACK-A1..A4). NEW `scripts/check-files-forbidden-paths.js` Safeguard 3 guard wired into `release:preflight`. See `Docs/cycle-history.md` § v0.98.1 + `Docs/plans/2026-06-11-v0.98.1-questionnaire-capture-{design,plan,result}.md`.

- **Workshop version (previous):** `0.98.0` (closed 2026-06-10) — v0.98.0 synopsis-density rewrite MINOR — shifts the five cadence atomic notes from coverage-emphasis shape to prediction-emphasis shape (one OPEN `[!info]+ <per-cadence title>` lead callout ≤80-word predictive synopsis; per-kind callouts collapsed by default; bottom `[!tip] <closing>` REMOVED). compose-body-helper.js scope expansion at S1.4. 11 new HC sub-asserts. cowork 0.35.4 → 0.36.0; workshop 0.97.4 → 0.98.0. See `Docs/cycle-history.md` § v0.98.0.

- **Workshop version (previous):** `0.97.4` (closed 2026-06-10) — v0.97.4 prose-invariant write-guards PATCH — closes three prose-level invariant gaps the v0.97.3 cloud-sync-parity ship left visible in today's accuris morning briefing. Where v0.97.3 made the SHAPE of the brief contractually correct, v0.97.4 makes the BEHAVIOR contractually correct. **Three new write-guards in `write-atomic-note-helper.js`:** (1) **rating-callout sentinel** — when `surfaced_kinds_for_rating.length>0 && learning_enabled !== false`, body MUST contain `<!-- cowork:rating-block schema=`; on miss return `failed:contract-violation:missing-rating-callout`, no file written. (2) **anti-echo callout** — when sidecar's `render_aspects_applied` includes `anti_echo:include`, body MUST contain `Outside yesterday's frame`; on miss return `failed:contract-violation:missing-anti-echo-callout`. Sidecar-driven gate. (3) **coverage-gap inject + sidecar mirror** — when `expected_kinds.length > surfaced_kinds.length`, inject `> [!warning]+ Coverage gap` callout BEFORE the first per-kind block AND mirror `coverage_gap: { expected, surfaced, skipped }` into the sidecar. Non-failing — some kinds legitimately have no data — but the gap is now VISIBLE to the user AND machine-readable for cross-day reconciler analysis. **6 sidecar schemas** gain an optional `coverage_gap` property. **`gather-from-served-by/SKILL.md`** gains an inline `## Microscope output-shape contract (INLINE — do not delegate to a separate read)` section listing the FOUR mandatory structural elements verbatim (Utilization snapshot + urgency tiers REPLY OWED - DIRECT / GROUP / TIME-SENSITIVE / FYI + inner-circle wikilinks `**[[Person Basename]]**` + calendar markdown table Time / Event / Organizer / Status) + verbal-commitment line + pre-render self-check checklist + `do NOT improvise a flat list` fallback prohibition. **5 orchestrator-instructions** (morning-briefing / midday-tripwire / eod-review / weekly-review / monthly-review) gain Step 8.5 guidance documenting the three new writeAtomicNote params (`surfaced_kinds_for_rating`, `learning_enabled`, `expected_kinds`). **plan-dispatch investigation:** HC-V0974-PD-1..5 prove the helper contract is already correct (5-priority engagements with custom_kind:true + UUID-shaped served_by + warn-on-unreachable all return 5 dispatch_plan entries). The accuris fire's "only 3 of 5 priorities surfaced" was an LLM-side priority-loop skip — the coverage-gap write-guard now makes that exact class of failure VISIBLE in the brief. cowork@0.35.3 → 0.35.4 PATCH; workshop 0.97.3 → 0.97.4 PATCH; **contract_version UNCHANGED at 0.35.1**. **23 new HC sub-asserts** (12 WG + 5 PD + 6 GS). Final harness: helper-cases **2290 / 0** (+23); cowork-smoke **954 / 0**; claude-surface **214 / 0**; integration-smoke **36 / 0**; cli **132 / 0**; bootstrap **85 / 0**. **Action required post-deploy:** `sauce update --bump-pins` per consumer vault. Cloud-sync prompt is UNCHANGED from v0.97.3 — the source files now produce a stricter wrapper (rating/anti-echo/coverage-gap guidance flows through Steps 4/6/8.5; gather-from-served-by inline contract is loaded on every priority-loop iteration); sync mechanics unchanged. See `Docs/cycle-history.md` § v0.97.4 + `Docs/plans/2026-06-10-v0.97.4-prose-invariant-write-guards-{design,plan,result}.md`.

- **Workshop version (previous):** `0.97.3` (closed 2026-06-10) — v0.97.3 cloud-sync-parity PATCH — closes the cloud/local-sync divergence introduced by v0.97.2. The v0.97.2 `ANTI-DELEGATION (NON-NEGOTIABLE)` directive and date-resolution `PRELUDE` block were prepended by the JS helper at compose time — works locally (JS executes) but fails when the sync runs in claude.ai's Cowork UI (no Node runtime, cloud LLM can only read markdown files + substitute tokens). Today the cloud guardrail-verify correctly aborted because the directive lived only in the JS source. **Migration:** three new shared clauses (`anti_delegation_clause`, `prelude_block`, `done_block`) added to `_shared-clauses.md` as `## <key>` heading blocks. All 6 orchestrator-instructions cadence files (morning-briefing/midday-tripwire/eod-review/weekly-review/monthly-review/reconcile-cowork) now reference them at top + bottom via `{{shared.*}}`. JS composer's wrap-with-PRELUDE-and-DONE block stripped — `composeFromOrchestratorInstructions` returns `_substituteStaticTokens(template, tokens)` directly. Both rails (local Node, cloud LLM) now resolve the same vault sources and produce byte-identical composed wrappers. cowork@0.35.2 → 0.35.3 PATCH; workshop 0.97.2 → 0.97.3 PATCH; **contract_version UNCHANGED at 0.35.1** (contract shape didn't change). **11 new HC sub-asserts** (6 CSP-CADENCE references + 3 CSP-CLAUSE definitions + 2 CSP-HELPER absence). Final harness: helper-cases **2267 / 0** (+11); cowork-smoke **954 / 0**; claude-surface **214 / 0**; integration-smoke **36 / 0**; cli **132 / 0**; bootstrap **85 / 0**. Pre-existing HC-V0972-AD-* + HC-V0972-DP-* still GREEN (strings still in composed output, sourced from `_shared-clauses.md` now). **Action required post-deploy:** `sauce update --bump-pins` per consumer vault (materializes v0.97.3 `_shared-clauses.md` + cadence files); THEN `/cowork sync-scheduled-jobs` from claude.ai's Cowork UI — cloud LLM now sees all three guardrail substrings in vault sources, passes verify, pushes wrappers via `update_scheduled_task` MCP. See `Docs/cycle-history.md` § v0.97.3 + `Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-{design,plan,result}.md`.

- **Workshop version (previous):** `0.97.2` (closed 2026-06-10) — v0.97.2 wrapper-delegation-proof PATCH — closes the cron LLM delegation regression that broke accuris's 2026-06-10 morning fire while headspace's identical-architecture run at the same fire-time was structurally perfect. Same wrapper, same contract — different LLM behavioral choice. **Three knobs:** (1) **anti-delegation directive** prepended as the first non-blank paragraph of every composed wrapper, ahead of the PRELUDE — bans Task()/Agent()/subagent invocations + summarize-and-hand-off pattern. (2) **Inline write contract** — replaces Step 7's `READ <vault>/.claude/skills/cowork/skills/write-run-note-<cadence>/SKILL.md ... follow its Steps section` in ALL 5 atomic-note orchestrator-instructions files (morning-briefing, midday-tripwire, eod-review, weekly-review, monthly-review) with a literal 5-substep inline contract (7a path / 7b frontmatter YAML / 7c DataviewJS / 7d callout body skeleton / 7e obsidian_put_content invocation). Path is now the literal `spice/cowork/daily/{{$today_dirpath}}/{{$cadence}}.md`. Frontmatter shape is a literal YAML block with explicit FORBIDDEN-keys list (`cadence`, `date`, `engagement`, `generated_at`, `week`, `month`, `year`, `schema_version`). (3) **PRELUDE date fix** — replaces example-based PRELUDE (`e.g. "Tuesday"` which accuris's LLM literally copied into the title on a Wednesday) with a Bash-command-driven PRELUDE (`TZ='{{$timezone}}' date '+%A'` etc.) + verbatim `do NOT use any example value` instruction. cowork@0.35.1 → 0.35.2 PATCH; workshop 0.97.1 → 0.97.2 PATCH; **contract_version UNCHANGED at 0.35.1**. **26 new HC sub-asserts** (7 AD + 15 IW + 4 DP). Final harness: helper-cases **2256 / 0** (+26); cowork-smoke **954 / 0**; claude-surface **214 / 0**; integration-smoke **36 / 0**; cli **132 / 0**; bootstrap **85 / 0**. v0.97.3 immediate follow-up moved ANTI-DELEGATION + PRELUDE out of the JS helper into `_shared-clauses.md` for cloud-sync parity. See `Docs/cycle-history.md` § v0.97.2 + `Docs/plans/2026-06-10-v0.97.2-wrapper-delegation-proof-{design,plan,result}.md`.

- **Workshop version (previous):** `0.97.1` (closed 2026-06-09) — v0.97.1 reconciler-as-cadence PATCH — reconciler migrated from local launchd (v0.97.0 Rail R) to claude.ai scheduled job. NEW `reconcile-cowork` cadence in `scheduled-job-contract.json` fires nightly 03:00 local via the same Rail A mechanism as other cadences (default_cron `0 3 * * *`, `sub_skill_name: null` — no atomic note write, `includes_microscopes: false`). NEW `content/data/orchestrator-instructions/reconcile-cowork.md` (9-step LLM-driven reconciler: pre-flight → list yesterday's notes → backfill missing sidecars → parse rating callouts → aggregate observations → update learned_weights with clamp [0.10, 3.00] + banker's rounding + warmup graduation → write .bak + updated user-preferences.md → walk 30-day sidecar window for heartbeat → emit warning callout to memory.md if missed → append reconciler-log entry capped at 30). NEW `skills/orchestrators/reconcile-cowork/SKILL.md` (21-line shim per v0.97.0 single-source pattern). `sauce reconcile-cowork --install-launchd` deprecated (one-cycle grace; preserves `installer.installLaunchd()` call). cowork@0.35.0 → 0.35.1 PATCH; workshop 0.97.0 → 0.97.1 PATCH; contract_version 0.35.0 → 0.35.1 (lockstep). **15 new HC sub-asserts** (12 RC + 3 DEPR). Final harness: helper-cases **2230 / 0** (+15); cowork-smoke **954 / 0**. **Action required post-deploy:** run `/cowork sync-scheduled-jobs` once from claude.ai's Cowork UI — Rail A picks up the new cadence + creates `cowork-reconcile-cowork-<engagement_id>` tasks per enabled engagement. See `Docs/cycle-history.md` § v0.97.1 + `Docs/plans/2026-06-09-v0.97.1-reconciler-as-cadence-{design,plan,result}.md` + `Docs/install/v0.97.1-onboarding.md`.

- **Workshop version (previous):** `0.97.0` (closed 2026-06-09) — v0.97.0 cowork-rethought-2 MINOR — five rails closing the v0.96.x cron-LLM dead-code-skill + sidecar-not-written + manual-paste-flow surface. **Rail O (orchestrator-instructions single-source):** orchestrator bodies migrated from SKILL.md to canonical `data/orchestrator-instructions/<cadence>.md` per cadence + `_shared-clauses.md` (9 reusable blocks). SKILL.md becomes a ≤30-line shim pointing at the canonical source via `readCadenceOrchestrator`. **Rail T (wrapper template inlines orchestrator):** `composeFromOrchestratorInstructions` rewrite of compose-scheduled-job-wrappers-helper; `scheduled-job-contract.json` `wrapper_template` field replaced by `wrapper_template_source` pointer. Fire-time tokens `{{$today_*}}` stay literal — LLM resolves at fire time, killing the timestamp-drift class entirely. Validator gains shape-tolerance (legacy inline OR new pointer); legacy HC-V0930-* + HC-V0931-* + HC-V0951-K3 compose-path asserts re-routed via frozen fixture at `platform/test/fixtures/cowork/legacy-contracts/v0.34.1-scheduled-job-contract.json`. **Rail S (sidecar write):** cron LLM writes `.cowork.json` sidecar via `obsidian_put_content` immediately after `.md`; reconciler backfills missing sidecars overnight from `.md` prose. **Rail A (claude.ai-Cowork-UI-native auto-sync):** `cowork:sync-scheduled-jobs` SKILL.md rewritten — uses `list_scheduled_tasks` + `update_scheduled_task` + `create_scheduled_task` MCP tools; schedule preservation invariant (cron field NEVER set on update); orphan tasks warn (not delete); idempotent re-run; aborts cleanly when MCP missing. NEW `cowork-sync-mcp-helper`. **Rail R (local launchd reconciler):** NEW `sauce reconcile-cowork` CLI verb (`cmd-reconcile-cowork.js`) + NEW `cowork-reconciler` mechanism (5 sub-modules: sidecar-backfill, learned-weights-update, heartbeat-check, reconciler-log, index) + NEW `vault-paths-registry-helper` + NEW launchd plist template + `--install-launchd` one-shot. Nightly 03:00 MT processes all configured vaults via shared filesystem. cowork@0.34.1 → 0.35.0 MINOR; workshop 0.96.2 → 0.97.0 MINOR; contract_version 0.34.1 → 0.35.0 (lockstep mirror per landmine #20); mechanism count unchanged at 17. **76 new HC sub-asserts** (24 Rail O + 24 Rail T + 12 Rail A + 16 Rail R). Final harness: helper-cases **2215 / 0** (+62); cowork-smoke **954 / 0** (+2); claude-surface **213 / 0**; integration-smoke **36 / 0**; cli **132 / 0**. Preflight `version-sync ok: 0.97.0` ALL GREEN. **Resolves:** Findings 2 + 3 from Workflow run wzr3g3uad (sidecars not written + orchestrator skill not loaded in cron). **Parked for cowork-rethought-3 or later:** per-item L2R + Thompson sampling, memory salience/decay, per-skill SemVer + versioning CI, the v0.95.1-migrator. See `Docs/cycle-history.md` § v0.97.0 + `Docs/plans/2026-06-09-v0.97.0-cowork-rethought-2-{design,plan,result}.md` + `Docs/install/v0.97-onboarding.md`.

- **Workshop version (previous):** `0.96.2` (closed 2026-06-09) — v0.96.2 daily-dashboard-tskeys-hotfix PATCH — one-stage hotfix patching the daily-blueprint dashboard. `SpaceDailyDashboard`'s `tsKeys` arg passed to `ActivityFeed.render` now includes `"day"` as the FIRST element, so cowork atomic notes are bucketed by their semantic `day:` frontmatter field (canonical day-of-action) before falling back to wall-clock `created_at` / `status_changed_at`. Local `inWindow` predicate inside `_getActivityCount` consults `day` → `created_at` → `status_changed_at` in order; when `day` is present, it is fully authoritative (no OR-fallback) — prevents double-surface on both canonical day and wall-clock day. Closes the timestamp-drift class diagnosed via Workflow run wzr3g3uad (cowork EOD note with `day:"2026-06-08"` but `created_at: 2026-06-09T04:10:...` dropped from Monday daily). daily@0.13.5 → 0.13.6 PATCH; workshop 0.96.1 → 0.96.2 PATCH; cowork blueprint UNCHANGED at 0.34.1 (landmine #20 pins contract to cowork, not daily); mechanism count unchanged at 17. **3 HC sub-asserts added** (HC-V0962-DASH-1..3). Final harness: helper-cases **2153 / 0** (+3); cowork-smoke **952 / 0**; claude-surface **213 / 0**; integration-smoke **36 / 0**; activity-feed **120 / 0**; bootstrap **85 / 0**. Preflight `version-sync ok: 0.96.2` ALL GREEN. **Open carry-forwards to v0.97:** (1) activity-feed mechanism's own inWindow predicate still uses pure OR-semantics across all tsKeys — may double-surface at render time even with dashboard pre-count fixed; (2) sidecars-not-written finding from same Workflow run; (3) skill-not-loaded finding from same Workflow run. See `Docs/cycle-history.md` § v0.96.2 + `Docs/plans/2026-06-09-v0.96.2-daily-dashboard-tskeys-hotfix-result.md`.

- **Workshop version (previous):** `0.96.1` (closed 2026-06-08) — v0.96.1 cowork-rethought-1-patch PATCH — two rails patching v0.96.0 cowork-rethought-1 surface. **Rail M (multi-engagement learned_weights nesting):** `learned_weights:` shape switches from flat single-engagement to nested-by-engagement_id; legacy v0.96.0 flat shape auto-migrates on first post-patch synthesize-day fire via NEW `_normalizeLearnedWeights` adapter in `learn-from-checks-helper.js`. `composeFinalPreferences` reads nested shape (with backwards-compat fallback when `engagement_id` not passed). learn-from-checks engagement-mismatch guard retired (no longer applicable). schema_version 1.0.0 → 1.1.0. **Rail H (cron heartbeat read):** NEW sub-skill `cowork:check-heartbeat` walks `.cowork.json` sidecars in synthesize-day post-step; emits `> [!warning]+ Cron heartbeat anomaly` callout to memory.md when any enabled cadence misses its freshness window. NEW `check-heartbeat-helper.js` (walks sidecars + computes per-cadence age vs window). NEW data file `cadence-freshness-windows.json`. NEW data file `learned_weights-skeleton.json` (canonical nested shape for migration). Silent on green. First-fire mitigation message for fresh-vault scenarios. **Cron prompts UNCHANGED** — no `/cowork sync-scheduled-jobs` re-run needed by consumers. **1 new helper** (check-heartbeat-helper); **1 new sub-skill** (check-heartbeat); **2 new data files** (cadence-freshness-windows, learned_weights-skeleton). cowork@0.34.0 → 0.34.1 PATCH; workshop 0.96.0 → 0.96.1 PATCH; contract_version 0.34.0 → 0.34.1 (lockstep mirror per landmine #20); mechanism count unchanged at 17. **15 HC sub-asserts added** (9 Rail M + 6 Rail H). Final harness: helper-cases **2150 / 0** (+15); cowork-smoke **952 / 0** (+3); claude-surface **213 / 0** (+1 — new check-heartbeat sub-skill claude_surface entry); integration-smoke **36 / 0**. Preflight `version-sync ok: 0.96.1` ALL GREEN. **Parked for cowork-rethought-2 or later:** same as v0.96.0 — W2 (full structured-output API rewrite), memory salience/decay, per-item L2R + Thompson sampling, cron heartbeat WRITE surface (this patch is read-only via sidecars), per-skill SemVer + versioning CI, the v0.95.1-migrator. See `Docs/cycle-history.md` § v0.96.1 + `Docs/plans/2026-06-08-v0.96.1-cowork-rethought-1-patch-{design,plan,result}.md`.

- **Workshop version (previous):** `0.96.0` (closed 2026-06-08) — v0.96.0 cowork-rethought-1 MINOR — three rails closing the cowork sidecar-determinism + MCP auto-discovery + per-kind preference learning surface. **Rail W (sidecar determinism):** NEW `.cowork.json` sidecar per atomic note; JSON-schema Verify replaces regex; `body_assertions[]` retired; per-cadence write-run-note slimmed to shims around `write-atomic-note-helper.js`. NEW JSON Schema files for the 5 cadence sidecar shapes + cold-MB variant. **Rail D (MCP auto-discovery):** NEW `classifyConnectedKinds` with deterministic patterns (8 canonical kinds) + LLM fallback (cached); `kind-patterns.json` + `kind-classifier-cache.json` data files; 14th contract key `pending_confirmations[]`; detection callout default-include; bootstrap-vault auto-fill from kind baselines (docs/weather new baselines). **Rail L (per-kind preference learning):** NEW `learned_weights:` in user-preferences.md (lazy-init on first fire — S0.5 mitigation for materialize_once SKIP behavior); `> [!todo]+ Was today useful?` rating callout default-on; NEW sub-skill `cowork:learn-from-checks` at `skills/skills/learn-from-checks/SKILL.md` invoked from synthesize-day post-step; weight-aware `composeFinalPreferences` ordering; day-14 must-surface backstop. **Subsumed** auto-update-prefs (was queued v0.96.0+) → Rail L at MVP granularity. **4 new helpers** (write-atomic-note, validate-sidecar, kind-classifier, learn-from-checks); **1 new sub-skill** (learn-from-checks); **6 new JSON Schema files**; **2 new data files** (kind-patterns, kind-classifier-cache); **2 new baselines** (docs, weather). Plan-dispatch contract grows 13 → 14 keys (NEW `pending_confirmations[]`). 3 new UI surfaces (detection callout, rating callout, upgrade notice). cowork@0.33.0 → 0.34.0 MINOR; workshop 0.95.1 → 0.96.0 MINOR; contract_version 0.33.0 → 0.34.0 (lockstep mirror per landmine #20); mechanism count unchanged at 17. Final harness: helper-cases **2135 / 0** (+38 from v0.95.1's 2076 baseline including new HC + re-routed); cowork-smoke **949 / 0** (+12); claude-surface **212 / 0** (+1 — new learn-from-checks sub-skill claude_surface entry); integration-smoke **36 / 0**. Preflight `version-sync ok: 0.96.0` ALL GREEN. **Parked for cowork-rethought-2 or later:** W2 (full structured-output API rewrite), memory salience/decay, per-item L2R + Thompson sampling, cron heartbeat, per-skill SemVer + versioning CI, the v0.95.1-migrator. See `Docs/cycle-history.md` § v0.96.0 + `Docs/plans/2026-06-08-v0.96.0-cowork-rethought-1-{design,plan,result}.md`.

- **Workshop version (previous):** `0.95.1` (closed 2026-06-08) — v0.95.1 cowork-anti-echo MINOR — three composable knobs breaking the cowork memory echo loop. Bundled-MINOR scope (Approach B, opt-in everywhere, LLM-based theme extraction). **Knob 1 (anti_echo render aspect):** NEW `render_aspects.anti_echo` (opt-in `include` / default `skip`) emits per-fire `[!question] Outside yesterday's frame` callout in MB/midday/EOD naming ONE item from today's gather that doesn't relate to yesterday's carry-forward bullets. NEW `dispatch-plan-helper.js` export `deriveExcludedThemes(yesterdayMemory)`; NEW `compose-body-helper.js` exports `injectAntiEchoCallout` + `ANTI_ECHO_ELIGIBLE_CADENCES` const. **plan-dispatch contract grows 12 → 13 keys** (NEW `excluded_themes: string[]` always-present, empty when not opted in). **Knob 2 (capture-frame-drift tripwire):** NEW sub-skill `cowork:capture-frame-drift` at `skills/skills/capture-frame-drift/SKILL.md` — background tripwire post-synthesize-day, gated by `plan.tripwire_aspects.includes("frame_drift")`. Requires ≥5 days of memory.md syntheses; calls `claude-haiku-4-5` to extract themes per day; deterministic 3-flag DriftReport (frame_repeat ≥4/5, subject_dominance ≥3/5, explicit_null 3 consecutive). On any flag, appends `[!warning]- Frame may be stuck` callout to today's memory.md + frontmatter additions. NEW `capture-frame-drift-helper.js` (3 exports). read-memory grows 3-key → 4-key return shape (adds `drift_warning: string | null`); MB Yesterday-at-a-glance injects drift_warning when non-null. **Knob 3 (lens_shift cadence):** NEW cadence opt-in via `supported_cadences`; default cron `0 7 * * 6` (weekly Saturday 7am cold-mode MB). MB orchestrator gains `--cadence lens_shift` branch that skips pre-flight 3a (read-memory) + 3a.5 (gather-semantic-related) — fully cold MB. NEW atomic-note type `cowork-morning-briefing-cold` + rule_fragment; slug pattern `morning-briefing-cold-<engagement>.md`. scheduled-job-contract.json gains `lens_shift` entry; contract_version `0.32.0` → `0.33.0`. compose-scheduled-job-wrappers-helper emits 6th wrapper block when engagement opts in. NEW CustomJS class `CoworkLensShiftCards` renders Daily Hub side-by-side warm/cold pair (slug-match, no `companion_to:`). 3 engagement-types (personal/w2-fte/consulting) gain `render_aspects.anti_echo: "skip"` default + `supported_cadences += ["lens_shift"]`. cowork:bootstrap-vault interview grows 3 new opt-in questions. activity-feed allowlist registers `cowork-morning-briefing-cold`. cowork@0.32.0 → 0.33.0 MINOR; workshop 0.95.0 → 0.95.1 MINOR; mechanism count unchanged at 17. Final smoke **2076 / 0** (helper-cases +39 from v0.95.0's 2037); cowork-smoke **927 / 0** (+3); claude-surface **211 / 0** (+1). ALL GREEN; preflight `version-sync ok: 0.95.1`. Per-cycle VERSION pin sweep: 24 hardcoded `"0.95.0"`/`"0.32.0"` assertions in run-helper-cases.js + 12 in run-bootstrap.js + 1 in run-cowork-smoke.js bumped; ranch/platform-subscription.json lockstep workshop_version + cowork pin bumped (v0.93.3 lesson 5.4). Migrator design + plan PARKED to v0.96.0+ (S0.6 parking memos; execution-ready). See `Docs/cycle-history.md` § v0.95.1.

- **Workshop version (previous):** `0.95.0` (closed 2026-06-07) — v0.95.0 cowork-spine MINOR — bundles skill structural cohesion sweep + knob ergonomics refactor. NEW `cowork:plan-dispatch` sub-skill wraps the pre-existing `dispatch-plan-helper.js` PLUS 3 NEW additive exports. Returns a 12-key contract that orchestrators consume as their single source of truth for Gather + Write. 5 atomic-note orchestrators slim by net **−587 lines workshop-wide**. NEW `data/kind-titles.json` v1.0.0 + NEW `engagement.overrides` schema on vault-config.md engagements[] + NEW `Docs/agent-guides/cowork-orchestrator-template.md` v1.0.0. cowork@0.31.2 → 0.32.0 MINOR. +15 HC-V0950-* sub-asserts; ~36 pre-existing re-routed through plan-dispatch. Final smoke 2037 / cowork-smoke 924 / claude-surface 210. Per-cycle VERSION pin sweep: 22 hardcoded "0.94.0" + 16 hardcoded "0.31.2" assertions bumped. See `Docs/cycle-history.md` § v0.95.0.

- **Workshop version (previous):** `0.94.0` (closed 2026-06-07) — v0.94.0 applyExternalPluginInstall MINOR — closes v0.93.3 existing-consumer install gap; adds claudian as a default. New install-time helper `applyExternalPluginInstall` in `platform/install.js` mirrors `phaseFetchPlugins`'s behavior at install time (warn-and-continue posture). convenience 0.3.0 → 0.4.0 (`external_plugins[]` += `{id: "realclaudian"}` — YishenTu/claudian). Landmine #17 amended: two network gateways, two postures, one shared fetch layer. +6 HC-EPI cases in run-bootstrap.js. Per-cycle VERSION pin sweep: 10 hardcoded "0.93.3" assertions in run-helper-cases.js bumped to "0.94.0". Five NEW lessons captured in result doc.

- **Workshop version (previous):** `0.93.3` (closed 2026-06-07) — v0.93.3 required-plugins PATCH (contract-redefined mid-execution) — two mechanism-manifest deltas closing a documentation gap where two de-facto required Obsidian community plugins were not declared by the platform manifest. **Critical mid-execution finding**: `applyExternalPlugins` in `platform/install.js:2693` is warning-only (`"the installer cannot install Obsidian community plugins itself"`); actual plugin install lives in `phaseFetchPlugins` (`platform/bootstrap.js:120`), invoked only during fresh-vault bootstrap. So v0.93.3 ships as: **fresh consumers** (`sauce bootstrap`) → both plugins auto-installed; **existing consumers** (`sauce update --bump-pins`) → warning Notices + manual install required. Existing-consumer auto-install deferred to v0.94.0 (extend `platform/install.js` to call `fetchPlugin` for absent `external_plugins[]`). convenience 0.2.4 → 0.3.0 (`external_plugins[]` += new-tab-default-page + `community_plugin_settings[]` += new-tab-default-page with whatToOpen=daily-notes/mode=reading-mode). smart-connections-bridge 0.1.1 → 0.2.0 (`external_plugins[]` += `{id: "smart-connections", required: true}`). +3 HC sub-asserts: BS18 + BS19 in run-bootstrap.js (exercise the bootstrap fetch path); CP6 in run-helper-cases.js (applyCommunityPluginData write). Per-cycle VERSION-pin maintenance: 10 hardcoded assertion values bumped "0.93.2" → "0.93.3" across HC-V0891/V0900/V0920/V0930/V0931 blocks (landmine #16 in-cycle bump rule — not in S1.4 plan; folded into S1.4b commit). Final smoke **2022 / 0** unchanged from v0.93.2 + 3 new (2025); cowork-smoke 923 / 0; claude-surface 209 / 0; dogfood install clean exit 0 with two intentional warning Notices reflecting the warning-only contract.

- **Workshop version (previous):** `0.93.2` (closed 2026-06-07) — v0.93.2 contract_version lockstep + fixture regeneration PATCH — closes the `contract_version_mismatch:0.31.0:0.31.1` warning surfaced by v0.93.1's first live run on accuris (laptop). Root cause: v0.93.1 PATCH bumped cowork blueprint (helper code + SKILL.md prose) but left `scheduled-job-contract.json` at contract_version 0.31.0 because the contract DATA didn't change. **Fix**: codify convention — `contract_version` ALWAYS mirrors cowork blueprint version exactly, even when contract data unchanged. Retroactive lockstep bump: contract_version 0.31.0 → 0.31.2, cowork 0.31.1 → 0.31.2, workshop 0.93.1 → 0.93.2 + 18+ HC pin assertions + HC-V0930-CONTRACT-A2 assertion. 8 byte-identical fixtures regenerated via helper to capture bumped version strings; case-edge-contract-version-mismatch retains deliberate mismatch. No helper code change. Final smoke **2016 / 0** unchanged from v0.93.1.

- **Workshop version (previous):** `0.93.1` (closed 2026-06-06)
- **Most recent cycle:** v0.93.1 FLN-v93-8 helper schema adapters + FLN-v93-9 scheduled-tasks MCP push PATCH — bundles two same-day fixes after v0.93.0's first live run. FLN-v93-8: `composeMcpDispatchLines` adds `_resolveServedBy()` adapter accepting `served_by` as array (picks first non-`<text> — `-marker string + strips surrounding quote chars) — closes headspace `chat.served_by: ["iMCP"]` drift. `composeScheduledJobWrappers` voice-notes fallback chain extends `notes → vibe_notes → voice_notes → pep_talk_style` — closes headspace `vibe_notes`-instead-of-`notes` drift. FLN-v93-9: `cowork:sync-scheduled-jobs/SKILL.md` gains NEW `## Apply` section (6 steps 18-23) that auto-pushes wrapper bodies into claude.ai's scheduled tasks via the Cowork scheduled-tasks MCP when reachable (`update_scheduled_task({taskId, prompt})`). Hard rules: NEVER touch `cron`/`schedule` (user's live schedules diverge from contract `schedule_hint` docs; ground truth lives on the task); NEVER touch tasks outside the 5-cadence pattern (out-of-scope `cowork-debt-scoreboard-*` / `cowork-capture-tick-*` / `cowork-synthesize-day/week-*` skipped silently). Idempotent on re-run. File write preserved as audit artifact + paste fallback (`mcp-unreachable` non-fatal warning). 4 new Apply-step failure tokens: `mcp-unreachable`, `mcp-task-absent:<taskId>`, `mcp-update-failed:<taskId>:<reason>`, `mcp-listed-out-of-scope-task:<taskId>`. ~18 new HC sub-asserts: HC-V0931-ADAPTER (4) + NOTES (4) + APPLY (6) + VERSION (4). Existing 12 byte-identical fixtures unchanged. cowork@0.31.0 → 0.31.1 PATCH; workshop 0.93.0 → 0.93.1 PATCH; contract_version unchanged at 0.31.0 (no schema or wrapper_template change). Final smoke **2016 passed / 0 failed** (delta +18 from v0.93.0's 1998); cowork-smoke 923 / 0 unchanged; claude-surface 209 / 0 unchanged. Eliminates manual paste workflow on every re-run — orchestrator auto-pushes to claude.ai's Cowork UI.

- **Workshop version (previous):** `0.93.0` (closed 2026-06-06)
- **Most recent cycle:** v0.93.0 cowork:sync-scheduled-jobs orchestrator + compose-scheduled-job-wrappers helper + scheduled-job-contract.json MINOR — closes the Cowork scheduled-job wrapper drift problem: claude.ai's scheduled-task UI is the cron runtime for the 5 cowork cadences (prompt body lives in claude.ai's UI outside the sauce vault), so after each v-bump touching the cron contract (v0.91.1 path / v0.91.2 frontmatter / v0.91.3 connectivity / v0.92.0 body-shape) the user had to remember to re-paste 5 wrapper bodies per engagement. v0.93.0 ships a user-invocable orchestrator `cowork:sync-scheduled-jobs` (FLN-v79-4 FLAT dest, 108-line SKILL.md with 17 numbered steps + frontmatter + 8 sections) that emits paste-ready wrapper bodies at `spice/cowork/scheduled-job-wrappers/<engagement_id>.md` aligned with current sauce + cowork versions + engagement's actual prefs.mcps state. Backed by NEW pure helper `compose-scheduled-job-wrappers-helper.js` (4 exports — composeScheduledJobWrappers + composeMcpDispatchLines + validateContract + substituteTemplate; 4-mode landmine #20 substitution-token validator; caller supplies timestamps + version strings for full determinism) + NEW canonical data file `scheduled-job-contract.json` (contract_version 0.31.0; cadence_order [5]; 13 substitution_tokens; 3 shared_clauses; 5 cadences with per-cadence wrapper_template + includes_microscopes flag — midday-tripwire omits MICROSCOPES clause). **Architectural commitment**: contract.json contains ZERO MCP brand strings; all dispatch lines computed kind-by-kind from prefs.mcps at compose time. case-novel-mcp-shape fixture with contrived `slack` kind round-trips byte-identically without contract changes — load-bearing for the dynamic-design guarantee. 12 golden fixtures: 8 byte-identical (case-accuris-shape load-bearing — derived from accuris production wrappers + case-headspace-shape + case-novel-mcp-shape + 5 edge cases) + 4 validator-failure fixtures (one per Mode 1-4). ~70 new HC sub-asserts across COMPOSE-SHAPE (16) / VALIDATOR (12) / SUB-HELPER (12) / CONTRACT (6) / SKILL (10) / INTEGRATION (5) / MANIFEST (5) / VERSION (4). cowork@0.30.0 → 0.31.0 MINOR; workshop 0.92.0 → 0.93.0 MINOR; mechanism count unchanged at 17. Final smoke **1998 passed / 0 failed** (delta +70 from v0.92.0's 1928); cowork-smoke 923 unchanged; claude-surface 209 (delta +1 — new sync-scheduled-jobs claude_surface entry). Dogfood install clean; consumer install (accuris/headspace) pin bumps applied (cowork 0.31.0); full materialization deferred to post-Homebrew-release per existing convention. **5 FLNs queued by v0.93.0**: FLN-v93-1 (extract wrapper_template strings to per-cadence files if editing ergonomics motivate), FLN-v93-2 (factor engagement-resolution into shared sub-skill — now 8 carriers), FLN-v93-3 (delta-from-prior-contract section + contract_history.json), FLN-v93-4 (--dry-run flag), FLN-v93-5 (cowork:audit-scheduled-jobs drift detector). **Sequencing**: v0.92.1 = required-plugins PATCH still queued (in-flight design at `Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md`; needs target_workshop_version rewrite from 0.92.0 to 0.92.1 — note this is now a v0.92.x PATCH for v0.92.0 body-composer, not v0.93.0 — wait until user revisits). **Cycle stack 2-day stretch**: 12 cycles shipped (v0.89.0 → ... → v0.93.0); +682 HC sub-asserts (1316 → 1998).

- **Workshop version (previous):** `0.92.0` (closed 2026-06-06) — v0.92.0 cowork:compose-body sub-skill + body-shape write-guard + Step 14 refactor MINOR — closes the orchestrator body-composition prose-attention ceiling surfaced by 2026-06-05's accuris morning-briefing live run on the laptop (SOFT body-composition rule at orchestrator Step 14 line 292 was DROPPED in favor of plain `##` markdown headers, even as all HARD v0.91.x rules fired correctly). The fix is the 4th layer in the v0.91.x defense stack: NEW `cowork:compose-body` pure-helper sub-skill (replaces v0.78.0 stub) produces canonical body markdown + body_assertions[] list; orchestrator Step 14 invokes it and passes body_assertions through to write-run-note; NEW body-shape write-guard in 5 write-run-note sub-skills hard-rejects bodies that don't satisfy each assertion via `body.includes(assertion)`. 9 byte-identical golden fixtures (5 cadences + 4 edge cases); case-morning-briefing derived from accuris production 2026-06-05 morning-briefing.md. ~113 new HC sub-asserts across COMPOSE / GUARD / ORCH / INTEGRATION / MANIFEST / VERSION / SURFACE groups. cowork@0.29.3 → 0.30.0 MINOR; workshop 0.91.3 → 0.92.0 MINOR; mechanism count unchanged at 17. Final smoke **1928 passed / 0 failed** (delta +113 from v0.91.3's 1815); cowork-smoke 923 (delta -8 — HC-V0780-E1 retired + 4 legacy assertion groups redirected to v0.92.0 source-of-truth equivalents); claude-surface 208 (delta +1 — new compose-body claude_surface entry). Dogfood install clean; consumer install (accuris/headspace) pin bumps applied (cowork 0.30.0); full materialization deferred to post-Homebrew-release per existing convention. **Bundled workshop-hygiene cleanups**: workshop self-subscription gained `people @ 0.6.0` (was missing, blocking cowork dogfood since cowork@0.27.0 v0.89.0); 4 stale mechanism pins caught up (styling/platform-claude/activity-feed/scratch). **5 FLNs queued by v0.92.0**: FLN-v92-1 (shared write-guard helper), FLN-v92-2 (full orchestrator slim), FLN-v92-3 (per-cadence end-to-end fixtures with frontmatter + write-guard roundtrip), FLN-v92-4 (workshop self-subscription completion for the 7 remaining unsubscribed blueprints), FLN-v92-5 (installed.workshop_path cache invalidation on workshop bump). **Sequencing**: v0.92.1 = required-plugins PATCH (in-flight design at `Docs/plans/2026-06-01-v0.92.0-required-plugins-design.md` — needs target_workshop_version rewrite to 0.92.1); v0.93.0 = scheduled-jobs-sync MINOR (design + plan at `Docs/plans/2026-06-05-v0.92.x-scheduled-jobs-sync-{design,plan}.md`).

- **Workshop version (previous):** `0.91.3` (closed 2026-06-05) — v0.91.3 trust-user-prefs-over-vault-config + deferred-tool-loading + dataviewjs-regex-tightening PATCH — three coordinated structural fixes after live accuris morning-briefing run on 2026-06-05 produced a useless "MCP unavailable" stub-warning artifact while the same machine's weekly-review run produced rich content WITHOUT consulting the orchestrator. Three-fix bundle attacks distinct root causes. **Fix A — connectivity signal authority.** 5 orchestrator SKILL.md files (morning-briefing/midday-tripwire/eod-review/weekly-review/monthly-review) gain a "Connectivity signal authority (v0.91.3)" clause in Step 3c/3d dispatch plan: TRUST `prefs.mcps[<kind>].served_by` + `prefs.mcps[<kind>].connected` (recent, runtime-authoritative); DO NOT TRUST `vault-config.mcp_map` (bootstrap-time-only, stale-prone, audit hint only). Cross-reference resolved served_by names against `reachable_namespaces[]` for final dispatch decision. Accuris vault-config.mcp_map had stale `gmail: missing` / `gcal: missing` entries (accuris uses M365 not Google) which the orchestrator was reading as runtime truth — causing it to emit "MCP unavailable" warnings instead of dispatching to the connected M365 served_by. **Fix B — deferred MCP tool loading.** 5 orchestrators gain MANDATORY (v0.91.3) paragraph at top of `## Gather`, after the existing MANDATORY priority-loop paragraph: "load deferred MCP tools UPFRONT before the priority loop." LLMs running orchestrators frequently encountered deferred-tool stubs (mcp__claude_ai_*, mcp__accuris-obsidian__*, mcp__plugin_*) and silently fell back to warning callouts. Eager-load eliminates the silent-fallback path. **Fix C — dataviewjs regex tightening.** 5 write-run-note sub-skills' canonical SpaceNavButtons invocation check + 5 orchestrator Step 17 regexes tightened to require canonical pattern `dv.view("ranch/views/customjs-guard", { class: "SpaceNavButtons" })`. Explicitly REJECTS the hallucinated shape `const { SpaceNavButtons } = customJS; SpaceNavButtons(dv, {...})` which produces a `SpaceNavButtons is not a function` TypeError at render time. Accuris morning-briefing on 2026-06-05 emitted the hallucinated invocation. Sub-skill regex backstop refuses the bad pattern even if orchestrator prose check passes. cowork@0.29.2 → 0.29.3 PATCH; workshop 0.91.2 → 0.91.3 PATCH; mechanism count unchanged at 17. 55 HC-V0913-* sub-asserts (PREFS-A1..E1 = 15; TOOLS-A1..E1 = 15; DVJS-A1..E1 = 15; VERIFY-A1..E1 = 10). Final smoke **1815 passed / 0 failed** (delta +55 from v0.91.2's 1760); cowork-smoke 931 / claude-surface 207 unchanged. Preflight `version-sync ok: 0.91.3` ALL GREEN. **Bundled data-layer fixes on accuris vault** (synced to laptop): (a) vault-config.md frontmatter closing `---` fence restored — pre-v0.90.3 discover-people corruption legacy where reconstruction missed `fmMatch[3]`; (b) mcp_map corrected — M365/github/ADO connected, gmail/gcal not_applicable (accuris is M365, not Google); (c) user-preferences.md `mcps.ado.served_by` updated from legacy alias `"accuris-ado"` to actual UUID `1151913a-530a-4a36-a5f6-3bedcab4e4da`. **Deliberate NON-trim** of orchestrator prose this cycle. v0.92.0 MINOR refactor candidate queued: move imperative prose (CRITICAL callouts, verbal commitments, write-guard reminders) to sub-skills, slim orchestrators to ~50-line dispatcher. Today's weekly-review success WITHOUT reading the orchestrator is the load-bearing evidence — orchestrator prose burden has hit LLM-attention ceiling. **Cycle stack 2-day stretch:** 10 cycles shipped (v0.89.0 → v0.89.1 → v0.90.0 → v0.90.1 → v0.90.2 → v0.90.3 → v0.91.0 → v0.91.1 → v0.91.2 → v0.91.3); +499 HC sub-asserts (1316 → 1815). FLN-v90-4 / FLN-v91-1 / FLN-v89-7 / v89-8 / v89-10 / v891-1 / v891-2 / v90-1 / v90-3 / WS-C v0.30.0+ stay queued.

- **Workshop version (previous):** `0.91.2` (closed 2026-06-05) — v0.91.2 frontmatter-write-guard PATCH — defense-in-depth layer 2 atop v0.91.1's path write-guard. Accuris morning-briefing on 2026-06-05 wrote correctly to the canonical path post-v0.91.1, but emitted wrong frontmatter `type: cowork-run-note` instead of canonical `type: cowork-morning-briefing`. The LLM hallucinated a generic stub type when the orchestrator's frontmatter spec wasn't salient. 5 write-run-note sub-skills (`write-run-note-{morning-briefing,midday-tripwire,eod-review,weekly-review,monthly-review}`) gain a frontmatter `type:` validation clause in their `## Pre-write self-check` section: REJECTS any computed frontmatter whose `type:` field is not the canonical per-orchestrator value (`cowork-morning-briefing`, `cowork-midday-tripwire`, `cowork-eod-review`, `cowork-weekly-review`, `cowork-monthly-review`); returns `failed:contract-violation:wrong-frontmatter-type`. Companion guard to v0.91.1's path-write-guard — same architecture, different field. cowork@0.29.1 → 0.29.2 PATCH; workshop 0.91.1 → 0.91.2 PATCH; mechanism count unchanged at 17. ~40 HC-V0912-* sub-asserts (TYPE-CHECK-A1..E1 = 25; COMMIT-A1..E1 OR-substring widening = 15). Final smoke **1760 passed / 0 failed** (delta +40 from v0.91.1's 1720). Preflight `version-sync ok: 0.91.2` ALL GREEN. Brief cycle-close artifacts deferred during the live-bug recovery window; full narrative folded into v0.91.3 cycle-close at 2026-06-05 22:50 UTC.

- **Workshop version (previous):** `0.91.1` (closed 2026-06-05) — v0.91.1 orchestrator-write-guard + claude_surface-disambiguation + verbal-commitment PATCH — three structural defenses against the recurring wrong-path bug that surfaced AGAIN on accuris-laptop post-v0.91.0 deploy: the LLM wrote the morning-briefing atomic note to `spice/daily/Friday-2026-06-05.md` instead of canonical `spice/cowork/daily/2026/06-June/2026-06-05/morning-briefing.md`. Headspace works on the same machine — accuris specifically fails because its consumer CLAUDE.md has LEGACY accuris content (`Daily notes are gateways` non-negotiable + `Naming: daily YYYY-MM-DD-DayName.md` + `Daily | spice/daily | /daily` resolver row) that the LLM defers to over v0.90.2's `[!warning]+ CRITICAL` orchestrator callout. Prose-only fixes have an LLM-attention ceiling; v0.91.1 adds deterministic backstops. **Fix A:** write-guard `## Pre-write self-check` clause in 5 `cowork:write-run-note-<orchestrator>` sub-skills (morning-briefing/midday-tripwire/eod-review/weekly-review/monthly-review) — REJECTS any computed path starting with `spice/daily/` OR matching `<weekday>-<YYYY-MM-DD>.md` filename pattern OR missing the canonical `spice/cowork/daily/` (or `weekly/`/`monthly/`) prefix; returns `failed:contract-violation:wrong-output-path`. Deterministic backstop: even if LLM passes wrong path to the sub-skill, sub-skill refuses. **Fix B:** NEW `claude_md_row` in cowork manifest emitting `| Cowork Atomic Notes (cron output) | spice/cowork/daily/ | (cron-only — NOT spice/daily/) |` row in consumer CLAUDE.md resolvers table. After v0.91.1 install + `sauce update --bump-pins`, the resolvers table shows BOTH `spice/daily` AND `spice/cowork/daily/` side-by-side with explicit topic disambiguation + "NOT spice/daily/" in the command column. **Fix C:** verbal-commitment Notice at orchestrator pre-flight Step 1b in 5 orchestrators: emits Obsidian Notice `"cowork:<orchestrator> committing to canonical write path: spice/cowork/daily/.../<orchestrator>.md (NOT spice/daily/...)"` BEFORE any other action. Two purposes: (1) commit path to LLM working memory at run-start so it stays salient through long gather/compose steps, (2) create audit trail in Obsidian Notice log if wrong-path write happens despite commitment. cowork@0.29.0 → 0.29.1 PATCH; workshop 0.91.0 → 0.91.1 PATCH; mechanism count unchanged at 17. ~33 HC-V0911-* sub-asserts (WRITE-GUARD-A1..E1 = 15; DISAMBIG-A1 = 3; COMMIT-A1..E1 = 15). 2 stage commits S0..S1 + deploy. Final smoke **1720 passed / 0 failed** (delta +33 from v0.91.0's 1687); cowork-smoke 931 / claude-surface 207 unchanged. Preflight `version-sync ok: 0.91.1` ALL GREEN. **Three-layer defense in depth:** (1) [!warning]+ CRITICAL callout at top of orchestrator (v0.90.2), (2) verbal-commitment Notice at orchestrator Step 1b (v0.91.1 Fix C), (3) deterministic write-guard in write-run-note sub-skill (v0.91.1 Fix A) — even when 1+2 fail, the sub-skill refuses bad paths. Plus claude_surface disambiguation (Fix B) reduces the CLAUDE.md ambiguity that caused the failure. **User Layer 1 fix** (explicit-path scheduled-job prompt wrappers) is also provided in the v0.91.1 design doc §8 — bakes the canonical path into the cron-job prompt itself so it's loaded with every invocation and stays top-of-context. User applies this on the laptop for accuris. **Tomorrow's accuris morning briefing should write to canonical path** even if the LLM-attention bug recurs — between the user-side prompt wrapper + the deterministic write-guard, the wrong path is rejected at write time. **Cycle stack 2-day stretch:** 8 cycles shipped (v0.89.0 → v0.89.1 → v0.90.0 → v0.90.1 → v0.90.2 → v0.90.3 → v0.91.0 → v0.91.1); +404 HC sub-asserts (1316 → 1720). FLN-v90-4 / FLN-v91-1 / FLN-v89-7 / v89-8 / v89-10 / v891-1 / v891-2 / v90-1 / v90-3 / WS-C v0.30.0+ stay queued.

- **Workshop version (previous):** `0.91.0` (closed 2026-06-05) — v0.91.0 cowork:find-missing-people MINOR — NEW orchestrator complementing v0.28.0 `cowork:discover-people`. **Gap closed:** atomic notes (morning-briefing / eod-review / etc.) emit `[[Person]]` wikilinks from memory ticks + gather output. When `spice/people/<Name>.md` doesn't exist, Obsidian renders the wikilink dim ("greyed out"). The headspace 2026-06-05 morning briefing had `[[Ellen Senders]]` dimmed because Ellen wasn't in the chat microscope's "Inner circle" section (she's in "Open loops" / elevation sections). discover-people populates from microscopes; find-missing-people populates from atomic-note emission. **Skill:** `cowork:find-missing-people` reachable via `/cowork find-missing-people [<engagement_id>] [days_back=30]`. User-invoked only. NEW pure helper `find-missing-people-helper.js` exports 7 functions: `scanAtomicNoteForWikilinks` (extracts `[[Name]]`, rejects embeds + anchors), `filterToPersonShapedWikilinks` (rejects path-shaped, `.md` extensions, abbreviations, cards, products — reuses v0.90.3's KNOWN_NON_PERSON_NAMES via v0.91.0 export from discover-people-helper), `aggregateWikilinksAcrossNotes` (dedup + surface_count + first_seen + last_seen + surfaced_in[]), `filterToMissingPeople` (diff against existing spice/people/ basenames), `composePersonNoteStubFromSurfaces` (v0.6.0 typed-alias schema + `discovered_from` frontmatter + source-listing callout), `composeReviewTable` (HIGH/MEDIUM/LOW tiers by surface count with row numbers for batch-confirm), `composeReport`. 12-step orchestrator flow: pre-flight + engagement resolution + date-context window (default 30 days) → discover (enumerate `spice/cowork/daily/<YYYY>/<MM-Month>/<YYYY-MM-DD>/*.md` in window; aggregate wikilinks; person-shape filter) → enumerate existing person notes → filter to missing → review (HIGH/MEDIUM/LOW table) → batch confirm (y/n/skip <rows>) → apply (composePersonNoteStubFromSurfaces + obsidian_put_content with pre-existence check) → optional inner-circle promotion (reuses v0.90.3-fixed composeUpdatedVaultConfig from discover-people-helper; `.bak` backup per landmine #12) → report. `[!warning]+ CRITICAL: output path (v0.91.0)` callout at top per v0.90.2 convention — names canonical `spice/people/<basename>.md` write path; forbids cowork-cadence-atomic-note writes from this orchestrator. cowork@0.28.2 → 0.29.0 MINOR (new orchestrator + helper + 2 manifest files[] + 2 claude_surface entries); workshop 0.90.3 → 0.91.0 MINOR; mechanism count unchanged at 17. ~40 HC-V0910-* sub-asserts (A1 = 5; B1 = 7; C1 = 5; D1 = 3; E1 = 5; F1 = 5; G1 = 4; SKILL-A1 = 6). 4 stage commits S0..S3 + deploy. Final smoke **1687 passed / 0 failed** (delta +40 from v0.90.3's 1647); cowork-smoke 931 / claude-surface 206 → 207 (+1 new orchestrator entry); install 15 unchanged. Preflight `version-sync ok: 0.91.0` ALL GREEN. **After v0.91.0 deploys + user runs `/cowork find-missing-people headspace`**, the dim `[[Ellen Senders]]` wikilink in today's headspace morning briefing (and every other dim wikilink from the last 30 days of atomic notes) resolves to a real (stub) person note. Optional Step 11 also promotes discovered names to `engagement.inner_circle_people`, closing the loop for future cadence runs. **Cycle stack 2-day stretch:** 7 cycles shipped (v0.89.0 → v0.89.1 → v0.90.0 → v0.90.1 → v0.90.2 → v0.90.3 → v0.91.0); +336 HC sub-asserts (1316 → 1687). **NEW FLN-v91-1 queued:** the cowork.md slash-command body has grown with each new orchestrator (discover-people v0.28.0, find-missing-people v0.29.0). A meta-helper or template that auto-emits sub-command sections from `claude_surface[]` entries would reduce drift risk going forward. **FLN-v90-4 (closing-fence helper-hardening sweep) + WS-C v0.29.0+ MCP-discovery + FLN-v89-7 / v89-8 / v89-10 / v891-1 / v891-2 / v90-1 / v90-3 stay queued.**

- **Workshop version (previous):** `0.90.3` (closed 2026-06-05) — v0.90.3 discover-people-helper-bug-fixes + prose-extraction-fallback PATCH — three coordinated fixes to `cowork:discover-people` surfaced by live `/cowork discover-people headspace` run (pre-v0.90.2). **Fix B1 (composeUpdatedVaultConfig)** — TWO bugs in the helper: (1) reconstruction missed `fmMatch[3]` (the closing `\n---\n` fence) — function returned `before + fm + after` instead of `before + fm + closing + after`, consuming the closing fence into body content; LLM caught it via `.bak` + manual edit. (2) inline `inner_circle_people: []` (empty-array shorthand) wasn't detected — only multi-line block header matched; fall-through inserted a new block without removing the inline line, producing duplicate-key YAML. Fix: preserve `closing` const + add inline-empty branch that splice-replaces in-place. **Fix B2 (parseInnerCircleFromMicroscope)** — widened regex coverage with Pass B (bold-name extraction in inner-circle-adjacent H3/H4 subsections anywhere in body — catches headspace's prose-narrative format with `### Inner circle first` / `### Going quiet` / `### Cadence sweep` sections containing `**Diana**`/`**Mo Holton**`/etc.) + KNOWN_NON_PERSON_NAMES filter rejecting ~40 common false positives (cards Cap1 Platinum/SCHEELS, abbreviations EMS/AWS/ADO, products GitHub/iMCP/WhatsApp, time terms Friday/Tonight, status labels Reply/Owed). User batch-confirm gate remains final filter. **Fix B3 (discover-people SKILL.md)** — NEW Step 4b: LLM-side prose-extraction fallback when helper returns <3 candidates AND microscope body >500 chars. Scans inner-circle-adjacent H3/H4 sections for proper-noun person names; rejects abbreviations/cards/products; appends to candidates with `source_type: "microscope-prose-llm"`. Two-layer defense: deterministic helper catches structured patterns + LLM fallback covers remaining prose-narrative coverage. cowork@0.28.1 → 0.28.2 PATCH (helper + SKILL.md changes); workshop 0.90.2 → 0.90.3 PATCH; mechanism count unchanged at 17. ~35 HC-V0903-* sub-asserts (B1-A1 = 5; B1-A2 = 3; B2-A1 = 7; B2-A2 = 5; B3-A1 = 4; B2-REGRESSION = 11 confirming v0.28.0 accuris fixture still extracts all 11 inner-circle names post-widening). 3 stage commits S0..S2 + deploy. Final smoke **1647 passed / 0 failed** (delta +35 from v0.90.2's 1612); cowork-smoke 931 / claude-surface 206 unchanged. Preflight `version-sync ok: 0.90.3` ALL GREEN. **After v0.90.3 deploys + headspace gets new helper**, re-running `/cowork discover-people headspace` should catch Ellen Senders + Mo Holton + Evie + Andres Cornejo + Galen Gossman + David Panlilio + Mrs. Panlilio + Tyler Igoe + Grace Fellhoelter — names the v0.28.1 helper missed today. **NEW FLN-v90-4 surfaced**: `composeUpdatedVaultConfig` closing-fence bug existed since v0.28.0; HC fixture-driven tests didn't catch it because all fixtures had body content AFTER the frontmatter that masked the missing `\n---\n`. Add a "closing fence preserved" assertion to every helper that touches frontmatter going forward; queued as a Tier 2 helper-hardening sweep. **WS-C v0.29.0+ MCP-discovery extension** still queued; **N1 cowork:find-missing-people skill** (next cycle per user direction) addresses unresolved-wikilink-in-atomic-note pattern (`[[Ellen Senders]]` greyed-out cases).

- **Workshop version (previous):** `0.90.2` (closed 2026-06-05) — v0.90.2 orchestrator-path-callout + prompt-body-sentinel PATCH — closes two live-runtime failures observed at v0.90.1 deploy. **Failure 1 (output-path):** scheduled morning-briefing on accuris (willfell-laptop machine) wrote the atomic note to `spice/daily/2026/06-June/Friday-2026-06-05.md` (daily-blueprint path) instead of `spice/cowork/daily/2026/06-June/2026-06-05/morning-briefing.md` (cowork atomic-note path). The LLM read consumer CLAUDE.md row `| Daily | spice/daily | /daily |` and conflated the daily-blueprint surface with the cowork atomic-note surface; never reached Step 16's write-sub-skill invocation. **Failure 2 (empty-prompt false-positive):** scheduled morning-briefing on accuris (this machine) wrote a stub note because the LLM treated the v0.4.0 installer-default user-prompt blockquote *"Vault-editable prompt for cowork:morning-briefing. Edit the body to customize..."* as "non-empty content" and short-circuited to Step 10's empty-prompt stub branch without trying engagement-template fallback. Headspace's identical prompt content was handled correctly by a different LLM session — LLM-behavior variance on ambiguous content. Both failures share a root cause: LLM attention truncates / mis-interprets in long SKILL.md files. Three-fix bundle: **Fix A** — `[!warning]+ CRITICAL: output path (v0.90.2)` callout block at TOP of 5 orchestrator SKILL.md files (morning-briefing, midday-tripwire, eod-review, weekly-review, monthly-review). Positioned immediately after the H1 heading, before the intro paragraph — first thing the LLM reads after frontmatter. Names canonical `spice/cowork/daily/<YYYY>/<MM-Month>/<YYYY-MM-DD>/<orchestrator>.md` (or `spice/cowork/weekly/...` / `spice/cowork/monthly/...` for weekly/monthly); forbids `spice/daily/` path; names the write sub-skill; forbids direct writes via Write/Edit/`mcp__obsidian__obsidian_put_content`. Explicitly cites the CLAUDE.md `spice/daily/` row as a known trap. **Fix B (FLN-v90-2)** — v0.4.0 installer-default sentinel detection in Step 10 of 5 orchestrators (detects 1-blockquote shape + first line `> Vault-editable prompt for ` + body contains `Empty body is a no-op stub for now`; treats as empty so engagement-template fallback fires). Plus morning-briefing + midday-tripwire gain the full fallback chain that eod/weekly/monthly already had (user-prompt → sentinel-check → template-prompt → empty), unifying behavior across all 5. **Fix B.2** — 5 source prompt files emptied at `platform/blueprints/cowork/content/prompts/` (body removed; just frontmatter remains); `materialize_once: true` added to the 5 prompt `files[]` entries in cowork manifest. Future fresh installs get truly-empty prompts (Step 10's empty-check fires cleanly) AND installer never overwrites user customizations on existing vaults. Existing vaults with v0.4.0 prompts stay untouched; Fix B.1's runtime sentinel detection handles them. cowork@0.28.0 → 0.28.1 PATCH; workshop 0.90.1 → 0.90.2 PATCH; mechanism count unchanged at 17. ~25 HC-V0902-* sub-asserts (PATH-A1..E1 = 10; SENTINEL-A1..E1 = 5; MATERIALIZE-A1 = 5; SOURCE-A1 = 5). Bumped pin tests: HC-V0900-VERSION-A/B/C/D + HC-V0901-CATALOGUE-A1 + HC-V0891-VERSION-A/C + V0750-VERSION cowork-smoke pin. 4 stage commits S0..S4 + deploy. Final smoke **1612 passed / 0 failed** (delta +25 from v0.90.1's 1587); cowork-smoke 931 / claude-surface 206 also ALL GREEN. Preflight `version-sync ok: 0.90.2` ALL GREEN. **FLN-v90-2 CLOSED.** Verification owed at tomorrow's (2026-06-06) scheduled crons on both machines: morning briefings should now (a) write to canonical `spice/cowork/daily/<YYYY>/<MM-Month>/<YYYY-MM-DD>/morning-briefing.md` (Fix A) AND (b) use engagement-template fallback when user-prompt is the v0.4.0 sentinel (Fix B). New FLN candidate from this cycle: **FLN-v90-3** — write-guard in write-run-note sub-skills that aborts if invoked from outside the expected orchestrator call path (defense against future prose-imperative fails). Queued.

- **Workshop version (previous):** `0.90.1` (closed 2026-06-04) — v0.90.1 catalogue-pin-sync + read-user-preferences-prose-update PATCH — hot-fix following v0.90.0 deploy. Closes two structural gaps that broke v0.90.0 cycle's runtime intent: (1) `platform/manifest.json` blueprints[] catalogue still pinned cowork at 0.27.1 — `--bump-pins` reads the catalogue to decide whether to update files[] in consumer vaults; with the stale pin, the new discover-people-helper.js never materialized to accuris. Bumped to 0.28.0. (2) `read-user-preferences` SKILL.md prose described the OLD v0.89.0 wikilink rule body and didn't specify the v0.90.0 composition order (rule at index 0, before user hard_rules). At runtime the LLM follows SKILL.md prose (not the JS helper); without the prose update, the v0.90.0 unshift reorder had no runtime effect. SKILL.md rewritten with full v0.90.0 body (PRECEDENCE OVERRIDE prefix + microscope override language) + explicit composition order description (index 0 = wikilink, 1..N = user, N+1 = no_emoji); Step 7 compose-effective_hard_rules instruction updated to match. Workshop 0.90.0 → 0.90.1 PATCH (no cowork bump — SKILL.md materialization happens via claude_surface, not version-gated). 6 HC sub-asserts (HC-V0901-CATALOGUE-A1 + HC-V0901-SKILL-A1 × 4 sub-asserts) + HC-V0900-VERSION-A/B/C/D + HC-V0891-VERSION-C pin bumps. Smoke 1581 → 1587 / 0. **FLN-v90-1 candidate surfaced:** platform/manifest.json blueprints[] catalogue needs auto-sync with each blueprint's own manifest.json version; currently manually kept in sync, and v0.90.0 cycle missed the catalogue bump.

- **Workshop version (previous):** `0.90.0` (closed 2026-06-04) — v0.90.0 wikilink-imperative + eod-imperative-gather + discover-people-extraction MINOR — three-workstream bundle closing FLN-v89-9 + FLN-v891-3 + WS2 v0.28.0 microscope-extraction first slice. Live verification on accuris 2026-06-04 revealed v0.89.1's wiring was present but wikilinks weren't landing (chat microscope's explicit `## Output shape` per-item-line spec dominated the soft prompt; EOD ran in legacy mode and skipped chat gather entirely). **WS-A** promotes the v0.89.0 canonical `wikilink_people` hard rule (already present in `read-user-preferences-helper.js`) from end-of-array to TOP of `effective_hard_rules[]` (via `out.unshift`) so it appears as the FIRST bullet in the dispatch contract's `## Hard rules` block; strengthens `WIKILINK_PEOPLE_RULE.body` with explicit `PRECEDENCE OVERRIDE` prefix asserting override of microscope `## Output shape` format specs ("Even when a microscope's `## Output shape` instructs a specific per-item format, the canonical name MUST be wrapped in `[[Basename]]` wikilink syntax"); `gather-from-served-by` SKILL.md `## Hard rules` section gains a v0.90.0 Precedence note distinguishing STRUCTURAL layout (microscope's domain) from IDENTIFIER format (hard rule's domain). **WS-B** ships MANDATORY prose at the top of `## Gather` sections in all 5 atomic-note orchestrator SKILL.md files (morning-briefing / midday-tripwire / eod-review / weekly-review / monthly-review): "execute the priority loop for EVERY entry in `dispatch_plan`; memory ticks are SUPPLEMENTARY context, not a REPLACEMENT for live MCP gather output; failing to fire the priority loop means the dispatch contract's 'Known people in scope' wikilink instruction never reaches the LLM." Strengthens the existing dispatch-mode fallback Notice with explicit `PREFS UNAVAILABLE` prefix + consequence callout ("Chat and any custom kinds will NOT fire in legacy mode; inner-circle wikilink emission will NOT occur"). **WS-C** ships NEW `cowork:discover-people` orchestrator at `platform/blueprints/cowork/skills/orchestrators/discover-people/SKILL.md` reachable via `/cowork discover-people [<engagement_id>]` + NEW pure helper `discover-people-helper.js` exporting 11 functions (parseInnerCircleFromMicroscope / parsePromotionRowsFromSibling with suppress_list extraction / parseStakeholdersFromVaultConfig / parseCurrentInnerCircle / aggregateCandidates / classifyCandidate / composePersonNoteStub / composeUpdatedSibling / composeUpdatedVaultConfig / composeReviewTable / composeReport). 11-step orchestrator flow: pre-flight + engagement resolution (default-if-one auto-pick, ask-if-many prompt) → discover (microscopes + per-mcp/<kind>/people-aliases.md sibling tables + vault-config structured fields stakeholders/manager/direct_reports) → classify (per-candidate `cowork:resolve-person` + classifyCandidate → CREATE_AND_PROMOTE / PROMOTE_EXISTING / ALREADY_PROMOTED / SUPPRESS) → review (composeReviewTable single batch-confirm question: y / n / skip <list>) → apply (person-note stubs via composePersonNoteStub + obsidian_put_content with pre-existence check + vault-config inner_circle_people append via composeUpdatedVaultConfig idempotent dedup + per-kind people-aliases.md Promote? confirm → promoted flip via composeUpdatedSibling; `.bak` backups per landmine #12 on vault-config + sibling writes) → report. USER-AUTHORED microscope.md files are READ-ONLY (never modified). User-invoked only; never cron-scheduled. cowork@0.27.1 → 0.28.0 MINOR (new orchestrator + helper); workshop 0.89.1 → 0.90.0 MINOR; mechanism count unchanged at 17. Helper-cases ~99 new HC-V0900-* sub-asserts (WIKILINK-A1..A4+B1 = 16; IMPERATIVE-A1..E1 + FALLBACK-A1..E1 = 20; DISCOVER-HELPER-A1..I1 = 48; DISCOVER-SKILL-A1..A5 = 5; VERSION-A..D + MANIFEST-A1 = 10). Also bumps stale pin tests: HC-V0890-VERSION-A floor (smoke-pattern), HC-V0891-VERSION-A/C exact pins, V0750-VERSION cowork pin, HC-V0790-A3 ordering (now [wikilink, user, no_emoji]). 7 stage commits S0..S5 + S7 + S0.5 plan/design refinement (8 total including S0 design). Final smoke **1581 passed / 0 failed** (delta +99 from v0.89.1's 1482); cowork-smoke 931 / claude-surface 206 also ALL GREEN; install 15 / version-sync ok: 0.90.0. **FLN-v89-9 CLOSED** (wikilink hard rule promoted to top of effective_hard_rules + explicit PRECEDENCE OVERRIDE language defeats microscope `## Output shape` precedence). **FLN-v891-3 closed pending live verification** at tomorrow's EOD run (prose-imperative fix targets the LLM-shortcut hypothesis; if root cause is read-user-preferences returning non-ok or something else, v0.90.1 PATCH needed). **WS-C v0.28.0 microscope-extraction first slice DELIVERED**: tomorrow user can run `/cowork discover-people accuris` to operationalize the 11 inner-circle names from chat microscope + 7 frequent collaborators from people-aliases sibling + Hayden Remington from vault-config stakeholders into the engagement's `inner_circle_people` allowlist. End-to-end M365 morning-briefing verification on accuris-sauce DEFERRED to post-deploy at tomorrow morning's scheduled cron. Future: v0.29.0+ MCP-discovery extension reuses aggregateCandidates / classifyCandidate / write composers as the same write-side foundation. FLN-v89-7 / FLN-v89-8 / FLN-v89-10 / FLN-v891-1 / FLN-v891-2 stay queued.

- **Workshop version (previous):** `0.89.1` (closed 2026-06-04) — v0.89.1 served-by-resolver-and-daily-title-fix PATCH — Workstream 1 of the post-v0.89.0 brainstorm handoff bundled with a daily title typed-alias guard. Closes the load-bearing v0.89.0 gap where M365 / microscope-routed vaults (accuris) did not get gather-time `[[Person]]` wikilink emission across `gather-from-served-by`-routed kinds (chat / email / calendar / ado / github / finance) — v0.89.0's `wikilink_people` voice rule was soft and required in-dispatch instruction to fire reliably. NEW pure JS helper `platform/blueprints/cowork/helpers/resolve-inner-circle-helper.js` exports `composeInnerCircleAllowlist(resolverOutputs) → { resolved, unresolved, phone_filter_list }` — pure shape composer; no MCP, no I/O; idempotent on null/empty; drops malformed entries silently; phone_filter_list deduplicates with first-occurrence ordering. Hybrid pre-resolve + soft prompt architecture — orchestrator drives the per-name MCP loop, helper unifies the shape into a deterministic allowlist. `gather-from-served-by` envelope gains new inputs (`inner_circle_resolved`, `engagement_id`) + new output (`inner_circle_resolved_count`); NEW Step 3 "Known people in scope" dispatch contract section injects the allowlist verbatim plus a soft prompt for ambient resolver calls (emit `**[[<basename>]]**` on hit / plaintext on miss). Microscope `## Output shape` ALWAYS-WIN precedence preserved (FLN-v89-9 stays queued — intentional PATCH-scope decision). 5 atomic-note orchestrators wired — morning-briefing refactored to consume the new helper (replacing v0.89.0 inline phone-list union); midday-tripwire / eod-review / weekly-review / monthly-review each gain a NEW pre-resolution step before the kind-priority gather loop; each passes `allowlist.resolved` + `engagement_id` to every `gather-from-served-by` call; morning-briefing additionally passes `allowlist.phone_filter_list.join(",")` to `gather-imessage`'s existing E.164 `inner_circle` input. Bundled daily fix: SpaceDailyDashboard `_resolveTitle` typed-alias guard at `platform/blueprints/daily/helpers/space-daily-dashboard.js` — pre-fix `aliases[0]` branch unconditionally stringified the first alias via `String(a0).trim()`, producing `"[object Object]"` row titles for newly-created person notes with people@0.6.0 typed aliases; fix introduces a `_safe(v)` helper at function top that returns `""` when the value would stringify to `"[object Object]"`, lets each branch short-circuit and fall through to the next; aliases[0] branch detects typed-alias shape and uses `a0.value`; bare-string back-compat preserved; defensive fallthrough catches Dataview Link wrappers across `p.name` / `p.title` / `p.file.outline` paths. cowork 0.27.0 → 0.27.1 (PATCH — pure-additive helper + SKILL.md prose; no `claude_surface[]` change), daily 0.13.4 → 0.13.5 (PATCH — typed-alias guard), mechanism count unchanged at 17. ~14 HC-V0891-* cases / ~30+ sub-asserts (HELPER-A..C output shape / empty handling / phone dedup; GATHER-A..C SKILL.md prose / helper validation / empty-allowlist ambient path; ORCHESTRATOR-A..E 5 orchestrators substring-presence; DAILY-A..B typed-alias `.value` extraction + behavior probe; VERSION-A..D). 5 stage commits S0..S4 + 2 mid-cycle fixups (S2.1 orchestrator require-path + write-run-note redirect; S3.1 cowork manifest dest `{{helpers_dir}}` → `{{module_directory}}/helpers/` correction). Final smoke **1482 passed / 0 failed** (delta +72 from v0.89.0's 1410); cowork-smoke 931 / claude-surface 205 also ALL GREEN. Preflight `version-sync ok: 0.89.1` ALL GREEN. **FLN-v89-6 CLOSED.** FLN-v89-7 / FLN-v89-8 / FLN-v89-9 / FLN-v89-10 stay queued. NEW FLN-v891-1 (manifest dest substitution-token validator — surfaced from S3.1 typo), NEW FLN-v891-2 (write-run-note Step 2a prose cleanup post-S2.1 redirect). End-to-end M365 morning-briefing verification on accuris-sauce DEFERRED to post-deploy (claude.ai desktop M365 connector required). **v0.90.0+ next: distill-week + auto-update prefs (third memory-arc shift); Workstream 2 connector-agnostic people-discovery skill queued v0.90.x+.**

- **Workshop version (previous):** `0.89.0` (closed 2026-06-03) — v0.89.0 people-cohesion-2 MINOR — slices B+C+D of the people-cohesion arc bundled (slice C narrowed to integration-without-auto-stub). NEW mechanism `people-identity@0.1.0` (PeopleIdentity class: resolvePerson / findByAlias / getAliases / listAliasesOfType; sorted-folder collision policy with console.warn; bare-string aliases normalized to `{type: "name", value}` at read time; NEVER throws). NEW sub-skill `cowork:resolve-person` at `skills/skills/resolve-person/SKILL.md` (FLN-v79-4 nested dest) wraps spice/people/ frontmatter scan over MCP. 3 gather skills (imessage / gmail / calendar) wired to call resolver + emit `[[Person]]` wikilinks on hit / plaintext on miss (bold-wrap preserved in both branches). NEW canonical `wikilink_people` hard rule injected into every engagement's `effective_hard_rules[]` by `read-user-preferences-helper.js` — platform-default, always-on; binds atomic-note bodies + synthesis bodies + callout titles + dispatch-contract output. `write-run-note-morning-briefing` Step 2 translates `inner_circle_people: string[]` names → E.164 phones via `aliases_by_type.phone` before passing to `gather-imessage`; unresolved names emit Notice + frontmatter warning. Two-tier wikilink convention: per-gather-skill resolve+emit + central voice-block hard rule (catches LLM free-form composition outside gather output). Forward-only emission; no historical backfill of memory.md / synthesis.md. Third memory-arc shift: distill-week → v0.90.0; retro/insights + slice E (brain-map rollups) coalesce at v0.91.0. people 0.5.2 → 0.6.0 (typed-alias schema convention in rule_fragment.aliases_schema; depends_on +people-identity ≥0.1.0). cowork 0.26.0 → 0.27.0 MINOR. Mechanism count 16 → 17. ~14 HC-V0890-* cases / ~40 sub-asserts (PEOPLE-IDENTITY-A..D, RESOLVE-PERSON-A..D, GATHER-IMESSAGE/GMAIL/CALENDAR-A, VOICE-A, MORNING-BRIEFING-A, VERSION-A..D); 3 golden-fixture JSON pairs (case-a basename hit, case-b alias collision, case-c miss); 3 fixture vault person notes (Stefan de Pagter, Steve A, Steve B). 6 stage commits S0..S5. Final smoke **1410 passed / 0 failed** (delta +94 from v0.88.2's 1316). Preflight `version-sync ok: 0.89.0` ALL GREEN. NEW FLN-v89-1..5 carry-forwards: auto-stub-on-miss (handle-only contacts), collision-as-audit-error promotion, performance index for >500-person vaults, inner_circle_people migration to wikilinks, personality.hard_rules disable-path for wikilink_people. **v0.90.0 next: distill-week + auto-update prefs (third memory-arc shift).**

- **Workshop version (previous):** `0.88.2` (closed 2026-06-03) — v0.88.2 orphan-folder-template-cleanup PATCH — closes the underlying double-fire bug that v0.88.0 was originally supposed to fix. v0.88.0 removed the `templater_folder_templates[]` block from people manifest; HC tests asserted the manifest no longer contained it; but the installer was **additive-only** on the corresponding entry in `.obsidian/plugins/templater-obsidian/data.json`. Result: every consumer vault that had `people@0.4.0` installed previously still had `{ folder: "spice/people", template: "ranch/templates/Template, People.md" }` in its Templater config after v0.88.0/v0.88.1 — so `+ New Person` still triggered the double-fire on file landing, producing broken notes with `PeopleRendering.renderMentionList: unknown mode undefined` toasts (the user-facing bug v0.88.0 was supposed to fix). Structural gap closed via NEW `removed_templater_folder_templates: [<folder>, ...]` manifest field + NEW `applyTemplaterFolderTemplateRemovals(tp, manifest, variables, history, git)` install helper. Helper called inside `installItem` immediately BEFORE `applyTemplaterFolderTemplates` so removals + re-adds compose correctly. Idempotent (no-op after first removal); backup-on-edit; failure-loud. People manifest declares `["spice/people"]`. people 0.5.1 → 0.5.2. 2 new HC-V0882 cases / 7 assertTrue calls. Smoke 1309 → 1316. Preflight `version-sync ok: 0.88.2` ALL GREEN. FLN-v88-7 CLOSED. NEW FLN-v88-8 surfaced (same additive-only pattern likely affects templater_startup_templates / templater_hotkeys / slash_commander_bindings / nav_buttons / claude_surface — full audit candidate). **v0.89.0 next: slice B of people-cohesion arc** (unchanged from v0.88.1 sequencing).

- **Workshop version (previous):** `0.88.1` (closed 2026-06-03) — v0.88.1 accent-button-null-guard PATCH — hot-fix following v0.88.0 deploy. Closes a latent rendering bug where `customJS.AccentButton.render(parent, {label, icon: null, ...})` produced the literal string `"null"` before the label in the rendered button DOM. `PersonNavButtons` is the only caller passing `icon: null`; pre-v0.88.0 the person-note dataviewjs block never executed (the templater double-fire bug corrupted body content before render), so this latent AccentButton bug was hidden for ~70 cycles. v0.88.0's clean creation path unblocked the render → exposed the bug. Two coordinated fixes: (a) accent-button 0.1.0 → 0.1.1 — `(opts.icon || "")` null-guard in the `innerHTML` concat; (b) people 0.5.0 → 0.5.1 — PersonNavButtons now passes a real Lucide `arrow-left` SVG icon and drops the redundant `←` arrow-char from the label. 2 new HC-V0881 cases / 6 assertTrue calls. Existing HC-V0880-PEOPLE-D widened from exact `=== "0.5.0"` to floor `^0\.5\.\d+$`. Final smoke 1303 → 1309 passed / 0 failed. Preflight `version-sync ok: 0.88.1` ALL GREEN. **v0.89.0 next: slice B of people-cohesion arc** (entity-create people-multi prompt type + meeting button suggester + foundations for identity resolution) — second displacement of memory arc (distill-week → v0.90.0+, retro/insights → v0.91.0+, slices C/D/E → v0.92.0+).

- **Workshop version (previous):** `0.88.0` (closed 2026-06-03) — v0.88.0 people-cohesion-1 MINOR — slice A of a five-slice people-cohesion arc. Closes the half-baked state of the people blueprint as it interacts with meetings: template error on `+ New Person` creation (entity-create vs Templater double-fire), dead `people:` frontmatter field, over-strict naming pattern, no autocomplete in Meeting attendees prompt. people 0.4.0 → 0.5.0 (MINOR — removed `templater_folder_templates[]` binding for `spice/people/`, relaxed `naming_pattern` to accept mononyms / diacritics / multi-part names / "Dr. Strange" / straight apostrophes; curly apostrophe variant intentionally dropped post-S1.1); `Template, People.md` drops the `tp.file.move` directive. meetings 0.6.0 → 0.7.0 (MINOR — `Meeting.md` Templater template's opening `<%* ... -%>` block replaced with suggester loop over existing `spice/people/*.md` plus `+ Add new person…` and `Done — finish meeting` sentinels; `people:` frontmatter mirrors `attendees:` at creation; `SUGGESTER_LOOP_V0880` regression sentinel embedded). Sequencing override: displaces planned v0.88.0 distill-week per `Docs/plans/2026-06-02-memory-leverage-sequencing-decision.md` (now bears a Superseded callout). distill-week shifts to v0.89.0, retro/insights to v0.90.0. Two audit-vault fixtures: `Madonna.md` (mononym), `Élise Martin.md` (diacritic). `run-audit.js` AU6/AU7 pattern literals updated in lockstep with the manifest. `run-helper-cases.js` FA2-MEETINGS-1 pinned to 0.7.0 (label + console.log header); FA2-PEOPLE-1 regex widened to `^0\.(3|4|5)\.\d+$`. 8 NEW HC-V0880 cases / ~29 `assertTrue` calls (PEOPLE-A/B/C/D + MEETINGS-A/B/C/D). HC-V0880-DOCS-A from spec §5.1 moved to S3.1 manual grep (supersession doc edit lands in S3, after smoke runs). 4 stage commits S1 (a1a003e) + S1 polish (11a36ca catalogue pins) + S1 polish 2 (dd65f48 subscription pin) + S2 (36bb19b workshop_version) + S3 cycle-close. Preflight `version-sync ok: 0.88.0` ALL GREEN. Final helper-cases smoke **1303 passed / 0 failed** (delta +29 from v0.87.0's 1274 baseline; note prior cycle-status snapshot at v0.87.0 cited 929 reflecting a different counter — recount adopted post-v0.87.0). **v0.89.0 next: distill-week + auto-update prefs (shifted from v0.88.0).** Slices B-E (identity-resolution mechanism, cowork auto-stub people, wikilink-in-output convention, brain-map fields + rollups) follow in subsequent cycles per design §9 forward references. Manual cleanup of pre-existing broken person notes deferred to user (see `Docs/upgrades/v0.88.0-people-notes-cleanup.md`).

- **Workshop version (previous):** `0.87.0` (closed 2026-06-02) — v0.87.0 semantic-memory-retrieval MINOR — closes FLN-v84-4 (carry-forward from v0.84.0+). NEW sub-skill `cowork:gather-semantic-memory` at `skills/skills/gather-semantic-memory/SKILL.md` (FLN-v79-4 nested dest) wraps `sc-bridge semantic-search` behind a structured-output API ({ engagement_id, anchor_text, top_k, exclude_window, min_similarity } → { found, matches[{path, similarity_score, day_or_week, tier, synthesis_excerpt}], anchor_text_used, exclusion_count, error }). Tier 2 (synthesis.md) prioritized over Tier 1 (memory.md); Tier 0 (per-tick) excluded. Graceful failure-mode taxonomy (empty_anchor / sc_bridge_unavailable / index_unavailable / timeout / empty_matches) — never throws. NEW pure helper `composeSemanticEchoesCallout(gatherOutput) -> string` renders `[!quote]+ Echoes from your record` callout byte-identical to golden fixture (HC-V0870-B2); null-data clean-omit on every failure mode. MB step 3b (NEW; after v0.85.0 step 3a) composes anchor_text from dispatch_plan + calendar + email (≤500 chars), invokes the sub-skill with top_k=2 + exclude_window=last-30d + min_similarity=0.45, then body composition appends Echoes after Overnight, before the v0.85.0 §0.3 Memory-log backlink. Existing MB steps 3b/3c renumbered to 3c/3d; 3 cross-references inside MB SKILL.md updated in lockstep. cowork 0.25.0 → 0.26.0 (MINOR — new install surface: sub-skill + helper files[] entry + MB SKILL.md modification). ~10 HC-V0870-A1..D1 sub-asserts (~28 inner assertions) + V0750-VERSION cowork-pin bumped 0.25.0 → 0.26.0 at S1 + 1 new fixture (input JSON + expected callout MD pair). 4 stage commits S0..S3. Final smoke **929 passed / 0 failed** (delta +27 from v0.86.0's 902). Preflight `version-sync ok: 0.87.0` ALL GREEN. **FLN-v84-4 CLOSED.** NEW FLN-v87-1 surfaced (empirical corpus-density validation owed at deploy — does min_similarity=0.45 filter noise on real accuris/headspace corpus?), FLN-v87-2 (`/cowork recall <query>` USER-INVOCATION slash command deferred — could ship as v0.87.1), FLN-v87-3 (Tier 2 prioritization requires synthesize-week to have fired at least once; first week post-deploy Tier 1 dominates).

- **Workshop version (previous):** `0.86.0` (closed 2026-06-02) — v0.86.0 cross-orchestrator-wire-through MINOR — closes FLN-v84-2 (deferred at v0.84.0). 4 atomic-note orchestrators (midday-tripwire / eod-review / weekly-review / monthly-review) gain pre-flight step 3a invoking `cowork:read-memory` with appropriate tier+window + body-composition step piping structured output through 4 NEW per-orchestrator pure helpers (composeMidamMemoryCallout / composeEodMemoryCallout dual-output / composeWeeklyMemoryCallout / composeMonthlyMemoryCallout). Each helper byte-identical to a golden fixture; null-data gating preserves backward-compat. Memory-read frequency amplifies 5× per engagement per day. cowork 0.24.1 → 0.25.0 (MINOR — new install surface). ~16 HC-V0860-A1..D4 sub-asserts + 10 new fixtures + V0750-VERSION pin bumped. 4 stage commits S0..S3. Final smoke 902/0. **FLN-v84-2 CLOSED.** FLN-v86-1/2/3 surfaced.

- **Workshop version (previous):** `0.85.1` (closed 2026-06-02) — v0.85.1 deploy-mechanics-and-cron-fix PATCH — closes FLN-v85-1 (v0.85.0 §0.1 hardening's libexec-only heuristic was insufficient for the active-pantry deploy layout; generalized `_resolveWorkshopPath` to walk for any ancestor with a valid sauce manifest via new `_isValidWorkshopRoot` helper — covers brew Cellar + active-pantry + in-vault pantry uniformly). `installed.workshop_path` now auto-populated in `handleBumpPins` on first successful resolve so future runs short-circuit without re-walk. Synthesize-week onboard default cron `0 17 * * 5 → 30 17 * * 5` (Friday 17:30, after synthesize-day's 17:20 — captures Friday data in the week roll-up). NEW `_warnIfActivePantryDrift` helper in `cmd-update.js` fires a stderr Notice with `git pull --ff-only origin main` suggestion when active-pantry checkout is N-behind `origin/main`. 4 pre-existing fixtures patched at S1 collateral (caseV0760B2/B3/B4 + 6 `_bpRun` callers + HC-V0850-G1 `--workshop-path` pin polish). cowork 0.24.0 → 0.24.1 (PATCH; SKILL.md prose). ~6 HC-V0851 sub-asserts. 6 stage commits S0..S5. Final smoke 868 passed / 0 failed. **FLN-v85-1 CLOSED.** NEW FLN-v85-4 surfaced. See `Docs/cycle-history.md`.

- **Workshop version (previous):** `0.85.0` (closed 2026-06-02) — v0.85.0 tier-2-and-read-memory + polish-prelude MINOR. Tier 2 of cowork continuous-memory architecture + the load-bearing structured-output read-memory primitive. cowork 0.23.0 → 0.24.0; engagement-types schema 0.4.0 → 0.5.0 (additive `synthesize_week`). NEW sub-skill `cowork:read-memory` + NEW orchestrator `cowork:synthesize-week` + 2 NEW pure helpers + NEW canonical type `cowork-weekly-synthesis` + rule_fragment. Morning-briefing step 3a byte-identical refactor. 4 polish-prelude items folded (§0.1 FLN-v83-2 `handleBumpPins` defensive hardening — superseded by v0.85.1 generalization; §0.2 visible `## Memory` section on Cowork.md hub; §0.3 atomic-note `[!quote]- Memory log` backlink footer; §0.4 headspace eod-review monochrome-callout post-deploy validation noted). ~24 new HC-V0850-* sub-asserts. 12 logical stages → 16 commits + S12 cycle-close. Final smoke 864 passed / 0 failed. **FLN-v83-2 CLOSED.** FLN-v85-1/2/3 surfaced. Mid-cycle cohabitation with v0.84.2/3/4 daily polish PATCHes. See `Docs/cycle-history.md`.

- **Workshop version (previous):** `0.84.4` (closed 2026-06-02) — v0.84.4 polish-pills PATCH. Three CSS polish tweaks on v0.84.3's right-aligned pills (§1 adjacent-sibling rule suppresses chevron auto-margin when counts precedes it; §2 inter-pill gap 6px → 10px; §3 count-pill border `--background-modifier-border` → `-hover`). Two lockstep bumps (workshop / daily 0.13.4). 3 new HC-V0844-A* sub-asserts. Single cycle commit. Preflight ALL GREEN. See `Docs/cycle-history.md`.

- **Workshop version (previous):** `0.84.3` (closed 2026-06-02) — v0.84.3 header-pills PATCH. See `Docs/cycle-history.md` for the v0.84.3 narrative.

- **Workshop version (previous):** `0.84.2` (closed 2026-06-02) — v0.84.2 carryforward PATCH. See `Docs/cycle-history.md` for the v0.84.2 narrative.

- **Workshop version (previous):** `0.84.1` (closed 2026-06-02) — v0.84.1 cleanup-1 PATCH. See `Docs/cycle-history.md` for the v0.84.1 narrative.

- **Workshop version (previous):** `0.84.0` (closed 2026-06-02) — v0.84.0 cowork-memory-layer MINOR. See `Docs/cycle-history.md` for the v0.84.0 narrative.

- **Workshop version (previous):** `0.83.0` (closed 2026-06-01) — v0.83.0 cowork-engagement-type-materialization MINOR. See `Docs/cycle-history.md` for the v0.83.0 narrative.

- **Workshop version (previous, retained for reference):** v0.83.0 cowork-engagement-type-materialization MINOR — closes FLN-v82-1 (engagement-type accessibility). 3 engagement-type JSONs (`w2-fte.json`, `personal.json`, `consulting.json`) added to cowork's `files[]`; materialize to `spice/cowork/context/engagement-types/` on consumer vaults. All 7 cowork orchestrators updated to read from the canonical materialized path. `eod-review`, `weekly-review`, `monthly-review` gain explicit engagement-not-found guard (matching morning-briefing + midday-tripwire baseline). STOCK row added to `cowork-customization-contract.md`. Workshop 0.82.1 → 0.83.0; cowork 0.21.1 → 0.22.0 (MINOR — new install surface). ~10 cases / ~14 HC-V0830-* sub-asserts (A/B/C/D groups); HC-V0750-C1/C3 updated (S4.1 follow-up). 9 stage commits + S7 cycle-close. Preflight `version-sync ok: 0.83.0` ALL GREEN. 3 new lessons surfaced; FLN-v83-1 surfaced (orchestrator prose-consistency lint candidate).

- **Workshop version (previous):** `0.82.1` (closed 2026-06-01) — v0.82.1 fln-cleanup-bundle-2 PATCH. See `Docs/cycle-history.md` for the v0.82.1 narrative.

- **Workshop version (previous, retained for reference):** v0.82.1 fln-cleanup-bundle-2 PATCH — 4 FLN cleanups (FLN-v82-2 resetSourceContributions + per-item-loop wiring + cowork PATCH bump 0.21.0→0.21.1 to clear landmine-#16 version-skip guard; FLN-v82-3 parseYamlIsh hyphen-key support; FLN-v81-1 semver-helper.js extraction + 3 floor-assertion sites refactored; FLN-v79-5 CS-MIG-1 counts derived from manifest.claude_surface[] groupBy — 4 hardcoded magic numbers removed). `ranch/rules/cowork.json` natural shrink 7929→635 lines / 281→22 entries at S8 dogfood. Workshop 0.82.0 → 0.82.1; cowork 0.21.0 → 0.21.1 (PATCH, mechanism-internal, mid-cycle scope expansion). ~10 HC-V0821-* sub-asserts (A1.1a, A1.1b, A1.2, A1.3, B1.1, B1.2, C1.1..C1.4). 9 stage commits + S8.1 + S9 cycle-close. Preflight `version-sync ok: 0.82.1` ALL GREEN. 3 new FLNs surfaced: FLN-v821-1 (V0750-VERSION exact-version assertions), FLN-v821-2 (installItem version-skip + new in-loop mechanism), FLN-v821-3 (S0 verification pattern for cleanup FLNs).

- **Workshop version (previous):** `0.82.0` (closed 2026-06-01) — v0.82.0 callout-types-tables-and-tripwire MINOR. See `Docs/cycle-history.md` for the v0.82.0 narrative.

- **Workshop version (previous, retained for reference):** v0.82.0 callout-types-tables-and-tripwire MINOR — two coherent UX-polish workstreams on top of v0.79+v0.80+v0.81's structural foundation. **WS-A** per-kind callout types: `read-user-preferences-helper.js` resolves `mcps.<kind>.callout_type` per kind (explicit OR default mapping OR `example`); `gather-from-served-by-helper.js` accepts `callout_type` input + parameterizes `[!<callout_type>]+ <kind_title>` prefix + echoes `callout_type_used`; 5 atomic-note orchestrators pass `callout_type: prefs.mcps[entry.kind_name].callout_type` in the gather loop. Default mapping: chat→info, finance→warning, calendar→tip, email→quote, ado→example, github→note. Result: morning briefings now render 5-6 kind sections in 5-6 visually-distinct colors via existing v0.79.0 per-type CSS. **WS-B** gather contract softening: Step 3 "Bulleted lines preferred (no tables)" prohibition replaced with table-or-bullet decision rule; microscope `## Output shape` directives explicitly noted as overriding. **WS-C dropped** at S0 — plan premise was wrong (w2-fte.json fix was already done in v0.13.0; engagement-type JSONs aren't materialized to consumer vaults). Real fix folded into FLN-v82-1. cowork@0.20.1 → 0.21.0 MINOR. ~17 HC-V0820-* sub-asserts added (A1..A4, B1..B3, C1..C3, D1). 9 stage commits + S10 cycle-close. Preflight `version-sync ok: 0.82.0` ALL GREEN.

- **Workshop version (previous):** `0.81.1` (closed 2026-05-30) — v0.81.1 cowork-slash-doc-fix PATCH. See `Docs/cycle-history.md` for the v0.81.1 narrative.

- **Workshop version (previous, retained for reference):** v0.81.1 cowork-slash-doc-fix PATCH — single doc fix: `platform/blueprints/cowork/commands/cowork.md` (the `/cowork` slash command body) gains a `## /cowork audit-siblings [<kind>] (v0.81.0)` section paralleling the existing v0.79.0 microscope section. Closes a v0.81.0 carry-forward gap surfaced during pre-test vetting: the audit-siblings skill materialized correctly in consumer vaults, but the slash command body didn't document how to invoke it. Without the doc entry, Will running `/cowork audit-siblings` (per tomorrow's headspace test plan Step 7) would rely on Claude Code's skill auto-discovery, which is risky for test confidence. The fix adds an explicit invocation section with em-dash UX hardening callout. cowork@0.20.0 → 0.20.1 PATCH (content change triggers re-materialization of cowork.md slash body in consumer vaults). HC-V0811-A1 added to run-cowork-smoke.js (prose-lint asserting slash body mentions audit-siblings sub-command + names the skill). 4 stage commits + S4 cycle-close. Preflight `version-sync ok: 0.81.1` ALL GREEN.

- **Workshop version (previous):** `0.81.0` (closed 2026-05-30) — v0.81.0 cowork-audit-siblings MINOR. See `Docs/cycle-history.md` for the v0.81.0 narrative.

- **Workshop version (previous, retained for reference):** v0.81.0 cowork-audit-siblings MINOR — new pure-read-only `cowork:audit-siblings` orchestrator skill closes the silent-degradation gap v0.80.0 left open. Two-axis check: dangling references (`microscope.md` names a sibling that's absent → `[!warning]`) + orphan files (sibling present but unreferenced → `[!info]`). New helper exports `parseReferences` + `auditSiblings` (pure, deterministic, sorted by (kind, name); em-dash separator required). New SKILL.md at orchestrator-tier dest `{{skills_dir}}/audit-siblings/SKILL.md` with em-dash UX hardening note. cowork@0.19.0 → 0.20.0 MINOR. CS-MIG-1 counts 37→38 / 46→47 (lockstep dance closed). +26 HC-V0810-* sub-asserts in run-cowork-smoke.js + 1 in run-claude-surface.js. 7 stage commits + S8 cycle-close. **FLN-v81-1 surfaced + partially mitigated:** semver-regex assertions across 3 test files widened from 0.8.x-0.19.x to 0.8.x-0.29.x (ceiling now at 0.30.x). Preflight `version-sync ok: 0.81.0` ALL GREEN. Cycle executed subagent-driven-development style.

- **Workshop version (previous):** `0.80.1` (closed 2026-05-30) — v0.80.1 fln-cleanup-bundle PATCH. See `Docs/cycle-history.md` for the v0.80.1 narrative.

- **Workshop version (previous, retained for reference):** v0.80.1 fln-cleanup-bundle PATCH — closes 5 documented footguns from prior cycles. FLN-v79-2: `install.js` CLI handler so `node install.js --vault . --auto-approve` actually works (was silently no-op'ing); +2 in-stage hardening fixes (--vault no-value, --auto-approve/--decline-all mutual exclusion). FLN-v80-1: softened HC-V0800-A1 per-mcp glob regex to accept split phrasing. FLN-v79-3: 80-char `agent_markdown` floor documented in build-test-verify.md `## Writing HC cases`. FLN-v79-4: orchestrator-vs-sub-skill dest convention in architecture.md. FLN-v80-2: landmine #16 entry updated to note `materializeClaudeSurface` SKILL.md catch-up posture. No contract surface change; pure cleanup. 6 stage commits + S6 cycle-close. Cycle executed subagent-driven-development style. Preflight `version-sync ok: 0.80.1` ALL GREEN.

- **Workshop version (previous):** `0.80.0` (closed 2026-05-30) — v0.80.0 cowork-sibling-files MINOR. See `Docs/cycle-history.md` for the v0.80.0 narrative.

- **Workshop version (previous, retained for reference):** v0.80.0 cowork-sibling-files MINOR — closes the third resolution path in `cowork:edit-microscope`'s gap-finding triad (the `user-supplied` placeholder from v0.79.0). New convention: any markdown file in `spice/cowork/prompts/per-mcp/<kind>/` except `microscope.md` and `_*.md` is a USER-owned context surface; orchestrators discover via new step 2c (glob + filter); `gather-from-served-by` injects each verbatim under `**User-supplied reference: <name>**` in dispatch contract Step 3 (after `what_matters`, before `**Hard rules ...**`), echoes back `siblings_used[]`. `cowork:edit-microscope` step 4 user-supplied branch pre-checks existence + scaffolds via new `composeSibling` helper (gap-text-keyed column heuristic: phone/email/account/vip/generic) + writes `## References` into `microscope.md` via extended `composeMicroscope`. cowork@0.18.0 → 0.19.0 MINOR. +43 HC-V0800-* sub-asserts (A1..A3, B1..B3, C1..C6, D1..D3, E1) in run-cowork-smoke.js + 3 in run-install.js (F1). No CS-MIG-1 count change. 12 stage commits + S12 cycle-close. Preflight `version-sync ok: 0.80.0` ALL GREEN.

- **Workshop version (previous):** `0.79.0` (closed 2026-05-29) — v0.79.0 cowork-microscope MINOR. See `Docs/cycle-history.md` for the v0.79.0 narrative.

- **Workshop version (previous, retained for reference):** v0.79.0 cowork-microscope MINOR — closes four gaps surfaced by the headspace 2026-05-29 morning-briefing run (emojis leaking into the body, monochrome callouts, shallow finance contract despite explicit notes, privacy-limited chat MCP). Ships a complete vertical slice: **(WS-A) Microscope** — per-kind USER-owned deep gather contracts at `spice/cowork/prompts/per-mcp/<kind>/microscope.md` authored by the new MCP-tool-aware iterative capture skill `cowork:edit-microscope` (`/cowork microscope <kind>`); when present, the 5 atomic-note orchestrators route the kind through `gather-from-served-by` with the microscope body as the deep `what_matters` and the prior `notes` as `baseline_notes`. Contract file is NOT in cowork's `files[]` (bootstrap-vault-style seeding; never overwritten by `update`/`reinstall`). **(WS-B) Hard rules** — `personality.hard_rules[]` + `personality.no_emojis` parse in `read-user-preferences` → composed `effective_hard_rules[]` propagated to all three output layers: voice-contract Hard-rules block, `gather-from-served-by` dispatch `## Hard rules` section binding callout TITLE + BODY (this is the load-bearing fix for emoji leak in served-by-path kinds), write-run-note skeleton binding paragraph on all 5 sub-skills. Canonical `[!warning]` exempt. **(WS-C) Admonition fix** — styling 0.1.2 → 0.2.0 ships `sauce-callouts.css` defining per-type `--callout-color` for info/note/tip/success/warning/caution/example/quote/danger across light (rose-pine-light) + dark (melange-dark); wired via `applySnippets` (manifest `snippets[]` + `appearance.enabledCssSnippets`). Workshop dogfood materializes `.obsidian/snippets/sauce-callouts.css`. **(WS-D doc-only)** — iMessage `search_contacts` closes number→name gap in-gather; WhatsApp recommend `lharries/whatsapp-mcp` swap for message-level depth (surfaced live by `edit-microscope`'s gap classification). New helper `edit-microscope-helper.js` (pure `resolveKind`/`classifyGap`/`composeMicroscope`). Modified helpers: `read-user-preferences-helper.js`, `dispatch-plan-helper.js`, `gather-from-served-by-helper.js`. 5 orchestrators + 5 write-run-notes carry identical edits (per-cadence range untouched). cowork@0.17.0 → 0.18.0 MINOR. +28 HC-V0790-* sub-asserts in run-cowork-smoke.js + 3 in run-install.js (F1 reinstall preservation, mirrors caseV0760E1 scaffold) + FOCUSED_USER_PATHS extended with the per-mcp glob. CS-MIG-1 cowork counts 36→37 / 45→46. **VISUAL verification of WS-C still owed by user** (Obsidian rendering check — remote-control session had no UI access; snippet + appearance registration verified on disk). 16 stage commits + S17 cycle-close. Final smoke 608 passed / 0 failed; preflight `version-sync ok: 0.79.0` ALL GREEN.

## Cycle order (chronological)

v0.1.0 → v0.1.1 → v0.1.x → v0.1.3 → v0.1.2 → v0.2.0 → v0.3.0 → v0.4.0 → v0.3.2 → v0.4.2 → v0.5.0 → v0.11.0 → v0.12.0 → v0.13.0 → v0.14.0 → v0.6.0 → v0.16.0 → v0.17.0 → v0.18.0/.1/.2 → v0.19.0 → v0.20.0 → v0.21.0/.1 → v0.22.0/.1 → v0.23.0 → v0.24.0 → v0.25.0 → v0.26.0/.1 → v0.27.0 → v0.28.0 → v0.29.0 → v0.30.0 ⏭️ → v0.31.0 → v0.32.0 → v0.33.0/.1 → v0.36.0/.1 → v0.37.0 → v0.38.0/.1 → v0.40.0 → v0.41.0/.5 → v0.42.0 → v0.43.0 → v0.44.0 → v0.45.0 → v0.46.0/.1/.2 → v0.47.0 → v0.48.0 → v0.49.0 → v0.49.1 ⏭️ → v0.49.2 → (v0.50.0–v0.62.0 narratives lost; pre-v0.63 narrative below resumes) → v0.63.0 → v0.63.1 → v0.63.2 → v0.63.3 → v0.64.0 → v0.64.1 → v0.64.2 → v0.64.3 → v0.65.0 → v0.66.0 → v0.66.1 → v0.66.2 → v0.67.0 → v0.67.1 → v0.67.2 → v0.67.3 → v0.70.0 → v0.70.1 → v0.70.2 → v0.70.3 → v0.70.4 → v0.70.5 → v0.70.6 → v0.70.7 → v0.71.0 → v0.71.1 → v0.72.0 → v0.72.1 → v0.73.0 → v0.74.0 → v0.75.0 → v0.75.1 → v0.76.0 → v0.77.0 → v0.78.0 → v0.78.1 → v0.79.0 → v0.80.0 → v0.80.1 → v0.81.0 → v0.81.1 → v0.82.0 → v0.82.1 → v0.83.0 → v0.84.0 → v0.84.1 → v0.84.2 → v0.84.3 → v0.84.4 → v0.85.0 → v0.85.1 → v0.86.0 → v0.87.0 → v0.88.0 → v0.88.1 → v0.88.2 → v0.89.0 → v0.89.1 → v0.90.0 → v0.90.1 → v0.90.2 → v0.90.3 → v0.91.0 → v0.91.1 → v0.91.2 → v0.91.3 → v0.92.0 → v0.93.0 → v0.93.1 → v0.93.2 → v0.93.3 → v0.94.0 → v0.95.0 → v0.95.1 → v0.96.0 → v0.96.1 → v0.96.2 → v0.97.0 → v0.97.1 → v0.97.2 → v0.97.3 → v0.97.4 → v0.98.0 → v0.98.1 → v0.98.2 → v0.99.0 → v0.100.0 → v0.100.1 → v0.101.0 → v0.101.1 → v0.102.0 (current).

> Gap note: per `Docs/cycle-history.md` line count (57 closed-cycle sections ending at v0.47.0, plus a v0.48–v0.67.3 archive), the v0.50.0 → v0.62.0 narratives were not captured in cycle-history.md during their respective closes. The CLAUDE.md claim that they were "archived to Docs/cycle-history.md" was stale. Backfill from `Docs/plans/` is possible but deferred.

## In-flight / next-candidate queue

Live brainstorm list (also referenced in `Docs/plans/` and brainstorm shelf files):

- ~~**v0.98.2 — reconciler free-text + per-item-tick ingest (NEXT MINOR).**~~ **SHIPPED 2026-06-11.** Closed the v0.98.1 capture loop AND the user's 2026-06-10 vision in full. NEW `ingest-feedback-helper.js` deterministic core (rollup math + entity weight apply + clamp + decay + schema 2 → 3 normalizer + intent validation + audit-entry compose). NEW `ingest-feedback/SKILL.md` shim (blueprint source first per v0.98.1 lesson #4). `reconcile-cowork.md` extended at Step 3 (BOTH `rating-block` AND `feedback-capture` v=1/v=2 sentinels — RESTORES EOD signal regression) + NEW Step 3.5 (deterministic rollup) + NEW Step 3.6 (INLINE LLM intent extraction; no separate API call, no model pin) + extended Steps 4-5 (learned_weights schema 2 → 3 in-place additive in `user-preferences.md` frontmatter; NESTED per-kind entity maps) + NEW Step 5.5 (microscope APPEND-only / voice PROPOSED / coverage queue / audit log with source quotes) + NEW Step 5.6 (voice-proposal approval sweep — tick-to-approve from yesterday's morning brief). Rail L v=2 adds per-kind Didn't-like list sharing same `^item-<kind>-<sha>` IDs as Mattered list; sidecar 1.2.0 adds `items[]` identity registry. Morning-briefing voice-proposals callout renders only when proposals pending (collapsed, compact). Consumption thresholds (≥1.20 lead / ≤0.50 cap / ≤0.25 omit) added to morning-briefing + eod-review OIs. cowork 0.37.0 → 0.38.0; workshop 0.98.1 → 0.98.2; contract_version UNCHANGED at 0.35.1; eod sidecar schema enum + `"1.2.0"`; learned_weights schema 2 → 3. 23 new HC sub-asserts (18 HC-V0982 + 5 EODITEMS fixture). 6 cycle discoveries (see result doc). See `Docs/cycle-history.md` § v0.98.2 + `Docs/plans/2026-06-11-v0.98.2-feedback-loop-closure-{design,plan,result}.md` + `Docs/prompts/2026-06-11-post-v0.98.2-next-cycle-handoff.md`.
- **v0.98.2.x PATCH candidates.** (a) `cowork:doctor` (sidecar conformance scan; surfaced concretely by Discovery #5 — live headspace 2026-06-10 eod-review sidecar carries `coverage_gap: []` array vs schema's object shape; write-guard bypassed at fire time; observability would catch). (b) Per-item ticks on weekly / monthly cadences — v0.98.2 Rail L v=2 is EOD-only; additive extension if post-deploy data shows demand. (c) Strict-mode adoption for helpers (per Discovery #2 — S1.3 tick-block rewrite orphaned `let m;` declaration → implicit global, caught by S1.3.1 hotfix; `"use strict"` adoption workshop-wide is small PATCH-sized hygiene).
- ~~**v0.99.0+ (shipped 2026-06-12).**~~ v0.100.0 project visual polish MINOR also SHIPPED 2026-06-12 (same day — small cycle).
- ~~**v0.101.0 — verbal feedback everywhere (MINOR).**~~ **SHIPPED 2026-06-13.** Capture v=4 on all five cadences (tap + typing box on top everywhere; EOD per-item lists; 4 non-EOD cadences ONE collapsed kind checklist; rating-block EMISSION retired, parser forever; either-marker write-guard) + per-cadence satisfaction `{day, cadence, useful}` via learned_weights schema 4 → 5 (PURELY ADDITIVE — no reset) + generalized prose ingest (`source_cadence` tagging; dedup per (day, intent, kind, entity); prose-over-tap; NEW `satisfaction` intent; downrank-kind-no-entity = kind skip; `applyIntentKindObservations` rename). cowork 0.39.0 → 0.40.0; contract 0.35.1 UNCHANGED (zero new tokens); eod sidecar enum + `"1.4.0"`/+ `"v=4"`; 4 non-EOD schemas additive slim `feedback_capture`. helper-cases **2455 / 0** (+85; 24 new HC-V1010 cases). Displaces the doctor by one cycle (its KPI is 5× richer with five cadences feeding it). See `Docs/cycle-history.md` § v0.101.0 + `Docs/plans/2026-06-13-v0.101.0-verbal-feedback-everywhere-result.md` + `Docs/prompts/2026-06-13-post-v0.101.0-next-cycle-handoff.md`.
- ~~**v0.102.0 — project workspace (MINOR).**~~ **SHIPPED 2026-06-13.** Sections (filesystem subfolders + per-project override; defaults Knowledge/Notes) + Confluence-style Docs Hub (NEW `ProjectDocsSections` helper; per-bucket `+ New <Section>` button via NEW EntityCreate `presetPrompts` capability) + Project ↔ Meetings link (NEW `ProjectMeetingsPanel` helper + project pill on hub rows + singular `project: "[[ProjectName]]"` field) + adjacent install fix (`applyVaultDefaultPaths` configures Obsidian "Files & Links" defaults to `spice/resources/{notes,attachments}/`) + `applyProjectSectionsMigration` Strategy A auto-migration. workshop 0.101.1 → 0.102.0; project 1.15.2 → 1.16.0; meetings 0.7.0 → 0.8.0; entity-create 0.4.0 → 0.5.0; platform-claude 0.1.2 → 0.1.3; cowork 0.40.0 UNCHANGED; contract 0.35.1 UNCHANGED; mechanism count 17 UNCHANGED. helper-cases 2457 → **2573 / 0** (+116). 5 lessons (see result doc) — workshop catalogue/per-mechanism drift codified as landmine #24. See `Docs/cycle-history.md` § v0.102.0 + `Docs/plans/2026-06-13-v0.102.0-project-workspace-result.md` + `Docs/prompts/2026-06-13-post-v0.102.0-next-cycle-handoff.md`.
- **v0.102.1 (NEXT, queued — search fast-follow).** Pure UX layer on top of finalized v0.102.0 section structure; ships within 1-2 days of v0.102.0 dogfood validation. Three components, all layered into `ProjectDocsSections`: (1) filter input above section buckets — text input passes `filterContext.text`; each section's rows filter in-memory by `file.name` + `frontmatter.tags` + first 200 chars of `file.content`; (2) tag chips between filter input and section buckets — dynamic from `getMostUsedTags(allDocs, top: 8)`; click toggles active; multi-select AND; (3) scoped-search button — icon button opens Obsidian global search with `path:"spice/projects/<slug>/docs"` pre-filled. Status pill ("Filtering: `ingress + #spec` — 3 of 31 docs") with clear-all button. No schema change. See `Docs/plans/2026-06-13-v0.102.0-project-workspace-design.md` § Section 13.
- **cowork:doctor (QUEUED v0.103.0 — displaced TWO cycles by v0.101.0 + v0.102.0; was slotted "v0.99.1").** Embedded in the nightly reconciler; 4 checks: (1) wrapper/sidecar version-drift vs installed; (2) sidecar schema conformance over a rolling window; (3) align-audit staleness (`scheduled-jobs.md` last_synced vs installed); (4) self-grading digest (satisfaction trend — **PER-CADENCE since v0.101.0** — + top entity-weight movers). Anomalies -> one collapsed callout in next morning brief; details -> reconciler log. No new scheduled job; no contract bump. Brainstorm: 3 open questions (detail-log location; digest cadence; version-drift stamp source) + the per-cadence digest-shape question. See `Docs/prompts/2026-06-13-post-v0.102.0-next-cycle-handoff.md`.
- ~~**v0.98.1.x PATCH candidates.**~~ (a) Bare-fence visual UX — DEFERRED / not yet surfaced. (b) Per-item DOWNVOTE row — CLOSED in v0.98.2 (shipped as v=2 Rail L Didn't-like list). (c) Integration fixture for composeBody cadence dispatch — CLOSED in v0.98.2 S1 (5 EODITEMS fixture sub-asserts).
- **v0.97.0+ cowork-rethought-2 candidate (still queued post-v0.96.1):** natural next codename. Carry-forwards from v0.96.0 design § 1.3 + § 11.5, minus what v0.96.1 closed: W2 (full structured-output API rewrite for compose-body, deeper than v0.96.0's sidecar Rail W), memory salience + decay + supersession (Mem0/Zep/Graphiti/Letta-style retention policy that supersedes FLN-v84-3), per-item L2R + Thompson sampling + PU correction (extension of Rail L's per-kind weights to per-item), cron heartbeat WRITE surface (v0.96.1 Rail H ships a READ-only check via sidecars; full write surface still queued), per-skill SemVer + versioning CI (independent SemVer per cowork sub-skill with CI gate), the v0.95.1-migrator carry-forward (`sauce update --migrate-config` from pre-v0.95.0 vault-config.md still parked).
- **hub-nav@0.1.0 mechanism** (extraction candidate)
- **claude_surface[] wave 3** (boards / people / to-do / finance / journal / trips adopt `claude_surface[]` + retire `Docs/Meta/<X>-System.md`; cowork already retired; cowork backwards-compat shim removal)
- **v0.44.1 deploy-hardening** (deferred)
- **audit YAML parser swap**
- **Remaining-blueprint seed coverage**
- **v0.47.0 / v0.48.0 FLN cleanup cycles**
- **FLN-v66-1 cleanup** — 7 legacy-shape project hubs in accuris/ero use `type: structure`/`project-board` → rollup silently drops them; migration helper or rollup type-predicate widening
- **FLN-v66-5 audit assert** — every `new_entity_buttons[].prompts[].key` should appear in `frontmatter_template` as `{{prompts.<key>}}` (catches v0.66.2-style wiring gaps platform-wide)
- **FLN-v64-6** — scratch body-first-line title fallback for legacy untitled scratches
- **v0.79.0 candidate: `kind_title` field on `mcps.<kind>` blocks** — v0.78.0 uses the bare kind key (e.g., `chat`, `ado`) as the rendered Example title; add an optional `kind_title:` for prettier display strings (e.g., `Microsoft Teams chat`). Pure cosmetic.
- ~~**v0.79.0 candidate: personality hard rules**~~ — CLOSED in v0.79.0 (WS-B). Also reaches the gather dispatch + write-run-note skeleton (not voice-contract-only) since served-by-path callouts were the dominant emoji-leak surface.
- **v0.79.0 candidate: expanded known-kinds catalog** — v0.77.0's catalog is intentionally Minimum-4 (calendar / email / chat / finance). Strong v0.79.0 candidates: drive (Google Drive / Dropbox / S3) / code-platform (GitHub / GitLab) / project-tracker (Linear / Jira / ADO) / monitoring (NewRelic / Datadog / PagerDuty). Custom-kind path covers everything else for now.
- **v0.79.0 candidate: promote frequently-used custom kinds to known** — once post-v0.78.0 deployment usage data exists, promote the most-common user-defined custom kinds into the known catalog so they get hand-curated question sets instead of free-text `what_matters`.
- **v0.79.0 candidate: cross-vault preferences inheritance (`inherits_from:`)** — allow a vault's user-preferences.md to declare it inherits from another vault's (`inherits_from: ../other-vault/spice/cowork/context/user-preferences.md`); useful for users with multiple vaults sharing personality + cross-cutting priorities. Resolution-order spec + cycle-detection guard required.
- **v0.76.0 carry-forward: promote 5 cowork prompts to strict-USER (`materialize_once`) OR formalize the `.bak` behavior** — currently the 5 prompts are USER-DRAFTABLE-WITH-BACKUP (in `files[]` without `materialize_once`; `.bak`'d-then-overwritten via v0.2.0 Option B). Pick one direction and codify it. Still open.
- **v0.79.0 candidate: 5-orchestrator engagement-template fallback parity** (v0.76.0 carry-forward) — v0.76.0's S6 covered eod/weekly/monthly. Morning-briefing + midday-tripwire still rely on implicit `onboard-scheduled-jobs` install-time seeding; add explicit fire-time fallback.
- **v0.79.0 candidate: sc-bridge fallback anchor when calendar empty** (from v0.75.0/v0.75.1) — bridge stays cold when morning-briefing fires with no calendar events; fire sc-bridge with vault-root anchor to keep model warm.
- **v0.79.0 candidate: `--subscribe <mech>=<ver>` CLI flag** (from v0.75.0/v0.75.1) — new-in-workshop mechanism subscription still requires manual jq edit.
- **v0.79.0 candidate: HC-V0751-A1 registry-driven** (from v0.75.1) — hardcodes 3 engagement-type names; should discover dynamically from `platform/blueprints/cowork/engagement-types/*.json`.
- **v0.79.0 candidate: factor shared test utilities** (from v0.75.1) — `assertEqual`/`assertTrue`/UNIT_TEST_MODE duplicated in run-install.js + run-cli.js; extract to `platform/test/harness-utils.js`.
- **v0.79.0 candidate: check-claude-surface harness** (from v0.75.1) — assert dest content matches what source would produce (prevents install.md source-vs-dest drift).
- **v0.81+ candidate: microscope vertical-slice carry-forwards (from v0.79.0 design §11)** — ~~`contacts-map.md` sibling-file format + gather consumption~~ CLOSED in v0.80.0; WhatsApp MCP-swap follow-through once user adopts `lharries/whatsapp-mcp`; `samples/` sibling dir (cached example outputs per kind); per-kind microscope versioning/changelog; hard-rule post-processor (mechanical emoji-strip / word-budget pass); expanded known-kinds catalog (drive / code-platform / project-tracker / monitoring); cross-vault `inherits_from` for microscope contracts; canonical-gather × microscope hybrid path. Full list in `Docs/plans/2026-05-29-v0.79.0-handoff.md`.
- **v0.81+ candidate: sibling-file vertical-slice carry-forwards (from v0.80.0 design §11)** — interactive editing of EXISTING sibling content (v0.80.0 scaffolds only); dedicated `/cowork sibling <kind> <name>` slash command (decoupled from edit-microscope); typed sibling parsers + deterministic pre-agent substitution (vs. v0.80's opaque-injection model); hard-rule-style post-processor for sibling-application compliance (overlaps with v0.79.0 §5); sibling-file versioning/changelog; multiple-microscope-per-kind (cadence-specific); soft-limit warning for very large sibling files (>50KB); sibling-file-aware reverse audit (verify `## References` entries point to extant files). Full list in `Docs/plans/2026-05-30-v0.80.0-handoff.md`.
- ~~**FLN-v80-1: HC-V0800-A1 glob-prose regex over-strict.**~~ — CLOSED in v0.80.1. Regex softened in `run-cowork-smoke.js` to accept either the literal `per-mcp/<kind_name>/*.md` substring OR a split `per-mcp/<kind_name>/` + "matching `*.md`" phrasing (`hasLiteralGlob || hasSplitPhrasing` boolean OR; 7-line change; sub-assert count unchanged at 1).
- ~~**FLN-v80-2: landmine #16 less load-bearing than v0.79.0 close documented.**~~ — CLOSED in v0.80.1. `Docs/landmines.md` entry #16 narrative extended with the v0.80.0 + v0.80.1 observation that `materializeClaudeSurface` keeps SKILL.md dests current per-install regardless of the per-item version short-circuit.
- **FLN-v80-3: result-doc structure stability.** v0.79.0 + v0.80.0 result docs follow the same structure. Worth codifying in a result-doc template skill if cycle-close documentation continues at this volume.
- **2026-05-30 night plan (in-flight tonight):** 3-session roadmap — Session 1 (polish + test plan, no version bump) → Session 2 (v0.80.1 PATCH bundling FLN-v79-2/v79-3/v79-4/v80-1/v80-2) → Session 3 (v0.81.0 MINOR shipping `cowork:audit-siblings` reverse-audit skill). Design: `Docs/plans/2026-05-30-night-plan-design.md`. Headspace test plan for tomorrow morning: `Docs/plans/2026-05-30-headspace-test-plan.md`.
- **FLN-v79-1 (urgent): VISUAL verification of WS-C (sauce-callouts.css)** still owed by user — the remote-control session that closed v0.79.0 had no Obsidian UI access. Snippet + appearance registration verified on disk; need eyeball confirmation that info/warning/tip/example callouts render in DISTINCT colors in the workshop vault. If still monochrome, set non-monochrome callout keys in `platform/mechanisms/styling/data/style-settings-default.json`.
- ~~**FLN-v79-2: `install.js` has no CLI handler**~~ — CLOSED in v0.80.1. install.js now detects `require.main === module` + parses `--vault <path>` + passthrough flags + delegates to run-install.js via subprocess. Two in-stage hardening fixes added: --vault no-value detection + --auto-approve/--decline-all mutual exclusion. HC-V0801-A1 verifies subprocess spawn end-to-end (4 sub-asserts).
- ~~**FLN-v79-3: gather-from-served-by 80-char `agent_markdown` floor**~~ — CLOSED in v0.80.1. Documented in `Docs/agent-guides/build-test-verify.md` `## Writing HC cases` with the canonical 4-line fixture pattern + the +1-bullet fix that recovers under-floor inputs.
- ~~**FLN-v79-4: orchestrator-vs-sub-skill dest convention not codified**~~ — CLOSED in v0.80.1. `Docs/agent-guides/architecture.md` now contains an orchestrator-vs-sub-skill dest convention paragraph: orchestrators flatten to `{{skills_dir}}/<name>/SKILL.md`; sub-skills nest under `{{skills_dir}}/skills/<name>/SKILL.md`.
- ~~**FLN-v79-5: CS-MIG-1 hardcoded counts**~~ — CLOSED in v0.82.1. `run-claude-surface.js` now derives expected counts from `platform/manifest.json` at runtime via `groupBy(claude_surface[], source)`; 4 hardcoded magic numbers (47/38/3/6) removed. Future `claude_surface[]` additions require no harness edits.
- **FLN-v79-6: `classifyGap` resolution heuristics are tight** — the v0.79.0 `RESOLVING_TOOL_SIGNALS` + `CONTENT_TOOL_SIGNALS` regex lists cover the two motivating cases (iMessage `search_contacts`, WhatsApp privacy-cap). Easy lift; expand as new MCPs surface.
- ~~**FLN-v81-1: semver-regex ceiling.**~~ — CLOSED in v0.82.1. New `platform/test/helpers/semver-helper.js` exports `versionAtLeast(version, floor)`. 3 cowork-version-floor assertion sites in `run-cowork-smoke.js`, `run-claude-surface.js`, `run-helper-cases.js` refactored from hardcoded regex to `versionAtLeast()` calls. No ceiling at any semver version.
- **FLN-v81-2: M3 em-dash UX hardening.** `audit-siblings-helper.js`'s `ENTRY_RX` regex requires em-dash `—` (U+2014; matches `composeMicroscope`'s output) but this requirement is invisible to a user hand-editing `microscope.md`. Folded into SKILL.md prose at S4 as a hardening note. Future plan-template wisdom: when a parser depends on a non-obvious character (em-dash, NBSP, ZWJ), document it in the user-facing SKILL.md prose, not just helper code comments.
- ~~**FLN-v82-1: engagement-type-accessibility fix.**~~ — CLOSED in v0.83.0. All 3 engagement-type JSONs (`w2-fte.json`, `personal.json`, `consulting.json`) added to cowork's `files[]`; materialize to `spice/cowork/context/engagement-types/` on consumer vaults. All 7 orchestrators read from the canonical materialized path. STOCK row added to customization contract. cowork 0.21.1 → 0.22.0 MINOR. Preflight `version-sync ok: 0.83.0` ALL GREEN.
- **FLN-v83-1 (next-cycle candidate): orchestrator SKILL.md prose-consistency lint.** Surfaced from v0.83.0 S3/S4/S4.2 plan-inheritance gaps. No HC case currently asserts that each cowork orchestrator's Step 2 contains BOTH (a) the canonical engagement-type materialized-path Read AND (b) a `If not found, exit silently.` or `emit Notice ... and exit.` guard. A future prose-lint HC case would catch plan-spec drift across orchestrators before it ships. ~1-2 stages (write failing test + wire assertions per orchestrator).
- **FLN-v84-1 (NEW — v0.85.0 MINOR candidate): Tier 2 (weekly synthesis) + Tier 3 (monthly synthesis) deferred.** Tier 0 (capture-tick) + Tier 1 (synthesize-day) shipped in v0.84.0. Tier 2 synthesizes daily summaries into a weekly roll-up; Tier 3 synthesizes weekly summaries into a monthly roll-up. Both are well-specified in the v0.84.0 design doc. v0.85.0 MINOR candidate; could bundle with FLN-v84-2.
- **FLN-v84-2 (NEW — v0.85.0+ MINOR candidate): wire-through to midday-tripwire / eod-review / weekly-review / monthly-review deferred.** Each orchestrator could read recent memory (parallel to morning-briefing's step 3a wire-through). Each needs its own pre-flight step + body composition update. Could bundle with FLN-v84-1 in a v0.85.0 MINOR cycle.
- **FLN-v84-3 (NEW — design candidate): memory retention / compression policy.** Memory files accumulate indefinitely. At 1-2 ticks/day × ~50 lines/tick, a year-long active engagement accumulates ~73k lines. A rolling summarization or date-based archival policy will eventually be needed. Design-only until compression logic is scoped.
- **FLN-v841-1 (NEW — v0.85.0+ candidate): test-pin equality helper.** More test-pin sites use hardcoded version strings (SHC-S1, FA6-MANIFEST, AF-1d, AF-V065). FLN-v81-1 closed three pin sites by adopting `versionAtLeast()`; the remaining hardcoded-equality sites surfaced again at v0.84.1 S7. Future cycle could extend `semver-helper.js` with a `versionEquals()` helper and refactor the equality sites where exact pinning is intentional (vs. converting to floor checks where appropriate).
- **FLN-v841-2 (NEW — long-term candidate): prune stale defaultClosed group state.** Activity-feed group state persistence is now asymmetric: `defaultClosed` groups skip reading persistence but still write to it. Toggling a `defaultClosed` group open still persists `open:true` to `dashboard-section-state.json`; that value is just ignored on subsequent renders. Cleanup: prune persisted state entries for groups currently in `defaultClosed` at write time.
- **FLN-v842-1 (NEW — v0.85.0+ small candidate): make `--bump-pins` the implicit default for `sauce update`.** Today consumers must remember to pass `--bump-pins` to actually pick up new workshop versions; plain `sauce update` re-applies current pins and (combined with landmine #16's version-equal short-circuit) silently no-ops the per-item install loop. For the "always track latest" usage pattern across all 4 consumer vaults, this is a footgun the user hits every release. Proposed: flip the default to bump-pins-on; add `--no-bump-pins` for the legacy idempotent-reinstall behavior. One-line CLI change in `platform/cli/verbs/update.js`. Update install.md doc + the build-test-verify.md release-workflow narrative accordingly. PATCH-cycle scope.
- ~~**FLN-v84-4 (Smart Connections semantic retrieval over memory notes):**~~ CLOSED in v0.87.0. NEW sub-skill `cowork:gather-semantic-memory` at `skills/skills/gather-semantic-memory/SKILL.md` wraps `sc-bridge semantic-search` behind a structured-output API; Tier 2 (synthesis.md) prioritized over Tier 1 (memory.md); Tier 0 (per-tick) excluded. Graceful failure-mode taxonomy (empty_anchor / sc_bridge_unavailable / index_unavailable / timeout / empty_matches) — never throws. NEW pure helper `composeSemanticEchoesCallout` renders `[!quote]+ Echoes from your record` callout byte-identical to golden fixture. MB step 3b auto-includes Echoes when sc-bridge + SC index available. cowork 0.25.0 → 0.26.0 MINOR. ~10 HC-V0870 sub-asserts + 1 fixture pair.
- ~~**FLN-v84-1 (Tier 2 weekly synthesis):**~~ CLOSED in v0.85.0. `cowork:synthesize-week` orchestrator (Friday 17:00) ships writing `spice/cowork/memory/<engagement>/YYYY/MM-Month/YYYY-Www/synthesis.md`; idempotent re-fires preserve `created_at`. Engagement-types schema 0.4.0 → 0.5.0 (additive `synthesize_week`). HC-V0850-E1..E2 + F1 + G1.
- ~~**FLN-v84-2 (cross-orchestrator memory wire-through):**~~ CLOSED in v0.86.0. 4 atomic-note orchestrators (midday-tripwire / eod-review / weekly-review / monthly-review) gain pre-flight step 3a invoking `cowork:read-memory` with appropriate tier+window + body-composition step piping output through a NEW pure helper per orchestrator (`composeMidamMemoryCallout` / `composeEodMemoryCallout` dual-output / `composeWeeklyMemoryCallout` / `composeMonthlyMemoryCallout`). Each byte-identical to a golden fixture; null-data gating preserves backward-compat. 5 of 5 atomic-note orchestrators now memory-aware. Memory-read frequency amplifies 5× per engagement per day. cowork 0.24.1 → 0.25.0 MINOR. ~16 HC-V0860 sub-asserts + 10 fixtures.
- ~~**FLN-v83-2 (--bump-pins blueprint-pin handling):**~~ CLOSED in v0.85.0 §0.1. `handleBumpPins`'s `_resolveWorkshopPath` now detects when `installed.workshop_path` diverges from the `__filename` ancestry walk (stale Cellar keg scenario) and prefers ancestry. Design's "skips when N-2+ behind" hypothesis disconfirmed. Real-world deploy-time validation on accuris/headspace still owed (see FLN-v85-1).
- ~~**FLN-v85-1 (NEW — v0.86.0+ candidate): deploy-time validation of FLN-v83-2 hardening on accuris/headspace.**~~ — CLOSED in v0.85.1. Real-world deploy on accuris exposed that v0.85.0 §0.1's libexec-only heuristic was insufficient for the active-pantry layout (no `libexec/`-named ancestor). v0.85.1 §1 generalizes `_resolveWorkshopPath` via new `_isValidWorkshopRoot()` helper to walk for any ancestor with a valid sauce manifest (`platform/manifest.json` parseable + `workshop_version` field present); covers brew Cellar + active-pantry + in-vault pantry uniformly. v0.85.1 §2 auto-populates `installed.workshop_path` in `handleBumpPins` on first successful resolve so future runs short-circuit without re-walk. HC-V0851-A1..A4 (4 layouts + auto-populate). Validated against the v0.85.1 dogfood + synthetic active-pantry HC-V0851-A2 fixture. Consumer-vault validation owed at the v0.85.1 brew-upgrade.
- **FLN-v85-2 (NEW — process candidate): formalize prior-cycle assertion supersedure.** When a refactor obsoletes prose checks (e.g., HC-V0840-C1/C2 obsoleted by v0.85.0 S8's morning-briefing refactor), retire them in the SAME stage rather than leaving them RED. Codification candidate for `Docs/agent-guides/build-test-verify.md` § Writing HC cases.
- **FLN-v85-3 (NEW — test-pin hygiene candidate): smoke regex assertions on JSON-stored naming_patterns must accept both escaped + unescaped forms.** S10 polish surfaced that the rule-engine wants escaped (`^synthesis\\.md$`) while pragmatic readers may use either. Future rule_fragment cycles should pre-flight assertion regexes against both forms at S0.
- **FLN-v85-4 (NEW — v0.86.0+ small candidate): scheduled-job cron migration hints.** v0.85.1 §3 changed synthesize-week default cron `0 17 * * 5 → 30 17 * * 5`. Existing scheduled jobs registered at v0.85.0 onboard (e.g., `cowork-synthesize-week-accuris`) won't auto-update — consumer must hand-edit each `cowork-synthesize-week-<engagement>` task via the scheduled-tasks MCP. Future cycles that change a default cron value (or add/move a cadence) should ship a migration hint (e.g., an idempotent `cowork:migrate-cron <old> <new>` skill) or document the manual one-liner more prominently. Generalized cron-migration tooling is a worthwhile small candidate independent of this specific change.
- **FLN-v86-1 (NEW — v0.87.0+ candidate): compose helper consolidation question.** 4 per-orchestrator helpers (composeMidamMemoryCallout / composeEodMemoryCallout / composeWeeklyMemoryCallout / composeMonthlyMemoryCallout) chosen over single consolidated switch-based composer at v0.86.0 design time. Empirically correct given each orchestrator's body context differs enough to justify separation. Consolidation is a refactor candidate only when patterns prove uniform across future wire-through cycles (e.g., v0.87.0 semantic-retrieval + v0.89.0 distill-week additions). Defer the question until at least 2 more wire-through cycles have shipped + the helper count exceeds 6.
- **FLN-v86-2 (NEW — process candidate): stash + dogfood install collision pattern.** Document in `Docs/agent-guides/build-test-verify.md` § Dogfood install: avoid `git stash` across install boundaries because the install writes to `CLAUDE.md` + `ranch/claude-surface-registry.json` + `ranch/platform-installed.json` that aren't idempotent with stash semantics. Future cycles needing selective drift management should use `git checkout HEAD -- <files>` for targeted restoration rather than stash + unstash.
- **FLN-v86-3 (NEW — small polish candidate): weekly-review + monthly-review missing v0.85.0 §0.3 `[!quote]- Memory log` backlink.** v0.85.0 §0.3 added the atomic-note backlink footer to morning-briefing / midday-tripwire / eod-review but not weekly-review / monthly-review (those weren't memory-aware at the time). With v0.86.0's wire-through, weekly-review + monthly-review are now memory-aware too — they should also carry the `[!quote]- Memory log` backlink. Retroactive add is a small polish PATCH candidate; could fold into v0.86.1 or pair with FLN-v85-4 cron-migration tooling.
- **FLN-v87-1 (NEW — empirical validation owed at deploy):** Does `min_similarity: 0.45` actually filter noise on the real accuris / headspace corpus tomorrow? Design-time guess based on the sequencing decision's "200 ticks noisy" analysis. Empirical lower bound unknown until v0.87.0 deploy runs against real corpora. Post-deploy task: review first 3-5 morning briefings; if Echoes callouts surface obviously-unrelated matches, raise threshold to 0.55 / 0.65; if relevant matches are consistently below 0.45 (filtered out), lower it. Could ship as v0.87.1 PATCH if the bound is wrong.
- **FLN-v87-2 (NEW — USER-INVOCATION slash command deferred):** `/cowork recall <query>` — explicit user-driven semantic query against the memory corpus (vs the v0.87.0 auto-include in MB). Could ship as v0.87.1 once the auto-include behavior is validated; user-invoke would surface `cowork:gather-semantic-memory` directly with a user-supplied anchor + on-the-fly top_k / threshold args. Deferred from v0.87.0 scope per design § 1.4.
- **FLN-v87-3 (NEW — Tier 2 prioritization timing observation):** Tier 2 (synthesis.md) prioritization in `gather-semantic-memory` only manifests once `cowork:synthesize-week` has fired at least once per engagement (i.e., the first Friday after v0.85.0 deploy). For the first week post-v0.87.0 deploy (before the first Friday), retrieval falls through to Tier 1 (memory.md) only. Not a regression — just an empirical observation that the Tier 2 advantage manifests gradually over the first 2-3 weeks of accumulated weekly syntheses.
- ~~**v0.89.0 next-cycle pointer (people-cohesion arc slice B):**~~ CLOSED in v0.89.0. Slices B+C+D bundled into a single vertical slice (slice C narrowed to integration-without-auto-stub). NEW mechanism `people-identity@0.1.0` + NEW sub-skill `cowork:resolve-person` + 3 gather-skill integrations + canonical `wikilink_people` hard rule + morning-briefing inner-circle name→phone resolution. people 0.5.2 → 0.6.0; cowork 0.26.0 → 0.27.0. Mechanism count 16 → 17. Slice E (brain-map rollups) deferred to v0.91.0 (coalesced with retro/insights).
- **v0.90.0+ pointer (shifted from v0.89.0 — third memory-arc displacement):** MINOR — distill-week + auto-update prefs per the (now thrice-superseded) sequencing decision. NEW `cowork:distill-week` orchestrator (Friday after synthesize-week + gather-semantic-memory for entity-dedup) reads the prior week's synthesis + composes interactive distillation proposing preference updates (entity additions / kind-priority shifts). NEW `cowork:apply-distillation` sub-skill prompts Y/N for each proposed update and patches `spice/cowork/context/user-preferences.md`. Optional NEW canonical type `cowork-distillation`. Cowork 0.27.0 → 0.28.0. ~7-9 stages. See `Docs/plans/2026-06-02-v0.86-v0.89-scope-sketches.md` § v0.88.0 (sketch text still references v0.88.0; treat as v0.90.0).
- **v0.91.0+ pointer (shifted + coalesced — was v0.90.0+ retro/insights AND v0.92.0+ slice E):** retro views + insights surfacing PLUS people-cohesion arc slice E (brain-map fields + rollups: last-interaction, co-occurrence, frequency, semantic similarity per person note — now signal-rich after ~2 weeks of forward-only wikilink emission from v0.89.0). See sketches doc + `Docs/plans/2026-06-03-v0.88.0-people-cohesion-1-design.md` §9.
- **FLN-v89-1 (NEW — v0.91.0+ candidate): auto-stub-on-miss for handle-only contacts.** Original slice C scope; narrowed at v0.89.0 brainstorm to plaintext-on-miss. Future cycle if real-world deploy of v0.89.0 generates demand (handle-only iMessage/WhatsApp contacts with no person note still emit plaintext + remain unsearchable as `[[Person]]`).
- **FLN-v89-2 (NEW — v0.91.0+ candidate): collision-as-audit-error promotion.** v0.89.0 emits `console.warn` on alias collision; future cycle promotes to validator audit-error so collisions surface in `/audit` output rather than only in dev console.
- **FLN-v89-3 (NEW — long-term candidate): performance index for >500-person vaults.** v0.89.0 PeopleIdentity scans frontmatter on every resolve. No caching this cycle; FLN candidate when a consumer vault hits >500 person notes.
- **FLN-v89-4 (NEW — small candidate): `inner_circle_people: string[]` migration to `list[wikilink]`.** Current shape is a bare-string list (e.g., `["Stefan de P.", "Steve"]`); migrating to wikilink-shape (`[[Stefan de Pagter]]`) is stronger semantic + future-proofs against rename. v0.89.0 keeps string-shape working via aliases_by_type.phone resolution.
- **FLN-v89-5 (NEW — forward-looking breadcrumb): `personality.hard_rules` disable-path for `wikilink_people`.** v0.89.0 SKILL.md documents the opt-out signature (`personality.hard_rules: [{id: "wikilink_people", disabled: true}]`) but no implementation; helper currently always-injects.
- ~~**FLN-v89-6 (load-bearing — `gather-from-served-by` does not call `cowork:resolve-person`):**~~ CLOSED in v0.89.1. M365 / microscope-routed vaults (accuris) did not get gather-time `[[Person]]` wikilink emission across `gather-from-served-by`-routed kinds because v0.89.0's voice rule was soft and required in-dispatch instruction to fire reliably. v0.89.1 wires hybrid pre-resolve + soft prompt: NEW pure JS helper `resolve-inner-circle-helper.js` exposes `composeInnerCircleAllowlist` which shapes `cowork:resolve-person` outputs into `{ resolved, unresolved, phone_filter_list }`; gather-from-served-by envelope gains `inner_circle_resolved` + `engagement_id` inputs + `inner_circle_resolved_count` output + Step 3 "Known people in scope" dispatch contract section (allowlist verbatim + ambient resolver soft prompt). 5 atomic-note orchestrators pre-resolve the inner-circle and pass the allowlist + engagement_id to every gather-from-served-by call. Microscope `## Output shape` ALWAYS-WIN precedence preserved (FLN-v89-9 stays queued). cowork 0.27.0 → 0.27.1 PATCH; mechanism count unchanged at 17.
- **FLN-v89-7 (NEW — v0.79.0 carry-forward, hit twice during 2026-06-04 deploy): `--subscribe <mech>=<ver>` CLI flag.** New-in-workshop mechanism subscription still requires manual `mechanisms[]` append in `ranch/platform-subscription.json` before `sauce update --bump-pins` cleanly installs. Hit twice on 2026-06-04 deploy (headspace + accuris). Real footgun, recurrent on every cycle that adds a mechanism.
- **FLN-v89-8 (NEW — small candidate): phone-alias E.164 normalization.** `cowork:resolve-person` does exact-match only; user-typed `620-388-2868` (US-shaped) won't match apple-mcp's `+16203882868` (E.164) in the iMessage filter without normalization at typing time. Currently depends on hand-normalization. Future cycle: add a deterministic E.164 normalizer on the resolver write-path so typed-alias values are stored canonically.
- **FLN-v89-9 (NEW — microscope authoring contract update): explicit wikilink-emit instruction in user microscopes.** v0.89.1's envelope-only fix preserves microscope `## Output shape` ALWAYS-WIN precedence intentionally — a user microscope that says "resolved canonical display name via people-aliases.md" (accuris chat microscope's current phrasing) STILL wins locally and may cause plaintext leak. Real-world dogfood post-v0.89.1 deploy determines whether this gap is observable. If observable, v0.89.2+ candidate: extend `cowork:edit-microscope` SKILL.md prose + audit prompt to explicitly instruct emitting `[[Person Name]]` wikilinks when an inner-circle name resolves.
- **FLN-v89-10 (NEW — low priority): toast-source diagnostic on accuris.** Residual `unknown mode undefined` toasts post-v0.88.2 recreate-and-reload on accuris-sauce still unconfirmed whether they're a real caller (some legacy renderMentionList caller still wired into the layout) or a transient race. Low priority; diagnostic work owed.
- **FLN-v891-1 (NEW — v0.89.2+ small candidate): manifest `files[].dest` substitution-token validator.** v0.89.1 S3.1 fixup (`0f95a1e`) corrected a typo where the initial S3 cowork manifest entry for `resolve-inner-circle-helper.js` used `dest: "{{helpers_dir}}/cowork/resolve-inner-circle-helper.js"` — `{{helpers_dir}}` is not a real token in the install runtime variable table (the canonical pattern is `{{module_directory}}/helpers/`). No preflight check currently rejects undefined substitution tokens. Candidate: add a validator pass that asserts every `{{...}}` token in any manifest `dest` (across all blueprints + mechanisms) exists in the install runtime's variable table. Would catch this class of typo at preflight rather than at first install attempt against a fresh vault.
- **FLN-v891-2 (NEW — v0.89.2+ small candidate): `write-run-note-morning-briefing` Step 2a prose cleanup.** v0.89.1 S2.1 (`3e08e8f`) converted the sub-skill's inline inner-circle phone-list derivation step into a documentation pointer redirecting to the morning-briefing orchestrator's new pre-resolution step. Redundancy structurally cleared but prose still semantically overlaps (sub-skill names a step that no longer carries logic). Candidate: prune Step 2a down to a single sentence pointing readers to the orchestrator, or drop the step number entirely and renumber subsequent steps.
- ~~**FLN-v82-2: ranch/rules/cowork.json dedup.**~~ — CLOSED in v0.82.1. NEW `resetSourceContributions(contributions, sourceName)` helper in `install.js` + wired into per-item install loop. `ranch/rules/cowork.json` shrank 7929→635 lines / 281→22 entries at S8 dogfood. Design pivoted at S0.3 from key-based dedup to per-source reset (two scope shapes in the real file; object-form + string-form; key-based dedup would have dropped string-scope entries). cowork PATCH bump 0.21.0→0.21.1 added mid-cycle to clear landmine-#16 version-skip guard and validate end-to-end.
- ~~**FLN-v82-3: parseYamlIsh hyphenated-key support.**~~ — CLOSED in v0.82.1. `context-builder-dry-run.js#parseYamlIsh` flat-key regex widened from `/^([a-z_]+):/` to `/^([a-z_][a-z0-9_-]*):/`. Hyphenated MCP kinds (e.g., `lharries-whatsapp`, `sharepoint-online`) can now appear in `user-preferences.md` without being silently dropped. HC-V0821-B1.1/B1.2 added.
- **FLN-v821-1: V0750-VERSION exact-version assertions.** `run-cowork-smoke.js` carries a hardcoded `V0750-VERSION` exact-version pin asserting `cowork === X.Y.Z` (distinct from floor checks covered by FLN-v81-1's `versionAtLeast()`). When cowork bumped 0.21.0 → 0.21.1 at S8.1, this pin broke and required a manual edit. Candidate fix: extend `semver-helper.js` with `versionEquals(version, expected)` + refactor all exact-pin sites; or migrate pins to floor checks where semantically appropriate.
- **FLN-v821-2: installItem version-skip + new in-loop mechanism interaction.** When new logic is introduced INSIDE the per-item install loop (e.g., `resetSourceContributions`), `install.js:223`'s per-item version-skip guard silently bypasses it unless the blueprint's version bumps. Discovered at v0.82.1 S8: production dogfood short-circuited cowork's loop body entirely (version unchanged), requiring a co-occurring PATCH bump (S8.1) to validate end-to-end. Plan-template addition candidate: for any cycle touching `installItem`-internal logic, mandate a co-occurring blueprint PATCH bump as part of the dogfood stage. Landmine #23 candidate.
- **FLN-v821-3: S0 verification pattern for cleanup FLNs.** The v0.82.1 S0.3 divergent-bodies check caught a design-assumption error (two scope shapes, not one) before committing a buggy dedup key that would have silently dropped all string-scope entries. Candidate fix: add a "S0 data-shape verification" protocol to the cleanup-cycle plan template or `Docs/agent-guides/build-test-verify.md` — for any cleanup FLN touching an accumulator on real-world production files, enumerate all value shapes before writing any code.

## Mechanisms (17)

| Name | Version | Role |
| --- | --- | --- |
| `customjs-guard` | 1.0.0 | Cold-load TDZ guard for Dataview views |
| `validator` | 0.3.0 | Per-file rules engine + Layer 2 manifest-convention rules |
| `audit` | 0.3.0 | `claude-surface` walker + entity-create walker + `/audit` slash command |
| `nav-buttons` | 2.7.0 | Registry-driven nav-button renderer; consumes icons mechanism |
| `activity-feed` | 0.7.1 | Bucketed activity-feed renderer; framed groups; persisted `<details>` state across re-renders (v0.7.0); numeric ISO compare + defaultClosed gate fix (v0.7.1) |
| `kanban-status-sync` | 0.2.0 | Syncs obsidian-kanban column → frontmatter; NEW `KanbanStatusSyncInit` startup-script class moves sync out of render hot path (v0.2.0) |
| `cards` | 0.2.6 | BeaconCards row/stacked layouts; function-form `meta` opt |
| `accent-button` | 0.1.0 | AccentButton render helper |
| `icons` | 0.1.1 | Lucide kebab → SVG resolver; ~21 vendored Tier 1 SVGs + Obsidian `setIcon` Tier 2 fallback |
| `people-rendering` | 0.1.0 | People page renderers |
| `people-identity` | 0.1.0 | Identity resolver for spice/people/ — handle/email/name/alias → person wikilink |
| `styling` | 0.2.1 | Vendored sauce theme + CSS variables (v0.79.0: per-type callout-color snippet `sauce-callouts.css`); `.sauce-tasks-done` CSS rule (v0.2.1) |
| `convenience` | 0.3.0 | Consumer-default hotkeys/snippets/app-settings; v0.93.3 declares new-tab-default-page in `external_plugins[]` + `community_plugin_settings[]` (whatToOpen=daily-notes / mode=reading-mode) |
| `platform-claude` | 0.1.3 | `/install` `/upgrade` `/bootstrap` lifecycle slash commands + CLAUDE.md marker renderer; v0.1.3 adds `spice/resources/` to `DIRECTORY_MAP_SEEDS` (vault-default-paths namespace introduced by `applyVaultDefaultPaths`) |
| `entity-create` | 0.5.0 | Declarative `new_entity_buttons[]` spec; inside-block JS-comment sentinel; substitution catalogue with `derive`/`validate`/`inline_body` extensions; v0.5.0 adds `presetPrompts` (skips matching prompt UI + uses preset value at button level) + `options_source: "all_projects"` (select prompt resolves to project basenames via `dv.pages().where(p => p.type === "project")`) |
| `backlink-panel` | 0.1.0 | Backlink panel renderer |
| `smart-connections-bridge` | 0.2.0 | Node CLI bridge over `.smart-env/multi/*.ajson` for SC semantic retrieval; `--quiet` suppresses non-fatal parse-skip stderr; v0.93.3 declares `smart-connections` plugin as `required: true` in `external_plugins[]` (warning-only on existing consumers — install path is bootstrap-only until v0.94.0) |

Per-mechanism version history is in `Docs/cycle-history.md`. Current canonical catalogue lives at `platform/manifest.json`.

## Blueprints (13)

| Name | Version | Slash command | Module dir |
| --- | --- | --- | --- |
| `boards` | 0.2.1 | — | `spice/boards/` |
| `cowork` | 0.39.0 | — | `spice/cowork/` |
| `daily` | 0.13.1 | `/daily` | `spice/daily/` |
| `journal` | 0.2.0 | — | `spice/journal/` |
| `meetings` | 0.8.0 | `/meetings` | `spice/meetings/` |
| `people` | 0.6.0 | — | `spice/people/` |
| `products` | 0.3.0 | — | `spice/products/` |
| `project` | 1.16.0 | `/project` | `spice/projects/` |
| `scratch` | 0.5.2 | `/scratch` | `spice/scratch/` |
| `teams` | 0.3.0 | — | `spice/teams/` |
| `to-do` | 0.3.3 | — | `spice/to-do/` |
| `trips` | 0.3.0 | — | `spice/trips/` |
| `finance` | 0.4.0 | — | `spice/finance/` |

> Note: this table's blueprint versions track `platform/manifest.json`'s catalogue, not the per-blueprint `manifest.json`. The two must match (lockstep gate); if you see drift, that's a `check-version-sync.js` violation. Per-blueprint version history is in `Docs/cycle-history.md`.

## Test harnesses (23)

Whole-suite GREEN preserved v0.21.0 → current. Files in `platform/test/run-*.js`:

`run-activity-feed`, `run-audit`, `run-backlink-panel`, `run-bootstrap`, `run-claude-surface`, `run-cli`, `run-cowork-smoke`, `run-doctor-self`, `run-entity-create`, `run-helper-cases`, `run-install`, `run-install-sh`, `run-integration-smoke`, `run-migrate`, `run-migrate-frontmatter`, `run-migrate-layout`, `run-registry`, `run-renderer`, `run-seed`, `run-smart-connections-bridge` (NEW v0.75.0), `run-todo-modal` (NEW v0.63.0), `run-validator`, `run-wiki-to-docs-migration`.

v0.75.1 added 11 new cases (HC-V0751-A1, HC-V0751-B1..B4, HC-V0751-C1..C2, HC-V0751-D1, HC-V0751-E1, HC-V0751-H1..H2) distributed across four existing harnesses. No new harness files; file count stays at 23.

v0.76.0 added 16 new cases (HC-V0760-A1, B1..B4, C1..C4, D1..D3, E1, F1..F2, H1) distributed across run-cli.js, run-install.js, run-cowork-smoke.js. No new harness files; file count stays at 23.

v0.77.0 added 11 new cases (HC-V0770-A1..A3, B1..B2, C1..C4, D1, E1) all in run-cowork-smoke.js. No new harness files; file count stays at 23.

v0.78.0 added 14 new cases (HC-V0780-A1..A4, B1..B3, C1..C4, D1..D2, E1) all in run-cowork-smoke.js. No new harness files; file count stays at 23.

v0.78.1 added 2 new cases (HC-V0781-A1, B1) — static SKILL.md prose lints in run-cowork-smoke.js. No new harness files; file count stays at 23.

v0.79.0 added ~30 new sub-asserts distributed across run-cowork-smoke.js (28 — HC-V0790-A1..A3 / B1..B4 / C1..C2 / D1 / E1..E3 / G1; FOCUSED_USER_PATHS extended with the per-mcp glob) and run-install.js (HC-V0790-F1, 3 sub-asserts mirroring caseV0760E1's reinstall scaffold). CS-MIG-1 hardcoded counts bumped 36→37 skill entries / 45→46 contributions in run-claude-surface.js. No new harness files; file count stays at 23. Cowork-smoke final tally: 608 passed / 0 failed.

v0.80.0 added ~43 new sub-asserts in run-cowork-smoke.js (HC-V0800-A1..A3 / B1..B3 / C1..C6 / D1..D3 / E1) + 3 in run-install.js (HC-V0800-F1 mirrors v0.79.0 F1 scaffold with a non-microscope sibling sentinel `vip-list.md` under `chat` kind to prove preservation generality across per-mcp/**). No new harness files; file count stays at 23. No CS-MIG-1 count change (no new claude_surface[] entry — `composeSibling` + `composeMicroscope` extension are helper-only; orchestrator step 2c + edit-microscope SKILL.md step 4 expansion add no new dest). Cowork-smoke final tally: 651 passed / 0 failed.

v0.80.1 added 4 new sub-asserts in run-install.js (HC-V0801-A1 install.js CLI handler — no-args usage hint + real --vault invocation + sentinel-written verification + tightened exit-code-2 pinning, via subprocess spawn against a consumer-bootstrap-shape synthetic fixture). No new cowork-smoke cases; 1 logical relaxation on HC-V0800-A1 (FLN-v80-1 closure; sub-assert count unchanged). No new harness files; file count stays at 23. No CS-MIG-1 count change. Cowork-smoke final tally: 655 passed / 0 failed (+4 from somewhere in the suite during S2/S3 — see harness output); install-harness +4 sub-asserts.

v0.81.0 added ~26 new sub-asserts in run-cowork-smoke.js (HC-V0810-A1..A4 / B1..B6 / C1) + 1 in run-claude-surface.js (CS-MIG-1 count bump for the new audit-siblings claude_surface entry; 37→38 skill entries / 46→47 total contributions). No new harness files; file count stays at 23. New orchestrator-tier skill + helper materialize into `.claude/skills/cowork/audit-siblings/SKILL.md`. Cowork-smoke final tally: ~688 passed / 0 failed.

v0.81.1 added 1 new sub-assert in run-cowork-smoke.js (HC-V0811-A1 — prose-lint asserting the `/cowork` slash command body at `platform/blueprints/cowork/commands/cowork.md` documents the `audit-siblings` sub-command + names the `cowork:audit-siblings` skill). No new harness files; file count stays at 23. No CS-MIG-1 count change (doc-only fix to existing cowork.md surface). Cowork-smoke final tally: 685 passed / 0 failed.

v0.82.0 added ~17 new sub-asserts in run-cowork-smoke.js (HC-V0820-A1..A4 / B1..B3 / C1..C3 / D1; E1..E3 dropped with WS-C). No new harness files; file count stays at 23. No CS-MIG-1 count change (no new claude_surface[] entry — WS-A is helper + SKILL.md + orchestrator content edits only). Cowork-smoke final tally: 713 passed / 0 failed.

v0.82.1 added ~10 new sub-asserts: HC-V0821-A1.1a, A1.1b, A1.2, A1.3 (resetSourceContributions semantics) + B1.1, B1.2 (parseYamlIsh hyphen-key) in run-helper-cases.js; HC-V0821-C1.1..C1.4 (manifest-derived CS-MIG-1 counts) in run-claude-surface.js. New `platform/test/helpers/semver-helper.js` added (shared helper; not a harness file). No new run-*.js harness files; file count stays at 23. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (manifest unchanged; only assertion mechanic changed — now derived dynamically).

v0.83.0 added ~10 cases / ~14 new sub-asserts: HC-V0830-A1..A3 (materialization: files[] presence + path resolution + field correctness) + HC-V0830-B1..B5 (atomic-note orchestrators read materialized path) + HC-V0830-C1..C2 (bootstrap-vault + onboard-scheduled-jobs) + HC-V0830-D1 (prose-lint: engagement-not-found guard) distributed across run-install.js and run-cowork-smoke.js. HC-V0750-C1 + C3 updated (S4.1 follow-up — modified, not new sub-asserts). No new run-*.js harness files; file count stays at 23. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (no new claude_surface[] entries; manifest-derived counts absorb automatically).

v0.84.0 added ~14 cases / ~29 actual sub-asserts: HC-V0840-A1..A3 (capture-tick orchestrator prose-lint + output contract) + HC-V0840-B1..B3 (synthesize-day orchestrator prose-lint + output contract) + HC-V0840-C1..C2 (morning-briefing pre-flight step 3a + null-data gate) + HC-V0840-D1 (engagement-type schema 0.4.0: supported_cadences + default_cadences across all 3 types; 12 sub-asserts) + HC-V0840-E1..E2 (onboard-scheduled-jobs cadence walk + Cowork.md resolver) + HC-V0840-F1 (customization contract STOCK row for memory/**) all in run-cowork-smoke.js. 4 pre-existing gaps closed at S7.1: HC-V0710-1c (cowork-memory discriminator_tags entry) + HC-V0740-1 (engagement-type exact-version pin 0.3.1 → 0.4.0). Started at 754 passed / 0 failed (post-v0.83.0); ended at 803 passed / 0 failed. No new run-*.js harness files; file count stays at 23. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (no new claude_surface[] entries).

v0.84.1 added ~32 HC-V0841-* sub-asserts: A1 ×7 (`_coerceDay` regression — string, Luxon, Date→null, null/undefined/object→null) + A2 ×15 (`ScratchDayMigrate` behavior — passthrough, Date rewrite, path/filename synthesis, no-op, idempotency, manifest wiring) in NEW `run-scratch.js`; A3 ×2 (`inWindow` numeric epoch compare source-lint) + D1/D2 (`defaultClosed` `!isClosed` gate + toggle WRITE preservation) appended to `run-activity-feed.js`; C1 ×6 (Tasks header forms + `titleHtml` opt + CSS rule) appended to `run-renderer.js`. 3 in-cycle test-pin updates surfaced at S7 (run-helper-cases SHC-S1 + FA6-MANIFEST-daily/activity-feed; run-activity-feed AF-1d + AF-V065; run-install-sh PID-1). NEW `run-scratch.js` harness file added; file count bumps to 24. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (no new claude_surface[] entries).

v0.85.0 added ~24 HC-V0850-* sub-asserts in `run-cowork-smoke.js` (A1..A4 parseMemoryFile parser correctness; B1..B5 parseSynthesisFile parser correctness; C1..C5 composeMemoryCallouts contract — **C3..C5 are golden fixtures** asserting v0.84.0 prose byte equality; D1×3 `cowork:read-memory` SKILL.md structured-output contract; E1..E2 `cowork:synthesize-week` orchestrator prose-lint; F1 engagement-types 0.5.0 schema; G1 cowork-weekly-synthesis canonical type discriminator + rule_fragment; G2 onboard-scheduled-jobs cadence walk includes synthesize-week; G3×3 STOCK row + activity-feed `_DEFAULT_BLUEPRINTS` + dispatch wiring) + HC-V0850-G1 sub-assert in `run-cli.js` (synthesize-week dispatch wiring) + 2 in-cycle test-pin updates (HC-V0740-1 engagement-type schema 0.4.0 → 0.5.0 in `run-helper-cases.js`; V0750-VERSION cowork-pin 0.23.0 → 0.24.0 in `run-cowork-smoke.js`). 2 HC-V0840-C1/C2 superseded at S8 (commented stub left as supersedure note — FLN-v85-2 pattern). CS-MIG-1 counts grow (+2 skill entries: synthesize-week + read-memory); v0.82.1 manifest-derived counting absorbs automatically — no harness edits needed. No new harness files; file count stays at 24. Cowork-smoke final tally: **864 passed / 0 failed**.

v0.85.1 added ~6 HC-V0851-* sub-asserts: A1..A4 (`_resolveWorkshopPath` brew Cellar / active-pantry / in-vault pantry layouts + `handleBumpPins` auto-populates `installed.workshop_path`) + C1 (`_warnIfActivePantryDrift` export + warning prose source-lint) in `run-cli.js`; B1 (synthesize-week SKILL.md schedule line + onboard cadence walk both use `30 17 * * 5`) in `run-cowork-smoke.js`. 1 in-cycle test-pin update (V0750-VERSION cowork-pin 0.24.0 → 0.24.1 in `run-cowork-smoke.js` at S2; FLN-v821-1 lockstep). HC-V0850-G1 polish (S1 polish `0b1df7b`) pinned `--workshop-path` explicitly to keep the case hermetic now that the generalized resolver accepts the real sauce-repo ancestry. 4 pre-existing fixtures patched at S1 collateral (caseV0760B2/B3/B4 + 6 `_bpRun` callers in run-install.js) for the same hermeticity reason. No new harness files; file count stays at 24. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (no new claude_surface[] entries — PATCH cycle). Smoke final tally: **868 passed / 0 failed** (delta +4 from v0.85.0's 864).

v0.87.0 added ~10 HC-V0870-* sub-asserts (~28 inner assertions) in `run-cowork-smoke.js`: A1..A4 (`gather-semantic-memory/SKILL.md` exists + declares required sections `## Inputs`/`## Pre-flight`/`## Gather`/`## Decide`/`## Done`/`## Harness testing` + output-shape substrings `matches`/`similarity_score`/`synthesis_excerpt` + graceful failure clause `found: false`/`error:`/`sc_bridge_unavailable`); B1 (`composeSemanticEchoesCallout` helper exports correct function); **B2 is a golden fixture** asserting `composeSemanticEchoesCallout(populated-fixture) === expected.md` byte-identical; B3 (null-data clean-omit: null OR `{found:false}` OR `{found:true, matches:[]}` → empty string); C1 (MB SKILL.md step 3b invokes `cowork:gather-semantic-memory` with `top_k: 2` + `exclude_window: "last-30d"`); C2 (MB body composition invokes `composeSemanticEchoesCallout` helper); D1 (cowork manifest declares `gather-semantic-memory` in `claude_surface[]` + `compose-semantic-echoes-callout.js` in `files[]`). 1 in-cycle test-pin update (V0750-VERSION cowork-pin 0.25.0 → 0.26.0 in `run-cowork-smoke.js` at S1; FLN-v821-1 lockstep). 1 new fixture pair in `platform/test/fixtures/` (1 input JSON + 1 expected callout MD for HC-V0870-B2). No new harness files; file count stays at 24. CS-MIG-1 counts grow (+1 skill entry for `gather-semantic-memory`); v0.82.1 manifest-derived counting absorbs automatically — no harness edits needed. Smoke final tally: **929 passed / 0 failed** (delta +27 from v0.86.0's 902).

v0.86.0 added ~16 HC-V0860-* sub-asserts in `run-cowork-smoke.js` distributed across 4 groups (one per orchestrator) — A1..A4 (pre-flight `cowork:read-memory` invocation references with correct tier+window: A1 midday tier:tick limit_ticks=4; A2 eod dual tier:tick limit_ticks=16 + tier:day window:today; A3 weekly tier:week window:this-week; A4 monthly tier:week window:{start,end}), B1..B4 (body composition step invokes compose helper by name: B1 `composeMidamMemoryCallout`; B2 `composeEodMemoryCallout`; B3 `composeWeeklyMemoryCallout`; B4 `composeMonthlyMemoryCallout`), **C1..C4 are golden fixtures** asserting helper output is byte-identical to expected MD, D1..D4 (null-data clean-omit: empty/null input → empty string output). 1 in-cycle test-pin update (V0750-VERSION cowork-pin 0.24.1 → 0.25.0 in `run-cowork-smoke.js` at S1; FLN-v821-1 lockstep). 10 NEW golden fixtures in `platform/test/fixtures/` (5 input JSON + 5 expected callout MD; eod's dual helper covered by single input + single expected MD covering both output fields). No new harness files; file count stays at 24. CS-MIG-1 counts grow (no new claude_surface[] entries — only files[] additions for the 4 new helpers; manifest-derived counting absorbs automatically — no harness edits needed). Smoke final tally: **902 passed / 0 failed** (delta +34 from v0.85.1's 868).

v0.98.0 added 11 new HC-V0980-* sub-asserts in `run-helper-cases.js`: HC-V0980-SYNOPSIS-A1..A6 (lead callout `[!info]+ What matters today` + closing-callout absence + per-kind `[!example]-` sigil + ≤80-word cap + first-sentence-concrete + empty-day "Quiet day" plain-acknowledgment) + HC-V0980-CADENCES-A1..A5 (4 per-cadence variant titles: `What changed since morning` / `What landed today` / `Where the week landed` / `Where the month landed` + cross-cadence parity sweep). Per-cycle VERSION-pin sweep: 26 hardcoded `"0.97.x"` / `"0.35.x"` assertion values bumped to `"0.98.0"` / `"0.36.0"` across HC-V0891 / V0900 / V0901 / V0920 / V0930 / V0931 VERSION-* + MANIFEST blocks (23 in `run-helper-cases.js`; 3 in `run-cowork-smoke.js`). 9 compose-body golden fixtures regenerated under `platform/blueprints/cowork/helpers/fixtures/compose-body/case-*/` (input.json drops `closing_md` key; expected-body.md regenerated from new helper output; expected-assertions.json pins new lead-callout titles + `[!example]-` sigil + closing-callout absence). No new harness files; file count stays at 24. CS-MIG-1 count posture UNCHANGED at 47/38/3/6 (no new `claude_surface[]` entries — content-shape contract change only). Helper-cases final tally: **2301 passed / 0 failed** (delta +11 from v0.97.4's 2290).

v0.98.1 added 12 new HC-V0981-* sub-asserts in `run-helper-cases.js`: HC-V0981-FEEDBACK-A1..A8 (sentinel emission `<!-- cowork:feedback-capture v=1 -->` + per-kind `[!summary]-` sub-callout + knob row `**Fire <kind>:** \`[ ] less\` \`[ ] same\` \`[ ] more\`` + tagged fenced `` ```feedback ``` `` + re-fire tick/knob/free-text preservation + empty-day minimal Rail L + block-ID `^item-<kind>-[0-9a-f]{7}` regex + ambiguous-knob sidecar flag) + HC-V0981-PARSEFEEDBACK-A1..A4 (tick map extraction + knob state extraction + free-text fence extraction + graceful null-sentinel degradation). Per-cycle VERSION-pin sweep: ~25 hardcoded `"0.98.0"` / `"0.36.0"` assertion values bumped to `"0.98.1"` / `"0.37.0"` across HC-V0891 / V0900 / V0901 / V0920 / V0930 / V0931 VERSION-* + MANIFEST blocks (~22 in `run-helper-cases.js`; ~3 in `run-cowork-smoke.js`). NEW `scripts/check-files-forbidden-paths.js` wired into `release:preflight` (Safeguard 3 — fails if any `files[]` dest path matches user-owned patterns). NEW harness file count UNCHANGED at 24 (Safeguard 3 guard is a preflight script, not a run-*.js harness). CS-MIG-1 count +1 (new compose-feedback-capture shim `claude_surface[]` entry): 215 total. eod-review sidecar schema_version: `1.0.0` + `1.1.0` (additive; filename `eod-review@1.0.0.json` unchanged). Helper-cases final tally: **2313 passed / 0 failed** (delta +12 from v0.98.0's 2301).

v0.98.2 added 18 new HC-V0982-* sub-asserts in `run-helper-cases.js` + 5 EODITEMS fixture sub-asserts (closing v0.98.1's compose-body cadence-dispatch end-to-end carry-forward): HC-V0982-CAPTURE-A1..A6 (`<!-- cowork:feedback-capture v=2 -->` sentinel + Mattered + Didn't-like sections share `^item-<kind>-<sha>` IDs + re-fire preserves both lists + flags + v=1 prior parses with empty downvotes + both-lists-ticked → `ambiguous_items[]` + empty-day minimal Rail L) + HC-V0982-PARSE-A1..A4 (downvotes map extraction + v=1 tolerance + Tasks-suffix tolerance `✅ YYYY-MM-DD` on tick lines AND `parseRatingCallout` kind-checkbox lines + section state machine no kind leak) + HC-V0982-INGEST-A1..A8 (mattered → +0.05 entity delta + didn't-like → −0.10 + knob ±0.05 post-formula re-clamp + floor-set 0.10 hard-suppression + decay `w' = 1.00 + (w − 1.00) × 0.995` toward 1.00 + schema 2 / missing / `"1.1.0"` / 3 normalizer tolerance + intent validation rejects bad target / missing source quote / kindless `learned_weights` intent + audit-entry format golden `## <date> (run-id: fd-<ymd>-0300)`). Per-cycle VERSION-pin sweep: ~25 hardcoded `"0.98.1"` / `"0.37.0"` assertion values bumped to `"0.98.2"` / `"0.38.0"` (~22 in `run-helper-cases.js`; ~3 in `run-cowork-smoke.js`); `ranch/platform-subscription.json` workshop + cowork (`blueprints[].cowork.version`) lockstep per v0.98.1 lesson #5. CS-MIG-1 count +1 (new ingest-feedback shim `claude_surface[]` entry): 216 total. eod-review sidecar schema_version enum: `["1.0.0", "1.1.0", "1.2.0"]` (additive; filename `eod-review@1.0.0.json` unchanged; 1.2.0 adds `items[]` + `ambiguous_items[]`). learned_weights schema 2 → 3 (nested per-kind entity maps; in-place additive migration on `user-preferences.md` frontmatter). NEW harness file count UNCHANGED at 24. Helper-cases final tally: **2336 passed / 0 failed** (delta +23 from v0.98.1's 2313).

v0.99.0 added 26 new HC-V0990-* sub-asserts in `run-helper-cases.js`: HC-V0990-CAPTURE-A1..A7 (v=3 sentinel + section order + v=3/v=2/v=1 prior tolerance + ambiguous tap + empty-day + Tasks-suffix) + PARSE-A1..A4 (tap extraction yes/no/both/neither + body tolerance + state-machine non-leak + mirrored-parser lockstep) + GATE-A1..A6 (classification matrix + ENGAGED full pipeline + TAP-ONLY satisfaction-only + SILENT audit-only + prose-counts-as-engaged/uprank-as-tick + rolling-30 trim) + SCHEMA-A1..A6 (V4 normalizer tolerance + silence-reset + idempotency + string-"4" coercion + deep-copy + .bak) + PREFIX-A1..A3 (bind + fall-through + case-insensitive dispatch-set membership). Final: helper-cases **2362 / 0** (+26).

v0.100.0 added 7 net new sub-asserts in `run-helper-cases.js`: PDC-3 rewritten (1 -> 2 asserts — `_formatCreated` dead-key fix); PDC-8 x3 (`layout: "row"` + right-side meta format + `_formatCreated` fallback); PDC-9 x1 (list-row ordering); PNB-WRAP-2/3 deleted (-2); PNB-ACC-1..3 x4 (AccentButton delegation contract). Final: helper-cases **2369 / 0** (+7). (v0.100.1 hotfix added PDC-10: **2370 / 0**.)

v0.101.0 added 24 new HC-V1010-* cases (+85 net new sub-asserts) in `run-helper-cases.js`: HC-V1010-CAPTURE (compose v=4 — sentinel on all five cadences, section order, non-EOD `Kinds — quick ticks` checklist, v=3/v=2/v=1 prior tolerance, LEGACY rating-block upgrade-day transition, `kinds_listed` sidecar) + GUARD (either-marker write-guard incl. `learning_enabled: false` gate-closed) + PARSE (`kind_ticks` checklist section + state-machine non-leak + mirrored-parser lockstep) + SAT (per-(day, cadence) append/overwrite + day-window trim 30 + 200-entry cap + prose-over-tap + schema-4-era normalize to `cadence: "eod-review"`) + INTENT (`satisfaction` intent + `satisfaction-series` target + dedup applies-once/logs-twice + downrank-kind-no-entity skip + `source_cadence`) + SCHEMA (`normalizeLearnedWeightsV5` tolerance chain + non-destructive 4 → 5 pin + deep-copy + idempotency + `.bak`) + DISPATCH (all five paths emit v=4 + uniform `surfaced_kinds_for_rating` + ENGAGED/TAP-ONLY/SILENT matrix unchanged on multi-note days). Supersedures: HC-V0990-SCHEMA-A6 (impossible-date fixture), HC-V0960-L-12 (retired rating-block emission pin), HC-V0970-O-7/T-3/T-14 (OI/template pins). Per-cycle pin sweep 23+2 (`"0.100.1"`/`"0.39.0"` → `"0.101.0"`/`"0.40.0"`). 5 unconsumed compose-feedback-capture fixture dirs DELETED under `helpers/fixtures/`. Final: helper-cases **2455 / 0** (+85 from v0.100.1's 2370).

v0.101.1 added 2 new HC sub-asserts (DHB-1 template canonical-form assert in `run-helper-cases.js`) + 6 new DHBR-1..3 behavioral cases in `run-wiki-to-docs-migration.js` (applyDocsHubButtonRepair: project-gated, idempotent, failure-loud; healed 19 hubs at S3 dogfood). 1 REWRITE: `R-WIKI-1` (run-renderer) had asserted the template MUST contain the broken `AccentButton` + `id: "doc-note"` form — i.e. it locked in the bug since v0.50.0. Per-cycle pin sweep: ~13 hardcoded `"0.101.0"` / `"1.15.1"` assertion values bumped to `"0.101.1"` / `"1.15.2"` across HC-V01010 + V0101-PROJ + V0950-VERSION blocks. Final: helper-cases **2457 / 0** (+2); wiki-to-docs-migration **30 / 0** (+6); cowork-smoke 954 / 0 UNCHANGED. CS-MIG-1 count posture UNCHANGED at 216 (no new claude_surface[] entries).

v0.102.0 added ~116 net new sub-asserts in `run-helper-cases.js` distributed across NEW HC families: HC-V01020-EC-1..3 (EntityCreate `presetPrompts` destructured + forwarded + `_resolveOptionsSource("all_projects")` resolves project basenames + schema accepts optional fields) + HC-V01020-PDS-1..9 (`ProjectDocsSections` per-section bucket emission + BeaconCards row layout + presetPrompts wiring + Unfiled bucket + mtime-desc ordering + accent button styling + header-bar section buttons) + HC-V01020-PMP-1..7 (`ProjectMeetingsPanel` query by `project:` match + top-5 + view-all expander + presetPrompts `+ New meeting` button + double-click guard + alias-link match) + HC-V01020-PROJ-MAN-1..4 (project manifest 1.16.0 pin + `sections: []` in frontmatter_template + new_entity_buttons[] doc-note with `section` prompt + folder pattern + `applyProjectSectionsMigration` referenced) + HC-V01020-PROJ-TPL-1..3 (`Template, Docs Hub.md` invokes `ProjectDocsSections`; `Template, Project.md` includes `## Meetings` H2 + `ProjectMeetingsPanel` invocation) + HC-V01020-MEET-1..2 (meetings manifest 0.8.0 + `project` prompt with `options_source: "all_projects"` + frontmatter_template + hub-pill subtitle wiring) + HC-V01020-PSM-1..4 (`applyProjectSectionsMigration` idempotency + flat→knowledge move + subfolder preservation + frontmatter add) + HC-V01020-VDP-1..3 (`applyVaultDefaultPaths` folder creation + `.obsidian/app.json` read-modify-write + user-customization respect) + HC-V01020-CLAUDE-MD-RES-1 (`DIRECTORY_MAP_SEEDS` includes `spice/resources/`). 4 distinct lockstep version-pin events across 5 commits — case lockstep updates (DOC-2, R-WIKI-1, DOCS-INT-2, EC-2, FA2-MEETINGS-1, HC-V0880-MEETINGS-D, DHB-1, several VERSION-* cases) absorbed cleanly. Per-cycle pin sweep across run-helper-cases.js / run-entity-create.js / run-integration-smoke.js / run-renderer.js: `"0.101.1"` / `"1.15.2"` / `"0.7.0"` / `"0.4.0"` / `"0.1.2"` → `"0.102.0"` / `"1.16.0"` / `"0.8.0"` / `"0.5.0"` / `"0.1.3"`; `ranch/platform-subscription.json` lockstep. CS-MIG-1 count +1 (new `spice/resources/` directory-map row in CLAUDE.md). Final: helper-cases **2573 / 0** (+116 from v0.101.1's 2457); claude-surface **217 / 0** (+1); cowork-smoke 954 / 0 UNCHANGED; bootstrap 85 / 0; integration-smoke 36 / 0; wiki-to-docs-migration 30 / 0. Preflight `version-sync ok: 0.102.0` ALL GREEN.

Per-cycle sub-assert deltas are in `Docs/cycle-history.md`. Run via `npm run release:preflight` (gated first on `scripts/check-version-sync.js` per v0.38.0).

## Landmines

**24 entries** as of v0.102.0 close. Full canonical list with rationales + helper-count + stub-md5 invariant in `Docs/landmines.md`.

Most recent additions:

- #24 (v0.102.0) — workshop manifest catalogue drifts from per-mechanism manifests; self-install catches it, preflight doesn't
- #23 (v0.70.7) — `file.mtime` is unreliable on Obsidian Mobile; `created_at` is the only authoritative "created today" signal
- #22 (v0.32.0) — `.local/` is the only consumer override seam
- #21 (v0.29.0) — `sauce audit` is read-only against the audited vault
- #20 (v0.28.0) — source vault is read-only during `sauce migrate`
- #19 (v0.26.0) — platform-managed dir names lowercase

**Landmine #12 allowlist:** currently **18 paths + CLAUDE.md marker regions** (v0.41.0 amendment) covering Templater/Slash-Commander/Daily-Notes/Customjs/Dataview/Hotkeys/Vendored-plugin data.json files + sauce-namespaced snippets + claude_surface markers.

## Update protocol

Edit this file at every cycle close (per the canonical cycle-close artifact list in `Docs/prompts/SESSION-START.md`):

1. Bump **Current** → workshop_version + most-recent-cycle pointer.
2. Append the new version to the **Cycle order** line.
3. If mechanism or blueprint versions changed, update their rows in the **Mechanisms / Blueprints** tables.
4. If a new harness was added, append to the **Test harnesses** list and bump the count.
5. If new landmines were added, update the **Landmines** section.
6. Update the **In-flight / next-candidate queue** to reflect FLNs closed this cycle + new FLNs surfaced.

The full per-cycle narrative goes into `Docs/cycle-history.md` as a new `## v<X.Y.Z> <topic> CLOSED <date>` section — not into this file.
