# v0.81.1 — cowork-slash-doc-fix (result)

**Status:** CLOSED 2026-05-30
**Workshop:** `0.81.0` → `0.81.1`
**Predecessor:** v0.81.0 (cowork-audit-siblings), commit `8a48e18`.

## What shipped

Single doc fix: `platform/blueprints/cowork/commands/cowork.md` (the `/cowork` slash command body) gains a `## /cowork audit-siblings [<kind>] (v0.81.0)` section paralleling the existing v0.79.0 microscope section. Closes a v0.81.0 carry-forward gap: the audit-siblings skill shipped + materialized correctly, but the slash command body didn't document how to invoke it, leaving the user to rely on Claude Code's skill auto-discovery (risky for confidence in tomorrow's headspace test).

## Why this fix mattered

Tomorrow's headspace test plan (`Docs/plans/2026-05-30-headspace-test-plan.md`) Step 7 instructs Will to run `/cowork audit-siblings`. Without the slash body documenting this sub-command, Claude Code would read the cowork.md body, see only bootstrap-vault + microscope sections, and either auto-discover the audit-siblings skill OR get confused. v0.81.1 makes the documented invocation explicit.

## HC cases added

- `HC-V0811-A1` (run-cowork-smoke.js) — prose-lint: slash body documents audit-siblings sub-command + names `cowork:audit-siblings` skill.

## Version moves

| Item | Before | After |
|---|---|---|
| workshop_version | 0.81.0 | **0.81.1** |
| cowork blueprint | 0.20.0 | **0.20.1** (PATCH — content change) |
| package.json | 0.81.0 | 0.81.1 |
| subscription pins | 0.81.0 / 0.20.0 | 0.81.1 / 0.20.1 |

## Stages

| Stage | Commit | Summary |
|---|---|---|
| S1 | test | failing HC-V0811-A1 prose-lint |
| S2 | feat | add audit-siblings section to cowork.md slash body |
| S3 | chore | version bumps + dogfood |
| S4 | docs | cycle-close artifacts |
