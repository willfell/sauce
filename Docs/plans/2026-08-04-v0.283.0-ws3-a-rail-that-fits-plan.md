# Loop-integrity workstream 3 — a rail that fits — plan (pointer)

**Cycle:** 2026-08-04 · **Version:** workshop 0.282.1 → 0.283.0 · **Workstream:** loop-integrity program #3

The implementation plan for this cycle lives at
[`Docs/superpowers/plans/2026-08-04-loop-integrity-ws3-a-rail-that-fits.md`](../superpowers/plans/2026-08-04-loop-integrity-ws3-a-rail-that-fits.md):
TDD tasks for the `adopted` authority tier in `delivery.topology`, the `adopt`
verb (originally two tasks, merged into one deliverable mid-plan — see
`docs(plan): merge ws3 tasks 2 and 3 into one adopt deliverable`), provenance
classification in `collectBoardHealth`, `card_note_sha` / `foreign_write`
detection in `projectCard`, the `preimage_sha` plan-and-apply guard in
`heal-epic-bindings`, the shared `concurrent_modification` code, and docs +
full verification.

Execution diverged from the plan in two places, both recorded rather than
back-fitted: the ledger phase became `adopted` rather than `completed`
(Director ruling, see the result doc), and the whole-branch review before close
added a fix wave — `adopt`'s missing projection refresh, the mis-named
`concurrent_modification` verb, `card_note_sha` stamping in three further
writers, and the missing cycle-close artifacts.
