---
type: cycle-result
cycle: v0.59.0
workstream: frontmatter-alignment
card: FA-7 · Finance migration
date: 2026-05-17
status: shipped
design_doc: Docs/plans/2026-05-16-frontmatter-alignment-design.md
predecessor: v0.58.0 (FA-6 Domain wave)
---

# v0.59.0 — FA-7 Finance migration (result)

Finance alone — largest single-blueprint surface in the alignment workstream. Three sub-flows (budget + paycheck + invoice) × 2-3 templates each + 3 entity-create entries + 4 new rule_fragments. `finance@0.3.1 → 0.4.0` MINOR. `workshop_version 0.58.0 → 0.59.0` MINOR. Controller-direct (no FA-7a/b/c split — single cycle handled cleanly). FA-7 closes Wave 4 of the umbrella design.

## What shipped

### Templates (6) — canonical `created_at:` + discriminator tag strips

| Template | created → created_at | type | Cleanups |
|---|---|---|---|
| `Budget Template.md` | ISO+TZ via `tp.file.creation_date` | `budget` retained | **`budget_month:` → `month:` canonical alignment** (cross-blueprint with cowork-monthly + invoice); drop `[finance, budget]` tags |
| `Paycheck Template.md` | ISO+TZ | `paycheck` retained | drop `[finance, paycheck]` tags |
| `Invoice Template.md` | ISO+TZ | `invoice` retained | drop `[finance, invoice]` tags; `month:` + `date:` already canonical |
| `Time Log Template.md` | ISO+TZ | `time-log` retained | drop `[finance, time-log]` tags; `month:` already canonical |
| `Invoice Board Card.md` | ISO+TZ via `tp.file.creation_date` | **NEW `invoice-board-card`** | drop temporal `YYYY/MM/DD` tag; preserve `kanban-card` + `invoice-card` (functional) |
| `Invoice Board.md` | static `2026-05-17T16:45:00-06:00` | `kanban` (plugin-managed) | drop `[board, finance, invoice]` discriminator tags; preserve `kanban-plugin: board` + `title:` (Kanban plugin-owned) |

### Hubs (4) — static `created_at:` + tag rationalization

| Hub | Static created_at | Tags |
|---|---|---|
| `Finance.md` | `2026-05-17T16:45:00-06:00` | `[finance]` → `[finance-hub]` (functional marker replaces discriminator) |
| `Budgets.md` | same | `[finance, budget]` → `[finance-hub]` |
| `Paychecks.md` | same | `[finance, paycheck]` → `[finance-hub]` |
| `Invoices.md` | same | `[finance, invoice]` → `[finance-hub]` |

### Entity-create entries (3 + 2 extra_files) — canonical `created_at:` substitution

All 3 `new_entity_buttons[]` entries' `frontmatter_template.created` renamed to `created_at` with `{{now.YYYY-MM-DDTHH:mm:ssZ}}` substitution (entity-create catalogue supports `{{now.<moment-format>}}` per ctx.now.format dispatch). All `tags: [finance, <subtype>]` arrays dropped (cleanup — categorization belongs in user-tagged content, not auto-injected by entity-create).

- **budget** entry: also renames `budget_month → month` in `frontmatter_template` (cross-blueprint canonical alignment).
- **paycheck** entry: preserves `pay_period_start/end` validate `gte:start_date` + `min:0` + `|number` predicates.
- **invoice** entry: preserves `extra_files[]` (Time-Log + Board sub-kanban).
- **Time-Log extra_file:** `created → created_at`, drop tags.
- **Board extra_file:** `created_at` ADDED (was missing), drop `board/finance` tags; preserve `invoice/{{prompts.month}}` (path-scoped functional).

### Rule fragments (NEW — 4 entries; was empty)

Finance had no rule_fragments pre-FA-7. NEW canonical-vocab-extending fragments for all 4 sub-types:

| Scope | Type + key shape | Naming pattern |
|---|---|---|
| `spice/finance/budgets/**/*.md` | `type: budget`; `month: ^\d{4}-\d{2}$` | `^Budget-\d{4}-\d{2}\.md$` |
| `spice/finance/paychecks/**/*.md` | `type: paycheck`; `pay_period_{start,end}: ^\d{4}-\d{2}-\d{2}$` | `^Paycheck-\d{4}-\d{2}-\d{2}\.md$` |
| `spice/finance/invoices/*/Invoice-*.md` | `type: invoice`; `month: ^\d{4}-\d{2}$`; `date: ^\d{4}-\d{2}-\d{2}$` | — |
| `spice/finance/invoices/*/Time-Log-*.md` | `type: time-log`; `month: ^\d{4}-\d{2}$` | — |

All declare `extends: "_canonical-vocab"` (FA-1 substrate inherits canonical `type:` regex + `created_at:` ISO+TZ requirement). Invoice Board.md hub is plugin-managed and intentionally outside canonical-vocab contract.

### Helpers (2) — backwards-compat shim on `budget_month`

- `budgets-cards.js`: 2 read-sites changed `p.budget_month` → `(p.month || p.budget_month)` (canonical-first with legacy fallback).
- `finance-status.js`: 1 read-site same shim.

Backwards-compat shim accepts non-migrated consumer notes during the deploy-round transition. Once migration runs at consumer vaults, the fallback path is dead code (cleanup candidate for a future cycle).

## Catalogue + version lockstep

- `platform/manifest.json`: finance 0.3.1 → 0.4.0; workshop_version 0.58.0 → 0.59.0.
- `ranch/platform-subscription.json`: workshop_version 0.58.0 → 0.59.0.
- `package.json`: 0.58.0 → 0.59.0.

## Whole-suite delta

| Harness | v0.58.0 baseline | v0.59.0 result | Δ |
|---|---|---|---|
| run-helper-cases.js | 813 | 839 | +26 (FA7-MANIFEST + 5 templates × 2 + budget month canonical + 3 entity-create × 2 + budget canonical + 4 extends scopes) |
| other 17 harnesses | — | unchanged | 0 |

**Net +26 sub-asserts** vs v0.58.0 baseline. Whole-suite preflight green at tag.

## Stage commits on `origin/main`

| Stage | Commit | Description |
|---|---|---|
| S0 | — | Plan inline (FA-3 / FA-5 / FA-6 precedent — controller-direct, no separate plan doc) |
| S1-S4 | `8ad8723` | finance canonical-vocab adoption (6 templates + 4 hubs + 3 entity-create entries + 2 helper shims + 4 NEW rule_fragments + catalogue lockstep + workshop_version bump + harness deltas) |
| Tag | `v0.59.0` | Annotated tag pushed to origin |

Single mega-commit groups the entire blueprint delta. Tag `v0.59.0` pushed; `release.yml` workflow triggers homebrew tap auto-bump PR.

## Consumer-vault migration: DEFERRED to next deploy round

Accumulating: v0.57.0 (FA-5 cowork) + v0.58.0 (FA-6 trips+to-do+boards) + v0.59.0 (FA-7 finance). Three workshop tags ahead of brew sauce.

Deploy round checklist (when triggered):
1. Tap formula bump on `willfell/homebrew-sauce` to v0.59.0 (likely needs PR-merge or direct main push consolidating v0.57.0+v0.58.0+v0.59.0).
2. `brew upgrade sauce` to v0.59.0.
3. Bump consumer subscription pins at all 4 vaults: cowork 0.7.0 → 0.8.0, trips 0.1.7 → 0.2.0, to-do 0.1.4 → 0.2.0, boards 0.1.0 → 0.2.0, finance 0.3.1 → 0.4.0, workshop_version 0.56.x → 0.59.0.
4. Add migration rule for `budget_month → month` to `platform/migrations/v0.53-frontmatter.json` scoped to `type: budget` (parallel to FLN-FA5-1 `month_iso → month` for cowork-monthly).
5. `sauce migrate-frontmatter --vault <v> --blueprint cowork --apply` (FA-5 carry).
6. `sauce migrate-frontmatter --vault <v> --blueprint trips --apply` (FA-6).
7. `sauce migrate-frontmatter --vault <v> --blueprint to-do --apply` (FA-6).
8. `sauce migrate-frontmatter --vault <v> --blueprint boards --apply` (FA-6).
9. `sauce migrate-frontmatter --vault <v> --blueprint finance --apply` (FA-7).
10. `sauce reinstall --vault <v>` × 4 vaults.
11. `sauce audit --frontmatter-alignment` × 4 vaults — zero `legacy_key_used` expected.

## FIX-LATER notes

1. **FLN-FA7-1 — `budget_month → month` migration rule missing.** Mirrors FLN-FA5-1 (cowork `month_iso → month`). v0.53 migration spec needs `{ from: "budget_month", to: "month", scope: { type: "budget" } }`. Both FLN-FA5-1 + FLN-FA7-1 should land in the same migration-spec patch before the deploy round.

2. **FLN-FA7-2 — `helpers/budgets-cards.js` + `finance-status.js` carry backwards-compat shim** `(p.month || p.budget_month)`. Once consumer vaults migrate (deploy round), the fallback is dead code. Cleanup candidate for a follow-up patch.

3. **FLN-FA7-3 — `invoice-board-card` + `trip-board-card` + `task-board-card` are 3 parallel patterns**; could unify via a generic `<X>-board-card` audit walker rule. Out of scope this cycle.

4. **FLN-FA7-4 — Invoice Board.md kanban-plugin hub intentionally excluded from rule_fragments.** Kanban-plugin owns `kanban-plugin: board` + `title:` keys. The hub gets a static `created_at:` but no enforced rule. Same posture as boards' To-Do-Board.md (FA-6) + cowork-monthly's NOT existing (kanban boards aren't part of cowork's surface).

5. **FLN-FA7-5 — Time-Log + Board extra_file fragments NOT in rule_fragments.** The rule_fragment scope for Time-Log is included (`spice/finance/invoices/*/Time-Log-*.md`) but the Board sub-kanban isn't (it's plugin-managed; same as FLN-FA7-4 disposition).

6. **FLN-FA7-6 — Invoice Board.md `tags: []` is empty.** Was `[board, finance, invoice]`. Empty tags is awkward but accurate (all 3 were discriminators/categorization-by-platform). A future cycle might add a `[finance-board]` functional marker if any helper needs to query board files specifically. For now, the kanban-plugin doesn't need a tag.

7. **FLN-FA7-7 — Existing consumer-vault budget notes have `budget_month:` not `month:`.** Until migration runs (deploy round), the helpers' fallback shim ensures continued correct rendering. Audit would flag `missing canonical month:` on those — visible at first `sauce audit --frontmatter-alignment --blueprint finance` run; expected; resolves post-migration.

## Recommended next round

**FA-8 · Universal backlink panels** per umbrella design §C Wave 5. **Payoff cycle** — pays off the canonical-vocab investment visibly. Materializes `BacklinkPanel` on entity atlases (Person / Project / Team / Product / Trip). Cross-blueprint canonical key reuse (`people:`, `projects:`, `teams:`, `products:`, `trips:`, `meetings:`) makes this a single reusable Dataview query parameterized by entity type.

- **NEW `backlink-panel@0.1.0` mechanism** (under `platform/mechanisms/`).
- **NEW `BacklinkPanel` CustomJS class** with `render(dv, entityType)` API.
- **Per-entity-atlas materialization** — new dataviewjs block injected into existing atlas templates (Person.md, Project Atlas, Team.md, Product.md, Trip Atlas).
- **Test surface:** NEW `platform/test/run-backlink-panel.js` (~20 sub-asserts) + renderer cases for each entity atlas.

Whole-suite delta target ~+25 sub-asserts.

Alternates:
- **Deploy round** — three accumulating workshop tags (v0.57.0 + v0.58.0 + v0.59.0) need to reach the 4 consumer vaults. Heaviest deploy round yet (5 blueprint migrations + 4 subscription pins).
- **FA-9 · Activity feeds + rollups** — cross-blueprint activity feed (cowork hubs / "Today" surface) + project-rollup dashboard. Lands the full user-facing payoff. May split FA-9a / FA-9b.
- **Pause for deploy + smoke test** — round 13's project-flow fix not yet observed working at any vault. Three FA cycles deep without consumer validation. Pausing might be prudent.
