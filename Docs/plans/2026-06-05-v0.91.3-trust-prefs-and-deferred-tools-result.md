---
purpose: v0.91.3 cycle-close result. Records what shipped, smoke deltas, and verification posture for the trust-user-prefs + deferred-tool-loading + dataviewjs-regex-tightening PATCH.
related: 2026-06-05-v0.91.3-trust-prefs-and-deferred-tools-design.md
---

# v0.91.3 — result

## What shipped

Three coordinated structural fixes in a single PATCH cycle, plus three accuris vault data-layer fixes synced to laptop.

### Fix A — Connectivity signal authority

5 orchestrator SKILL.md files modified (`morning-briefing`, `midday-tripwire`, `eod-review`, `weekly-review`, `monthly-review`):

- Added "Connectivity signal authority (v0.91.3)" clause inside the Step 3c/3d dispatch-plan prose.
- TRUST: `prefs.mcps[<kind>].served_by` + `prefs.mcps[<kind>].connected` (runtime-authoritative, last-seen recent).
- DO NOT TRUST: `vault-config.mcp_map` (bootstrap-time-only audit hint; stale-prone).
- Cross-reference resolved served_by names against `reachable_namespaces[]` for final dispatch decision.

### Fix B — Deferred MCP tool loading

5 orchestrators gain MANDATORY (v0.91.3) paragraph at top of `## Gather`, after the existing MANDATORY priority-loop paragraph:

> Load deferred MCP tools UPFRONT before the priority loop. Use ToolSearch to load candidate tools by capability, then dispatch. Do not let a deferred-tool stub trigger a silent fallback to a warning callout.

Per-orchestrator candidate tool lists baked in.

### Fix C — Dataviewjs regex tightening

C.1 — 5 write-run-note sub-skills (`skills/skills/write-run-note-*`) tightened the canonical SpaceNavButtons invocation check:

- Required pattern: `dv.view("ranch/views/customjs-guard", { class: "SpaceNavButtons" })`
- Explicitly REJECTS hallucinated shape: `const { SpaceNavButtons } = customJS; SpaceNavButtons(dv, {...})`

C.2 — 5 orchestrator Step 17 regexes updated to the canonical pattern:

```
/```dataviewjs\s*\n\s*await\s+dv\.view\(\s*["']ranch\/views\/customjs-guard["']\s*,\s*\{\s*class:\s*["']SpaceNavButtons["']\s*\}\s*\)/
```

### Bundled data-layer fixes on accuris vault

Synced to laptop. Not platform code — vault state repair.

- `vault-config.md` frontmatter closing `---` fence restored (pre-v0.90.3 corruption legacy).
- `vault-config.md` mcp_map: M365/github/ADO connected; gmail/gcal not_applicable.
- `user-preferences.md` `mcps.ado.served_by` updated from `"accuris-ado"` alias to UUID `1151913a-530a-4a36-a5f6-3bedcab4e4da`.

## Versions

- cowork: 0.29.2 → **0.29.3** PATCH
- workshop: 0.91.2 → **0.91.3** PATCH
- mechanism count: 17 (unchanged)

## HC + smoke

55 new HC-V0913-* sub-asserts:

- HC-V0913-PREFS-A1..E1 = 15 (Fix A clause presence + content in 5 orchestrators)
- HC-V0913-TOOLS-A1..E1 = 15 (Fix B MANDATORY paragraph presence + content in 5 orchestrators)
- HC-V0913-DVJS-A1..E1 = 15 (Fix C.1 canonical-pattern requirement + REJECT clause in 5 sub-skills)
- HC-V0913-VERIFY-A1..E1 = 10 (Fix C.2 Step 17 canonical regex literal in 5 orchestrators)

Smoke deltas:

- helper-cases: 1760 → **1815** passed / **0** failed (+55)
- cowork-smoke: **931** (unchanged)
- claude-surface: **207** (unchanged)
- version-sync: `ok: 0.91.3`

## Deploy

- Tag: `v0.91.3` pushed to `origin/main`
- release.yml: completed successfully
- homebrew-sauce PR #129: merged
- `brew upgrade sauce` on workshop laptop: pending (this machine — applies after cycle-close commit).
- `sauce update --bump-pins` on accuris + headspace: pending (user-initiated after `brew upgrade`).

## Verification posture

- v0.91.3 prose additions did NOT trim existing orchestrator prose. v0.92.0 MINOR candidate queued: relocate imperative prose to sub-skills + slim orchestrators to ~50-line dispatcher. Today's weekly-review success WITHOUT consulting orchestrator is the load-bearing evidence — prose burden has hit LLM-attention ceiling.
- Defense-in-depth holds at 4 layers: orchestrator CRITICAL callout (v0.90.2) → verbal-commitment Notice (v0.91.1 Fix C) → path write-guard (v0.91.1 Fix A) → frontmatter type write-guard (v0.91.2). Fix C.1 sub-skill regex backstop adds a 5th layer specific to dataviewjs body.
- Tomorrow's (2026-06-06) scheduled accuris morning briefing is the live verification window for Fix A + Fix B. If it produces a real M365 calendar/email/chat-driven artifact (not "MCP unavailable" stub), the cycle is empirically validated.

## Carry-forward

- v0.92.0 MINOR orchestrator-prose-refactor candidate.
- FLN-v90-4 (closing-fence helper-hardening sweep), FLN-v91-1 (cowork.md slash-body auto-emission template), FLN-v89-7/8/10, FLN-v891-1/2, FLN-v90-1/3 stay queued.
- WS-C v0.30.0+ MCP-discovery extension queued.
