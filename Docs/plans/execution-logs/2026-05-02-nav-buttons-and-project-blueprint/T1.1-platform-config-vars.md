---
date: 2026-05-02
task: 1.1
stage: 1
status: completed
---

## What I did

Extended the workshop's `Docs/Meta/platform-config.json` to support the new nav-buttons mechanism + project blueprint installer work. Added a top-level `vault_identity` key set to `"workshop"` (so installer logic can branch on vault role), and appended three new substitution variables (`rules_path`, `templates_path`, `commands_path`) to the `variables` object alongside the existing three. Preserved the existing `_comment` and `workshop_relative_path` keys verbatim. Existing values for `views_path`, `templater_scripts_path`, and `scripts_path` matched the spec already, so no changes there.

## Files changed

- `/Users/willfell/Documents/obsidian/sync/workshop/poc-vault/Docs/Meta/platform-config.json`

## Before / after

> [!example]- Before

```json
{
  "_comment": "Workshop self-install path map. workshop_relative_path '.' = install into this same vault.",
  "workshop_relative_path": ".",
  "variables": {
    "views_path": "Docs/Meta/Views",
    "templater_scripts_path": "Docs/Meta/Templater",
    "scripts_path": "Docs/Meta/Scripts"
  }
}
```

> [!example]- After

```json
{
  "_comment": "Workshop self-install path map. workshop_relative_path '.' = install into this same vault.",
  "workshop_relative_path": ".",
  "vault_identity": "workshop",
  "variables": {
    "views_path": "Docs/Meta/Views",
    "templater_scripts_path": "Docs/Meta/Templater",
    "scripts_path": "Docs/Meta/Scripts",
    "rules_path": "Docs/Meta/rules",
    "templates_path": "Docs/Meta/Templates",
    "commands_path": "commands"
  }
}
```

## Verification

> [!success] `python3 -m json.tool` output

```json
{
    "_comment": "Workshop self-install path map. workshop_relative_path '.' = install into this same vault.",
    "workshop_relative_path": ".",
    "vault_identity": "workshop",
    "variables": {
        "views_path": "Docs/Meta/Views",
        "templater_scripts_path": "Docs/Meta/Templater",
        "scripts_path": "Docs/Meta/Scripts",
        "rules_path": "Docs/Meta/rules",
        "templates_path": "Docs/Meta/Templates",
        "commands_path": "commands"
    }
}
```

Parses cleanly. All three pre-existing keys retained; `vault_identity`, `variables.rules_path`, `variables.templates_path`, and `variables.commands_path` all present.

## Notes

> [!info] Call-outs for the next task
> - `vault_identity` was placed between `workshop_relative_path` and `variables` (top-level sibling, not nested inside `variables`) per the plan's example shape. If subsequent installer code expects it inside `variables`, revisit T1.2.
> - The directory `Docs/Meta/rules` does not yet exist in this workshop vault. Subsequent tasks that copy mechanism rule files will need to ensure that directory is created or that the installer's file-copy logic handles missing parent dirs.
> - The directory `Docs/Meta/Templates` likewise does not yet exist; same caveat for blueprint template copies.
> - No deviations from the plan. Existing values for the first three variables already matched the spec, so no preservation-vs-replace decision was needed.
