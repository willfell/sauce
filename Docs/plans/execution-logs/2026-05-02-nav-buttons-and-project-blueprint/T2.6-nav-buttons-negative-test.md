---
date: 2026-05-03
task: 2.6
stage: 2
status: in_progress
---

# T2.6 — Nav-Buttons Dep Resolver Negative Test

> [!abstract] Goal
> Prove the dep resolver correctly skips `nav-buttons` when its declared `depends_on` on `customjs-guard` is unsatisfiable, and that the skip is recorded in `platform-installed.json` history with an attributed reason. Stage 2 analogue of T1.8.

---

## Setup

> [!warning] Temporary edit — must be reverted
> The `depends_on` range in the canonical workshop file was bumped to an impossible value:
> `platform/mechanisms/nav-buttons/manifest.json`
>
> Field changed:
> ```json
> "depends_on": [
>   { "name": "customjs-guard", "range": ">=99.0.0" }
> ]
> ```
>
> The workshop ships `customjs-guard@1.0.0`, which does NOT satisfy `>=99.0.0`. Therefore the resolver should skip `nav-buttons` on the next install run.
>
> Revert plan: change the range back to `">=1.0.0"`.

---

## Pre-test state

> [!info] Captured from `Docs/Meta/platform-installed.json` before the test run
> - `nav-buttons.installed_at` = `2026-05-03T06:08:05.648Z` (must NOT change after the test)
> - `history.length` = `5` (will grow by 1 after the skip event)

---

## User action required

> [!todo] Steps for the user in Obsidian (workshop vault: `poc-vault`)
> - [ ] Reload Templater user scripts (Settings → Templater → User Script Functions → Reload).
> - [ ] Open `Docs/Meta/_install-platform.md` and run the template (or trigger via the Templater command palette).
> - [ ] Watch the Notice toasts for a skip message — paraphrasing: *"skipping nav-buttons — depends on customjs-guard >=99.0.0 but subscription pins customjs-guard@1.0.0"*.
> - [ ] DO NOT manually fix anything; just observe.
> - [ ] Report back what was seen (Notice text + any errors).

---

## Test results

_To be filled in after the user reports back._

---

## Revert

_To be filled in after the user reports back. Will restore `platform/mechanisms/nav-buttons/manifest.json` `depends_on[0].range` to `">=1.0.0"` and confirm the file matches its pre-test shape._
