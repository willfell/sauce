---
date: 2026-05-03
tasks: 2.1-2.4
stage: 2
status: completed
---

# T2.1–T2.4 — nav-buttons mechanism bundle

## What I did

Bundled Stage 2's mechanism setup: copied the canonical `SpaceNavButtons` class byte-identically from `tmp-acc-vault/Docs/Meta/Scripts/space-nav-buttons.js` into the workshop at `platform/mechanisms/nav-buttons/space-nav-buttons.js`, authored the matching `manifest.json` (declares dependency on `customjs-guard >=1.0.0`, exposes `SpaceNavButtons` as a customjs class, installs via `{{scripts_path}}/nav-buttons/`), registered the mechanism as the fourth entry in `platform/manifest.json`, and subscribed the workshop itself to it via `Docs/Meta/platform-subscription.json`. Workshop is now ready for the HUMAN dogfood install in Task 2.5, which will materialize `Docs/Meta/Scripts/nav-buttons/space-nav-buttons.js` in this very vault.

## Files changed/created

- created `platform/mechanisms/nav-buttons/space-nav-buttons.js` (copy of tmp-acc-vault canonical)
- created `platform/mechanisms/nav-buttons/manifest.json`
- modified `platform/manifest.json` (appended nav-buttons mechanism entry)
- modified `Docs/Meta/platform-subscription.json` (appended nav-buttons subscription entry)

## Verification

T2.1 — diff source vs destination:

```
$ diff tmp-acc-vault/Docs/Meta/Scripts/space-nav-buttons.js poc-vault/platform/mechanisms/nav-buttons/space-nav-buttons.js
DIFF_EMPTY_OK
```

T2.2 — JSON validation:

```
$ python3 -m json.tool < platform/mechanisms/nav-buttons/manifest.json
{
    "name": "nav-buttons",
    "version": "1.0.0",
    "description": "SpaceNavButtons — universal vault-level nav block consumed via customjs-guard.",
    "depends_on": [
        { "name": "customjs-guard", "range": ">=1.0.0" }
    ],
    "customjs_classes": ["SpaceNavButtons"],
    "files": [
        { "source": "space-nav-buttons.js", "dest": "{{scripts_path}}/nav-buttons/space-nav-buttons.js" }
    ],
    "post_install": [],
    "rule_fragments": []
}
```
(parses cleanly)

T2.3 — workshop manifest mechanisms:

```
$ python3 -c "import json; m=json.load(open('platform/manifest.json')); print(len(m['mechanisms']), [x['name'] for x in m['mechanisms']])"
4 ['customjs-guard', 'validator', 'audit', 'nav-buttons']
```

T2.4 — workshop subscription mechanisms:

```
$ python3 -c "import json; m=json.load(open('Docs/Meta/platform-subscription.json')); print(len(m['mechanisms']), [x['name'] for x in m['mechanisms']])"
4 ['customjs-guard', 'validator', 'audit', 'nav-buttons']
```

## Hardcoded path findings

> [!warning] Vault-specific path literals present in `space-nav-buttons.js` — shipping as-is for v1.0.0, follow-up for templatization

The file contains hardcoded paths tied to the consumer vault's folder layout. None blocks v1.0.0 (the file installs verbatim and the consumer happens to have a matching layout), but they are NOT vault-agnostic and should be templatized in a later version.

| Line | Literal | Notes |
|:----:|---------|-------|
| 6 | `Extras/Scripts/customjs-guard` | Docstring example referencing consumer's scripts location. |
| 35 | `/todo/` | Note-type detection by path substring. |
| 36 | `/meetinghubs/`, `/meetings/` | Same. |
| 37 | `/summary/` | Same. |
| 38 | `/timestamps/` | Same. |
| 39 | `boards/planning/planning-board` | Same. |
| 40 | `planning/` | Same. |
| 52 | `Timestamps/{YYYY}/{MM}-{MMMM}/...` | Daily note path generator. |
| 53 | `Extras/Templates/Template, Daily.md` | Templater template path. |
| 54 | `Timestamps/{YYYY}/{MM}-{MMMM}` | Daily folder. |
| 55 | `Timestamps/ToDo/...` | ToDo path. |
| 56 | `Extras/Templates/Template, ToDo.md` | ToDo template. |
| 57 | `Timestamps/ToDo` | ToDo folder. |
| 58 | `Timestamps/MeetingHubs/...` | Meeting hub path. |
| 59 | `Extras/Templates/Template, MeetingsHub.md` | Meeting hub template. |
| 60 | `Timestamps/MeetingHubs` | Meeting hub folder. |
| 61 | `Timestamps/Summary/...` | Summary path. |
| 62 | `boards/To-Do-Board` | Board path. |
| 63 | `boards/planning/Planning-Board` | Projects path. |
| 64 | `Planning/Planning` | Planning path. |
| 123 | `boards/To-Do-Board`, `boards/to-do/` | Board detection. |

> [!info] Recommendation
>
> For v1.x, templatize via `{{daily_path}}`, `{{todo_path}}`, etc., with a per-vault config object loaded from `platform-config.yaml`. Per the bundle instructions: "DO NOT modify them yet (templatization is a follow-up consideration). For v1.0.0 we ship as-is."
