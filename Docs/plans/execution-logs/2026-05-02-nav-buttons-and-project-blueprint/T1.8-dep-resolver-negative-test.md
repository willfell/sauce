---
date: 2026-05-02
task: 1.8
stage: 1
status: in_progress
---

# T1.8 — Dep Resolver Negative Test

> [!abstract] Goal
> Prove the dep resolver correctly skips an item with an unsatisfied `depends_on`, and that the skip is recorded in `platform-installed.json` history with an attributed reason.

---

## Setup

> [!warning] Temporary edit — must be reverted
> A synthetic `depends_on` was injected into the canonical workshop file:
> `platform/mechanisms/audit/manifest.json`
>
> Field added (right after `description`):
> ```json
> "depends_on": [{ "name": "validator", "range": ">=99.0.0" }]
> ```
>
> The subscription pins `validator@0.1.0`, which does NOT satisfy `>=99.0.0`. Therefore the resolver should skip `audit` on the next install run.
>
> Revert plan: delete the entire `depends_on` line from `platform/mechanisms/audit/manifest.json`. (The original file had no `depends_on` field.)

---

## Pre-test state

> [!info] Captured from `Docs/Meta/platform-installed.json` before the test run
> - `audit.installed_at` = `2026-05-03T02:27:18.939Z`
> - `history.length` = `3` (one `install` event each for `customjs-guard`, `validator`, `audit`)

---

## User action required

> [!todo] Steps for the user in Obsidian
> - [ ] Reload Templater user scripts (Settings → Templater → User Script Functions → Reload).
> - [ ] Run the `_install-platform` template.
> - [ ] Watch the Notice toasts for a skip message — paraphrasing: *"skipping audit — depends on validator >=99.0.0 but subscription pins validator@0.1.0"*.
> - [ ] DO NOT manually fix anything; just observe.
> - [ ] Report back what was seen (Notice text + any errors).

---

## Test results

_To be filled in after the user reports back._

---

## Revert

_To be filled in after the user reports back. Will remove the synthetic `depends_on` from `platform/mechanisms/audit/manifest.json` and confirm the file matches its pre-test shape._
