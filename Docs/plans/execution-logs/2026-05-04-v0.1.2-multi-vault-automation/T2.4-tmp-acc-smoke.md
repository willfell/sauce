---
date: 2026-05-04
task: T2.4 — tmp-acc smoke via stub (lag preserved)
status: complete (with deviation note)
---

# T2.4 — tmp-acc smoke via stub

## Plan deviation

Plan T2.4.1 expected exit 0. Actual exit code is **1** because tmp-acc's deliberate-lag subscription produces 2 skip events (`nav-buttons@1.0.0 vs workshop 2.0.0`, `project@0.1.0 vs workshop 0.2.1`), and the harness treats `event:"skip"` history entries as exit-1 triggers (`run-install.js:326-327`).

This is **pre-existing baseline behavior**, not introduced by v0.1.2. Verified by temporarily restoring tmp-acc's PRE-stub bootstrap (md5 `28f5b025...` from `_backups/`), re-running, and confirming exit 1 with the same 2 skip events. Then restored the stub (md5 `a39257da...`) and re-verified shape.

## v0.1.2 deliverable verified

The actual v0.1.2 test — does the stub dispatch + does gitState capture git fields on tmp-acc — passes:

- Stub at md5 `a39257da1dd49ae4481e5cd0a42bdac4` (same as canonical + workshop bootstrap + barebones bootstrap).
- 2 skip history entries each carry `git_commit: "cee5afd5...", git_tag: null, git_dirty: true` — populated by gitState() against the workshop. Real 40-char sha confirms the dispatch reached canonical `install.js`.
- Git-fields assertion: `OK: 2 new history entries; git fields present on all; at least one has a real 40-char sha.`

## Lag preserved

tmp-acc's subscription pins:
```
"workshop_version": "0.3.0",
"mechanisms": [validator@0.1.1, audit@0.1.1, nav-buttons@1.0.0]
"blueprints":  [project@0.1.0]
```

These pins are unchanged by the stub deployment — lag preserved at the subscription level (the stub only routes; it doesn't mutate subscription).

## Conclusion

T2.4 is functionally green for v0.1.2's purposes. The exit-1 is a pre-existing tmp-acc-lag artifact and is unrelated to v0.1.2. If desired, future cycles could update the harness to differentiate "deliberate lag skips" from "unexpected skips" (e.g., new exit-code rule), but that's out of v0.1.2 scope.
