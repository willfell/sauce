---
date: 2026-05-04
task: T1.4 — TDD failing assertion (pre-T1.3-wire-up)
status: complete (FAIL confirmed, restored to baseline)
---

# T1.4 — Failing assertion (TDD red)

## What was added

`platform/test/run-install.js` extended (insert before "----- exit code -----" block) with a `Git-fields assertion` that iterates over `newHistory` (entries added during the current harness run) and asserts every entry has `git_commit`, `git_tag`, `git_dirty` keys present. At least one entry must have a real 40-char sha. Scoped to `newHistory` (not full `history`) because pre-v0.5.0 entries genuinely lack the fields per landmine #14.

## Why scoped to newHistory (deviation from plan literal text)

Plan Step 1.4.2 reads `installed.history` (full). Workshop has 20 pre-cycle history entries, all without `git_commit`. Checking full history would FAIL forever even after T1.3 wire-up — pre-cycle entries can't be retroactively patched. Landmine #14 explicitly tolerates null/missing for pre-v0.5.0 entries. Scoping to `newHistory` matches the spec's intent: every entry written this run (post-resolution) carries the 3 fields.

## TDD red verification

1. Workshop install is idempotent → 0 newHistory → assertion vacuously passes. Couldn't fire failing test.
2. Backed up `Docs/Meta/platform-installed.json` to `/tmp/installed-backup.json`; replaced with `{"mechanisms":[],"blueprints":[],"history":[]}` to force full reinstall.
3. Ran `node platform/test/run-install.js .`:
   - **Exit code:** 1 ✅
   - **Output:** `FAIL: missing git fields on 30 site(s):`
   - 30 missing-field lines (10 newHistory entries × 3 fields each) reported.
4. Restored installed.json from backup; `diff` confirmed byte-identical restore.

The TDD red light is confirmed: the assertion correctly fires when post-resolution history entries lack the git fields. Wire-up T1.2 + T1.3 will turn this green.

## Next

T1.2: insert `const git = gitState(workshopPath)` after workshopPath resolution (before manifest read).
