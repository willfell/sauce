---
date: 2026-05-04
task: T2.2 — workshop self-test via stub
status: complete
---

# T2.2 — Workshop self-test via stub

- `node platform/test/run-install.js .` (where `.` is the workshop) → exit 0.
- 0 new history entries (workshop is idempotent post-S1).
- Final notice: `platformInstall: complete.` (canonical installer's success notice — proves stub-dispatch reached canonical `install.js`).
- Harness's `tp.app.vault.adapter.basePath` is wired to the workshop path; the stub's `path.resolve(basePath, cfg.workshop_relative_path)` resolved to the workshop root; `require(<workshop>/platform/install.js)` succeeded.
- Workshop bootstrap is now the 20-line stub (md5 `a39257da1dd49ae4481e5cd0a42bdac4`); workshop dogfoods via stub.

The harness path now exercises the same dispatch flow Templater will exercise in T2.5 — modulo the harness using a synthetic `tp` mock vs Obsidian providing the real one.
