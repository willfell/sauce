---
date: 2026-05-04
task: T1.7 — smoke C (non-git scratch)
status: complete
---

# T1.7 — Smoke C (non-git path)

Direct unit test against `/tmp/v0.1.2-non-git-smoke` (created without `git init`):

```
{"commit":null,"tag":null,"dirty":null}
```

stderr printed `fatal: not a git repository ...` lines from `git rev-parse HEAD` and `git status --porcelain` (each invocation's stderr surfaced before execSync threw). `git describe ... 2>/dev/null` was silenced by the redirect inside the helper. All three try/catch blocks correctly swallowed the throws and left null defaults.

Scratch dir removed. gitState() honors landmine #14: never throws; returns all-null on non-git paths.
