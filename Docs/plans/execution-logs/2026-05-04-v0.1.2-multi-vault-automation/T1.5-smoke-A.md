---
date: 2026-05-04
task: T1.5 — smoke A (workshop shape verification)
status: complete
---

# T1.5 — Smoke A (shape verification)

Workshop is dirty mid-cycle (T1.1–T1.3 working-tree edits + untracked execution-logs); literal "clean tagged" state is not reachable without stashing the cycle's own changes. Verified shape via blank-installed-then-run instead.

- Backed up `Docs/Meta/platform-installed.json` to `/tmp/installed-T1.5.json`
- Blanked installed.json; ran harness; restored installed.json afterward
- First new entry shape:
  ```json
  {
    "event": "install",
    "kind": "mechanism",
    "name": "customjs-guard",
    "version": "1.0.0",
    "installed_at": "...",
    "git_commit": "f7eaee82253e1f80ce70712cb638bf87698c845f",
    "git_tag": null,
    "git_dirty": true
  }
  ```
- 40-char sha captured; `git_tag` null (HEAD past v0.4.1, no exact tag); `git_dirty` true (working-tree mid-cycle).
- Assertion: `OK: 10 new history entries; git fields present on all; at least one has a real 40-char sha.`
- Exit code 0.
- installed.json restored byte-identical (diff empty).
