---
date: 2026-05-04
task: T2.3 — barebones smoke via stub
status: complete
---

# T2.3 — Barebones smoke via stub

- `node platform/test/run-install.js ../tmp-test-barebones-vault` → exit 0.
- 0 new history entries (idempotent).
- Bootstrap: `wc -l` = 20 (= the stub).
- md5: `a39257da1dd49ae4481e5cd0a42bdac4` (same as canonical `platform/installer-stub.js`).

Stub-dispatch works for barebones. Canonical installer reachable via `require()` from the stub through `tp.app.vault.adapter.basePath` → `<workshop>/platform/install.js`.
