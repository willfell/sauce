---
date: 2026-05-04
task: T1.9 — barebones harness smoke
status: complete
---

# T1.9 — Barebones harness smoke

Two runs:

**Run 1 (idempotent):** harness against `tmp-test-barebones-vault` with existing installed.json. Exit 0; 0 new history entries; assertion vacuously passes. Confirms wire-up doesn't break the idempotent path.

**Run 2 (blanked installed, proof of capture):** backed up barebones installed.json; replaced with empty shell; ran harness; got 11 new history entries; assertion fired green:
```
OK: 11 new history entries; git fields present on all; at least one has a real 40-char sha.
```
First new entry confirmed: `git_commit: f7eaee82253e... | git_tag: null | git_dirty: true`. Captured workshop HEAD (the workshop is in the middle of v0.1.2 S1, hence dirty). Restored barebones installed.json byte-identical.

Barebones now uses the synced bootstrap copy at md5 `28f5b0256900e4f3584e1776953ceb2a` (matches canonical + workshop bootstrap + tmp-acc bootstrap).
