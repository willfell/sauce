---
date: 2026-05-04
task: T2.5 — manual Obsidian smoke (workshop in real Obsidian + Templater)
status: pass
---

# T2.5 — Manual Obsidian smoke (CRITICAL)

User executed the manual smoke per plan Step 2.5.1–2.5.4:

1. Opened workshop vault in Obsidian.
2. Cmd+R full reload.
3. Ran `_install-platform.md` template via Templater command palette.
4. Reported back: **"Pass — install completed cleanly"**.

This is the critical end-to-end test that verifies the thin stub through Templater's actual user-script ABI — the harness can't exercise this path because the harness mocks `tp` rather than going through Templater. T2.5 confirms:

- The stub's `tp.app.vault.adapter.read("Docs/Meta/platform-config.json")` works under real Templater (not just the harness's mock).
- `path.resolve(tp.app.vault.adapter.basePath, cfg.workshop_relative_path)` resolves correctly (workshop's relative path = `..` because it's its own consumer; resolves back to the workshop root).
- `require()` of the canonical `<workshop>/platform/install.js` succeeds.
- The `delete require.cache[require.resolve(installer)]` line works in Obsidian's Electron Node runtime.
- The full canonical installer flow — including v0.1.3's `applyExternalPlugins` + `applyTemplaterHotkeys` + `applySlashCommanderBindings` + the new v0.1.2 `gitState()` capture — runs to completion.

No rollback executed; no `_backups/` files needed.

## Acceptance gate impact

This was the load-bearing T2.5 gate. With it passing, the stub deployment is fully validated for production use. S2 acceptance gate items 1, 2, and 4 are now confirmed.
