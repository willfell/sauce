---
date: 2026-05-04
task: T1.6 — smoke B (dirty marker)
status: complete
---

# T1.6 — Smoke B (dirty workshop)

- `touch platform/.dirty-smoke-marker` — verified `?? platform/.dirty-smoke-marker` in `git status --porcelain`.
- Blanked installed.json; ran harness; restored installed.json afterward.
- Last new entry: `git_dirty: true | git_tag: null`.
- Marker removed; `ls` confirms no such file.
- Exit code 0.

Note: tree was already dirty pre-marker (from T1.1–T1.3 working-tree edits), so marker addition is additive but the test still legitimately exercises the `git_dirty: true` branch — `git status --porcelain` returns non-empty.
