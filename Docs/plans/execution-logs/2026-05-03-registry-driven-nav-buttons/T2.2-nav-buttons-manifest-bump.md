# T2.2 — nav-buttons manifest 1.0.0 → 2.0.0

**Date:** 2026-05-03
**Stage:** S2
**Status:** complete

## What changed

`platform/mechanisms/nav-buttons/manifest.json`:

```diff
   "name": "nav-buttons",
-  "version": "1.0.0",
-  "description": "SpaceNavButtons — universal vault-level nav block consumed via customjs-guard.",
+  "version": "2.0.0",
+  "description": "Thin renderer over Docs/Meta/nav-buttons-registry.json. Each blueprint/mechanism declares nav_buttons[]; installer aggregates.",
```

`nav_buttons[]` field NOT added (the renderer is not a contributor itself). `customjs_classes`, `files`, `depends_on`, `post_install`, `rule_fragments` unchanged.

## Final manifest

```json
{
  "name": "nav-buttons",
  "version": "2.0.0",
  "description": "Thin renderer over Docs/Meta/nav-buttons-registry.json. Each blueprint/mechanism declares nav_buttons[]; installer aggregates.",
  "depends_on": [
    { "name": "customjs-guard", "range": ">=1.0.0" }
  ],
  "customjs_classes": ["SpaceNavButtons"],
  "files": [
    { "source": "space-nav-buttons.js", "dest": "{{scripts_path}}/nav-buttons/space-nav-buttons.js" }
  ],
  "post_install": [],
  "rule_fragments": []
}
```

## Verification

Harness install picked up the version bump and recorded `nav-buttons@2.0.0` in `Docs/Meta/platform-installed.json` (replacing the prior 1.0.0 entry).
