# T2.3 — workshop manifest nav-buttons entry bump

**Date:** 2026-05-03
**Stage:** S2
**Status:** complete

## What changed

`platform/manifest.json` — `mechanisms[]` entry for nav-buttons:

```diff
-    { "name": "nav-buttons", "version": "1.0.0", "path": "mechanisms/nav-buttons" }
+    { "name": "nav-buttons", "version": "2.0.0", "path": "mechanisms/nav-buttons" }
```

`workshop_version` left at `0.4.0` (per task constraint — already bumped in S1; no S2 bump).

`blueprints[]` left untouched in this top-level manifest — project blueprint stays at 0.1.0 here. T2.4 only drops project from the **subscription**; the workshop manifest still describes project as a published item available for other consumers.

## Verification

Manifest catalogue matches the per-mechanism manifest's version (2.0.0) — no version mismatch warning.
