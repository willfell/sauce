---
cycle_arc: v0.112.0 workshop tooling MINOR
kind: plan
status: APPROVED — ready for execution
predecessors:
  - 2026-06-15-v0.112.0-workshop-tooling-brief.md
  - 2026-06-15-v0.112.0-workshop-tooling-design.md
target_artifacts:
  - scripts/workshop-status.js (NEW)
  - scripts/regen-cycle-status.js (NEW)
  - scripts/scaffold-behavioral-harness.js (NEW)
  - Docs/agent-guides/dev-workflow.md (NEW)
  - package.json (npm scripts + version bump 0.111.3 → 0.112.0)
  - platform/manifest.json (workshop_version 0.111.3 → 0.112.0)
  - ranch/platform-subscription.json (workshop_version 0.111.3 → 0.112.0)
  - CLAUDE.md (router link to dev-workflow.md)
  - platform/test/run-helper-cases.js (caseV0112Tooling* family + version-pin sweep)
  - Docs/plans/2026-06-15-v0.112.0-workshop-tooling-result.md (cycle-close)
  - Docs/prompts/2026-06-15-post-v0.112.0-next-session-handoff.md
---

# v0.112.0 plan — workshop tooling

Seven stages. Conventional commits format. Single bundle commit acceptable; per-stage commits preferred for traceability. Each stage independently verifiable.

## S1. `scripts/workshop-status.js`

**Deliverable:** new file. Pattern from design §1.

**Acceptance:**
- `node scripts/workshop-status.js` exits 0 in under 1 second.
- Output includes: workshop version, blueprint version line, branch/sync state, HEAD commit, recent cycles (≥ 1), in-flight pointers, harness gate line.
- `--json` outputs valid JSON parseable by `JSON.parse`.
- `--fresh` re-runs harnesses (slow path; not required to pass in <1s).
- Running outside a workshop root exits non-zero with friendly error.

**Commit:** `feat(tooling): v0.112.0 S1 — scripts/workshop-status.js one-shot survey`

## S2. `scripts/regen-cycle-status.js`

**Deliverable:** new file. Pattern from design §2.

**Acceptance:**
- `node scripts/regen-cycle-status.js --check` exits non-zero when cycle-status.md is stale (current state: it claims v0.108.0; should detect drift to v0.112.0).
- Default invocation rewrites `Docs/agent-guides/cycle-status.md § Current` to reference v0.112.0.
- Re-running on the rewritten file yields no diff (idempotent).
- Malformed result doc → falls back, emits warning, doesn't crash.

**Decision at execution time:** do we apply the regen during this cycle (which would surface all 11 missed cycles into the historical list) OR do we leave the file stale and only fix it forward from v0.112.0? **Recommend: apply forward-only first cycle (sets v0.112.0 as `## Current`; doesn't backfill missed history).** Backfilling can be a v0.112.1 PATCH if user wants the long history reconstructed.

**Commit:** `feat(tooling): v0.112.0 S2 — scripts/regen-cycle-status.js + apply forward-only refresh`

## S3. `scripts/scaffold-behavioral-harness.js`

**Deliverable:** new file. Pattern from design §3. Pulls boilerplate strings from `run-v01103-monthly-overview.js` (canonical zero-dep template).

**Acceptance:**
- `node scripts/scaffold-behavioral-harness.js v01120 workshop-tooling` writes `platform/test/run-v01120-workshop-tooling.js`.
- File contains canonical sections: opening doc-comment, `makeEl`, `makeStrictDA`, `loadClass`, verdict block.
- Re-running without `--force` exits non-zero (refuses overwrite).
- Invalid cycle-id / topic-slug → exit 2.
- After scaffold, `node platform/test/run-v01120-workshop-tooling.js` exits 0 (template has zero cases; verdict prints `passed: 0` `failed: 0`).

**Commit:** `feat(tooling): v0.112.0 S3 — scripts/scaffold-behavioral-harness.js`

## S4. `Docs/agent-guides/dev-workflow.md` + CLAUDE.md router

**Deliverable:**
- New `Docs/agent-guides/dev-workflow.md` (~250 lines, per design §4).
- One-line addition to `CLAUDE.md` "Further reading" router section.

**Acceptance:**
- File exists and renders cleanly in Obsidian (no broken wikilinks).
- CLAUDE.md `## Further reading` list includes a line: `- [\`Docs/agent-guides/dev-workflow.md\`](Docs/agent-guides/dev-workflow.md) — local-clone vs brew, dev-sync.sh, workshop-status / regen-cycle-status / scaffold-behavioral-harness scripts.`

**Commit:** `docs(workshop): v0.112.0 S4 — dev-workflow.md guide + CLAUDE.md surface`

## S5. HC test family + npm scripts wiring

**Deliverable:**
- `platform/test/run-helper-cases.js` — add `caseV0112Tooling*` per design §5 + invoke in main runner block.
- `package.json` — add scripts:
  ```
  "status": "node scripts/workshop-status.js",
  "regen-cycle-status": "node scripts/regen-cycle-status.js",
  "scaffold-harness": "node scripts/scaffold-behavioral-harness.js"
  ```
- Landmine #16 version-pin sweep: any hardcoded `"0.111.3"` in run-helper-cases.js → `"0.112.0"`.

**Acceptance:**
- `node platform/test/run-helper-cases.js` exit 0 with new family green (≥ 15 new sub-asserts).
- `npm run status` works.
- `npm run regen-cycle-status -- --check` exits non-zero (drift detected — we haven't applied regen yet) OR exits 0 if regen was applied in S2.

**Commit:** `test(v0.112.0): S5 — caseV0112Tooling* + npm scripts + version-pin sweep`

## S6. Manifest bumps + workshop self-install dogfood

**Deliverable edits:**
- `platform/manifest.json` — `workshop_version` 0.111.3 → 0.112.0.
- `package.json` — `version` 0.111.3 → 0.112.0.
- `ranch/platform-subscription.json` — workshop_version 0.111.3 → 0.112.0.

**Run:** `node platform/install.js --vault . --auto-approve`

**Acceptance:**
- All three manifests carry `0.112.0`.
- Self-install exits 0.
- `platform-installed.json` has fresh history entries with the new git head.
- Preflight full chain: exit 0; helper-cases ≥ 3389 / 0 (3374 baseline + 15 new V0112-T-* assertions).

**Commit:** `chore(workshop): v0.112.0 S6 — manifest bumps + self-install dogfood`

## S7. Cycle-close artifacts + commit + push

**Deliverable files:**
- `Docs/plans/2026-06-15-v0.112.0-workshop-tooling-result.md` — what shipped, harness deltas, lessons.
- `Docs/prompts/2026-06-15-post-v0.112.0-next-session-handoff.md` — next-session handoff.
- (NO `Docs/cycle-history.md` append OR `Docs/agent-guides/cycle-status.md` update in this stage — the latter is the very thing we just shipped. Decision: run `node scripts/regen-cycle-status.js` to dogfood the new script.)

**Push:** `git push origin main`.

**Tag:** **gated on user approval**. Recommend `v0.112.0` annotated tag if user confirms. Do NOT tag unprompted.

**Commit:** `docs(workshop): v0.112.0 S7 — cycle-close + dogfooded regen-cycle-status`

## Summary of commits

```
feat(tooling):     v0.112.0 S1 — scripts/workshop-status.js
feat(tooling):     v0.112.0 S2 — scripts/regen-cycle-status.js + apply forward-only refresh
feat(tooling):     v0.112.0 S3 — scripts/scaffold-behavioral-harness.js
docs(workshop):    v0.112.0 S4 — dev-workflow.md guide + CLAUDE.md surface
test(v0.112.0):    S5 — caseV0112Tooling* + npm scripts + version-pin sweep
chore(workshop):   v0.112.0 S6 — manifest bumps + self-install dogfood
docs(workshop):    v0.112.0 S7 — cycle-close + dogfooded regen-cycle-status
```

7 commits. Could be collapsed into one merge commit at user discretion; per-stage commits preserve traceability.

## Harness budget

- Pre-S1 baseline: `run-helper-cases.js` 3374 / 0.
- Post-S5 target: ≥ 3389 / 0 (+15 V0112-T-* sub-asserts).
- `run-v01103-monthly-overview.js`: 91 / 0 (unchanged).
- Preflight exit 0.

## Risks at execution time

- **`regen-cycle-status.js` heuristic might mangle the existing complex prose summaries.** Mitigation: write the script with verbose comments, run `--check` mode first to see proposed diff, only apply if diff is clean.
- **`workshop-status.js` `--fresh` mode is slow** (full preflight is ~30s). Mitigation: default cache-only; document `--fresh` as the "I want live numbers" mode.
- **Scaffold script's template diverges from future behavioral harnesses' actual structure.** Mitigation: ship as-is; if v0.113.0+ behavioral harnesses need different boilerplate, the script is one Edit tool call away from updating.

---

**End of plan.** Result doc to be written at `Docs/plans/2026-06-15-v0.112.0-workshop-tooling-result.md` at S7.
