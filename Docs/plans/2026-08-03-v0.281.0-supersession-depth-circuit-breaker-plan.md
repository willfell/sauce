# Supersession-depth circuit-breaker — plan

**Cycle:** v0.281.0 · **Date:** 2026-08-03

## Files

1. `platform/mechanisms/delivery/data/delivery-schema.json`
   - Add `policies.slice_scope_caps.supersession_depth = 3`.

2. `scripts/autoloop/codex-coordinator.js`
   - `supersessionChainDepth(card, state)` — backward walk over `superseded_by`.
   - `supersessionDepthLimit()` — registry accessor (0/absent disables).
   - `commandDiscard`: before `discardCardCore`, if `supersededBy` present and
     `depth >= limit`, return the `awaiting_user_decision` /
     `supersession_depth_exceeded` receipt (no write).
   - `commandSupersessionDepth` — read-only query; dispatch + STRICT_CLI_OPTIONS
     + usage string + export.

3. `plugins/loop/skills/run/SKILL.md` + `plugins/loop/skills/execute/SKILL.md`
   - Second-refutation step probes `supersession-depth` and escalates at the
     ceiling; `supersession_depth_exceeded` discard is a hard stop. `execute`
     NEVER list gains "supersede past the depth ceiling".

## TDD order (red → green watched at each step)

1. Coordinator test: a lineage at the depth limit refuses (`awaiting_user_decision`
   + `supersession_depth_exceeded`), writes nothing, leaves the board line + note.
   Plus a shallow supersession still discards.
2. Read-only query test: depth + `at_limit` for at-limit and shallow lineages.

## Verification

- `run-codex-autoloop` green (2591 assertions).
- `run-card-intake` 249 · `run-delivery-contract` 279 · `lint-schemas` 0 issues.
- `npm run release:preflight` green end to end.
- `npm run release:preflight-bumped` green on a clean tree.

## Out of scope

Board-projection bug (intake path canonicalization + discard orphan-pruning +
ero-egnyte-mcp data heal) — deferred to its own cycle. See design + result.
