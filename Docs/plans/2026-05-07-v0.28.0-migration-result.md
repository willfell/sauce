---
date: 2026-05-07
cycle: v0.28.0
type: result
status: closed
predecessors:
  - Docs/plans/2026-05-07-v0.28.0-migration-design.md (commit 67d251c)
  - Docs/plans/2026-05-07-v0.28.0-migration-plan.md (commit e6a3279)
ships:
  - cli verb: sauce migrate (NEW; 5th verb)
  - lib: platform/migrate/{dispatcher,section-extract,wikilink-rewrite,plan,commit,verbatim}.js (NEW)
  - 5 per-blueprint migrators: people / daily / meetings-note / meetings-hub / to-do
  - new harness: platform/test/run-migrate.js (93 sub-asserts across 9 selectors)
  - end-to-end Accuris migration committed: /sync/accuris (1.5GB, ~30k files) → /sync/workshop/accuris-beacon-poc (1670 plan entries; 0 collisions; clean platform state)
  - Docs/migrate.md (NEW user-facing guide)
informs:
  - v0.28.x carries: ero / headspace migrators (different source layouts), project / boards / journal / finance / trips migrators, v0.27.x carries (autocomplete, AS6, tmp-orphan, NITs)
---

# v0.28.0 — Accuris Migration Tooling — RESULT

> [!success] CLOSED 2026-05-07
> Workshop_version `0.27.0 → 0.28.0` MINOR. Ships `sauce migrate` CLI verb + 5 per-blueprint migrators + 5-phase atomic --commit orchestrator + cross-blueprint wikilink-rewrite pass. End-to-end Accuris migration committed: real `/sync/accuris/` source (1.5GB) migrated into `/sync/workshop/accuris-beacon-poc/` with zero data loss + canonical Sauce schema + Obsidian Sync continuity preserved (in-place wipe). User-confirmed phone-Sync visual smoke PASS.

## Summary

First-of-its-kind orchestration cycle. Six stages (S0 design, S1 TDD-RED, S2 atomic implementation with 5 parallel implementer subagents + two-stage spec/quality review, S3 dry-run + plan review, S4 --commit + smoke, S5 docs + close). Total **3 in-cycle CFs**: CF-1 skip-list extension surfaced at S3 dry-run (cruft inflated 35,495 → 1,670 entries; 21x reduction); CF-2 phase-2 canonical config (legacy variables.Object.assign override broke template paths); CF-3 .obsidian/ scrub of allowlist-managed plugin data files post-restore. Reserved-headroom precedent extends to **16 data points; 6th MIXED case** (4-8 CF expectation per first-of-its-kind precedent v0.21.0=5 / v0.22.0=1 / v0.27.0=3 — hit cleanly in middle of range).

## What shipped

### NEW `sauce migrate` CLI verb

5th verb in `platform/cli/sauce-cli.js` VERBS registry. Invoked as:

```
sauce migrate --from <src> [--commit] [--no-keep-backups]
```

Dry-run by default (writes `migration-plan.json` + emits stdout summary; exits 0 with no other writes). `--commit` triggers 5-phase atomic write. Verb is fully discoverable via `sauce help`; argv parse + arg validation tested via run-cli.js cases C23-C28.

### NEW `platform/migrate/` subsystem

| File | Purpose |
|---|---|
| `dispatcher.js` | Walks source recursively; routes per file to highest-priority migrator whose `canHandle` returns true; aggregates planEntries; collision detection; per-entry I/O errors degrade to warnings. Skip-list excludes `.obsidian/`, `.git/`, `.DS_Store`, `node_modules/`, `venv/`, `.venv/`, `__pycache__/`, `.smart-env/`, `.smart-connections/`, `.trash/`, `Invalid date/`, `*.tmp`, `*.pyc`. |
| `section-extract.js` | Markdown heading + callout-aware section extraction; per v0.27.0 lesson g (section-scoped extraction default). Used by every per-blueprint migrator's body-content lift. |
| `wikilink-rewrite.js` | Phase 4.5 cross-blueprint pass. 5 rules: daily prefix→suffix; meetings-hub prefix→suffix; to-do prefix→suffix; `Extras/People → spice/people`; `Resources/People → spice/people`. Idempotent. |
| `plan.js` | Stdout summary + atomic `migration-plan.json` writer (only writes when explicit outDir given). |
| `commit.js` | 5-phase atomic orchestrator (precheck → backup → bootstrap → carry-verbatim → rewrite-blueprints → wikilink-rewrite → finalize). Crash-safe via `.in-progress` marker + sentinel JSON. Failure-loud abort+restore on phase 2-4 errors. Belt-and-suspenders `_assertTargetWithinRoot` at orchestrator level. |
| `verbatim.js` | Priority-9999 fallback. cp + mtime preservation + parent dir creation; path-traversal validator + tgtRoot containment. |
| `migrators/people.js` | priority 10. Identity copy frontmatter (drop empty aliases + missing phone); regenerate `# [[Name]]` heading + `## Meetings` block; preserve `## Notes`. |
| `migrators/daily.js` | priority 20. Filename rewrite prefix→suffix; preserve created/tags/cssclasses; regenerate top dataviewjs blocks; preserve Morning Briefing callout + free-form notes below dashboard markers. |
| `migrators/meetings-note.js` | priority 30. Filename rewrite prefix→suffix space-preserved; preserve frontmatter (incl `person/X` tags); drop legacy MOC line + Date line + H1; prepend `## Attendees` chip block (per v0.27.0 meetings@0.3.0); preserve agenda/notes/action items; preserve `**Clients**` block when non-empty (spec-review IMP fix). |
| `migrators/meetings-hub.js` | priority 40. Filename rewrite prefix→suffix + folder date-routed; body 100% regenerated (hubs are pure platform); preserve created/daily_note/cssclasses. |
| `migrators/to-do.js` | priority 50. Filename rewrite prefix→suffix + folder date-routed; preserve `## Today's Tasks` checkbox list; regenerate back-button as SpaceNavButtons block. |

### NEW `platform/test/run-migrate.js` harness

9 selectors / 42 cases / 93 sub-asserts (TDD-first per v0.21.1 + v0.27.0 precedent):

- `dispatcher` (M1-M5) — walk + canHandle priority + collision detection + skip-list (extended with venv/__pycache__/.smart-env/*.pyc per CF-1) + return shape
- `verbatim` (V1-V3) — priority + canHandle + plan + migrate
- `people` (MP1-MP5) — canHandle + plan target + frontmatter identity + empty-aliases drop + Notes preserve
- `daily` (MD1-MD6) — canHandle + filename rewrite + target path + platform blocks regenerate + Morning Briefing preserve + free-form preserve
- `meetings-note` (MM1-MM7) — canHandle + filename rewrite + target path + chip block prepend + MOC drop + Date line drop + person tags preserve
- `meetings-hub` (MMH1-MMH4) — canHandle + filename + body regenerate + cssclasses preserve
- `to-do` (MT1-MT4) — canHandle + filename + tasks preserve + back-button regenerate
- `wikilink` (WL1-WL5) — daily/hub/to-do prefix→suffix + path rewrite + idempotent
- `commit` (CM1-CM3) — dry-run no-write + commit API shape + restoreFromBackup helper

### Synthetic fixture vault

`platform/test/fixtures/migrate-source/` — 14 files exercising the full migrator + skip-list surface without touching real Accuris content.

### End-to-end Accuris migration committed

Source: `/Users/willfell/Documents/obsidian/sync/accuris/` (1.5GB total; ~30k files including venv + __pycache__ + .smart-env caches).

Target: `/Users/willfell/Documents/obsidian/sync/workshop/accuris-beacon-poc/` (in-place wipe; existing Obsidian-registered vault id `a89e0d50d31c0c51`; sibling backup at `accuris-beacon-poc.pre-migration-20260507-182546/`).

Plan summary:

| Migrator | Count |
|---|---|
| **people** | 111 |
| **daily** | 224 |
| **meetings-note** | 288 |
| **meetings-hub** | 57 |
| **to-do** | 53 |
| **verbatim** | 937 |
| **TOTAL** | **1670** |

0 collisions, 0 warnings, 272 wikilink rewrites of 733 .md files scanned. 5-phase --commit completed in 3.3 seconds (post-warm). `sauce status` reports `9 mechanisms · 9 blueprints · Drift: none`.

## Cycle shape

| Stage | Tasks | Commits | Outcome |
|---|---|---|---|
| **S0** brainstorm + design | (this) | 1 (`67d251c`) | Design approved section-by-section (architecture / matrix / dispatcher contract / 5-phase flow / mapping table / testing / cycle shape). |
| **S1** TDD-RED baseline | 7 (S1.1-S1.7) | 1 (`491e5ab`) | New harness 0/42 RED at close (10 sub-asserts pass via skeleton-contract checks); cli +6 cases; cmd-migrate stub + VERBS register; 11 skeleton files honoring LITERAL contract. |
| **S2** atomic implementation | 8 (T2.1-T2.8) | 1 (`2d4d8b0`) | **5 parallel implementer subagents** at T2.1-T2.5 all landed clean first-pass — third data point reaffirming v0.21.1 lesson a / v0.27.0 lesson b. Master-driven sequential on T2.6 (helpers + dispatcher + plan + verbatim + wikilink) + T2.7 (commit orchestrator). Two-stage subagent review at T2.8: spec found 1 IMPORTANT (Clients block silently dropped) applied; quality found 2 CRITICAL + 6 IMPORTANT applied (atomic write, in-progress marker, sentinel restore validation, walker error handling, plan.js atomic write, target containment, tmp filename uniqueness, _carryVerbatim throw on missing source, _finalize log on failure). All cases flipped 0/42 → 88/0 GREEN. |
| **S3** Accuris dry-run + plan review | 4 (T3.1-T3.4) | 1 (`33848ac`) | Real /sync/accuris dry-run produced 35,495 entries — but 28,500+ were Python venv + __pycache__ + .smart-env cache cruft. **CF-1**: skip-list extended (`venv`, `.venv`, `__pycache__`, `.smart-env`, `.smart-connections`, `.trash`, `*.pyc`); 35,495 → 1,670 entries (21x reduction). M4 case extended +5 sub-asserts. User greenlit S4 --commit. |
| **S4** Accuris --commit + Obsidian smoke | 3 (T4.1-T4.3) | 1 (`3238cfd`) | First --commit run revealed two phase-2 issues. **CF-2**: `_bootstrap` was honoring legacy `ctx.config.variables` via Object.assign, writing pre-Sauce-rebrand paths (`Extras/Templates`, `ranch/Templater` uppercase) that didn't exist post-wipe. Fix: write canonical Sauce paths only; preserve only `vault_identity_tag` from legacy. **CF-3**: `.obsidian/` restore brought back legacy templater folder-templates from backup; installer's first-wins additive merge couldn't overwrite them. Fix: scrub 9 allowlist-managed plugin data files (`templater-obsidian/data.json`, `slash-commander/data.json`, `daily-notes.json`, `appearance.json`, `obsidian-style-settings/data.json`, `hotkeys.json`, `dataview/data.json`, `customjs/data.json`, `app.json`) after `.obsidian/` restore so helpers write fresh canonical entries. Re-run --commit verified clean state: 7 templater folder-templates all pointing at `ranch/templates/*`, ranch/ subdirs all lowercase (#19), platform-config.json has canonical paths. Phone-Sync visual smoke PASS (user-confirmed). |
| **S5** docs + close | 9 (T5.1-T5.9) | 1 (this commit) | Result writeup, migrate.md user guide, CLAUDE.md status snapshot, +1 landmine (#20 source-vault-read-only), install.md upgrade section, next-cycle handoff doc, workshop_version bump (USER APPROVED). |

**Reserved-headroom-USED: 3 in-cycle CFs (CF-1 / CF-2 / CF-3). 16th data point reinforcing the precedent.** Pattern: first-of-its-kind orchestration cycles draw 1-5 review-surfaced + smoke-surfaced CFs at integration boundaries; v0.28.0 = 3 = within expected range.

## Plan deviations (resolved at design/build time; documented for forensics)

1. **CF-1 skip-list** — design Section 3 listed `.obsidian/`, `.git/`, `.DS_Store`, `node_modules/`, `*.tmp`, `Invalid date/` only. Real Accuris source contained Python venvs + `__pycache__` + `.smart-env` plugin cache totaling ~28k files. Skip-list extended in-cycle; M4 case + 3 fixture files added. Lesson surfaces a generalizable pattern: **dry-run against full real-data source is the only reliable surface for skip-list completeness**; synthetic fixtures can't anticipate every cache + cruft pattern in a 1.5GB+ vault.

2. **CF-2 legacy-config override** — design Section 4 phase-2 said "invoke shared bootstrap orchestrator phases ... using `--non-interactive --mechanisms=all --blueprints=all`". The implementation honored `ctx.config.variables` via `Object.assign(canonical, variables)` — defensible "respect consumer config" posture, but wrong for migration cycles where the goal IS to bring vault to current Sauce schema. Lesson: **migration phase 2 should always write canonical paths; legacy config preservation is the wrong default for cycles whose stated goal is schema modernization**.

3. **CF-3 .obsidian scrub** — design Section 4 phase-2 said "restore .obsidian/ from backup" as a way to preserve user's vault-level Obsidian config without re-fetching plugins. But the restore brought back legacy `data.json` plugin state (templater folder-templates pointing at non-existent paths) that the installer's first-wins additive merge couldn't overwrite. Lesson: **after .obsidian/ restore from backup, surgically delete allowlist-managed plugin data files so installer helpers write fresh canonical entries**. Codified as a v0.28.0 commit.js code comment + new landmine candidate amendment to #12.

## In-cycle CFs (review + smoke surfaced)

**S2 spec review IMP-1: meetings-note `**Clients**` block silently dropped.** Design Section 5 row 3 + plan line 288 required preserving the block when non-empty; `_renderBody` only extracted Agenda / Notes / Action Items. Fix: added `extractSection(rest, /^\*\*Clients\*\*\s*$/m)` + `_hasMeaningfulBullets` check; emits between attendees and Agenda when content present. MM cases unchanged at 11/0 (fixture's Clients block is empty so the gate keeps it dropped — verifying both branches works).

**S2 quality review CRITICAL-1: `_carryVerbatim` swallowed missing source silently.** Inconsistent with phase 4's failure-loud throw. Fix: throw `Error('commit verbatim: source missing for ${entry.src}')` to match phase 4 abort+restore semantics.

**S2 quality review CRITICAL-2: `_finalize` swallowed log-write failures silently.** Migration would report `{ok: true}` even with no log written. Fix: `console.error` on failure (gated on `!SAUCE_TEST_MODE`).

**S2 quality review IMPORTANT-1: phase 1 backup not crash-safe.** A SIGKILL during cp loop left backup half-populated with no marker. Fix: `.in-progress` sentinel file written at start of `_backup`, removed at end; `.sauce-migration-meta.json` records vaultPath. precheck refuses to proceed if a stale in-progress marker found.

**S2 quality review IMPORTANT-2: `restoreFromBackup` didn't validate vaultDir matches sentinel.** Risk of restoring into wrong directory. Fix: read sentinel JSON, refuse on vaultPath mismatch.

**S2 quality review IMPORTANT-3: walker propagated I/O errors aborting plan generation.** Fix: try/catch around `readdirSync` + `statSync`; degrade to warning in `walkSource(srcRoot, warnings)`.

**S2 quality review IMPORTANT-4: `plan.js` migration-plan.json non-atomic write.** Fix: temp+rename idiom matching commit.js posture.

**S2 quality review IMPORTANT-5: target containment check inconsistent across migrators.** Fix: belt-and-suspenders `_assertTargetWithinRoot` in commit.js orchestrator before invoking each migrate(); each migrator retains its own raw-segment validator.

**S2 quality review IMPORTANT-6: `daily.js` + `meetings-hub.js` tmp filenames missing Date.now().** Fix: append `Date.now()` for uniqueness; matches sibling migrators.

**S3 CF-1 skip-list extension.** See plan-deviation 1 above.

**S4 CF-2 phase-2 canonical config.** See plan-deviation 2 above.

**S4 CF-3 .obsidian/ scrub.** See plan-deviation 3 above.

## Harness deltas

| Harness | Before | After | Delta |
|---|---|---|---|
| migrate (NEW) | 0 | 93/0 | +93 across 9 selectors |
| bootstrap | 58/0 | 58/0 | UNCHANGED |
| cli | 42/0 | 50/0 | +8 sub-asserts (C23-C28; baseline drift +6 from plan harmless per v0.26.1 lesson f) |
| install-sh | 14/0 | 14/0 | UNCHANGED |
| helper-cases | 424/0 | 424/0 | UNCHANGED |
| renderer | 30 cases exit 0 | 30 cases exit 0 | UNCHANGED |

Stub md5 invariant `ea23aa812503bfca66359d3b2b239ba8` UNCHANGED. Allowlist (#12) UNCHANGED at 13 paths. Helper count UNCHANGED at 12. Landmines `19 → 20` (NEW #20 — source vault read-only).

## Smoke matrix

> [!success] S4-A acceptance gate (real Accuris → accuris-beacon-poc)
> - [x] **A1** spice/people/ shows 112 notes (111 + hub)
> - [x] **A2** 5 random People notes render with chip + nav buttons + frontmatter
> - [x] **A3** Daily 2026-05-01-Friday renders dashboard + Morning Briefing callout
> - [x] **A4** Meeting note 2025-04-29 Dev Enablement Weekly Team Meeting renders Attendees chips above bullet list (per v0.27.0 meetings@0.3.0 pilot)
> - [x] **A5** Meeting Hub 2026-01-06-Meetings renders current Sauce hub layout (no legacy Daily Navigation Footer)
> - [x] **A6** Cowork/, Files/, attachments/, MOCs/, Docs/, boards/, Planning/ visible at original paths (verbatim carry)
> - [x] **A7** `sauce status` reports `9 mechanisms · 9 blueprints · Drift: none`
> - [x] **A8** Phone Sync delta received; vault opens; user-confirmed visual PASS

## NEW lessons (v0.28.0)

(a) **Parallel-subagent dispatch with literal-contract prompts works clean at 5+ subagents.** Third data point after v0.21.1 (3 parallel) + v0.27.0 (5 parallel — same scale as here). All 5 implementer subagents (T2.1-T2.5) landed clean first-pass with zero merge conflicts and zero re-review cycles needed. The LITERAL contract from S1.4 step 1 quoted verbatim in each subagent prompt was the load-bearing surface; without it, subagents would have diverged on `plan()` / `migrate()` shape.

(b) **Two-stage subagent review caught 2 CRITICAL + 6 IMPORTANT pre-S2-commit.** **9 data points reaffirming v0.20.0 lesson d** (after v0.20.0 / v0.22.1 / v0.23.0 / v0.24.0 / v0.25.0 / v0.26.0 / v0.26.1 / v0.27.0). Spec-only review would have shipped: silent verbatim swallow, silent finalize swallow, non-crash-safe backup, unverified restore target, propagating walker errors, non-atomic plan writes, inconsistent target containment, non-unique tmp filenames. Quality review is non-negotiable — never skip.

(c) **Dry-run against full real-data source is the only reliable surface for skip-list completeness.** Synthetic fixtures can't anticipate Python venvs + `__pycache__` + `.smart-env` plugin caches in a 1.5GB+ source vault. CF-1 surfaced 28,500+ cruft entries that no design-time review would have caught. Generalize: any new migrator + skip-list cycle should include a real-data dry-run as a S3 mandatory step (already in v0.28.0 plan; reaffirm for ero/headspace).

(d) **Migration phase 2 must write canonical paths; legacy config preservation is the wrong default for schema-modernization cycles.** CF-2 root cause was "respect consumer config" posture — defensible for plain `sauce update` reinstall flow but wrong when the cycle's stated goal IS schema modernization. Pattern reusable for ero/headspace migration: `legacyVars` should be inspected for ONLY a small whitelist (here: `vault_identity_tag`); everything else uses canonical defaults.

(e) **After .obsidian/ restore from backup, scrub allowlist-managed plugin data files.** CF-3 root cause was first-wins additive merge in installer helpers — they couldn't overwrite legacy data.json entries from backup. Fix codified in commit.js phase 2c: `ALLOWLIST_MANAGED_PATHS` constant lists the 9 paths to delete after restore. Pattern reusable for ero/headspace + any future migration cycle that restores `.obsidian/`.

(f) **Crash-safety markers (`.in-progress` + sentinel JSON) are cheap insurance for multi-phase orchestrators.** v0.28.0's commit.js writes `.in-progress` at start of phase 1 and `.sauce-migration-meta.json` recording vaultPath; precheck can detect orphan in-progress markers and `restoreFromBackup` validates the sentinel before wiping. Total cost: ~10 LOC; total benefit: silent data loss prevention if SIGKILL hits mid-backup.

(g) **Plan-baseline-count drift continues to be harmless when commit-message reports actual reality.** Plan T1.6 predicted run-cli.js cli=48/0 (+6 sub-asserts); actual was 50/0 (+8). Each C-case has 1-2 sub-asserts; baseline drift +2 is normal cycle-time-accuracy noise. Fifth data point (v0.13.0 / v0.21.1 / v0.22.1 / v0.26.1 / v0.28.0).

(h) **`process.cwd()` fallback in plan.emitPlan was a harness-pollution bug latent in design.** When dispatcher.run is invoked from harness with `ctx={}`, the plan-emit step wrote `migration-plan.json` to the workshop root (cwd at harness-run time). Fix: don't fall back to cwd; require explicit `outDir` from caller; harness path skips file-write entirely. Pattern: any helper that writes-to-cwd-as-fallback risks polluting the test environment; require explicit outDir.

(i) **Reserved-headroom precedent extends to 16 data points; 6th MIXED case** (v0.22.1 / v0.23.0 / v0.24.0 / v0.25.0 / v0.26.0 / v0.26.1 / v0.27.0 / v0.28.0). First-of-its-kind orchestration cycles consistently draw 1-5 CFs in middle-of-range; pure-additive cycles draw 0; pure-rename cycles draw 1-3.

## Carry-forward

| Item | Type | Priority |
|---|---|---|
| **ero migrator** (different layout: `Extras/People/` exists; tighter scope) | Cycle | v0.28.x or v0.29.x |
| **headspace migrator** (different layout: `Resources/People/`; Cowork hub propagation; 107 People) | Cycle | v0.29.x |
| **project migrator** (Accuris uses `Planning/<slug>/`; non-trivial body shape rewrite) | Cycle | v0.28.x |
| **boards migrator** (Accuris boards = different Kanban shape than Sauce boards@0.1.0) | Cycle | v0.28.x |
| **journal migrator** (no Accuris source; needed for ero/headspace) | Cycle | v0.29.x |
| **finance + trips migrators** (no Accuris source) | Cycle | v0.29.x |
| **NewMeetingButton attendees autocomplete** (v0.27.0 carry) | UX enhancement | v0.28.x |
| **AS6 backup-fails-aborts coverage** (v0.26.1 I-4 carry) | Harness | v0.28.x |
| **Atomic-write tmp orphan cleanup** (v0.26.1 I-3 carry) | Defensive | v0.28.x |
| **Pantry-clone source mutation investigation** (v0.26.0 lesson e carry) | Investigation | v0.28.x |
| **`span.cls` dead-code cleanup** (v0.27.0 NIT-1 carry) | Code hygiene | v0.28.x |
| **Cards subtitle-callback try/catch wrap** (v0.27.0 NIT-2 carry) | Defensive | v0.28.x |
| **Validation regex internationalization** (v0.27.0 spec NIT-2 carry) | i18n | v0.28.x |
| **People rendering for daily / journal / projects / todos** | Cross-blueprint | v0.28.x |
| **Multi-theme presets** | DEFERRED 9th time | v0.29.x or de-queue |
| **Project Sub-tasks panel** (carry since v0.18.2) | UI cycle | v0.29.x+ |
| **Sticky-notes blueprint** | Blueprint cycle | v0.29.x+ |
| **install.js `NO_OP_DUE_TO_MISSING_PREREQ` refactor** | Architectural | v0.30.0+ |
| **Frontmatter parser dedupe** (5 reimplementations across migrators; v0.28.0 quality NIT-1 carry) | DRY | v0.28.x or v0.29.x |
| **Per-phase timing in migration.log** (v0.28.0 quality NIT-5 carry) | Observability | v0.28.x |
| **`--keep-backups` flag wired or removed** (v0.28.0 quality NIT-4 carry) | Cleanup | v0.28.x |

## Commits

- `67d251c` `docs(v0.28.0): design — Accuris migration tooling`
- `e6a3279` `docs(v0.28.0): implementation plan — 6 stages, ~50 tasks, TDD-first`
- `491e5ab` `test(v0.28.0): S1 TDD-RED — migrate harness + cmd-migrate stub + skeletons`
- `2d4d8b0` `feat(v0.28.0): S2 — migration tooling implementation (5 migrators + 5-phase commit)`
- `33848ac` `fix(v0.28.0): S3 CF-1 — extend dispatcher skip-list to exclude env/cruft`
- `3238cfd` `fix(v0.28.0): S4 CF-2 + CF-3 — phase-2 canonical config + .obsidian/ scrub`
- `<this commit>` `docs(v0.28.0): cycle close — workshop_version 0.27.0 → 0.28.0 MINOR (Accuris migration tooling)`

Tag: `v0.28.0` annotated and pushed (USER APPROVED at S5 close).

## Next cycle

**v0.28.x carries bundle (PATCH)** — recommended next based on plan-line 142 + v0.27.0 carry list. Bundle the v0.27.x carries (autocomplete, AS6, tmp-orphan, NITs) PLUS v0.28.0 quality-review NITs (DRY frontmatter parsing, per-phase timing, keep-backups flag) into one PATCH cycle before tackling project / boards / journal migrators.

OR **v0.28.1 / v0.29.0 ero migrator** — if user wants to run ero migration immediately (smaller scope: 12 People + similar layout to Accuris). Per v0.27.0 + v0.28.0 lessons: parallel-subagent dispatch on independent migrator files works clean; can drop in ero migrator alongside the existing 5 with minimal scaffold work.

User discretion at S5 close.
