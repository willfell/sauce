# v0.62.0 — FA-9a Activity Feeds — Implementation Plan

**Reference:** `Docs/plans/2026-05-18-v0.62.0-fa-9a-design.md`
**Stages:** 9 stages, ~11-13 commits, ~3-4 hours.

## S0. Verification gate (~3 min)

- [ ] `git status` clean on `main` (one untracked: `.claude/scheduled_tasks.lock`)
- [ ] `cat platform/manifest.json | grep workshop_version` → `"0.61.0"`
- [ ] `cat package.json | grep '"version"'` → `"0.61.0"`
- [ ] `npm run release:preflight` GREEN at baseline (17 harnesses)
- [ ] FA-9 card frontmatter shows `status: in_progress`

## S1. Scaffold mechanism (~30 min)

- [ ] `mkdir platform/mechanisms/activity-feed`
- [ ] Create `platform/mechanisms/activity-feed/manifest.json` (mirror backlink-panel manifest)
- [ ] Create `platform/mechanisms/activity-feed/activity-feed.js`:
  - `class ActivityFeed`
  - `render(dv, opts)` entry — validates scope, dispatches by groupBy
  - `_DEFAULT_BLUEPRINTS` getter returning the canonical type list (~17 entries)
  - `_resolveTimeWindow(scope)` using `window.moment` (with native Date fallback)
  - `_query(dv, blueprints, window, useStatusChangedAt)` — `dv.pages().where(...)`
  - `_renderEmpty(dv, scope)` — italic dimmed "No activity in this <scope>"
  - `_renderGroupedByBlueprint(dv, pages)` — H4 per `type:`, BeaconCards per group
  - `_renderGroupedByHour(dv, pages)` — H4 per `HH:00`, BeaconCards per group
  - `_renderFlat(dv, pages)` — single BeaconCards row layout
  - Notice on invalid scope
  - Notice on BeaconCards unavailable
  - Header comment with API surface + landmines #1/#11 cross-refs
- [ ] Commit: `feat(activity-feed): v0.62.0 S1 — NEW activity-feed@0.1.0 mechanism`

## S2. Test harness (~30 min)

- [ ] Create `platform/test/run-activity-feed.js` — mirror run-backlink-panel.js shape with AF-1..15 asserts
- [ ] `package.json`: add `test:activity-feed` script + append to `release:preflight`
- [ ] Run `npm run test:activity-feed` — GREEN
- [ ] Commit: `test(activity-feed): v0.62.0 S2 — NEW run-activity-feed.js (~15 asserts)`

## S3. Workshop manifest catalogue (~10 min)

- [ ] Edit `platform/manifest.json`:
  - Insert `{"name":"activity-feed","version":"0.1.0","path":"mechanisms/activity-feed"}` after `audit` in `mechanisms[]` (alphabetical-ish)
- [ ] Commit: `feat(manifest): v0.62.0 S3 — register activity-feed@0.1.0 in catalogue`

## S4. Cowork blueprint embeds (~60 min)

Five sub-commits (S4a..S4e). Each independent + reverts cleanly.

### S4a. Cowork manifest (~15 min)
- [ ] Edit `platform/blueprints/cowork/manifest.json`:
  - `version: "0.8.0" → "0.9.0"`
  - description append: ` v0.9.0 MINOR (v0.62.0 FA-9a): depends_on activity-feed >=0.1.0; Daily/Weekly/Monthly Hub grow Recent-Activity sections via ActivityFeed.render; NEW content/Today.md (cowork-today-hub) — curated landing surface for Today's daily/meetings/scratches/project-status-changes.`
  - `depends_on[]` += `{"name":"activity-feed","range":">=0.1.0"}`
  - `files[]` += `{"source":"content/Today.md","dest":"{{module_directory}}/Today.md"}`
  - `rule_fragments[]` += a fragment for `type: cowork-today-hub` matching cowork-daily-hub pattern (scope `spice/cowork/Today.md`)
- [ ] Commit: `feat(cowork): v0.62.0 S4a — cowork@0.9.0 manifest (activity-feed dep + Today.md)`

### S4b. Daily Hub embed (~10 min)
- [ ] Edit `platform/blueprints/cowork/content/Daily Hub.md`:
  - After the `# Daily Notes` H1 + existing CoworkDailyHubCards block, append:
    ```markdown

    ## Today's Activity

    ```dataviewjs
    await dv.view("ranch/views/customjs-guard", {
      class: "ActivityFeed",
      method: "render",
      args: [{ scope: "today", groupBy: "hour", limit: 50 }]
    });
    ```
    ```
- [ ] Commit: `feat(cowork): v0.62.0 S4b — Daily Hub Today's Activity panel`

### S4c. Weekly Hub embed (~10 min)
- [ ] Edit `platform/blueprints/cowork/content/Weekly Hub.md`:
  - Append:
    ```markdown

    ## This Week's Activity

    ```dataviewjs
    await dv.view("ranch/views/customjs-guard", {
      class: "ActivityFeed",
      method: "render",
      args: [{ scope: "week", groupBy: "blueprint", limit: 100 }]
    });
    ```
    ```
- [ ] Commit: `feat(cowork): v0.62.0 S4c — Weekly Hub This Week's Activity panel`

### S4d. Monthly Hub embed (~10 min)
- [ ] Edit `platform/blueprints/cowork/content/Monthly Hub.md`:
  - Append:
    ```markdown

    ## This Month's Activity

    ```dataviewjs
    await dv.view("ranch/views/customjs-guard", {
      class: "ActivityFeed",
      method: "render",
      args: [{ scope: "month", groupBy: "blueprint", limit: 100 }]
    });
    ```
    ```
- [ ] Commit: `feat(cowork): v0.62.0 S4d — Monthly Hub This Month's Activity panel`

### S4e. NEW Today.md (~15 min)
- [ ] Create `platform/blueprints/cowork/content/Today.md` per design §"NEW content file"
- [ ] Commit: `feat(cowork): v0.62.0 S4e — NEW Today.md landing surface (cowork-today-hub)`

## S5. Subscription lockstep + dogfood reinstall (~10 min)

- [ ] Edit `ranch/platform-subscription.json`:
  - `workshop_version: "0.62.0"`
  - mechanisms += `{"name":"activity-feed","version":"0.1.0"}`
- [ ] Run `node platform/cli/sauce-cli.js reinstall --vault .`
- [ ] Verify installer history shows `ranch/scripts/activity-feed/activity-feed.js` materialized; zero errors
- [ ] Commit: `chore(workshop): v0.62.0 S5 — subscription lockstep + dogfood reinstall`

## S6. `run-helper-cases.js` +3 lint asserts (~10 min)

- [ ] Add 3 cases after the v0.61.0 BLP-LINT block: AF-LINT-1..3
- [ ] Wire into the harness run sequence
- [ ] Run `npm run test:helpers` GREEN
- [ ] Commit: `test(helper-cases): v0.62.0 S6 — +3 AF-LINT asserts for activity-feed class`

## S7. `run-cowork-smoke.js` +4 sub-asserts (~15 min)

- [ ] Add COWORK-AF-1..4: regex check H2 markers in Daily/Weekly/Monthly Hub source + presence + dataviewjs blocks in Today.md
- [ ] Run `node platform/test/run-cowork-smoke.js` GREEN
- [ ] Commit: `test(cowork-smoke): v0.62.0 S7 — +4 COWORK-AF asserts for hub Recent-Activity panels`

## S8. Version bump + sync check (~5 min)

- [ ] `platform/manifest.json`: `workshop_version` 0.61.0 → 0.62.0; verify `date: "2026-05-18"`
- [ ] `package.json`: version 0.61.0 → 0.62.0
- [ ] `node scripts/check-version-sync.js` exit 0
- [ ] Update `platform/manifest.json` `blueprints[]` cowork entry 0.8.0 → 0.9.0
- [ ] Commit: `chore(release): v0.62.0 — workshop_version bump + lockstep`

## S9. Preflight + tag + push (~15 min)

- [ ] `npm run release:preflight` ALL GREEN (18 harnesses + version-sync check)
- [ ] `git push origin main`
- [ ] `git tag -a v0.62.0 -m "..."`
- [ ] `git push origin v0.62.0`
- [ ] Capture homebrew-sauce tap PR # for handoff if auto-bump fires.

## Rollback

If preflight fails after tag:
- `git tag -d v0.62.0` locally; don't push the tag.
- Fix the failure; new commit; re-tag.

If post-tag headspace smoke fails:
- File FLN against v0.62.0 result doc → v0.62.1 follow-up cycle.
