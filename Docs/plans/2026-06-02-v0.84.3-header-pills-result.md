---
cycle: v0.84.3
codename: header-pills
kind: PATCH
status: closed
opened: 2026-06-02
closed: 2026-06-02
predecessor: v0.84.2
---

# v0.84.3 header-pills — result

Shipped as designed in `2026-06-02-v0.84.3-header-pills-design.md` and planned in `2026-06-02-v0.84.3-header-pills-plan.md`. Single cycle commit (Task 7) after Task 0's pre-cycle docs commit. No spec deviations. Preflight green.

## What landed

| § | Surface | Files |
| --- | --- | --- |
| §1, §5.1 | Tasks header — orange `N Open` (glow) + optional green `K Done` | `platform/blueprints/daily/helpers/space-daily-dashboard.js` |
| §5.2 | Meetings header — neutral `N` count pill | same |
| §5.3 | Activity header — neutral `N` count pill | same |
| §4 | `_renderSection` `rightHtml` opt | same |
| §3 | Three new pill classes + `.sauce-section-counts` container; retired `.sauce-tasks-done`; mobile pass | `platform/blueprints/daily/helpers/sauce-daily-dashboard.css` |
| §6 | HC-V0843-A1..A10 replace HC-V0841-C1; HC-V0842-A1 retained | `platform/test/run-renderer.js` |
| Dogfood | Workshop's `ranch/scripts/daily/...js` + `.obsidian/snippets/...css` synced byte-equal | `ranch/scripts/daily/space-daily-dashboard.js`, `.obsidian/snippets/sauce-daily-dashboard.css` |
| Version pins | workshop + daily bumps; FA6-MANIFEST daily pin tracked | `package.json`, `platform/manifest.json`, `platform/blueprints/daily/manifest.json`, `platform/test/run-helper-cases.js` |
| Cycle-close | History narrative (v0.84.2 catch-up + v0.84.3); status swap; result doc | `Docs/cycle-history.md`, `Docs/agent-guides/cycle-status.md`, this file |

## Version table

| Unit | From | To |
| --- | --- | --- |
| workshop | 0.84.2 | 0.84.3 |
| daily | 0.13.2 | 0.13.3 |

## Tests

11 HC sub-asserts in the renderer harness:

- **A1** `getTasks` still splits `open`/`done` (v0.84.1 invariant preserved).
- **A2** Tasks `title: "Tasks"` literal — no parenthetical count in the title string.
- **A3** Tasks call site references `sauce-section-open-pill`.
- **A4** Tasks call site references `sauce-section-done-pill`.
- **A5** Meetings `title: "Meetings"` + `sauce-section-count-pill` referencing `meetings.length`.
- **A6** Activity `title: "Activity"` + `sauce-section-count-pill` referencing `activityCount`.
- **A7** `_renderSection` signature includes `rightHtml` opt.
- **A8** `_renderSection` wraps `rightHtml` in `<span class="sauce-section-counts">…</span>`.
- **A9** CSS defines all three pill classes + counts container.
- **A10** Retired `.sauce-tasks-done` is gone from CSS.
- **HC-V0842-A1** (retained) — canonical and `ranch/`-materialized SDD byte-equal post-dogfood.

## Surfaced deviations

None. Implementer bundle (Tasks 1-3) delivered DONE with all 10 new tests passing + HC-V0842-A1 failing as expected (cleared at Task 4). All other tasks executed inline by the controller.

## Carry-forwards

- **FLN-v843-1 (NEW — long-term candidate): CSS byte-equal drift guard.** `HC-V0842-A1` covers JS-file drift between canonical and dogfood. The same drift can happen for the CSS (`platform/blueprints/daily/helpers/sauce-daily-dashboard.css` vs `.obsidian/snippets/sauce-daily-dashboard.css`). Pre-cycle inspection showed those two were already drifted at design-time — they aligned again via Task 4's explicit `cp`. A future PATCH could add a parallel byte-equal HC case for the CSS so drift would fail preflight automatically.
- **Catch-up note:** v0.84.2 cycle-close narrative was missing from `Docs/cycle-history.md` at this cycle's start (v0.84.2 had shipped without artifact-writing). Task 6 included the v0.84.2 narrative as a catch-up alongside the v0.84.3 narrative.

## Preflight

`version-sync ok: 0.84.3` ALL GREEN at single commit.

## Deploy

Tag → release.yml → tap PR → squash-merge → `brew upgrade sauce 0.84.2 → 0.84.3` → `sauce update --bump-pins` × 4 vaults (barebones, accuris-sauce, ero-sauce, headspace-sauce). All four reported clean run / exit 0. Cross-vault verification confirmed `workshop_version: 0.84.3`, `daily: 0.13.3`, and the three new pill class references present in each vault's `ranch/scripts/daily/space-daily-dashboard.js`.
