---
cycle_arc: v0.97.4 prose-invariant write-guards
kind: plan
status: SHIPPED
companion_design: Docs/plans/2026-06-10-v0.97.4-prose-invariant-write-guards-design.md
companion_result: Docs/plans/2026-06-10-v0.97.4-prose-invariant-write-guards-result.md
---

# v0.97.4 prose-invariant write-guards — plan

## Stage breakdown

### S1 — RED (test-first)

Add 23 new sub-asserts to `platform/test/run-helper-cases.js`:

- HC-V0974-WG-1..4 — rating-callout write-guard (4 asserts: rejects when sentinel absent; accepts when sentinel present; accepts when learning_enabled=false; accepts when surfaced_kinds_for_rating=[])
- HC-V0974-WG-5..8 — anti-echo write-guard (4 asserts: rejects when callout absent; accepts when callout present; accepts when aspect not applied; missing-input precedence)
- HC-V0974-WG-9..12 — coverage-gap callout injection (4 asserts: callout injected into body; sidecar.coverage_gap mirrored; no injection when complete; callout positioned BEFORE per-kind blocks)
- HC-V0974-PD-1..5 — plan-dispatch completeness (5 asserts: 5-priority engagement; custom_kind not filtered; UUID-shaped served_by; unreachable emits warn; priorities order preserved)
- HC-V0974-GS-1..6 — gather-from-served-by SKILL.md inline contract (6 asserts: inline section header; four mandatory elements; verbal-commitment line; pre-render self-check; flat-list fallback prohibition; urgency-tier labels)

Expected: 12 RED at S1 commit (PD-1..5 ship GREEN — helper contract already correct; GS-1..6 RED until S2 SKILL.md update; WG-1..12 RED until S2 helper implementation).

Commit: `test(cowork): v0.97.4 S1 — HC-V0974 RED (write-guards + plan-dispatch + gather-output-shape, 23 sub-asserts)`. Explicit `git add platform/test/run-helper-cases.js`.

### S2 — implement

- `platform/blueprints/cowork/helpers/write-atomic-note-helper.js` — add three guards in declared order. New optional params: `surfaced_kinds_for_rating`, `learning_enabled`, `expected_kinds`.
- `platform/blueprints/cowork/data/schemas/{6 cadence schemas}.json` — add optional `coverage_gap` property.
- `platform/blueprints/cowork/skills/skills/gather-from-served-by/SKILL.md` — add inline microscope output-shape contract section.
- `platform/blueprints/cowork/content/data/orchestrator-instructions/{morning-briefing,midday-tripwire,eod-review,weekly-review,monthly-review}.md` — update Step 8.5 with v0.97.4 guidance.

Run ALL test suites; expect ALL GREEN.

Commit: `feat(cowork): v0.97.4 S2 — prose-invariant write-guards + inline microscope output-shape contract`. Explicit `git add <each file>`.

### S3 — version + dogfood

Bump:
- `package.json` 0.97.3 → 0.97.4
- `platform/manifest.json` workshop_version + cowork blueprint pin 0.35.3 → 0.35.4
- `platform/blueprints/cowork/manifest.json` 0.35.3 → 0.35.4
- `ranch/platform-subscription.json` workshop + cowork pins
- `platform/test/run-cowork-smoke.js` V0750-VERSION pin
- `platform/test/run-helper-cases.js` HC-V0890/V0900/V0920/V0930/V0931 etc. version pins (bulk-replace)

Run preflight; expect GREEN. Run dogfood install (`node platform/install.js --vault . --auto-approve`). Verify materialized `.claude/skills/cowork/skills/gather-from-served-by/SKILL.md` contains the inline contract section; verify materialized `spice/cowork/helpers/write-atomic-note-helper.js` contains the three new guard error strings.

Commit: `chore(workshop): v0.97.4 S3 — version bumps + dogfood install`.

### S4 — cycle docs

- `Docs/plans/2026-06-10-v0.97.4-prose-invariant-write-guards-{design,plan,result}.md`
- Update `Docs/agent-guides/cycle-status.md` (live workshop version, mechanism + blueprint catalogue, in-flight queue)
- Append `Docs/cycle-history.md` with v0.97.3 entry (close prior cycle); v0.97.4 entry stays in cycle-status until next ship.

Commit: `docs(workshop): v0.97.4 S4 — design + plan + result + cycle-status`.

### S5 — push + tag + brew + deploy

1. `git push origin main`
2. `git tag v0.97.4 && git push origin v0.97.4`
3. Watch + merge brew tap PR: `gh pr list --repo willfell/homebrew-sauce --state open` + `gh pr merge --repo willfell/homebrew-sauce --squash --auto`
4. `brew update && brew upgrade sauce` locally → verify `sauce --version` reports 0.97.4
5. `sauce update --bump-pins` against reachable vaults (workshop + headspace)
6. Verify both vaults' `.claude/skills/cowork/skills/gather-from-served-by/SKILL.md` contains the new `Microscope output-shape contract (INLINE` section.

## Files touched

- `platform/blueprints/cowork/helpers/write-atomic-note-helper.js` (3 new write-guards)
- `platform/blueprints/cowork/skills/skills/gather-from-served-by/SKILL.md` (inline microscope output-shape contract)
- `platform/blueprints/cowork/content/data/orchestrator-instructions/{morning-briefing,midday-tripwire,eod-review,weekly-review,monthly-review}.md` (Step 8.5 guidance)
- `platform/blueprints/cowork/data/schemas/{6 cadence schemas}@1.0.0.json` (coverage_gap property)
- `platform/test/run-helper-cases.js` (23 new sub-asserts + version pin bumps)
- `platform/test/run-cowork-smoke.js` (V0750-VERSION pin bump)
- `package.json` + `platform/manifest.json` + `platform/blueprints/cowork/manifest.json` + `ranch/platform-subscription.json` (version bumps)

DO NOT touch `scheduled-job-contract.json` (contract_version stays 0.35.1).
