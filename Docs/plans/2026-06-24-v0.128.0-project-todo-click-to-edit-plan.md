---
cycle: v0.128.0
slug: project-todo-click-to-edit
date: 2026-06-24
status: plan
design: 2026-06-24-v0.128.0-project-todo-click-to-edit-design.md
---

# v0.128.0 — implementation plan

Stages numbered S0…S7. Each stage = one conventional-commit on the cycle branch `cycle/v0.128.0-project-todo-click-to-edit` (already created off `origin/main`).

## S1 — Land design + plan docs `[SERIAL]`

- [ ] Commit design.md + plan.md (this file).
- [ ] Commit message: `docs(plans): v0.128.0 — project-todo click-to-edit design + plan`.

## S2 — `task-interactions@0.2.0` extension `[SERIAL]`

- [ ] `platform/mechanisms/task-interactions/manifest.json` — bump `0.1.0 → 0.2.0`; append v0.2.0 description paragraph.

- [ ] `platform/mechanisms/task-interactions/task-interactions.js`:
  - Add `ownedTasksAnchor()` static + instance methods, mirroring `todayCaptureAnchor()` shape, returning `"<!-- OWNED_TASKS_MARKER -->"`.
  - Add `injectOwnedTasksMarker(body)` static + instance methods. Idempotent guard. Regex matcher for SectionLabel "Owned Tasks" with/without `top: true`. Insert marker after closing fence of that block. Returns body unchanged on no-anchor.
  - Extend `findTaskLines(content, sectionAnchor)`: accept `"ownedTasks"` as a third valid anchor string. Anchor = `ownedTasksAnchor()`. Terminates at next SectionLabel block or `## ` heading. Fence-aware (existing logic unchanged).
  - Update `appendTask` `project-todo` branch (§A.fix + §A.append):
    - Replace literal string needle with regex accepting both `"Owned Tasks"` and `"Owned Tasks", top: true`.
    - Marker-preferred path: try `injectOwnedTasksMarker(working)` first; insert task line immediately after marker. Fall back to v0.127.0 SectionLabel-fence-append behavior when marker injection fails (no Owned Tasks SectionLabel at all).
  - Ensure all new methods follow the instance-delegator pattern (FLN-todo-13 — landmine #2 of customjs static-on-instance rule).

- [ ] Workshop dogfood `node platform/install.js --vault . --auto-approve` exits 0.
- [ ] Commit: `feat(task-interactions): v0.2.0 — ownedTasksAnchor + injectOwnedTasksMarker + project-todo appendTask anchor loosen`.

## S3 — Extend `run-task-interactions.js` `[SERIAL after S2]`

- [ ] Add HC-V0128-TI-* cases:
  - `ownedTasksAnchor()` returns the literal sentinel.
  - `injectOwnedTasksMarker` — idempotency, fresh inject, no-anchor short-circuit, both SectionLabel forms accepted.
  - `findTaskLines(content, "ownedTasks")` — scope respected, terminates at next SectionLabel.
  - `appendTask` `project-todo` with marker present + without marker (injects first) + with `top: true` SectionLabel (regression coverage).

- [ ] Run `node platform/test/run-task-interactions.js` — expected PASS verdict (existing 112 + new ~15-20 = ~127-132).
- [ ] Commit: `test(task-interactions): v0.128.0 S3 — HC-V0128-TI-* covering ownedTasks anchor + appendTask project-todo regression`.

## S4 — `EditableTaskList` rename + sectionAnchor arg `[SERIAL after S3]`

- [ ] Rename `platform/blueprints/to-do/helpers/today-capture-editable-list.js` → `editable-task-list.js`. Use `git mv` to preserve history.

- [ ] In the renamed file:
  - Rename `class TodayCaptureEditableList` → `class EditableTaskList`.
  - Update `render(dv)` signature to `render(dv, opts = {})`. Extract `const sectionAnchor = (opts && opts.sectionAnchor) || "todayCapture";`.
  - Replace `customJS.TaskInteractions.findTaskLines(content, "todayCapture")` with `customJS.TaskInteractions.findTaskLines(content, sectionAnchor)`.
  - Append at end of file: `class TodayCaptureEditableList extends EditableTaskList {}` back-compat alias.

- [ ] `platform/blueprints/to-do/manifest.json`:
  - Bump `0.12.1 → 0.13.0`.
  - Append v0.13.0 description paragraph.
  - Add `"EditableTaskList"` to `customjs_classes[]`. KEEP `"TodayCaptureEditableList"` for back-compat.
  - Update `files[]` entry: source `helpers/editable-task-list.js`, dest `{{scripts_path}}/to-do/editable-task-list.js`. KEEP an entry for `today-capture-editable-list.js` until the source rename is propagated (verify: source file no longer exists at the old path → either remove the old files[] entry OR keep it pointing at the renamed file for materialization back-compat).

- [ ] `platform/blueprints/to-do/templates/Today To-Do.md` — rewrite the dataviewjs invocation to use the new class + explicit scope:
  ```
  await dv.view("{{views_path}}/customjs-guard", { class: "EditableTaskList", args: [{ sectionAnchor: "todayCapture" }] });
  ```

- [ ] Workshop dogfood exits 0.
- [ ] Commit: `feat(to-do): v0.13.0 — rename TodayCaptureEditableList → EditableTaskList; sectionAnchor arg generalizes the surface`.

## S5 — Project To-Do template + installer heal step 7 `[SERIAL after S4]`

- [ ] `platform/blueprints/to-do/templates/Project To-Do.md` — insert between Owned Tasks SectionLabel and From Meetings SectionLabel:
  ```
  
  <!-- OWNED_TASKS_MARKER -->
  
  ```dataviewjs
  await dv.view("{{views_path}}/customjs-guard", { class: "EditableTaskList", args: [{ sectionAnchor: "ownedTasks" }] });
  ```
  ```

- [ ] Check the entity-create registry path — if project-todo has an `inline_body` field in any blueprint's `new_entity_buttons[]`, mirror the marker + renderer there too. Search: `grep -n "project-todo\|project_todo" platform/blueprints/*/manifest.json | head`. Likely in `project` blueprint's `new_entity_buttons[]` extra_files for the project-todo materialization.

- [ ] `platform/install.js`:
  - Add file-scope constant `const OWNED_TASKS_MARKER = '<!-- OWNED_TASKS_MARKER -->';` next to existing markers.
  - In `_healNoteChromeBody`, add step 7 (project-todo): paired guards per design §D. Find Owned Tasks SectionLabel via regex (accepts both forms). Insert marker if absent. Insert renderer block if absent. Both guards independent.
  - In step 6 (to-do), add third guard (c): rewrite `class: "TodayCaptureEditableList"` invocations to `class: "EditableTaskList", args: [{ sectionAnchor: "todayCapture" }]` via bounded regex.
  - In `applyNoteChromeHeal` driver: extend allowed types array to include `"project-todo"`.
  - Update `module.exports` block to surface no new helpers (step 7 lives inside `_healNoteChromeBody` which is already exported per v0.127.0 S6).

- [ ] Workshop dogfood exits 0.
- [ ] Commit: `fix(installer,to-do): v0.128.0 — Project To-Do template + OWNED_TASKS_MARKER heal step 7; step 6 rewrites legacy class invocations`.

## S6 — Behavioral harness `run-v0128-owned-tasks-editable.js` `[SERIAL after S5]`

- [ ] Create harness mirroring `run-v0127-today-capture-editable-list.js` shape:
  - vm sandbox loads `editable-task-list.js`.
  - Stubs: `app.vault.read/getAbstractFileByPath`, `customJS.TaskInteractions.findTaskLines`, `customJS.ToDoCreateTask.open`, dv.container DOM stub, dv.current.
  - Cases:
    - HC-V0128-ETL-A: `EditableTaskList.render(dv, { sectionAnchor: "ownedTasks" })` renders one row per task in scope.
    - HC-V0128-ETL-B: `EditableTaskList.render(dv)` (no opts) defaults to `"todayCapture"` scope (back-compat).
    - HC-V0128-ETL-C: `TodayCaptureEditableList` instance behaves identically to `EditableTaskList` for `sectionAnchor: "todayCapture"`.
    - HC-V0128-ETL-D: clicking pencil invokes `ToDoCreateTask.open({ editExisting: ... })` with correct payload + filePath.
    - HC-V0128-ETL-E: `_healNoteChromeBody` step 7 fresh-inject — both marker + renderer added on a project-todo body that has neither.
    - HC-V0128-ETL-F: `_healNoteChromeBody` step 7 back-fill — body with marker only gets renderer added.
    - HC-V0128-ETL-G: `_healNoteChromeBody` step 7 idempotency — fully-healed body is byte-equal output.
    - HC-V0128-ETL-H: `_healNoteChromeBody` step 7 type-gating — type:to-do body is NOT touched by step 7 logic.
    - HC-V0128-ETL-I: `_healNoteChromeBody` step 6 rewrites `class: "TodayCaptureEditableList"` → `class: "EditableTaskList", args: [{ sectionAnchor: "todayCapture" }]`.

- [ ] Wire harness into `package.json` `release:preflight`: add `&& node platform/test/run-v0128-owned-tasks-editable.js` after `run-v01271-today-capture-renderer-heal.js`.

- [ ] Run new harness standalone — PASS.
- [ ] Run `run-task-interactions.js` — PASS (regression).
- [ ] Run `run-v0127-today-capture-editable-list.js` — PASS (back-compat alias holds).
- [ ] Workshop dogfood exits 0.
- [ ] Commit: `test(to-do): v0.128.0 S6 — HC-V0128-ETL-* covering EditableTaskList scope dispatch + heal step 7 + step 6 class rewrite`.

## S7 — Lockstep version sweep + preflight + push `[SERIAL after S6]`

- [ ] `platform/manifest.json` — workshop_version `0.127.1 → 0.128.0`; mechanism `task-interactions` pin `0.1.0 → 0.2.0`; blueprint `to-do` pin `0.12.1 → 0.13.0`.

- [ ] `package.json` — `"version": "0.127.1"` → `"0.128.0"`.

- [ ] `ranch/platform-subscription.json` + `platform/test/seed-vault/ranch/platform-subscription.json` — lockstep workshop_version + pins.

- [ ] HC-V0128-VERSION-* sweep in `platform/test/run-helper-cases.js`:
  - Widen workshop_version assertions to `/^0\.128\.\d+$/` (PATCH-tolerant per landmine #16 v0.127.1 lesson).
  - Update `task-interactions` mechanism pin assertion to `"0.2.0"` or `/^0\.2\.\d+$/`.
  - Update `to-do` blueprint pin assertion to `"0.13.0"` or `/^0\.13\.\d+$/`.

- [ ] `npm run release:preflight` MUST exit 0.
- [ ] `node platform/install.js --vault . --auto-approve` MUST exit 0 idempotent.
- [ ] Commit: `chore(release): v0.128.0 — workshop + task-interactions 0.2.0 + to-do 0.13.0; HC version sweep + lockstep subscriptions`.

- [ ] Push: `git push -u origin cycle/v0.128.0-project-todo-click-to-edit`.
- [ ] Report PR URL: `https://github.com/willfell/sauce/pull/new/cycle/v0.128.0-project-todo-click-to-edit`. STOP — do NOT tag.

## Open issues during execution

- `entity-create` registry path may need mirroring — check during S5.
- Class rename collision in `customjs_classes[]` — both names registered for back-compat; later cycle retires the alias.
- Workshop seed vault has no project-todo notes (verify by ls `platform/test/seed-vault/spice/projects/*/` ); if so, step 7 dogfood verification needs the harness path (not the workshop self-install) — same posture as v0.127.0's note_chrome_heal step which reported `healed 0` for empty seed surfaces.
- v0.127.0 S5 surfaced a customjs static-on-instance trap at S9 preflight time — keep the instance-delegator pattern in mind when extending `TaskInteractions` in S2.
