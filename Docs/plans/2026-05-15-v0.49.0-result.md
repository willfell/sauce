---
type: cycle-result
cycle: v0.49.0
title: L2 listener registration + BUG-7 + BUG-8
status: closed
date: 2026-05-16
tag: v0.49.0
plan: Docs/plans/2026-05-15-v0.49.0-plan.md
design: Docs/plans/2026-05-15-v0.49.0-design.md
revalidation: Docs/plans/2026-05-15-v0.49.0-headspace-revalidation.md
predecessor_result: Docs/plans/2026-05-15-v0.48.0-result.md
---

# v0.49.0 — L2 listener registration + BUG-7 + BUG-8 (CLOSED 2026-05-16)

## Summary

v0.49.0 resolves v0.48.0's known issue (BUG-5 listener never subscribed at headspace because Templater `startup_templates[]` registration didn't fire at boot) by swapping to L2 (customjs `startupScriptNames[]`). The v0.48.0 `ProjectTaskCreateListener` class is unchanged — only the registration vector changes. v0.48.0's Templater wiring stays as belt-and-suspenders backstop (idempotent init via `_initialized` flag). Bundled BUG-7 fix (Create Board AccentButton gate widened from task-hub-only to task-hub OR task-note context) + BUG-8 fix (entity-create sentinel relocated from outside-block HTML comment to inside-block JS comment across all 7 entity-create-using hubs; `injectAccentButtonBlock` becomes verify-only per Choice A).

Workshop preflight green across 15 harnesses with +8 sub-asserts vs v0.48.0 baseline. Brew formula auto-bumped via release.yml chain; tap PR #27 merged 2026-05-16T06:47:35Z; brew upgrade 0.48.0 → 0.49.0 verified. Headspace reinstall (via brew sauce v0.49.0) clean — 97 history entries, 8 content overwrites; customjs/data.json `startupScriptNames[]` contains `"ProjectTaskCreateListenerInit"`; all 7 hubs have inside-block sentinel; audit zero findings.

**Manual 8-checkpoint smoke at headspace deferred-to-day-of-use** per autonomous-run directive. CLI-level evidence is comprehensive (L2 plumbing verified end-to-end at the consumer); risk profile for the manual smoke is very low (customjs's startupScriptNames is the SAME plugin that hosts the listener class — if startupScriptNames doesn't fire, no other customjs class would work at headspace).

## Stage commits

| Stage | Commit | Subject |
|---|---|---|
| (pre-S0) | `9095f33` | docs(plans): v0.49.0 design — L2 listener registration + BUG-7 + BUG-8 |
| (pre-S0) | `0bb80ce` | docs(plans): v0.49.0 implementation plan |
| S0 | `1393cd3` | docs(plans): S0 verification log (CLI parts pass; manual deferred) |
| S1 | `b652cb6` | feat(project): NEW init script + customjs_startup_scripts manifest field |
| S2 | `24320b5` | feat(installer): applyCustomJsStartupScripts helper |
| S3 | `803159f` | fix(project): BUG-7 widen Create Board gate to task-note context |
| S4 | `dd688b3` | feat(installer): injectAccentButtonBlock becomes verify-only |
| S4b | `a517891` | fix(installer): pushErr step name disambiguates from pushWarn (mid-stage S4 code-review fix) |
| S5 | `69bddf8` | feat(blueprints): BUG-8 sentinel relocation across 7 hub files + smoke regex update |
| S6 | `97a8ecb` | test(v0.49.0): PCSI + CSS + KMC + smoke sub-asserts |
| S7 | `8c37ee3` | chore(release): catalogue + subscription lockstep + workshop dogfood |
| S8 | tag `v0.49.0` | Annotated release tag pushed; release.yml preflight → bump-tap chain auto-bumped Formula/sauce.rb |
| S8b | tap PR #27 merged | brew upgrade 0.48.0 → 0.49.0 |
| S9 | `980fa8c` | docs(plans): headspace re-validation (CLI-verified; manual smoke pending) |

## Plan vs reality

### S0 verification gate retrospect

Per autonomous-run directive: scriptable parts (1.1 customjs data.json shape, 1.2 customjs/data.json in landmine #12 allowlist) retired headlessly; manual Cmd-R reload + console-log smoke (Step 1.4) deferred to S9. Risk-profile reasoning: customjs's startupScriptNames is qualitatively different from v0.48.0's Templater startup_templates because customjs is the SAME plugin that hosts ProjectTaskCreateListener — if startupScriptNames doesn't fire, NO other customjs class would work at headspace. Risk: very low.

### S4 mid-stage correction (S4b)

S4 code reviewer flagged: `pushErr` inside the new verify-only `injectAccentButtonBlock` reused `step: "entity_create_block_missing"` (same as `pushWarn`), but the error case (target_path missing / read failure) is semantically I/O-error, not block-missing. Fixed via S4b commit `a517891`: `step: "entity_create_block_error"` distinguishes the two states by step name alone.

### S5 hub source files — all Flavor B

Of 7 entity-create-using hubs, ALL turned out to be Flavor B (standalone outside-block marker; v0.46.0 installer was supposed to inject the dataviewjs block at install time). The S5 implementer correctly added the full dataviewjs block (with inside-block sentinel) in source for each.

### Harness pin lockstep (FLN-v47-f pattern at S7)

S7 catalogue bumps surfaced 2 harness pin failures (matches v0.47.0's FLN-f lesson that Icons-style consumer migrations need 1-3 harness pin updates):
- `run-helper-cases.js` SHC-S1: scratch manifest.version pinned 0.3.0 → 0.3.1
- `run-entity-create.js` EC-2: entity-create manifest.version pinned 0.2.1 → 0.3.0

### CSS-1..3 stubbed

`applyCustomJsStartupScripts` unit cases CSS-1..3 in run-helper-cases.js are STUBBED (label includes "STUB — full adapter-stub harness deferred to follow-up FLN-v49"). Full extraction-pattern adapter stub is non-trivial; captured as FLN-v49-N for a follow-up cycle.

## Versions

| Component | Before | After |
|---|---|---|
| `workshop_version` | 0.48.0 | **0.49.0** (MINOR) |
| `package.json` | 0.48.0 | **0.49.0** |
| `project` blueprint | 1.8.0 | **1.9.0** (MINOR — NEW ProjectTaskCreateListenerInit + customjs_startup_scripts field; v0.48.0 Templater wiring retained as backstop) |
| `entity-create` mechanism | 0.2.1 | **0.3.0** (MINOR — sentinel format change from outside-block HTML comment to inside-block JS comment + verify-only injectAccentButtonBlock) |
| `meetings` blueprint | 0.5.0 | **0.5.1** (PATCH — Meeting Hub.md sentinel relocation) |
| `people` blueprint | 0.2.1 | **0.2.2** (PATCH — People.md sentinel relocation) |
| `scratch` blueprint | 0.3.0 | **0.3.1** (PATCH — Scratch Day Hub.md sentinel relocation) |
| `finance` blueprint | 0.3.0 | **0.3.1** (PATCH — Budgets/Paychecks/Invoices.md sentinel relocation) |

Mechanisms catalogue unchanged at 12 entries. Blueprints catalogue unchanged at 11 entries.

## Whole-suite preflight (15 harnesses, all green)

| Harness | Δ vs v0.48.0 | Total |
|---|---:|---:|
| run-helper-cases.js | +7 (PCSI-1..3 + KMC-1 + CSS-1..3 STUB) | 696 (was 687 after v0.48.0; PCSI-3 is 3 sub-asserts: 3a/3b/3c) |
| run-integration-smoke.js | +1 (smoke-prj-customjs-startup) | 31 (was 30) |
| run-entity-create.js | 0 (EC-2 version pin lockstep updated 0.2.1 → 0.3.0) | 38 |
| Others | unchanged | — |

Whole-suite total: **+8 sub-asserts** vs v0.48.0 baseline (PCSI-1 + PCSI-2 + PCSI-3a + PCSI-3b + PCSI-3c + KMC-1 + smoke-prj-customjs-startup + 3 CSS stubs = 10 raw, of which 3 CSS are STUBBED so 7 load-bearing + 1 smoke).

## NEW FIX-LATER-NOTEs

4 FIX-LATER items captured during S1-S6 reviews:

- **FLN-v49-1 (Minor):** schema comment needed for `customjs_startup_scripts[]` (stores class names) vs `templater_startup_templates[]` (stores paths). Parallel arrays with different value semantics; brief inline comment in manifest or `how.md` would prevent future installer-author confusion. (S1 code review)
- **FLN-v49-2 (Minor cosmetic):** `files[]` entry for new init script doesn't trail-pad the `"dest"` column with the rest of the codebase's manual alignment. (S1 code review)
- **FLN-v49-3 (Minor):** Line-number cross-references in helper comments drift as install.js grows (e.g., the new `applyCustomJsStartupScripts` references `install.js:3938`/`3920` which will shift). Future cleanup: grep-friendly anchor comments instead of line numbers. (S2 code review)
- **FLN-v49-4 (Minor):** `smoke-ec-markers-present-at-hub-paths` regex could theoretically match a dataviewjs block containing TWO different entity-create id sentinels — zero false-positive risk in practice given 1:1 hub-to-id invariant, but worth a comment. (S5 review)
- **FLN-v49-CSS (Deferred):** CSS-1..3 unit cases in run-helper-cases.js are STUBBED bodies. Full adapter-stub harness extraction (matching the PTCL-1 pattern) deferred to follow-up. Cases pass trivially with explicit "STUB" label in console output. (S6 plan)

All 11 FLN-v48-N items from v0.48.0 remain queued; FLN-v48-8 (whitespace-only post-substitution gap) was PARTIALLY addressed in v0.49.0's `applyCustomJsStartupScripts` (which includes the `.trim()` check) but `applyTemplaterStartupTemplates` + `applyTemplaterFolderTemplates` still have the gap.

## Acceptance gates summary

### Workshop preflight ✅

All 15 harnesses pass; +8 sub-asserts vs v0.48.0 baseline. Workshop dogfood install green via in-tree CLI. `sauce audit --entity-create` zero findings.

### Headspace re-validation (CLI-level) ✅

After brew upgrade + subscription bump + reinstall:
- 97 history entries, 8 content overwrites, clean run
- ranch/scripts/project/project-task-create-listener-init.js materialized (1048 bytes)
- .obsidian/plugins/customjs/data.json startupScriptNames[] contains "ProjectTaskCreateListenerInit"
- installed.json reflects all bumped versions
- All 7 hubs swapped to inside-block sentinel; no outside-block HTML markers remain
- sauce audit --entity-create zero findings (5 aligned, 2 unverifiable pre-existing v0.46.x FLN)

### Headspace manual smoke (8 checkpoints) ⏭️ deferred-to-day-of-use

Per autonomous-run directive: cycle closes on CLI-level evidence. See `Docs/plans/2026-05-15-v0.49.0-headspace-revalidation.md` for the PENDING table the user fills in at day-of-use. If any checkpoint surfaces a regression, capture in `Docs/prompts/2026-05-16-v0.50.0-<brief>-handoff.md`.

## Links

- [Design](2026-05-15-v0.49.0-design.md)
- [Plan](2026-05-15-v0.49.0-plan.md)
- [Headspace re-validation note](2026-05-15-v0.49.0-headspace-revalidation.md)
- [v0.48.0 result (predecessor — closed-with-known-issue)](2026-05-15-v0.48.0-result.md)
- Tag: [v0.49.0](https://github.com/willfell/sauce/releases/tag/v0.49.0)
