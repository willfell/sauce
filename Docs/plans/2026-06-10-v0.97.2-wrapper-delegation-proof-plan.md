---
cycle_arc: v0.97.2 wrapper-delegation-proof
kind: plan
status: SHIPPED 2026-06-10
companion: Docs/plans/2026-06-10-v0.97.2-wrapper-delegation-proof-design.md
---

# v0.97.2 wrapper-delegation-proof — plan

## Stage breakdown

5 stages, all on `main` (workshop convention: direct-push, no PRs).

- **S1** — RED: add ~25 HC sub-asserts (HC-V0972-AD/IW/DP) to `platform/test/run-helper-cases.js`. Run helper-cases; expect ~25 RED. Commit.
- **S2** — implement: anti-delegation directive in helper + inline write contract in 5 orchestrator-instructions files + PRELUDE date fix in helper. Run all 6 test suites; expect ALL GREEN. Reroute 5+1 pre-existing legacy delegation-pattern asserts. Commit.
- **S3** — version + dogfood: bump workshop 0.97.1 → 0.97.2, cowork 0.35.1 → 0.35.2, contract_version UNCHANGED. Pin sweep across run-helper-cases + run-bootstrap + run-cowork-smoke. Run preflight + dogfood install. Commit.
- **S4** — cycle docs: design + plan + result + cycle-status + cycle-history. Commit.
- **S5** — push + tag + brew + deploy. User-approved.

## Stage S1 — RED

### S1.1 Add HC suite

Insert after the HC-V0971-DEPR-3 case in `platform/test/run-helper-cases.js`, before the final summary block. Re-use the existing v0.97.0 Rail T fixture loaders (`_loadSjHelperT`, `_loadSjContractT`, `_baseEngagementT`, `_basePrefsT`, `SOURCE_ROOT_T`).

HC suite layout:

| Group | Cases | Asserts | What it covers |
|---|---|---|---|
| AD (anti-delegation) | HC-V0972-AD-1..7 | 7 | Directive present per cadence (6) + appears BEFORE PRELUDE (1) |
| IW (inline write) | HC-V0972-IW-1..15 | 15 | Inline contract marker + old hook absent (5) + path template per cadence (5) + FORBIDDEN keys per key (5) |
| DP (date PRELUDE) | HC-V0972-DP-1..4 | 4 | No hardcoded weekday/month examples + references `date` command + verbatim "do NOT use any example value" |

Total: 26 new sub-asserts (target ~25 from prompt; mild overshoot for IW-6..10 which split the path-template check by cadence).

### S1.2 Run helper-cases

Baseline: 2230 / 0. After S1: 2233 / 23 (3 IW-6..8 already-GREEN; 23 RED).

### S1.3 Commit

```
test(cowork): v0.97.2 S1 — HC-V0972-AD/IW/DP RED (~25 new sub-asserts)
```

Staged explicitly: `git add platform/test/run-helper-cases.js`.

## Stage S2 — implement

### S2.1 Helper change (knob A + knob C)

`platform/blueprints/cowork/helpers/compose-scheduled-job-wrappers-helper.js`:

- Inject `const antiDelegation = "ANTI-DELEGATION (NON-NEGOTIABLE): ..."` just before the existing PRELUDE block. Wrap into the final `wrapper` template so it lands as the very first non-blank content of the composed prompt.
- Replace the PRELUDE block (~line 594-601) with a Bash-driven PRELUDE that runs `date '+%Y-%m-%d'` etc. in the engagement's timezone + carries the verbatim instruction `do NOT use any example value`.

### S2.2 Orchestrator-instructions changes (knob B × 5)

For each of `morning-briefing.md`, `midday-tripwire.md`, `eod-review.md`, `weekly-review.md`, `monthly-review.md` in `platform/blueprints/cowork/content/data/orchestrator-instructions/`:

- Replace the existing 3-substep Step 7 (`7a` shared.frontmatter_base + `7b` shared.dataviewjs_block + `7c` READ write-run-note-<cadence> SKILL.md) with the new 5-substep inline contract: 7a path / 7b frontmatter YAML / 7c DataviewJS / 7d callout body skeleton / 7e obsidian_put_content invocation.
- Per-cadence flavor adjustments: midday-tripwire frontmatter adds `severity: <warn | alert>`; weekly + monthly note that `day:` is the week/month-anchor day (no separate `week:`/`month:` keys in this writer contract); morning-briefing adds a lens_shift-variant parenthetical (preserves HC-V0951-K3-D's cold-slug assertion).

### S2.3 Reroute pre-existing tests

The Step 7 retirement breaks 5+1 legacy delegation-pattern asserts:

- `HC-V0920-ORCH-{MB,MT,EOD,WR,MR}-A4` in `run-helper-cases.js`: shift from `body: body_md, sidecar_json: sidecar_json` regex to `INLINE CONTRACT — do not delegate` + `obsidian_put_content` markers.
- `HC-V0970-T-3` allowlist in `run-helper-cases.js`: add `{{$title_template_resolved}}` as a fire-time-resolved token.
- `S6` (morning-briefing dispatches write-run-note-morning-briefing) in `run-cowork-smoke.js`: shift to `INLINE CONTRACT — do not delegate` marker.
- `v065-S3` 5-cadence loop in `run-cowork-smoke.js`: shift each orchestrator's `expect: "write-run-note-<cadence>"` to `expect: "INLINE CONTRACT — do not delegate"`. Forbid list (`write-callout-*` / `write-summary-*`) preserved.

### S2.4 Run all 6 suites

| Suite | Expected |
|---|---|
| helper-cases | 2256 / 0 (+26 from S1 baseline) |
| cowork-smoke | 954 / 0 (unchanged net; 6 reroutes) |
| claude-surface | 214 / 0 |
| integration-smoke | 36 / 0 |
| cli | 132 / 0 |
| bootstrap | 85 / 0 |

### S2.5 Commit

```
feat(cowork): v0.97.2 S2 — wrapper-delegation-proof (anti-delegation +
inline write contract + PRELUDE date fix)
```

Staged explicitly with the 6 implementation files + 2 test reroutes.

## Stage S3 — version + dogfood

### S3.1 Bump

- `package.json` 0.97.1 → 0.97.2
- `platform/manifest.json` workshop_version + cowork blueprint pin
- `platform/blueprints/cowork/manifest.json` 0.35.1 → 0.35.2
- `ranch/platform-subscription.json` workshop_version + cowork pin
- **`scheduled-job-contract.json` contract_version UNCHANGED at 0.35.1.**

### S3.2 Per-cycle VERSION-pin sweep (landmine #16)

- run-helper-cases.js: 14 `0.97.1`/`0.35.1` assertions → `0.97.2`/`0.35.2`
- run-bootstrap.js: 12 `workshop_version: "0.97.1"` → `"0.97.2"`
- run-cowork-smoke.js: 1 `cowork manifest.version === "0.35.1"` → `"0.35.2"`
- TOTAL: 27 pin bumps.

Preserve at 0.35.1:
- `HC-V0970-T-9-contract-version` (line 14889-14890): contract_version assertion.
- `HC-V0971-RC-8` (line 15722-15723): contract_version assertion.

Preserve at 0.97.1:
- `HC-V0971-DEPR-1` (line 15788-15799): asserts cmd-reconcile-cowork.js content includes the historical `[DEPRECATED v0.97.1]` notice.
- Test description string at HC-V0972-IW-N (line 15948): historical context.
- Comments/headers referencing v0.97.1 cycle.

### S3.3 Preflight

`npm run release:preflight` → ALL GREEN. `version-sync ok: 0.97.2`.

### S3.4 Dogfood install

`node platform/install.js --vault . --auto-approve` → clean exit 0.

Expected materializations (6 content overwrites):
- `spice/cowork/helpers/compose-scheduled-job-wrappers-helper.js`
- `spice/cowork/data/orchestrator-instructions/morning-briefing.md`
- `spice/cowork/data/orchestrator-instructions/midday-tripwire.md`
- `spice/cowork/data/orchestrator-instructions/eod-review.md`
- `spice/cowork/data/orchestrator-instructions/weekly-review.md`
- `spice/cowork/data/orchestrator-instructions/monthly-review.md`

### S3.5 Commit

```
chore(workshop): v0.97.2 S3 — version bumps + dogfood install
```

## Stage S4 — cycle docs

### S4.1 New plan docs

- `Docs/plans/2026-06-10-v0.97.2-wrapper-delegation-proof-design.md`
- `Docs/plans/2026-06-10-v0.97.2-wrapper-delegation-proof-plan.md` (this file)
- `Docs/plans/2026-06-10-v0.97.2-wrapper-delegation-proof-result.md`

### S4.2 Update cycle-status

`Docs/agent-guides/cycle-status.md`:
- Top "Current" line shifts to v0.97.2.
- v0.97.1 demoted to "previous".

### S4.3 Append cycle-history

`Docs/cycle-history.md`: new v0.97.2 entry inserted above v0.97.1.

### S4.4 Commit

```
docs(workshop): v0.97.2 S4 — design + plan + result + cycle-status
```

## Stage S5 — push + tag + brew + deploy

User-approved per prompt ("go through, deploy, push your changes").

1. `git push origin main`
2. `git tag v0.97.2 && git push origin v0.97.2`
3. Watch for brew tap PR at `willfell/homebrew-sauce`. Once it appears with title containing `v0.97.2`, merge via `gh pr merge --repo willfell/homebrew-sauce --squash --auto`.
4. `brew update && brew upgrade sauce` on this machine.
5. Both vaults' `sauce update`:
   - Workshop: already updated by S3 dogfood.
   - Headspace: `cd /Users/willfellhoelter/.../headspace-sauce && sauce update`.
   - Accuris: pending user brew upgrade on the OTHER machine + sauce update there.
6. Verify materialized morning-briefing.md in each vault contains "INLINE CONTRACT — do not delegate" + helper has new PRELUDE shape.
