# v0.61.0 — FA-8 — Implementation Plan

**Reference:** `Docs/plans/2026-05-18-v0.61.0-fa-8-design.md`
**Stages:** 8 stages, ~9-12 commits, ~3-5 hours estimated.

## S0. Verification gate (~5 min)

Sanity-check workshop baseline.

- [ ] `git -C ~/projects/repos/sauce status` clean on `main` (one expected: `.claude/scheduled_tasks.lock` untracked)
- [ ] `cat platform/manifest.json | grep workshop_version` → `"0.60.0"`
- [ ] `cat package.json | grep '"version"'` → `"0.60.0"`
- [ ] `npm run release:preflight` GREEN (15 harnesses)
- [ ] FA card `FA-8 · Backlink panels.md` frontmatter shows `status: in_progress`

If any check fails → fix or escalate before proceeding.

## S1. Scaffold mechanism + write the class (~45 min)

**Deliverables:** `platform/mechanisms/backlink-panel/manifest.json` + `backlink-panel.js`.

- [ ] Create `platform/mechanisms/backlink-panel/manifest.json` per design §Architecture
- [ ] Create `platform/mechanisms/backlink-panel/backlink-panel.js` — ~200 LOC CustomJS class
  - [ ] Constructor-free class (mirrors PeopleRendering / BeaconCards pattern)
  - [ ] `_ENTITY_TYPE_TO_KEY` map literal (6 entries)
  - [ ] `render(dv, opts)` entry point — validates entityType, dispatches by groupBy
  - [ ] `_reverseQuery(dv, entityType)` — Dataview `.where()` chain
  - [ ] `_renderEmpty(dv)` — italic dimmed "No mentions yet"
  - [ ] `_renderFlat(dv, pages)` — single BeaconCards delegation
  - [ ] `_renderGroupedByType(dv, pages)` — partitions by `p.type`, emits H4 per group
  - [ ] `_renderGroupedByMonth(dv, pages)` — partitions by `created_at` YYYY-MM, emits H4 per month
  - [ ] Notice on invalid entityType
  - [ ] Notice on BeaconCards unavailable
  - [ ] Header comment with API summary + landmine cross-refs (#1 customjs-guard, #11 module-directory invariant)
- [ ] Commit: `feat(backlink-panel): v0.61.0 S1 — NEW backlink-panel@0.1.0 mechanism scaffold + class`

## S2. Test harness `run-backlink-panel.js` (~45 min)

**Deliverables:** `platform/test/run-backlink-panel.js` (~250 LOC, 16 sub-asserts).

- [ ] Create `platform/test/run-backlink-panel.js`
  - [ ] Boilerplate matching `run-helper-cases.js` style (assertEq + assertTrue helpers; pass/fail counters)
  - [ ] WORKSHOP path resolution
  - [ ] Pass 1: read + parse `manifest.json`, run BP-1..3
  - [ ] Pass 2: read `backlink-panel.js` source as string, lint via Function ctor + grep for landmark literals (BP-4..16)
  - [ ] Summary block: `console.log("backlink-panel: <pass>/<total>")` + `process.exit(fail ? 1 : 0)`
- [ ] `package.json` `release:preflight` script — append `node platform/test/run-backlink-panel.js`
- [ ] Add `test:backlink-panel` npm script (`node platform/test/run-backlink-panel.js`)
- [ ] Run `npm run test:backlink-panel` — must GREEN against the S1 mechanism source
- [ ] Commit: `test(backlink-panel): v0.61.0 S2 — NEW run-backlink-panel.js (16 asserts)`

## S3. Workshop manifest catalogue (~15 min)

**Deliverables:** `platform/manifest.json` mechanisms[] += backlink-panel.

- [ ] Edit `platform/manifest.json`:
  - [ ] Find the `mechanisms[]` block (12 entries currently)
  - [ ] Insert a new entry `{ "name": "backlink-panel", "version": "0.1.0", "path": "mechanisms/backlink-panel" }` in alphabetical-ish position (after `audit`, before `cards`)
- [ ] Verify total mechanisms count is now 13
- [ ] Commit: `feat(manifest): v0.61.0 S3 — register backlink-panel@0.1.0 in catalogue`

## S4. Blueprint template embeds + depends_on bumps (~60 min)

Six edits across 5 blueprints. Each blueprint commit is independent — one commit per blueprint for clean revert ergonomics.

### S4a. teams blueprint

- [ ] Edit `platform/blueprints/teams/manifest.json`:
  - [ ] `version: "0.2.0"` → `"0.3.0"`
  - [ ] `description:` append `" v0.3.0 MINOR (v0.61.0 FA-8): depends_on backlink-panel >=0.1.0; Template, Team.md grows ## Mentions H2 below TeamPageCards block via BacklinkPanel.render(dv, {entityType: 'team'})."`
  - [ ] `depends_on[]` add `{ "name": "backlink-panel", "range": ">=0.1.0" }`
- [ ] Edit `platform/blueprints/teams/templates/Template, Team.md`:
  - [ ] Append (after the TeamPageCards dataviewjs block):
    ```markdown

    ## Mentions

    ```dataviewjs
    await dv.view("ranch/views/customjs-guard", {
      class: "BacklinkPanel",
      method: "render",
      args: [dv, { entityType: "team" }]
    });
    ```
    ```
- [ ] Commit: `feat(teams): v0.61.0 S4a — teams@0.3.0 — Mentions panel via BacklinkPanel`

### S4b. products blueprint

- [ ] Edit `platform/blueprints/products/manifest.json` — analogous (version 0.2.0 → 0.3.0; depends_on + description)
- [ ] Edit `platform/blueprints/products/templates/Template, Product.md` — append Mentions block (entityType: "product")
- [ ] Commit: `feat(products): v0.61.0 S4b — products@0.3.0 — Mentions panel via BacklinkPanel`

### S4c. trips blueprint

- [ ] Edit `platform/blueprints/trips/manifest.json` — version 0.2.0 → 0.3.0; depends_on + description
- [ ] Edit `platform/blueprints/trips/templates/Trip Atlas.md` — append Mentions block (entityType: "trip")
- [ ] Commit: `feat(trips): v0.61.0 S4c — trips@0.3.0 — Mentions panel via BacklinkPanel`

### S4d. people blueprint

- [ ] Edit `platform/blueprints/people/manifest.json`:
  - [ ] version 0.3.0 → 0.4.0
  - [ ] depends_on + description
  - [ ] `new_entity_buttons[0].inline_body` — append `\n\n## Mentions\n\`\`\`dataviewjs\nawait dv.view(\"ranch/views/customjs-guard\", { class: \"BacklinkPanel\", method: \"render\", args: [dv, { entityType: \"person\" }] });\n\`\`\`\n`
- [ ] Edit `platform/blueprints/people/templates/Template, People.md`:
  - [ ] Insert the Mentions block AFTER the "## Daily Mentions" block, BEFORE the `<%* await tp.file.move(...) %>` directive line.
- [ ] Commit: `feat(people): v0.61.0 S4d — people@0.4.0 — Mentions panel (Template + inline_body)`

### S4e. project blueprint

- [ ] Edit `platform/blueprints/project/manifest.json`:
  - [ ] version 1.13.6 → 1.14.0
  - [ ] depends_on + description
- [ ] Edit `platform/blueprints/project/templates/Project.md`:
  - [ ] Insert Mentions block BEFORE the existing `> [!example]- Project Notes & Referenced By` callout block.
  - [ ] Mentions block uses `entityType: "project", groupBy: "type"` per design table.
- [ ] Commit: `feat(project): v0.61.0 S4e — project@1.14.0 — Mentions panel (entityType=project, groupBy=type)`

## S5. Subscription lockstep + workshop dogfood reinstall (~15 min)

- [ ] Edit `ranch/platform-subscription.json` (workshop's own dogfood subscription):
  - [ ] `workshop_version: "0.61.0"`
  - [ ] mechanisms[] += backlink-panel@0.1.0
  - [ ] blueprints[] bumps: teams 0.3.0, products 0.3.0, trips 0.3.0, people 0.4.0, project 1.14.0
- [ ] Run `node bin/sauce install --vault .` (workshop self-install)
- [ ] Verify installer history shows:
  - [ ] backlink-panel mechanism materialized at `ranch/scripts/backlink-panel/backlink-panel.js`
  - [ ] 5 blueprint template updates (one per blueprint)
  - [ ] Zero errors
- [ ] Commit: `chore(workshop): v0.61.0 S5 — subscription lockstep + dogfood reinstall`

## S6. `run-helper-cases.js` +3 lint asserts (~15 min)

- [ ] Edit `platform/test/run-helper-cases.js`:
  - [ ] Add a new case `caseBackLinkPanelLint` (or extend an existing lint block) with 3 asserts: BLP-LINT-1..3 per design
  - [ ] Wire into the harness's run sequence
- [ ] Run `npm run test:helpers` — GREEN
- [ ] Commit: `test(helper-cases): v0.61.0 S6 — +3 BLP-LINT asserts for backlink-panel class`

## S7. Version bump + sync check (~10 min)

- [ ] Edit `platform/manifest.json`:
  - [ ] `workshop_version: "0.60.0"` → `"0.61.0"`
  - [ ] `date: "2026-05-18"` (verify already set)
- [ ] Edit `package.json`:
  - [ ] `version: "0.60.0"` → `"0.61.0"`
- [ ] Run `node scripts/check-version-sync.js` — exit 0
- [ ] Commit: `chore(release): v0.61.0 — workshop_version bump + lockstep`

## S8. Preflight + tag + push (~15 min)

- [ ] `npm run release:preflight` — ALL GREEN
  - 15 prior harnesses + run-backlink-panel + version-sync check
  - Expected: ~17 GREEN
- [ ] If anything red: fix, commit, re-run. NO tag until preflight is fully green.
- [ ] `git push origin main`
- [ ] `git tag v0.61.0`
- [ ] `git push origin v0.61.0`
- [ ] Confirm release.yml workflow fires (push to tag triggers homebrew tap auto-bump PR) — capture PR number for handoff.

## Out-of-band: defer to user-driven deploy

Tap merge + `brew upgrade sauce` + 4-vault reinstall + manual headspace smoke fire OUTSIDE this round per round-19 precedent. Documented in handoff so user can run when ready.

## Risk gates (proceed only if)

- S1 class throws no syntax errors when loaded via Function ctor (S2 RED → GREEN proves this)
- S4 template edits don't break dataviewjs parsing (workshop dogfood reinstall via S5 catches this)
- Preflight stays green through S6 incremental adds
- Version-sync check passes after S7

## Rollback plan

If S8 preflight fails after the tag:
- Don't push the tag remotely. `git tag -d v0.61.0` locally.
- Diagnose. Fix. New commit. Re-tag with same name.

If post-tag breakage discovered in headspace smoke:
- User reports → file FLN against v0.61.0 result doc → v0.61.1 follow-up cycle with the fix. Tag history preserved.
