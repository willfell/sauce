---
date: 2026-05-05
phase: design
status: frozen
target_cycle: v0.20.0
cycle_codename: docs-polish
predecessors:
  - Docs/prompts/2026-05-05-post-v0.19.0-next-cycle-handoff.md
  - Docs/plans/2026-05-05-v0.19.0-styling-mechanism-result.md
successor_plan: Docs/plans/2026-05-05-v0.20.0-docs-polish-plan.md
target_artifact: Docs/plans/2026-05-05-v0.20.0-docs-polish-result.md
workshop_version_bump: 0.19.0 → 0.20.0 MINOR
---

# v0.20.0 Design — docs polish + T4.0 harness fix + landmine #16 + trailing-whitespace lint

> [!abstract] Cycle goal
> Close 12 cycles of compounding maintenance debt in a single PATCH-shape MINOR cycle: fix the 12-cycles-old `run-renderer.js` no-selector fatal; lift 11 cycles of load-bearing lessons (v0.11.0–v0.19.0) into `Docs/how.md`; promote the in-cycle re-process bump rule to numbered landmine **#16**; add a trailing-whitespace lint for blueprint template bodies (carry from v0.18.1 lesson 2); clean up `blueprints i want.md` of items that landed in v0.18.1 / v0.18.2 / v0.19.0. Pure docs + harness; zero installer / mechanism / blueprint changes; manual-smoke-free.

---

## Section 1 — Cycle scope

**Workshop bump:** `0.19.0 → 0.20.0` MINOR.

**Why MINOR not PATCH:** new landmine, new harness behavior (lint), substantive 11-cycle docs codification. Matches the v0.13.0 / v0.14.0 docs-+-harness-substantive precedent.

**Deliverables:**

1. **T4.0 fatal fix** — drop `fs.readFileSync` of deleted `kanban-board.md` in `platform/test/run-renderer.js`; inline a synthetic kanban body string. `node platform/test/run-renderer.js` (no selector) exits 0 for the first time since v0.2.0.
2. **Trailing-whitespace lint** in `platform/test/run-helper-cases.js` — iterate `platform/blueprints/*/*.md`, assert no `[ \t]+$` on any line, fail-loud with `<file>:<line>` on violation. ~19 template files → +N sub-asserts (final N counted at run-time).
3. **Landmine #16 promotion** — codify in-cycle re-process bump rule. Quote `platform/install.js:111` short-circuit literally. Cite 5-data-point precedent (v0.6.0 / v0.17.0 / v0.18.0 / v0.18.1 / v0.19.0).
4. **`Docs/how.md`** "Gotchas / Lessons learned (v0.11.0–v0.19.0)" section — 10 load-bearing entries with footer pointer to per-cycle result writeups.
5. **`blueprints i want.md`** cleanup — delete styling section (DONE in v0.19.0), delete "new meeting button parity" line (SATISFIED at v0.18.1; verified — NewMeetingButton already uses overlay-modal + `customJS.BeaconButton.render`), delete "Project bugs > sub-task on map" entry (FIXED in v0.18.2). Retain finance personal/business expansion as the open carry.
6. **CLAUDE.md** status snapshot updated; commit + push; annotated tag `v0.20.0`; result writeup; post-v0.20.0 handoff.

**Out of scope (carry forward):**
- Comprehensive 11-cycle lift (every lesson from every result writeup) — deferred to a future dedicated docs cycle if depth ever needed.
- TDD-first FAIL test for the trailing-whitespace lint (introduce trailing space, verify lint catches, remove). Skipped per user preference; lint is failure-loud and exercised on every harness run.
- Project Sub-tasks panel on task hub (next cycle candidate).
- Styling presets / FrontmatterMutator promotion (premature).
- Finance polish carries (Total Planned/Actual row, All Paid toggle, migration helper).

---

## Section 2 — T4.0 harness fix mechanics

**Surface:** `platform/test/run-renderer.js`

**The fatal:**
```
FATAL: ENOENT: no such file or directory, open '.../platform/blueprints/project/content/kanban-board.md'
```
Caused by line 87 hardcoding a path deleted in v0.2.0 (boards cycle dropped the project Board contribution; project blueprint manifest's `files[]` lost `content/kanban-board.md`). Whole-suite `node platform/test/run-renderer.js` has fataled for 12 cycles; selector-based runs (`empty`, `malformed`, `unknown-action`, `beacon-cards`, `date-aware`, `finance`, `beacon-button`) sidestep T4.0 and have been the workaround.

**Change A — drop the constant and the read:**

| Line | Before | After |
|---|---|---|
| 87 | `const KANBAN_TEMPLATE_SRC_REL = 'platform/blueprints/project/content/kanban-board.md';` | DELETE |
| 498-501 | `const templateSourceAbs = path.join(WORKSHOP, KANBAN_TEMPLATE_SRC_REL); const templateBody = fs.readFileSync(templateSourceAbs, 'utf8'); const templateBodyLen = templateBody.length; console.log(`  template source: ${KANBAN_TEMPLATE_SRC_REL} (${templateBodyLen}B)`);` | `const templateBody = '# Synthetic Kanban\n\n```kanban\n## Backlog\n\n## In Progress\n\n## Done\n```\n'; const templateBodyLen = templateBody.length; console.log(`  template source: synthetic://kanban-board (${templateBodyLen}B)`);` |
| 515 | `template_source: KANBAN_TEMPLATE_SRC_REL,` | `template_source: 'synthetic://kanban-board',` |
| 534 | `if (p === KANBAN_TEMPLATE_SRC_REL) return templateBody;` | `if (p === 'synthetic://kanban-board') return templateBody;` |

**Change B — verify no other callers reference the constant.** Already verified during design — only 4 sites (declaration + read + registry + adapter), all inside `testLazyScaffold()`. Self-contained.

**What the test still validates:** the renderer's `createFromTemplate` action_type dispatch path (`platform/mechanisms/nav-buttons/space-nav-buttons.js:275`) — still live. Test stubs `getAbstractFileByPath` to make the target appear non-existent, asserts the renderer dispatches a `templater.create_new_note_from_template` call with the expected folder + filename + template body.

**Why "synthetic://" prefix?** Sentinel string makes the test's intent obvious to a future reader (`grep "synthetic://"` finds all synthetic-template tests at once). No special handling — it's just a string the adapter override matches exactly.

**Verification gates:**
- `node platform/test/run-renderer.js` (no selector) → exit 0; T4.0 prints `PASS`.
- All 7 existing selectors still PASS individually.

---

## Section 3 — Trailing-whitespace lint mechanics

**Surface:** `platform/test/run-helper-cases.js` (new selector `templates-whitespace`).

**Discovery glob:**
- `platform/blueprints/*/*.md` (one level deep — blueprints flatly hold templates at the root; verified during design with `find platform/blueprints -name '*.md'`).
- No subdir convention currently in use. If a future blueprint adds `platform/blueprints/<bp>/templates/*.md`, the lint extends additively.

**Files in scope at v0.20.0 close (19 template files observed):**
- `boards/Template, Board Card.md`
- `daily/` — none (daily blueprint has no template body materialized at install; entry UX is the core Daily Notes plugin command).
- `finance/Budget Template.md` + `Invoice Board Card.md` + `Invoice Board.md` + `Invoice Template.md` + `Paycheck Template.md` + `Time Log Template.md`
- `journal/Today Journal.md`
- `meetings/Meeting Hub.md` + `Meeting.md`
- `project/Kanban Card.md` + `Project Board.md` + `Project Map.md` + `Project.md` + `Task Board Card.md` + `Task Board.md` + `Task Note.md`
- `to-do/Today To-Do.md`
- `trips/Trip Atlas.md` + `Trip Board Card.md` + `Trip Board.md` + `Trip Flights.md` + `Trip Notes.md` + `Trip Packing List.md` + `Trip Stay.md` + `Trip To Do.md`

**Logic (pseudocode):**
```js
async function lintTemplatesWhitespace() {
  const blueprintsDir = path.join(WORKSHOP, 'platform', 'blueprints');
  const blueprints = await fs.promises.readdir(blueprintsDir);
  const violations = [];
  let filesScanned = 0;
  for (const bp of blueprints) {
    const bpDir = path.join(blueprintsDir, bp);
    const stat = await fs.promises.stat(bpDir);
    if (!stat.isDirectory()) continue;
    const entries = await fs.promises.readdir(bpDir);
    for (const e of entries) {
      if (!e.endsWith('.md')) continue;
      const abs = path.join(bpDir, e);
      const body = await fs.promises.readFile(abs, 'utf8');
      filesScanned++;
      const lines = body.split('\n');
      for (let i = 0; i < lines.length; i++) {
        if (/[ \t]+$/.test(lines[i])) {
          violations.push(`${path.relative(WORKSHOP, abs)}:${i + 1}  ${JSON.stringify(lines[i])}`);
        }
      }
    }
  }
  // Fail-loud: print all violations, exit non-zero if any.
  // Sub-assert count = filesScanned (one per file).
}
```

**Sub-assert convention:** one sub-assert per file scanned (PASS = no trailing whitespace anywhere in that file). If a file has 3 violations, the file's single sub-assert FAILS once; the harness output lists all 3 line:col pairs for actionability.

**Expected outcome at v0.20.0 ship:** all 19 files PASS. Helper-cases 312 → 331 (final N reflected in plan + result; counted at implementation time). If the lint surfaces a violation, in-stage fix (trim the offending line) + re-run before stage close — this is the lint's whole point.

**Why no FAIL-then-clean TDD dance:** user-locked at brainstorm time. The lint is failure-loud and exercised on every harness run; it's self-validating in production.

---

## Section 4 — Landmine #16 (in-cycle re-process bump rule)

**Surface:** `Docs/landmines.md` — new entry slotted after #15.

**Body (frozen — write to disk verbatim, modulo the literal install.js:111 quote which is captured at write time):**

```markdown
### 16. In-cycle re-process bump rule — when reusing in-cycle staged work, in-cycle revisions MUST bump the version

The installer's `installItem()` short-circuits when `subscribed.version === installed.version`. Any in-cycle CF that revises content WITHOUT bumping the item's version reaches barebones (or any other consumer) as the *previous* content because the version-equal short-circuit fires before the new content is read.

**Quoted call site (`platform/install.js:111`):**
```javascript
// <literal quote from install.js:111 — captured at write time>
```

**Five-data-point precedent:**
- **v0.6.0** trips 0.1.0 → 0.1.1 → 0.1.2 → 0.1.3 → 0.1.4 (4 in-cycle bumps for hub/atlas/sections/Trip Board CFs).
- **v0.17.0** finance 0.1.4 → 0.2.0 → 0.2.1 → 0.2.2 → 0.2.3 → 0.2.4 → 0.2.5 → 0.2.6 (6 in-cycle bumps across CF-1..CF-5).
- **v0.18.0** finance 0.2.6 → 0.2.7 → 0.2.8 → 0.2.9 (3-bump stack for CF-1 InvoiceControls Save fix).
- **v0.18.1** to-do 0.1.2 → 0.1.3 → 0.1.4 (CF-1 trailing-space trim forced re-process).
- **v0.19.0** styling 0.1.0 → 0.1.1 (CF-1 prereq gate). Mechanism cycles extend lockstep to **4 files** (workshop manifest + mechanism manifest + workshop subscription + barebones subscription) per v0.19.0 CF-1 reinforcement.

**Rule:** any in-cycle revision after first install MUST bump the item version (PATCH for fixes, MINOR for additive). Lockstep edits to:
1. blueprint or mechanism manifest (`platform/<kind>/<name>/manifest.json:version`)
2. workshop catalogue line (`platform/manifest.json:<kind>[].<name>.version`)
3. workshop subscription (mechanisms only — workshop self-subscription dogfoods mechanisms)
4. barebones subscription (`../barebones-beacon-poc/Docs/Meta/platform-subscription.json:<kind>[].<name>.version`)

**Recovery if a CF lands without a version bump:** bump the item version + re-run install. The barebones drift is silent — the `installed.json` will still record the prior content's SHA in history.

**Long-term fix candidate (deferred):** a `--force-reinstall <name>` flag for `install.js` so test mechanics + in-cycle CFs can skip the version-equal short-circuit without touching files. Same fix candidate as landmine #10. Not blocking; not free.

Codified in v0.20.0.
```

**Adjacent edits:**
- CLAUDE.md status snapshot landmines-list line: `15 entries → 16 entries`.
- `Docs/how.md` gotcha-9 references (if any) → cross-link to landmine #16.

---

## Section 5 — `Docs/how.md` "Gotchas / Lessons learned" section

**Surface:** new top-level section appended at the end of `Docs/how.md` (after current line 183).

**Section header:** `## Gotchas / Lessons learned (v0.11.0–v0.19.0)`

**The 10 entries (locked):**

1. **In-cycle re-process bump rule** — when revising in-cycle staged work, bump the item version (PATCH for fixes). See landmine #16 for the full mechanic. Surfaced: v0.6.0 / v0.17.0 / v0.18.0 / v0.18.1 / v0.19.0.

2. **API-contract quoting** — when authoring a new mechanism API or invoking an existing shared API in a design doc, QUOTE the literal call shape. Inferring from a sibling fails (TripsHubCards inferred BeaconCards' API → CF-1; design described "field-function API matching ProjectsHubCards precedent" but didn't quote the call shape). Surfaced: v0.6.0.

3. **dv-shim composition discipline** — when a hub renderer hands a sub-container to another customJS class, the shim MUST proxy every dv method the sub-class calls (`pages` + `current` + `el` minimum). A `{container: sub}`-only shim breaks any sub-renderer that calls `dv.pages(...)`. Surfaced: v0.17.0 CF-1 (latent v0.16.0 bug).

4. **Mobile-aware visual design** — every CustomJS class designed for desktop AND mobile from the start. Installer is desktop-only (landmine #8); renderers are not. Surfaced: v0.4.2 gotcha 7.

5. **Embed dedup via `closest(".markdown-embed")`** — when a note is embedded via `![[X]]`, its dataviewjs blocks should suppress duplicated nav-buttons / widgets at the top of the embedded block via `if (dv.container.closest(".markdown-embed")) return;`. Surfaced: v0.16.0.

6. **YAML date auto-parsing + frontmatter parser variance** — frontmatter values matching `YYYY-MM-DD` or `YYYY-MM` auto-parse to Date|Luxon objects; quote on write (`"${month}"`) + tolerant `_toMoment(val)` on read (accepts Luxon/Date/string). Inline-flow booleans may parse as strings; centralize coercion via `isPaid()`-style helpers. Surfaced: v0.16.0.

7. **NBSP gotcha — widgets are the canonical fix** — manual frontmatter editing introduces U+00A0 NBSPs through chat / markdown copy-paste, corrupting inline-flow YAML arrays. Editor widgets (e.g., BudgetCategoriesEditor / PaycheckExpensesEditor / InvoiceTimeLogEditor) bypass every YAML edge case at the source by mutating frontmatter via `app.fileManager.processFrontMatter`. Surfaced: v0.16.0 → fixed in v0.17.0.

8. **Two backup-suffix conventions** — `.bak` for file-content overwrite (vendored themes; v0.2.0 boards Option B precedent), `.beacon-backup` for plugin-data additive merge. Mixing them is confusing; codified in landmine #12 mechanic #2. Surfaced: v0.19.0.

9. **Helpers that materialize new state need explicit prereq gates** — `applyExternalPlugins` only emits warnings; helpers that mkdir + fresh-write must short-circuit at the top via `_externalPluginsSatisfied(tp, manifest)` or equivalent prereq check. Pattern reusable for any future helper whose missing-prereq state isn't recoverable post-hoc. Surfaced: v0.19.0 CF-1.

10. **Bounded cohesion — BeaconCards = hubs only / bullet lists for tasks; outline-accent buttons go through BeaconButton** — BeaconCards for hub-style listings (real files with title/subtitle/meta worth surfacing visually); plain `<ul>` for compact at-a-glance task panels (v0.12.0 daily blueprint S3.4.1 inline-CF). Outline-accent action buttons go through `customJS.BeaconButton.render(...)` — no inline cssText + hover handlers (v0.18.0 BeaconButton mechanism promotion).

**Footer pointer:**

> Per-cycle detail in result writeups under `Docs/plans/` (filename pattern: `<date>-<cycle>-result.md`). CLAUDE.md status snapshot is the running summary across all cycles; landmines.md is the rule registry.

---

## Section 6 — `blueprints i want.md` cleanup

**Items to delete:**

| Lines | Content | Reason |
|---|---|---|
| 11-67 | Styling section + frozen design pointer + 38-key Style Settings JSON callout | DONE in v0.19.0 (styling@0.1.1 mechanism shipped) |
| 68 | "I want the new meeting button to reflect the pattern used for the new project button" | SATISFIED at v0.18.1 (NewMeetingButton uses overlay-modal `inputModal` + `customJS.BeaconButton.render` per `platform/blueprints/meetings/helpers/new-meeting-button.js:8-101`) |
| 72-73 | "Project bugs > sub-task on map" entry | FIXED in v0.18.2 (Project Map filter excludes sub-task cards via `e967bde feat(project): v0.18.2 — Map filter excludes sub-task cards`) |

**Items to retain:**

| Lines | Content | Reason |
|---|---|---|
| 2-10 | Finance personal/business expansion ask (Headspace + ERO patterns) | Open carry; not yet scoped |

**Final shape:** the file becomes a small focused wishlist of open carries only. If wishlist hits zero items in a future cycle, deletion-of-file is a candidate.

---

## Section 7 — Stage breakdown

**Three stages, master-driven; no subagent dispatch needed (pure docs + harness).**

### S1 — Harness hardening (1 commit)

| Task | Surface | Action |
|---|---|---|
| T1.1 | `platform/test/run-renderer.js` | Inline synthetic kanban body; drop `KANBAN_TEMPLATE_SRC_REL`; sentinel `'synthetic://kanban-board'` |
| T1.2 | `node platform/test/run-renderer.js` | Verify exit 0 + T4.0 PASS + 7 selectors PASS |
| T1.3 | `platform/test/run-helper-cases.js` | Add `lintTemplatesWhitespace` case + new selector `templates-whitespace` |
| T1.4 | `node platform/test/run-helper-cases.js` | Verify 312 + N PASS, 0 FAIL (final N counted) |
| T1.5 | git commit + push | `test(harness): v0.20.0 S1 — fix T4.0 fatal + trailing-whitespace lint for blueprint templates` |

**Stage-close AskUserQuestion checkpoint** before S2.

### S2 — Docs polish (1 commit)

| Task | Surface | Action |
|---|---|---|
| T2.1 | `Docs/landmines.md` | Append landmine #16; literal quote of `install.js:111`; cite 5-data-point precedent |
| T2.2 | `Docs/how.md` | Append "Gotchas / Lessons learned (v0.11.0–v0.19.0)" section with 10 entries |
| T2.3 | `blueprints i want.md` | Delete styling section (lines 11-67), meeting-button line (68), project-bugs sub-task entry (72-73) |
| T2.4 | git commit + push | `docs(v0.20.0): S2 — landmine #16 + how.md gotchas section + blueprints i want.md cleanup` |

**Stage-close AskUserQuestion checkpoint** before S3.

### S3 — Cycle close (1-2 commits + tag)

| Task | Surface | Action |
|---|---|---|
| T3.1 | `platform/manifest.json` | Bump `workshop_version` 0.19.0 → 0.20.0 — REQUIRES user approval per CLAUDE.md |
| T3.2 | `CLAUDE.md` | Status snapshot entry for v0.20.0 close + landmines list 15 → 16 + helper-cases 312 → final-N |
| T3.3 | dogfood | `node platform/test/run-install.js .` (workshop) → clean + idempotent; same for `../barebones-beacon-poc` |
| T3.4 | `Docs/plans/2026-05-05-v0.20.0-docs-polish-result.md` | Result writeup |
| T3.5 | git commit + push | `docs(v0.20.0): cycle close — docs polish + T4.0 + landmine #16 + trailing-whitespace lint + workshop_version 0.19.0 → 0.20.0 MINOR` |
| T3.6 | annotated tag `v0.20.0` | REQUIRES user approval per CLAUDE.md |
| T3.7 | `Docs/prompts/2026-05-05-post-v0.20.0-next-cycle-handoff.md` | Post-v0.20.0 handoff |
| T3.8 | git commit + push | `docs(v0.20.0): post-v0.20.0 next-cycle handoff` |

**No manual smokes.** Harnesses ARE the smoke (workshop self-install + barebones install + helper-cases + renderer).

---

## Section 8 — Approval gates

Per CLAUDE.md "ask before acting":
- **T3.1** workshop_version bump 0.19.0 → 0.20.0 — explicit user approval required.
- **T3.6** annotated tag `v0.20.0` push — explicit user approval required.

All other surfaces are local edits to docs / harness; auto-proceed.

---

## Section 9 — Risks + mitigations

| Risk | Likelihood | Mitigation |
|---|:-:|---|
| Trailing-whitespace lint surfaces unknown latent violations across 19 templates | Medium | In-stage trim + re-run before S1 stage close. Lint is doing its job; trim is a 1-line edit per violation. |
| Post-T4.0 fix surfaces a different harness fatal further down `run-renderer.js` | Low | Whole-suite renderer harness has been silent for 12 cycles ONLY because of T4.0; if a 2nd carry-forward exists, it surfaces at S1 T1.2. Triage in-stage; if scope creeps, defer the secondary fix to a v0.20.x carry. |
| `install.js:111` literal quote drift between design and write time | Low | Capture the literal quote at S2 T2.1 by reading the file then; design body has a placeholder `<literal quote from install.js:111 — captured at write time>` that gets replaced. |
| how.md section becomes stale immediately if v0.20.x cycle adds a new lesson | Low | Each future cycle's result writeup remains the source of truth; how.md gotchas section is a "snapshot at v0.19.0 close + v0.20.0 codification" reference. Update on lesson-introducing cycles only. |
| Reserved-headroom-UNUSED breaks for this cycle (CF surfaced) | Low | 5-data-point precedent (v0.13.0 + v0.14.0 + v0.18.0 + v0.18.1 + v0.18.2) holds for non-UI dedupe / pure adoption / pure label / pure docs / scoped-bug-fix cycles. This cycle is pure docs + harness; should land clean. If a CF surfaces, in-cycle bump per landmine #16 itself (recursive eat-our-own-dogfood validation). |

---

## Section 10 — Cycle-close gates (explicit)

> [!success] Done when:
> - [ ] T4.0 fatal eliminated; whole-suite `run-renderer.js` exits 0
> - [ ] Trailing-whitespace lint shipped + green across all blueprint templates
> - [ ] Landmine #16 codified in `Docs/landmines.md` with literal `install.js:111` quote
> - [ ] `Docs/how.md` "Gotchas / Lessons learned" section appended (10 entries)
> - [ ] `blueprints i want.md` cleaned of v0.18.1 / v0.18.2 / v0.19.0 closures
> - [ ] `workshop_version` bumped to 0.20.0 (user-approved)
> - [ ] CLAUDE.md status snapshot updated
> - [ ] Workshop self-install + barebones install both clean + idempotent
> - [ ] Result writeup committed
> - [ ] Annotated tag `v0.20.0` pushed (user-approved)
> - [ ] Post-v0.20.0 handoff committed + pushed
> - [ ] Final user message: copy-paste slash command + status summary (per `Docs/prompts/_handoff-protocol.md`)

---

## Section 11 — Open questions

> [!info] None remain.
> All four design questions resolved during brainstorming:
> 1. ✅ Workshop bump → 0.20.0 MINOR
> 2. ✅ T4.0 fix → inline synthetic body
> 3. ✅ how.md scope → load-bearing 10 entries only
> 4. ✅ Lint scope → templates only, no FAIL test (necessary surface only)

---

End of design.
