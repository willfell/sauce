# v0.82.0 handoff — design + plan committed, ready for execution

**Status:** design + plan committed; execution deferred to next session for context cleanliness.
**Date:** 2026-06-01 (evening).
**Predecessor:** v0.81.1 (cowork-slash-doc-fix), workshop `0.81.1` closed 2026-05-30, HEAD prior to this commit.
**Next action:** paste the continuation prompt below into a fresh Claude Code session in `/Users/willfellhoelter/projects/repos/sauce` tomorrow morning.

---

## What's committed + ready for execution

- **Design doc** (`Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-design.md`) — 3 workstreams covered:
  - **WS-A** Per-kind callout types — chat → `info`, finance → `warning`, calendar → `tip`, email → `quote`, ado → `example`, github → `note`; user-overridable per kind in `user-preferences.md`
  - **WS-B** Soften gather contract's "no tables" prohibition into table-or-bullet decision rule; microscope output_shape overrides
  - **WS-C** w2-fte engagement-type manifest gains `midday` + `tripwire_aspects` array — accuris midday-tripwire stops being a silent no-op

- **Implementation plan** (`Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-plan.md`) — 10 stages, subagent-driven-development execution, ~27 HC sub-asserts, single coherent cycle. Workshop 0.81.1 → 0.82.0; cowork 0.20.1 → 0.21.0. USER APPROVAL gate at S10.9 (tag).

- **This handoff** — the continuation prompt + pre-execution checklist + references.

---

## Pre-execution checklist (run BEFORE starting S0)

When the new session opens, before invoking subagent-driven-development:

1. **Verify workshop is at v0.81.1.** Tonight's morning-briefing hot-fix (table-emitting ADO + GitHub microscope edits) lived ONLY in the accuris consumer vault — no workshop commit needed for that. Workshop HEAD should be at the v0.82.0 design+plan+handoff commits.

2. **Confirm last night's hot-fix landed on accuris.** Will edited the accuris ADO + GitHub microscopes to add `## Output shape` directives forcing tables. The 2026-06-01 morning brief output should now have tables for those kinds. If tables landed → WS-B is contract-truthing (we're catching the SKILL.md up to what the agent already does). If tables didn't land → WS-B is the load-bearing fix.

3. **Verify clean tree on origin/main.** No untracked work-in-progress files.

4. **Read the 3 cycle-specific files in full** before starting S0:
   - `Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-plan.md`
   - `Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-design.md`
   - `Docs/plans/2026-06-01-v0.82.0-handoff-and-continuation.md` (this file)

5. **Also load the routing guides** (per SESSION-START.md convention):
   - `Docs/agent-guides/cycle-status.md`
   - `Docs/agent-guides/build-test-verify.md`
   - `Docs/agent-guides/cowork-customization-contract.md`
   - `Docs/landmines.md`

---

## The continuation prompt — paste this into a fresh Claude Code session tomorrow

```
v0.82.0 callout-types-tables-and-tripwire — EXECUTE the committed implementation plan on this machine.

REPO: /Users/willfellhoelter/projects/repos/sauce (sauce WORKSHOP vault, branch main,
currently workshop v0.81.1). Dogfoods as its own first consumer — NOT a consumer vault.
Run the CLAUDE.md vault-identity check before any write.

STEP 0 — sync + load:
  cd /Users/willfellhoelter/projects/repos/sauce
  git rev-parse --show-toplevel && git log --oneline -5
  # HEAD should include the v0.82.0 design + plan + handoff commits from 2026-06-01.
  # If not: git fetch origin && git checkout main && git pull --ff-only

Then Read these IN FULL by absolute path:
  /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-plan.md
  /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-design.md
  /Users/willfellhoelter/projects/repos/sauce/Docs/plans/2026-06-01-v0.82.0-handoff-and-continuation.md

Also read the routing guides:
  Docs/agent-guides/cycle-status.md
  Docs/agent-guides/build-test-verify.md
  Docs/agent-guides/cowork-customization-contract.md
  Docs/landmines.md

WHAT TO DO: Use superpowers:subagent-driven-development (the plan's recommended sub-skill)
and execute the plan stage-by-stage, S0 → S10, exactly as written.

WHAT IT SHIPS:
- WS-A per-kind callout types: chat=info, finance=warning, calendar=tip, email=quote,
  ado=example, github=note (user-overridable per kind via mcps.<kind>.callout_type in
  user-preferences.md). read-user-preferences-helper.js gains DEFAULT_CALLOUT_TYPE_BY_KIND
  + resolveCalloutType(). gather-from-served-by-helper.js accepts + validates +
  echoes callout_type_used. 5 orchestrators pass callout_type in gather loop.
- WS-B soften "no tables" prohibition in gather-from-served-by/SKILL.md Step 3 into
  table-or-bullet decision rule; microscope ## Output shape directives override.
- WS-C engagement-types/w2-fte.json gains midday in supported_cadences +
  tripwire_aspects array (calendar-drift, queue-growth, pr-review-backlog, chat-urgency).
- VERSIONS at close: workshop 0.81.1 → 0.82.0; cowork 0.20.1 → 0.21.0.

CONVENTIONS (baked in from v0.79+v0.80+v0.81 close):
- Dogfood install: node platform/install.js --vault . --auto-approve (v0.80.1 FLN-v79-2
  CLI handler works; this is the documented invocation).
- Landmine #16 + FLN-v80-2: install short-circuits cowork until subscription pin bumps,
  but global materializeClaudeSurface keeps SKILL.md dests current per-install. Stage
  commits include .claude/ catch-ups where relevant.
- agent_markdown 80-char floor still applies for any new gather-helper HC fixtures.
- No CS-MIG-1 bump this cycle (no new claude_surface entries) — counts stay at 38/47.
- Direct-push origin/main; ONE commit per stage; conventional commits
  `feat|fix|chore|docs|test(scope): v0.82.0 SN — summary`; NO Co-Authored-By trailer;
  explicit `git add <paths>` (never -A). After any SKILL.md/manifest change,
  re-materialize (`node platform/install.js --vault . --auto-approve`) and stage
  regenerated ranch/ + .claude/ in the same commit. Preflight gated on
  scripts/check-version-sync.js (package.json version == workshop_version).
  Get USER APPROVAL before pushing any annotated tag.

IN-STAGE VERIFICATIONS the plan flags inline (read live files, don't guess):
- S2.1: exact shape of read-user-preferences-helper.js (kind-block iteration pattern
  dictates where to call resolveCalloutType)
- S4.2: exact wording of gather-from-served-by/SKILL.md Step 3 dispatch contract template
- S4.4: appropriate anchor in read-user-preferences/SKILL.md for callout_type docs
- S6.2: per-orchestrator anchor (confirmed identical across 5 in v0.80.0; sanity-check)
- S8.2: exact JSON shape of w2-fte.json (single-line vs multi-line array)
- S10.4: cycle-status.md current state for the 4 edits

PRE-EXECUTION SANITY CHECK (do this BEFORE S0):
1. Confirm workshop at v0.81.1 (jq -r .workshop_version ranch/platform-installed.json)
2. Confirm Will's 2026-06-01 accuris hot-fix landed (his ADO + GitHub microscopes have
   ## Output shape sections that force tables). Workshop has no commits for this — it's
   vault-only. The hot-fix tells us WS-B's soften is contract-truthing.
3. Verify clean tree on origin/main (git status returns clean).

END STATE: workshop on v0.82.0, all S1–S10 commits on origin/main, preflight ALL GREEN
at version-sync 0.82.0, cycle-close artifacts written. After USER APPROVAL gate at
S10.9, tag v0.82.0 + tap PR merge + brew upgrade + sauce update --bump-pins on all 4
vaults to deploy.

After deployment, the user-side test cycle:
1. Wait for midday-tripwire cron at 12:30 PM Mountain on accuris — should now FIRE
   (not silent no-op) with calendar-drift / queue-growth / pr-review-backlog / chat-
   urgency signals as data warrants.
2. Wait for morning-briefing cron tomorrow on both vaults — chat/finance/calendar/email/
   ado/github sections render in 5-6 distinct colors per the default mapping.
3. Verify ADO + GitHub sections continue to emit tables on accuris (microscope override
   + WS-B contract softening; either should work alone, both together = belt+suspenders).
4. Capture per-vault test results to Docs/plans/2026-06-02-<vault>-test-results.md.
```

---

## References (for the new session to load if needed)

- v0.79.0 design + result: `Docs/plans/2026-05-29-v0.79.0-*` (microscope contract; hard_rules; callout CSS)
- v0.80.0 design + result: `Docs/plans/2026-05-30-v0.80.0-*` (sibling files; orchestrator step 2c)
- v0.81.0 design + result: `Docs/plans/2026-05-30-v0.81.0-*` (audit-siblings)
- v0.81.1 result: `Docs/plans/2026-05-30-v0.81.1-flnsbundle-result.md` (slash doc fix)
- Cohort summary: `Docs/plans/2026-05-30-cowork-cohort-summary-and-headspace-onboarding.md`
- Multi-vault test guide: `Docs/plans/2026-05-31-multi-vault-onboarding-and-official-test-guide.md`
- Headspace test plan: `Docs/plans/2026-05-30-headspace-test-plan.md`

---

## Why this handoff approach (vs. attempt-execute-tonight)

Three reasons:

1. **Context cleanliness.** Tonight's session has been long. v0.82.0 is ~10 stages × subagent-driven discipline = 30+ subagent dispatches. Starting fresh with the design+plan as the contract is mechanically cleaner.
2. **Test isolation.** Tomorrow morning Will tests v0.81.x state on both vaults (the microscope authoring + hot-fix table output + audit-siblings) BEFORE v0.82.0 ships. That gives clean baseline data for the v0.82.0 cycle's own design input.
3. **USER APPROVAL gate timing.** The tag at S10.9 needs Will's eyes. Better to have that gate hit during fresh-context work hours than late-night decision fatigue.

---

## Carry-forward — what v0.82.0 does NOT cover

Explicitly deferred to v0.83+ (per design §11):

- **midday-tripwire for consulting + personal engagement-types** — same fix pattern as w2-fte, different tripwire_aspects per type
- **eod / weekly / monthly aspect-overrides** — same engagement-type config gap may exist for those cadences
- **Per-callout-type default microscope output_shape recipes** — e.g., `warning` → table-heavy default
- **Callout-type validation harness** — walk all 5 orchestrators + verify default mapping covers all kinds in any consumer vault
- **CSS theming for non-builtin callout types** — sauce-callouts.css covers 9 Obsidian builtins; custom types like `[!finance-summary]` would need new rules

---

## End

This handoff doc + design + plan are committed together. Tomorrow morning's continuation prompt above is self-contained — paste it into a fresh Claude Code session in the workshop repo, and the new chat picks up the cycle with zero context gap.

Good night.
