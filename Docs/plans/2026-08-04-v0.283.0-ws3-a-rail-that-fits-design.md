# Loop-integrity workstream 3 — a rail that fits — design (pointer)

**Cycle:** 2026-08-04 · **Version:** workshop 0.282.1 → 0.283.0 · **Workstream:** loop-integrity program #3

The approved design for this cycle lives at
[`Docs/superpowers/specs/2026-08-04-loop-integrity-ws3-a-rail-that-fits-design.md`](../superpowers/specs/2026-08-04-loop-integrity-ws3-a-rail-that-fits-design.md)
(committed on the cycle branch; program decomposition at
[`Docs/superpowers/specs/2026-08-04-loop-integrity-program.md`](../superpowers/specs/2026-08-04-loop-integrity-program.md)).
It covers disease C (no legitimate escape hatch, so work leaves the rail): the
measurement that sized the cycle, the `adopted` authority tier, the `adopt`
verb's refusal set and provenance tiers, provenance classification of untracked
board members in `board-health`, the cross-process write guard
(`card_note_sha` / `foreign_write`), and the bulk-verb fail-closed posture.

Unlike workstream 2, this design **did** change during implementation. Rather
than being silently rewritten, the spec carries a dated
**§ Post-implementation corrections (2026-08-05)** section recording every
divergence — the Director's `phase: 'completed'` → `phase: 'adopted'` ruling,
the added `adopt_pr_not_found` refusal, the detection semantics for
`foreign_write`, the true breadth of `card_note_sha` stamping, and how much
narrower §6.2 turned out to be. Read that section together with the body; the
body alone is the design as originally written, not as shipped.

Spec interpretations and the whole-branch review fix wave are recorded in the
result doc
([`2026-08-04-v0.283.0-ws3-a-rail-that-fits-result.md`](2026-08-04-v0.283.0-ws3-a-rail-that-fits-result.md)
§ Spec interpretations and divergences, § Final whole-branch review).
