---
cycle: v0.84.1
codename: cleanup-1
kind: PATCH
status: closed
opened: 2026-06-02
closed: 2026-06-02
predecessor: v0.84.0
---

# v0.84.1 cleanup-1 — result

Shipped as designed in `2026-06-02-v0.84.1-cleanup-1-design.md` and planned in `2026-06-02-v0.84.1-cleanup-1-plan.md`. Eight stage commits, preflight green, two surfaced deviations (test-shim completeness + test-pin tracking) handled inline.

## What landed

| § | Surface | Files |
| --- | --- | --- |
| §1 | propertiesInDocument default = hidden | `platform/manifest.json`, `.obsidian/app.json`, `platform/test/run-install-sh.js` (PID-1) |
| §2.1 | `_coerceDay` drops Date branch | `platform/blueprints/scratch/helpers/scratch-day-list.js` |
| §2.2 | `ScratchDayMigrate` + Init startup-script | NEW `platform/blueprints/scratch/helpers/scratch-day-migrate.js`, `scratch-day-migrate-init.js`; `platform/blueprints/scratch/manifest.json` |
| §2.4 | activity-feed numeric ISO compare | `platform/mechanisms/activity-feed/activity-feed.js` (`inWindow`); test-shim `valueOf()` added in `run-activity-feed.js` |
| §3 | Tasks header open · done + CSS + `titleHtml` opt | `ranch/scripts/daily/space-daily-dashboard.js`, `platform/blueprints/daily/helpers/sauce-daily-dashboard.css` |
| §4 | defaultClosed wins over persisted state | `platform/mechanisms/activity-feed/activity-feed.js` (`_renderFramedGroup`) |

## Version table

| Unit | From | To |
| --- | --- | --- |
| workshop | 0.84.0 | 0.84.1 |
| platform-claude | 0.1.1 | 0.1.2 |
| activity-feed | 0.7.0 | 0.7.1 |
| styling | 0.2.0 | 0.2.1 |
| daily | 0.13.0 | 0.13.1 |
| scratch | 0.5.1 | 0.5.2 |

## Tests

~32 HC-V0841-* sub-asserts across NEW `run-scratch.js` + appended blocks in `run-activity-feed.js` and `run-renderer.js`:

- A1 ×7 — `_coerceDay` regression (string, Luxon, Date→null, null/undefined/object → null)
- A2 ×15 — `ScratchDayMigrate` behavior (passthrough, Date rewrite, path/filename synthesis, no-op, idempotency, manifest wiring)
- A3 ×2 — `inWindow` numeric epoch compare source-lint
- C1 ×6 — Tasks header forms + `titleHtml` opt + CSS rule
- D1 + D2 — `defaultClosed` `!isClosed` gate + toggle WRITE preservation

3 in-cycle test-pin updates surfaced at S7 (track-the-bump, not weakening):
- `run-helper-cases.js` SHC-S1 + FA6-MANIFEST-daily/activity-feed
- `run-activity-feed.js` AF-1d + AF-V065
- `run-install-sh.js` PID-1 (asserts new propertiesInDocument value)

## Surfaced deviations

| Deviation | Stage | Resolution |
| --- | --- | --- |
| Test shim `makeMomentShim` lacked `valueOf()` after S4 added `window.moment().valueOf()` in production | S4 | Added `valueOf()` to the shim in `run-activity-feed.js`. Production unchanged. |
| `_renderSection` summary.innerHTML used inline ternary `titleHtml ? titleHtml : this._escapeHtml(title)` instead of the spec's local-variable form | S5 | Functionally equivalent (empty string and undefined both falsy). Test C1.6 regex matched the inline form. |
| 3 test-harness files had hardcoded version pins that broke after S7's manifest bumps | S7 | Pin-tracking updates: SHC-S1, FA6-MANIFEST, AF-1d, AF-V065, PID-1. Strict track-the-bump; no assertion weakening. |

## Carry-forwards

- **FLN-v841-1 (NEW — v0.85.0+ candidate):** More test-pin sites use hardcoded version strings (SHC-S1, FA6-MANIFEST, AF-1d, AF-V065). FLN-v81-1 closed three pin sites by adopting `versionAtLeast()`; the remaining hardcoded-equality sites surfaced again at S7. Future cycle could extend `semver-helper.js` with a `versionEquals()` helper and refactor the equality sites where exact pinning is intentional (vs. converting to floor checks where appropriate).
- **FLN-v841-2 (NEW — long-term candidate):** Activity-feed group state persistence is now asymmetric: `defaultClosed` groups read-skip persistence but still write to it. Toggling a `defaultClosed` group open still persists `open:true` to `dashboard-section-state.json`; that value is just ignored on subsequent renders. The stale data isn't load-bearing but does grow the cache file unnecessarily. Cleanup: prune persisted state entries for groups currently in `defaultClosed` at write time.

## Preflight

`version-sync ok: 0.84.1` ALL GREEN at S7 close. Re-confirmed at S8 final preflight.
