# v0.41.0 — convenience-cohesion design

**Cycle slot:** v0.41.0 MINOR
**Owner mechanism:** `convenience@0.1.0 → 0.2.0` MINOR (absorbs all three behaviors)
**Date:** 2026-05-13

---

## Intent

Bundle three consumer-default behaviors under the `convenience` mechanism + fold the dormant v0.39.0 partial-work cleanup:

1. **Reader-view default** — every newly opened note starts in reading view (not editor).
2. **Cmd+T → today's daily** — new-tab hotkey opens today's daily instead of an empty tab.
3. **Tasks-plugin emojis → Lucide icons** — replaces 📅 ⏫ 🔼 🔽 ⏳ 🛫 ✅ 🔁 with monochrome SVG glyphs via a CSS snippet shipped + auto-enabled.
4. **v0.39.0 limbo cleanup** — revert orphaned `project@1.6.0` manifest changes (ProjectActionButtons + products/teams deps reference never-created files / blueprints).

---

## Deliverable 1 — Reader-view default

- `platform/manifest.json` `app_settings` += `"defaultViewMode": "preview"`, `"livePreview": false`.
- Existing `applyAppSettings` helper writes both keys to `<vault>/.obsidian/app.json` (platform-as-overrider semantics already in place since v0.26.1).
- Zero installer changes.
- Effect: every freshly opened note starts in reading view; `Cmd+E` flips to edit.

## Deliverable 2 — Cmd+T → today's daily

- `convenience` manifest `hotkeys[]` += `{ "command_id": "daily-notes:goto-today", "modifiers": ["Mod"], "key": "t" }`.
- Built-in command. Inherits new-tab semantics from already-shipped `alwaysOpenInNewTab: true`.
- Zero new code; pure manifest delta routed through existing `applyHotkeys` helper.

## Deliverable 3 — Tasks-plugin emojis → Lucide icons

**Source:** `/Users/willfell/Documents/obsidian/sync/old-vaults/accuris/.obsidian/snippets/tasks-icons.css` (verified working in user's prior accuris vault).

**Mechanics:**
- Hides emoji text on `.task-list-item .task-extras span[data-task-priority]` + `.task-due` / `.task-start` / `.task-scheduled` / `.task-done` / `.task-recurring` via `font-size: 0`.
- Restores size on `::before` pseudo-elements + injects Lucide SVGs as `-webkit-mask-image` data-URIs (monochrome, inherits `currentColor`).
- Works on default Tasks-plugin emoji format — no `taskFormat` setting change needed.

**Ship path:** vendored at `platform/mechanisms/convenience/assets/snippets/sauce-tasks-icons.css`; installer copies → `<vault>/.obsidian/snippets/sauce-tasks-icons.css`; auto-enabled in `<vault>/.obsidian/appearance.json` `enabledCssSnippets[]`.

**New manifest schema (convenience@0.2.0):**

```json
{
  "snippets": [
    { "source": "assets/snippets/sauce-tasks-icons.css",
      "name":   "sauce-tasks-icons" }
  ],
  "appearance": {
    "enabledCssSnippets": ["sauce-tasks-icons"]
  }
}
```

**Installer wiring (simplified after scouting):**
- ONE new helper: `applySnippets` — copies declared CSS assets to `<vault>/.obsidian/snippets/<name>.css`. Mirrors `applyVendoredThemes` posture (sha256-compare overwrite-with-backup; `.sauce-backup` suffix; failure-loud history).
- Existing `applyAppearance` helper (since v0.19.0) ALREADY supports `enabledCssSnippets` additive union — registration piggybacks on the existing `appearance` block at convenience manifest. No new registration helper.

**Helper count:** 13 → 14 (one new helper: `applySnippets`).

## Deliverable 4 — v0.39.0 limbo cleanup

**Symptom:** `platform/blueprints/project/manifest.json` is at `1.6.0` declaring:
- `customjs_classes: [..., "ProjectActionButtons"]` — but `helpers/project-action-buttons.js` does not exist.
- `depends_on: [..., { "name": "products", "range": ">=0.1.0" }, { "name": "teams", "range": ">=0.1.0" }]` — but neither blueprint exists.
- `status` / `status_changed_at` / `teams[]` / `products[]` frontmatter additions referenced in description.

Workshop subscription pins `project@1.5.0` so the broken `1.6.0` manifest hasn't actually installed — but it pollutes audit + would crash a fresh subscriber that tried to install latest.

**Cleanup action:**
- Revert `platform/blueprints/project/manifest.json` to its `1.5.0` shape (drop ProjectActionButtons + products/teams deps + 1.6.0 description sentence).
- `platform/manifest.json` blueprints catalogue `project` stays at `1.5.0` (no version-bump churn).
- A future cycle re-attempts the v0.39.0 work cleanly (separate plan). This cycle does not ship the v0.39.0 feature, only un-pollutes the manifest.

---

## Allowlist expansion (landmine #12) — USER APPROVED

17 → 18 paths (`.obsidian/appearance.json` already covered since v0.19.0):
- `.obsidian/snippets/sauce-*.css` (platform-vendored, sauce-namespaced; mirrors `.obsidian/themes/<Name>/` vendored-theme posture from v0.19.0 / landmine #15).

**Landmine #12 mechanics amendment:** the existing carve-out reads "User-owned snippets at `.obsidian/snippets/` (NOT in the allowlist; user-managed; never touched by the installer)". v0.41.0 narrows this to: user-owned snippets are everything in `.obsidian/snippets/` EXCEPT files matching `sauce-*.css`. Platform-vendored `sauce-*.css` files are write-overrides per the vendored-theme posture (sha256-compare; `.sauce-backup` on overwrite); the carve-out keeps the explicit `sauce-` prefix so a consumer's hand-written snippet at e.g. `my-custom.css` is never touched.

Helper count: 13 → 14.

---

## Testing deltas

| Harness | Δ | Notes |
| --- | --- | --- |
| `run-helper-cases.js` | +6 | 3 ES-cases (`applySnippets`+`applyEnabledSnippets`: copy-when-absent / additive-merge / idempotent-rerun); 2 AS-cases (defaultViewMode + livePreview key writes); 1 H-case (Cmd+T hotkey entry round-trip) |
| `run-renderer.js` | UNCHANGED (36 cases) |
| `run-audit.js` | UNCHANGED (77 sub-asserts) |
| `run-cli.js` | UNCHANGED |
| `run-bootstrap.js` | UNCHANGED |
| `run-integration-smoke.js` | UNCHANGED |

**Whole-suite target:** 1224 → 1230 sub-asserts + 36 renderer cases. Workshop self-install green pre-tag.

---

## Stage shape

| Stage | Work | Bumps |
| --- | --- | --- |
| **S1** | Revert `project/manifest.json` → 1.5.0 shape (cleanup) | — |
| **S2** | `platform/manifest.json` `app_settings` += defaultViewMode + livePreview | — |
| **S3** | Vendor `sauce-tasks-icons.css` under `convenience/assets/snippets/`; add `snippets[]` + `enabled_snippets[]` to convenience manifest; add Cmd+T hotkey | convenience 0.1.0 → 0.2.0 |
| **S4** | `applySnippets` + `applyEnabledSnippets` installer helpers; wire into `install.js` | — |
| **S5** | Test-harness additions (run-helper-cases.js +6) | — |
| **S6** | Landmine #12 wording update (17 → 19 paths); CLAUDE.md "Status (live)" Mechanisms catalogue line for convenience | — |
| **S7** | Workshop self-install via `sauce reinstall --vault $PWD`; whole-suite preflight green | — |
| **S8** | Version-lockstep: workshop_version 0.40.4 → 0.41.0; package.json 0.40.4 → 0.41.0; subscription convenience entry 0.1.0 → 0.2.0; commit + tag + push | workshop 0.41.0 |

Pacing: controller-direct (no subagents) — small substrate-extension cycle with one new helper-pair and one CSS asset.

---

## Risks + mitigations

| Risk | Mitigation |
| --- | --- |
| Tasks plugin not installed yet → CSS selectors don't match | Snippet harmless when plugin absent; appearance.json registration is plugin-independent |
| `defaultViewMode: "preview"` annoys editing-first users on fresh notes | Documented in convenience manifest description; `Cmd+E` flips per-note; can be overridden by removing from `platform/manifest.json` app_settings |
| `daily-notes:goto-today` opens in current tab if today's daily already focused | Acceptable — Cmd+T pressed when on today's daily is a no-op-by-design; alwaysOpenInNewTab covers the first-open case |
| `.obsidian/appearance.json` additive-merge collision with user's other enabled snippets | Helper merges sauce-snippets-list ∪ existing; never replaces; backup-on-edit per landmine #12 mechanics |
| v0.39.0 cleanup deletes work-in-progress for ProjectActionButtons | Work is partial + never installed (1.5.0 pinned in subscription); future cycle can re-introduce cleanly. Git history preserves the 1.6.0 manifest for reference |

---

## Out of scope

- v0.39.0 re-attempt (separate future cycle)
- Tasks plugin `data.json` configuration changes (CSS-only fix; user keeps default emoji format)
- `taskFormat: "dataview"` migration (not needed; snippet works on emoji format)
- Wave 3 (v0.34.0) blueprint claude_surface adoption (separate cycle)
- Editor-view exception for specific blueprints (universal preview-default per user's "every newly opened note" pick)
