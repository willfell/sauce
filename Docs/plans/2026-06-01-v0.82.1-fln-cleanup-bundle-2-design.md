# v0.82.1 fln-cleanup-bundle-2 — design

**Status:** DRAFT 2026-06-01 (evening session, post-v0.82.0 close).
**Predecessor:** v0.82.0 (callout-types-tables-and-tripwire), commit `6fa0db9` (next-cycle handoff) on `origin/main`.
**Cycle shape:** PATCH cleanup (mirrors v0.80.1 fln-cleanup-bundle precedent).
**Versions at close:** workshop `0.82.0 → 0.82.1`; cowork unchanged at `0.21.0`; no mechanism bumps; CS-MIG-1 counts unchanged (mechanic-only refactor).

---

## 1. Scope

Bundle four pre-existing FLN cleanups, all surfaced through the v0.79.0 → v0.82.0 cycle arc. All four are platform-internal — no new contract surfaces, no install-surface changes, no new claude_surface[] entries.

| FLN | Site | Change |
| --- | --- | --- |
| **v82-2** | `platform/install.js:1303-1374` (`applyRuleFragment`) + `ranch/rules/cowork.json` | Idempotent `(scope.path_glob, extends)` dedup of loaded existing array + incoming push. Natural cleanup of bloated file via S9 dogfood install. |
| **v82-3** | `platform/blueprints/cowork/helpers/context-builder-dry-run.js:35` | Widen flat-key regex from `/^([a-z_]+):/` to `/^([a-z_][a-z0-9_-]*):/` to support hyphenated YAML keys (e.g., `lharries-whatsapp:`, `sharepoint-online:`). |
| **v81-1** | NEW `platform/test/helpers/semver-helper.js` + 3 test files | Extract shared `versionAtLeast(actual, minStr)` helper; refactor `run-claude-surface.js:562`, `run-cowork-smoke.js:435`, `run-helper-cases.js:6880` away from the brittle `/^0\.(8|9|[12]\d)\.\d+$/` regex. |
| **v79-5** | `platform/test/run-claude-surface.js:578-594` (CS-MIG-1 case) | Derive `47 total / 38 skill / 3 command / 6 resolver` counts from `manifest.claude_surface[]` groupBy; remove the four hardcoded magic numbers. |

Out of scope (deferred):
- FLN-v82-1 (engagement-type accessibility) — designated v0.83.0 MINOR per the post-v0.82.0 next-cycle handoff.
- People/products/teams semver-regex sites (`run-helper-cases.js:6702, 6718, 6732`) — outside FLN-v81-1's named scope.
- FLN-v79-1 visual verification of sauce-callouts.css — user-side observation, not a workshop cycle item.

---

## 2. Per-FLN design

### 2.1 FLN-v82-2 — `applyRuleFragment` idempotent dedup

**Problem.** `applyRuleFragment` at `platform/install.js:1303` reads the existing rule file, then unconditionally pushes `frag.fragment` into `existing.contributions[sourceName]`. Every install re-runs every fragment → file grows monotonically. HEAD-of-v0.82.0 has `ranch/rules/cowork.json` at **7,929 lines / 281 entries / ~20 unique path_globs** (~13× duplication). Pre-existing across cycles since at least v0.78.0 — every cycle's S9 dogfood commits the same ~+630-line delta.

**Design.** Make the helper idempotent on the `(scope.path_glob, extends)` key. Two-axis dedup in a single pass:

1. **Load-side dedup:** when reading existing `contributions[sourceName]`, dedupe in-place by `(scope.path_glob, extends)` tuple before any new push.
2. **Push-side dedup:** check the incoming `frag.fragment`'s `(path_glob, extends)` against the dedup'd existing array; only push if not present.
3. **Write back the dedup'd array.**

**Cleanup mechanic.** No separate cleanup commit. When S9's dogfood install runs against today's bloated `ranch/rules/cowork.json`, the load-side dedup naturally shrinks the file to its canonical form. Git diff at S9 shows `ranch/rules/cowork.json: 7929 → ~600 lines deleted` as a side effect of `chore: workshop 0.82.0 → 0.82.1`.

**Key choice rationale.** `(path_glob, extends)` rather than full fragment-body equality because: (a) the observed bloat has identical bodies per (path_glob, extends) group, so the simpler key catches the actual bug; (b) two genuinely-distinct rules at the same path_glob would still be representable by distinct `extends` values; (c) full-body equality is over-paranoid for the surfaced footgun.

**Pre-fix sanity check (S0 task).** Before committing S2, verify that every (path_glob, extends) group in the existing `ranch/rules/cowork.json` has identical bodies. One-liner check; if any group has divergent bodies, escalate to full-body equality key.

### 2.2 FLN-v82-3 — `parseYamlIsh` hyphenated-key support

**Problem.** `context-builder-dry-run.js:35`'s flat-key regex `/^([a-z_]+):\s*(.*)$/` silently drops keys with hyphens. Custom MCP kinds with hyphens — `lharries-whatsapp`, `sharepoint-online`, `microsoft-graph` — can't appear in `user-preferences.md` today. Surfaced during HC-V0820-A4 authoring (had to switch test kind from `my-custom-kind` to `notion` for parser compatibility).

**Design.** Widen the regex to `/^([a-z_][a-z0-9_-]*):\s*(.*)$/`. First char retains the `[a-z_]` constraint (no leading hyphen or digit); remaining chars accept `[a-z0-9_-]`. One-line change.

**Test home.** HC-V0821-B1 lands in `run-helper-cases.js` (the canonical home for cowork-helper unit tests, alongside the existing applyRuleFragment cases). Exercises `_parseYamlIsh` via the `module.exports._parseYamlIsh` accessor already wired at `context-builder-dry-run.js:352`.

**No version bump.** This is a code path inside a helper file. The helper is `require()`'d by both test code (test loads via relative path) and the live skill body (production load path). Neither is gated by cowork's version short-circuit at `install.js:223`. Workshop dogfood at S9 re-materializes the helper if it's listed in cowork's `files[]` (verify at S0). If a hidden version-gate test surfaces, scope expands to cowork 0.21.0 → 0.21.1 — but the v0.79.0/v0.80.0/v0.82.0 precedent is that helper-file changes don't gate.

### 2.3 FLN-v81-1 — semver-helper extraction

**Problem.** Three test files duplicate `/^0\.(8|9|[12]\d)\.\d+$/` to assert cowork version ≥ 0.8.0. Pattern accepts 0.8.x through 0.29.x. Bumping cowork past 0.29.x will require touching all three files — repeat of the v0.81.0 widen-from-0.19.x scramble.

**Design.** Pure refactor — no behavior change, no failing test.

1. **NEW file:** `platform/test/helpers/semver-helper.js`. Exports:
   - `versionAtLeast(actualStr, minStr) → boolean` — parses both as `MAJOR.MINOR.PATCH`, compares numerically. Returns `false` for malformed inputs.
   - `parseSemver(str) → {major, minor, patch} | null` — internal but exported for tests.
2. **Refactor 3 sites** to use `versionAtLeast(m.version, '0.8.0')`:
   - `platform/test/run-claude-surface.js:562`
   - `platform/test/run-cowork-smoke.js:435`
   - `platform/test/run-helper-cases.js:6880`

Assertion messages preserved verbatim. No new test cases added (the helper is exercised indirectly via the three existing assertions).

**Out of scope.** The narrower patterns at `run-helper-cases.js:6702/6718/6732` (people 0.3.x-0.4.x; products 0.2.x-0.3.x; teams 0.2.x-0.3.x). FLN-v81-1's named scope is cowork-specific. Refactoring those is a separate follow-up cleanup.

### 2.4 FLN-v79-5 — CS-MIG-1 manifest-derivation

**Problem.** The CS-MIG-1 case at `run-claude-surface.js:556-606` hardcodes four counts (47 total, 38 skill, 3 command, 6 resolver). Every claude_surface[] addition forces a lockstep bump in this file. v0.79.0/v0.80.0/v0.81.0 each had this bump as a stage chore (v0.81.0's S7 was the most recent: 37→38 skill, 46→47 contributions).

**Design.** Convert hardcoded counts to manifest-derived expectations.

```js
// At top of caseCSMIG1CoworkAggregation()
const cs = bpMan.claude_surface || [];
const expected = {
  skill: cs.filter(e => e.kind === "skill").length,
  command: cs.filter(e => e.kind === "command").length,
  resolver: cs.filter(e => e.kind === "claude_md_row" && e.row && e.row.table === "resolvers").length,
  total: cs.length,
};

// Replace hardcoded counts:
assertEq("CS-MIG-1: contributions match manifest claude_surface[] length",
  out.registry.contributions["cowork"].length, expected.total);
assertEq("CS-MIG-1: skill entries match manifest", skillEntries.length, expected.skill);
assertEq("CS-MIG-1: command entries match manifest", cmdEntries.length, expected.command);
assertEq("CS-MIG-1: resolver rows match manifest", coworkRows.length, expected.resolver);
```

**Sentinel tradeoff (accepted).** Hardcoded counts catch unintentional manifest deletions ("someone accidentally drops a claude_surface[] entry → CI red → review forces verification"). Derived counts only catch aggregator drops, not intentional manifest changes. Mitigation: the existing dest-substitution assertions (lines 587-590) continue to validate that each skill dest starts with `.claude/skills/cowork/`, which catches structural aggregator bugs.

**Assertion message wording.** Drop the hardcoded `"47 contributions (38 skill + 3 command + 6 claude_md_row)"` parenthetical; the manifest is the source of truth now. Keep the case name `caseCSMIG1CoworkAggregation` and the test ID `CS-MIG-1` — this is a mechanic refactor, not a new case.

**CS-MIG-1 count change posture: UNCHANGED** because manifest still has 47 entries. Only the assertion mechanic changes.

---

## 3. Stage map

10 commit-bearing stages (S0 preflight is no-commit) + USER APPROVAL gate at S9.9. Mirrors v0.80.1's S0..S6 cadence, scaled up for 4 FLNs vs. v0.80.1's 5.

| Stage | Kind | Summary |
| --- | --- | --- |
| S0 | (no commit) | Preflight ALL GREEN at version-sync 0.82.0. Verify: (a) cowork.json bloat-group bodies are identical (justifies the `(path_glob, extends)` dedup key); (b) no test gates parseYamlIsh re-material on a cowork version bump. Surface scope-expansion if either check fails. |
| S1 | `test` | HC-V0821-A1 failing test (run-helper-cases.js, `applyRuleFragment` group) — incoming-dup dedup + pre-existing-dup dedup + distinct-preservation. 3 sub-asserts. |
| S2 | `fix` | `install.js:applyRuleFragment` — two-axis dedup on `(scope.path_glob, extends)`. Load-side dedup + push-side check + write-back. |
| S3 | `test` | HC-V0821-B1 failing test (run-helper-cases.js) — parseYamlIsh accepts hyphenated keys at both top level and nested `mcps:` block. 2 sub-asserts. |
| S4 | `fix` | `context-builder-dry-run.js:35` — widen flat-key regex. Re-materialize cowork helper in same stage if listed in cowork's `files[]`. |
| S5 | `refactor` | NEW `platform/test/helpers/semver-helper.js` exporting `versionAtLeast()`. Refactor 3 sites. No new test cases; existing assertions stay green. |
| S6 | `test` | HC-V0821-C1 failing test (run-claude-surface.js, CS-MIG-1 case) — derived counts from manifest.claude_surface[] groupBy. Replaces 4 hardcoded magic numbers. |
| S7 | `refactor` | `caseCSMIG1CoworkAggregation()` — replace hardcoded counts with manifest-derived expectations. Drop hardcoded parenthetical in assertion message. |
| S8 | `chore` | Workshop 0.82.0 → 0.82.1. Bump package.json + platform/manifest.json + ranch/platform-subscription.json (workshop pin). Dogfood install: `node platform/install.js --vault . --auto-approve`. Natural cleanup: `ranch/rules/cowork.json` shrinks 7929 → ~600 lines via new applyRuleFragment dedup. Preflight `version-sync ok: 0.82.1` ALL GREEN. |
| S9 | `docs` | Cycle-close artifacts: result.md + post-v0.82.1 next-cycle-handoff.md + cycle-history.md update + cycle-status.md update + install.md upgrade section + landmines.md (if applicable). |
| **S9.9** | **GATE** | **USER APPROVAL for annotated tag `v0.82.1`.** After approval: push tag, tap PR merge, brew upgrade, `sauce update --bump-pins` on all 4 consumer vaults. |

**Subagent dispatch (per `superpowers:subagent-driven-development`, codified in `Docs/agent-guides/build-test-verify.md`):**
- Test + fix stages (S1, S2, S3, S4, S6, S7): implementer → spec reviewer → code-quality reviewer
- Refactor stage (S5): implementer + reviewers in parallel (low blast radius)
- Chore stage (S8): self-review only (version bumps + dogfood verification)
- Docs stage (S9): spot-check inline (FLN-v801-3 codification — pure-prose changes don't need reviewer dispatch)

**Commit hygiene:**
- Conventional commits: `feat|fix|chore|docs|test|refactor(scope): v0.82.1 SN — summary`
- NO `Co-Authored-By` trailer.
- Explicit `git add <paths>` (never `-A`).
- After any SKILL.md/manifest change, re-materialize and stage regenerated `ranch/` + `.claude/` in the same stage commit.

---

## 4. Risks

| # | Risk | Mitigation |
| --- | --- | --- |
| 1 | parseYamlIsh re-material gated on cowork version bump | S0 verification step; if gating test exists, expand scope to cowork 0.21.0 → 0.21.1 |
| 2 | CS-MIG-1 sentinel loss (derived counts can't catch intentional manifest deletions) | Accepted tradeoff. Existing dest-substitution assertions (lines 587-590) partially substitute as structural sentinel |
| 3 | FLN-v82-2 dedup key over-broad (drops legitimately-distinct fragments with same path_glob + same extends but different body) | S0 verification step confirms all existing duplicate groups have identical bodies. If divergent bodies exist, escalate to full-body equality key |
| 4 | S9 git diff size (~7300 deleted lines from cowork.json) | Explicit commit message line documenting the file shrink + dedup mechanism so future cycle authors reading `git log` don't think entries were accidentally lost |
| 5 | Subscription-pin lockstep forgotten | Preflight `scripts/check-version-sync.js` gates this — failure if package.json / platform/manifest.json / ranch/platform-subscription.json drift |

---

## 5. End state

- Workshop on `v0.82.1`.
- All S1–S9 commits on `origin/main`.
- Preflight `version-sync ok: 0.82.1` ALL GREEN.
- `ranch/rules/cowork.json` cleanly dedup'd (~600 lines from 7929).
- 4 FLN cleanups closed.
- After USER APPROVAL gate at S9.9: annotated tag `v0.82.1`, tap PR merge, brew upgrade, `sauce update --bump-pins` on all 4 consumer vaults.

**Next cycle after v0.82.1 deployment:** v0.83.0 cowork-engagement-type-materialization (FLN-v82-1 option a) — already designated per `Docs/prompts/2026-06-02-post-v0.82.0-next-cycle-handoff.md`.

---

## 6. References

- v0.80.1 result (precedent fln-cleanup-bundle): `Docs/plans/2026-05-30-v0.80.1-flnsbundle-result.md`
- v0.80.1 plan: `Docs/plans/2026-05-30-v0.80.1-flnsbundle-plan.md`
- v0.82.0 result (predecessor cycle): `Docs/plans/2026-06-01-v0.82.0-callout-types-tables-and-tripwire-result.md`
- Post-v0.82.0 next-cycle handoff: `Docs/prompts/2026-06-02-post-v0.82.0-next-cycle-handoff.md`
- Build-test-verify guide: `Docs/agent-guides/build-test-verify.md`
- Code conventions: `Docs/agent-guides/code-conventions.md`
- Landmines: `Docs/landmines.md` (#10, #12, #14, #16 most relevant)
- Cycle-status (live): `Docs/agent-guides/cycle-status.md`
