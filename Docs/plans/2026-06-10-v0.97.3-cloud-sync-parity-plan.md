---
cycle_arc: v0.97.3 cloud-sync-parity
kind: plan
status: SHIPPED 2026-06-10
companion_design: Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-design.md
companion_result: Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-result.md
---

# v0.97.3 cloud-sync-parity — execution plan

> [!info]+ Status
> AUTO-EXECUTE in one Claude Code session. Direct push to main per workshop convention. Push gated to S5 with explicit user approval. SHIPPED 2026-06-10.

## S1 — RED

Add HC-V0973-CSP-1..11 to `platform/test/run-helper-cases.js` immediately after HC-V0972-DP-4 (before the `console.log('========')` final-summary block). All 11 expected to fail today because:

- Cadence files don't reference `{{shared.anti_delegation_clause}}` yet.
- `_shared-clauses.md` doesn't define the new clause headings yet.
- `compose-scheduled-job-wrappers-helper.js` still contains the literal text.

Run `node platform/test/run-helper-cases.js` — verify 2256 passed, 11 failed.

Commit: `test(cowork): v0.97.3 S1 — HC-V0973-CSP RED (11 new sub-asserts)`. Explicit `git add platform/test/run-helper-cases.js`.

## S2 — implement

1. **Add 3 new clauses to `_shared-clauses.md`** (insert BEFORE the existing `## sidecar_schema_template` heading):
    - `## anti_delegation_clause` — verbatim from v0.97.2 JS helper.
    - `## prelude_block` — parameterized via `{{$timezone}}` + `{{$engagement_id}}`.
    - `## done_block` — parameterized via `{{$cadence}}` + `{{$engagement_label}}` + `{{$workshop_version}}` + `{{$cowork_version}}` + `{{$contract_version}}`.

2. **Update all 6 orchestrator-instructions cadence files** — add top references after frontmatter closing `---`, before the `# cowork:<cadence> — orchestrator-instructions` H1:
    ```
    {{shared.anti_delegation_clause}}

    {{shared.prelude_block}}
    ```
    And add bottom reference at the very end of the file:
    ```
    {{shared.done_block}}
    ```

3. **Strip the wrap-with-PRELUDE-and-DONE block from `composeFromOrchestratorInstructions`** in `compose-scheduled-job-wrappers-helper.js`. After the `_substituteStaticTokens(template, tokens)` call, just `return` the result. No more `antiDelegation` / `prelude` / `done` literal strings, no more `${antiDelegation}\n\nYou are running cowork:...` wrapping logic.

4. **Run ALL test suites:**
    - `npm run test:helpers` — expect 2267 GREEN, 0 failed.
    - `node platform/test/run-cowork-smoke.js` — expect 954 GREEN.
    - `node platform/test/run-claude-surface.js` — expect 214 GREEN.
    - `node platform/test/run-integration-smoke.js` — expect 36 GREEN.
    - `node platform/test/run-cli.js` — expect 132 GREEN.
    - `node platform/test/run-bootstrap.js` — expect 85 GREEN.

Commit: `feat(cowork): v0.97.3 S2 — cloud-sync-parity (shared-clause migration of ANTI-DELEGATION + PRELUDE + DONE)`. Explicit `git add` per file.

## S3 — version + dogfood

1. Bump `package.json` `version: "0.97.2"` → `"0.97.3"`.
2. Bump `platform/manifest.json` `workshop_version: "0.97.2"` → `"0.97.3"`, and the `blueprints[].cowork.version: "0.35.2"` → `"0.35.3"`.
3. Bump `platform/blueprints/cowork/manifest.json` `version: "0.35.2"` → `"0.35.3"`.
4. Bump `ranch/platform-subscription.json` `workshop_version: "0.97.2"` → `"0.97.3"`, and `blueprints[].cowork.version: "0.35.2"` → `"0.35.3"`.
5. Roll forward hardcoded version-pin assertions in `platform/test/run-helper-cases.js` (`"0.97.2"` → `"0.97.3"`, `"0.35.2"` → `"0.35.3"`) and `platform/test/run-cowork-smoke.js`.
6. Run `npm run release:preflight` — expect 0 failures.
7. Run `bash scripts/sauce update` against the workshop dogfood. Confirm 8 cowork content overwrites (helper + `_shared-clauses.md` + 6 cadence files) with `.bak` files dropped alongside.
8. Verify `spice/cowork/data/orchestrator-instructions/morning-briefing.md` contains `{{shared.anti_delegation_clause}}` at top, and `_shared-clauses.md` contains `## anti_delegation_clause` heading.

Commit: `chore(workshop): v0.97.3 S3 — version bumps + dogfood install`.

## S4 — cycle docs

1. Write `Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-design.md` (this design doc).
2. Write `Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-plan.md` (this file).
3. Write `Docs/plans/2026-06-10-v0.97.3-cloud-sync-parity-result.md` (post-S5 update, summarizing what shipped).
4. Update `Docs/agent-guides/cycle-status.md` — bump workshop version, cowork blueprint version, mention v0.97.3 codename.
5. Append v0.97.3 entry to `Docs/cycle-history.md`.
6. Update `CLAUDE.md` if any version strings live there.

Commit: `docs(workshop): v0.97.3 S4 — design + plan + result + cycle-status`.

## S5 — push + tag + brew + deploy

1. `git push origin main`
2. `git tag v0.97.3 && git push origin v0.97.3`
3. Watch the brew tap PR via `gh pr list --repo willfell/homebrew-sauce --state open`. Merge with `gh pr merge --repo willfell/homebrew-sauce --squash --auto`.
4. `brew update && brew upgrade sauce`. Verify `sauce --version` reports 0.97.3.
5. Run `sauce update --bump-pins` against the workshop dogfood + headspace consumer + accuris-sauce (if reachable from this machine).
6. Verify each materialized `spice/cowork/data/orchestrator-instructions/morning-briefing.md` contains `{{shared.anti_delegation_clause}}` at top AND `_shared-clauses.md` contains the 3 new heading blocks.

(No commit — push + tag + brew + deploy operations only.)

## Verification checklist (must pass before complete)

- [x] HC-V0973-CSP-1..11 all GREEN
- [x] HC-V0972-AD-* and HC-V0972-DP-* still GREEN
- [x] All other test suites GREEN
- [x] Workshop tag v0.97.3 pushed
- [x] Brew tap PR merged
- [x] `sauce --version` reports 0.97.3 on this machine
- [x] Reachable vaults' orchestrator-instructions/*.md contain `{{shared.anti_delegation_clause}}` reference at top
- [x] `_shared-clauses.md` in reachable vaults contains the three new `## anti_delegation_clause` / `## prelude_block` / `## done_block` blocks
- [x] composeFromOrchestratorInstructions still produces wrappers containing literal `ANTI-DELEGATION (NON-NEGOTIABLE)` text (proves substitution still works)
