# v0.68.0 cowork-orchestrator-cohesion — result

**Date closed:** 2026-05-20
**Workshop:** 0.67.4 → 0.68.0 MINOR
**Cowork blueprint:** 0.10.2 → 0.11.0 MINOR
**Driver:** v0.65.0's atomic-note write contract was correct, but the 5 per-job orchestrator SKILL.md `description:` + opening prose still described the v0.45.0 callout-patching pattern. Claude in Cowork formed a legacy mental model from those surfaces top-down and improvised the legacy implementation — output landed at `Timestamps/Summary/...` (accuris-specific legacy path) and as a `Teams Briefing` callout on the daily note instead of the contract path `spice/cowork/daily/.../YYYY-MM-DD/morning-briefing.md`.

## What shipped

**S1 — Orchestrator description rewrites** (commit `f674dc2`)
All 5 per-job orchestrator SKILL.md files (`morning-briefing`, `midday-tripwire`, `eod-review`, `weekly-review`, `monthly-review`) had both their YAML `description:` field AND opening prose paragraph rewritten to:
- Explicitly name the atomic-note contract path per orchestrator
- Name the `prompt_source` path
- Disclaim: "This orchestrator NEVER patches the daily note's callouts, edits the daily-note template, or writes to legacy paths. The v0.65.0 atomic-note write contract is the only output surface."

Step bodies were already correct from v0.65.0 — only the description + intro surfaces lied.

**S2 — patch-daily-callouts orphan deletion** (commit `39f336d`)
v0.65.0 S3.8 deleted 7 dirs but missed `platform/blueprints/cowork/skills/skills/patch-daily-callouts/`. Removed the directory + its `claude_surface[]` entry from `cowork/manifest.json` + the row in `About Cowork.md`'s skills catalogue. Cleaned up `midday-tripwire/SKILL.md` step 6 which referenced `patch-daily-callouts` for a dedupe-read (dedupe is implicit in the v0.65.0 deterministic-path overwrite contract — the step is now a no-op acknowledgment).

**S3 — 15 engagement-type default prompts** (commit `e54a5b0`)
3 engagement types × 5 orchestrators = 15 NEW files at `platform/blueprints/cowork/content/context/engagement-templates/<engagement.type>/prompts/<orch>.md`. Each describes the atomic-note BODY content only (H1 + H2-per-aspect + bullets). Forbidden phrases (`callout`/`patch`/`daily note`/`Timestamps/`/`append to`/`into today`/`into the daily`/`summaries/`) all absent. Per-engagement-type render_aspects respected — personal has the full 5; w2-fte has morning/eod/weekly real + midday/monthly placeholders (default_cadences false for both); consulting has morning/eod/weekly/monthly real + midday placeholder. Manifest `files[]` += 15 entries.

**S4 — Onboard option (c) contract-guard** (commit `22742ff`)
`cowork:onboard-scheduled-jobs/SKILL.md` step 6(c) "tell me what this should emit" interactive flow now scans the user's description for legacy-surface substrings (case-insensitive: `callout`, `patch`, `daily note`, `Timestamps/`, `append to`, `into today`, `into the daily`, `summaries/`). On match, the skill explains the v0.65.0 atomic-note contract, offers to draft a body that captures the same CONTENT but strips legacy-surface references, and re-prompts on refusal. Edit-loop also re-runs the guard before writing.

**S5 — CoworkLatestRuns helper + Cowork.md embed** (commit `7c2c135`)
NEW `platform/blueprints/cowork/helpers/cowork-latest-runs.js` (~110 LOC). Renders 5 rows (one per orchestrator). Each row shows: orchestrator label, last-run timestamp, ~200 char body preview (stripped of leading YAML frontmatter, whitespace-collapsed). Whole row clickable to open the atomic note. Embedded in `Cowork.md` directly under the existing `CoworkReadiness` panel via a dataviewjs block.

**S6 — Version lockstep** (commit `7c2c135` + re-apply commit `7c2c135`)
- `cowork@manifest.json` version 0.10.2 → 0.11.0 MINOR; `customjs_classes[]` += `CoworkLatestRuns`; `files[]` += helper
- `platform/manifest.json` workshop_version 0.67.4 → 0.68.0; cowork catalogue entry → 0.11.0; entity-create catalogue 0.3.2 → 0.4.0 (catalogue lockstep with mechanism manifest from commit `b90969c`); scratch catalogue 0.4.1 → 0.5.0 (catalogue lockstep with blueprint manifest)
- `package.json` 0.67.4 → 0.68.0
- `ranch/platform-subscription.json` workshop_version → 0.68.0; entity-create → 0.4.0; scratch → 0.5.0; NEW cowork@0.11.0 subscribed in workshop dogfood

**S7 — Cross-vault contamination investigation** (no code changes)
The user observed `Talkiatry, Toyota, Yuki, Xfinity` (headspace personal-finance data dated 2026-04-14) in accuris's `spice/cowork/context/weekly-snapshot.md`. **Investigation result:** the `update-weekly-snapshot` sub-skill computes its target path as `<vault_path>/spice/cowork/context/weekly-snapshot.md` where `vault_path` is an input from the orchestrator — no cross-vault-write code path exists. The contamination is data hygiene, not a current code bug; most likely a pre-v0.65 manual edit or one-time mis-routed write. No FLN filed; cleanup is a one-line user action (overwrite the file content via re-bootstrap or hand-edit).

**S8 — Test harness deltas** (commit `6ad3b84`)
- `run-cowork-smoke.js` NEW `assertCoworkV068Shape` — +91 sub-asserts across V068-DESC-{5 orchs}, V068-ORPHAN-{1,2,3}, V068-DEFAULTS-{3×5+CLEAN}, V068-DEFAULTS-MANIFEST-{15}, V068-ONBOARD-GUARD{,-MSG}, V068-LATEST-RUNS-{1..4+TYPES×5}, V068-MANIFEST-FILES, V068-VERSION. Total: 287 → 378.
- `run-helper-cases.js` NEW `caseCOWORKV068LatestRunsLints` — +10 sub-asserts: file exists, no trailing whitespace, no tab/space mix, class declared, queries each of the 5 cowork-* run-note types, async render(dv). Total: 936 → 946.

**S9 — Whole-suite preflight** (commit `8ebf649`)
Surfaced 2 stale baselines unrelated to v0.68 substantive scope but blocking the release. Both fixed:
- entity-create catalogue 0.3.2 → 0.4.0 (cause of scratch silently skipping at install, breaking integration-smoke's `smoke-ec-registry-has-subscribed-entries`)
- `run-claude-surface` CS-MIG-1 baseline 33 → 32 skill entries / 42 → 41 contributions (lockstep with S2 deletion)

Preflight exit 0. 22 harnesses pass.

**S10 — Dogfood + tag + push** (commit `9a3a8d8`)
Workshop self-install clean (61 history entries; exit 0). Materialized artifacts confirmed: `ranch/scripts/cowork/cowork-latest-runs.js`; `spice/cowork/Cowork.md` embeds `CoworkLatestRuns` block; 15 engagement-templates prompts materialized.

## Files touched (summary)

| Category | Count | Notes |
|---|---|---|
| Orchestrator SKILL.md rewrites | 5 | description + intro paragraph (step bodies untouched) |
| Deleted skills | 1 | `patch-daily-callouts/SKILL.md` + claude_surface entry + About Cowork.md row |
| NEW engagement-type default prompts | 15 | 3 types × 5 orchestrators |
| NEW helper | 1 | `cowork-latest-runs.js` (~110 LOC) |
| Onboard SKILL.md guard | 1 | step 6(c) contract-guard |
| Manifest bumps | 4 | cowork 0.10.2→0.11.0; workshop 0.67.4→0.68.0; entity-create 0.3.2→0.4.0; scratch 0.4.1→0.5.0 |
| Test harness deltas | 2 | run-cowork-smoke +91; run-helper-cases +10 |

## FIX-LATER-NOTEs

- **FLN-v68-1** — accuris-sauce `spice/cowork/context/weekly-snapshot.md` contains stale headspace personal-finance data from 2026-04-14 (pre-v0.65 contamination; no code bug). User to clean by re-running `cowork:bootstrap-vault` against accuris OR hand-editing the file.
- **FLN-v68-2** — Workshop self-subscription was drifted 0.67.3 + missing many newer mechanism/blueprint versions; brought up to 0.68.0 baseline in S10. Future cycles should bump the workshop subscription alongside the catalogue at S6/S7.
- **FLN-v68-3** — Edit-tool revert observed mid-cycle: S6's manifest + Cowork.md + package.json edits were applied via Edit but reverted between Edit and commit; re-applied in commit `7c2c135` (no hooks present in `.git/hooks`, so cause unknown — possibly external tooling). Worth investigating if it recurs.

## What unblocks for the user

After `brew upgrade sauce` + reinstall on each consumer vault:
1. **The atomic-note contract is enforced top-down.** Future scheduled morning-briefing fires read `description:` first, see the contract, and write to the contract path.
2. **Option (a) of `cowork:onboard-scheduled-jobs` works.** Users get a sauce-shipped default prompt body per (engagement.type × orchestrator) pair.
3. **Option (c) of onboard refuses legacy-surface phrasing** and rephrases the draft toward the contract.
4. **Cowork.md gains a live `Latest cowork runs` panel** showing the most recent atomic note per orchestrator with body preview.

## Next: user re-runs onboard on accuris (and other vaults)

After this cycle ships, user re-runs `cowork:onboard-scheduled-jobs` from Claude Cowork against each vault, accepting the new sauce-default option (a) prompts. The next scheduled morning-briefing fire after that should land at the contract path and surface in Daily Hub / SpaceDailyDashboard / Cowork.md Latest-Runs panel.
