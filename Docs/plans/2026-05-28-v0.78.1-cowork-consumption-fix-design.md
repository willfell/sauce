# v0.78.1 — cowork-consumption-fix (design)

**Status:** design — pending plan + execution.
**Date:** 2026-05-28.
**Cycle topic slug:** `cowork-consumption-fix`.
**Predecessor:** v0.78.0 (cowork-consumption), tag `v0.78.0`, HEAD `02fce88`.
**Trigger:** v0.78.0 shipped and deployed to all 4 consumer vaults. First real cron-fire on accuris (the same vault that triggered v0.78.0) produced a morning-briefing.md that the agent autonomously labeled as `Dispatch mode = legacy with prefs-aware composition`. The agent's stated reason: "The prefs-mode helper at `.local/blueprints/cowork/helpers/dispatch-plan-helper.js` is not materialized in the vault, so `read-user-preferences` cannot return `status: 'ok'`." Two separate but related issues exposed:

1. **Helper-primary contract trap.** The v0.78.0 SKILL.md files for `read-user-preferences` and the 5 orchestrator step 3c blocks tell the agent to "delegate to the helper at `.local/blueprints/cowork/helpers/<name>-helper.js`." That path NEVER exists in consumer vaults — the v0.78.0 helpers live only in the workshop dev repo at `platform/blueprints/cowork/helpers/`. Cowork helpers that DO materialize (cowork-readiness.js, cowork-daily-hub-cards.js, etc.) land at `ranch/scripts/cowork/<name>.js` via cowork manifest's `files[]` entries — but those are CustomJS view helpers consumed by Dataview, not agent-side parsers, and the v0.78.0 helpers were intentionally helpers-for-harness-only. The trap is that the SKILL.md wording implies the helper is the primary path; when it's missing, the agent treats prefs-status as undeterminable and falls back to legacy mode.

2. **No URL contract in gather-from-served-by output.** The v0.78.0 output contract said "Aim for ≤200 words. Bulleted lines preferred (no tables — cross-vendor portability). Honor what_matters as the gather contract." Nothing required URLs. The accuris run's GitHub section came back as prose-only — no links to PRs/issues — making the briefing much less actionable than it could be (the user reads in Obsidian where inline markdown links open in browser).

v0.78.1 closes both issues with SKILL.md prose-only edits. No new helpers, no new manifest entries, no schema changes.

---

## 1. Cycle motivation

Two coupled workstreams against a single patch release (v0.78.1).

**Workstream A — invert SKILL.md primary/secondary contract.** Rewrite `read-user-preferences/SKILL.md` and the 5 orchestrator step 3c blocks (morning-briefing / midday-tripwire / eod-review / weekly-review / monthly-review) so the agent-side algorithm is the PRIMARY documented behavior. The helper-mention moves to a new `## Harness testing` section that explicitly notes the helper is NOT materialized into consumer vaults and production agents execute the algorithm directly. The pseudocode in step 3c becomes literal in-prose dispatch rules with explicit `kind_title` derivation, explicit `bookkeeping` field stripping, explicit voice-contract composition.

**Workstream B — mandatory URL contract in gather-from-served-by output.** Add a `## Source URL requirements` section to `gather-from-served-by/SKILL.md`. Mandates inline markdown links to source URLs when the served_by MCP exposes URL-shaped fields. Per-kind MUST/SHOULD discipline: github + ado + email = MUST; calendar + chat + finance = SHOULD; custom kinds = SHOULD. Includes per-kind URL-shape examples. Fallback for genuinely-no-URL runs is an italicized "_did not expose URL fields_" note line.

Together: A makes prefs-mode actually engage on cron-fire (closes the accuris legacy-fallback bug); B makes briefings actionable (closes the no-PR-links gap).

---

## 2. Out of scope (explicitly deferred)

| Item | Why deferred |
|---|---|
| Observable dispatch-mode Notice (announcing prefs vs legacy at end of step 3c) | Considered but rejected — debug-only feature; adds noise to every run output. Re-evaluate if future failures recur. |
| SKILL.md lint harness as a separate cycle workstream | Tracked separately as the check-claude-surface harness candidate in cycle-status. v0.78.1 adds 2 targeted SKILL.md lints (HC-V0781-A1, B1) but does not generalize. |
| Helper materialization to consumer vaults | Wrong shape — agent doesn't shell out to Node helpers; the JS files don't help unless the SKILL.md says "Bash: `node ...`" which is awkward and slow. |
| Helper-level URL validator (dry-run mode) | Can't statically know which URL fields a MCP exposed in a real session. URL contract is a SKILL.md prose discipline; static lint asserts the contract is documented (HC-V0781-B1) but cannot validate agent compliance per-run. |
| Adding new kinds to the catalog (drive / code-platform / project-tracker / monitoring) | Carry-forward from v0.78.0 candidates. Custom-kind path covers these for now. |
| Cross-vault preferences inheritance, kind_title schema field, personality hard rules | All v0.79.0+ candidates per v0.78.0 design doc. |
| Re-materializing the 4 v0.78.0 helpers via files[] | Same wrong-shape reasoning as above. They stay in `platform/blueprints/cowork/helpers/` for harness use only. |

---

## 3. Workstream A — invert SKILL.md primary/secondary contract

### 3.1 `read-user-preferences/SKILL.md` rewrite

Replace the existing `## Steps` section (currently 3 steps that say "Delegate to the helper… The helper does X…") with the following 8-step agent-side algorithm:

````markdown
## Steps

1. **Read the user-preferences.md file.** Use the Read tool at `spice/cowork/context/user-preferences.md` (or `mcp__obsidian__get_file_contents` with that vault-relative path). If the file does not exist, return `{ prefs: null, status: "empty", reason: "file_not_found" }`.

2. **Extract the leading frontmatter block.** Match the YAML between leading `---` and the next `---` markers. If no frontmatter block is present, return `{ prefs: null, status: "malformed", reason: "no_frontmatter_block" }`.

3. **Parse the YAML frontmatter.** Treat as YAML 1.1 with the indentation conventions context-builder writes. On parse error, return `{ prefs: null, status: "malformed", reason: "yaml_parse_error: <error message>" }`.

4. **Assert the type tag.** If `type` is missing or not equal to `cowork-user-preferences`, return `{ prefs: null, status: "malformed", reason: "type_tag_missing_or_wrong" }`.

5. **Apply v1→v2 migration (idempotent).** If `mcps.gmail` is present, rename it to `mcps.email`. If `mcps.imessage` is present, rename it to `mcps.chat`. If both source and destination already exist, prefer the destination (don't overwrite). This step is a no-op for v2-shaped prefs (already-migrated).

6. **Apply seed-shape rule.** If after migration `priorities` is `[]` AND `mcps` is `{}`, return `{ prefs: null, status: "empty", reason: "unpopulated_seed" }`. This is the workshop-seed shape that install.js writes — it means context-builder has not been run yet.

7. **Coerce optional defaults.**
   - `personality.{vibe, formality, pep_talk, length, notes}` default to `null` when absent.
   - For each `mcps[<kind>]`: `connected` defaults to `false`, `custom_kind` defaults to `false`, `override_classified` defaults to `false`. Preserve every other field on the mcps entry verbatim (kind-specific answer fields like `vip_senders`, `surface_event_kinds`, `inner_circle`, `inner_circle_channels`, `what_matters` etc.).

8. **Return `{ prefs, status: "ok" }`.** Canonical shape per Outputs section above.

This skill is a PURE read — never modify the file, never write to disk, never call any MCP that mutates state. The orchestrator's dispatch planning depends on the canonical output shape.

## Harness testing

A helper at `platform/blueprints/cowork/helpers/read-user-preferences-helper.js` lives in the workshop dev repo (NOT materialized into consumer vaults) and exports `readUserPreferences({ vaultRoot })` for the HC-V0780-A* harness cases. Production agents in consumer vaults execute the algorithm above directly — they do NOT depend on the helper file existing.
````

### 3.2 Orchestrator step 3c rewrite (5 files: morning + midday + eod + weekly + monthly)

Replace each orchestrator's current step 3c body (which says "Invoke the helper at `.local/blueprints/cowork/helpers/dispatch-plan-helper.js`'s `planDispatch(...)`") with explicit in-prose dispatch rules:

````markdown
3c. **Plan dispatch.** Determine dispatch mode and build the priority-ordered dispatch plan.

   Compute `dispatch_mode`:

   ```
   dispatch_mode = (prefs_result.status === "ok") ? "prefs" : "legacy"
   ```

   When `dispatch_mode == "legacy"`:
   - Emit Obsidian Notice: `cowork:<this-orchestrator> -- user-preferences <prefs_result.status> (<prefs_result.reason>); using engagement-template defaults`.
   - Skip the remainder of step 3c. The legacy gather sequence (existing v0.77.0 gather steps) fires unchanged.

   When `dispatch_mode == "prefs"`:

   1. From `check-vault-routing`'s prior result (pre-flight step 1), capture `reachable_namespaces` as the set of MCP namespace segments the agent has tools for in this session. Extract by walking your tool list: for every `mcp__<ns>__<tool>` name, add `<ns>` to the set.

   2. Read `mcp-skill-map.json` from `spice/cowork/context/mcp-skill-map.json` via the Read tool. Capture as `mcp_skill_map`. (The map is materialized into every consumer vault as a `files[]` entry.)

   3. Build `dispatch_plan[]` as an ordered array. For each `kind_name` in `prefs.priorities` (in order):

      ```
      mcps_entry = prefs.mcps[kind_name]    # may be undefined

      # Compute kind_title
      if kind_name in {calendar, email, chat, finance}:
          kind_title = canonical lookup ("Calendar", "Email", "Chat", "Finance")
      else:
          kind_title = title-case of kind_name (whitespace/underscore-split; "ado" -> "Ado", "github" -> "Github", "monitoring" -> "Monitoring")

      # Determine action
      if mcps_entry is undefined:
          push { kind_name, action: "warn", reason: "not_classified", kind_title, mcps_entry: null }
          continue
      if mcps_entry.connected == false:
          push { kind_name, action: "warn", reason: "not_connected", kind_title, mcps_entry }
          continue
      if mcps_entry.served_by is set and not in reachable_namespaces:
          push { kind_name, action: "warn", reason: "served_by_unreachable", kind_title, mcps_entry }
          continue
      if mcps_entry.custom_kind == true OR mcps_entry.override_classified == true:
          bookkeeping = {served_by, what_matters, connected, captured_at, custom_kind, override_classified}
          question_set_answers = {k: v for k, v in mcps_entry if k not in bookkeeping}
          push {
            kind_name,
            action: "gather_from_served_by",
            served_by: mcps_entry.served_by,
            what_matters: mcps_entry.what_matters or "",
            question_set_answers: mcps_entry.custom_kind ? null : (question_set_answers if non-empty else null),
            kind_title,
            mcps_entry,
          }
          continue
      # Default: known canonical-vendor kind (kind_name in mcp_skill_map.kinds[].kind)
      if any entry in mcp_skill_map.kinds has .kind == kind_name:
          push {
            kind_name,
            action: "gather_canonical",
            gather_skill: <that entry>.gather_skill,
            kind_title,
            mcps_entry,
          }
      else:
          # Rare: kind name not recognized and not flagged custom — treat as gather_from_served_by
          push { kind_name, action: "gather_from_served_by", served_by, what_matters, question_set_answers: null, kind_title, mcps_entry }
      ```

   4. Capture `voice_contract` from `prefs.personality`: if every field (`vibe`, `formality`, `pep_talk`, `length`, `notes`) is null/undefined, `voice_contract = ""`. Otherwise compose:

      ```
      Voice contract (from spice/cowork/context/user-preferences.md):
      - Vibe: <prefs.personality.vibe or "default">
      - Formality: <prefs.personality.formality or "default">
      - Pep talk: <"yes" if prefs.personality.pep_talk else "no">
      - Length: <prefs.personality.length or "default">
      - Notes: <prefs.personality.notes verbatim, collapsed to single line>

      Apply this voice ONLY to narrative sections (frontmatter summary, [!info]- synopsis, [!tip] closing). Do NOT apply to tabular [!example]+ blocks (their content comes from gather sub-skills and is contractually shaped).

      ---

      ```

This step is PURE — no MCP calls, no file writes. It builds in-memory state used by the gather phase.
````

Each orchestrator gets a `## Harness testing` appendix:

> A helper at `platform/blueprints/cowork/helpers/dispatch-plan-helper.js` exports `planDispatch`, `decideDispatchMode`, `composeVoiceContract`, `composeWarningCallout` for the HC-V0780-C* / D* harness cases. Production agents in consumer vaults execute step 3c's algorithm directly — they do NOT depend on the helper file existing.

The `cowork:<this-orchestrator>` token in the Notice text gets substituted per orchestrator: `cowork:morning-briefing`, `cowork:midday-tripwire`, `cowork:eod-review`, `cowork:weekly-review`, `cowork:monthly-review`. No other per-orchestrator differences in step 3c's content — the per-orchestrator default range table (set in v0.78.0 design) still lives in each orchestrator's gather-phase, not in step 3c.

---

## 4. Workstream B — Source URL contract in gather-from-served-by

Insert a new `## Source URL requirements` section into `gather-from-served-by/SKILL.md` between the existing `## Steps` and `## Dry-run mode` sections:

````markdown
## Source URL requirements

The output `markdown` MUST include source URLs when the `served_by` MCP exposes them. The user reads briefings in Obsidian where inline markdown links open in the browser; URL-less prose is much less useful than a one-click jump to the source.

**How to find URLs in tool results:** when invoking tools from `available_tools`, inspect each return value for URL-shaped fields. Common field names across MCPs:

- `url`, `web_url`, `webUrl`, `webLink`, `html_url`, `htmlUrl`
- `permalink`, `permalink_url`
- `_links.html.href`, `_links.web.href` (HAL-style)
- `link`, `href`

If a tool result includes any of these, surface the URL inline in your `[!example]+ <kind_title>` callout as `**[<short-label>](<url>)**: <1-line context>`.

**Per-kind expected URL discipline:**

| kind_name | Strictness | Expected URL shape |
|---|---|---|
| `github` | **MUST** | `https://github.com/<owner>/<repo>/pull/<n>` or `/issues/<n>` per PR/issue bullet — `html_url` on GitHub API responses |
| `ado` | **MUST** | Work-item URL per ticket bullet — typically `https://dev.azure.com/<org>/<proj>/_workitems/edit/<id>`; surfaced as `_links.html.href` or `url` |
| `email` | **MUST** | Message permalink per surfaced thread — `webLink` field on the message resource (Outlook/M365 Graph) |
| `calendar` | **SHOULD** | Outlook web event link — `webLink` on the event resource (omit only when MCP genuinely lacks one) |
| `chat` | **SHOULD** | Teams chat/channel message permalink — `webUrl` on the message resource (omit only when MCP genuinely lacks one) |
| `finance` | **SHOULD** | Receipt/statement URL when exposed by the MCP |
| any custom kind beyond the above | **SHOULD** | Any URL field present in tool results |

**If a MUST-kind gather returns markdown with NO URLs:** that's a contract violation. Re-inspect tool results, verify whether the MCP genuinely exposes no URL field (the answer is almost always no for github/ado/email — they all expose URLs). Either include the URLs OR emit a brief explanatory line at the bottom of the callout: `> _Note: <served_by> did not expose URL fields on <tool_name> in this run._`

**Inline-link formatting examples:**

```
> [!example]+ Github
> - **[PR 234: Add NSP rules for hpcc-prod](https://github.com/accuris/internal-platform/pull/234)**: Hayden requested your review (filed Wed 16:42); 3 files touched, no test changes
> - **[PR 247: Decommission old document-registry-stage](https://github.com/accuris/internal-platform/pull/247)**: yours, awaiting Stefan; merged-into-target branch already
> - **[Issue 89: Bom Reporting CSV export](https://github.com/accuris/bom-reporting/issues/89)**: re-opened by Ying yesterday; you're on the assignees
```

```
> [!example]+ Ado
> - **[705679 — Document Registry Stage cutover bake step](https://dev.azure.com/accuris/EPD/_workitems/edit/705679)**: yours; moved to Active Tue; linked to feature 698094
> - **[704521 — Azure NSP rules round 5](https://dev.azure.com/accuris/EPD/_workitems/edit/704521)**: Hayden's; closed Wed 16:30
```

This URL requirement applies regardless of dispatch path. It binds the agent's gather output; the dry-run helper validates the markdown's structural shape but cannot validate URL presence (it can't know what URLs the MCP would have exposed in a real session).
````

The dry-run helper's existing validation rules (callout-prefix, no top-level backticks, length floor) are NOT changed. URL discipline is a SKILL.md prose contract; HC-V0781-B1 asserts the prose is documented but cannot validate per-run agent compliance.

---

## 5. Failure-mode matrix (delta from v0.78.0)

| Failure | v0.78.0 behavior | v0.78.1 behavior |
|---|---|---|
| `user-preferences.md` populated correctly, helper file absent | Agent treats helper-missing as undeterminable → legacy fallback (BUG) | Agent reads file directly, parses, returns `status: "ok"` → prefs-mode (FIXED) |
| `user-preferences.md` malformed YAML | Same as above bug + agent might emit confusing error | Agent returns `status: "malformed"` with parse-error reason → legacy fallback (correct) |
| Github gather returns prose without URLs | Acceptable per output contract (BUG — useless to user) | Contract violation when github is MUST kind; agent must include URLs or emit the fallback note (FIXED) |
| ADO gather returns prose without URLs | Same as above | Same fix as github |
| Email gather returns prose without URLs | Same | Same fix |
| Calendar/chat gather returns prose without URLs | Acceptable | SHOULD-kind — acceptable when MCP lacks URL fields; still preferred to include |
| Custom-kind gather without URLs | Acceptable | SHOULD — acceptable; agent included URLs when possible |

---

## 6. Test plan — HC-V0781-* cases (2 new cases in `platform/test/run-cowork-smoke.js`)

### HC-V0781-A1 — Agent-side algorithm primary, helper harness-only

Static SKILL.md prose lint. Asserts:

- `read-user-preferences/SKILL.md` does NOT contain `Delegate to the helper at \`.local/blueprints` (would re-introduce the bug)
- `read-user-preferences/SKILL.md` DOES contain `Read the user-preferences.md file` (agent-side algorithm documented)
- `read-user-preferences/SKILL.md` DOES contain v1→v2 migration prose
- `read-user-preferences/SKILL.md` DOES contain `## Harness testing` section
- `read-user-preferences/SKILL.md` DOES contain `NOT materialized into consumer vaults` explicit note
- For each of the 5 orchestrators (morning-briefing / midday-tripwire / eod-review / weekly-review / monthly-review):
  - SKILL.md does NOT contain `Invoke the helper at \`.local/blueprints`
  - SKILL.md DOES contain `reachable_namespaces` or `tool list.*mcp__` prose (documents extraction)
  - SKILL.md DOES contain `## Harness testing` section

### HC-V0781-B1 — gather-from-served-by has URL contract

Static SKILL.md prose lint on `gather-from-served-by/SKILL.md`. Asserts:

- Contains `## Source URL requirements` section header
- Github / ado / email each appear with **MUST** strictness
- Calendar / chat each appear with **SHOULD** strictness
- Contains at least one inline-link formatting example matching `**[<text>](http(s)://...)**`
- Documents the `did not expose URL` fallback note pattern

### Existing-harness preservation

`HC-V0780-*` cases (read-user-preferences parser, gather-from-served-by dry-run, dispatch planning, legacy fallback, body composition) — unchanged. v0.78.0 helpers stay in `platform/blueprints/cowork/helpers/` and remain test-callable.
`HC-V0760-A1` (user-preferences.md preservation across reinstall) — unchanged.
v0.77.0 / v0.77.0 carry-overs — unchanged.

### Manual verification (post-deploy)

1. `npm run release:preflight` → ALL GREEN with 2 new HC-V0781-* cases passing
2. `sauce reinstall` against workshop self-install vault — no regressions
3. After tag + tap merge + brew upgrade + `sauce update --bump-pins` on consumer vaults: re-fire `cowork:morning-briefing` against accuris
4. Expected: agent reads user-preferences.md directly; status: ok; dispatch_mode: prefs (NOT legacy); 5 `[!example]+` blocks in chat → ado → github → calendar → email order; GitHub bullets contain inline markdown links to `https://github.com/...`; ADO bullets link to `https://dev.azure.com/...`
5. After accuris validates: user repeats scheduled-jobs setup on headspace vault using the same prompt pattern

---

## 7. Stage plan preview (4 stages — finalized in plan doc)

| Stage | Description |
|---|---|
| **S0** | Preflight: clean tree at v0.78.0 HEAD `02fce88`; ALL GREEN baseline; tag `v0.78.0` exists |
| **S1** | Failing HC-V0781-A1 + B1 in `platform/test/run-cowork-smoke.js` (TDD-red) |
| **S2** | Rewrite the 7 SKILL.md files (read-user-preferences + 5 orchestrator step 3c + gather-from-served-by URL section). Re-materialize via `node platform/test/run-install.js`. Tests pass. Preflight ALL GREEN |
| **S3** | Version bumps + cycle-close: workshop `0.78.0` → `0.78.1`; cowork stays `0.17.0` (SKILL.md prose changes only — no manifest schema change, no new helpers). cycle-history + cycle-status + handoff doc + TAG approval gate |

Patch cycle, 4 stages, ~1 hour wall time end-to-end.

---

## 8. References

- v0.78.0 design: `Docs/plans/2026-05-27-v0.78.0-cowork-consumption-design.md` (commit `5c58fff`)
- v0.78.0 plan: `Docs/plans/2026-05-27-v0.78.0-cowork-consumption-plan.md` (commit `52d4c21`)
- v0.78.0 result: `Docs/plans/2026-05-27-v0.78.0-cowork-consumption-result.md` (commit `02fce88`)
- Accuris 2026-05-28 morning-briefing run — forcing function (in conversation transcript)
- v0.78.0 carry-forward candidates list in `Docs/agent-guides/cycle-status.md` § In-flight / next-candidate queue
