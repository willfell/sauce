# Post-v0.80.0 handoff (next cycle)

**Closed cycle:** v0.80.0 cowork-sibling-files (closed 2026-05-30).
**Result + stage log:** `Docs/plans/2026-05-30-v0.80.0-cowork-sibling-files-result.md`.
**Design:** `Docs/plans/2026-05-30-v0.80.0-cowork-sibling-files-design.md`.
**Plan:** `Docs/plans/2026-05-30-v0.80.0-cowork-sibling-files-plan.md`.

## Start here

v0.80.0 closes the third resolution path in `cowork:edit-microscope`'s gap-finding triad (the `user-supplied` placeholder from v0.79.0). The new convention: any markdown file in `spice/cowork/prompts/per-mcp/<kind>/` except `microscope.md` and `_*.md` is a USER-owned context surface; the 5 atomic-note orchestrators discover them via new step 2c (glob + filter); `cowork:gather-from-served-by` injects each verbatim into the dispatch contract under `**User-supplied reference: <name>**` blocks (after `<what_matters verbatim>`, before `**Hard rules ...**`); `cowork:edit-microscope` step 4 user-supplied branch scaffolds new siblings inline via the new `composeSibling` helper (gap-text-keyed column heuristic) and writes a `## References` paragraph into `microscope.md` via the extended `composeMicroscope`.

Workshop `0.79.0 → 0.80.0`. cowork blueprint `0.18.0 → 0.19.0` MINOR. ~43 HC-V0800-* sub-asserts in run-cowork-smoke.js + 3 in run-install.js (F1). Cowork-smoke final tally: **651 passed / 0 failed**. Preflight `version-sync ok: 0.80.0` ALL GREEN at S12 close. CS-MIG-1 cowork counts unchanged (no new claude_surface[] entry — composeSibling is helper-only).

The user mental model post-v0.80.0: "you set up the kind (`/cowork preferences`), then you deepen it (`/cowork microscope <kind>`) — and as gaps surface, the skill scaffolds USER-owned sibling files alongside the microscope. The next orchestrator run reads them automatically." The capture loop is still where all the value lives; v0.80.0 made the loop's output reach deeper into the gather without typed parsers (opaque-injection model preserved; carry-forward §3 in case agent compliance proves insufficient).

## Carry-forward queue

### From v0.80.0 design §11 (sibling-file extensions still deferred)

1. **Interactive editing of EXISTING sibling content** — v0.80.0 scaffolds new siblings only; existing ones the user hand-edits.
2. **Dedicated `/cowork sibling <kind> <name>` skill** — decoupled from edit-microscope.
3. **Typed sibling parsers + deterministic pre-agent substitution** — vs. v0.80.0's opaque-injection model.
4. **Hard-rule-style post-processor for sibling-application compliance** — mechanical verification that every phone number maps to a `contacts-map.md` entry.
5. **Sibling-file versioning / changelog** — `_*.md` escape is the closest thing today.
6. **Multiple-microscope-per-kind** — cadence-specific (`microscope-morning.md` / `microscope-eod.md`).
7. **Soft-limit warning for very large sibling files** — >50KB injected whole today.
8. **Sibling-file-aware reverse audit** — verify `## References` entries point to extant files.

### From v0.79.0 carry-forward (still open as v0.81+ candidates)

The v0.79.0 handoff §11 list — items 2 (WhatsApp MCP-swap follow-through), 3 (`samples/` sibling dir), 4 (per-kind microscope versioning), 5 (hard-rule post-processor — overlaps with v0.80.0 §4), 6 (expanded known-kinds catalog), 7 (cross-vault `inherits_from`), 8 (canonical-gather × microscope hybrid). v0.79.0 carry-forward item 1 (`contacts-map.md` sibling-file convention) is **CLOSED in v0.80.0**.

### Cycle-specific FLNs surfaced during v0.80.0 execution

- **FLN-v80-1: HC-V0800-A1 glob-prose regex over-strict.** The plan's A1 regex required the literal substring `per-mcp/<kind_name>/*.md` in the orchestrator's step 2c prose; the plan's verbatim prose split that into two phrases (`per-mcp/<kind_name>/` + separately "matching `*.md`"). Fixed inline at S7 by amending the prose to "files matching the `per-mcp/<kind_name>/*.md` glob" — semantically identical, satisfies the regex. Carry-forward: either soften the regex (allow split phrasing) or codify the literal-glob phrasing in the plan-template HC skeleton so future cycles avoid the same friction.
- **FLN-v80-2: landmine #16 less load-bearing than v0.79.0 close documented.** Per-stage `node platform/test/run-install.js . --auto-approve` did regenerate cowork SKILL.md dests under `.claude/skills/cowork/*` at every install in this cycle — the per-item version short-circuit does NOT prevent dest catch-up for surfaces processed by `materializeClaudeSurface`. Future plans can stop predicting "deferred to S11" for SKILL.md surface drift; the global aggregate step keeps them current. (May still be true for files[] surfaces — the cowork-prompts catch-up was bundled into S11's full re-process anyway via the subscription pin bump.)
- **FLN-v80-3: result-doc structure stability.** v0.79.0 + v0.80.0 result docs follow the same structure (what shipped → HC cases → version moves → stages → observations → carry-forward → cycle-specific FLNs). Worth codifying in a result-doc template skill if cycle-close documentation continues at this volume.

### Still-open FLNs from prior cycles (carried forward as-is)

- **FLN-v79-1 (urgent): VISUAL verification of WS-C (sauce-callouts.css)** still owed by user.
- **FLN-v79-2: `install.js` has no CLI handler.** Use `node platform/test/run-install.js . --auto-approve` for dogfood. Either sweep the docs or add CLI arg parsing to install.js. v0.80.0 used the harness wrapper exclusively.
- **FLN-v79-3: gather-from-served-by 80-char `agent_markdown` floor** as a documented trap in plan-template HC skeleton.
- **FLN-v79-4: orchestrator-vs-sub-skill dest convention not codified.**
- **FLN-v79-5: CS-MIG-1 hardcoded counts** require lockstep bumps when cowork claude_surface[] entries are added. v0.80.0 added none → counts unchanged.
- **FLN-v79-6: `classifyGap` resolution heuristics are tight.** Expand regex lists as new MCPs surface.

## Lateral context the next chat will want

- **The opaque-injection thesis (carry-forward of v0.79.0's hard-rules thesis).** v0.80.0 sibling-file injection mirrors v0.79.0's `hard_rules[]` model: the agent reads the sibling body as part of the dispatch contract and applies it per the microscope.md narrative. No typed parser, no pre-resolution. The bet is that prompt-level trust + microscope.md's explicit narrative ("Use `contacts-map.md` to resolve sender phone numbers …") is sufficient. If compliance drift emerges in practice, design §11 item 4 (hard-rule-style post-processor) is the YAGNI escape valve.
- **The "where all value is" thesis (preserved).** v0.80.0 didn't change `cowork:edit-microscope`'s capture-loop UX (the "one question at a time" + consent-gated sample steps); it just extended what the loop can produce. The capture-loop UX remains the load-bearing surface for the next round of polish.
- **The microscope-routing trade-off** (microscope existence forces served-by routing) is unchanged. Sibling files INJECT into that served-by routing; they don't change which routing path applies.

## Quick test-runner cheatsheet

- Smoke (cowork): `node platform/test/run-cowork-smoke.js` — **651 passed / 0 failed** at S12 close.
- Install harness (incl. F1): `node platform/test/run-install.js` (unit mode, no positional arg). With a positional arg + `--auto-approve`, runs a real install against the named vault.
- Full preflight: `npm run release:preflight` — ALL GREEN at S12 close, `version-sync ok: 0.80.0`.
- Workshop dogfood install: `node platform/test/run-install.js . --auto-approve` (NOT `node platform/install.js`; see FLN-v79-2).
- Quick siblings-feature spot-check: `grep -c "Read per-kind sibling files" platform/blueprints/cowork/skills/orchestrators/*/SKILL.md` → 5 (one per orchestrator).
