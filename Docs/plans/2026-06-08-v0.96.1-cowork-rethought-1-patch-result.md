---
cycle_arc: v0.96.1 cowork-rethought-1-patch
kind: result
status: SHIPPED 2026-06-08
companion_docs:
  - Docs/plans/2026-06-08-v0.96.1-cowork-rethought-1-patch-design.md
  - Docs/plans/2026-06-08-v0.96.1-cowork-rethought-1-patch-plan.md
versions_at_close:
  workshop: "0.96.0 → 0.96.1"
  cowork_blueprint: "0.34.0 → 0.34.1"
  scheduled_job_contract: "0.34.0 → 0.34.1"
  mechanism_count: 17 (unchanged)
---

# v0.96.1 cowork-rethought-1-patch — result

## Stage-by-stage outcomes

| Stage | Status | Highlights |
|---|---|---|
| S0 | DONE | Baseline confirmed post-v0.96.0; preflight GREEN |
| S1.1 | DONE | 15 HC RED (9 Rail M + 6 Rail H); 3 smoke RED; 6 fixtures |
| S1.2 | DONE | _normalizeLearnedWeights + composeFinalPreferences nested read + learned_weights-skeleton data file |
| S1.3 | DONE | learn-from-checks SKILL.md Steps 3-4 rewritten; engagement-mismatch guard retired; renumbered 12→11 steps |
| S2.1 | DONE | check-heartbeat-helper + cadence-freshness-windows + check-heartbeat sub-skill |
| S2.2 | DONE | synthesize-day post-step wired to check-heartbeat (non-fatal) |
| S3 | DONE | Cross-cutting verification; all green |
| S4 | DONE | Version bumps + cycle-history + result doc + tag (gated) |

## Key implementation decisions

1. **Lazy migration on read** (Rail M) — no installer changes needed. `_normalizeLearnedWeights` runs at learn-from-checks read time; first post-patch fire migrates user-preferences.md transparently.
2. **Backwards-compat fallback in composeFinalPreferences** — when `engagement_id` is not provided AND top-level `per_kind` exists, read it directly. Preserves v0.96.0 HC-V0960-L-14/L-17 GREEN (those tests don't pass `engagement_id`).
3. **Sidecar-based heartbeat** (Rail H) — no dedicated heartbeat.json write surface. Sidecars already carry `generated_at` per fire; check-heartbeat just walks them.
4. **CADENCE_BY_TYPE mapping** uses `lens-shift` (hyphen) as the canonical key for cold MB, matching HC-V0961-H-3 expected shape.

## HC sub-assert final counts

| Group | Count | Status |
|---|---|---|
| HC-V0961-M-* | 9 | GREEN (multi-engagement learned_weights nesting) |
| HC-V0961-H-* | 6 | GREEN (cron heartbeat) |
| **Total v0.96.1** | **15 new** | All GREEN |

## Cycle artifacts

| Artifact | Value |
|---|---|
| Workshop version | 0.96.0 → 0.96.1 PATCH |
| Cowork blueprint version | 0.34.0 → 0.34.1 PATCH |
| scheduled-job-contract version | 0.34.0 → 0.34.1 (landmine #20) |
| Mechanism count | unchanged (17) |
| New skills (sub-skill) | 1 (cowork:check-heartbeat) |
| New helpers | 1 (check-heartbeat-helper.js) |
| New data files | 2 (cadence-freshness-windows.json, learned_weights-skeleton.json) |
| Stage count | 8 (S0, S1.1, S1.2, S1.3, S2.1, S2.2, S3, S4) |
| Cron prompts changed | 0 — no consumer cron updates needed |

## Push / deploy posture

This cycle is **closed locally** at HEAD with v0.96.1 tag pending. Push to origin + tag publish is **gated awaiting user approval** per sauce convention.

When pushed:
1. `git push origin main`
2. `git push origin v0.96.1`
3. brew tap PR auto-opens via release.yml → merge → `brew upgrade sauce`
4. Re-deploy consumer vaults: `sauce update --bump-pins` on accuris + headspace
5. **NO** `/cowork sync-scheduled-jobs` re-run needed (cron prompts unchanged)
