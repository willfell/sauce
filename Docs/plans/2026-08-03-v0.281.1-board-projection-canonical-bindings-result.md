# Board-projection canonical bindings — result

**Closed:** 2026-08-03 · **Version:** workshop 0.281.0 → 0.281.1

## What shipped

The deferred follow-up to v0.281.0: the `ero-egnyte-mcp` parent board froze
because every projection threw and the throw was swallowed into
`projection_error`, while the coordinator kept advancing phases in the ledger.
Two independent root causes, both reproduced live before any fix, plus the
reusable repair for boards already broken.

**Files touched:**

- `.agents/skills/card-intake/scripts/card-intake.js` — `physicalProjectPrefix`
  (mirror of the coordinator's, `''`-on-non-canonical instead of throwing),
  `projectPrefix` derives from the on-disk cards root first, `parentBoardRef`
  supplies the canonical parent-board reference to `renderEpicAtlas` and
  `renderCard`.
- `scripts/autoloop/codex-coordinator.js` — `owningEpicBoardPath` + the
  `discardCardCore` board-target fallback; `canonicalEpicBindings`,
  `planEpicBindingHeal`, `commandHealEpicBindings` plus CLI wiring
  (`STRICT_CLI_OPTIONS`, dispatch, usage, exports).
- `Docs/agent-guides/delivery-board.md` — the new verb under § Coordinator
  operations.

## Root cause

**Mode 1 — mints born un-projectable.** `card-intake`'s `projectPrefix` derived
the vault-relative project prefix from *caller input* (`spec.source_board`),
collapsing to `''` for the absolute `board_path` the intake skill actually
passes. Atlases got an absolute `source_board`/`kanban_board` and a
project-relative `epic_board`; slices got a project-relative
`task_parent`/`source_board`. None satisfy `canonicalWorkspacePath`, so every
projection threw `does not bind its canonical parent board`.

**Mode 2 — discard orphans its own line.** `discardCardCore` took its board
target only from a *successful* `canonicalEpicProjection`. Any refusal (including
a Mode-1 atlas) fell back to the parent board, which carries no slice lines — so
it removed nothing and unlinked the note anyway. The surviving sub-board line
then made every later projection of that epic throw `epic slice <X> note is
missing`. Mode 1 manufactures Mode 2; each orphan then poisons the epic
independently.

## Process

systematic-debugging → TDD. Mode 1 was **verified live, not assumed**: a
reproduction minted a fresh epic through the real intake rail into a canonical
vault shape using the skill's exact arguments and ran the real
`canonicalEpicProjection` against the result — absolute `source_board`,
project-relative backlinks, `PROJECTION THREW`. A read-only sweep of all 63
epics on the live board then confirmed the cascade (the 2 orphaned sub-board
lines sat in the same epic as the absolute atlas), and a cross-vault scan found
the same rot in `ero-sauce/planner-agent-integration` and
`headspace-sauce/spice/projects/sauce` — which is why the heal shipped as a
reusable verb rather than a one-off script.

All three increments were driven red → green with the failure watched:

1. Intake bindings — 6 assertions red (each wrong field, plus the projection
   throw), then green.
2. Discard orphan — red on `discard targets the slice's OWN epic sub-board`,
   then green.
3. `heal-epic-bindings` — red on `commandHealEpicBindings is not a function`,
   then green.

## Tests

- `run-codex-autoloop` — **2618** assertions, 0 failed (was 2591): `BPX-ORPHAN`
  (7) + `BPX-HEAL` (20).
- `run-card-intake` — **256** passed, 0 failed (was 249): `BPX-CANONICAL-BINDINGS`.
- `npm run release:preflight` — GREEN end to end.
- `npm run release:preflight-bumped` — GREEN on a clean tree.

No existing test was weakened. The intake fallback for non-canonical roots is
what keeps the existing `run-card-intake` fixtures (a tmp `cards_root` outside
`spice/projects` with a relative `source_board`) green unchanged.

## Deploy + heal

Tag `v0.281.1` → tap PR #529 → `brew upgrade sauce` → **0.281.1** installed.
`scripts/autoloop/deploy.js run` deployed all three vaults; each verified at
`0.281.1`.

The heal ran through the **installed** (brew Cellar) coordinator, per project,
dry-run → snapshot → apply → replay. Before/after, measured by running the
installed `canonicalEpicProjection` against every slice:

| Vault / epic | Before | After |
| --- | --- | --- |
| `ero-egnyte-mcp` / Config Truth and Guarded Deploys | 0/6 | **6/6** |
| `ero-egnyte-mcp` / Build Observability and Metrics | 0/6 | **6/6** |
| `ero-egnyte-mcp` / Toolset Value and Answer Quality | 0/11 | **11/11** |
| `planner-agent-integration` / CI Gates for the IaC Repo | 0/5 | **5/5** |
| `headspace-sauce/sauce` / GraphView Frontier Lens | 0/5 | **5/5** |
| `headspace-sauce/sauce` / Loop Ops | 0/13 | **11/13** ¹ |

29 notes rewritten (3 atlases + 26 slices). **All 29 bodies byte-identical** —
verified by diffing the pre-apply snapshots with frontmatter stripped. Replay of
`--apply` on every project returned `no_op: true`. A full re-sweep across all
three vaults reports zero remaining drift. `accuris-sauce` was clean throughout.

Note the poisoning this quantifies: `Loop Ops` was 0/13 from a **single** slice
with a project-relative `task_parent`, and `Toolset Value` 0/11 from three. One
bad binding freezes an entire epic.

¹ The two `Loop Ops` stragglers are a different, pre-existing defect — `GA-OPS18c`
and `GA-OPS18g` are superseded predecessors whose notes were never deleted while
the live tail `GA-OPS18h` sits on the board. That is **tombstone residue**, which
the existing `reap` verb heals and `status --json` already reports. It is not a
freeze: all 11 actual board members project.

### Concurrent-writer handling

`headspace-sauce` had a live loop session writing its board (last write 22 min
before the heal). It was held back and applied only after confirming **zero file
overlap** between the heal's 7 targets (`GraphView Frontier Lens`, `Loop Ops`)
and the session's 4 recently-written files (`GraphView Blocking Lens`,
`GraphView Visual Polish`). `ero-sauce` was quiet for 68 minutes before its
apply. Pre-apply snapshots of every target were taken in both cases.

## Released as a patch, not a minor (process lesson)

This cycle adds a new coordinator CLI verb, which is semantically a **minor**.
`npm run release:plan` on the branch agreed — it read the branch's two commits
(`feat(coordinator)` + `fix(intake)`) and reported `v0.282.0 (minor)`.

It shipped as **v0.281.1 (patch)**. PRs land by squash-merge, so the branch
collapsed into one commit whose subject is the **PR title** — `fix(loop): …` —
and that single `fix` is the only conventional commit the bumper saw. The
`feat(coordinator)` type was discarded at merge. `release:plan` cannot catch
this: run locally it reports the pre-squash answer.

Not corrected after the fact — hand-tagging or re-versioning to "fix" a computed
release is explicitly off-limits. The durable fix is the new rule in
[build-test-verify.md](../agent-guides/build-test-verify.md) § What Claude does:
**the PR title must carry the highest commit type on the branch.**

## Carry-forward

- **`run-sticky-notes-render-guards.js` is flaky on `main`.** Measured on an
  unmodified base tree: **3 of 6 runs failed** with `PERF-7-HARNESS:
  asynchronous fixtures did not finish`. It is inside `release:preflight`, so it
  can wedge `prepare-release` on merge. Deliberately not fixed here (this cycle
  stays isolated to the control plane). Needs its own cycle: the harness's async
  fixture completion is racing rather than awaited.
- **Concurrent vault writers vs. the heal.** The live `ero-egnyte-mcp` board was
  being actively mutated by another session *during* this cycle (dozens of notes
  rewritten mid-investigation; the `Build Observability` atlas and its two orphan
  lines were repaired by that other writer between the diagnosis and the heal
  dry-run). `heal-epic-bindings` takes the selector lock, but a non-coordinator
  writer (a live Codex session, Obsidian Sync from another machine) is outside
  that boundary. Running the heal against a board with a live loop session is
  unsafe; there is currently no cross-process guard for that.
- **`canonicalEpicSurface` now refuses legacy epics.** With `route.board_ref`
  vault-relative, intake's direct-execution path refuses to route into an epic
  whose atlas still carries a project-relative `epic_board`. That is correct
  (it prevents minting more slices into an un-projectable epic) and the heal is
  the remedy, but it means un-healed vaults will see intake refusals rather than
  silent breakage.

## Commits

Branch `fix/board-projection-canonical-bindings`: `fix(intake)` (derivation +
tests), `feat(coordinator)` (discard fallback + heal verb + tests), `docs`
(design/plan/result + delivery-board). Release, tag, and tap: automatic.
