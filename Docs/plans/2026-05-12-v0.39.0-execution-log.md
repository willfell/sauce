# v0.39.0 — Projects Org-Chart Foundation: Execution Log

Append-only log of stage closures, FIX-LATER-NOTE candidates surfaced by reviewers, and concerns informing future cycles. One section per stage. Updated by the executing agent after each stage closes (implementer DONE + spec ✅ + quality APPROVED + commits pushed).

---

## S1 — `products@0.1.0` scaffold (CLOSED 2026-05-12)

**Commits (6):**
- `8e38432` feat(products): v0.39.0 S1.1 — products@0.1.0 manifest skeleton
- `87af495` feat(products): v0.39.0 S1.2 — Products.md hub content
- `a1b2a7d` feat(products): v0.39.0 S1.3 — ProductsHubCards customjs class
- `ebd00a1` feat(products): v0.39.0 S1.4 — /products command + new-product SKILL.md
- `f295107` feat(products): v0.39.0 S1.5 — register products@0.1.0 in platform manifest
- `58de975` test(products): v0.39.0 S1.6 — +3 helper-cases for products install coverage

**Harness deltas:** `run-helper-cases.js` 532 → 553 sub-asserts (+21 from PROD-1/PROD-2/PROD-3: 8 + 7 + 6). All other harnesses unchanged. `npm run release:preflight` exit 0.

**Implementer adaptations:**
- Plan-illustrated `freshFixtureVault / installPlatform / runValidator` test-harness helpers don't exist in actual code; implementer translated to the modern convention: `scaffoldBlueprintVault(scratch, [...])` + `runHarness(scratch)` against a `_fake-workshop` fixture, asserting on the actual aggregator-registry shapes (`registry.contributions["products"]`, `rule.contributions.products[0]`) rather than the plan's illustrative flat-shape readers. Same invariants exercised.

**FIX-LATER-NOTE candidates from S1 reviewers (deferred to natural-home stages):**
1. **`nav-buttons` ICONS map missing `package`.** Manifest declares `"icon": "package"` per plan; nav-buttons mechanism's icon dict at `space-nav-buttons.js:62-76` has no `package` SVG, so the products-hub navbar entry renders the fallback letter glyph ("P") instead of an SVG. Plan-faithful but inconsistent with other hub icons. Home: S3 (teams will also need a new icon) or S8 (per-product/per-team rollups stage). May warrant an additive `nav-buttons@2.6.0 → 2.7.0` patch bundling both new icons together.
2. **`ProductsHubCards` date sort posture.** `dates.array().sort().pop()` on values derived from `status_changed_at || created || "1970-01-01"` is safe with string-only dates but theoretically fragile if Obsidian's YAML auto-parses dates to Date/Luxon objects (CLAUDE.md gotcha-in-waiting). Home: S5 — once `status_changed_at` ships as a real frontmatter field, exercise + coerce via explicit `.toString()` or `dv.func.dateformat` to be type-stable.
3. **Optional PROD-4 case for `claude_md_row` aggregation.** PROD-1 spots `kind: command` + `kind: skill` materialization; coverage of the row-aggregation behavior is delegated to existing `run-claude-surface.js`. Not S1-blocking; small consistency polish if desired. Home: S3 (mirror in teams' helper-cases coverage) or skip — `run-claude-surface.js` carries the load.
4. **`enriched.find(...)` inside `meta:` callback is O(n) per card → O(n²) total render.** Fine at sauce vault scales (~5-20 products); convert to `Map` keyed on `file.path` if vaults scale up. Home: S8 (per-product rollup reuses similar enrichment logic — natural refactor window).
5. **Test-harness `mkdtemp` prefix `beacon-prod{1,2,3}-`.** Consistent with rest of `run-helper-cases.js` (only the CLI's mkdtemp was renamed `beacon- → sauce-` in v0.36.1). If a vault-wide sweep is desired, do it as a standalone PATCH cycle rather than folding into v0.39.0.

**Stage close-out: PROCEED to S2.**

---

## S2 — `products` finish (CLOSED 2026-05-12)

**Commits (4):**
- `07f8b2f` feat(products): v0.39.0 S2.1 — Template, Product.md
- `af3220c` feat(products): v0.39.0 S2.2 — ProductActionButtons + New Product
- `2793b99` feat(products): v0.39.0 S2.3 — ProductPageCards stub (full rollup in S8)
- `eddb303` test(products): v0.39.0 S2.4 — +2 validator-green/red cases for Product notes

**Harness deltas:** `run-helper-cases.js` 554 → 556 (+2 PROD-4 green + PROD-5 red). `npm run release:preflight` exit 0.

**Implementer adaptations:**
- `customJS.BeaconAccentButton` (plan name, wrong) → `customJS.AccentButton` (actual class). Confirmed via `grep "^class " platform/mechanisms/accent-button/accent-button.js`.
- Plan's illustrative `freshFixtureVault / installPlatform / runValidator` → `_ruleRunner.applyRules(fragments, record, { workshopRoot })` mirroring existing HC-PR-1/2 cases. Same engine the in-vault validator + CLI auditor invoke; production-equivalent coverage.

**FIX-LATER-NOTE candidates from S2 reviewers:**
1. **Coordinated `{{views_path}}` + `{{templates_path}}` + `spice/{{module_directory}}` placeholder migration.** Templater templates + accent-button-driven new-X buttons across people / meetings / finance / products / (future) teams all hardcode `ranch/templates/`, `ranch/views/`, `spice/<dir>/` rather than using placeholders. `install.js:817` already runs `substituteLenient` on `files[]` source-text, so placeholders would work. Each blueprint matched its own existing precedent; a future PATCH cycle could flip them all in lockstep. NOT blocking; consistency-preserving where it is.
2. **PROD-5 violation-count tightening.** Test uses `.some(v => ...)` existence check. Could pin `violations.length === 1` to catch future regressions where a second rule accidentally fires on the same red record. Matches HC-PR-2 stylistic precedent; FIX-LATER if violation pinning becomes the new pattern.
3. **NewPersonButton header comment misquotes landmine #19** (#19 is lowercase-dir-names, not hardcode-paths). Fix in a future people-blueprint cleanup PATCH; S2 correctly did NOT propagate the misquote into product-action-buttons.
4. **Template, Product.md doesn't use `tp.file.move`** (relies on `templater_folder_templates` + button's explicit folder arg). Works for the 2 real entry points; manual-template-apply from elsewhere would leave the note where Templater was invoked. People template DOES emit `tp.file.move` (mixed convention across blueprints).
5. **Templater spacing convention** `<% name %>` (with spaces) confirmed as the dominant sauce convention; plan's `<%name%>` was the outlier. S2 used the dominant style.

**Stage close-out: PROCEED to S3.**

---

<!-- Future stages will append here. -->
