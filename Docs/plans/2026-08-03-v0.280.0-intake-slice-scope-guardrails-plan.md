# Intake slice-scope guardrails — plan

**Cycle:** v0.280.0 · **Date:** 2026-08-03

## Files

1. `platform/mechanisms/delivery/data/delivery-schema.json`
   - Add `policies.slice_scope_caps = { touch_zones: 6, acceptance_tests: 12,
     risk_dimensions: 3, carried_findings: 12 }` beside the existing policy
     knobs.

2. `.agents/skills/card-intake/scripts/card-intake.js`
   - Read `SLICE_SCOPE_CAPS` from `delivery.registry.policies.slice_scope_caps`
     (defensive `|| {}` so an absent policy is a no-op).
   - `validateCard`: after the existing non-empty lower-bound loop, add the
     `slice_scope_ceiling` upper-bound loop over `touch_zones`,
     `acceptance_tests`, `risk_dimensions` (each capped only if a numeric cap
     exists). Reads the raw card field (pre-normalization), documented inline.
   - `validateSupersede`: after the carried-findings non-empty check, add the
     `carried_findings_ceiling` check.

3. `platform/test/run-card-intake.js`
   - Four over-cap refusals (touch_zones 7, acceptance_tests 13,
     risk_dimensions 4, carried_findings 13 on a supersede) + one at-the-cap
     positive (touch_zones 6, acceptance_tests 12, risk_dimensions 3) proving a
     legitimate bounded slice still mints.

## Verification

- `node platform/test/run-card-intake.js` — green (new asserts pass, none
  removed).
- `node platform/test/run-delivery-contract.js` — green (registry still valid).
- `node scripts/lint-schemas.js` — clean (new policy key accepted).
- `npm run release:preflight` — green end to end.
- `npm run release:preflight-bumped` — green on a clean tree (no release wedge).

## Ordering

Schema first (so the registry exposes the caps), then the JS checks that read
them, then the tests. Thresholds are additive and default-safe, so no migration
and no change to any existing refusal.

## Out of scope

Guardrail #2 (supersession-depth circuit-breaker) — deferred; see design +
result. Ship the mint-time ceilings alone.
