---
date: 2026-05-07
cycle: v0.24.0
status: DESIGN
predecessor_result: Docs/plans/2026-05-06-v0.23.0-sauce-rebrand-result.md
predecessor_handoff: Docs/prompts/2026-05-06-post-v0.23.0-next-cycle-handoff.md
target_artifact: Docs/plans/2026-05-07-v0.24.0-tree3-and-accent-button-plan.md
required_sub_skill: de:writing-plans
---

# v0.24.0 Design — Tree 3 runtime rename + `beacon-button` → `accent-button`

> [!abstract] Summary
> Tree 3 runtime plumbing rename: `<vault>/Docs/Meta/` → `<vault>/ranch/`. Stub body hardcode-rewrite (line 11 `Docs/Meta/platform-config.json` → `ranch/platform-config.json`); new md5 invariant rolls; landmine #13 wording amended. `beacon-button` mechanism → `accent-button` (`BeaconButton` class → `AccentButton`); 12+ call-sites migrated. Manual `mv` + `sed` recipe in install.md upgrade section. 3-stage shape mirroring v0.23.0. MINOR bump (BREAKING for v0.23.x consumers; one-time migration documented).

---

## Locks captured at brainstorm

| Decision | Choice |
|---|---|
| Cycle | v0.24.0 PRIMARY (Tree 3 + beacon-button rename) |
| Local workshop dev path rename | Standalone v0.23.x housekeeping (NOT bundled) |
| beacon-button new name | `accent-button` / `AccentButton` |
| Stub body approach | Hardcode `ranch/platform-config.json` (single body change at v0.24.0; new md5 invariant) |
| Migration story | Manual `mv` + `sed` recipe in `Docs/install.md`; no CLI verb |
| Cycle shape | 3 stages (S1 TDD-RED → S2 atomic rename → S3 close) |

---

## Architecture / scope

### Tree 3 — `Docs/Meta/` → `ranch/`

Consumer's runtime plumbing dir hosts:
- `platform-config.json`, `platform-subscription.json`, `platform-installed.json`, `nav-buttons-registry.json`, `bootstrap-last-install.log`
- 6 subdirs: `Templater/`, `Templates/`, `Scripts/`, `Views/`, `rules/`, `Content/`

Path variables in every consumer's `platform-config.json:variables`:
- `views_path: "Docs/Meta/Views"` → `"ranch/Views"`
- `templater_scripts_path: "Docs/Meta/Templater"` → `"ranch/Templater"`
- `scripts_path: "Docs/Meta/Scripts"` → `"ranch/Scripts"`
- `rules_path: "Docs/Meta/rules"` → `"ranch/rules"`
- `templates_path: "Docs/Meta/Templates"` → `"ranch/Templates"`

Stub body line 11 `"Docs/Meta/platform-config.json"` → `"ranch/platform-config.json"`. **New md5 invariant** computed at S2; landmine #13 amended to "body changes only on Tree 3 rename cycles".

Workshop self-install dogfood: `<workshop>/Docs/Meta/` dir → `<workshop>/ranch/`. All 4 stub copies move (`platform/installer-stub.js` is the canonical source; the other 3 are workshop self-install + 2 consumer dogfoods at `barebones-beacon-poc` + `accuris-beacon-poc`).

### `beacon-button` → `accent-button`

- `platform/mechanisms/beacon-button/` → `platform/mechanisms/accent-button/` (`git mv`)
- Class file `beacon-button.js` → `accent-button.js`; class symbol `BeaconButton` → `AccentButton`
- 12+ call-sites: `customJS.BeaconButton.render(...)` → `customJS.AccentButton.render(...)` across:
  - `platform/blueprints/project/helpers/project-nav-buttons.js` (4 sites)
  - `platform/blueprints/project/helpers/project-workstream-manager.js` (2 sites)
  - `platform/blueprints/finance/helpers/{new-budget-button,new-paycheck-button,new-invoice-button}.js` (3 sites)
  - `platform/blueprints/finance/helpers/{budget-categories-editor,paycheck-expenses-editor,invoice-time-log-editor}.js` (3 sites)
  - `platform/blueprints/finance/helpers/invoice-controls.js` (2 sites — Save + Submit)
  - `platform/blueprints/meetings/helpers/new-meeting-button.js` (1 site)
- Manifest entries: `mechanisms[]` `beacon-button` → `accent-button` (workshop manifest + consumer subscriptions lockstep)
- Renderer harness (`run-renderer.js`): `BB1`-`BB6` selector `beacon-button` → `accent-button`; mock `customJS.BeaconButton` → `customJS.AccentButton`
- `makeFinanceCustomJsStub` BeaconButton stub → AccentButton stub (unblocks FF4-FF8)

### Migration recipe (added to `Docs/install.md`)

```
# v0.23.x → v0.24.0 upgrade
cd <vault>
mv Docs/Meta ranch
sed -i '' 's|Docs/Meta|ranch|g' ranch/platform-config.json
# In Obsidian: Cmd+R, then run the platform install command
```

---

## Cycle shape (3 stages)

| Stage | What | Subagent shape |
|:----:|---|---|
| **S1** | TDD harness RED. Update fixture configs (`platform/test/fixtures/...`), harness expectations in run-bootstrap / run-cli / run-install-sh / run-helper-cases for `ranch/` paths + `accent-button` / `AccentButton`. Renderer BB1-BB6 selector update. Verify failures pre-S2. | Master-driven (mechanical sed across harness fixtures) |
| **S2** | Atomic mass rename. (a) `git mv platform/mechanisms/beacon-button → accent-button` + class file rename + class symbol rename + 12+ call-sites; (b) workshop manifest mechanism entry update + dogfood subscription; (c) stub body line 11 update + new md5 computed; (d) `Docs/Meta/` → `ranch/` rename across workshop self-install + `barebones-beacon-poc` + `accuris-beacon-poc`; (e) docs (`install.md`, `use.md`, `how.md`, `landmines.md`, `CLAUDE.md`) updated. **Two-stage subagent review** (spec, then quality) before commit. | Subagent dispatch for docs rebrand (single subagent: install.md + use.md + how.md + landmines.md). Master-driven for renames + stub body + harness fixtures. |
| **S3** | Cycle close. `workshop_version` 0.23.0 → 0.24.0 MINOR. Manual smoke on fresh `<vault>/scratch/6/` curl-bootstrap (acceptance gate: `<vault>/ranch/` populated + Daily Notes "Open today's daily note" works). CLAUDE.md status snapshot append. Result writeup. Tag `v0.24.0` annotated + push (REQUIRES user approval). Next-cycle handoff doc. | Master-driven |

---

## Harness extensions

No new test cases. Updates to existing fixtures + selectors only:
- `run-bootstrap.js`: BS fixtures `Docs/Meta/...` → `ranch/...`
- `run-cli.js`: any `Docs/Meta/` literal in CLI verb expectations
- `run-install-sh.js`: install.sh expectations (literal paths)
- `run-helper-cases.js`: HK/CP/VT/AP/SS path-variable references
- `run-renderer.js`: BB1-BB6 selector + stub map

Total harness count UNCHANGED at 387/0 + 23 renderer cases. Literal strings update.

---

## Risk + reserved-headroom

- **Wide-touching mechanical-rename** — same shape as v0.23.0. Reserved-headroom precedent: USED 3 review-surfaced CFs in v0.23.0 (10 data points). Plan for **2-4 review-surfaced CFs** caught pre-commit by two-stage subagent review.
- **sed regex word-boundary discipline (v0.23.0 lesson b)** —
  - `s|beacon-button|accent-button|g` is safe (compound word; no false-matching `beacon-cards` or `BeaconButton`).
  - `s|BeaconButton|AccentButton|g` is safe (PascalCase; no other matches expected).
  - `s|Docs/Meta|ranch|g` carries risk against unrelated `Docs/Meta-XXX` patterns — verify with grep before sed; constrain to platform-config.json variable values + manifest dest paths.
- **`.beacon-backup` suffix preserved (v0.23.0 lesson a)** — UNCHANGED. Backup file naming is platform-version-stable.
- **Stub body change is a one-time event** — landmine #13 wording amended in S2 commit; new md5 documented in CLAUDE.md status snapshot. Future cycles do NOT touch stub body.
- **Module-directory invariant (#11) UNCHANGED** — `beacon/<module>/` namespace stays. Tree 2 rename queues for v0.25.0.
- **Allowlist (#12) — 11 paths UNCHANGED** — migration changes path *prefixes* (`Docs/Meta/...` → `ranch/...`) but doesn't introduce new managed-write surfaces.

---

## Acceptance gate (S3)

Fresh `<vault>/scratch/6/` curl-bootstrap with v0.24.0 install.sh. Pass criteria:
- `<vault>/ranch/` populated (no `<vault>/Docs/Meta/`)
- All 8 mechanisms + 8 blueprints subscribed (idempotent re-run)
- `sauce status` reports "Drift: none"
- Daily Notes "Open today's daily note" creates `<vault>/beacon/daily/YYYY/MM-MMMM/<DayOfWeek>-YYYY-MM-DD.md`
- Banner reads `Sauce · v0.24.0`

---

## Unresolved questions

- None at design close. Plan stage will inventory exact call-sites + literal strings; any surprises surface there.
